# GENERATED by `just pmcp-train-config <env>` from deploy.toml.template.
#
# Never committed: it carries the resolved resource names, and the artifact
# bucket's name embeds the AWS ACCOUNT ID (buckets are globally unique, so the
# stack suffixes it). That is not a secret, but it is an account identifier with
# no business travelling to a public upstream repo — and this tree is destined
# for one.
#
# The template is the tracked artifact; regenerate this from the deployed stack.
deploy.toml

# cargo-pmcp's own per-deployment state.
deployment.toml
active-target
