//! `QualityBlock` assembly: per-row predictions -> the EVAL-01 metric set (05-08).
//!
//! Implementation lands with this task's GREEN commit.

#[cfg(test)]
#[path = "bench_metrics_tests.rs"]
mod bench_metrics_tests;
