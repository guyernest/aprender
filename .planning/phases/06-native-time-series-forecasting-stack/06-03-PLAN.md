---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - contracts/prophet-parity-v1.yaml
  - crates/aprender-forecast/src/prophet.rs
autonomous: true
requirements: [SC2]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Prophet port reproduces Python Prophet 1.4.0 on ALL SEVEN committed fixtures (peyton_manning, air_passengers, retail_sales from spike 001; peyton_default, peyton_holidays, wp_log_R_logistic, air_multiplicative from spike 003) as --lib tests CI's nextest leg runs without a workflow edit (D-04, SC2)"
    - "Every rung is its own test — data prep, objective at Python's MAP, Python's params through Rust predict, fitted objective vs Python's, future forecast vs Prophet's own optimizer band, components rebuilding yhat, 80 % band widths — so a regression names the rung, not the fixture (CONTEXT: keep the ladder shape)"
    - "Every tolerance the tests assert is read at test time from contracts/prophet-parity-v1.yaml (equations.<name>.float_tolerance) and never duplicated as a literal — the tracer's one 1e-9 literal is gone (D-15)"
    - "contracts/prophet-parity-v1.yaml is on the setfit-apr-v1.yaml shape (kind: kernel; equations, proof_obligations, falsification_tests, kani_harnesses declared-not-executed, qa_gate): pv validate reports 0 errors and pv status reports non-zero obligation and falsification counts (D-15; RESEARCH F4 — never the hollow neon-blis shape)"
    - "The seven-fixture ladder's warm debug-profile wall is MEASURED and recorded together with the root-manifest profile state 06-01 Task 2 decided (present/absent); this plan edits no root manifest, so every Wave 2 cargo run reads a stable Cargo.toml (RESEARCH Pitfall 9: 35x slower debug fits)"
    - "D-09 is observable in diagnostics: every fit reports rounds <= 8, Stalled or Converged status, and budget_hit false on the seven fixtures"
  artifacts:
    - path: "contracts/prophet-parity-v1.yaml"
      provides: "frozen Prophet parity tolerances + fit-recipe invariants, one falsification test per rung group"
      contains: "float_tolerance"
    - path: "crates/aprender-forecast/src/prophet.rs"
      provides: "#[cfg(test)] mod parity: >= 25 rung tests over 7 fixtures reading tolerances from the contract"
      contains: "equation_tolerance(\"prophet-parity-v1\""
  key_links:
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "contracts/prophet-parity-v1.yaml"
      via: "test_support::equation_tolerance reads equations.<name>.float_tolerance"
      pattern: "equation_tolerance\\(\"prophet-parity-v1\", \""
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "crates/aprender-forecast/tests/fixtures/*.json"
      via: "test_support::load_json over the seven fixture names"
      pattern: "load_json\\(\""
  prohibitions:
    - statement: "Do not judge parity by parameter equality or by daily-resolution yhat on monthly data — the yearly Fourier block is near-unidentified on air/retail; the bar is objective slack + the optimizer band (prophet-fit-and-predict.md)"
      status: resolved
      verification: judgment
    - statement: "Do not check the exact-L1 gradient by finite differences at Python's exact MAP — 12 deltas sit at +-0.0"
      status: resolved
      verification: judgment
    - statement: "Do not template the contract on contracts/neon-blis-v1.yaml — its keys parse to 0 obligations / 0 falsification tests under pv status (RESEARCH F4)"
      status: resolved
      verification: test
    - statement: "Do not edit the root Cargo.toml in this plan — the [profile.dev.package.aprender-forecast] decision was measured and taken in 06-01 Task 2 (Wave 1) so that no Wave 2 plan writes the manifest every other Wave 2 cargo run reads (RESEARCH Pitfall 9)"
      status: resolved
      verification: test
---

<objective>
Complete the Prophet parity ladder (SC2) as CI-reachable `--lib` tests over all seven committed Prophet
1.4.0 fixtures, with every tolerance frozen in `contracts/prophet-parity-v1.yaml` and read by the tests
(D-04, D-15), then measure the ladder's warm debug-profile wall under the root-manifest profile 06-01
Task 2 decided and record it (RESEARCH Pitfall 9) — no manifest edit here.

Purpose: parity over self-consistency is the phase's correctness bar; the contract makes the bar
citable and the ladder shape makes a regression point at a rung.
Output: one contract, ~36 rung tests, one recorded ladder timing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F4, F5, F10, Pitfall 3, Pitfall 9, Pattern 4)
@.planning/phases/06-native-time-series-forecasting-stack/06-PATTERNS.md  (sections: parity tests; contracts; root Cargo.toml)
@.claude/skills/spike-findings-aprender/references/prophet-fit-and-predict.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md  (the tracer's fixture layout and test_support helpers this plan consumes)

<interfaces>
<!-- Frozen tolerances (measured values in parentheses come from the spike RUN-OUTPUTs read this session). The executor writes these into the contract; the tests read them back. -->
equations.<name>.float_tolerance:
  data_prep_exact                       0.0      (t, y_scaled, changepoints_t, X first/last 3 rows: 0.00e0 on all seven fixtures)
  objective_at_python_map_abs           1.0e-9   (measured 9.1e-13 peyton, 5.7e-14 air, 2.3e-13 retail — D-04 literal)
  predict_path_abs_peyton               1.0e-10  (measured 5.3e-15 — D-04's literal, asserted on the D-04 dataset)
  predict_path_rel_yscale               1.0e-14  (max|Δ yhat| <= tol * y_scale on every fixture; measured <= 5.5e-16 relative: retail is 2.3e-10 ABSOLUTE at y_scale 518253, so an absolute 1e-10 bar would be false there)
  fitted_objective_slack                0.5      (f_rust <= f_python + 0.5; with the D-09 restart loop every fixture lands BELOW Python: -0.13, -0.54, -0.20, -0.04)
  future_yhat_band_peyton_abs           0.0079   (Prophet's own Newton-vs-L-BFGS disagreement on Peyton; Rust measured 0.0049 single round, 0.0030 with restarts) — applies to peyton_manning and peyton_default only; air/retail have no committed control band (their optimisers disagree by 58 / 5252 — recorded in the contract, not asserted)
  components_via_python_params_abs      1.0e-10  (measured <= 6.7e-16)
  components_rebuild_yhat_abs           1.0e-12  (trend*(1+multiplicative_terms)+additive_terms == yhat; measured 0.0)
  band_width_rel                        0.02     (history and future mean 80 % band width vs fixture uncertainty.hist_band_mean_width / future_band_mean_width; measured 0.02 %–0.4 %; seed variance ~1.2 %)
  band_width_last30_rel                 0.02     (uncertainty.future_band_last30_mean_width; measured up to 1.4 % on logistic)
  trend_band_width_rel                  0.05     (uncertainty.future_trend_band_mean_width; measured 3.9 % on peyton_default — excluded from the 2 % bar and given its own)
constants (top-level, read by constant_u64): max_rounds 8, max_iters_per_round 2000, fit_budget_secs 15, lbfgs_memory 20
Fixture keys (spike 001 files): dataset, prophet_version, y_scale, start, t_scale_days, changepoints_t, seasonality_columns, prior_scales, seasonalities, params{k,m,delta,beta,sigma_obs}, log_posterior_at_map_unnormalized, history{ds,t,y,y_scaled}, X_first3, X_last3, forecast{ds,trend,yhat,yhat_lower,yhat_upper,components}
Fixture keys (spike 003 files): + mode, growth, seasonality_mode, holidays_prior_scale, cap, holidays, columns, s_a, s_m, component_cols, component_modes, history.cap_scaled, forecast.trend_lower/upper, uncertainty{interval_width, uncertainty_samples, future_band_mean_width, hist_band_mean_width, future_trend_band_mean_width, future_band_last30_mean_width}, holdout{n_train, coverage_by_seed, mean_width_by_seed, mae}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contracts/prophet-parity-v1.yaml on the setfit-apr-v1 shape and prove it is not hollow</name>
  <reversibility rating="costly">Frozen tolerances are the phase's correctness bar; loosening one later is a deliberate `pv diff`-flagged contract edit (Phase 3 D-10(c) precedent), never an inline relaxation.</reversibility>
  <files>contracts/prophet-parity-v1.yaml</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (lines 1-30 metadata; 80-100 equation shape; 1010-1022 proof_obligations; 1103-1115 falsification_tests; 1243-1262 kani_harnesses with the DECLARED-NOT-EXECUTED house comment; 1276-1310 qa_gate)
    - crates/aprender-contracts/src/schema/types.rs (lines 202-230 Metadata; 248-281 Equation incl. `float_tolerance`; 289-321 ProofObligation `type` values; 448-491 FalsificationTest; 497-510 KaniHarness; enums at 326-367 ObligationType lowercase, 407-415 AppliesTo, 520-525 KaniStrategy snake_case)
    - crates/aprender-contracts/src/schema/validator.rs (lines 189-260, 320-380: SCHEMA-001..011, PROVABILITY-001 conditions)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F4: the minimal valid skeleton and why neon-blis is hollow)
    - .claude/skills/spike-findings-aprender/sources/001-prophet-map-fit-lbfgs/RUN-OUTPUT.md and sources/003-prophet-intervals-and-components/RUN-OUTPUT.md (the measured numbers behind every tolerance in the interfaces block)
  </read_first>
  <action>
    Create `contracts/prophet-parity-v1.yaml` with top-level keys in the template's order: `contract: prophet-parity`,
    `metadata` (version 1.0.0, created 2026-09-05, author PAIML Engineering, `kind: kernel`, a description that
    states D-04, names the seven fixtures and their oracle env `prophet==1.4.0`, and says WHY predict-path
    parity is stated relative to y_scale in addition to the Peyton absolute literal; `references`: Taylor &
    Letham 2018 "Forecasting at Scale", the Prophet 1.4.0 Stan model `prophet.stan`, the two spike references
    `.claude/skills/spike-findings-aprender/references/prophet-fit-and-predict.md` and `sources/001-*/RUN-OUTPUT.md`),
    `equations` — one per interfaces-block name, each with `formula` (the inequality in prose-math),
    `domain`, `codomain`, `float_tolerance` set to the frozen value, and `invariants` where relevant
    (e.g. `fit_recipe` equation: `LbfgsF64::new(2000, 1e-7, 20)`, objective / T, non-finite guard 1e300,
    exact L1 on delta, Stalled accepted, restarts <= 8 until relative improvement < 1e-6, x-keyed cache,
    15 s budget -> budget_hit — D-09), plus a top-level `constants:` map (max_rounds 8, max_iters_per_round 2000,
    fit_budget_secs 15, lbfgs_memory 20) documented in the description as the machine-readable mirror the
    tests read. For `band_width_rel` (and `band_width_last30_rel`, `trend_band_width_rel`) the equation's
    description MUST state what Python reference is compared, per fixture family: the spike-003 files carry
    precomputed `uncertainty.{hist,future}_band_mean_width` etc.; the spike-001 files (peyton_manning,
    air_passengers, retail_sales) carry no `uncertainty` block, so their reference is DERIVED from the
    fixture's own `forecast.yhat_upper - forecast.yhat_lower` (mean over the future rows — `ds` after the last
    `history.ds`, the 365 daily rows of `make_future_dataframe(periods=365)` — and over the history rows;
    `Prophet()` defaults interval_width 0.80, 1000 samples), and the Rust side is Python's parameters through
    `predict` (fit-independent). All seven fixtures are under the one 2 % bar (SC2 names all four datasets). `proof_obligations`: at least one per equation (`type: bound|invariant|equivalence|determinism`,
    `property`, `formal`, `applies_to: all`). `falsification_tests` (count >= obligations): ids
    FALSIFY-PROPHET-001.., `rule`, `prediction` quoting the tolerance, `test` naming the exact
    `cargo test -p aprender-forecast --lib prophet::parity::<fn>` filter Task 2 will create, `if_fails`.
    `kani_harnesses`: the verbatim DECLARED-NOT-EXECUTED house comment and >= 1 entry whose `harness`
    names the runnable bounded test `prophet::parity::future_days_strictly_increasing_bounded` (a grid over
    freq in D/W/MS, 20 start days, horizon <= 3650) that Task 2 places in prophet.rs's parity module.
    `qa_gate`: id F-FORECAST-PROPHET-001, checks list, pass_criteria
    "all FALSIFY-PROPHET tests pass and each was observed RED under an induced tolerance change". Validate
    with `cargo run -p aprender-contracts-cli --bin pv -- validate` (never a stale `target/release/pv`), then
    `pv status` and read the counts.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/prophet-parity-v1.yaml > /tmp/p06-03-t1a.log 2>&1; rc=$?; tail -3 /tmp/p06-03-t1a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-03-t1a.log</automated>
    <fails_when>non-zero exit or the output lacks "0 error(s)" (SCHEMA-00x or PROVABILITY-001 fired)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/prophet-parity-v1.yaml > /tmp/p06-03-t1b.log 2>&1; rc=$?; cat /tmp/p06-03-t1b.log; test $rc -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-03-t1b.log</automated>
    <fails_when>non-zero exit, or any of the three counts prints 0 — the hollow-template failure mode (RESEARCH F4)</fails_when>
  </verify>
  <done>The contract validates with 0 errors, reports non-zero counts, and carries every tolerance from the interfaces block as `float_tolerance` plus the `constants` map.</done>
  <acceptance_criteria>
    - `grep -c 'float_tolerance:' contracts/prophet-parity-v1.yaml` is >= 11
    - `contracts/prophet-parity-v1.yaml` contains `kind: kernel`, `data_prep_exact:`, `objective_at_python_map_abs:`, `predict_path_rel_yscale:`, `fitted_objective_slack:`, `future_yhat_band_peyton_abs:`, `band_width_rel:`, `trend_band_width_rel:`, `constants:`, `max_rounds: 8`, `FALSIFY-PROPHET-001`, `DECLARED, NOT EXECUTED`, `qa_gate:`
    - `grep -c 'prophet==1.4.0\|Prophet 1.4.0' contracts/prophet-parity-v1.yaml` is >= 1
    - `! grep -q '^falsification:' contracts/prophet-parity-v1.yaml && ! grep -q 'registry: true' contracts/prophet-parity-v1.yaml` (the two hollow-template keys)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The full seven-fixture Prophet ladder as --lib tests reading the contract</name>
  <files>crates/aprender-forecast/src/prophet.rs</files>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs (as left by 06-01: the `parity` module with the one Peyton rung and its 1e-9 literal)
    - crates/aprender-forecast/src/test_support.rs (load_json, read_csv, equation_tolerance, constant_u64 signatures)
    - .claude/skills/spike-findings-aprender/sources/001-prophet-map-fit-lbfgs/src/main.rs (lines 118-200: rungs 1-2 and the predict-path rung; how Python params are unpacked and pushed through `predict`)
    - .claude/skills/spike-findings-aprender/sources/003-prophet-intervals-and-components/src/main.rs (lines 95-175: design parity incl. columns/s_a/s_m, fit + Δf, components via Python params, band widths, the reconstruct-yhat check)
    - contracts/prophet-parity-v1.yaml (Task 1 — the tolerance names)
    - crates/aprender-forecast/src/fit.rs (fit_prophet(design, max_rounds) -> (Params, FitInfo))
  </read_first>
  <action>
    Expand `#[cfg(test)] mod parity` in prophet.rs. Add a private helper `fn ladder(fixture: &str) -> Ladder`
    that loads the JSON once, rebuilds the `Spec` from the fixture's `seasonalities` (spike 001 files) or from
    `mode/growth/seasonality_mode/holidays/cap` (spike 003 files), calls `make_design`, and exposes `design`,
    `fixture`, `python_params()`. Replace the tracer's literal with
    `test_support::equation_tolerance("prophet-parity-v1", "objective_at_python_map_abs")`. Then one test per
    rung per fixture, named `<fixture>_<rung>` (fixture stems: peyton_manning, air_passengers, retail_sales,
    peyton_default, peyton_holidays, wp_log_r_logistic, air_multiplicative):
    - `<f>_data_prep_exact`: max|Δ| of t, y_scaled, changepoints_t, X first/last 3 rows vs the fixture
      <= `data_prep_exact`; seasonality (name, period, order) list equal; `prior_scales` equal; for 003
      fixtures also `columns` order equal and `s_a`/`s_m` equal (rung 1).
    - `<f>_objective_at_python_map`: as the tracer's test, tolerance from the contract (rung 2).
    - `<f>_predict_path_via_python_params`: `predict(&design, &python_params, &future_days_of_fixture, seed)`
      -> max|Δ yhat| and max|Δ trend| vs `forecast.yhat/trend` <= `predict_path_rel_yscale * y_scale`, and
      for the two Peyton fixtures additionally <= `predict_path_abs_peyton`; each named component in
      `forecast.components` <= `components_via_python_params_abs`; `trend*(1+multiplicative_terms)+additive_terms`
      rebuilds `yhat` within `components_rebuild_yhat_abs` (rungs 4 + components).
    - `<f>_fit_objective_and_forecast`: `fit_prophet(&design, constant_u64("prophet-parity-v1","max_rounds"))`;
      assert `info.objective <= fixture.log_posterior... (as f_python) + fitted_objective_slack` (mind the sign
      convention the spike uses: f = -log posterior), `info.rounds <= max_rounds`, `!info.budget_hit`,
      `info.status` is "Stalled" or "Converged"; for peyton_manning and peyton_default also assert future
      max|Δ yhat| vs the fixture <= `future_yhat_band_peyton_abs` (rung 5). For air/retail record the future
      max|Δ| in the assertion message only (no bar — the contract explains why).
    - `<f>_band_widths_within_contract` (003 fixtures — peyton_default, peyton_holidays, wp_log_r_logistic,
      air_multiplicative): from the same fit, 1000 draws seeded 42, mean history band width vs
      `uncertainty.hist_band_mean_width` and mean future width vs `future_band_mean_width` within
      `band_width_rel` relative; last-30 within `band_width_last30_rel`; trend band within `trend_band_width_rel`.
    - `<f>_band_widths_within_contract` (001 fixtures — peyton_manning, air_passengers, retail_sales): these
      files have no `uncertainty` block, so DERIVE the Python reference from the fixture arrays: mean of
      `forecast.yhat_upper - forecast.yhat_lower` over the FUTURE rows (forecast `ds` after the last
      `history.ds`) and, separately, over the history rows. The Rust side is the SAME
      `predict(&design, &python_params, &forecast_ds_days, seed)` call the predict-path rung makes (Python's
      parameters through the Rust simulator — the spike-003 main.rs:150 "Python params through our simulator"
      probe; fit-independent, so air/retail's unbarred optimiser disagreement cannot leak into a band verdict),
      with the Spec's 1000 draws and seed 42; both means within `band_width_rel` relative. Put the measured
      relative deviation per fixture in the assertion message and the SUMMARY.
    - `future_days_strictly_increasing_bounded` (the kani-harness evidence): for freq in D/W/MS, 20 start days
      spanning leap years and month ends, horizon in {1, 28, 365, 3650}: strictly increasing, and for MS every
      day is the 1st of a month.
    Fits run once per fixture per test file run (7 fits); keep the 20 000-point budget case OUT of this module
    (Pitfall 9). Use `expect` with fixture names on every load; never skip.
    Timing record (RESEARCH Pitfall 9, F10): with the ladder green, run the same filter a second time (warm
    build) and record `debug parity ladder wall: N s` in the SUMMARY next to the root-manifest state 06-01
    Task 2 left (`grep -c '^\[profile\.dev\.package\.aprender-forecast\]' Cargo.toml` — 0 or 1). This plan
    edits NO root manifest: the profile decision was taken in Wave 1 so every Wave 2 cargo run reads a stable
    Cargo.toml. If the warm wall exceeds 60 s, record it as a finding for 06-09's deferred items; do not act
    on it here.
  </action>
  <verify>
    <automated>t0=$(date +%s); CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-03-t2.log 2>&1; rc=$?; t1=$(date +%s); echo "ladder wall=$((t1-t0))s (includes any rebuild — re-run warm for the SUMMARY number)"; tail -40 /tmp/p06-03-t2.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-03-t2.log > /dev/null && test "$(grep -c '^test prophet::parity::' /tmp/p06-03-t2.log)" -ge 30</automated>
    <fails_when>non-zero exit, any failed/ignored test, or fewer than 30 `test prophet::parity::` lines (7 x 5 rungs + the bounded grid = 36 expected; the ladder was not fully written or the filter drifted)</fails_when>
    <automated>grep -q '^mod parity' crates/aprender-forecast/src/prophet.rs || { echo "no column-0 'mod parity' in prophet.rs — the region gate cannot locate the test module"; exit 1; }; if sed -n '/^mod parity/,$p' crates/aprender-forecast/src/prophet.rs | grep -Eq '(^|[^0-9.])(1e-9|1\.0e-9|1e-10|1\.0e-10|0\.5|0\.02|0\.0079|0\.05)([^0-9]|$)'; then echo "tolerance literal found inside prophet.rs mod parity"; exit 1; fi</automated>
    <fails_when>exit 1 — either `mod parity` is not at column 0 (the region cannot be located; the gate refuses to pass vacuously rather than scanning nothing) or a parity tolerance literal is hardcoded INSIDE the test module instead of read from the contract (D-15). Production code above the module — e.g. the RNG's `laplace()` at spike prophet.rs:402 — is outside the region and is not consulted; there is no regex to narrow</fails_when>
  </verify>
  <done>>= 30 rung tests pass over the seven fixtures (band widths on all seven), every bar read from the contract, D-09 diagnostics asserted on every fit, the warm ladder wall recorded.</done>
  <acceptance_criteria>
    - `grep -c 'equation_tolerance("prophet-parity-v1"' crates/aprender-forecast/src/prophet.rs` is >= 10
    - `grep -c 'constant_u64("prophet-parity-v1"' crates/aprender-forecast/src/prophet.rs` is >= 1
    - prophet.rs contains test fns `peyton_manning_data_prep_exact`, `retail_sales_predict_path_via_python_params`, `wp_log_r_logistic_fit_objective_and_forecast`, `air_multiplicative_band_widths_within_contract`, `peyton_manning_band_widths_within_contract`, `air_passengers_band_widths_within_contract`, `retail_sales_band_widths_within_contract`, `peyton_holidays_data_prep_exact`, `future_days_strictly_increasing_bounded`
    - The tracer's literal tolerance no longer appears in the parity module (second automated check, region-scoped to `mod parity`)
    - `/tmp/p06-03-t2.log` shows `0 ignored`
    - SUMMARY contains `debug parity ladder wall: N s` and the root-manifest profile state (0 or 1) it was measured under; `git diff --stat -- Cargo.toml` is empty for this plan (no manifest edit in Wave 2)
  </acceptance_criteria>
</task>


</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed oracle fixtures -> tests | fixtures are data the tests trust; they are byte-verified against the spike originals in 06-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering | tolerance values | medium | mitigate | tolerances live in ONE contract file validated by `pv`; tests read them; a change is a `pv diff`-visible edit |
| T-06-11 | Repudiation | a parity claim that was never falsified | medium | mitigate | qa_gate demands each FALSIFY test be observed RED under an induced tolerance change (record one such run in the SUMMARY) |
| T-06-SC | Tampering | cargo installs | high | accept | no new dependency in this plan |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib prophet::parity` — >= 30 tests, 0 failed, 0 ignored
- `pv validate` 0 errors and `pv status` non-zero counts on the contract
- Warm debug ladder wall measured and recorded with the 06-01 profile state; root Cargo.toml untouched by this plan
</verification>

<success_criteria>
- SC2 is met as CI-reachable tests: data prep 0.0, objective 1e-9, predict path 1e-10 on Peyton (and 1e-14 x y_scale everywhere), fit <= Python + 0.5, Peyton forecast inside the 0.0079 band, components rebuild yhat, band widths within 2 % on all seven fixtures (derived reference for the three spike-001 files)
- D-15: every bar is contract-read; D-09 is diagnostics-asserted
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-03)
New file: `contracts/prophet-parity-v1.yaml` (equations data_prep_exact, objective_at_python_map_abs, predict_path_abs_peyton, predict_path_rel_yscale, fitted_objective_slack, future_yhat_band_peyton_abs, components_via_python_params_abs, components_rebuild_yhat_abs, band_width_rel, band_width_last30_rel, trend_band_width_rel, fit_recipe; constants max_rounds/max_iters_per_round/fit_budget_secs/lbfgs_memory; FALSIFY-PROPHET-0NN; F-FORECAST-PROPHET-001 qa_gate).
New tests in `crates/aprender-forecast/src/prophet.rs::parity`: `ladder()` helper and `<fixture>_{data_prep_exact, objective_at_python_map, predict_path_via_python_params, fit_objective_and_forecast, band_widths_within_contract}` for all seven fixtures, `future_days_strictly_increasing_bounded`.
Root `Cargo.toml`: NOT modified — the `[profile.dev.package.aprender-forecast]` decision is 06-01 Task 2's (Wave 1).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-03-SUMMARY.md` when done. Record the test count, the measured future max|Δ| for air/retail (unbarred), the per-fixture band-width relative deviations, the one induced-RED observation for the qa_gate, and `debug parity ladder wall: N s` with the profile state it was measured under.
</output>
