---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 15
type: execute
wave: 13
gap_closure: true
depends_on: [06-14]
files_modified:
  - crates/aprender-forecast/src/forecast.rs
  - crates/aprender-forecast/src/types.rs
  - crates/aprender-forecast/src/np.rs
  - crates/aprender-mcp-forecast/src/lib.rs
  - contracts/forecast-tool-boundary-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [SC1]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CLASS INVARIANT, HALF 2, NOW TRUE — AND THE PROOF IS A TEST GOING FROM RED TO GREEN: `types::tests::no_cost_axis_is_pending` SHIPPED RED at the close of wave 12 naming C-07 and C-08, and this plan is what turns it green by replacing both `unbounded_pending_06_15` markers with real bounds (or, for C-08 only, an honest `measured_at_structural_maximum`). After this plan every enumerated cost axis names either a real key in `constants:` or a measured disposition, and BOTH `no_cost_axis_is_pending` and `every_cost_axis_names_a_real_bound` are green on a complete axis list — that pair is the machine-checkable form of 'the class is closed'"
    - "C-07 is closed by a BOUND, never by a measurement, because it has no structural maximum to measure: `holidays[].name` is an unbounded `String` and there is no `DefaultBodyLimit`, `max_body` or content-length cap anywhere in either crate, nor any stdio framing cap. Its 06-14 entry carries `no_structural_maximum: true`, and this plan replaces the marker with `fit_max_holiday_name_len` rather than with a `measured_seconds` that would describe an arbitrarily chosen name length"
    - "C-07 closed: `holidays[].name` — a caller-settable field with NO enforcement at HEAD, found by the enumeration and by no review finding — is bounded by `MAX_HOLIDAY_NAME_LEN` at the door. `prophet::columns` clones the name TWICE per design column (`name` and `component`) and then uses it as the key of an `O(C log C)` string sort, so 1 000 columns times an N-byte name is a ~2 000:1 amplification off ONE payload occurrence"
    - "C-08 measured and dispositioned: the NeuralProphet training path has NO budget of any kind — `FIT_BUDGET_SECS` is read at exactly one place, `fit.rs:112`, inside `fit_prophet`, and `np::train` never reads it — and the door drives 2 or 3 full `np::train` calls per request through its learning-rate sweep, with `n_lags` reaching 365. (The axis is NOT that `n_train_grid` is bounded by a larger constant than `MAX_POINTS`: `types.rs:30` sets `MAX_SPAN_DAYS = MAX_POINTS as i64`, so the two are numerically identical. The drivers are the missing budget, the 2-3x sweep and `n_lags`.) The worst LEGAL request is MEASURED on release, and the axis either gains a door bound or is recorded `measured_at_structural_maximum` with the number that justifies it — which of the two happened is decided by the measurement, and the post-condition gate proves the worst legal request is thereafter either refused or under 2 s"
    - "WR-03 closed: `holiday_dates_total` is refused INSIDE the holiday loop, immediately after the accumulation, exactly as `holiday_columns` already is — so 1 000 holidays of 1 000 dates no longer run ~1 000 000 `parse_date` calls and allocate ~8 MB of `Vec<i64>` before the door discards all of it. The post-loop refusal is KEPT so the caller still learns the exact total; the in-loop message says it is a partial sum"
    - "Every new bound has an observed RED side through the live streamable-HTTP server: the constant is temporarily raised, the over-bound request is observed ACCEPTED, the constant is restored, and the request is observed refused. Both runs are recorded"
    - "No bound over-refuses: each new bound ships with a near-miss positive control that still fits and still returns the full response shape, and the NeuralProphet parity ladder (`np::parity`) and the Prophet parity ladder (`prophet::parity`, 32/0) are both unmoved"
    - "The in-loop refusal is proven to fire EARLY, not merely to fire: a request whose aggregate total is exceeded by its second holiday is refused while later holidays' dates are still unparsed, demonstrated by a case whose LATER holidays carry dates `parse_date` would reject — if the refusal fired after the loop, the caller would get a date-shape refusal instead of the aggregate one"
  artifacts:
    - path: "crates/aprender-forecast/src/forecast.rs"
      provides: "the in-loop aggregate-dates refusal, the holiday-name length refusal, and the NeuralProphet training-cost check (or the recorded decision that the structural maximum clears the bar)"
      contains: "max_holiday_name_len"
    - path: "crates/aprender-forecast/src/types.rs"
      provides: "MAX_HOLIDAY_NAME_LEN, the NP training-cost constant if one is added, and their mirror rows in cost_bounds_match_contract"
      contains: "MAX_HOLIDAY_NAME_LEN"
    - path: "crates/aprender-forecast/src/np.rs"
      provides: "the door-computable training-cost proxy as a named function, so the door and np::train cannot disagree about the work a request buys"
      contains: "fn train_cost"
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "constants.fit_max_holiday_name_len, the NP cost key or the measured_at_structural_maximum record, the holiday_name_cost_bounded and np_train_cost_bounded equations, their obligations and FALSIFY entries, and cost_axes entries with no remaining pending marker"
      contains: "fit_max_holiday_name_len"
    - path: "crates/aprender-mcp-forecast/src/lib.rs"
      provides: "the red-side and near-miss e2e pairs for each new bound, plus the early-refusal case for WR-03"
      contains: "refuses_holiday_name_over_length_bound"
    - path: "contracts/aprender/binding.yaml"
      provides: "binding rows for the new equations, resolving to real definition sites"
      contains: "holiday_name_cost_bounded"
  key_links:
    - from: "crates/aprender-forecast/src/forecast.rs"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "types::MAX_HOLIDAY_NAME_LEN asserted equal to constants.fit_max_holiday_name_len by cost_bounds_match_contract (D-15)"
      pattern: "fit_max_holiday_name_len"
    - from: "crates/aprender-forecast/src/forecast.rs"
      to: "crates/aprender-forecast/src/np.rs"
      via: "the door calls np::train_cost, the same function np::train's step count is derived from, so the door's cost estimate is the cost that is actually spent"
      pattern: "train_cost"
    - from: "crates/aprender-forecast/src/types.rs"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "no_cost_axis_is_pending goes RED -> GREEN across this plan, which is the observable event that makes the class invariant's second half satisfied rather than pending"
      pattern: "no_cost_axis_is_pending"
  prohibitions:
    - statement: "Never remove the post-loop `holiday_dates_total` refusal when adding the in-loop one. The in-loop count is a PARTIAL SUM, so its message cannot honestly report the request's total; both refusals are cheap and they say different true things"
      status: resolved
      verification: test
    - statement: "Never add a NeuralProphet cost bound without measuring first. If the structural maximum clears 2 s on a release build, the honest disposition is `measured_at_structural_maximum` with the number — inventing a bound to make a table look tidy refuses legitimate requests for no measured reason"
      status: resolved
      verification: test
    - statement: "Never let the door and `np::train` compute the training cost differently. The proxy is a named function in `np.rs` that the door calls; a second inline copy in `forecast.rs` is the drift hazard that `changepoint_count` was extracted to avoid in 06-14"
      status: resolved
      verification: test
    - statement: "Never touch a parity fixture, a parity tolerance, or the numbers in `contracts/{prophet,neuralprophet,chronos-bolt}-parity-v1.yaml`. A refusal added at the door must not move a fixture. `np::parity` and `prophet::parity` are both run before and after"
      status: resolved
      verification: test
    - statement: "Do not touch `crates/aprender-compute`, `.github/workflows/*.yml`, or `crates/aprender-serve` — scope fence for this gap-closure run"
      status: resolved
      verification: judgment
---

<objective>
Close the two unbounded cost axes 06-14's enumeration found and no review finding pointed at, and move
the aggregate-dates refusal to where its own comment already claims it is.

Purpose: rounds 1 and 2 each bounded exactly the axis that had been measured, and round 3 exists
because that produces a new critical finding every pass. 06-14 enumerated the door's whole surface and
left exactly two axes carrying a pending marker: `holidays[].name`, a caller-settable field with no
enforcement at all whose bytes are cloned twice per design column and then sorted, and the
NeuralProphet training path, which runs two or three full trainings per request under NO budget of any
kind. Closing them here is what turns `no_cost_axis_is_pending` — which 06-14 deliberately SHIPPED RED
naming both — from red to green, on a complete list —
the difference between "we bounded the thing we measured" and "every axis we can enumerate is
accounted for".

Output: `MAX_HOLIDAY_NAME_LEN` at the door; a measured disposition for the NeuralProphet training axis
(a bound, or a recorded structural-maximum measurement, decided by the number); the aggregate-dates
refusal moved into the loop with the exact-total refusal kept; red sides observed for every new bound;
and a `door_surface.cost_axes` list with no pending marker left in it — which is also what returns the
crate's `--lib` suite to fully green, since 06-14 left exactly one test red on purpose.
</objective>

<gap_closure_context>
Source: `.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md` for WR-03, and plan
06-14's `<door_surface_enumeration>` (re-derived and corrected by 06-14 Task 1) for C-07 and C-08.
`06-VERIFICATION.md` is STALE — it predates 06-10..06-13, which closed all three of its gaps. Do not
re-close them.

Findings and axes this plan owns:

| Item | Source | Where it lands |
|---|---|---|
| **WR-03** — the aggregate-dates refusal fires after the whole holiday loop, contradicting its own comment | `06-REVIEW.md` Warnings | Task 1 |
| **C-07** — `holidays[].name` is unbounded; `prophet::columns` clones it twice per column and sorts on it | 06-14 enumeration (found by inspection, not by a review finding) | Task 2 |
| **C-08** — `np::train` has no budget; the door drives it 2-3x per request through its learning-rate sweep | 06-14 enumeration | Task 3 |

WR-03's own measured trigger, from the review: 1 000 holidays each with `lower_window: 0,
upper_window: 0` (so `holiday_columns` reaches only 1 000 and never trips) and each carrying 1 000
dates. `parse_date` runs ~1 000 000 times and ~1 000 `Vec<i64>` totalling ~8 MB are allocated and
pushed into `holidays` before the refusal at `forecast.rs:245` discards all of it. The contract's own
measurement puts the set-construction half at 18.049 ms; the parse half is comparable. Amplification
is roughly 1:1 with the ~11 MB payload, so it is a hygiene defect rather than a second CR-01 — but it
is unbounded work performed after the door already knows the request will be refused, on the exact
surface this phase exists to bound.

This plan does NOT modify, supersede or re-create 06-01 … 06-14 or their SUMMARYs.
</gap_closure_context>

<review_dispositions_ledger>
## Review Dispositions Ledger — all 9 findings in `06-REVIEW.md`

Repeated verbatim in every round-3 plan so no plan can be read in isolation and conclude a finding was
dropped.

| ID | Severity | Disposition | Landing site |
|---|---|---|---|
| CR-01 | Critical | **INCORPORATED** | 06-14 T1 (enumeration + `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`), T3 (red-side e2e + `logistic_changepoint_cost_bounded`); 06-16 (the sweep gate that would have caught it) |
| WR-01 | Warning | **INCORPORATED, SPLIT** | 06-14 T2 (contract mirror via `constant_f64`); 06-17 T1 (the tight sub-threshold sweep point the normal branch actually fails) |
| WR-02 | Warning | **INCORPORATED** | 06-17 T1 (sample-variance assertion with its own contract-owned tolerance; zero-variance stub proven RED) |
| WR-03 | Warning | **INCORPORATED** | **06-15 T1 (this plan)** |
| WR-04 | Warning | **INCORPORATED** | 06-16 T1/T2/T3 (one parameterized SC1 sweep over freq x growth x holiday, in `just`, run without `--ignored`; three blind benches folded in; superlative reworded) |
| IN-01 | Info | **INCORPORATED** | 06-16 T2 (shared numeric-shape validator at ALL FIVE bar sites, with a must-match/must-not-match case table per CLAUDE.md rule 7) |
| IN-02 | Info | **INCORPORATED, NARROWED** | 06-17 T2 (`hol_sets.get(hi).is_some_and(…)` + a semver note; the signature is NOT reverted — that would re-open 06-11's measured design-build fix, and the review records the hazard as pre-existing) |
| IN-03 | Info | **INCORPORATED** | 06-14 T2 (doc count corrected; `DEFAULT_POOL` split into `pool_default_matches_contract`) |
| IN-04 | Info | **INCORPORATED IN PART, REST REJECTED WITH RATIONALE** | 06-17 T2 takes `debug_assert_eq!(w.len(), out * inp)` at `bolt.rs`'s transpose call and the semver notes. REJECTED, no action: "five files fall outside the stated 9-file scope" is an observation about the review's own scope statement, not a tree defect; the `dates.rs` rewrite it examined was found sound |
</review_dispositions_ledger>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md  (WR-03 in full, including its measured trigger and its suggested in-loop fix)
@.planning/phases/06-native-time-series-forecasting-stack/06-14-PLAN.md  (the `<door_surface_enumeration>` section — C-07 and C-08 are its findings, and this plan is what clears their pending markers and turns `no_cost_axis_is_pending` green)
@.planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md  (the RE-DERIVED enumeration and any delta from the plan's table — read the SUMMARY's version, not the plan's, where they differ)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-10 the NeuralProphet training recipe is spike-002 verbatim; D-11 refuse-never-default; D-15 the contract is the source)
@.planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md  (how a cost bound's value was derived, measured and doc-commented)
@CLAUDE.md  (Verification Discipline rules 1/2/5/6/7; `pmat query` not grep; dogfood `pv`; `unwrap()` banned)

<interfaces>
**The holiday loop** (`crates/aprender-forecast/src/forecast.rs`, approximately :183-243). Per holiday
it checks window signs, then `|window| <= MAX_HOLIDAY_WINDOW`, then `dates.len() <=
MAX_HOLIDAY_DATES`, then accumulates `holiday_dates_total += h.dates.len();`, then accumulates
`holiday_columns += (h.upper_window - h.lower_window + 1) as usize;` and IMMEDIATELY refuses if
`holiday_columns > MAX_HOLIDAY_COLUMNS`, then parses every date via `parse_date` and pushes a
`Holiday`. The `holiday_dates_total` comparison against `MAX_HOLIDAY_DATES_TOTAL` is at approximately
:245 — AFTER the loop. The comment at approximately :210-211 already claims the running sum "is
refused below the moment the aggregate ceiling is passed"; the fix is to make the code match the
comment, keeping the post-loop check as the one that reports the exact total.

**The holiday column builder** (`crates/aprender-forecast/src/prophet.rs`, `columns`, approximately
:146-165). For each holiday and each offset in `lower_window..=upper_window` it pushes a `Column`
whose `name` is `format!("{}_delim_{}{}", h.name, sign, off.abs())` and whose `component` is
`h.name.clone()` — TWO owned `String`s per column, both derived from one payload occurrence of the
name — and then `hcols.sort_by(|a, b| a.name.cmp(&b.name))`, an `O(C log C)` comparison sort whose
comparisons are byte-wise over those names. With `MAX_HOLIDAY_COLUMNS` at 1 000 that is 2 000 copies
plus ~10 000 string comparisons per request, each scaling linearly in the name length. Nothing bounds
the name length at HEAD. The response's `components` map is also keyed on the name, so the name also
reaches the serialized reply.

**The NeuralProphet arm** (`crates/aprender-forecast/src/forecast.rs`, the `"neuralprophet"` match
arm). It refuses `freq != "D"`, defaults `n_lags` to 0 and refuses `n_lags > 365`, builds
`np::NpData::new(&ds, &args.y, n_train, 10, 0.8)`, refuses `n_lags >= d.n_train_grid`, then runs a
learning-rate sweep: `&[0.03, 0.1]` when `n_lags > 0` and `&[0.01, 0.03, 0.1]` otherwise — TWO or
THREE full `np::train` calls per request — with `epochs: Some(np::auto_epochs(n_train).min(320))` when
lags are on and `None` (i.e. `auto_epochs(n)` computed inside `train`) otherwise. There is NO time
budget anywhere on this path; `fit::FIT_BUDGET_SECS` is read only by `fit::fit_prophet`.

**The training work** (`crates/aprender-forecast/src/np.rs`, `train`, approximately :493-515):
`n_grid = d.n_train_grid`; `samples` is the observed rows when `n_lags == 0` and `(l..n_grid)` when
lags are on; `n = samples.len()`; `batch = cfg.batch.unwrap_or_else(|| auto_batch(n))`;
`epochs = cfg.epochs.unwrap_or_else(|| auto_epochs(n))`; `n_batches = n.div_ceil(batch)`;
`total_steps = epochs * n_batches`. `auto_epochs` (:418-421) is
`(10.0 * (100.0 / n * 2^(2.25 * log10(10 + n))).ceil()).clamp(20.0, 500.0)`. `d.n_train_grid` is the
imputed DAILY grid length, bounded by `MAX_SPAN_DAYS` (20 000), not by `MAX_POINTS`. The AR head is
`ar_layers: vec![32]` when lags are on, so per-sample work scales with `n_lags`.

**The e2e harness** (`crates/aprender-mcp-forecast/src/lib.rs` `mod e2e`): `serve()`, `synth(n)`,
`valid_args()`, `with(extra)`, `refused(&mut c, args, needle)`, `tool_output(&r)`,
`assert_shared_shape(&out, horizon)`. `refuses_holiday_dates_total_over_bound` (:874) already builds
eleven 1 000-date holidays — the nearest existing analogue for both Task 1 and Task 2's payloads.

**Contract ids.** READ `contracts/forecast-tool-boundary-v1.yaml` for the next unused
`FALSIFY-BOUNDARY-0NN`; 06-14 added two beyond HEAD's 018.

**Binding rows** (`contracts/aprender/binding.yaml`, Phase 6 block from :1610): the header comment
states a row count that must be updated. `make contract-audit-phase6` resolves each row's `function`
to a definition site in the file its `module_path` names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-03 — refuse the aggregate holiday-date total INSIDE the loop, keep the exact-total refusal after it</name>
  <files>crates/aprender-forecast/src/forecast.rs, crates/aprender-mcp-forecast/src/lib.rs</files>
  <precondition>No other cargo build is running against this workspace's shared target directory, and `df -h .` shows headroom — this branch halted three times on ENOSPC during waves 1-7 and every round-3 plan runs one at a time.</precondition>
  <read_first>
    - crates/aprender-forecast/src/forecast.rs — the WHOLE holiday loop (~:183-243) and the post-loop
      product refusals (~:243-275): the order of the per-holiday checks, the two accumulators, the
      `holiday_columns` refusal that already fires in-loop (the shape to mirror), the comment block at
      ~:210-211 whose claim the code does not currently satisfy, and the `parse_date` collection and
      `Holiday` push that happen after the accumulation
    - crates/aprender-forecast/src/types.rs — `MAX_HOLIDAY_DATES`, `MAX_HOLIDAY_DATES_TOTAL`,
      `MAX_HOLIDAY_COLUMNS` and their doc comments, so the in-loop message uses the same key names
    - crates/aprender-forecast/src/dates.rs — `parse_date`'s shape gate, so the position test's
      deliberately-bad later dates are refused for the reason the test intends (a nine-byte string, not
      an impossible calendar date, which produces a different message)
    - crates/aprender-mcp-forecast/src/lib.rs — `refuses_holiday_dates_total_over_bound` (~:874) in
      full, including its eleven-holiday construction, because moving the check may change which of the
      two refusals that payload receives
    - .planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md WR-03 — the measured
      trigger and the suggested in-loop form
  </read_first>
  <behavior>
    - A request whose holidays' date counts cross `MAX_HOLIDAY_DATES_TOTAL` at the Kth holiday is
      refused while holidays K+1..N are still unparsed. Proven by making the LATER holidays carry
      dates `parse_date` would reject: if the refusal still fired after the loop, the caller would
      receive the date-shape refusal instead of the aggregate one, so the test discriminates position
      and not merely presence
    - A request whose total is over the bound still reports a refusal naming
      `max_holiday_dates_total`, whichever of the two checks fires
    - A request whose total is exactly at the bound is still accepted and still returns the full
      response shape — the in-loop check must use the same comparison as the post-loop one, not a
      tightened one
    - The post-loop refusal still reports the request's exact total; the in-loop one says its count is
      a running total so far, because a partial sum reported as "the total" would be a false statement
      in an error message
  </behavior>
  <action>
    (1) MOVE THE CHECK, KEEP BOTH. In `forecast.rs`'s holiday loop, immediately after
    `holiday_dates_total += h.dates.len();` and mirroring the `holiday_columns` refusal two lines
    below it, add a refusal when `holiday_dates_total > MAX_HOLIDAY_DATES_TOTAL`. Its message must name
    `max_holiday_dates_total` and its value and say what to reduce, and must describe its own count as
    a running total rather than as the request's total. KEEP the post-loop check: it is the one that
    can honestly report the exact figure, both are cheap, and deleting it would change an existing
    refusal message that `refuses_holiday_dates_total_over_bound` asserts on. Update the comment at
    approximately :210-211 so it describes what the code now does — the review's point is that a
    comment claiming a property the code lacks reads as deliberate rather than accidental.

    (2) POSITION-DISCRIMINATING TEST, WRITTEN FIRST. In `forecast::tests` add
    `the_aggregate_dates_refusal_fires_inside_the_holiday_loop`: build holidays such that the running
    total crosses `MAX_HOLIDAY_DATES_TOTAL` partway through, and give every LATER holiday a date string
    `parse_date` rejects (for example `"2020-1-01"`, nine bytes, which the shape gate refuses). Assert
    the refusal names `max_holiday_dates_total` and NOT the date-shape message. Run it against the
    unmoved check FIRST and record it RED — it must report the date-shape refusal, which is the direct
    observation that the work was being done before the door knew the answer. Then move the check and
    observe green.

    (3) TWO-SIDED CONTROL. Add or extend a case proving a request whose aggregate is exactly at
    `MAX_HOLIDAY_DATES_TOTAL` is still accepted, so the in-loop comparison did not become `>=`. If an
    equivalent already exists in `forecast::tests` or in `e2e`, extend it rather than duplicating it,
    and say in the SUMMARY which.

    (4) THROUGH THE SERVER. Add an e2e case that the early refusal is what a real client sees:
    `refuses_holiday_dates_total_before_parsing_the_rest` driving `serve()` with the same shape as (2).
    Keep `refuses_holiday_dates_total_over_bound` unchanged — it asserts the post-loop message and must
    keep passing.

    (5) OPTIONAL, ONLY IF CHEAP: record the saved work as a number rather than an argument. The review
    estimates ~1 000 000 `parse_date` calls and ~8 MB avoided. If a release-profile before/after wall
    on the review's exact payload (1 000 holidays x 1 000 dates, zero windows) can be taken in a few
    minutes, take it and record both figures; if not, record that it was not measured and why. Do not
    claim a saving that was not measured (CLAUDE.md rule 2).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast::tests -- --nocapture > /tmp/p06-15-t1a.log 2>&1; rc=$?; tail -20 /tmp/p06-15-t1a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit. A failure in `the_aggregate_dates_refusal_fires_inside_the_holiday_loop` whose message quotes the date-shape refusal means the check is still after the loop; a failure in the at-the-bound control means the in-loop comparison was written as `>=` and the bound now over-refuses by one</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast::tests::the_aggregate_dates_refusal_fires_inside_the_holiday_loop > /tmp/p06-15-t1b.log 2>&1; rc=$?; tail -8 /tmp/p06-15-t1b.log; test $rc -eq 0 && grep -q '1 passed' /tmp/p06-15-t1b.log</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary — the test name does not exist, so the filter matched nothing and the green exit proves nothing</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-15-t1c.log 2>&1; rc=$?; tail -15 /tmp/p06-15-t1c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — most likely `refuses_holiday_dates_total_over_bound`, whose eleven-holiday payload now crosses the bound at holiday eleven and may be answered by the in-loop message instead of the post-loop one. If so, decide deliberately which message that payload should get, adjust the test's needle to the shipped text, and say so in the SUMMARY; do not loosen the needle to something that matches both</fails_when>
  </verify>
  <acceptance_criteria>
    - In `crates/aprender-forecast/src/forecast.rs`, the FIRST occurrence of the literal
      `max_holiday_dates_total` is at a LOWER line number than the first occurrence of
      `max_holiday_design_cost`, and both are lower than the `make_design(` call. Quote all three line
      numbers in the SUMMARY — this is the positional claim, and a grep count alone cannot make it
    - `grep -c 'max_holiday_dates_total' crates/aprender-forecast/src/forecast.rs` is at least 2 (the
      in-loop refusal and the retained post-loop refusal)
    - `crates/aprender-forecast/src/forecast.rs` declares
      `the_aggregate_dates_refusal_fires_inside_the_holiday_loop` and it passes
    - The RED observation is recorded in the SUMMARY: the position test run BEFORE the move, reporting
      the date-shape refusal, quoted verbatim
    - `refuses_holiday_dates_total_over_bound` still passes; if its needle was changed, the SUMMARY
      states the old needle, the new needle and why the payload's answer changed
    - The comment block above the accumulation describes the code as it now is
  </acceptance_criteria>
  <done>The aggregate-dates ceiling is enforced where its own comment always claimed it was, the exact-total refusal is retained for the message it alone can honestly give, and the position — not merely the presence — of the check is proven by a test that would answer differently if the check moved back.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C-07 — bound `holidays[].name`, the caller-settable field with no enforcement at all</name>
  <files>crates/aprender-forecast/src/types.rs, crates/aprender-forecast/src/forecast.rs, crates/aprender-mcp-forecast/src/lib.rs, contracts/forecast-tool-boundary-v1.yaml</files>
  <reversibility rating="costly">Bounding a string field narrows the advertised tool boundary of a published crate: a client sending a long holiday label is refused where it was accepted. Reversible in code, but client-visible, so the contract takes a minor bump and `pv diff` is consulted. Not one-way.</reversibility>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs `columns` (~:126-165) — the holiday branch in full: the
      `format!("{}_delim_{}{}", h.name, sign, off.abs())` name, the `component: h.name.clone()`, and the
      `hcols.sort_by(|a, b| a.name.cmp(&b.name))` that follows. This is the amplification the bound is
      sized against, and it must be read rather than taken from this plan's table
    - crates/aprender-forecast/src/forecast.rs — the holiday loop as Task 1 left it, so the new
      per-holiday refusal sits beside the others in the same style
    - crates/aprender-forecast/src/types.rs — `HolidayArg` (the `name` field's doc comment is lifted
      into the advertised MCP schema by `schemars`, so a bound belongs in it),
      `MAX_HOLIDAY_COLUMNS`'s doc comment, and `cost_bounds_match_contract`'s table
    - crates/aprender-forecast/tests/fixtures/peyton_holidays_prophet140.json and the e2e holiday
      payloads — for the longest holiday name anything committed actually uses
    - contracts/forecast-tool-boundary-v1.yaml — `constants:`, `holiday_design_cost_bounded` (the
      equation style to match) and the `door_surface.cost_axes` C-07 entry 06-14 shipped with the
      `unbounded_pending_06_15` marker and `no_structural_maximum: true`
  </read_first>
  <behavior>
    - A holiday whose `name` exceeds `MAX_HOLIDAY_NAME_LEN` bytes is refused at the door, naming the
      holiday's position (not the whole name back — echoing an oversized string into an error message
      is the amplification the bound exists to stop), the observed length, the key
      `max_holiday_name_len` and its value
    - A holiday whose name is exactly at the bound is accepted and still fits
    - The refusal fires inside the holiday loop, before the name is cloned into any `Column`
    - An empty name is NOT refused by this bound (it is a separate question this plan does not open);
      the bound is one-sided by design and the test says so
  </behavior>
  <action>
    (1) DERIVE THE VALUE FROM WHAT IS COMMITTED, then state the amplification. The largest holiday name
    in any committed fixture, example or test is short — Prophet 1.4.0's own canonical
    `peyton_holidays` frame uses `playoff` and `superbowl`. Find the actual longest across
    `crates/aprender-forecast/tests/fixtures/`, the e2e cases and the examples, and state it. Propose
    `MAX_HOLIDAY_NAME_LEN: usize = 200` — two orders of magnitude above anything committed and far past
    any human label — and record the bounded worst case it implies: `MAX_HOLIDAY_COLUMNS` (1 000)
    columns x 2 owned copies x 200 bytes = ~400 KB of `String`, plus ~10 000 byte-wise comparisons of
    <= 200 bytes in `hcols.sort_by`. Compare that against the unbounded case the same arithmetic gives
    for a 1 MB name. Measure the sort-and-clone cost at the bound on a release build if it is cheap to
    do so; if it is not, say the bound was derived from the amplification ratio and the committed
    maximum rather than from a wall, and say which.

    (2) REFUSE AT THE DOOR, INSIDE THE HOLIDAY LOOP. Add the check to `forecast.rs`'s loop beside the
    other per-holiday refusals, keyed on `h.name.len()` (bytes — `String::len` is bytes, which is what
    the clone and comparison cost is proportional to; say so in a comment so a later reader does not
    "fix" it to `chars().count()`). The message must NOT echo the name back.

    (3) BYTES, NOT CHARS, AND A NON-ASCII CASE. Add a `forecast::tests` case with a multi-byte UTF-8
    name whose character count is under the bound and whose BYTE length is over it, asserting it is
    refused — that is the case that proves the bound is on the quantity that costs, and the case a
    `chars().count()` rewrite would silently break.

    (4) TWO-SIDED CONTROL AND THE SERVER PAIR. Library: `a_holiday_name_over_the_length_bound_is_refused`
    and `a_holiday_name_at_the_length_bound_is_accepted`. Server:
    `refuses_holiday_name_over_length_bound` driving `serve()` with a name one byte over, and
    `accepts_holiday_name_at_length_bound` asserting the full response shape via `assert_shared_shape`.
    Observe the RED side: raise `constants.fit_max_holiday_name_len` and
    `types::MAX_HOLIDAY_NAME_LEN` together, observe the over case ACCEPTED, restore, observe refused.
    Record both runs.

    (5) CONTRACT AND MIRROR. Add `fit_max_holiday_name_len` to `constants:` with a comment recording
    the amplification (one payload occurrence, ~2 000 derived copies), extend
    `cost_bounds_match_contract`'s table with the `MAX_HOLIDAY_NAME_LEN` row, and prove the mirror by
    changing the YAML value alone and observing the test red naming the key. Add a
    `holiday_name_cost_bounded` equation in `holiday_design_cost_bounded`'s style, its
    `proof_obligations` entry, and the next unused `FALSIFY-BOUNDARY-0NN` id. Update
    `door_surface.cost_axes`'s C-07 entry to name the real key, REPLACING the `unbounded_pending_06_15`
    marker 06-14 shipped (and dropping `no_structural_maximum: true`, which recorded why a measured
    disposition was never available for this axis). Add the binding row and update the Phase 6 header's
    row count.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib types::tests forecast::tests > /tmp/p06-15-t2a.log 2>&1; rc=$?; tail -20 /tmp/p06-15-t2a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit. A failure in `cost_bounds_match_contract` names the constant and the key that disagree; a failure in `every_cost_axis_names_a_real_bound` means C-07's `cost_axes` entry names a `constants:` key that does not exist; a failure in the at-the-bound case means the comparison was written as `>=`</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e::refuses_holiday_name_over_length_bound e2e::accepts_holiday_name_at_length_bound > /tmp/p06-15-t2b.log 2>&1; rc=$?; tail -8 /tmp/p06-15-t2b.log; test $rc -eq 0 && grep -q '2 passed' /tmp/p06-15-t2b.log</automated>
    <fails_when>non-zero exit, or fewer than `2 passed` — a name does not exist and the filter matched nothing, so neither side of the bound was actually driven through the transport</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-15-t2c.log 2>&1; rc=$?; tail -10 /tmp/p06-15-t2c.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-15-t2c.log</automated>
    <fails_when>non-zero exit, or the log does not carry `0 error(s)` — the new equation is malformed or nested at the wrong level. Fix the contract; do not route around `pv` with a shell script (CLAUDE.md)</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/types.rs` declares `pub const MAX_HOLIDAY_NAME_LEN: usize` with a
      doc comment stating the committed maximum it was derived against and the ~2 000:1 amplification
      it bounds
    - `contracts/forecast-tool-boundary-v1.yaml` `constants:` carries `fit_max_holiday_name_len`, and
      `cost_bounds_match_contract` asserts the two are equal
    - `grep -c 'max_holiday_name_len' crates/aprender-forecast/src/forecast.rs` is at least 1, and the
      refusal is inside the holiday loop — its line number is between the loop's opening `for h in` and
      the post-loop product checks. Quote the three line numbers in the SUMMARY
    - A multi-byte UTF-8 case whose char count is under the bound and whose byte length is over it is
      refused, and that case exists as a named test
    - The RED observation is recorded: constant raised -> over case ACCEPTED through the live server ->
      restored -> refused
    - The mirror mutation is recorded: `fit_max_holiday_name_len` changed in the YAML alone ->
      `cost_bounds_match_contract` red naming the key -> restored -> green
    - `door_surface.cost_axes`'s C-07 entry names `fit_max_holiday_name_len` and no longer carries the
      pending marker or `no_structural_maximum: true`, and `every_cost_axis_names_a_real_bound` passes
  </acceptance_criteria>
  <done>The one caller-settable field with no enforcement anywhere is bounded at the door, on the quantity that actually costs (bytes), with both sides controlled, the red side observed, and the contract owning the number.</done>
</task>

<task type="auto">
  <name>Task 3: C-08 — measure the unbudgeted NeuralProphet training path, then bound it or record why it needs no bound</name>
  <files>crates/aprender-forecast/src/np.rs, crates/aprender-forecast/src/forecast.rs, crates/aprender-forecast/src/types.rs, contracts/forecast-tool-boundary-v1.yaml, contracts/aprender/binding.yaml, crates/aprender-mcp-forecast/src/lib.rs</files>
  <reversibility rating="costly">If the measurement forces a bound, it refuses NeuralProphet requests that are accepted today at the extreme of the legal range. Reversible in code; client-visible, so the contract takes a bump. The MEASUREMENT itself is free and one-way in the useful sense — once taken, the disposition is determined by the number rather than by judgement.</reversibility>
  <read_first>
    - crates/aprender-forecast/src/np.rs — `NpData::new` in full (the imputed daily grid and
      `n_train_grid`), `auto_epochs`, `auto_batch`, `NpModel::new` and `n_params`, and `train`'s
      `samples` / `n` / `batch` / `epochs` / `n_batches` / `total_steps` block
    - crates/aprender-forecast/src/forecast.rs — the whole `"neuralprophet"` match arm: the `freq`
      refusal, the `n_lags` refusals, the `NpData::new` call, the learning-rate sweep and its
      `TrainConfig`, and the prediction path
    - crates/aprender-forecast/src/fit.rs — `FIT_BUDGET_SECS` and its doc comment, to confirm by
      reading rather than by assumption that nothing on the NP path consults it
    - crates/aprender-forecast/src/types.rs — `MAX_POINTS`, `MAX_SPAN_DAYS`, and the doc-comment style
      every bound here follows
    - .planning/phases/06-native-time-series-forecasting-stack/06-04-SUMMARY.md — what the NP parity
      ladder asserts and on which series, so the bound cannot be set below a rung's own geometry
    - .planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md — the re-derived C-08
      entry and the `unbounded_pending_06_15` marker 06-14 shipped it with
  </read_first>
  <action>
    (1) NAME THE PROXY ONCE, IN `np.rs`. Add `pub fn train_cost(n_samples: usize, epochs: usize,
    n_lags: usize) -> u64` (or the signature the reading supports) returning the door-computable work
    proxy — steps times per-sample feature width, i.e. on the order of
    `epochs * n_samples * (n_lags + 1)` — with a doc comment saying plainly that it is a PROXY for
    multiply-accumulates and not a wall-clock prediction, and that its only job is to be the same
    number the door checks and the trainer spends. Have `train` derive or assert its own
    `total_steps`-based figure against it, so the two cannot drift; a `debug_assert` is enough if a
    hard assert would cost anything on the hot path. The door multiplies by the learning-rate sweep
    width (2 or 3) because it, not `train`, is what runs the sweep.

    (2) MEASURE THE STRUCTURAL MAXIMUM, WITH A HARNESS WHOSE OUTPUT CAN BE GATED. Add an
    `#[ignore]`d `np_train_wall` harness to `crates/aprender-forecast/src/np.rs` (or the nearest
    existing test module) reading an `NP_WALL_MODE` env selector with at least `structural_max`,
    `mid_range` and `lag_free`, and printing ONE machine-parsable line per run:
    `NP TRAIN WALL: mode=<m> points=<p> span_days=<s> n_lags=<l> horizon=<h> train_cost=<c>
    outcome=<accepted|refused> total_s=<T> profile=<debug|release>`. The `outcome=` field is
    load-bearing: after step (3) a bounded request is REFUSED rather than slow, and the post-condition
    gate must be able to tell those two apart from the same line. On a RELEASE build measure the worst
    LEGAL NeuralProphet request — `MAX_POINTS`-scale history at a span approaching `MAX_SPAN_DAYS` (so
    `n_train_grid` is at its ceiling), `n_lags` at 365, `horizon` at `MAX_HORIZON`, `freq: "D"`, the
    only freq this arm accepts — plus a mid-range point and the lag-free arm, so a single number is not
    the whole basis (CLAUDE.md rule 6 — one input is an anecdote). Append all three to
    `/tmp/p06-15-t3-np-wall.log`. If the runs are long, run them detached and read the FILE; never read
    a wall through a pipe (CLAUDE.md rule 1 — `cmd > log 2>&1; rc=$?`).

    (3) DECIDE FROM THE NUMBER, NOT FROM PREFERENCE.
      - If the structural maximum exceeds the 2 s SC1 bar: add `MAX_NP_TRAIN_COST` to `types.rs` and
        `fit_max_np_train_cost` to `constants:`, refuse at the door immediately after
        `NpData::new` and the `n_lags` checks and BEFORE the first `np::train`, with a message naming
        the observed cost, its factors, the key and what to reduce (history length, span, `n_lags`).
        Set the value from the measurement — the largest round value whose measured wall clears 2 s at
        no fewer than three compositions, written into the doc comment the way
        `MAX_HOLIDAY_DESIGN_COST`'s three compositions are. Verify the NP parity ladder still passes:
        those rungs run Peyton with `n_lags` 0 and 30, and a bound that refuses them is wrong.
      - If the structural maximum clears 2 s: add NO bound. Record C-08 in `door_surface.cost_axes` as
        `bound: measured_at_structural_maximum` with `measured_seconds` set to the measured figure and
        a `measured_at` note naming the host and the profile. That is an honest closure of the axis and
        `every_cost_axis_names_a_real_bound` accepts it.
      In EITHER case say in the SUMMARY which branch was taken and quote the measurements that decided
      it. A bound invented to tidy the table refuses real requests for no measured reason; an axis left
      open because measuring looked expensive is round 4.

    (4) IF A BOUND WAS ADDED: the full treatment, same as every other bound in this phase. Mirror row
    in `cost_bounds_match_contract` with an observed YAML-only mutation; two-sided library control;
    an e2e pair through `serve()` with the RED side observed under a temporarily raised constant; an
    `np_train_cost_bounded` equation with `proof_obligations` and the next unused
    `FALSIFY-BOUNDARY-0NN`; a binding row; the Phase 6 header row count updated. IF NO BOUND WAS ADDED:
    add the equation anyway, stating the axis, the structural maximum, the measurement and the reason
    no door check exists — an axis whose closure is a measurement still deserves to be falsifiable, and
    its FALSIFY entry's `test:` is the sweep case 06-16 adds for the NP arm.

    (5) CLEAR THE PENDING MARKER AND TURN THE DECLARED-RED TEST GREEN. After Tasks 2 and 3, no
    `door_surface.cost_axes` entry may carry `unbounded_pending_06_15`. Run BOTH
    `no_cost_axis_is_pending` — which 06-14 deliberately shipped RED naming C-07 and C-08 — and
    `every_cost_axis_names_a_real_bound`, and record that the first has gone from red to GREEN and the
    second still passes on a now-complete list. That RED-to-GREEN transition is the observable moment
    the class invariant's second half becomes true rather than pending, and it is also what returns the
    crate's `--lib` suite to fully green; the SUMMARY should say so in exactly those terms, quoting
    06-14's failing output beside this plan's passing one.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-15-t3a.log 2>&1; rc=$?; tail -20 /tmp/p06-15-t3a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit. Watch specifically for `np::parity` — a NeuralProphet bound set below a parity rung's own geometry refuses the ladder's own request, and the ladder is the thing that must not move. Watch also for `types::tests::no_cost_axis_is_pending`, which 06-14 shipped RED and which stays red until BOTH C-07's and C-08's markers are replaced; note this command is run WITHOUT the `--skip` 06-14 used, because after this plan the whole suite must be green</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib types::tests::every_cost_axis_names_a_real_bound types::tests::no_cost_axis_is_pending -- --nocapture > /tmp/p06-15-t3b.log 2>&1; rc=$?; tail -12 /tmp/p06-15-t3b.log; test $rc -eq 0 && grep -q '2 passed' /tmp/p06-15-t3b.log</automated>
    <fails_when>non-zero exit, or fewer than `2 passed`. A failure in `no_cost_axis_is_pending` names the axis whose marker was not replaced — this is the test 06-14 shipped RED, and this plan is the only thing that clears it, so a red here means the round's central open item is still open. A failure in `every_cost_axis_names_a_real_bound` names an axis claiming a `constants:` key that does not exist. Fewer than 2 passed means a filter matched nothing</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-15-t3c.log 2>&1; rc=$?; tail -15 /tmp/p06-15-t3c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — if a NeuralProphet bound was added, the most likely cause is that `neuralprophet_lag_free_happy_path` now exceeds it, which means the bound is below a shipped happy path and is wrong</fails_when>
    <automated>test -f /tmp/p06-15-t3-np-wall.log && test "$(find /tmp/p06-15-t3-np-wall.log -newermt '-6 hours' | wc -l)" -eq 1 && n=0; bad=0; while read -r line; do case "$line" in *profile=release*) ;; *) echo "NOT RELEASE: $line"; bad=1; continue;; esac; t=$(printf '%s\n' "$line" | awk '{for(i=1;i<=NF;i++) if($i ~ /^total_s=/){sub(/^total_s=/,"",$i); print $i; exit}}'); case "$t" in ''|*[!0-9.]*|*.*.*) echo "NOT A NUMBER: total_s=$t in $line"; bad=1; continue;; esac; n=$((n+1)); done < <(grep '^NP TRAIN WALL: ' /tmp/p06-15-t3-np-wall.log); echo "np_walls=$n bad=$bad"; test "$n" -ge 3 && test "$bad" -eq 0</automated>
    <fails_when>`bad=1` in the echoed summary (a wall measured on a debug profile, or a `total_s` token that is empty / non-numeric / multi-dotted), `np_walls` below 3 (fewer than the structural maximum, a mid-range point and the lag-free arm were measured — one input is an anecdote, CLAUDE.md rule 6), or the freshness check failing because `/tmp/p06-15-t3-np-wall.log` is older than six hours, which means a stale log from an earlier attempt is being read as this run's evidence. This command checks the SHAPE of every number the C-08 disposition rests on; the next command checks the POST-CONDITION those numbers are supposed to establish</fails_when>
    <automated>NP_WALL_MODE=structural_max CARGO_INCREMENTAL=0 cargo test --release -p aprender-forecast --lib np_train_wall -- --ignored --nocapture > /tmp/p06-15-t3-np-post.log 2>&1; rc=$?; tail -20 /tmp/p06-15-t3-np-post.log; line=$(grep -m1 '^NP TRAIN WALL: ' /tmp/p06-15-t3-np-post.log); echo "$line"; case "$line" in *outcome=refused*) exit 0;; esac; t=$(printf '%s\n' "$line" | awk '{for(i=1;i<=NF;i++) if($i ~ /^total_s=/){sub(/^total_s=/,"",$i); print $i; exit}}'); case "$t" in ''|*[!0-9.]*|*.*.*) echo "FAIL: total_s=$t is not a number"; exit 1;; esac; awk -v v="$t" 'BEGIN{exit (v+0 < 2.0) ? 0 : 1}'</automated>
    <fails_when>a non-zero exit from the final `awk`, meaning the worst LEGAL NeuralProphet request is neither refused at the door nor under 2.000 s — which is the post-condition C-08's whole disposition claims. A `total_s=` that is not a number, or a missing `NP TRAIN WALL:` line, also fails here and is named. This is the check the marker-counting form could not make: it separates a wall under the bar from a wall over it</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-15-t3d.log 2>&1; rc=$?; tail -20 /tmp/p06-15-t3d.log; test $rc -eq 0 && test "$(grep -c 'Total equations:' /tmp/p06-15-t3d.log)" -ge 4 && test "$(grep -c 'BIND-' /tmp/p06-15-t3d.log)" -eq 0</automated>
    <fails_when>non-zero exit, or fewer than four `Total equations:` summaries. Any `BIND-` or `RESOLVE-` line means a new equation has no binding row, or a row's `function` does not resolve to a definition site in the file its `module_path` names</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/np.rs` declares `train_cost` and `train` cannot silently disagree
      with it: `grep -n 'fn train_cost' crates/aprender-forecast/src/np.rs` prints exactly one line
      (quote its number), and `train_cost` is
      referenced from both `np.rs` and `crates/aprender-forecast/src/forecast.rs`
    - At least three release-profile NeuralProphet walls (structural maximum, a mid-range point, the
      lag-free arm) are recorded in the SUMMARY, each with its `profile=release` marker and its
      computed `train_cost`
    - The SUMMARY states explicitly which branch of step (3) was taken and quotes the number that
      decided it
    - If a bound was added: `types.rs` declares the constant, `constants:` carries the key,
      `cost_bounds_match_contract` asserts equality, the YAML-only mutation control is recorded, and an
      e2e pair with an observed RED side exists
    - If no bound was added: C-08's `cost_axes` entry carries `bound: measured_at_structural_maximum`,
      a `measured_seconds` under 2.0, and a note naming the host and profile
    - No `door_surface.cost_axes` entry contains the pending marker; `no_cost_axis_is_pending` has gone
      from the RED 06-14 shipped to GREEN, and `every_cost_axis_names_a_real_bound` still passes. Quote
      06-14's failing output and this plan's passing output side by side in the SUMMARY
    - `cargo test -p aprender-forecast --lib` is run WITHOUT any `--skip` and is fully green — the
      declared-red state 06-14 introduced is closed by this plan and by nothing else
    - `np::parity` and `prophet::parity` both report the same counts as before this plan, and
      `git diff --stat ce3e5a8ea..HEAD` (the review's own diff base, pinned explicitly so a COMMITTED
      change cannot make the check pass vacuously) shows no change under
      `crates/aprender-forecast/tests/fixtures/` or to a numeric tolerance in any `*-parity-v1.yaml`
  </acceptance_criteria>
  <done>The one cost axis in this door with no budget of any kind has been measured at its structural maximum on a release build and dispositioned by that number — bounded at the door, or recorded as measured-safe with the figure. The enumeration's axis list carries no pending marker, so the class invariant's second half is satisfied rather than promised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers, default K=8) | untrusted, unauthenticated JSON-RPC `tools/call` arguments reach the holiday loop and the NeuralProphet training sweep |
| MCP client -> stdio | the same door behind a different transport; the transport re-checks nothing (D-11) |
| request payload -> derived allocations | one payload occurrence of a holiday name becomes ~2 000 owned copies; one holiday list becomes ~8 MB of parsed dates before the aggregate refusal at HEAD |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. The
genuine ASVS L1 surface is INPUT VALIDATION and RESOURCE EXHAUSTION.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-35 | Denial of Service | `prophet::columns`, reached with an unbounded `holidays[].name` | **high** | mitigate | One payload occurrence of a name becomes two owned `String` clones per design column, up to `MAX_HOLIDAY_COLUMNS` (1 000) columns, and then the sort key of an `O(C log C)` byte-wise comparison sort. A ~2 000:1 amplification off a field with NO enforcement at HEAD. Task 2 bounds it at the door, in bytes, with a non-ASCII case proving the bound is on the quantity that costs |
| T-06-36 | Denial of Service | `np::train`, driven 2-3x per request by the door's learning-rate sweep, with no budget on the path | **high** | mitigate | `fit::FIT_BUDGET_SECS` is a Prophet L-BFGS construct; nothing on the NeuralProphet path reads it. `n_train_grid` is bounded by `MAX_SPAN_DAYS` (20 000) rather than `MAX_POINTS`, `n_lags` reaches 365, and the sweep multiplies the whole thing by 2 or 3. Task 3 MEASURES the structural maximum on release and either bounds it at the door or records the measurement that makes a bound unnecessary — the disposition is decided by the number |
| T-06-37 | Denial of Service | the holiday loop's late aggregate refusal | medium | mitigate | ~1 000 000 `parse_date` calls and ~8 MB of `Vec<i64>` are spent AFTER the door already has the information to refuse. Roughly 1:1 with the ~11 MB payload, so amplification is low — but it is unbounded work performed on a request already known to be bad, on exactly the surface this phase exists to bound. Task 1 moves the check into the loop and proves the position with a test that would answer differently if it moved back |
| T-06-38 | Information Disclosure | the holiday-name refusal message | low | mitigate | Echoing an oversized name back into an error string re-materialises the very bytes the bound refuses, and reflects attacker-controlled content into logs. Task 2's message names the holiday's position and the observed LENGTH, never the name |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; every symbol used is already a dependency of these crates. No new dependency enters the graph, so the package-legitimacy gate is not engaged. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib` — 0 failed; `prophet::parity` unmoved at 32/0 and `np::parity`
  at its pre-plan count
- `cargo test -p aprender-forecast --lib types::tests::every_cost_axis_names_a_real_bound types::tests::no_cost_axis_is_pending` — `2 passed`
  on a marker-free axis list; the second of those was RED at the close of wave 12
- `cargo test -p aprender-mcp-forecast --lib e2e::` — 0 failed, including every new straddling pair
- `cargo test -p aprender-mcp-forecast --test e2e_stdio` — 0 failed (dark in CI; run deliberately)
- `pv validate contracts/forecast-tool-boundary-v1.yaml` — `0 error(s)`;
  `pv status contracts/forecast-tool-boundary-v1.yaml` lists every new equation
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings`
  and `cargo fmt --all -- --check` — rc=0
- At least three release-profile NeuralProphet walls, each printed with `profile=release`
</verification>

<success_criteria>
- The two axes 06-14's enumeration found and no review pointed at are closed, each by measurement
  rather than by assertion
- `door_surface.cost_axes` carries no pending marker; `no_cost_axis_is_pending` has gone RED -> GREEN,
  and the crate's `--lib` suite needs no `--skip` to be green
- WR-03's check is where its comment always said it was, proven by position and not by presence
- Every new bound has a near-miss positive control and an observed red side
- No parity fixture, parity tolerance or `*-parity-v1.yaml` numeric was touched, and both ladders
  report their pre-plan counts
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-15)

**New Rust constants** (`crates/aprender-forecast/src/types.rs`): `MAX_HOLIDAY_NAME_LEN`; and
`MAX_NP_TRAIN_COST` ONLY IF Task 3's measurement forces it — whether this symbol exists is decided by
a measurement, and the SUMMARY records which.

**New Rust functions**: `aprender_forecast::np::train_cost`.

**New test names**:
`forecast::tests` — `the_aggregate_dates_refusal_fires_inside_the_holiday_loop`,
`a_holiday_name_over_the_length_bound_is_refused`, `a_holiday_name_at_the_length_bound_is_accepted`,
the multi-byte UTF-8 byte-length case, and (conditionally) the NeuralProphet cost pair;
`aprender-mcp-forecast` `mod e2e` — `refuses_holiday_dates_total_before_parsing_the_rest`,
`refuses_holiday_name_over_length_bound`, `accepts_holiday_name_at_length_bound`, and (conditionally)
the NeuralProphet cost pair.

**New contract keys** (`contracts/forecast-tool-boundary-v1.yaml`):
`constants.fit_max_holiday_name_len`; `constants.fit_max_np_train_cost` (conditional);
`equations.holiday_name_cost_bounded`; `equations.np_train_cost_bounded`; their `proof_obligations`
entries; two new `falsification_tests` ids in the `FALSIFY-BOUNDARY-0NN` series (READ the file for the
next unused — 06-14 added two beyond HEAD's 018). `door_surface.cost_axes`'s C-07 and C-08 entries are
UPDATED, not created: 06-14 created them carrying the `unbounded_pending_06_15` marker (and, for C-07,
`no_structural_maximum: true`), which this plan replaces with real bounds.

**New binding keys** (`contracts/aprender/binding.yaml`): rows whose `equation:` values are
`holiday_name_cost_bounded` and `np_train_cost_bounded`; the Phase 6 block header's stated row count is
updated.

**New log token**: `profile=release` on the NeuralProphet wall lines this plan measures (the permanent
`SC1 WALL:` token is 06-16's).

**Symbols 06-14 created** (present after wave 12, not at HEAD — do not drift-check against HEAD):
`MAX_LOGISTIC_CHANGEPOINT_LAMBDA`, `prophet::changepoint_count`, `test_support::constant_f64`,
`test_support::contract_value`, `door_surface:` with `knobs:`/`cost_axes:`,
`constants.fit_max_logistic_changepoint_lambda`, `constants.poisson_normal_branch_lambda`,
`logistic_changepoint_cost_bounded`, `door_surface_is_complete`,
`every_request_knob_is_enumerated`, `every_cost_axis_names_a_real_bound`,
`pool_default_matches_contract`, `refuses_logistic_changepoint_lambda_over_bound`,
`accepts_logistic_changepoint_lambda_just_under_bound`, `no_cost_axis_is_pending` (shipped RED by
06-14; THIS plan is what turns it green), the `unbounded_pending_06_15` marker and the
`no_structural_maximum` flag.

**Symbols later round-3 plans create**: 06-16 — `crates/aprender-forecast/src/sc1_wall.rs`,
`just forecast-sc1-sweep`, the `SC1 WALL:` token, the shared numeric-shape validator script; 06-17 —
`variance_tolerance` on `poisson_sampler_domain`, `FALSIFY-PROPHET-014`.

Everything else named in this plan (`forecast`, `make_design`, `predict`, `columns`,
`holiday_day_sets`, `np::NpData`, `np::train`, `np::auto_epochs`, `np::auto_batch`, `parse_date`,
`MAX_HOLIDAY_DATES_TOTAL`, `MAX_HOLIDAY_COLUMNS`, `MAX_SPAN_DAYS`, `FIT_BUDGET_SECS`,
`cost_bounds_match_contract`, `refuses_holiday_dates_total_over_bound`,
`neuralprophet_lag_free_happy_path`, `serve`, `refused`, `assert_shared_shape`,
`contract-audit-phase6`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-15-SUMMARY.md` when done. Record:
the three line numbers proving the aggregate refusal now precedes the design-cost refusal and
`make_design`; the WR-03 position test's RED output before the move, quoted; the longest holiday name
in any committed fixture and the amplification arithmetic behind `MAX_HOLIDAY_NAME_LEN`; the
multi-byte UTF-8 case and what it proves; every RED observation (constant raised -> accepted ->
restored -> refused) and every YAML-only mirror mutation, with output; the three-or-more release
NeuralProphet walls with their `profile=release` markers and computed `train_cost`; WHICH branch of
Task 3 step (3) was taken and the number that decided it; the output of
`no_cost_axis_is_pending` going from 06-14's RED to GREEN (both outputs quoted) and
`every_cost_axis_names_a_real_bound` on the now-complete axis list; `np::parity` and `prophet::parity`
counts before and after; `pv diff`'s suggested semver bump (two real filesystem paths, never a git
revision) and the contract version before/after; and the Review Dispositions Ledger with WR-03 marked
closed.
</output>
