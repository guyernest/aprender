---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 12
type: execute
wave: 10
gap_closure: true
depends_on: [06-11]
files_modified:
  - crates/aprender-mcp-forecast/src/lib.rs
  - contracts/forecast-tool-boundary-v1.yaml
  - contracts/aprender/binding.yaml
  - justfile
autonomous: true
requirements: [SC1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The design-cost bar is asserted THROUGH THE SERVER, as every other SC1 refusal is: a live streamable-HTTP request whose every individual bound is satisfied and whose product is not is refused with a validation-class error naming the fix"
    - "The bar was shown to turn RED, not merely added: with `MAX_HOLIDAY_DESIGN_COST` temporarily raised above the case's cell count, the new e2e case is OBSERVED failing (the server accepts and fits), and that output is recorded before the constant is restored"
    - "The bar refuses only what it claims: a near-miss request one step under the bound is ACCEPTED through the same server and returns a full response shape, so the two-sided control is at the boundary rather than an order of magnitude away"
    - "`contracts/forecast-tool-boundary-v1.yaml` OWNS the rule: a `holiday_design_cost_bounded` equation, a proof obligation, and a FALSIFY-BOUNDARY entry naming the unit and e2e assertions that falsify it — so the HARD-ceiling claim at line 102 is now backed by a constant, a check and a test rather than by prose"
    - "`make contract-audit-phase6` reports zero BIND- and zero RESOLVE- findings over four contracts with the new equation bound to a real definition site — the audit's resolver must accept the new row, not merely its registry entry"
    - "`just forecast-holiday-bench` is a GATE, not a printout: it asserts the measured end-to-end wall of an at-the-bound holiday request is under SC1's 2 s bar, captures `rc` from a redirect rather than through a pipe, and fails hard when no measurement line was produced"
  artifacts:
    - path: "crates/aprender-mcp-forecast/src/lib.rs"
      provides: "the e2e over-cost refusal and the near-miss acceptance through a live server"
      contains: "refuses_holiday_design_cost_over_bound"
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "equation holiday_design_cost_bounded + a proof obligation + FALSIFY-BOUNDARY-018"
      contains: "holiday_design_cost_bounded"
    - path: "contracts/aprender/binding.yaml"
      provides: "a binding row resolving holiday_design_cost_bounded to a real definition site"
      contains: "holiday_design_cost_bounded"
    - path: "justfile"
      provides: "the 2 s SC1 assertion added to forecast-holiday-bench"
      contains: "forecast-holiday-bench"
  key_links:
    - from: "crates/aprender-mcp-forecast/src/lib.rs"
      to: "crates/aprender-forecast/src/forecast.rs"
      via: "the e2e case reaches the SAME door the library caller reaches — the transport re-checks nothing"
      pattern: "aprender_forecast::forecast"
    - from: "contracts/forecast-tool-boundary-v1.yaml"
      to: "crates/aprender-forecast/src/forecast.rs"
      via: "binding.yaml row holiday_design_cost_bounded -> aprender_forecast::forecast::forecast, resolved to a definition site by contract-audit-phase6"
      pattern: "holiday_design_cost_bounded"
    - from: "justfile"
      to: "crates/aprender-forecast/src/prophet.rs"
      via: "the recipe parses the single HOLIDAY DESIGN WALL: line the ignored release test prints and asserts total_s < 2.0"
      pattern: "HOLIDAY DESIGN WALL"
  prohibitions:
    - statement: "Never assert the wall inside a libtest assertion — the bar lives in the host-gated `just` recipe (REVIEW-06-04 precedent, and the same reasoning that moved the pool ratio out of `pool_equality`)"
      status: resolved
      verification: test
    - statement: "Never read `$?` through a pipe in the recipe — capture rc from a redirect. This shipped twice in this repo (#2336, #2360) and both times made a fail-the-job step unreachable"
      status: resolved
      verification: test
    - statement: "Never write a yq/python/bash script that re-implements what `pv` already does — `pv validate` / `pv status` / `pv diff` are the dogfooded tools and a shell replacement is muda (CLAUDE.md)"
      status: resolved
      verification: judgment
    - statement: "Do not touch crates/aprender-compute, .github/workflows/*.yml, or any parity fixture — scope fence for this gap-closure run"
      status: resolved
      verification: judgment
---

<objective>
Close the second half of VERIFICATION gap 2: prove the new design-cost bar through the server, give it
a contract identity, and turn the measurement recipe into a repeatable SC1 gate.

Purpose: 06-11 added the bound and proved it at the library door. The verifier's third `missing`
bullet is explicit that the bar "must be shown to turn RED, not just added", and every other SC1
refusal in this phase is asserted through a live streamable-HTTP server — a bound proven only in a
unit test is a bound the transport could still be bypassing.
Output: two e2e cases at the boundary, one contract equation with its obligation and falsification
test, one binding row that RESOLVES, and a `just` recipe that fails when the 2 s bar is missed.
</objective>

<gap_closure_context>
Source: `06-VERIFICATION.md` gap 2, `missing` bullet 3 — "One e2e case proving an over-cost request is
refused (the bar must be shown to turn RED, not just added)" — plus the contract half of bullet 1
(the equation and falsification test that keep `constants.fit_max_holiday_design_cost` honest; 06-11
landed the constant itself and the `cost_bounds_match_contract` assertion).

06-11 must be complete before this plan runs: `MAX_HOLIDAY_DESIGN_COST` and `MAX_HOLIDAY_DATES_TOTAL`
must exist in `crates/aprender-forecast/src/types.rs`, both refusals must be at the door in
`crates/aprender-forecast/src/forecast.rs`, and `just forecast-holiday-bench` must already print one
`HOLIDAY DESIGN WALL:` line. That is why this is a separate wave, not a separate task: the e2e suite
lives in `crates/aprender-mcp-forecast/src/lib.rs`, a file 06-11 does not touch, and the two plans
share `contracts/forecast-tool-boundary-v1.yaml` and `justfile`.

This plan does NOT modify, supersede or re-create 06-01 … 06-09 or their SUMMARYs.
</gap_closure_context>

<spec_and_probe_status>
No `06-SPEC.md` exists, so there are no SPEC `## Edge Coverage` / `## Prohibitions` sections to lift;
the spec-less probe fallback was SKIPPED because `phase_req_ids` is null and the probe had no input.
`must_haves` are derived from the phase goal, SC1 and the verifier's `missing` list. Recorded, not
silently omitted.
</spec_and_probe_status>

<assumption_delta_decision>
Primary noun: `forecast` (unchanged). Decision: `no-change`. Rationale: the detector's three advisory
`pluralization` signals are incidental prose (wave scheduling, a refuted cargo-CLI claim, a build-lock
precondition); none is a singular→plural, required→optional or derived→chosen transition in the
identity model, and this plan adds assertions and a contract block to an existing single door.
</assumption_delta_decision>

<human_items_still_open>
This plan does not touch the surface of any of the three `human_verification` items and closes none of
them. The browser MCP handshake + chart rendering check, the x86_64 `quantiles_abs_f32_nonaarch64`
measurement, and the SC4-dark-in-CI decision all REMAIN OPEN.
</human_items_still_open>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md  (gap 2 only)
@.planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md  (the chosen constant values, the attribution table and the at-the-bound walls this plan builds its cases from)
@.planning/phases/06-native-time-series-forecasting-stack/06-10-SUMMARY.md  (the e2e refusal-case shape landed one wave earlier)
@CLAUDE.md  (Verification Discipline rules 1/2/4/6/8; DOGFOOD `pv`; clippy/unwrap rules)

<interfaces>
Server-side e2e helpers already present in `crates/aprender-mcp-forecast/src/lib.rs` `mod e2e`:
  - `fn synth(n: usize) -> (Vec<String>, Vec<f64>)`            (:444)
  - `fn valid_args() -> serde_json::Value`                     (:457) — 60 points, horizon 7
  - `fn with(extra: serde_json::Value) -> serde_json::Value`   (:499) — one perturbation per case
  - `async fn refused(c: &mut Client, args, needle: &str)`     (:464) — asserts error-ness, the needle,
    the pinned `REFUSAL_CODE` and the `VALIDATION_PREFIX`; a new case adds no assertions of its own
  - `serve()` — the in-process streamable-HTTP server every case drives
  - existing happy paths to copy for the ACCEPT control's response-shape assertions:
    `prophet_logistic_with_holidays_happy_path`, `prophet_ms_multiplicative_happy_path`

`HolidayArg` JSON shape (crates/aprender-forecast/src/types.rs:60-73):
  `{"name": "...", "dates": ["YYYY-MM-DD", ...], "lower_window": <i64 <= 0>, "upper_window": <i64 >= 0>}`
  Column count for one holiday is `upper_window - lower_window + 1`.

Bounds the over-cost case must individually SATISFY while exceeding only the product (values in
`crates/aprender-forecast/src/types.rs`): `MIN_POINTS` 10, `MAX_POINTS` 20 000, `MAX_HORIZON` 3 650,
`MAX_SPAN_DAYS` 20 000, `MAX_HOLIDAY_WINDOW` 365, `MAX_HOLIDAY_COLUMNS` 1 000, `MAX_HOLIDAY_DATES`
1 000, plus `MAX_HOLIDAY_DATES_TOTAL` (added by 06-11). The refusal under test is on
`(points + horizon) x holiday_columns` vs `MAX_HOLIDAY_DESIGN_COST`.

Contract shapes in `contracts/forecast-tool-boundary-v1.yaml`:
  - `equations:` entries use `formula` / `domain` / `codomain` / `invariants` (+ optional
    `preconditions`); the nearest sibling is `fit_server_bounds` (:94-113), whose third invariant at
    line 102 is the HARD-ceiling claim
  - `proof_obligations:` entries use `type` / `property` / `formal` / `applies_to`
  - `falsification_tests:` entries use `id` / `rule` / `prediction` / `test` / `if_fails`. 06-10 added
    `FALSIFY-BOUNDARY-017`, so the next id is `FALSIFY-BOUNDARY-018` — READ the file rather than
    trusting this sentence, and take the next id after the highest actually present

Binding row shape (contracts/aprender/binding.yaml:1897-1903): `contract:` bare filename, `equation:`,
`module_path:`, `function:`, `signature:` (quoted), `status: implemented`, `notes:`.
`make contract-audit-phase6` RESOLVES `function` to a definition site in the file `module_path` maps
to (`aprender_forecast::forecast` -> `crates/aprender-forecast/src/forecast.rs`), so the function must
exist there.

Recipe to extend: `forecast-holiday-bench` in the `justfile`, added by 06-11. It runs the ignored
release test `prophet::design_cost::holiday_design_wall` and prints exactly one `HOLIDAY DESIGN WALL:`
line with fields `points= columns= dates= horizon= cells= triple= total_s= fit_s= predict_s= other_s=
arch= profile=`. The bar-assertion idiom to copy is `forecast-bench` (justfile:659-685): an `awk`
comparison, a hard failure with the measured number in the message, and a hard failure when the line
is absent ("the bar was never measured").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert the design-cost bar THROUGH THE SERVER — over-bound refusal and near-miss acceptance, RED side observed</name>
  <files>crates/aprender-mcp-forecast/src/lib.rs</files>
  <read_first>
    - crates/aprender-mcp-forecast/src/lib.rs lines 430-760 — `mod e2e` in full: `synth`, `valid_args`,
      `refused`, `with`, `serve`, the 24 `refuses_*` cases (22 from 06-06 plus the two 06-10 added) and
      the happy paths whose response-shape assertions the ACCEPT control copies
    - crates/aprender-forecast/src/forecast.rs — the two refusals 06-11 added, so the needles this task
      matches are the real message substrings and not a paraphrase
    - crates/aprender-forecast/src/types.rs — the values of `MAX_HOLIDAY_DESIGN_COST` and
      `MAX_HOLIDAY_DATES_TOTAL` as 06-11 chose them; the case geometry is derived from THESE numbers,
      not from any number written in a plan
    - .planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md — the at-the-bound
      configurations already measured, so the near-miss case can reuse one that is known fast
  </read_first>
  <action>
    (1) DERIVE THE TWO GEOMETRIES FROM THE ACTUAL CONSTANT. Read `MAX_HOLIDAY_DESIGN_COST` out of
    `crates/aprender-forecast/src/types.rs` and pick:
      - an OVER geometry: `points`, `horizon` and a single holiday whose `lower_window`/`upper_window`
        give `columns`, such that `(points + horizon) * columns` is strictly greater than the bound
        while `points <= MAX_POINTS`, `horizon <= MAX_HORIZON`, the ds span is within `MAX_SPAN_DAYS`,
        `|lower_window|` and `upper_window` are each within `MAX_HOLIDAY_WINDOW`, `columns <=
        MAX_HOLIDAY_COLUMNS`, the holiday's date count is within `MAX_HOLIDAY_DATES` and the total is
        within `MAX_HOLIDAY_DATES_TOTAL`. EVERY individual bound satisfied, ONLY the product over —
        that is the whole point of the case, and a case that trips a neighbouring bound proves nothing.
      - an UNDER geometry: the same shape with the product strictly below the bound, chosen from a
        configuration 06-11 already measured as fast so this test does not become the slowest in the
        suite.
    Prefer the smallest `points` that reaches the geometry: the refusal fires at the door before any
    design is built, so the OVER case pays only JSON assembly and date parsing, and a large `columns`
    with a modest `points` keeps the request payload small. Record both geometries and their computed
    cell counts in the SUMMARY, alongside the constant they were derived from.

    (2) ADD THE TWO CASES, beside the other `refuses_*` cases:
      - `refuses_holiday_design_cost_over_bound` — `#[tokio::test]`, builds the OVER geometry (its own
        `ds`/`y` via `synth(points)` and an explicit `holidays` array), and calls `refused(&mut
        serve().await, args, "max_holiday_design_cost").await`. The shared helper already asserts
        error-ness, the pinned `REFUSAL_CODE` and the `VALIDATION_PREFIX`, so validation-class-ness
        comes for free — do not re-assert it.
      - `accepts_holiday_design_cost_just_under_bound` — `#[tokio::test]`, builds the UNDER geometry,
        calls the tool, and asserts the FULL response shape the way the existing happy paths do:
        `isError` absent or false, `yhat` / `yhat_lower` / `yhat_upper` / `trend` present with length
        equal to the horizon, `components` present, and `diagnostics` present. A near-miss that merely
        "does not error" is not a control; it has to come back with the shape a caller was promised.
    Add a third case ONLY if `MAX_HOLIDAY_DATES_TOTAL` is not already exercised through the server:
    `refuses_holiday_dates_total_over_bound`, needle `max_holiday_dates_total`, same construction.
    Read the suite first and say in the SUMMARY whether it was needed.

    (3) OBSERVE THE RED SIDE, and record it. 06-11 already landed the door check, so the refusal case
    would be green on first run and would prove nothing about whether it can fail. Produce the RED
    observation by TEMPORARILY raising `MAX_HOLIDAY_DESIGN_COST` in
    `crates/aprender-forecast/src/types.rs` above the OVER geometry's cell count, running
    `cargo test -p aprender-mcp-forecast --lib e2e::refuses_holiday_design_cost_over_bound`, and
    observing it FAIL with `refused`'s own assertion text and a reply body carrying a forecast where a
    refusal was required. Note that this perturbation ALSO turns
    `types::tests::cost_bounds_match_contract` red — that is expected and is itself confirmation that
    06-11's contract binding is live; record both reds. Then restore the constant and observe both
    green. Quote the failing output verbatim in the SUMMARY. An assertion never seen failing is a
    green that measured nothing (CLAUDE.md Verification Discipline rule 4).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-12-t1a.log 2>&1; rc=$?; tail -12 /tmp/p06-12-t1a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-12-t1a.log > /dev/null</automated>
    <fails_when>non-zero exit — the over-bound request was accepted and fitted, the near-miss request was refused (the bound over-refuses), the near-miss response was missing a promised field, or an existing e2e case regressed; or no `test result: ok. N passed; 0 failed` line, which for a filtered run means the filter matched nothing</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib > /tmp/p06-12-t1b.log 2>&1; rc=$?; tail -8 /tmp/p06-12-t1b.log; N=$(grep -oE '[0-9]+ passed' /tmp/p06-12-t1b.log | head -1 | grep -oE '[0-9]+'); echo "passed=$N"; test $rc -eq 0 && test "${N:-0}" -ge 32</automated>
    <fails_when>non-zero exit, or the passed count is below 32 — the crate stood at 30 after 06-10 and this task adds at least 2, so a count under 32 means a case was replaced rather than added or the suite silently shrank</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib types::tests::cost_bounds_match_contract > /tmp/p06-12-t1c.log 2>&1; rc=$?; tail -6 /tmp/p06-12-t1c.log; test $rc -eq 0 && grep -E 'test result: ok\. 1 passed; 0 failed' /tmp/p06-12-t1c.log > /dev/null</automated>
    <fails_when>non-zero exit — the temporary constant raise used for the RED observation was not restored, so the Rust constant and its contract key now disagree; or the summary is not `1 passed; 0 failed`</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-12-t1d.log 2>&1; rc=$?; tail -8 /tmp/p06-12-t1d.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — a pedantic lint on the new async tests or a disallowed `unwrap()` (banned by .clippy.toml; use `expect()`). `--no-deps` is required because with deps `-D warnings` reaches the pre-existing redness in crates/aprender-compute (D-ITEM-06-09)</fails_when>
  </verify>
  <done>An in-bounds-except-for-the-product request is refused through a live streamable-HTTP server with a validation-class error naming `max_holiday_design_cost`; a near-miss request one step under the bound is accepted through the same server and returns the full promised response shape; the refusal case was OBSERVED failing under a temporarily raised constant and the constant was restored.</done>
  <acceptance_criteria>
    - `grep -n 'async fn refuses_holiday_design_cost_over_bound' crates/aprender-mcp-forecast/src/lib.rs` and `grep -n 'async fn accepts_holiday_design_cost_just_under_bound' crates/aprender-mcp-forecast/src/lib.rs` each print exactly one line number (read both and record them)
    - The SUMMARY records both geometries as numbers — `points`, `horizon`, `lower_window`, `upper_window`, derived `columns`, derived `(points + horizon) * columns` — beside the value of `MAX_HOLIDAY_DESIGN_COST` they were derived from, and shows the OVER product strictly above and the UNDER product strictly below it
    - The SUMMARY shows, for the OVER geometry, that every individual bound is satisfied: points vs `MAX_POINTS`, horizon vs `MAX_HORIZON`, span vs `MAX_SPAN_DAYS`, windows vs `MAX_HOLIDAY_WINDOW`, columns vs `MAX_HOLIDAY_COLUMNS`, dates vs `MAX_HOLIDAY_DATES`, total dates vs `MAX_HOLIDAY_DATES_TOTAL` — as a table with the observed and bound values side by side
    - The near-miss test asserts `yhat`, `yhat_lower`, `yhat_upper` and `trend` are all present with length equal to the horizon, and that `components` and `diagnostics` are present — the assertions are named in the SUMMARY
    - The SUMMARY quotes the RED observation verbatim: the raised constant value, the failing assertion text, the reply body fragment showing a forecast where a refusal was required, and the accompanying red of `types::tests::cost_bounds_match_contract`; then the restored value and both greens
    - `git diff crates/aprender-forecast/src/types.rs` is empty at the end of this task — the temporary raise was restored, not left in
    - The SUMMARY states whether a third case for `max_holiday_dates_total` was needed and why
  </acceptance_criteria>
  <reversibility rating="reversible">Two `#[tokio::test]` functions in an existing test module. Reverting is deleting them; nothing outside the test binary observes them, and the temporary constant perturbation is restored within the task.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Give the bound a contract identity — equation, obligation, FALSIFY entry, binding row, audit green</name>
  <files>contracts/forecast-tool-boundary-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - contracts/forecast-tool-boundary-v1.yaml — the `constants:` block including the two entries
      06-11 added; `fit_server_bounds` (:94-113) and especially its third invariant at line 102, the
      HARD-ceiling claim this equation makes true; the `proof_obligations:` block; the whole
      `falsification_tests:` block, to take the next unused `FALSIFY-BOUNDARY-0NN` id rather than
      assuming one
    - contracts/aprender/binding.yaml lines 1890-1935 — the existing forecast-tool-boundary rows, for
      key order and the bare-filename convention
    - Makefile, the `contract-audit-phase6` target — what it audits, what it counts, and the RESOLVE-
      step 06-09 added that maps a `module_path` to a file and requires a definition site for the row's
      `function`
    - .planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md — the derived constant
      values and the measured walls the invariants should cite
    - .planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md gap 2 — the measured
      numbers (16.113 / 8.926 / 2.664 s, 4.56e7 / 2.03e7 / 5.12e6 triple products, ~85 minutes at the
      in-bounds maximum, K=8 default pool)
  </read_first>
  <action>
    (1) EQUATION. Add `holiday_design_cost_bounded` to `equations:`, placed immediately after
    `fit_server_bounds` so it reads as the completion of that entry's line-102 claim. Its `formula`
    states that acceptance requires `(len(ds) + horizon) * holiday_columns <=
    fit_max_holiday_design_cost` AND `sum(len(h.dates)) <= fit_max_holiday_dates_total`; its `domain`
    is the fit server's holiday arguments together with the history length and horizon; its `codomain`
    is either a design build or a validation refusal naming the observed cost and the bound.

    Its `invariants` must state, in the file's own voice:
      - that the Rust constants are asserted EQUAL to `constants.fit_max_holiday_design_cost` and
        `constants.fit_max_holiday_dates_total` by `types::tests::cost_bounds_match_contract` — the
        contract is the source, the code is the mirror;
      - that the check runs BEFORE `make_design`, which is the only place it CAN run: `FIT_BUDGET_SECS`
        is a cooperative round-boundary budget entered inside `fit_prophet`, and the design build
        happens before that, so no budget setting could ever have seen this cost;
      - the MEASURED breach this closes — 3 000 points x 181 holiday columns x 84 holiday dates took
        16.113 s against SC1's 2 s bar at HEAD ce3e5a8ea, with 2 000x121x84 at 8.926 s and 1 000x61x84
        at 2.664 s, and the in-bounds maximum extrapolating to roughly 85 minutes for ONE request
        against a default pool of K=8;
      - that the three previously-isolated factors (`fit_max_points`, `fit_max_holiday_columns`,
        `fit_max_holiday_dates`) are each still enforced on their own and this bounds their
        composition, so a request must satisfy all of them AND this;
      - WHICH STATISTIC is bounded and why, in one sentence, taken from 06-11's measured statistic test
        — and if 06-11 concluded the literal triple product is the right statistic after all, state
        THAT instead. Do not write a justification the measurements do not support.

    (2) PROOF OBLIGATION. Add a `type: bound` entry whose `property` is that the work one holiday-
    carrying request can buy is bounded by a constant this file names, and whose `formal` reads
    `forall args: accept(args) implies (len(args.ds) + args.horizon) * holiday_columns(args) <=
    constants.fit_max_holiday_design_cost`, `applies_to: all`.

    (3) FALSIFICATION TEST. Add the next unused `FALSIFY-BOUNDARY-0NN` id (read the file; 06-10 added
    017). Its `prediction` NAMES every assertion that falsifies it:
    `types::tests::cost_bounds_match_contract` (the constants agree with this file),
    `forecast::tests::an_in_bounds_holiday_spec_whose_product_is_not_is_refused` and
    `forecast::tests::a_holiday_spec_just_under_the_design_cost_bound_is_accepted` (the two-sided
    library bar), and `e2e::refuses_holiday_design_cost_over_bound` /
    `e2e::accepts_holiday_design_cost_just_under_bound` (the same bar through the server). Its `test`
    is a runnable command pair over the two crates. Its `if_fails` says that the boundary is back to
    checking three factors in isolation while their product buys unbounded CPU from an unauthenticated
    caller, and that line 102's HARD-ceiling claim is false again.

    (4) VERSION. Materialise the pre-change file and let the dogfooded tool judge the change:
    `git show HEAD:contracts/forecast-tool-boundary-v1.yaml > /tmp/ftb-old-12.yaml` then
    `cargo run --release -p aprender-contracts-cli --bin pv -- diff /tmp/ftb-old-12.yaml
    contracts/forecast-tool-boundary-v1.yaml`. Record its suggested bump verbatim and apply it. `pv
    diff` takes TWO FILESYSTEM PATHS, never a git revision (CLAUDE.md) — passing `HEAD` reports a
    missing-file error that reads like a missing contract.

    (5) BINDING ROW. Append one row: `contract: forecast-tool-boundary-v1.yaml`,
    `equation: holiday_design_cost_bounded`, `module_path: aprender_forecast::forecast`,
    `function: forecast`, the real `signature`, `status: implemented`, and a `notes` line stating the
    refusal is at the door before `make_design` and that `FIT_BUDGET_SECS` is structurally blind to it.
    `function: forecast` resolves because `fn forecast` has a definition site in
    `crates/aprender-forecast/src/forecast.rs`.

    (6) Re-run `pv validate`, `pv status` and `make contract-audit-phase6`. Never substitute a
    yq/python/bash script for any of them.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-12-t2a.log 2>&1; rc=$?; tail -8 /tmp/p06-12-t2a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-12-t2a.log</automated>
    <fails_when>non-zero exit, or no `0 error(s)` line — the new equation, obligation or falsification entry is malformed for the KernelContract schema. Build `pv` through cargo, never a `target/release/pv` path: the gitignored `.cargo/config.toml` redirects target-dir and CLAUDE.md rule 8 makes a stale binary that validates the new block the exact failure mode</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- status contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-12-t2b.log 2>&1; rc=$?; tail -12 /tmp/p06-12-t2b.log; EQ=$(awk -F': ' '/^Equations: /{print $2}' /tmp/p06-12-t2b.log); OB=$(awk -F': ' '/^Proof obligations: /{print $2}' /tmp/p06-12-t2b.log); FT=$(awk -F': ' '/^Falsification tests: /{print $2}' /tmp/p06-12-t2b.log); echo "eq=[$EQ] ob=[$OB] ft=[$FT]"; test $rc -eq 0 && test "${EQ:-0}" -ge 18 && test "${OB:-0}" -ge 18 && test "${FT:-0}" -ge 18</automated>
    <fails_when>non-zero exit; or the echoed `eq=[..] ob=[..] ft=[..]` line shows any of the three still at 17 or below — the contract stood at 16/16/16 at verification time, 06-10 added one of each (17/17/17), and this task adds one more of each, so an unchanged number means that block did not land where `pv status` reads it. The three values are parsed from `pv status`'s own line-anchored labels (`^Equations: `, `^Proof obligations: `, `^Falsification tests: `, printed by crates/aprender-contracts-cli/src/commands/status.rs:13-18) and compared numerically — never by matching loose digits anywhere in the log, which would pass on the `20` inside `2026` or on a version string; an empty parse defaults to 0 and fails, so a missing or renamed label fails rather than passing vacuously. If 06-10's SUMMARY records a different post-06-10 baseline than 17/17/17, use that number plus one and say so</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-12-t2c.log 2>&1; rc=$?; tail -12 /tmp/p06-12-t2c.log; test $rc -eq 0 && test "$(grep -c '^BIND- ' /tmp/p06-12-t2c.log)" -eq 0 && test "$(grep -c '^RESOLVE- ' /tmp/p06-12-t2c.log)" -eq 0 && test "$(grep -n 'Total equations:' /tmp/p06-12-t2c.log | wc -l | tr -d ' ')" -eq 4</automated>
    <fails_when>non-zero exit; any `BIND-` line (the new equation has no binding row); any `RESOLVE-` line (the row names a function with no definition site in the file its module_path maps to); or fewer than four `Total equations:` summaries, which means an audit did not run and the green measured nothing</fails_when>
  </verify>
  <done>`forecast-tool-boundary-v1.yaml` owns `holiday_design_cost_bounded` with an equation, a proof obligation and a falsification test naming all five assertions; `pv validate` is clean, `pv status` counts rose; `binding.yaml` carries a row that both audits and RESOLVES; the HARD-ceiling claim at line 102 is backed by a constant, a check and a test.</done>
  <acceptance_criteria>
    - `grep -c 'holiday_design_cost_bounded' contracts/forecast-tool-boundary-v1.yaml` is at least 1 and `grep -n 'equation: holiday_design_cost_bounded' contracts/aprender/binding.yaml` prints exactly one line number
    - The new falsification entry's `prediction` names all five assertions by name: `cost_bounds_match_contract`, `an_in_bounds_holiday_spec_whose_product_is_not_is_refused`, `a_holiday_spec_just_under_the_design_cost_bound_is_accepted`, `refuses_holiday_design_cost_over_bound`, `accepts_holiday_design_cost_just_under_bound`
    - The equation's invariants cite the measured breach: the strings `16.113`, `8.926` and `2.664` all appear in the contract file
    - The equation's invariants state that the check runs before `make_design` and that `FIT_BUDGET_SECS` is structurally blind to it — `grep -c 'FIT_BUDGET_SECS' contracts/forecast-tool-boundary-v1.yaml` is at least 2 (the pre-existing line-102 mention plus the new one)
    - The binding row's `module_path` is `aprender_forecast::forecast` and its `function` is `forecast`; `make contract-audit-phase6` prints its `resolved N Phase 6 binding row` banner with N one greater than the count 06-10 recorded
    - The SUMMARY quotes `pv diff`'s suggested semver bump verbatim and states the version before and after
    - The SUMMARY states which statistic the equation declares bounded and confirms it matches what 06-11 measured — if the two disagree, that is a defect in one of them and must be reported, not reconciled by editing the prose
  </acceptance_criteria>
  <reversibility rating="reversible">A contract equation, a proof obligation, a falsification entry and one binding row — all text deletions to undo, with no external consumer and nothing persisted. (The behavioural narrowing this documents was already taken, and rated, in 06-11 Task 2.)</reversibility>
</task>

<task type="auto">
  <name>Task 3: Turn `just forecast-holiday-bench` into an SC1 gate — assert the at-the-bound wall is under 2 s</name>
  <files>justfile</files>
  <read_first>
    - justfile — the `forecast-holiday-bench` recipe as 06-11 left it, and `forecast-bench`
      (:659-685) whose bar-assertion idiom this task copies: `set +e` / `rc=$?` / `set -e` around the
      cargo call, a tail-and-fail on non-zero rc, an `awk` numeric comparison, a hard failure naming the
      measured number, and a hard failure when the expected line is absent ("the bar was never measured")
    - justfile :703 onward (`forecast-pool-ratio`) — how a host-gated recipe documents WHY the bar
      lives in a recipe rather than in a libtest assertion, and how it treats a non-zero cargo exit as
      a correctness failure distinct from a slow measurement
    - crates/aprender-forecast/src/types.rs — the value of `MAX_HOLIDAY_DESIGN_COST`, from which the
      at-the-bound default geometry is derived
    - .planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md — the three
      at-the-bound configurations already measured and their walls
  </read_first>
  <action>
    Extend `forecast-holiday-bench` so it ASSERTS rather than merely prints. Keep the parameterised
    configuration 06-11 gave it, but change the DEFAULTS to the at-the-bound geometry derived from
    `MAX_HOLIDAY_DESIGN_COST` (the largest legal `(points + horizon) * columns` at or just under the
    bound) so that the recipe run with no arguments measures the WORST request the door now accepts —
    that is the only configuration whose wall proves the bar.

    After the existing "no measurement line" guard, parse `total_s` off the single
    `HOLIDAY DESIGN WALL:` line with `awk` (field-split on the `total_s=` token, not on column
    position — the line's field order must not become load-bearing), and:
      - FAIL when `total_s` is empty, with a message saying the wall was never measured;
      - FAIL when `total_s >= 2.0`, with a message naming the measured value, the geometry and SC1;
      - print an `HOLIDAY DESIGN OK: <total_s> s < 2.0 s (SC1)` line on success, matching
        `forecast-bench`'s `ROUND TRIP OK` voice.
    Also assert the line says `profile=release` and FAIL if it does not: a debug wall is not the SC1
    bar, and this crate carries a dev-profile `opt-level = 3` override that makes a debug number look
    plausible while still not being the bar (CLAUDE.md Verification Discipline rule 2 — prove the
    mechanism engaged, never label a run by intent).

    Capture `rc` from a redirect, never through a pipe. This exact defect shipped twice in this repo
    (#2336 `qwen-story-daily` captured `tee`'s status so its fail-the-job step was unreachable and
    three green runs proved nothing; #2360 `make publish`'s POST-PUBLISH VERIFICATION did the same),
    and a bar whose failure path is unreachable is worse than no bar.

    Then PROVE THE RECIPE CAN FAIL. Run it once normally and record the OK line. Then run it once with
    a deliberately over-bound geometry passed as arguments — a configuration the door will REFUSE —
    and confirm the recipe fails loudly rather than passing on a missing measurement line. Record both
    outputs. Do not add the recipe to any `make` tier or to CI: it is host-gated and release-only, the
    same status `forecast-bench` and `forecast-pool-ratio` have, and editing `.github/workflows/*.yml`
    is an explicit human escalation per CLAUDE.md.

    Finally run `bashrs lint` on the recipe body if `bashrs` is available on this host (CLAUDE.md:
    shell scripts use bashrs, not shellcheck); if it is not available, say so in the SUMMARY rather
    than silently skipping.
  </action>
  <verify>
    <automated>just forecast-holiday-bench > /tmp/p06-12-t3a.log 2>&1; rc=$?; tail -12 /tmp/p06-12-t3a.log; test $rc -eq 0 && grep -q 'HOLIDAY DESIGN OK' /tmp/p06-12-t3a.log && grep -q 'profile=release' /tmp/p06-12-t3a.log && test "$(grep -c 'HOLIDAY DESIGN WALL:' /tmp/p06-12-t3a.log)" -eq 1</automated>
    <fails_when>non-zero exit (the at-the-bound wall reached or exceeded 2.0 s, so the bound 06-11 chose is too generous and must be lowered); no `HOLIDAY DESIGN OK` line; no `profile=release` (the recipe dropped `--release` and the number is not the SC1 bar); or a count of `HOLIDAY DESIGN WALL:` lines other than exactly 1, which would make the parse ambiguous</fails_when>
    <automated>just forecast-bench > /tmp/p06-12-t3b.log 2>&1; rc=$?; tail -8 /tmp/p06-12-t3b.log; test $rc -eq 0 && grep -q 'ROUND TRIP OK' /tmp/p06-12-t3b.log</automated>
    <fails_when>non-zero exit or no `ROUND TRIP OK` line — the pre-existing no-holiday 3 000-point SC1 recipe regressed, which would mean this task's edits leaked out of the new recipe</fails_when>
    <automated>grep -n 'rc=\$?' justfile > /tmp/p06-12-t3c.log 2>&1; cat /tmp/p06-12-t3c.log; test "$(awk '/^forecast-holiday-bench:/{f=1} f&&/rc=\$\?/{c++} f&&/^[a-z][a-z0-9-]*:/&&!/^forecast-holiday-bench:/{f=0} END{print c+0}' justfile)" -ge 1</automated>
    <fails_when>the count is 0 — the `forecast-holiday-bench` recipe body contains no `rc=$?` capture, meaning the cargo exit status is being read some other way (through a pipe, or not at all), which is the defect that made two previous gates in this repo unable to fail</fails_when>
  </verify>
  <done>`just forecast-holiday-bench` defaults to the worst request the door now accepts, asserts its measured release-profile wall is under SC1's 2 s bar, fails loudly when no measurement line was produced, captures rc from a redirect, and was observed failing on a deliberately over-bound geometry.</done>
  <acceptance_criteria>
    - `grep -c 'HOLIDAY DESIGN OK' justfile` is at least 1 and the recipe body contains a `2.0` comparison and a `profile=release` check
    - The recipe's default geometry is the at-the-bound one: the SUMMARY shows `(points + horizon) * columns` for the defaults and shows it at or just under `MAX_HOLIDAY_DESIGN_COST`
    - `just forecast-holiday-bench` exits 0 and prints one `HOLIDAY DESIGN WALL:` line and one `HOLIDAY DESIGN OK` line; the measured `total_s` is recorded in the SUMMARY
    - The SUMMARY records the FAILING run: the over-bound geometry passed, the recipe's non-zero exit and the message it printed — a bar never seen failing is not a bar
    - `awk` parses `total_s` by token, not by column position — the SUMMARY quotes the parse expression
    - The SUMMARY states whether `bashrs lint` was run on the recipe and its result, or that `bashrs` was unavailable on this host
    - No `.github/workflows/*.yml` file and no `Makefile` tier target references the new recipe — it stays host-gated like `forecast-bench` and `forecast-pool-ratio`
  </acceptance_criteria>
  <reversibility rating="reversible">A `just` recipe body. Reverting is a text deletion; the recipe is host-gated, wired into no CI job and no `make` tier, and nothing depends on its output but a human reading a SUMMARY.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers, default K=8) | untrusted JSON-RPC `tools/call` arguments; no auth, no session, no quota |
| MCP client -> stdio | the same arguments over a spawned process's stdin |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. The
genuine ASVS L1 surface is INPUT VALIDATION and RESOURCE EXHAUSTION; this plan is the proof side of
the exhaustion mitigation 06-11 built.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-22 | Denial of Service | the holiday design build reached through `crates/aprender-mcp-forecast/src/lib.rs` -> `aprender_forecast::forecast` | **high** (ASVS L1 blocking threshold) | mitigate | 06-11 added the bound; this plan proves it holds AT THE TRANSPORT, where the untrusted arguments actually arrive. Task 1's over-bound case is refused through a live streamable-HTTP server and was observed failing with the bound raised; Task 3 asserts the at-the-bound wall stays under SC1's 2 s bar on a release build, so a future regression in the design build is caught by a repeatable gate rather than by a reviewer |
| T-06-26 | Tampering | the boundary contract as a client-facing document | medium | mitigate | A bound enforced in code but absent from the contract is a rule a client cannot discover and a future editor can delete without a test going red. Task 2 adds the equation, the proof obligation and the falsification test, and `make contract-audit-phase6` resolves the binding row to a real definition site, so deleting the check breaks a named test rather than passing silently |
| T-06-27 | Denial of Service | the gate itself, if it cannot fail | medium | mitigate | A recipe that reads `$?` through a pipe, or that passes when no measurement line was produced, is theatre — that defect shipped twice in this repo (#2336, #2360). Task 3 captures rc from a redirect, fails when the line is absent, and is OBSERVED failing on a deliberately over-bound geometry before it is trusted |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan: the changes are two tests, a contract block, a binding row and a `just` recipe body. No new dependency enters the graph, so the package-legitimacy gate is not applicable. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-mcp-forecast --lib` — 32+ passed, 0 failed, including the over-bound refusal and the near-miss acceptance
- `cargo test -p aprender-forecast --lib types::tests::cost_bounds_match_contract` — 1 passed (the temporary RED-observation perturbation was restored)
- `pv validate contracts/forecast-tool-boundary-v1.yaml` — `0 error(s)`; `pv status` counts at 18 or more
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `just forecast-holiday-bench` — `HOLIDAY DESIGN OK`, `profile=release`, exactly one measurement line; and OBSERVED failing on an over-bound geometry
- `just forecast-bench` — `ROUND TRIP OK`, unchanged
- `cargo clippy -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings` and `cargo fmt --all -- --check` — rc=0
</verification>

<success_criteria>
- VERIFICATION gap 2 is closed on all three of its `missing` bullets across 06-11 and this plan, each traceable to a task
- The bar was shown to turn RED at the server, and the recipe gate was shown to turn RED on an over-bound geometry
- `contracts/forecast-tool-boundary-v1.yaml:102`'s HARD-ceiling claim is backed by a constant, a door check, a two-sided library pair, a two-sided e2e pair and a host-gated wall assertion
- The three human_verification items remain untouched and open
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-12)

New tests in `crates/aprender-mcp-forecast/src/lib.rs`:
`e2e::refuses_holiday_design_cost_over_bound`,
`e2e::accepts_holiday_design_cost_just_under_bound`, and conditionally
`e2e::refuses_holiday_dates_total_over_bound`.

New contract keys in `contracts/forecast-tool-boundary-v1.yaml`:
`equations.holiday_design_cost_bounded` (with its `formula` / `domain` / `codomain` / `invariants`),
one new `proof_obligations` entry, and one new `falsification_tests` id in the
`FALSIFY-BOUNDARY-0NN` series (the next unused one; 06-10 took 017).

New binding key in `contracts/aprender/binding.yaml`: the row whose `equation:` is
`holiday_design_cost_bounded`.

New stdout token in the `justfile`: `HOLIDAY DESIGN OK`.

Symbols this plan CONSUMES that 06-11 creates (not pre-existing at HEAD, do not drift-check against
HEAD): `MAX_HOLIDAY_DESIGN_COST`, `MAX_HOLIDAY_DATES_TOTAL`,
`constants.fit_max_holiday_design_cost`, `constants.fit_max_holiday_dates_total`,
`forecast-holiday-bench`, the `HOLIDAY DESIGN WALL:` token, `holiday_day_sets`,
`an_in_bounds_holiday_spec_whose_product_is_not_is_refused`,
`a_holiday_spec_just_under_the_design_cost_bound_is_accepted`,
`holidays_carrying_more_dates_than_the_total_bound_are_refused`. Symbols 06-10 creates:
`cap_is_logistic_only`, `FALSIFY-BOUNDARY-017`, `refuses_cap_without_logistic_growth`,
`refuses_cap_with_explicit_linear_growth`.

Everything else named in this plan (`forecast`, `refused`, `with`, `synth`, `valid_args`, `serve`,
`REFUSAL_CODE`, `VALIDATION_PREFIX`, `make_design`, `FIT_BUDGET_SECS`, `cost_bounds_match_contract`,
`contract-audit-phase6`, `forecast-bench`, `forecast-pool-ratio`, `ROUND TRIP OK`, `HolidayArg`,
`MAX_POINTS`, `MAX_HORIZON`, `MAX_SPAN_DAYS`, `MAX_HOLIDAY_WINDOW`, `MAX_HOLIDAY_COLUMNS`,
`MAX_HOLIDAY_DATES`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-12-SUMMARY.md` when done. Record:
both e2e geometries as numbers with the per-bound satisfaction table for the OVER case; the RED
observation verbatim (raised constant value, failing assertion, the reply body that showed a forecast,
and the accompanying `cost_bounds_match_contract` red) plus the restored greens; `pv diff`'s suggested
bump and the version before/after; the `resolved N Phase 6 binding rows` count before and after; the
`HOLIDAY DESIGN OK` line with its measured `total_s` and geometry; the recipe's observed FAILING run on
an over-bound geometry; whether `bashrs lint` was run; and an explicit statement that the three
`human_verification` items remain OPEN and were not planned as automated work.
</output>
