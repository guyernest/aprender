---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 10
type: execute
wave: 8
gap_closure: true
# Waves 8-11 continue the executed phase's numbering (06-01..06-09 ran in waves 1-7), so they
# cannot collide with it. `depends_on` is EMPTY on purpose: a gap plan depends on the executed
# work implicitly (06-01..06-09 are all complete with SUMMARYs), and `depends_on` is reserved for
# real ordering constraints BETWEEN the new gap plans — 06-11 -> 06-12 -> 06-13 declare theirs.
depends_on: []
files_modified:
  - crates/aprender-forecast/src/forecast.rs
  - crates/aprender-mcp-forecast/src/lib.rs
  - contracts/forecast-tool-boundary-v1.yaml
  - contracts/aprender/binding.yaml
  - .planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md
autonomous: true
requirements: [SC1]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `cap` sent on any NON-logistic growth arm — explicit `growth: \"linear\"`, explicit `growth: \"flat\"`, or the defaulted arm with no `growth` key at all — is REFUSED with the message `cap is logistic-only; set growth to \"logistic\"`, in the same shape as the neighbouring cross-MODEL refusals at forecast.rs:113-127 (SC1 / D-11; VERIFICATION gap 1)"
    - "The refusal does NOT over-refuse: `growth: \"logistic\"` with a finite cap above max(y) still fits and returns a band, and the two existing logistic refusals keep their own distinct messages (`logistic growth needs cap`, `cap C must exceed max(y) = M`) — the three cases are distinguished by three messages, not merged into one"
    - "The bar is asserted THROUGH THE SERVER as every other SC1 refusal is: at least two new `e2e::refuses_*` cases drive a live streamable-HTTP server and observe a validation-class refusal naming the fix"
    - "The RED side was OBSERVED, not asserted: the new unit and e2e cases were run BEFORE the door check existed and recorded FAILING (the door accepted `cap` and returned a forecast), and that failing output is quoted in the SUMMARY (CLAUDE.md Verification Discipline rule 4)"
    - "`spec.cap` can no longer carry a value the door did not validate: the cap that reaches `Spec` is bound inside the logistic branch, so an inert-but-accepted cap is structurally unreachable rather than merely guarded"
    - "The contract owns the rule: `forecast-tool-boundary-v1.yaml` carries a `cap_is_logistic_only` equation and a FALSIFY-BOUNDARY entry naming the new tests, the equation has a row in `contracts/aprender/binding.yaml`, and `make contract-audit-phase6` still reports zero BIND- and zero RESOLVE- findings (D-15; project rule: coverage without contracts is rejected)"
    - "`COVERAGE.md` exists in the phase directory and declares, with a reason, that Phase 6 integrates no external API — so the `api-coverage.verify-pre` seal gate has a decided answer rather than a re-run detector"
  artifacts:
    - path: "crates/aprender-forecast/src/forecast.rs"
      provides: "the cross-GROWTH cap refusal in the prophet arm, plus the extended cross-arm unit test"
      contains: "logistic-only"
    - path: "crates/aprender-mcp-forecast/src/lib.rs"
      provides: "e2e refusal cases proving the bar through a live server"
      contains: "refuses_cap_without_logistic_growth"
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "equation cap_is_logistic_only + FALSIFY-BOUNDARY-017 + a proof obligation"
      contains: "cap_is_logistic_only"
    - path: "contracts/aprender/binding.yaml"
      provides: "a binding row resolving cap_is_logistic_only to a real definition site"
      contains: "cap_is_logistic_only"
    - path: ".planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md"
      provides: "the reasoned no-external-API declaration the seal gate accepts"
      contains: "No external API integration:"
  key_links:
    - from: "crates/aprender-mcp-forecast/src/lib.rs"
      to: "crates/aprender-forecast/src/forecast.rs"
      via: "the e2e case reaches the SAME door the library caller reaches — the transport re-checks nothing"
      pattern: "aprender_forecast::forecast"
    - from: "contracts/forecast-tool-boundary-v1.yaml"
      to: "crates/aprender-forecast/src/forecast.rs"
      via: "binding.yaml row cap_is_logistic_only -> aprender_forecast::forecast::forecast, resolved to a definition site by contract-audit-phase6"
      pattern: "cap_is_logistic_only"
  prohibitions:
    - statement: "Never widen the refusal to the logistic arm — `growth: \"logistic\"` with a valid cap must still FIT. A refusal that swallows the happy path is a worse defect than the one it closes"
      status: resolved
      verification: test
    - statement: "Never change the two existing logistic refusal messages (`logistic growth needs cap`, `must exceed max(y)`); their e2e cases string-match them and 06-06 pinned them"
      status: resolved
      verification: test
    - statement: "Do not touch crates/aprender-compute, .github/workflows/*.yml, or any parity fixture — scope fence for this gap-closure run"
      status: resolved
      verification: judgment
    - statement: "Do not plan or perform the three human_verification items (browser MCP handshake + chart rendering, the x86_64 f32 tolerance measurement, the SC4-dark-in-CI decision) — they are routed to the human and stay open"
      status: resolved
      verification: judgment
---

<objective>
Close VERIFICATION gap 1: `cap` is accepted and provably inert on every non-logistic growth arm.
Add the cross-GROWTH refusal to THE door, prove it with unit cases and with e2e cases through a live
server, and give the rule a contract identity so it cannot be deleted silently.

Purpose: `contracts/forecast-tool-boundary-v1.yaml` FALSIFY-BOUNDARY-005 calls an ignored caller knob
"the worst failure class in this contract". The verifier MEASURED it: baseline-no-cap, linear+cap100,
bare-cap100 and linear+cap0.5 (BELOW max(y)=28.9) all returned `yhat_max=35.8994` with a byte-identical
`diagnostics` object, while the two logistic controls refused — which proves the refusal machinery
exists and is simply never reached.
Output: one refusal, two unit cases, two e2e cases, one contract equation, one binding row, and the
phase's `COVERAGE.md` declaration.

TRACER: this is the phase's gap-closure tracer. It is the smallest complete vertical — door refusal →
library unit assertion → refusal observed through the streamable-HTTP server — and it re-proves the
whole SC1 door path end to end before the heavier gap-2 work in 06-11/06-12 lands on the same files.
</objective>

<gap_closure_context>
Source: `.planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md`, gap 1
(`truth: SC1 — the forecast door refuses every malformed input, never a silent default`,
`status: failed`), measured at HEAD `ce3e5a8ea` on `gsd/phase-2-contract-gate`.

This plan does NOT modify, supersede or re-create 06-01 … 06-09 or their SUMMARYs. It adds work.

The verifier's three `missing` bullets and where each lands:

| # | `missing` bullet | Task |
|---|---|---|
| 1 | Refuse `cap` when growth != logistic, in the same shape as the neighbouring cross-model refusals (`cap is logistic-only; set growth to "logistic"`) | Task 1 |
| 2 | Extend `forecast::tests::an_option_belonging_to_the_other_model_is_refused_not_dropped` with `{growth: linear, cap: N}` and a bare `{cap: N}` | Task 1 |
| 3 | Add one e2e refusal case in `crates/aprender-mcp-forecast/src/lib.rs` so the bar is asserted through the server, as every other SC1 refusal is | Task 2 (two cases, not one — CLAUDE.md rule 6: one input is an anecdote) |
</gap_closure_context>

<spec_and_probe_status>
No `06-SPEC.md` exists for this phase, so there are no `## Edge Coverage` / `## Prohibitions` SPEC
sections to lift. The spec-less probe fallback was SKIPPED and is recorded as skipped: `phase_req_ids`
is null (`.planning/REQUIREMENTS.md` is the SetFit milestone's document and carries no forecasting
REQ-IDs — ROADMAP.md's Phase 6 `**Requirements note**` says so), so the probe had no input and no
probe-derived predicates were generated this run. `must_haves` below are derived from the phase goal,
SC1, and the verifier's `missing` list.
</spec_and_probe_status>

<assumption_delta_decision>
- **Detector:** `workflow.assumption_delta` fired `detected: true` with three `pluralization` signals,
  advisory and non-blocking. The snippets are `"alongside Phase 5's remaining GPU waves."`,
  ``"…`cargo test -- a b` dropping the second filter…"`` and `"…halts if another cargo build is
  running…"`.
- **Primary noun:** unchanged — `forecast` (the one stateless tool at the one door, D-01/D-11).
- **Decision:** `no-change`.
- **Rationale:** none of the three snippets is a singular→plural, required→optional or
  derived→chosen transition in the identity model. They are incidental prose about wave scheduling, a
  refuted cargo-CLI claim, and a build-lock precondition; no second platform, tenant, auth method or
  source of truth is introduced by this gap-closure run, and the door keeps exactly one primary noun.
</assumption_delta_decision>

<human_items_still_open>
This plan touches the surface of one item the verifier routed to `human_verification`, and does NOT
close it. `crates/aprender-mcp-forecast/static/index.html:19` and `:50` send `cap` only when the field
is non-empty and already label it `Cap (logistic)`, so a page user who types a cap on the linear arm
will, after this plan, receive a refusal naming the fix instead of a silently-linear forecast. That is
the intended behaviour and requires no page edit — but the human item **"drive initialize →
tools/list → tools/call on both demo pages in a browser and confirm a forecast is charted" REMAINS
OPEN**. Nothing in this plan asserts browser rendering, and no task pretends to.

The other two human items — measuring `quantiles_abs_f32_nonaarch64` on an x86_64 host, and deciding
whether SC4's Chronos ladder staying dark in CI is acceptable — are untouched by this plan and also
remain open.
</human_items_still_open>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md  (gap 1 only — the frontmatter `gaps[0]` entry and its `missing` list)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-11: the tool boundary refuses, never defaults)
@.planning/phases/06-native-time-series-forecasting-stack/06-06-SUMMARY.md  (the refusal e2e suite this plan extends, and the pinned validation error code)
@CLAUDE.md  (Verification Discipline; DOGFOOD `pv`; clippy/unwrap/unsafe rules; branch protection)

<interfaces>
The exact refusal message (verbatim from the verifier's `missing` bullet 1, and the shape the
neighbouring cross-model refusals already use):
    cap is logistic-only; set growth to "logistic"

The neighbouring refusals it must MATCH IN SHAPE (crates/aprender-forecast/src/forecast.rs:105-127):
  - `n_lags is neuralprophet-only; set model to "neuralprophet"`   (prophet arm, single field)
  - `{name} is prophet-only; set model to "prophet"`               (neuralprophet arm, loop over
    `growth` / `cap` / `seasonality_mode` / `holidays`)

The three logistic-arm messages that must stay DISTINCT after this change:
  - missing cap        -> `logistic growth needs cap`              (forecast.rs:151)
  - non-finite cap     -> `cap must be a finite number`            (forecast.rs:157)
  - cap at/below max(y)-> `cap {cap} must exceed max(y) = {y_max}` (forecast.rs:162)

Library-side test helpers already present in `crates/aprender-forecast/src/forecast.rs` `mod tests`:
  - `fn np_args(n: usize, horizon: usize) -> ForecastArgs`  (:456) — every field explicit; set
    `model = None` to reach the prophet arm
  - `fn refusal(args: &ForecastArgs, needle: &str)`         (:543) — asserts
    `Err(ForecastError::Validation(m))` and `m.contains(needle)`; panics naming `r.model` otherwise
  - the test to extend: `an_option_belonging_to_the_other_model_is_refused_not_dropped` (:588)

Server-side e2e helpers already present in `crates/aprender-mcp-forecast/src/lib.rs` `mod e2e`:
  - `fn synth(n: usize) -> (Vec<String>, Vec<f64>)`     (:444) — y reaches ~13 for n = 60
  - `fn valid_args() -> serde_json::Value`              (:457) — `{ds, y, horizon: 7}`, 60 points
  - `fn with(extra: serde_json::Value) -> serde_json::Value` (:499) — one perturbation per case
  - `async fn refused(c: &mut Client, args, needle: &str)` (:464) — asserts error-ness, the needle,
    the pinned `REFUSAL_CODE` and the `VALIDATION_PREFIX`
  - the two logistic cases to sit beside: `refuses_logistic_without_cap` (:651),
    `refuses_logistic_cap_below_max_y` (:661)

Contract shape to mirror when adding the new equation (contracts/forecast-tool-boundary-v1.yaml):
  - equations use keys `formula`, `domain`, `codomain`, `invariants`, optionally `preconditions`
  - the nearest sibling equation is `logistic_requires_cap_above_max_y` (:156-163)
  - `falsification_tests` entries use `id` / `rule` / `prediction` / `test` / `if_fails`; the last
    existing id is `FALSIFY-BOUNDARY-016`, so the new one is `FALSIFY-BOUNDARY-017`
  - `proof_obligations` entries use `type` / `property` / `formal` / `applies_to`

Binding row shape (contracts/aprender/binding.yaml:1897-1903, a real forecast-tool-boundary row):
  `- contract: forecast-tool-boundary-v1.yaml` / `equation: <key>` / `module_path: <rust path>` /
  `function: <fn>` / `signature: '<sig>'` / `status: implemented` / `notes: '<why>'`
  Bare filename in `contract:`, never a path. `make contract-audit-phase6` additionally RESOLVES the
  row's `function` to a definition site in the file `module_path` maps to, so the function must exist.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — refuse `cap` off the logistic arm at THE door, RED side observed first</name>
  <files>crates/aprender-forecast/src/forecast.rs</files>
  <read_first>
    - crates/aprender-forecast/src/forecast.rs — read the whole file. Lines 100-127 are the two
      existing cross-MODEL refusals whose SHAPE the new refusal must match; 128-167 are the prophet
      arm's `seasonality_mode` / `growth` matches and the logistic cap validation; 216 is the
      `spec.cap` assignment the verifier calls unconditional; 440-640 is `mod tests` with `np_args`,
      `refusal`, and the test to extend at :588
    - crates/aprender-forecast/src/prophet.rs lines 225-235 — `make_design`'s
      `match (spec.growth, spec.cap)`: it matches `(Growth::Logistic, Some(c))`, PANICS on
      `(Growth::Logistic, None)` and falls to `_ => None` for every other pair. This is why a cap off
      the logistic arm is provably inert rather than merely ignored
    - contracts/forecast-tool-boundary-v1.yaml lines 156-163 (`logistic_requires_cap_above_max_y`)
      and 377-382 (FALSIFY-BOUNDARY-005 and its `if_fails` naming this failure class)
    - .planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md — gap 1's `reason`
      (the seven-probe table) and `missing` list
  </read_first>
  <action>
    RED FIRST, and observe it. Before adding any door check, extend
    `forecast::tests::an_option_belonging_to_the_other_model_is_refused_not_dropped` (:588) with the
    two cases the verifier named, then RUN IT AND RECORD THE FAILURE:

      (a) explicit linear arm — start from `np_args(60, 7)`, set `model = None` (so the prophet arm is
          reached), `growth = Some("linear".into())`, `cap = Some(100.0)`, and call
          `refusal(&args, "logistic-only")`.
      (b) defaulted arm — start from `np_args(60, 7)`, set `model = None`, leave `growth = None`, set
          `cap = Some(100.0)`, and call `refusal(&args, "logistic-only")`. This is the bare-cap probe
          the verifier ran; it is a SEPARATE case from (a) because the defaulted arm is the one a real
          caller hits, and only this case proves the check is not keyed on the presence of `growth`.

    Run `cargo test -p aprender-forecast --lib forecast::tests::an_option_belonging` and observe it
    FAIL with `refusal`'s own panic text (`expected a Validation refusal, got ...`). Copy the failing
    output verbatim into the SUMMARY. Without this observation the assertion is unproven (CLAUDE.md
    Verification Discipline rule 4 — the old four-case proof does not transfer to the new scope).

    GREEN. In `forecast.rs`, inside the `"prophet"` match arm, AFTER the `growth` match has produced a
    `Growth` value (so an unknown growth string still refuses with `growth {o:?}: linear, logistic or
    flat` first, unchanged) and BEFORE the existing `if growth == Growth::Logistic` block, add:

      if growth != Growth::Logistic && args.cap.is_some() { return
      Err(ForecastError::Validation("cap is logistic-only; set growth to \"logistic\"".into())) }

    Use exactly that message text — the verifier's `missing` bullet specifies it verbatim and the e2e
    cases in Task 2 string-match the substring `logistic-only`.

    STRUCTURAL, not merely guarded. Make it impossible for `Spec` to carry a cap the door did not
    validate: declare `let mut checked_cap: Option<f64> = None;` immediately before the
    `if growth == Growth::Logistic` block, set `checked_cap = Some(cap);` at the END of that block
    (after the finiteness and `cap > y_max` checks have both passed), and change the assignment at
    :216 from the raw argument to `spec.cap = checked_cap;`. After this, the only path that puts a
    `Some` into `spec.cap` is the one that validated it, so the accepted-but-inert class is
    unreachable rather than merely unreached.

    POSITIVE CONTROL, in the same test. Add a third block asserting the refusal does not over-refuse:
    build `np_args(60, 7)`, set `model = None`, `growth = Some("logistic".into())`, and set `cap` to a
    value strictly above the series maximum computed FROM THE ARGS THEMSELVES (fold over `args.y` with
    `f64::max`, then add 1.0 — do not hardcode a number, the helper series may change), call
    `forecast(&args)` and `.expect(...)` a response, then assert `r.yhat.len() == 7`. A refusal that
    swallows the logistic happy path is a worse defect than the one being closed.

    Then re-run the same filter and observe GREEN. Also run the full crate lib suite to prove nothing
    else moved: the two existing logistic refusals keep their own messages, and no parity rung is
    touched (this change is at the door, before `make_design`).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast::tests::an_option_belonging > /tmp/p06-10-t1a.log 2>&1; rc=$?; tail -12 /tmp/p06-10-t1a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-10-t1a.log > /dev/null</automated>
    <fails_when>non-zero exit (the door still accepts a cap off the logistic arm, or the positive control was over-refused), or /tmp/p06-10-t1a.log carries no line matching `test result: ok\. [1-9][0-9]* passed; 0 failed` — a `0 passed` summary exits 0 and would mean the filter matched nothing</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-10-t1b.log 2>&1; rc=$?; tail -8 /tmp/p06-10-t1b.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed' /tmp/p06-10-t1b.log > /dev/null</automated>
    <fails_when>non-zero exit — any existing forecast/prophet/np/parity test regressed; in particular a failure in `prophet::parity` would mean the change reached the numerics instead of the door, and a failure in `an_infinite_cap_is_refused` or `a_span_far_wider_than_the_point_count_is_refused` would mean the new branch was inserted before an earlier refusal it must not pre-empt</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-10-t1c.log 2>&1; rc=$?; tail -8 /tmp/p06-10-t1c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — a pedantic lint, a disallowed `unwrap()` (banned by .clippy.toml; use `expect()` or `ok_or_else(|| ...)?`), or a needless-`else`/redundant-clone finding on the new code. `--no-deps` is required: with deps, `-D warnings` reaches the pre-existing redness in crates/aprender-compute (D-ITEM-06-09)</fails_when>
  </verify>
  <done>A `cap` on the linear, flat or defaulted growth arm is refused with `cap is logistic-only; set growth to "logistic"`; the logistic arm with a valid cap still fits and returns a 7-point band; the whole `aprender-forecast` lib suite is green; and the pre-fix FAILING output of the two new cases is recorded in the SUMMARY.</done>
  <acceptance_criteria>
    - `grep -c 'cap is logistic-only; set growth to' crates/aprender-forecast/src/forecast.rs` is at least 1
    - `grep -c 'checked_cap' crates/aprender-forecast/src/forecast.rs` is at least 3 (declaration, assignment inside the logistic branch, use at the `spec.cap` line)
    - `cargo test -p aprender-forecast --lib forecast::tests::an_option_belonging -- --exact --nocapture` reports `1 passed; 0 failed`
    - `cargo test -p aprender-forecast --lib forecast::tests::` reports `0 failed` and a passed count of at least the count recorded before the change (record both numbers in the SUMMARY)
    - `cargo test -p aprender-forecast --lib prophet::parity` reports `32 passed; 0 failed` — the parity ladder is UNMOVED, because this change is at the door and never reaches `make_design`
    - The SUMMARY quotes, verbatim, the pre-fix failing output of BOTH new cases (the `refusal` helper's panic line naming the model it got instead of a refusal) and the post-fix passing summary line
    - The SUMMARY states the four probe shapes now refused (explicit `linear` + cap, explicit `flat` + cap, defaulted arm + cap, and a cap below max(y) on a non-logistic arm) and confirms `model: "neuralprophet"` + cap still refuses with the older `cap is prophet-only` message, unchanged
  </acceptance_criteria>
  <reversibility rating="costly">The refusal narrows the door's accepted-input surface: a client that was silently sending `cap` on the linear arm now gets an error. Undo is a one-hunk revert with no migration and no persisted state, so it is not one-way — but it is not free either, because the boundary is published in `contracts/forecast-tool-boundary-v1.yaml` and a client written against the old behaviour changes. Taken deliberately: D-11 and FALSIFY-BOUNDARY-005's own `if_fails` text already promise this refusal, so the change makes a published promise true rather than making a new one.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Assert the bar THROUGH THE SERVER — two e2e refusal cases, RED side observed</name>
  <files>crates/aprender-mcp-forecast/src/lib.rs</files>
  <read_first>
    - crates/aprender-mcp-forecast/src/lib.rs lines 430-760 — `mod e2e`: `synth`, `valid_args`,
      `refused`, `with`, and the 22 existing `refuses_*` cases. Read `refused` in full: it asserts
      error-ness, the needle, the pinned `REFUSAL_CODE` and the `VALIDATION_PREFIX`, so a new case
      inherits all four assertions and must add none of its own
    - crates/aprender-mcp-forecast/src/lib.rs lines 645-700 — `refuses_logistic_without_cap` and
      `refuses_logistic_cap_below_max_y`, the two cases the new ones sit beside and must not disturb
    - crates/aprender-forecast/src/forecast.rs — the refusal message Task 1 added (the needle must be
      a substring of it)
    - crates/aprender-mcp-forecast/static/index.html lines 19 and 50 — how the demo page assembles
      `cap`; read it to confirm no page edit is required (it already labels the field `Cap (logistic)`
      and omits the key when the input is empty)
  </read_first>
  <action>
    RED FIRST. Add both cases and run them BEFORE rebuilding against Task 1's fix is possible — i.e.
    if Task 1 is already committed, produce the RED observation by temporarily commenting out the new
    door check in `forecast.rs`, running the two cases, recording the failure, and restoring it. State
    in the SUMMARY which of the two routes was used. An e2e assertion that was never seen failing is a
    green that measured nothing.

    Add, immediately after `refuses_logistic_cap_below_max_y` so the three cap cases read together:

      (a) `refuses_cap_without_logistic_growth` — `refused(&mut serve().await,
          with(serde_json::json!({"cap": 100.0})), "logistic-only").await;` — the BARE cap the
          verifier probed, on the defaulted growth arm. `valid_args()` is 60 synthetic points whose y
          reaches ~13, so 100.0 is a cap a caller might plausibly believe is being honoured.
      (b) `refuses_cap_with_explicit_linear_growth` — `refused(&mut serve().await,
          with(serde_json::json!({"growth": "linear", "cap": 100.0})), "logistic-only").await;` — the
          explicit arm. Two cases, not the one the verifier asked for, because CLAUDE.md rule 6 says
          one failing input is an anecdote: (a) alone would pass even if the check were keyed on the
          absence of a `growth` key.

    Both are `#[tokio::test]` `async fn`, matching every neighbouring case, and both use `with(...)`
    so exactly one field is perturbed relative to `valid_args()`.

    Do NOT add a happy-path e2e case here — `e2e::prophet_logistic_with_holidays_happy_path` already
    exercises the accepting logistic path through the same server and is the positive control for
    these two. Name it in the SUMMARY as the control and confirm it still passes in the same run.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-10-t2a.log 2>&1; rc=$?; tail -10 /tmp/p06-10-t2a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-10-t2a.log > /dev/null</automated>
    <fails_when>non-zero exit (a refusal case saw a forecast instead of an error, or the accepting logistic happy path regressed into a refusal), or no `test result: ok\. [1-9][0-9]* passed; 0 failed` line — a zero-test green</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib > /tmp/p06-10-t2b.log 2>&1; rc=$?; tail -8 /tmp/p06-10-t2b.log; N=$(grep -oE '[0-9]+ passed' /tmp/p06-10-t2b.log | head -1 | grep -oE '[0-9]+'); echo "passed=$N"; test $rc -eq 0 && test "${N:-0}" -ge 30</automated>
    <fails_when>non-zero exit, or the passed count is below 30 — the crate reported 28 passing at verification time and this task adds 2, so a count under 30 means a case was replaced rather than added, or the suite silently shrank</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --test e2e_stdio > /tmp/p06-10-t2c.log 2>&1; rc=$?; tail -6 /tmp/p06-10-t2c.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed' /tmp/p06-10-t2c.log > /dev/null</automated>
    <fails_when>non-zero exit — the spawned-binary stdio round trip broke, which would mean the door change reached the transport rather than staying in the library; or no `test result: ok. N passed` line with N >= 1 (this target is dark in CI per D-ITEM-06-06, so a local zero-test green would hide a regression entirely)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-10-t2d.log 2>&1; rc=$?; tail -8 /tmp/p06-10-t2d.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — a pedantic lint on the two new async tests, or a disallowed `unwrap()`</fails_when>
  </verify>
  <done>Two new `e2e::refuses_*` cases drive a live streamable-HTTP server, observe a validation-class refusal naming `logistic-only`, and were recorded failing before the door check was in place; the whole `aprender-mcp-forecast` lib suite is at 30+ passing with 0 failed; the stdio target still passes.</done>
  <acceptance_criteria>
    - `grep -n 'async fn refuses_cap_without_logistic_growth' crates/aprender-mcp-forecast/src/lib.rs` prints exactly one line number (read it and record it)
    - `grep -n 'async fn refuses_cap_with_explicit_linear_growth' crates/aprender-mcp-forecast/src/lib.rs` prints exactly one line number (read it and record it)
    - Both new functions carry `#[tokio::test]` and call the shared `refused(...)` helper — `grep -c 'logistic-only' crates/aprender-mcp-forecast/src/lib.rs` is at least 2
    - `cargo test -p aprender-mcp-forecast --lib e2e::refuses_` reports a passed count of at least 24 (22 existing + 2) and `0 failed`; the exact before/after counts are recorded in the SUMMARY
    - `cargo test -p aprender-mcp-forecast --lib e2e::prophet_logistic_with_holidays_happy_path` reports `1 passed; 0 failed` — the accepting logistic path through the server is unmoved
    - The SUMMARY records the RED observation route (tests written before the fix, or the door check temporarily commented out) and quotes the failing assertion text, including the reply body that showed a forecast where a refusal was required
    - The SUMMARY states explicitly that the demo page needed no edit and that the human browser-verification item remains OPEN
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Give the rule a contract identity, and land the phase's COVERAGE.md declaration</name>
  <files>contracts/forecast-tool-boundary-v1.yaml, contracts/aprender/binding.yaml, .planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md</files>
  <read_first>
    - contracts/forecast-tool-boundary-v1.yaml — the `equations:` block (from :94), especially
      `logistic_requires_cap_above_max_y` (:156-163) and `unknown_enum_refused` (:200-207) for the
      invariant voice; the `proof_obligations:` block (:269+); the `falsification_tests:` block
      (:351+), whose last id is `FALSIFY-BOUNDARY-016`
    - contracts/aprender/binding.yaml lines 1890-1935 — the existing forecast-tool-boundary rows, to
      copy the key order and the bare-filename convention exactly
    - Makefile — the `contract-audit-phase6` target (added by 06-09 Task 1): it audits the four Phase 6
      contracts, requires four `Total equations:` summaries, fails on any `BIND-` line, and
      additionally RESOLVES every Phase 6 binding row's `function` to a definition site in the file its
      `module_path` maps to, printing `RESOLVE- ...` and failing on a non-zero count or a zero total
    - .planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md — gap 1's `reason`,
      for the measured numbers the contract invariant should cite
  </read_first>
  <action>
    (1) CONTRACT. Add a new equation `cap_is_logistic_only` to
    `contracts/forecast-tool-boundary-v1.yaml`, placed immediately after
    `logistic_requires_cap_above_max_y` so the two cap rules read together. Its `formula` states that
    a present `cap` implies `growth == logistic`; its `domain` is the fit server's growth/cap
    arguments; its `codomain` is a validation refusal. Its `invariants` must state, in the file's own
    voice: that the message is `cap is logistic-only; set growth to "logistic"` and names the fix; that
    the DEFAULTED growth arm is covered, not only the explicitly-linear one; that the check runs after
    the growth enum is parsed so an unknown growth string still refuses with its own message; that the
    validated cap is bound inside the logistic branch so `Spec` cannot carry an unvalidated one; and
    the MEASURED breach this closes — five probes (baseline-no-cap, linear+cap100, bare-cap100,
    linear+cap0.5 below max(y)=28.9, flat+cap100) all returned yhat_max 35.8994 with a byte-identical
    diagnostics object at HEAD ce3e5a8ea, while the two logistic controls refused.

    Add a matching `proof_obligations` entry of `type: invariant` whose `formal` reads
    `forall args: args.cap is Some and args.growth != logistic implies result is Validation`, with
    `applies_to: all`.

    Add `FALSIFY-BOUNDARY-017` to `falsification_tests`. Its `prediction` NAMES the four assertions
    that falsify it — `forecast::tests::an_option_belonging_to_the_other_model_is_refused_not_dropped`
    (the explicit-linear and bare-cap cases plus the logistic positive control) and the two e2e cases
    `refuses_cap_without_logistic_growth` and `refuses_cap_with_explicit_linear_growth`. Its `test` is
    a runnable command pair over the two crates. Its `if_fails` states that the door is back to
    accepting a knob it does not use, which is the class FALSIFY-BOUNDARY-005 already calls the worst
    in this contract.

    Do NOT bump `metadata.version` by hand. Instead materialise the pre-change file and use the
    dogfooded tool to see what the change is worth: `git show HEAD:contracts/forecast-tool-boundary-v1.yaml
    > /tmp/ftb-old.yaml` then `cargo run --release -p aprender-contracts-cli --bin pv -- diff
    /tmp/ftb-old.yaml contracts/forecast-tool-boundary-v1.yaml`, and record its suggested semver bump
    verbatim in the SUMMARY. `pv diff` takes TWO FILESYSTEM PATHS, never a git revision (CLAUDE.md).
    Apply the bump it suggests. Never hand-write a yq/python/bash script that re-implements `pv`.

    (2) BINDING ROW. Append one row to `contracts/aprender/binding.yaml`:
    `contract: forecast-tool-boundary-v1.yaml`, `equation: cap_is_logistic_only`,
    `module_path: aprender_forecast::forecast`, `function: forecast`, the `signature` copied from the
    real declaration (`fn forecast(args: &ForecastArgs) -> Result<ForecastResponse, ForecastError>`),
    `status: implemented`, and a `notes` line saying the refusal is at THE door, before `make_design`,
    so no parity rung can move. `function: forecast` resolves because `fn forecast` has a definition
    site in `crates/aprender-forecast/src/forecast.rs`, which is where `aprender_forecast::forecast`
    maps.

    (3) COVERAGE.md. Write `.planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md` as
    the reasoned no-external-API declaration, not a fabricated matrix. The `api_coverage_gate` detector
    fired on a single signal — the noun `api` inside a source comment in an existing Phase 6 plan
    (`<!-- Core API the np.rs port calls ... -->`), which is an INTERNAL Rust API. Phase 6 integrates
    no external API, SDK or service: it ports Prophet 1.4.0, NeuralProphet 0.9.0 and Chronos-Bolt
    natively into pure Rust in `crates/aprender-forecast` and serves them through this project's own
    thin pmcp servers. The file must contain a `# API Coverage — Phase 6` heading, then a line that
    STARTS with the exact token `No external API integration: ` followed by that one-line reason and a
    terminating period, then two or three sentences of provenance (the detector's single signal, that
    the signal was an internal Rust API, and that the only network fetch in the phase — the pinned
    `amazon/chronos-bolt-tiny` weights via `just fetch-chronos-tiny` — is a one-time sha256-verified
    artifact download at build time, not a runtime API integration). No table rows: the seal gate
    accepts a reasoned declaration with no rows, and fabricating rows for capabilities that do not
    exist is the failure the fragment explicitly forbids.

    (4) Re-validate everything the change touches.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-10-t3a.log 2>&1; rc=$?; tail -8 /tmp/p06-10-t3a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-10-t3a.log</automated>
    <fails_when>non-zero exit, or the log has no `0 error(s)` line — the new equation, obligation or falsification entry is malformed for the KernelContract schema. Build `pv` through cargo rather than invoking a `target/release/pv` path: CLAUDE.md rule 8 (a shadowed artifact is worse than a missing one) and the gitignored .cargo/config.toml redirects target-dir, so no built-binary path is portable</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- status contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-10-t3b.log 2>&1; rc=$?; tail -12 /tmp/p06-10-t3b.log; EQ=$(awk -F': ' '/^Equations: /{print $2}' /tmp/p06-10-t3b.log); OB=$(awk -F': ' '/^Proof obligations: /{print $2}' /tmp/p06-10-t3b.log); FT=$(awk -F': ' '/^Falsification tests: /{print $2}' /tmp/p06-10-t3b.log); echo "eq=[$EQ] ob=[$OB] ft=[$FT]"; test $rc -eq 0 && test "${EQ:-0}" -ge 17 && test "${OB:-0}" -ge 17 && test "${FT:-0}" -ge 17</automated>
    <fails_when>non-zero exit; or the echoed `eq=[..] ob=[..] ft=[..]` line shows any of the three still at 16 or below — the contract stood at 16/16/16 at verification time and this task adds exactly one equation, one proof obligation and one falsification test, so an unchanged number means that block did not land where `pv status` reads it. The three values are parsed from `pv status`'s own line-anchored labels (`^Equations: `, `^Proof obligations: `, `^Falsification tests: `, printed by crates/aprender-contracts-cli/src/commands/status.rs:13-18) and compared numerically — never by matching loose digits anywhere in the log, which would pass on the `20` inside `2026` or on a version string and would be the project's documented "green that measured nothing"; an empty parse defaults to 0 and fails, so a missing or renamed label fails rather than passing vacuously</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-10-t3c.log 2>&1; rc=$?; tail -12 /tmp/p06-10-t3c.log; test $rc -eq 0 && test "$(grep -c '^BIND- ' /tmp/p06-10-t3c.log)" -eq 0 && test "$(grep -c '^RESOLVE- ' /tmp/p06-10-t3c.log)" -eq 0 && test "$(grep -n 'Total equations:' /tmp/p06-10-t3c.log | wc -l | tr -d ' ')" -eq 4</automated>
    <fails_when>non-zero exit; any `BIND-` line (the new equation has no binding row); any `RESOLVE-` line (the row names a function with no definition site in the file its module_path maps to); or fewer than four `Total equations:` summaries, which means an audit did not run and the green measured nothing</fails_when>
    <automated>F=.planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md; test -f "$F" && grep -qE '^No external API integration: .{40,}\.$' "$F" && test "$(grep -c '^|' "$F")" -eq 0 && echo "COVERAGE OK"</automated>
    <fails_when>the file is absent; no line starts with the exact token `No external API integration: ` followed by at least 40 characters and a terminating period (a bare prefix with no reason is an un-decided hole, which is exactly what the gate exists to close); or the file contains a markdown table row, which would be a fabricated matrix for capabilities that do not exist</fails_when>
  </verify>
  <done>`forecast-tool-boundary-v1.yaml` owns the `cap_is_logistic_only` rule with an equation, a proof obligation and FALSIFY-BOUNDARY-017; `pv validate` is clean and `pv status` counts rose; `binding.yaml` has a row that both audits and RESOLVES; `COVERAGE.md` carries the reasoned declaration with no fabricated rows.</done>
  <acceptance_criteria>
    - `grep -c 'cap_is_logistic_only' contracts/forecast-tool-boundary-v1.yaml` is at least 1 and `grep -n 'equation: cap_is_logistic_only' contracts/aprender/binding.yaml` prints exactly one line number
    - `grep -n 'FALSIFY-BOUNDARY-017' contracts/forecast-tool-boundary-v1.yaml` prints exactly one line number, and its `prediction` names all four assertions by test name (`an_option_belonging_to_the_other_model_is_refused_not_dropped`, `refuses_cap_without_logistic_growth`, `refuses_cap_with_explicit_linear_growth`, and the logistic positive control)
    - The `cap_is_logistic_only` invariants cite the measured breach: the string `35.8994` and the string `28.9` both appear in the contract file
    - The binding row's `module_path` is `aprender_forecast::forecast` and its `function` is `forecast`, and `make contract-audit-phase6` prints its `resolved N Phase 6 binding row` banner with N one greater than the count recorded before this task (record both numbers)
    - The SUMMARY quotes `pv diff`'s suggested semver bump verbatim and states which bump was applied to `metadata.version` (it was 1.0.0 before this plan)
    - `.planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md` exists, its declaration line names what the phase touches instead of an external API, and the file contains zero table rows
    - No `.github/workflows/*.yml` file and no file under `crates/aprender-compute/` appears in `git status --porcelain` for this task
  </acceptance_criteria>
  <reversibility rating="reversible">A contract equation, a proof obligation, a falsification entry, one binding row and one planning-directory markdown file. Every one is a text deletion to undo, nothing is persisted outside the repo, and no consumer outside this workspace reads these files.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers) | untrusted JSON-RPC `tools/call` arguments; no auth, no session, no persistence |
| MCP client -> stdio | same arguments over a spawned process's stdin |
| demo page -> same-origin `/mcp` | browser-assembled arguments; `cap` is sent whenever the field is non-empty |

Scope note: this is a stateless local numeric door. There is no authentication, no persistence, no
PII and no multi-tenancy, so the genuine ASVS L1 surface here is INPUT VALIDATION and RESOURCE
EXHAUSTION. The register below models only that; padding it with irrelevant STRIDE categories would
be decoration.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-19 | Tampering | `ForecastArgs.cap` on a non-logistic arm (`crates/aprender-forecast/src/forecast.rs`) | high | mitigate | Task 1 refuses `cap` unless `growth == logistic`, with the message naming the fix; Task 2 asserts it through a live server; Task 3 pins it as `cap_is_logistic_only` + FALSIFY-BOUNDARY-017. This is the ASVS L1 "reject unexpected input" case: a caller-supplied parameter that the server accepted and then ignored, returning a plausible answer to a question the caller did not ask |
| T-06-20 | Repudiation | the `diagnostics` object of a forecast response | medium | mitigate | The refusal removes the class entirely rather than echoing the ignored knob: the verifier's five accepting probes returned byte-identical `diagnostics`, so a caller had no way to discover their `cap` was dropped. After Task 1 there is no accepted-and-dropped path to report |
| T-06-21 | Denial of Service | the door's option-validation order | low | accept | The new check is an `Option::is_some` test and one enum comparison, placed before any design build; its own cost is unbounded by nothing and bounded by everything. Accepted as negligible. The REAL exhaustion surface — the unbounded holiday design cost — is VERIFICATION gap 2 and is modelled in 06-11/06-12, not here |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan: no new dependency enters the graph (the change is a refusal branch, two tests, a contract block and a markdown file). Package-legitimacy gate not applicable; if that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib` — 0 failed; `prophet::parity` still 32 passed (the door change never reaches the numerics)
- `cargo test -p aprender-mcp-forecast --lib` — 30+ passed, 0 failed, including 24+ `e2e::refuses_*`
- `cargo test -p aprender-mcp-forecast --test e2e_stdio` — 1 passed (dark in CI per D-ITEM-06-06; run locally)
- `pv validate` + `pv status` on `contracts/forecast-tool-boundary-v1.yaml` — clean, counts risen from 16
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings` and `cargo fmt --all -- --check` — rc=0
- `.planning/phases/.../COVERAGE.md` matches the declaration shape with a real reason and no rows
</verification>

<success_criteria>
- VERIFICATION gap 1 is closed on all three of its `missing` bullets, each traceable to a task above
- The RED side of every new assertion was OBSERVED and is quoted in the SUMMARY (CLAUDE.md rule 4)
- SC1's "every malformed input is a validation error, never a silent default" holds for `cap`
- The three human_verification items are untouched and remain open, and the SUMMARY says so
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-10)

New file: `.planning/phases/06-native-time-series-forecasting-stack/COVERAGE.md`.

New error message string (created by this plan — not pre-existing, do not drift-check against HEAD):
`cap is logistic-only; set growth to "logistic"`.

New local binding in `crates/aprender-forecast/src/forecast.rs`: `checked_cap` (the validated cap
carried into `Spec`).

New tests: `crates/aprender-mcp-forecast/src/lib.rs` `e2e::refuses_cap_without_logistic_growth`,
`e2e::refuses_cap_with_explicit_linear_growth`. Extended (not new):
`crates/aprender-forecast/src/forecast.rs`
`forecast::tests::an_option_belonging_to_the_other_model_is_refused_not_dropped`.

New contract keys in `contracts/forecast-tool-boundary-v1.yaml`: `equations.cap_is_logistic_only`
(and its `formula` / `domain` / `codomain` / `invariants`), one new `proof_obligations` entry, and
`falsification_tests` id `FALSIFY-BOUNDARY-017`.

New binding key in `contracts/aprender/binding.yaml`: the row whose `equation:` is
`cap_is_logistic_only`.

Everything else named in this plan (`forecast`, `np_args`, `refusal`, `with`, `refused`, `synth`,
`valid_args`, `make_design`, `Growth`, `Spec`, `MAX_HOLIDAY_COLUMNS`, `FALSIFY-BOUNDARY-005`,
`contract-audit-phase6`, `prophet_logistic_with_holidays_happy_path`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-10-SUMMARY.md` when done. Record:
the pre-fix FAILING output of both new unit cases and both new e2e cases, verbatim; the before/after
passed counts for `aprender-forecast --lib`, `aprender-mcp-forecast --lib` and `e2e::refuses_`; the
`prophet::parity` count (32, unmoved); `pv diff`'s suggested semver bump and the bump applied; the
`resolved N Phase 6 binding rows` count before and after; and an explicit statement that the three
`human_verification` items from `06-VERIFICATION.md` remain OPEN and were not planned as automated work.
</output>
