---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 13
type: execute
wave: 11
gap_closure: true
depends_on: [06-12]
files_modified:
  - crates/aprender-forecast/src/prophet.rs
  - contracts/prophet-parity-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [SC1]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The changepoint-count sampler is a NAMED, DIRECTLY TESTABLE function rather than a loop buried inside `predict`, so its numerical domain can be asserted at all — today the only way to reach it is through a full logistic forecast"
    - "The sampler's mean tracks lambda across its whole reachable domain, not just where Knuth's product method is numerically valid: at lambda = 900 and lambda = 2839 the sampled mean is within the contract's relative tolerance of lambda, where the HEAD implementation saturates near 745 regardless"
    - "The RED side was OBSERVED: the falsification test was run against the unmodified Knuth sampler FIRST and recorded failing with the saturated mean printed, before any branch was added (CLAUDE.md Verification Discipline rule 4)"
    - "The fix does not move a parity rung: `prophet::parity` stays at exactly 32 passed / 0 failed, because the only logistic fixture (`wp_log_R_logistic`) has lambda far below the branch threshold and takes the unchanged Knuth path"
    - "The branch threshold is stated and justified against the fixture regime, not chosen silently: the SUMMARY records the lambda the existing logistic fixture actually produces, so the claim that no rung crosses the threshold is a measurement rather than an assertion"
    - "The degradation is no longer silent: the regime is reachable in bounds (100 daily points with `horizon: 3650, growth: \"logistic\"` gives lambda ~= 922; the 10-point minimum gives lambda ~= 2839), and after this plan the bands from those requests are computed by a sampler operating inside its domain"
    - "`contracts/prophet-parity-v1.yaml` OWNS the rule: an equation carrying the relative tolerance the test reads via `equation_tolerance`, a proof obligation, and a FALSIFY-PROPHET entry — no tolerance literal is duplicated in the test (D-15)"
  artifacts:
    - path: "crates/aprender-forecast/src/prophet.rs"
      provides: "the extracted `poisson` sampler with a valid-domain branch, and the sampler falsification tests"
      contains: "fn poisson"
    - path: "contracts/prophet-parity-v1.yaml"
      provides: "equation poisson_sampler_domain with float_tolerance + a proof obligation + a FALSIFY-PROPHET entry"
      contains: "poisson_sampler_domain"
    - path: "contracts/aprender/binding.yaml"
      provides: "a binding row resolving poisson_sampler_domain to a real definition site"
      contains: "poisson_sampler_domain"
  key_links:
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "contracts/prophet-parity-v1.yaml"
      via: "equation_tolerance(\"prophet-parity-v1\", \"poisson_sampler_domain\") read at test time — the bar lives in the contract, not in a test literal"
      pattern: "poisson_sampler_domain"
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "crates/aprender-forecast/src/prophet.rs"
      via: "predict's Growth::Logistic uncertainty arm calls the extracted poisson instead of an inline Knuth loop"
      pattern: "poisson\\(&mut rng"
  prohibitions:
    - statement: "Never loosen a parity tolerance to accommodate this change. If any `prophet::parity` assertion moves, the branch threshold is wrong (it is reaching the fixture regime) — lower the reach, do not raise the bar"
      status: resolved
      verification: test
    - statement: "Never leave the out-of-domain behaviour silent. The three accepted outcomes are a valid sampler branch, an explicit refusal, or an explicit clamp that says so in `diagnostics` — a narrower band with no warning is the defect being closed"
      status: resolved
      verification: test
    - statement: "Never hardcode the relative tolerance in the test — read it from the contract with `equation_tolerance`, as the eleven existing call sites do (D-15)"
      status: resolved
      verification: test
    - statement: "Do not touch crates/aprender-compute, .github/workflows/*.yml, or any parity fixture — scope fence for this gap-closure run"
      status: resolved
      verification: judgment
---

<objective>
Close VERIFICATION gap 3: the logistic-band changepoint sampler runs outside its numerical domain and
degrades silently, so long-horizon logistic requests get bands that are too narrow with no warning.

Purpose: `crates/aprender-forecast/src/prophet.rs:794-805` draws the simulated changepoint count with
Knuth's product method, whose `l = (-lambda).exp()` is exactly 0.0 for lambda > 745.13. Past that the
loop exits only when `pp` itself underflows, so `n_changes` becomes a function of f64 subnormal
exhaustion rather than of the Poisson law — it saturates near 745 regardless of lambda. Python Prophet
calls `np.random.poisson`, which is correct at any lambda. The regime is reachable IN BOUNDS, and
`wp_log_R_logistic` is the only logistic fixture and exercises no such horizon ratio, so the full
parity ladder passes while this path is wrong.
Output: an extracted, directly testable sampler with a valid-domain branch; a falsification test
observed RED first; and a contract equation that owns the bar.
</objective>

<gap_closure_context>
Source: `06-VERIFICATION.md` gap 3 (`truth: SC1 — the response's yhat_lower / yhat_upper are the bands
they claim to be`, `status: failed`), confirmed by the verifier reading `prophet.rs:794-805` directly.

Reachability, from the verifier's `reason` (both configurations are inside every existing bound):

| points | span | n_changepoints | horizon | growth | t_max | lambda |
|---|---|---|---|---|---|---|
| 100 daily | 99 | 25 | 3 650 | logistic | ~37.9 | **~922** |
| 10 (the MIN_POINTS floor) | 9 | — | 3 650 | logistic | — | **~2 839** |

Both are far past 745.13. The verifier confirmed the parity ladder passes (32 tests) while this path
is wrong.

The verifier's two `missing` bullets and where each lands:

| # | `missing` bullet | Task |
|---|---|---|
| 1 | A normal-approximation (or transformed-rejection) branch above lambda ~= 30, or an explicit refusal/clamp rather than silent degradation | Task 2 |
| 2 | A falsification test asserting the sampler's mean at lambda = 900 is within a few percent of 900 — today the equivalent saturates near 745 regardless of lambda | Task 1 (written and observed RED) and Task 2 (observed green) |

This plan does NOT modify, supersede or re-create 06-01 … 06-09 or their SUMMARYs.
</gap_closure_context>

<spec_and_probe_status>
No `06-SPEC.md` exists, so there are no SPEC `## Edge Coverage` / `## Prohibitions` sections to lift;
the spec-less probe fallback was SKIPPED because `phase_req_ids` is null and the probe had no input.
`must_haves` are derived from the phase goal, SC1 and the verifier's `missing` list. Recorded, not
silently omitted.
</spec_and_probe_status>

<assumption_delta_decision>
Primary noun: `forecast` (unchanged). Decision: `no-change`. Rationale: the detector's three advisory
`pluralization` signals are incidental prose (wave scheduling, a refuted cargo-CLI claim, a build-lock
precondition); none is a singular→plural, required→optional or derived→chosen transition in the
identity model. Adding a second numerical BRANCH inside one sampler is an implementation detail of a
single function, not a second variant of a modelled entity — the sampler keeps one name, one
signature and one contract equation.
</assumption_delta_decision>

<human_items_still_open>
This plan does not touch the surface of any of the three `human_verification` items and closes none of
them. The browser MCP handshake + chart rendering check, the x86_64 `quantiles_abs_f32_nonaarch64`
measurement, and the SC4-dark-in-CI decision all REMAIN OPEN.
</human_items_still_open>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md  (gap 3 only — its `reason`, `artifacts` and `missing`)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-15: tolerances live in contracts and are cited by tests, never hardcoded twice)
@.planning/phases/06-native-time-series-forecasting-stack/06-03-SUMMARY.md  (how the Prophet parity ladder and its contract-read tolerances are wired)
@CLAUDE.md  (Verification Discipline rules 4/6/8; DOGFOOD `pv`; clippy/unwrap/unsafe rules)

<interfaces>
The defect (crates/aprender-forecast/src/prophet.rs, the `Growth::Logistic` arm of `predict`'s
uncertainty block, around :790-810): per simulation row it computes
`lambda = s_cnt * (t_max - 1.0)` where `s_cnt = d.changepoints_t.len() as f64`, then draws
`n_changes` with Knuth's product method — `let l = (-lambda).exp();` then `loop { pp *= rng.uniform();
if pp <= l { break } n_changes += 1 }`. For lambda > 745.13, `l` is exactly 0.0 and the loop can only
end when `pp` underflows to zero, which happens after roughly 744 multiplications on average because
E[-ln U] = 1 and f64's smallest positive subnormal is near e^-744.

The RNG already available on the same `rng` value (crates/aprender-forecast/src/prophet.rs:626-650):
  - `pub fn uniform(&mut self) -> f64`
  - `pub fn normal(&mut self) -> f64`  (:642-646, Box-Muller from two uniforms)
  - `pub fn laplace(&mut self, b: f64) -> f64`
`normal()` is what a normal-approximation branch needs; nothing new has to be written.

The only logistic parity fixture is `wp_log_R_logistic_prophet140.json`. Its lambda must be measured,
not assumed — `lambda = n_changepoints_effective * (t_max - 1)`, and both factors come from the
fixture's own history length, changepoint count and forecast horizon.

The contract to extend: `contracts/prophet-parity-v1.yaml`. Its `band_width_rel` equation (:158-175)
is the nearest sibling — the band-width bar this sampler feeds. Existing falsification ids run to
`FALSIFY-PROPHET-012`, so the new one is `FALSIFY-PROPHET-013` (READ the file and take the next unused
id rather than trusting this sentence).

The tolerance reader (crates/aprender-forecast/src/test_support.rs:129-137):
`pub(crate) fn equation_tolerance(contract: &str, equation: &str) -> f64` reads
`equations.<equation>.float_tolerance` and panics naming the key if it is absent. `prophet.rs` is in
the same crate, so a `#[cfg(test)]` module there can call it directly — there are already eleven such
call sites in this file.

Test module layout in prophet.rs: the file currently has exactly one `#[cfg(test)] mod parity` at
:863. A sampler test belongs in its own sibling module so the parity filter
`prophet::parity` keeps meaning "the ladder" and nothing else.

Binding row shape (contracts/aprender/binding.yaml, e.g. the `band_width_rel` row at :1680-1686):
`contract:` bare filename, `equation:`, `module_path:`, `function:`, quoted `signature:`,
`status: implemented`, `notes:`. `make contract-audit-phase6` RESOLVES `function` to a definition site
in the file `module_path` maps to (`aprender_forecast::prophet` -> `crates/aprender-forecast/src/prophet.rs`),
matching a bare `name` against `fn name`, so an extracted free function resolves.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the sampler and OBSERVE it failing across its reachable domain</name>
  <files>crates/aprender-forecast/src/prophet.rs</files>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs :740-815 — the whole uncertainty block: `ns`,
      `n_future`, `n_past`, `single_diff`, `likelihood`, `mean_delta`, the `Growth::Linear` arm and the
      `Growth::Logistic` arm containing the Knuth loop. Read `Rng` (:626-650) in full, especially
      `uniform`, `normal` and `laplace`
    - crates/aprender-forecast/src/prophet.rs :315-350 — `logistic_gammas` and `piecewise_logistic`,
      the per-row work whose size grows with `n_changes`; both are linear in the changepoint count, so
      a larger `n_changes` costs linearly, not quadratically
    - crates/aprender-forecast/src/prophet.rs :863 onward — `mod parity`, for the module conventions
      and how `equation_tolerance` is called at its eleven sites
    - crates/aprender-forecast/src/test_support.rs :129-150 — `equation_tolerance` and `constant_u64`
    - .planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md gap 3 — the
      reachability table and the 745.13 threshold
  </read_first>
  <action>
    (1) EXTRACT, WITHOUT CHANGING BEHAVIOUR. Lift the Knuth loop out of the `Growth::Logistic` arm into
    a file-level free function `fn poisson(rng: &mut Rng, lambda: f64) -> usize` placed next to `Rng`,
    with the body byte-for-byte the current algorithm (`let l = (-lambda).exp();` then the
    multiply-until-`pp <= l` loop). Replace the inline loop with a call to it. This step must be a pure
    refactor: run the whole crate suite and confirm nothing moves, especially `prophet::parity` at
    exactly 32 passed / 0 failed. Extraction is not cosmetic — it is what makes the defect assertable
    at all, because today the only path to the sampler is a full logistic forecast.

    (2) MEASURE THE FIXTURE REGIME, so the threshold decision in Task 2 rests on a number. For the one
    logistic fixture `wp_log_R_logistic_prophet140.json`, compute the lambda its parity run actually
    produces (`changepoints_t.len() as f64 * (t_max - 1.0)`, with `t_max` the last future `t`). Print
    it from a temporary probe or a `--nocapture` assertion and record it in the SUMMARY. If `rtk`
    swallows the `println!` through the Bash tool (see `.planning/spikes/CONVENTIONS.md`), redirect to
    a log file and read the FILE.

    (3) WRITE THE FALSIFICATION TEST, AND RUN IT RED. Add a `#[cfg(test)] mod sampler` to prophet.rs
    with `poisson_mean_tracks_lambda_across_its_whole_domain`:
      - draw N = 20 000 samples from `poisson(&mut rng, lambda)` with a fixed seed, for each lambda in
        {5.0, 29.0, 31.0, 100.0, 900.0, 2839.0} — six points, not one, because one failing input is an
        anecdote (CLAUDE.md rule 6) and the set must straddle the branch threshold as well as the
        745.13 underflow threshold;
      - assert `|mean - lambda| / lambda <= equation_tolerance("prophet-parity-v1",
        "poisson_sampler_domain")` for each, with an assertion message naming the lambda, the observed
        mean and the bar. Do NOT hardcode the tolerance (D-15) — the contract key is added in Task 3,
        so for the RED run the test will panic in `equation_tolerance` naming the missing key; that is
        an acceptable first red, but the red that MATTERS is the one after the key exists;
      - also print, on one line per lambda, `POISSON MEAN: lambda=<L> n=<N> mean=<M> rel=<R>` so the
        saturation is visible in the log rather than inferred.
    Note the sample size is chosen so the sampling error cannot be mistaken for the defect: at
    lambda = 900 the standard error of the mean over 20 000 draws is sqrt(900/20000) ~= 0.21, i.e.
    ~0.02 % of lambda — three orders of magnitude below the ~17 % shortfall the saturation produces.
    State that arithmetic in the SUMMARY so the tolerance is not mistaken for a fudge factor.

    (4) OBSERVE THE RED. Temporarily add the contract key with a placeholder value (or read a local
    constant) so the test reaches its assertions rather than panicking on a missing key, run
    `cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture`, and record the printed
    `POISSON MEAN:` lines verbatim. The expectation from the verifier's analysis is that lambda = 900
    and lambda = 2839 both report a mean near 745 while lambda = 5, 29, 31 and 100 track. IF THE
    MEASUREMENT DISAGREES with that expectation, stop and report it — the gap's premise is a code
    reading and this is the first direct measurement of it; a contradicted premise is a finding, not
    something to work around.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-13-t1a.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t1a.log; test $rc -eq 0 && grep -E 'test result: ok\. 32 passed; 0 failed' /tmp/p06-13-t1a.log > /dev/null</automated>
    <fails_when>non-zero exit, or the summary is not exactly `test result: ok. 32 passed; 0 failed` — the extraction was not behaviour-preserving (a parity number moved) or the ladder's test count changed, meaning a test was added to or removed from the parity module by mistake</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture > /tmp/p06-13-t1b.log 2>&1; rc=$?; tail -20 /tmp/p06-13-t1b.log; test "$(grep -n 'POISSON MEAN: lambda=' /tmp/p06-13-t1b.log | wc -l | tr -d ' ')" -eq 6</automated>
    <fails_when>fewer than six `POISSON MEAN: lambda=` lines in the log — the six-point sweep did not run, or `rtk` swallowed the output through the Bash tool (in which case read the redirected FILE, which this command already does, and re-run rather than assuming the test did not print). Note this command deliberately does NOT assert rc=0: during the RED observation the test is EXPECTED to fail, and what is being verified here is that the measurement was produced at all</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-13-t1c.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t1c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — a pedantic finding on the extracted function (a `cast_precision_loss` or `cast_possible_truncation` on the count conversion is the likely one and must be handled with an explicit allow carrying a reason, or by restructuring, not by widening a workspace lint), or a disallowed `unwrap()`</fails_when>
  </verify>
  <done>`poisson` is a named free function with the current algorithm unchanged; the parity ladder is still exactly 32 passed / 0 failed; a six-lambda sweep printed its means; and the saturation near 745 at lambda 900 and 2839 is recorded verbatim as the observed RED.</done>
  <acceptance_criteria>
    - `grep -n 'fn poisson' crates/aprender-forecast/src/prophet.rs` prints exactly one line number, and `grep -c 'poisson(&mut rng' crates/aprender-forecast/src/prophet.rs` is at least 1 (the `predict` call site)
    - `grep -n 'mod sampler' crates/aprender-forecast/src/prophet.rs` prints exactly one line number — the sampler tests live in their own module so `prophet::parity` keeps meaning "the ladder"
    - `cargo test -p aprender-forecast --lib prophet::parity` reports exactly `32 passed; 0 failed` AFTER the extraction (record the same run before it, to prove the refactor was behaviour-preserving)
    - The SUMMARY records the measured lambda of the `wp_log_R_logistic` fixture as a number, with how it was computed, and states whether it is above or below 30
    - The SUMMARY quotes all six `POISSON MEAN:` lines from the RED run verbatim, and states the observed mean at lambda = 900 and lambda = 2839
    - The SUMMARY states the standard-error arithmetic (sqrt(lambda/N) at lambda = 900, N = 20 000) and why it rules out sampling noise as the explanation
    - If the RED measurement contradicted the expected saturation, the SUMMARY says so prominently and the task HALTS for review rather than proceeding to Task 2
  </acceptance_criteria>
  <reversibility rating="reversible">A pure function extraction with one call site, plus a `#[cfg(test)]` module. Reverting is inlining the loop again; no behaviour, no persisted state and no published surface changes in this task.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Add the valid-domain branch, observe GREEN, and prove the parity ladder did not move</name>
  <files>crates/aprender-forecast/src/prophet.rs</files>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs — the extracted `poisson` from Task 1, `Rng::normal`
      (:642-646), and the `Growth::Logistic` uncertainty arm that consumes `n_changes` (the `new`
      vector allocation, the sort, and the `piecewise_logistic` re-evaluation per simulation row)
    - .planning/phases/06-native-time-series-forecasting-stack/06-13-SUMMARY.md drafts / Task 1's
      recorded output — the measured fixture lambda and the six RED means, which decide the threshold
    - crates/aprender-forecast/src/prophet.rs :315-350 — `logistic_gammas` (linear in the changepoint
      count) and `piecewise_logistic` (a single monotone merge), which is why a larger `n_changes`
      costs linearly rather than quadratically per simulation row
  </read_first>
  <action>
    (1) BRANCH. In `poisson`, keep the existing Knuth product method for `lambda <= 30.0` byte-for-byte,
    and add a normal-approximation branch for `lambda > 30.0`:
    draw `z = rng.normal()`, form `lambda + lambda.sqrt() * z`, round to nearest, clamp at zero, and
    return it as a `usize`. The mean and the variance of that draw are both exactly lambda, which is
    what the falsification test measures. Use a `#[allow(clippy::cast_..., reason = "...")]` with a
    written reason on the one conversion that needs it rather than widening a workspace lint, and never
    `unwrap()` (banned by `.clippy.toml`).

    Choose 30.0 because it is the threshold the verifier named and because it is far above the
    fixture regime Task 1 MEASURED — state both facts and the measured fixture lambda in a code
    comment on the branch, so a future reader can see the parity argument without re-deriving it.
    If Task 1's measurement puts the fixture lambda at or above 30, DO NOT proceed with 30: raise the
    threshold above the fixture regime with margin, re-run the ladder, and record why the verifier's
    suggested value could not be used.

    The comment must also record WHY the normal approximation is the right shape here rather than a
    transformed-rejection port of numpy's PTRS: this sampler never matched `np.random.poisson`
    stream-for-stream anyway (the RNG is this file's own seeded generator, not MT19937), the parity bar
    it feeds is `band_width_rel` — a RELATIVE bar on mean band width, stated in the contract as a
    Monte-Carlo estimate — and matching lambda's mean and variance is exactly what a band-width
    estimate depends on.

    (2) OBSERVE GREEN across the whole sweep. Re-run
    `cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture` and record all six
    `POISSON MEAN:` lines. Put them beside Task 1's RED lines in the SUMMARY as a before/after table:
    lambda 5 / 29 unchanged (Knuth, below the branch), 31 / 100 / 900 / 2839 now tracking lambda.

    (3) PROVE NO RUNG MOVED. Run the full parity ladder and the full crate suite. `wp_log_R_logistic`
    is the only logistic fixture; if its lambda is below the threshold it takes the unchanged Knuth
    path and its numbers cannot move. If any parity assertion DOES move, the threshold is reaching the
    fixture regime — raise it, do not loosen the bar.

    (4) MEASURE THE COST OF THE FIX, because it makes `n_changes` larger in exactly the regime that was
    previously truncated. Time one in-bounds worst case — 100 daily points, `horizon: 3650`,
    `growth: "logistic"` with a valid cap — before and after, through `aprender_forecast::forecast` on
    a release build, and record both walls against SC1's 2 s bar. The per-row work
    (`logistic_gammas` + a sort + `piecewise_logistic`) is linear in the changepoint count, so ~2839
    versus ~745 changes is a bounded multiple rather than a blow-up — but that is an argument, and the
    two numbers are evidence. If the after wall crosses 2 s, that is a NEW finding: report it with the
    numbers rather than reverting the correctness fix, because a fast wrong band is not the goal.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture > /tmp/p06-13-t2a.log 2>&1; rc=$?; tail -20 /tmp/p06-13-t2a.log; test $rc -eq 0 && test "$(grep -n 'POISSON MEAN: lambda=' /tmp/p06-13-t2a.log | wc -l | tr -d ' ')" -eq 6 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-13-t2a.log > /dev/null</automated>
    <fails_when>non-zero exit — the sampled mean at one of the six lambdas is outside the contract's relative tolerance, which for lambda 900 or 2839 means the branch is not being taken and for lambda 5 or 29 means the Knuth path was disturbed; fewer than six `POISSON MEAN:` lines (the sweep did not run in full); or no `test result: ok. N passed; 0 failed` line</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-13-t2b.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t2b.log; test $rc -eq 0 && grep -E 'test result: ok\. 32 passed; 0 failed' /tmp/p06-13-t2b.log > /dev/null</automated>
    <fails_when>non-zero exit, or the summary is not exactly `test result: ok. 32 passed; 0 failed` — the branch threshold is reaching the `wp_log_R_logistic` fixture regime, in which case RAISE the threshold above the measured fixture lambda; never loosen a parity tolerance to accommodate this change</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-13-t2c.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t2c.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-13-t2c.log > /dev/null</automated>
    <fails_when>non-zero exit — any crate test regressed, including the door refusals 06-10/06-11 added and the `np::parity` ladder; or no `test result: ok. N passed; 0 failed` line</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-13-t2d.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t2d.log; test $rc -eq 0; CARGO_INCREMENTAL=0 cargo fmt --all -- --check > /tmp/p06-13-t2e.log 2>&1; rc2=$?; tail -5 /tmp/p06-13-t2e.log; test $rc -eq 0 && test $rc2 -eq 0</automated>
    <fails_when>either non-zero: a pedantic clippy finding on the branch (a numeric-cast lint is the likely one and must carry a written `reason`, not a widened workspace lint), a disallowed `unwrap()`, or a `cargo fmt --all -- --check` diff</fails_when>
  </verify>
  <done>`poisson` keeps Knuth below the threshold and uses a mean-and-variance-exact normal draw above it; all six sampled means track lambda within the contract tolerance; `prophet::parity` is still exactly 32 passed / 0 failed; and the worst in-bounds logistic wall is measured before and after against the 2 s bar.</done>
  <acceptance_criteria>
    - `grep -c 'normal()' crates/aprender-forecast/src/prophet.rs` is at least 2 (the pre-existing observation-noise draw at the end of `predict`, plus the new branch)
    - The branch threshold appears as a named value or literal in `poisson` with a code comment stating (a) the 745.13 underflow point of Knuth's `(-lambda).exp()`, (b) the MEASURED `wp_log_R_logistic` lambda from Task 1, and (c) why a normal approximation rather than a PTRS port
    - The SUMMARY carries the six-lambda BEFORE/AFTER table with the actual `POISSON MEAN:` lines from both runs, showing the HEAD means at lambda 900 and 2839 saturating near 745 and the post-fix means tracking lambda
    - `cargo test -p aprender-forecast --lib prophet::parity` reports exactly `32 passed; 0 failed`
    - The SUMMARY records the release-profile wall of the 100-point / horizon-3650 / logistic worst case BEFORE and AFTER the branch, with both compared to SC1's 2 s bar; if the after wall crosses 2 s, the finding is reported with both numbers rather than the fix reverted
    - The SUMMARY states which of the three accepted outcomes was taken (valid sampler branch / explicit refusal / explicit clamp reported in `diagnostics`) and why — the plan expects the branch, and any other choice needs its reason recorded
    - If the threshold had to be raised above 30 because the fixture lambda was not below it, the SUMMARY says so with the measured number
  </acceptance_criteria>
  <reversibility rating="costly">The band numbers a long-horizon logistic request returns CHANGE — they get wider, which is the point, but any consumer who recorded the old (too narrow) bands sees a different answer for the same seed and inputs. Undo is deleting one branch, with no migration and no persisted state, so it is not one-way. Taken deliberately: the old numbers were produced by a sampler operating outside its numerical domain, so they were never the bands the response claimed, and SC1's `yhat_lower`/`yhat_upper` promise is what makes the change mandatory rather than optional.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Give the sampler's domain a contract identity — equation with the tolerance, obligation, FALSIFY entry, binding row</name>
  <files>contracts/prophet-parity-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - contracts/prophet-parity-v1.yaml — the `equations:` block, especially `band_width_rel`
      (:158-175) whose `float_tolerance` shape and RELATIVE-bar rationale the new equation mirrors;
      the `proof_obligations:` block; the whole `falsification_tests:` block, to take the next unused
      `FALSIFY-PROPHET-0NN` id rather than assuming one
    - contracts/aprender/binding.yaml :1679-1700 — the `band_width_rel` and `trend_band_width_rel`
      rows, for key order and the `aprender_forecast::prophet` module-path convention
    - crates/aprender-forecast/src/test_support.rs :129-137 — `equation_tolerance` reads
      `equations.<key>.float_tolerance` and panics naming the key if absent, so the key name and the
      test's string argument must match exactly
    - Makefile, the `contract-audit-phase6` target — the BIND- and RESOLVE- steps the new row must pass
    - .planning/phases/06-native-time-series-forecasting-stack/06-VERIFICATION.md gap 3 — the measured
      reachability numbers the invariants should cite
  </read_first>
  <action>
    (1) EQUATION. Add `poisson_sampler_domain` to `contracts/prophet-parity-v1.yaml`'s `equations:`,
    placed beside `band_width_rel` because that is the bar this sampler feeds. Give it a
    `float_tolerance` set to the RELATIVE bar the Task 1 test reads — the verifier asked for "within a
    few percent"; pick the value from the measured post-fix means with headroom over the sampling
    error, and record in the SUMMARY both the chosen value and the standard-error arithmetic that
    shows it is a real bar rather than a fudge (sqrt(lambda/N) at lambda = 900, N = 20 000, is ~0.02 %
    of lambda). The `formula` states that for every reachable lambda the sampled mean is within
    `float_tolerance` of lambda relatively; the `domain` is the changepoint-count draw of the logistic
    uncertainty path; the `codomain` is a non-negative count whose mean and variance are both lambda.

    Its `invariants` must state, in the file's own voice:
      - the numerical fact: Knuth's product method computes `l = (-lambda).exp()`, which is exactly
        0.0 for lambda > 745.13, after which the loop ends only on f64 subnormal underflow, so the
        count saturates near 745 regardless of lambda;
      - the REACHABILITY, in bounds: 100 consecutive daily points (span 99, n_changepoints 25) with
        `horizon: 3650, growth: "logistic"` gives t_max ~= 37.9 and lambda ~= 922, and the 10-point
        `fit_min_points` floor with the same horizon gives lambda ~= 2839;
      - that Python Prophet calls `np.random.poisson`, which is correct at any lambda, so this is a
        PARITY defect and not a design difference;
      - that `wp_log_R_logistic` is the only logistic fixture and its measured lambda (quote the number
        Task 1 measured) is far below the branch threshold, which is WHY the full 32-test ladder passed
        while this path was wrong — the ladder's silence here is a coverage fact, recorded so it is not
        mistaken for evidence;
      - that the sampler's stream is this file's own seeded `Rng`, never MT19937, so the bar is on the
        DISTRIBUTION's mean and variance and never on a draw-for-draw match with numpy.

    (2) PROOF OBLIGATION. Add a `type: invariant` entry whose `property` is that the changepoint-count
    sampler operates inside its numerical domain for every lambda the door can produce, and whose
    `formal` reads `forall lambda in (0, s_max * (max_horizon_ratio - 1)]:
    |E[poisson(lambda)] - lambda| <= float_tolerance * lambda`, `applies_to: all`.

    (3) FALSIFICATION TEST. Add the next unused `FALSIFY-PROPHET-0NN` id (read the file; the highest
    existing is 012). Its `prediction` names
    `prophet::sampler::poisson_mean_tracks_lambda_across_its_whole_domain` and states the six lambdas
    and the sample size, and says explicitly that the PRE-FIX implementation reports a mean near 745 at
    lambda 900 and 2839 — so the entry records the red side, not only the green. Its `test` is the
    runnable command. Its `if_fails` says the logistic bands are back to being computed by a sampler
    outside its domain, so `yhat_lower` / `yhat_upper` are narrower than the interval they advertise,
    with nothing in `diagnostics` to say so.

    (4) VERSION. Materialise the pre-change file and let the dogfooded tool judge:
    `git show HEAD:contracts/prophet-parity-v1.yaml > /tmp/pp-old-13.yaml` then
    `cargo run --release -p aprender-contracts-cli --bin pv -- diff /tmp/pp-old-13.yaml
    contracts/prophet-parity-v1.yaml`. Record the suggested bump verbatim and apply it. `pv diff` takes
    TWO FILESYSTEM PATHS, never a git revision (CLAUDE.md) — passing `HEAD` reports a missing-file
    error that reads like a missing contract.

    (5) BINDING ROW. Append one row: `contract: prophet-parity-v1.yaml`,
    `equation: poisson_sampler_domain`, `module_path: aprender_forecast::prophet`,
    `function: poisson`, the real `signature` (`fn poisson(rng: &mut Rng, lambda: f64) -> usize`),
    `status: implemented`, and a `notes` line saying the sampler was extracted from `predict`'s
    logistic uncertainty arm precisely so its domain could be asserted directly. `function: poisson`
    resolves because `fn poisson` has a definition site in `crates/aprender-forecast/src/prophet.rs`.

    (6) Re-run `pv validate`, `pv status` and `make contract-audit-phase6`, and re-run the sampler test
    so it now reads its bar from the real contract key rather than from the Task 1 placeholder. Never
    substitute a yq/python/bash script for any of the `pv` commands (CLAUDE.md: that is muda and will
    be rejected).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/prophet-parity-v1.yaml > /tmp/p06-13-t3a.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t3a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-13-t3a.log</automated>
    <fails_when>non-zero exit, or no `0 error(s)` line — the new equation, obligation or falsification entry is malformed for the KernelContract schema. Build `pv` through cargo, never a `target/release/pv` path: the gitignored `.cargo/config.toml` redirects target-dir and CLAUDE.md rule 8 makes a stale binary that validates the new block the exact failure mode</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::sampler > /tmp/p06-13-t3b.log 2>&1; rc=$?; tail -10 /tmp/p06-13-t3b.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-13-t3b.log > /dev/null</automated>
    <fails_when>non-zero exit — most likely `equation_tolerance` panicking because `equations.poisson_sampler_domain.float_tolerance` is absent or misspelled (the panic names the key), or a sampled mean outside the bar the contract now sets; or no `test result: ok. N passed; 0 failed` line</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-13-t3c.log 2>&1; rc=$?; tail -12 /tmp/p06-13-t3c.log; test $rc -eq 0 && test "$(grep -c '^BIND- ' /tmp/p06-13-t3c.log)" -eq 0 && test "$(grep -c '^RESOLVE- ' /tmp/p06-13-t3c.log)" -eq 0 && test "$(grep -n 'Total equations:' /tmp/p06-13-t3c.log | wc -l | tr -d ' ')" -eq 4</automated>
    <fails_when>non-zero exit; any `BIND-` line (the new equation has no binding row); any `RESOLVE-` line (the row names a function with no definition site — most likely if `poisson` was left inside an `impl` block, where a bare-name search would still match `fn poisson` but the notes would be wrong, or if it was named differently); or fewer than four `Total equations:` summaries, which means an audit did not run and the green measured nothing</fails_when>
    <automated>make contract-validate > /tmp/p06-13-t3d.log 2>&1; rc=$?; tail -8 /tmp/p06-13-t3d.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — one of the workspace's contracts no longer validates, which after this plan would mean the `prophet-parity-v1.yaml` edit broke a file the $(CONTRACTS) loop covers</fails_when>
  </verify>
  <done>`prophet-parity-v1.yaml` owns `poisson_sampler_domain` with the relative tolerance the test reads, a proof obligation and a FALSIFY-PROPHET entry that records both the red and the green side; `binding.yaml` carries a row that audits and RESOLVES; `pv validate` and `make contract-audit-phase6` are clean.</done>
  <acceptance_criteria>
    - `grep -c 'poisson_sampler_domain' contracts/prophet-parity-v1.yaml` is at least 1 and `grep -n 'equation: poisson_sampler_domain' contracts/aprender/binding.yaml` prints exactly one line number
    - `grep -c 'float_tolerance' contracts/prophet-parity-v1.yaml` increased by exactly 1 relative to HEAD (record both counts) — the tolerance is contract-owned and appears once
    - `grep -c 'equation_tolerance("prophet-parity-v1", "poisson_sampler_domain")' crates/aprender-forecast/src/prophet.rs` is at least 1, and the sampler test contains no numeric tolerance literal (D-15: the bar lives in the contract, not in two places)
    - The equation's invariants cite the numbers: the strings `745.13`, `922` and `2839` all appear in the contract file
    - The new falsification entry's `prediction` names `poisson_mean_tracks_lambda_across_its_whole_domain`, the six lambdas and the sample size, and states the pre-fix saturation near 745
    - The binding row's `module_path` is `aprender_forecast::prophet` and its `function` is `poisson`; `make contract-audit-phase6` prints its `resolved N Phase 6 binding row` banner with N one greater than the count 06-12 recorded
    - The SUMMARY quotes `pv diff`'s suggested semver bump verbatim and states `prophet-parity-v1.yaml`'s version before and after
    - The SUMMARY records the chosen `float_tolerance` and the standard-error arithmetic showing it is a real bar
  </acceptance_criteria>
  <reversibility rating="reversible">A contract equation with a tolerance, a proof obligation, a falsification entry and one binding row — all text deletions to undo, with no external consumer and nothing persisted. (The behavioural change this documents was taken, and rated, in Task 2.)</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers, default K=8) | untrusted JSON-RPC `tools/call` arguments reach the logistic band path via `growth`, `horizon` and `cap` |
| forecast response -> client | `yhat_lower` / `yhat_upper` are consumed as an interval with a stated width |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. The
genuine ASVS L1 surface is INPUT VALIDATION and RESOURCE EXHAUSTION. This gap is neither an injection
nor an authz defect — it is a correctness defect in an output the caller is told to trust, so the
register models integrity-of-output and the resource consequence of fixing it.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-28 | Tampering | the logistic band path (`crates/aprender-forecast/src/prophet.rs`, `predict`'s `Growth::Logistic` uncertainty arm) | **high** (ASVS L1 blocking threshold) | mitigate | A caller-reachable input combination (`growth: "logistic"` with a large `horizon`/history ratio, both inside every existing bound) silently changes the meaning of the response: the interval comes back narrower than the `interval_width` it advertises, because the sampler runs past f64's exp-underflow point. An output whose semantics change without the caller being told is an integrity defect, not a precision preference. Task 2 adds the valid-domain branch; Tasks 1 and 3 make it assertable and contract-owned |
| T-06-29 | Repudiation | `diagnostics` on a long-horizon logistic response | medium | mitigate | The degradation left no trace — nothing in `diagnostics`, no `warning`, no `budget_hit`. The chosen mitigation removes the degradation rather than reporting it; if Task 2 instead lands a clamp or a refusal, that outcome MUST be surfaced in `diagnostics` (recorded as an explicit acceptance criterion), because a silent clamp is the same defect wearing a different hat |
| T-06-30 | Denial of Service | the cost of the fix (`n_changes` grows from ~745 to ~lambda in the affected regime) | medium | mitigate | Correcting the sampler makes the per-row changepoint list up to ~3.8x longer at lambda 2839. The per-row work (`logistic_gammas` + a sort + `piecewise_logistic`) is LINEAR in that count, so the increase is a bounded multiple — but Task 2 MEASURES the worst in-bounds logistic wall before and after against SC1's 2 s bar rather than arguing it, and reports a crossing as a new finding |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan: the change uses `Rng::normal`, which is already in this file. No new dependency enters the graph, so the package-legitimacy gate is not applicable. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture` — 6 `POISSON MEAN:` lines, all within the contract's relative bar, and the pre-fix RED run recorded beside it
- `cargo test -p aprender-forecast --lib prophet::parity` — exactly 32 passed, 0 failed, before AND after the branch
- `cargo test -p aprender-forecast --lib` — 0 failed
- `pv validate contracts/prophet-parity-v1.yaml` — `0 error(s)`; `make contract-validate` — rc=0
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings` and `cargo fmt --all -- --check` — rc=0
- The worst in-bounds logistic wall (100 points, horizon 3650, logistic) measured on release before and after, against SC1's 2 s bar
</verification>

<success_criteria>
- VERIFICATION gap 3 is closed on both of its `missing` bullets, each traceable to a task above
- The RED side was OBSERVED at six lambdas and is quoted in the SUMMARY, not asserted
- No parity rung moved, and the reason (the measured fixture lambda sits below the threshold) is a number rather than a claim
- The bar lives in `contracts/prophet-parity-v1.yaml` and the test reads it — no duplicated tolerance literal
- The three human_verification items remain untouched and open
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-13)

New Rust function: `aprender_forecast::prophet::poisson` (extracted from `predict`'s logistic
uncertainty arm, then given a valid-domain branch).

New test module and test: `crates/aprender-forecast/src/prophet.rs` `mod sampler`, containing
`poisson_mean_tracks_lambda_across_its_whole_domain`.

New stdout token: `POISSON MEAN:` with fields `lambda=`, `n=`, `mean=`, `rel=`.

New contract keys in `contracts/prophet-parity-v1.yaml`: `equations.poisson_sampler_domain` (with its
`formula` / `domain` / `codomain` / `invariants` / `float_tolerance`), one new `proof_obligations`
entry, and one new `falsification_tests` id in the `FALSIFY-PROPHET-0NN` series (the next unused one;
the highest existing at HEAD is 012).

New binding key in `contracts/aprender/binding.yaml`: the row whose `equation:` is
`poisson_sampler_domain`.

Symbols earlier plans in this gap-closure run create (not pre-existing at HEAD, do not drift-check
against HEAD): from 06-10 — `cap_is_logistic_only`, `FALSIFY-BOUNDARY-017`,
`refuses_cap_without_logistic_growth`, `refuses_cap_with_explicit_linear_growth`, `checked_cap`,
`COVERAGE.md`; from 06-11 — `MAX_HOLIDAY_DESIGN_COST`, `MAX_HOLIDAY_DATES_TOTAL`,
`holiday_day_sets`, `HOLIDAY DESIGN WALL:`, `forecast-holiday-bench`,
`constants.fit_max_holiday_design_cost`, `constants.fit_max_holiday_dates_total`; from 06-12 —
`holiday_design_cost_bounded`, `refuses_holiday_design_cost_over_bound`,
`accepts_holiday_design_cost_just_under_bound`, `HOLIDAY DESIGN OK`.

Everything else named in this plan (`predict`, `Rng`, `Rng::normal`, `Rng::uniform`, `Rng::laplace`,
`piecewise_logistic`, `logistic_gammas`, `Growth`, `band_width_rel`, `trend_band_width_rel`,
`equation_tolerance`, `wp_log_R_logistic_prophet140.json`, `contract-audit-phase6`,
`contract-validate`, `FALSIFY-PROPHET-012`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-13-SUMMARY.md` when done. Record:
the measured `wp_log_R_logistic` lambda and how it was computed; the six-lambda BEFORE/AFTER
`POISSON MEAN:` table verbatim, showing the HEAD saturation near 745 at lambda 900 and 2839; the
standard-error arithmetic justifying the sample size and the chosen `float_tolerance`; the branch
threshold and why it was chosen (and if it had to be raised above 30, the measurement that forced it);
the `prophet::parity` count (32) before and after; the release-profile worst-case logistic wall before
and after against SC1's 2 s bar; which of the three accepted outcomes (branch / refusal / reported
clamp) was taken; `pv diff`'s suggested semver bump and the version before/after; the
`resolved N Phase 6 binding rows` count before and after; and an explicit statement that the three
`human_verification` items remain OPEN and were not planned as automated work.
</output>
