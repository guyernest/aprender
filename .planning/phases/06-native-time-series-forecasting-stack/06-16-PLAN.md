---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 16
type: execute
wave: 14
gap_closure: true
depends_on: [06-15]
files_modified:
  - crates/aprender-forecast/src/sc1_wall.rs
  - crates/aprender-forecast/src/lib.rs
  - crates/aprender-forecast/src/prophet.rs
  - scripts/assert_measurement_under.sh
  - scripts/check_assert_measurement_under_cases.sh
  - justfile
  - contracts/forecast-tool-boundary-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [SC1]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ONE SC1 gate sweeps the surface where the cost is decided, not the one axis each bench was born from: `just forecast-sc1-sweep` runs freq {D, W, MS} x growth {linear, logistic, flat} x holiday {none, at-the-design-cost-bound} at the tightest legal history span, on a release build, and FAILS if any composition's `total_s` is at or above 2.0 s"
    - "THE GATE IS `just forecast-sc1-sweep` AT THE AT-THE-BOUND GEOMETRY, and the same harness ALSO runs in the ordinary `--lib` suite at a reduced default geometry as a SHAPE check. Both statements are needed and neither is the other: the in-suite run proves every composition is still ACCEPTED by the door, still produces a parsable measurement line and has not rotted — it does NOT assert the 2 s SC1 bar, because its geometry is far from any bound and its profile is debug. Nothing is `#[ignore]`d and no `--ignored` flag exists anywhere in this harness, which is the specific defect being closed: `logistic_band_wall` could only ever be reached with `--ignored`, had no recipe, no bar and no `freq` knob, and all four of its recorded lines were `freq: \"D\"`, the one value that does not exhibit CR-01"
    - "The in-suite cost is budgeted and measured, not assumed: the default matrix's wall in CI's debug `--lib` run is MEASURED and recorded, and must come in under 60 s. `[profile.dev.package.aprender-forecast] opt-level = 3` covers this crate but not its dependencies, and this sweep joins a job that already runs 80 604 tests, so an unbudgeted 18-composition addition including logistic with 1 000 uncertainty samples is a real CI regression"
    - "The sweep would have caught CR-01: at the pre-06-14 bound the `freq: \"MS\"` x `growth: \"logistic\"` composition is the 2.334 s configuration the review measured, and the plan RECORDS a run of the sweep against a deliberately raised `fit_max_logistic_changepoint_lambda` observing that composition FAIL. A gate that cannot be shown failing on the defect it exists for is theater (CLAUDE.md rule 5)"
    - "IN-01 closed at ALL FIVE bar sites, not just the new one: no SC1/SC4/SC5 bar in the justfile reads a measurement through `awk -v v=\"$x\" 'BEGIN { exit (v + 0 < BAR) ? 0 : 1 }'`, which coerces a non-numeric token to 0 and prints OK. A single shared validator script checks the SHAPE first and exits non-zero on a token that is empty, non-numeric, or multi-dotted"
    - "The validator ships a case table and the table is RUN, not read: `scripts/check_assert_measurement_under_cases.sh` drives a must-match / must-not-match table through the validator (CLAUDE.md rule 7 — the `apr`-invocation patterns were wrong five times and every one was caught by a case table, none by review), and `just forecast-sc1-sweep` invokes it before it measures anything"
    - "The three geometry-hard-coded benches are folded into one shared builder: `holiday_design_wall` and `logistic_band_wall` either call the sweep's composition builder or are deleted in favour of a sweep case, so no bench can be blind to another's axis again. `forecast-holiday-bench` keeps working as a single-composition entry point onto the same code"
    - "WR-04's second half is fixed: the `forecast-holiday-bench` comment no longer claims its defaults are the worst shape the door accepts — a claim the same comment block contradicts nine lines later by recording an accepted 4 700-point / 5-column request walling at ~4.2 s. It says what the three measured compositions actually support"
  artifacts:
    - path: "crates/aprender-forecast/src/sc1_wall.rs"
      provides: "the parameterized SC1 sweep: one composition builder over freq x growth x holiday shape, one measurement line per case, one bar assertion, no #[ignore]"
      contains: "SC1 WALL:"
    - path: "scripts/assert_measurement_under.sh"
      provides: "the shared numeric-shape validator every wall-clock bar in the justfile now calls"
      contains: "assert_measurement_under"
    - path: "scripts/check_assert_measurement_under_cases.sh"
      provides: "the must-match / must-not-match case table that proves the validator rejects what it claims to reject"
      contains: "MUST_FAIL"
    - path: "justfile"
      provides: "the forecast-sc1-sweep recipe, and all five bar sites routed through the shared validator"
      contains: "forecast-sc1-sweep"
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "the sc1_wall_swept equation naming the swept surface, its obligation and its FALSIFY entry"
      contains: "sc1_wall_swept"
  key_links:
    - from: "justfile"
      to: "crates/aprender-forecast/src/sc1_wall.rs"
      via: "forecast-sc1-sweep runs the sweep under --release and re-checks each SC1 WALL: line's total_s through the shared validator, so the bar is asserted in TWO places and neither can pass vacuously"
      pattern: "SC1 WALL:"
    - from: "justfile"
      to: "scripts/assert_measurement_under.sh"
      via: "every wall-clock bar site calls the shared validator instead of an inline awk coercion"
      pattern: "assert_measurement_under.sh"
    - from: "scripts/check_assert_measurement_under_cases.sh"
      to: "scripts/assert_measurement_under.sh"
      via: "the case table drives the validator with tokens that must pass and tokens that must fail; forecast-sc1-sweep runs the table before it measures"
      pattern: "assert_measurement_under.sh"
  prohibitions:
    - statement: "Never mark the sweep `#[ignore]`. The whole finding is that the one harness watching the axis CR-01 lives on could only be reached with an explicit flag. If the sweep is too slow for the default `--lib` run, make it a smaller default matrix that still covers all three freq values and all three growth arms, and let the justfile widen it by env — do not hide it behind a flag"
      status: resolved
      verification: test
    - statement: "Never assert a wall-clock bar inside a libtest that runs in CI's debug `--lib` sweep. This crate carries `[profile.dev.package.aprender-forecast] opt-level = 3`, so a debug wall looks plausible and is not the SC1 bar. The sweep PRINTS under any profile and ASSERTS the bar only when `cfg!(debug_assertions)` is false — and it prints `profile=` either way so a reader can tell (CLAUDE.md rule 2)"
      status: resolved
      verification: test
    - statement: "Never leave any of the five bar sites on the old inline awk coercion. Extending a guard's SCOPE requires re-mutating in the new scope (CLAUDE.md rule 4): each converted site gets a garbage token fed through it once, observed failing, and restored"
      status: resolved
      verification: test
    - statement: "Never touch a parity fixture, a parity tolerance, or the numbers in the three `*-parity-v1.yaml` files"
      status: resolved
      verification: test
    - statement: "Do not touch `crates/aprender-compute`, `.github/workflows/*.yml`, or `crates/aprender-serve` — scope fence. Wiring the sweep into CI is a human decision, not this plan's"
      status: resolved
      verification: judgment
---

<objective>
Replace three geometry-hard-coded benches, each blind to the others' axis, with ONE parameterized SC1
gate that sweeps the surface where the cost is actually decided — and fix the measurement defect that
let a bad bar pass.

Purpose: `06-REVIEW.md` WR-04 is CLAUDE.md Verification Discipline rule 5 in the concrete. Each round
added a bench hard-coded to the geometry it was born from: `forecast-bench` sweeps point count with no
holidays, `forecast-holiday-bench` sweeps the holiday axis, and `logistic_band_wall` — added by 06-13
*precisely because* its fix raises per-sample work — is `#[ignore]`d, has no `just` recipe, no bar and
no `freq` knob, and all four of its recorded lines are `freq: "D"`, the one value that does not
exhibit CR-01 (0.231 s versus 2.334 s for `"MS"` at the same points and horizon). Nothing in the phase
could have caught CR-01. And IN-01 is the same disease one layer down: `awk -v v="$total" 'BEGIN {
exit (v + 0 < 2.0) ? 0 : 1 }'` coerces a non-numeric measurement to 0 and prints OK — a gate that
cannot fail, at four pre-existing sites plus the one 06-12 added.

Output: `crates/aprender-forecast/src/sc1_wall.rs` sweeping freq x growth x holiday shape with a real
bar and no `#[ignore]`; `just forecast-sc1-sweep` wired as the gate; a shared numeric-shape validator
with a case table that is RUN; and a recorded run of the sweep FAILING on a deliberately unbounded
lambda, which is the evidence that this gate would have caught CR-01.
</objective>

<gap_closure_context>
Source: `.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md`, WR-04 and IN-01.
`06-VERIFICATION.md` is STALE — it predates 06-10..06-13, which closed all three of its gaps.

| Finding | Where it lands |
|---|---|
| **WR-04** — the SC1 gate surface covers only the two shapes the phase chose; the harness 06-13 added for the third is `#[ignore]`d, has no recipe, no bar and no `freq` knob; and the recipe's comment asserts a superlative its own next paragraph contradicts | Task 1 (the sweep), Task 2 (the recipe), Task 3 (folding the benches in, rewording the comment) |
| **IN-01** — `justfile:844`'s `awk -v v="$total" 'BEGIN { exit (v + 0 < 2.0) ? 0 : 1 }'` reads a non-numeric measurement as 0 and passes; the same pattern is pre-existing at `:620`, `:680` and `:742` | Task 2, at all five sites, with a case table |

The four measured rows behind the sweep's axis choice (`06-REVIEW.md` CR-01; release, `--http --pool 1`,
min of 2 runs, HEAD column):

| composition | HEAD |
|---|---|
| 33 pts / 1 d spacing, `horizon: 3650`, `freq: "MS"`, `growth: "logistic"`, `cap: 50` | **2.334 s** |
| same but `freq: "W"` | 0.634 s |
| same but `freq: "D"` | 0.231 s |
| same but `growth: "linear"` — the arm that does not call `poisson` | 0.149 s |

Three of these four differ ONLY in `freq`. `logistic_band_wall` has no `freq` knob. That is the whole
finding.

This plan does NOT modify, supersede or re-create 06-01 … 06-15 or their SUMMARYs.
</gap_closure_context>

<review_dispositions_ledger>
## Review Dispositions Ledger — all 9 findings in `06-REVIEW.md`

Repeated verbatim in every round-3 plan so no plan can be read in isolation and conclude a finding was
dropped.

| ID | Severity | Disposition | Landing site |
|---|---|---|---|
| CR-01 | Critical | **INCORPORATED** | 06-14 T1/T3; **06-16 (this plan) — the sweep gate that would have caught it, proven by a recorded failing run** |
| WR-01 | Warning | **INCORPORATED, SPLIT** | 06-14 T2 (contract mirror via `constant_f64`); 06-17 T1 (the tight sub-threshold sweep point the normal branch actually fails) |
| WR-02 | Warning | **INCORPORATED** | 06-17 T1 (sample-variance assertion with its own contract-owned tolerance; zero-variance stub proven RED) |
| WR-03 | Warning | **INCORPORATED** | 06-15 T1 (aggregate-dates refusal moved into the holiday loop; exact-total refusal kept) |
| WR-04 | Warning | **INCORPORATED** | **06-16 T1/T2/T3 (this plan)** |
| IN-01 | Info | **INCORPORATED** | **06-16 T2 (this plan)** — shared validator at all five sites, with a must-match/must-not-match case table |
| IN-02 | Info | **INCORPORATED, NARROWED** | 06-17 T2 (`hol_sets.get(hi).is_some_and(…)` + a semver note; the signature is NOT reverted) |
| IN-03 | Info | **INCORPORATED** | 06-14 T2 (doc count corrected; `DEFAULT_POOL` split out) |
| IN-04 | Info | **INCORPORATED IN PART, REST REJECTED WITH RATIONALE** | 06-17 T2 takes the `bolt.rs` `debug_assert_eq!` and the semver notes. REJECTED, no action: the "9 vs 14 files" observation is about the review's own scope statement, not a tree defect |
</review_dispositions_ledger>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md  (WR-04 and IN-01 in full, plus CR-01's four measured rows — the axis evidence)
@.planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md  (the door enumeration, the lambda bound value and its three at-the-bound compositions — the sweep must cover the same geometry)
@.planning/phases/06-native-time-series-forecasting-stack/06-15-SUMMARY.md  (the holiday-name and NeuralProphet dispositions; if C-08 shipped as `measured_at_structural_maximum`, the sweep is what keeps that measurement honest)
@.planning/phases/06-native-time-series-forecasting-stack/06-12-SUMMARY.md  (how `forecast-holiday-bench` was turned from a printout into a gate — the recipe shape to reuse)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-11; D-15)
@CLAUDE.md  (Verification Discipline rules 1/2/4/5/6/7; `bashrs`, NOT shellcheck, for shell scripts; sourced libraries must be option-neutral; `unwrap()` banned)

<interfaces>
**The two existing harnesses, both to be folded in.**
`crates/aprender-forecast/src/prophet.rs` `#[cfg(test)] mod design_cost` (from ~:2040):
`env_usize(key, default)`; `holidays_for(columns, dates_per_holiday, t0, points)` which splits a
requested column count across as few holidays as `2 * MAX_HOLIDAY_WINDOW + 1` = 731 allows; and
`holiday_design_wall`, `#[ignore]`d, reading `HOLIDAY_BENCH_POINTS` / `_COLUMNS` / `_DATES` /
`_HORIZON`, printing `HOLIDAY DESIGN WALL: … total_s=… profile=…` and asserting NO bar.
`crates/aprender-forecast/src/prophet.rs` `#[cfg(test)] mod sampler`'s `logistic_band_wall`
(from ~:1051): `#[ignore]`d, printing `LOGISTIC BAND WALL: … total_s=… profile=…` and asserting no
bar. **AS WAVE 12 LEFT IT** it also reads a `LOGISTIC_BENCH_FREQ` selector beside its
`LOGISTIC_BENCH_POINTS` / `LOGISTIC_BENCH_HORIZON` ones and prints a `freq=` token — 06-14 T1 added
both, because the three at-the-bound compositions its lambda bound rests on could not otherwise be
measured. So at HEAD-of-wave-14 the `freq` knob EXISTS; WR-04's finding, that the harness watching
the axis CR-01 lives on had no freq knob, is still the reason this sweep exists, but it is now a
statement about the code the review saw and not about the code this task edits. Read the file rather
than either sentence. `holiday_design_wall` still has no freq knob, and neither harness has a bar or a
recipe.

**The recipe to copy and the four sites to convert** (`justfile`):
`forecast-holiday-bench` (~:799-847) is the most complete pattern — `set -euo pipefail`, redirect and
`rc=$?` read from the redirect and NOT through a pipe, a hard failure when the measurement line is
absent, a `total_s` parse BY TOKEN rather than by column position, a `case "$line" in *profile=release*)`
guard, and the bar. Its bar line is the IN-01 instance at `:844`. The three pre-existing instances are
`:620` (chronos-bench, a 100 ms bar), `:680` (forecast-bench, the 2 s bar) and `:742`
(forecast-pool-ratio, a `>=` ratio bar — note the DIRECTION differs, so the shared validator needs both
an "under" and an "at least" mode, or two thin scripts; decide by reading all four).

**Shell rules** (CLAUDE.md): lint with `bashrs lint`, never shellcheck; `set -euo pipefail` in
EXECUTED scripts; quoted variables; no `ls` for iteration. A SOURCED library must be option-neutral —
`set` in a sourced file mutates the caller's shell, which is what
`scripts/check_sourced_libs_option_neutral.sh` exists to catch. Make the validator an EXECUTED script
invoked as a command, not a sourced library, and the option-neutrality question does not arise; say so
in its header.

**The crate's module list** (`crates/aprender-forecast/src/lib.rs`): the `pub mod` block that 06-04 and
06-05 both appended to. A test-only module is declared `#[cfg(test)] mod sc1_wall;` so it stays in
`--lib` reach (the phase's own "keep tests in `--lib` reach" pattern) without entering the public
surface.

**The door's bounds the sweep must respect** (`crates/aprender-forecast/src/types.rs`): every
composition must be ACCEPTED by the door, or the sweep measures a refusal instead of a wall. After
06-14 and 06-15 that includes `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`, `MAX_HOLIDAY_DESIGN_COST`,
`MAX_HOLIDAY_DATES_TOTAL`, `MAX_HOLIDAY_NAME_LEN` and whatever 06-15 decided for the NeuralProphet
axis. The sweep asserts each composition is accepted before it times it — a refusal timed as a wall is
a 0.001 s pass that means nothing.

**Contract ids.** READ `contracts/forecast-tool-boundary-v1.yaml` for the next unused
`FALSIFY-BOUNDARY-0NN`; 06-14 and 06-15 together added four beyond HEAD's 018. Note the precedent that
a `just` recipe can be a binding target: the Phase 6 binding block already carries two rows binding to
`justfile` recipes rather than to Rust, because a benchmark ratio is falsified by a recipe and no
function can be that claim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One parameterized SC1 sweep over freq x growth x holiday shape, with a real bar and no ignore flag</name>
  <files>crates/aprender-forecast/src/sc1_wall.rs, crates/aprender-forecast/src/lib.rs</files>
  <precondition>No other cargo build is running against this workspace's shared target directory, and `df -h .` shows headroom — this branch halted three times on ENOSPC during waves 1-7 and every round-3 plan runs one at a time.</precondition>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs `#[cfg(test)] mod design_cost` in full (~:2040 onward) —
      `env_usize`, `holidays_for` and `holiday_design_wall`, including the printed line's field list
      and the `profile=` marker; this is the harness whose builder the sweep reuses
    - crates/aprender-forecast/src/prophet.rs `#[cfg(test)] mod sampler`'s `logistic_band_wall`
      (~:1051) in full — what it reads from the environment (including the `LOGISTIC_BENCH_FREQ`
      selector 06-14 T1 added) and what it prints (including the `freq=` token that came with it).
      WR-04's "no freq knob" is a fact about the code the REVIEW saw, not about the code you are
      reading: wave 12 already closed that half. What is still open here, and is what this task
      closes, is that the harness is reachable only with `--ignored`, has no recipe and asserts no bar
    - crates/aprender-forecast/src/forecast.rs — the whole door as 06-14 and 06-15 left it, so every
      composition the sweep builds is one the door ACCEPTS; a composition that is refused is timed as a
      meaningless fast pass
    - crates/aprender-forecast/src/types.rs — every bound, including the two or three added this round,
      so the "tightest legal history span" for each composition is legal in fact and not by assumption
    - crates/aprender-forecast/src/lib.rs — the module block, for where `#[cfg(test)] mod sc1_wall;`
      goes and how the crate keeps test modules in `--lib` reach
    - crates/aprender-forecast/src/dates.rs `future_days` — the 1 / 7 / ~30.4 day step per freq, which
      is what makes freq an axis at all
    - .planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md — the three
      at-the-bound compositions and their measured walls, which the sweep's matrix must cover
  </read_first>
  <behavior>
    - Every composition in the matrix is ACCEPTED by the door before it is timed; a composition that
      the door refuses fails the sweep loudly naming the composition and the refusal, because a timed
      refusal is a meaningless fast pass
    - On a release build the sweep asserts every composition's total under 2.0 s; on a debug build it
      PRINTS every composition and asserts nothing, and the printed `profile=` token says which
    - Every composition prints one machine-parsable line whose fields are read BY TOKEN, never by
      column position
    - Measure and print ALL compositions first, then assert — a print-and-assert loop aborts at the
      first failure and hides the shape of the failure across the rest of the matrix, which is exactly
      how a one-axis bench stays convincing
    - With `fit_max_logistic_changepoint_lambda` raised past the structural maximum, the
      `freq=MS growth=logistic` composition FAILS the bar. That is the CR-01 regression case
  </behavior>
  <action>
    (1) NEW FILE, DECLARED IN `lib.rs`. Create `crates/aprender-forecast/src/sc1_wall.rs` and add
    `#[cfg(test)] mod sc1_wall;` to `crates/aprender-forecast/src/lib.rs`'s module block. A new file
    rather than a third module inside `prophet.rs`: the sweep calls `forecast::forecast` across model
    and growth arms, so it belongs beside neither the Prophet numerics nor the door, and a separate
    file is also what keeps this plan's diff off the files 06-14 and 06-15 just rewrote.

    (2) ONE COMPOSITION BUILDER. Write `fn composition(freq, growth, holiday_shape, points, horizon)
    -> ForecastArgs` (names and exact signature at the implementer's discretion) that builds an
    ACCEPTED request for any point in the matrix, at the TIGHTEST LEGAL HISTORY SPAN for that
    composition — which is the geometry that maximises `t_max` and therefore the logistic cost, and is
    the geometry the review measured. Reuse `design_cost::holidays_for` for the holiday shape rather
    than writing a second splitter; move it into this file and have `design_cost` call it, or make it
    `pub(crate)` — one implementation, either way. `growth: "logistic"` compositions need a `cap` above
    `max(y)`; derive it from the series rather than hardcoding 50.0 so a change to the synthetic series
    cannot silently make the composition invalid.

    (3) THE MATRIX. At minimum: freq {`D`, `W`, `MS`} x growth {`linear`, `logistic`, `flat`} x holiday
    {none, at-the-design-cost-bound} = 18 compositions. Add the NeuralProphet arm as its own row (it
    accepts only `freq: "D"` and no holidays, so it is not a cross product) at whatever geometry 06-15
    established as its worst legal shape. Every composition must be inside every door bound — assert
    that by calling `forecast::forecast` and `expect`ing acceptance with a message naming the
    composition. Keep the default matrix small enough to run in the ordinary `--lib` sweep; if the full
    matrix is too slow for that, shrink the DEFAULT point count and horizon while keeping ALL THREE
    freq values and ALL THREE growth arms, and let the recipe widen them by env. Never shrink by
    dropping an axis — dropping an axis is the defect.

    (4) THE LINE AND THE BAR. Print one line per composition:
    `SC1 WALL: freq=<f> growth=<g> holidays=<shape> points=<p> horizon=<h> lambda=<L> cells=<C>
    total_s=<T> fit_s=<..> predict_s=<..> profile=<debug|release>`. Include `lambda` (via
    `prophet::changepoint_count`, the function 06-14 extracted) and `cells` so a future reader can see
    WHICH axis a slow composition is loaded on — a bare `total_s` tells you a composition is slow and
    not why. Then, only when `cfg!(debug_assertions)` is false, assert every composition's `total_s`
    below 2.0. Collect all results first and assert in a second pass, naming every failing composition
    in one message.

    (5) NO IGNORE — AND BE HONEST ABOUT WHICH RUN IS THE GATE. The sweep runs under a plain
    `cargo test -p aprender-forecast --lib sc1_wall::`. Verify by observing a non-zero passed count
    WITHOUT passing `--ignored`: an ignored test reports `0 passed` with an ignored count, which is
    exactly how `logistic_band_wall` managed to exist for a whole wave while watching nothing.

    But the in-suite run and the GATE are two different things and the plan must not blur them. The
    DEFAULT matrix is a SHAPE check: it proves every composition is still accepted by the door, still
    emits a parsable line, and has not rotted — at a reduced points/horizon geometry, on whatever
    profile the caller used. The GATE is `just forecast-sc1-sweep` (Task 2), which widens the matrix by
    env to the AT-THE-BOUND geometry, forces `--release`, and asserts the 2 s bar. Say this in the
    module doc comment on `sc1_wall.rs` in those words, so nobody later reads a green `--lib` run as an
    SC1 guarantee — that misreading is the WR-04 posture in a new place.

    (5b) BUDGET THE IN-SUITE COST, DO NOT ASSUME IT. Size the DEFAULT points/horizon so the whole
    `sc1_wall::` module completes in under 60 s on a debug `--lib` run, MEASURE that wall and record it.
    `[profile.dev.package.aprender-forecast] opt-level = 3` covers this crate but NOT its dependencies,
    and CI's `workspace-test` job already runs 80 604 tests, so 18 compositions including logistic with
    1 000 uncertainty samples is not free. Add a guard inside the harness that fails if the module's
    TOTAL elapsed exceeds 120 s with a message naming the budget — note this is deliberately NOT an SC1
    bar and not throttling-sensitive the way a 2 s bar is; it is a "this test does not blow up CI"
    guard, at twice the target, and the prohibition against asserting the SC1 bar in debug is
    untouched. If the reduced geometry cannot come in under 60 s while keeping all three freq values
    and all three growth arms, REPORT that rather than dropping an axis: dropping an axis is the
    defect.

    (6) THE CR-01 REGRESSION OBSERVATION. Temporarily raise `constants.fit_max_logistic_changepoint_lambda`
    and `types::MAX_LOGISTIC_CHANGEPOINT_LAMBDA` past the structural maximum (~86 700) and widen the
    sweep's points/horizon to the review's geometry (33 points, `horizon: 3650`). Run the sweep on a
    RELEASE build and record the output: `freq=MS growth=logistic` must FAIL the 2 s bar with a
    `total_s` near 2.3, and `freq=D` must pass. Restore both constants and record the passing run. This
    is the evidence that the gate covers the surface where the decision is made; without it, "the sweep
    would have caught CR-01" is a claim.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test --release -p aprender-forecast --lib sc1_wall:: -- --nocapture > /tmp/p06-16-t1a.log 2>&1; rc=$?; tail -30 /tmp/p06-16-t1a.log; test $rc -eq 0 && test "$(grep -c '^SC1 WALL: ' /tmp/p06-16-t1a.log)" -ge 18</automated>
    <fails_when>non-zero exit (a composition was refused by the door, or a composition crossed the 2 s bar — the message names which), or fewer than 18 `SC1 WALL:` lines, meaning the matrix lost an axis or the sweep did not run to completion</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test --release -p aprender-forecast --lib sc1_wall:: > /tmp/p06-16-t1b.log 2>&1; rc=$?; tail -8 /tmp/p06-16-t1b.log; test $rc -eq 0 && grep -qE 'test result: ok\. [1-9][0-9]* passed' /tmp/p06-16-t1b.log</automated>
    <fails_when>non-zero exit, or a summary reporting `0 passed` — with no `--ignored` flag on this command, `0 passed` means the sweep is still gated behind an ignore attribute and is running nothing, which is the exact state WR-04 describes</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test --release -p aprender-forecast --lib sc1_wall:: -- --nocapture > /tmp/p06-16-t1c.log 2>&1; test "$(grep -c 'profile=release' /tmp/p06-16-t1c.log)" -ge 18</automated>
    <fails_when>a count below the number of compositions — some line was produced under a debug profile, or the `profile=` token is missing. This crate carries `[profile.dev.package.aprender-forecast] opt-level = 3`, so a debug wall looks plausible and still is not the SC1 bar (CLAUDE.md rule 2)</fails_when>
    <automated>rm -f /tmp/p06-16-t1d.log; s=$(date +%s); CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib sc1_wall:: -- --nocapture > /tmp/p06-16-t1d.log 2>&1; rc=$?; e=$(date +%s); d=$((e-s)); tail -12 /tmp/p06-16-t1d.log; echo "debug_sc1_wall_seconds=$d"; test $rc -eq 0 && test "$d" -lt 60</automated>
    <fails_when>non-zero exit (the debug run must stay green, which means the sweep asserts NO wall-clock SC1 bar under `cfg!(debug_assertions)` — a 2 s bar in the debug sweep is a flake wired into CI), or `debug_sc1_wall_seconds` at or above 60. That budget is the point: this module joins a `workspace-test` job already running 80 604 tests, `[profile.dev.package.aprender-forecast] opt-level = 3` does not cover this crate's dependencies, and an unbudgeted 18-composition sweep with 1 000 uncertainty samples per logistic case is a real CI regression. If it cannot fit under 60 s while keeping all three freq values and all three growth arms, report it — do not drop an axis</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-16-t1e.log 2>&1; rc=$?; tail -12 /tmp/p06-16-t1e.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the whole debug `--lib` suite for this crate must stay green with the new module in it</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/sc1_wall.rs` exists and `crates/aprender-forecast/src/lib.rs`
      declares `mod sc1_wall`
    - A release run of `cargo test --release -p aprender-forecast --lib sc1_wall::` with NO `--ignored`
      flag reports a non-zero passed count
    - `sc1_wall.rs`'s module doc comment states in plain words that the default in-suite run is a SHAPE
      check at a reduced geometry and that `just forecast-sc1-sweep` at the at-the-bound geometry is
      the GATE — so a green `--lib` run is never misread as an SC1 guarantee
    - The DEFAULT matrix's debug `--lib` wall is MEASURED and recorded in the SUMMARY and is under
      60 s; the harness carries a 120 s total-elapsed guard whose message names the budget and says it
      is not an SC1 bar
    - At least 18 `SC1 WALL:` lines are produced, covering all three `freq` values and all three
      `growth` arms in both holiday shapes, plus at least one NeuralProphet row
    - Every `SC1 WALL:` line carries `total_s=`, `lambda=`, `cells=` and `profile=` tokens
    - The debug `--lib` run is green — the bar is asserted only when `cfg!(debug_assertions)` is false
    - The CR-01 regression observation is recorded in the SUMMARY: the raised-constant release run with
      `freq=MS growth=logistic` FAILING near 2.3 s and `freq=D` passing, then the restored run passing
      everywhere. Quote both outputs
    - `holidays_for` has exactly one definition in the crate — `grep -rn 'fn holidays_for'
      crates/aprender-forecast/src/` prints exactly one line; quote the file and line number rather
      than a count
  </acceptance_criteria>
  <done>One harness sweeps freq x growth x holiday shape with a real 2 s bar on release, runs without an ignore flag, names the axis a slow composition is loaded on, and has been OBSERVED failing on the exact defect the phase's three previous benches could not see.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IN-01 — one numeric-shape validator, a case table that is RUN, and all five bar sites converted</name>
  <files>scripts/assert_measurement_under.sh, scripts/check_assert_measurement_under_cases.sh, justfile</files>
  <read_first>
    - justfile lines ~599-660 (`chronos-bench`, its 100 ms bar at ~:620), ~659-685 (`forecast-bench`,
      its 2 s bar at ~:680), ~703-760 (`forecast-pool-ratio`, its `>=` ratio bar at ~:742) and
      ~780-847 (`forecast-holiday-bench`, its 2 s bar at ~:844) — ALL FOUR in full. The direction and
      the units differ between them, and a mechanical find-and-replace here inverts a gate
    - crates/aprender-forecast/src/sc1_wall.rs as Task 1 left it — the exact `SC1 WALL:` field names
      the recipe parses by token
    - scripts/apr_bin.sh — the repository's one sourced shell library, and
      scripts/check_sourced_libs_option_neutral.sh, for the rule the new scripts must visibly not be
      subject to (they are executed, not sourced) and the header style that says so
    - Any two existing scripts under scripts/ that pass `bashrs lint` — for the house conventions:
      `set -euo pipefail`, quoted variables, explicit error handling, no `ls` for iteration
    - CLAUDE.md "Verification Discipline" rules 1, 4 and 7 and "Shell Scripts: Use bashrs (NOT
      shellcheck)" — rule 7 is why the case table exists and rule 4 is why every converted site is
      re-mutated
  </read_first>
  <behavior>
    - `assert_measurement_under.sh <value> <bar> <label>` exits 0 only when `<value>` is a
      well-formed non-negative decimal number strictly below `<bar>`
    - It exits non-zero, with a message naming the label and the offending token, for: an empty token,
      a token containing any non-digit non-dot character, a token with more than one dot, a lone dot,
      and a numerically at-or-above-bar value. `total_s=abc` can no longer print OK
    - A sibling mode (or a sibling script) covers the `>=` direction that `forecast-pool-ratio` needs,
      with the same shape checks — the ratio bar is `best >= 2.0`, not `< 2.0`, and silently reusing
      the "under" script there would invert a gate
    - `check_assert_measurement_under_cases.sh` drives a table of MUST_PASS and MUST_FAIL tokens
      through the validator and exits non-zero if any row behaves the other way
  </behavior>
  <action>
    (1) WRITE THE CASE TABLE FIRST, AND WATCH IT FAIL. Create
    `scripts/check_assert_measurement_under_cases.sh` before the validator exists. MUST_PASS rows at
    minimum: `0`, `0.0`, `1.999`, `.5`, `1`. MUST_FAIL rows at minimum: the empty string, `abc`,
    `2.0` (at the bar, not under), `2.5`, `1.2.3`, `.`, `-1`, `1e3` (an exponent form `awk` would
    happily coerce and no measurement line here ever emits), `1 2` (two tokens), and a token with a
    trailing carriage return. Run it against the yet-to-exist validator and record that it fails to
    invoke — then against a deliberately naive validator (the current inline `awk` coercion, pasted
    in) and record WHICH rows it lets through. That recorded output is the mutation this task turns
    green, and it is what CLAUDE.md rule 7 asks for: the `apr`-invocation patterns were wrong five
    times, every one caught by a must-match/must-not-match table and none by review.

    (2) WRITE THE VALIDATOR. `scripts/assert_measurement_under.sh`, an EXECUTED script (never sourced —
    say so in the header, so the option-neutrality rule that governs `scripts/apr_bin.sh` is visibly
    not applicable here), `set -euo pipefail`, quoted variables, no `ls` for iteration. Check the SHAPE
    first with a `case` pattern that rejects empty, non-`[0-9.]`, and multi-dot tokens — the review's
    own suggested form is `case "$total" in ''|*[!0-9.]*|*.*.*) …;; esac` — then compare numerically.
    Emit a message naming the label, the token and the bar on failure, and a one-line OK on success so
    a passing gate still says what it measured. Handle the `>=` direction per the behaviour block.
    Lint both scripts with `bashrs lint` and make sure `bashrs` is clean before wiring anything.

    (3) CONVERT ALL FIVE SITES. In the `justfile`, replace the inline `awk -v v=…` bar check at every
    site with a call to the validator: `forecast-holiday-bench` (the new instance, ~:844),
    `forecast-bench` (~:680), `chronos-bench` (~:620, a 100 ms bar — note the units), and
    `forecast-pool-ratio` (~:742, the `>=` direction). The fifth is the new `forecast-sc1-sweep` recipe
    in step (4). Read all four before converting: the direction and the units differ, and a
    mechanical find-and-replace here inverts a gate. Preserve everything each recipe already does right
    — the redirect-and-`rc=$?` idiom, the absent-line hard failure, the by-token parse, the
    `profile=release` guard.

    (4) THE GATE RECIPE. Add `forecast-sc1-sweep` to the `justfile`, modelled on
    `forecast-holiday-bench`: run `scripts/check_assert_measurement_under_cases.sh` FIRST (so the
    validator's own guard is exercised on every gate run, not just when someone remembers), then
    `CARGO_INCREMENTAL=0 cargo test --release -p aprender-forecast --lib sc1_wall:: -- --nocapture`
    redirected to a log with `rc=$?` read from the redirect and NOT through a pipe, fail hard on a
    non-zero rc, fail hard if the log carries no `SC1 WALL:` line at all, then for EACH `SC1 WALL:`
    line: parse `total_s` by token, require the line to carry `profile=release`, and pass the value
    through the validator against 2.0. Accept env overrides for the matrix width so the recipe can be
    widened without editing Rust. Print a final summary naming how many compositions were checked —
    a gate that checked zero lines and exited 0 is the vacuous case this whole task is about.

    (5) RE-MUTATE IN THE NEW SCOPE. CLAUDE.md rule 4: extending a guard's scope requires re-mutating
    there, because the old proof does not transfer. For EACH of the five converted sites, feed a
    garbage token through it once (the cheapest way is a temporary edit that substitutes a literal
    `abc` for the parsed value), observe the recipe FAIL naming the token, and restore. Record all five
    observations. Four of these sites are pre-existing and have never had this done.
  </action>
  <verify>
    <automated>bash scripts/check_assert_measurement_under_cases.sh > /tmp/p06-16-t2a.log 2>&1; rc=$?; tail -30 /tmp/p06-16-t2a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the log names the row and whether it passed when it should have failed or the reverse. A MUST_FAIL row that passes is the IN-01 defect still present in the new validator</fails_when>
    <automated>bash scripts/assert_measurement_under.sh abc 2.0 "shape probe" > /tmp/p06-16-t2b.log 2>&1; rc=$?; cat /tmp/p06-16-t2b.log; test $rc -ne 0</automated>
    <fails_when>an exit code of 0 — the validator accepted a non-numeric token, which is exactly the `awk` coercion IN-01 reports. Note this command asserts rc is NON-zero: a zero exit here is the failure</fails_when>
    <automated>bash scripts/assert_measurement_under.sh 1.999 2.0 "shape probe" > /tmp/p06-16-t2c.log 2>&1; rc=$?; cat /tmp/p06-16-t2c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the validator rejects a legitimate under-bar measurement, so every real gate would now fail closed and the bar would be re-loosened by whoever debugs it next</fails_when>
    <automated>bashrs lint scripts/assert_measurement_under.sh scripts/check_assert_measurement_under_cases.sh > /tmp/p06-16-t2d.log 2>&1; rc=$?; tail -20 /tmp/p06-16-t2d.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — `bashrs` (never shellcheck, per CLAUDE.md) reports an unquoted variable, a missing `set -euo pipefail`, an `ls` used for iteration, or an unhandled error path</fails_when>
    <automated>bash scripts/check_sourced_libs_option_neutral.sh > /tmp/p06-16-t2e.log 2>&1; rc=$?; tail -10 /tmp/p06-16-t2e.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — a script that other scripts source now carries `set`, which leaks options into the caller's shell. The two new scripts are EXECUTED, not sourced, so this should stay green; if it goes red, one of them was wired in with a `.` or `source` somewhere</fails_when>
    <automated>just forecast-sc1-sweep > /tmp/p06-16-t2f.log 2>&1; rc=$?; tail -30 /tmp/p06-16-t2f.log; test $rc -eq 0 && grep -q 'SC1 SWEEP OK' /tmp/p06-16-t2f.log</automated>
    <fails_when>non-zero exit, or no `SC1 SWEEP OK` summary line — either a composition crossed the bar (the log names it), or the recipe checked zero lines and fell through, which the summary line's composition count is there to expose</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/assert_measurement_under.sh` and `scripts/check_assert_measurement_under_cases.sh` exist,
      are executable, and both pass `bashrs lint`
    - The case table carries at least the eleven rows named in the action, split into MUST_PASS and
      MUST_FAIL, and it is invoked by `just forecast-sc1-sweep` before any measurement is taken
    - All five bar sites call the validator. Confirm by a positive check with comments filtered, since
      a `grep -c` over a file whose header prose mentions the script would count itself:
      `grep -v '^\s*#' justfile | grep -c 'assert_measurement_under.sh'` is at least 5
    - `just forecast-sc1-sweep` prints a summary naming the number of compositions it checked, and that
      number matches the `SC1 WALL:` line count in its log
    - All five re-mutation observations are recorded in the SUMMARY: garbage token in, recipe failing
      and naming the token, restored. Four of the five sites are pre-existing and have never had this
      done — say so
    - The naive-validator run from step (1) is recorded, naming exactly which MUST_FAIL rows the old
      inline `awk` coercion let through
  </acceptance_criteria>
  <done>No wall-clock bar in this repository can pass on a measurement it could not parse, the validator that guarantees that ships a case table which runs on every gate invocation, and each of the five converted sites has been observed failing on a garbage token in its own scope.</done>
</task>

<task type="auto">
  <name>Task 3: Fold the three blind benches into the sweep, reword the superlative, and let the contract own the swept surface</name>
  <files>crates/aprender-forecast/src/prophet.rs, crates/aprender-forecast/src/sc1_wall.rs, justfile, contracts/forecast-tool-boundary-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs `mod design_cost` and `mod sampler`'s
      `logistic_band_wall` — both harnesses in full, including their comment blocks, so nothing
      load-bearing is lost when they are folded in
    - crates/aprender-forecast/src/sc1_wall.rs as Task 1 left it — the composition builder is what the
      folded harnesses must call
    - justfile `forecast-holiday-bench`'s whole comment block, especially the paragraph asserting a
      superlative about its defaults and the paragraph nine lines later recording a 4 700-point /
      5-column request walling at ~4.2 s — the two cannot both be true
    - contracts/forecast-tool-boundary-v1.yaml — `holiday_design_cost_bounded` and the two binding rows
      that bind to `justfile` recipes rather than to Rust (the precedent for binding a benchmark claim)
    - .planning/phases/06-native-time-series-forecasting-stack/06-12-SUMMARY.md — the reasoning behind
      the current `forecast-holiday-bench` defaults, so the reworded claim matches what was measured
  </read_first>
  <action>
    (1) ONE BUILDER, THREE ENTRY POINTS. Rewrite `holiday_design_wall` and `logistic_band_wall` so each
    is a single-composition call onto `sc1_wall`'s composition builder and measurement printer —
    or delete them outright in favour of a sweep case selected by env, if the recipes that drive them
    can be pointed at the sweep instead. Either is acceptable; what is NOT acceptable is three
    harnesses each building their own geometry. State which was chosen and why. If they are kept,
    `forecast-holiday-bench` keeps working unchanged from a caller's point of view, which is worth
    something: it is referenced from `06-EVIDENCE.md` and from the contract.

    (2) REWORD THE SUPERLATIVE. In `forecast-holiday-bench`'s comment block, replace the sentence
    claiming the defaults are the worst shape the door still accepts with a statement of what the three
    measured compositions actually support — that the defaults are the slowest of the three measured
    at-the-design-cost-bound compositions (1.692 s balanced, versus 1.174 s many-rows and 0.106 s
    many-columns), which is a different and true claim. Keep the honest disclaimer already above it.
    Add a pointer to `just forecast-sc1-sweep` as the gate that covers the surface this one recipe
    cannot. <!-- planner-discipline-allow: WORST SHAPE THE DOOR -->

    (3) THE CONTRACT OWNS THE SWEPT SURFACE. Add an `sc1_wall_swept` equation to
    `contracts/forecast-tool-boundary-v1.yaml` stating, as a formula over the composition set, that
    every accepted composition of freq x growth x holiday shape at the tightest legal history span
    walls under 2 s on a release build; a `domain` naming the matrix; a `codomain` naming the pass or
    the named failing composition; and `invariants` recording (a) that this is a BENCHMARK claim
    falsified by a `just` recipe and never by a libtest assertion, following the same split
    `pool_speedup_host_gated` already makes and for the same reason (a wall-clock assertion inside
    libtest moves with CPU throttling), (b) that three prior benches each covered one axis and the
    axis CR-01 lived on was covered by none of them, and (c) that the gate was OBSERVED failing on the
    CR-01 configuration with the lambda bound raised. Add the `proof_obligations` entry and the next
    unused `FALSIFY-BOUNDARY-0NN`, whose `test:` is `just forecast-sc1-sweep`. Add a binding row —
    binding to the `justfile` recipe, following the two existing rows that do exactly that — and update
    the Phase 6 block header's stated row count.

    (4) CHECK THE WHOLE SURFACE ONE MORE TIME. Enumerate every wall-clock bar in the repository, not
    just the ones this plan already knew about: `pmat query "wall clock bar assertion benchmark"
    --limit 10` plus a read of every `just` recipe that compares a measurement, and confirm each either
    calls the validator or is deliberately excluded with a stated reason. CLAUDE.md rule 5: enumerate
    the decision surfaces, then check coverage — covering "the ones we touched" is not covering "the
    bars".

    (5) SEMVER. `git show HEAD:contracts/forecast-tool-boundary-v1.yaml > /tmp/ftb-old-16.yaml` then
    `pv diff /tmp/ftb-old-16.yaml contracts/forecast-tool-boundary-v1.yaml` — TWO FILESYSTEM PATHS,
    never a git revision, which `pv` would report as a missing contract file. Apply the suggested bump.
  </action>
  <verify>
    <automated>just forecast-sc1-sweep > /tmp/p06-16-t3a.log 2>&1; rc=$?; tail -30 /tmp/p06-16-t3a.log; test $rc -eq 0 && grep -q 'SC1 SWEEP OK' /tmp/p06-16-t3a.log</automated>
    <fails_when>non-zero exit, or no `SC1 SWEEP OK` line — a composition crossed the 2 s bar (the log names it), the case table failed, or the recipe checked zero lines</fails_when>
    <automated>just forecast-holiday-bench > /tmp/p06-16-t3b.log 2>&1; rc=$?; tail -20 /tmp/p06-16-t3b.log; test $rc -eq 0 && grep -q 'HOLIDAY DESIGN OK' /tmp/p06-16-t3b.log</automated>
    <fails_when>non-zero exit, or no `HOLIDAY DESIGN OK` line — folding the harness onto the shared builder changed what this recipe measures or broke its measurement line, and this recipe is cited by `06-EVIDENCE.md` and by the contract</fails_when>
    <automated>just forecast-bench > /tmp/p06-16-t3c.log 2>&1; rc=$?; tail -20 /tmp/p06-16-t3c.log; test $rc -eq 0 && grep -q 'ROUND TRIP OK' /tmp/p06-16-t3c.log</automated>
    <fails_when>non-zero exit, or no `ROUND TRIP OK` line — the validator conversion broke the no-holiday SC1 recipe, most likely by passing the arguments in the wrong order or by using the wrong direction</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-16-t3d.log 2>&1; rc=$?; tail -12 /tmp/p06-16-t3d.log; test $rc -eq 0 && grep -q 'prophet::parity' /tmp/p06-16-t3d.log</automated>
    <fails_when>non-zero exit, or no `prophet::parity` test names in the log at all — folding the harnesses moved or deleted something the parity module depends on. `holidays_for` in particular is shared</fails_when>
    <automated>test "$(grep -v '^\s*#' justfile | grep -c 'assert_measurement_under.sh')" -ge 5</automated>
    <fails_when>a count below 5 — a bar site reverted to the inline coercion, or the sweep recipe was written without the validator. Comments are filtered because the recipes' own header prose mentions the script and would inflate a raw count</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-16-t3e.log 2>&1; rc=$?; tail -20 /tmp/p06-16-t3e.log; test $rc -eq 0 && test "$(grep -c 'Total equations:' /tmp/p06-16-t3e.log)" -ge 4 && test "$(grep -c 'BIND-' /tmp/p06-16-t3e.log)" -eq 0</automated>
    <fails_when>non-zero exit, or fewer than four `Total equations:` summaries. Any `BIND-` or `RESOLVE-` line means `sc1_wall_swept` has no binding row, or its row does not resolve — note that a `justfile`-bound row resolves differently from a Rust-bound one, so copy an existing recipe-bound row rather than a function-bound one</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one composition builder exists in the crate: `grep -rn 'fn holidays_for'
      crates/aprender-forecast/src/` prints exactly one line, and neither `holiday_design_wall` nor
      `logistic_band_wall` builds its own `ForecastArgs` geometry independently of `sc1_wall`'s builder
      (or both are deleted — state which)
    - `just forecast-holiday-bench`, `just forecast-bench` and `just forecast-sc1-sweep` all pass, and
      the first two still emit their original OK tokens
    - The `forecast-holiday-bench` comment block no longer claims its defaults are the worst accepted
      shape and instead states the slowest-of-three-measured claim, with the three numbers
    - `contracts/forecast-tool-boundary-v1.yaml` carries `equations.sc1_wall_swept`, its
      `proof_obligations` entry and a new `FALSIFY-BOUNDARY-0NN` whose `test:` is `just forecast-sc1-sweep`
    - `contracts/aprender/binding.yaml` carries a row for `sc1_wall_swept` bound to the `justfile`
      recipe, and the Phase 6 header's stated row count matches the actual row count
    - The step (4) enumeration is recorded: every wall-clock bar in the repository listed, each marked
      converted or deliberately excluded with a reason
    - `pv diff` was run on two real filesystem paths and the suggested bump applied; before/after
      versions in the SUMMARY
  </acceptance_criteria>
  <done>There is one SC1 gate and one composition builder; the two harnesses that each watched a single axis now feed it; the recipe comment claims only what was measured; and the contract owns the swept surface as a benchmark claim falsified by a recipe, matching the split this contract already makes for the pool ratio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers, default K=8) | the surface the SC1 bar defends: an accepted request must not consume more than 2 s of a pooled worker |
| measurement -> gate decision | a wall-clock token parsed out of a log becomes a pass/fail; a parser that coerces garbage to 0 turns the gate into a rubber stamp |
| bench geometry -> coverage claim | a bench hard-coded to one axis licenses a false belief that the whole surface is walled |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. This
plan adds no request-handling code; its threats are to the INTEGRITY OF THE GATE, which is what
decides whether a resource-exhaustion defect ships.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-39 | Denial of Service | the SC1 gate surface as a whole (`forecast-bench`, `forecast-holiday-bench`, `logistic_band_wall`) | **high** | mitigate | Three benches, three hard-coded geometries, and the axis CR-01 lived on covered by none of them — `logistic_band_wall` had no `freq` knob and all four of its recorded lines were the one value that does not exhibit the defect. A resource-exhaustion defect shipped through a green gate. Task 1 sweeps freq x growth x holiday shape and is OBSERVED failing on the CR-01 configuration; Task 3 folds the single-axis harnesses onto the same builder so the divergence cannot recur |
| T-06-40 | Spoofing | the wall-clock bar checks (`justfile:620`, `:680`, `:742`, `:844`) | **high** | mitigate | `awk -v v="$x" 'BEGIN { exit (v + 0 < BAR) ? 0 : 1 }'` coerces a non-numeric token to 0 and prints OK, so a broken measurement line makes every bar pass — a gate reporting a result it never measured. Task 2 replaces all five with a shape-checking validator, ships a must-match/must-not-match case table, runs the table on every gate invocation, and re-mutates each of the five sites in its own scope (CLAUDE.md rule 4) |
| T-06-41 | Repudiation | the `forecast-holiday-bench` comment block | low | mitigate | A superlative ("the worst shape the door still accepts") that the same comment contradicts nine lines later is the sentence a future reader will quote as coverage evidence. Task 3 replaces it with the claim the three measurements support |
| T-06-42 | Denial of Service | the sweep itself, run in CI's debug `--lib` job | medium | accept | A wall-clock assertion in the debug sweep would flake with CPU throttling and background load, exactly as `pool_equality_under_load` documented. Accepted by design: the sweep PRINTS under any profile and ASSERTS only when `cfg!(debug_assertions)` is false, with `profile=` on every line so a reader can tell which run they are looking at. The bar lives in the `just` recipe, following this contract's existing correctness-claim / benchmark-claim split |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. `bashrs` and `just` are pre-existing repository tooling and are invoked, not installed. No new dependency enters the graph. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test --release -p aprender-forecast --lib sc1_wall:: -- --nocapture` — at least 18
  `SC1 WALL:` lines, every one `profile=release`, all under 2 s, and NO `--ignored` flag used
- `cargo test -p aprender-forecast --lib` — 0 failed on the debug profile (the sweep asserts no bar
  there); `prophet::parity` unmoved
- `bash scripts/check_assert_measurement_under_cases.sh` — rc=0, every MUST_PASS and MUST_FAIL row
  behaving as the table says
- `bashrs lint` on both new scripts — rc=0; `scripts/check_sourced_libs_option_neutral.sh` — rc=0
- `just forecast-sc1-sweep`, `just forecast-holiday-bench`, `just forecast-bench` — all rc=0 with their
  OK tokens present
- `pv validate` / `pv status` on `contracts/forecast-tool-boundary-v1.yaml` — `0 error(s)` and
  `sc1_wall_swept` listed
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings` and
  `cargo fmt --all -- --check` — rc=0
</verification>

<success_criteria>
- The SC1 gate scans the surface where the cost is decided, and that claim is backed by a RECORDED
  failing run on the CR-01 configuration rather than by an argument
- The gate needs no flag to run and asserts a real bar on a release profile
- No wall-clock bar anywhere in the repository can pass on a measurement it could not parse, and the
  guard proving that is exercised by a case table on every gate run
- One composition builder, not three
- The only claim the `forecast-holiday-bench` comment makes is the one its three measurements support
- No parity fixture, parity tolerance or `*-parity-v1.yaml` numeric was touched
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-16)

**New files**: `crates/aprender-forecast/src/sc1_wall.rs`;
`scripts/assert_measurement_under.sh`; `scripts/check_assert_measurement_under_cases.sh`.

**New Rust module**: `#[cfg(test)] mod sc1_wall` declared in `crates/aprender-forecast/src/lib.rs`.

**New Rust functions** (in `sc1_wall.rs`, exact names at the implementer's discretion): the
composition builder and the sweep test. `prophet::design_cost::holidays_for` is MOVED or made
`pub(crate)` so there is one definition.

**New `just` recipe**: `forecast-sc1-sweep`.

**New stdout tokens**: `SC1 WALL:` (with fields `freq=`, `growth=`, `holidays=`, `points=`,
`horizon=`, `lambda=`, `cells=`, `total_s=`, `fit_s=`, `predict_s=`, `profile=`) and the recipe's
`SC1 SWEEP OK` summary.

**New contract keys** (`contracts/forecast-tool-boundary-v1.yaml`): `equations.sc1_wall_swept`, its
`proof_obligations` entry, and the next unused `falsification_tests` id in the `FALSIFY-BOUNDARY-0NN`
series (READ the file — 06-14 and 06-15 together added four beyond HEAD's 018).

**New binding key** (`contracts/aprender/binding.yaml`): the row whose `equation:` is
`sc1_wall_swept`, bound to the `justfile` recipe following the two existing recipe-bound rows; the
Phase 6 block header's stated row count is updated.

**Symbols 06-14 and 06-15 created** (present after waves 12-13, not at HEAD — do not drift-check
against HEAD): `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`, `prophet::changepoint_count`,
`test_support::constant_f64`, `test_support::contract_value`, `door_surface:` with
`knobs:`/`cost_axes:`, `constants.fit_max_logistic_changepoint_lambda`,
`constants.poisson_normal_branch_lambda`, `constants.fit_max_holiday_name_len`,
`MAX_HOLIDAY_NAME_LEN`, `np::train_cost`, `logistic_changepoint_cost_bounded`,
`door_surface_is_complete`, `holiday_name_cost_bounded`, `np_train_cost_bounded`,
`every_request_knob_is_enumerated`, `every_cost_axis_names_a_real_bound`,
`no_cost_axis_is_pending`, `pool_default_matches_contract`, **the `LOGISTIC_BENCH_FREQ` env selector
and the `freq=` token on `logistic_band_wall`'s `LOGISTIC BAND WALL:` line** (06-14 T1 added both so
it could measure the three at-the-bound compositions; they are NOT at HEAD, so do not drift-check
them against HEAD, and Task 3 folds or deletes them along with the rest of that harness), and
conditionally `MAX_NP_TRAIN_COST` / `constants.fit_max_np_train_cost`.

**Symbols 06-17 creates**: `variance_tolerance` on `poisson_sampler_domain`, `FALSIFY-PROPHET-014`.

Everything else named in this plan (`forecast-bench`, `forecast-holiday-bench`, `chronos-bench`,
`forecast-pool-ratio`, `holiday_design_wall`, `logistic_band_wall`, `holidays_for`, `env_usize`,
`HOLIDAY DESIGN WALL:`, `LOGISTIC BAND WALL:` — the last two as LINE PREFIXES; note
`LOGISTIC BAND WALL:` gained a `freq=` field in wave 12, so the prefix is at HEAD but that field is
not — `HOLIDAY DESIGN OK`, `ROUND TRIP OK`,
`pool_speedup_host_gated`, `bashrs`, `check_sourced_libs_option_neutral.sh`,
`contract-audit-phase6`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-16-SUMMARY.md` when done. Record:
the full `SC1 WALL:` matrix output on release, all compositions with their walls; the CR-01 regression
observation — the raised-lambda run with `freq=MS growth=logistic` FAILING near 2.3 s and `freq=D`
passing, then the restored run — quoted verbatim, because that is the evidence the gate covers the
decision surface; the naive-validator case-table run naming exactly which MUST_FAIL rows the old inline
`awk` coercion let through; all five re-mutation observations, one per converted bar site, noting that
four are pre-existing sites that had never been mutated; whether the two old harnesses were folded or
deleted and why; the step (4) enumeration of every wall-clock bar in the repository with its
conversion status; `prophet::parity` count before and after; `pv diff`'s suggested semver bump (two
real filesystem paths) and the contract version before/after; and the Review Dispositions Ledger with
WR-04 and IN-01 marked closed.
</output>
