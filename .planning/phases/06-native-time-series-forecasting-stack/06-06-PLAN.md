---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 06
type: execute
wave: 3
depends_on: [06-01, 06-05]
files_modified:
  - contracts/forecast-tool-boundary-v1.yaml
  - crates/aprender-forecast/src/types.rs
  - crates/aprender-mcp-forecast/src/lib.rs
  - crates/aprender-mcp-forecast/src/main.rs
  - crates/aprender-mcp-forecast/tests/e2e_stdio.rs
autonomous: true
requirements: [SC1, SC5]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every malformed input SC1 lists — unknown field, fewer than 10 or more than 20 000 points, ds/y length mismatch, unsorted or duplicate or impossible dates, constant y, horizon 0 or above 3 650, unknown freq, interval_width outside (0,1), logistic growth without a valid cap, unknown model — is a pmcp::Error::validation with a fix-naming message through the streamable-HTTP server, each with its own e2e case (D-11)"
    - "The advertised tool schema has additionalProperties: false and requires exactly ds, y, horizon; the Rust bounds MIN_POINTS/MAX_POINTS/MAX_HORIZON equal the contract's constants (D-11, D-15)"
    - "Eight concurrent requests to the pooled streamable-HTTP app return responses whose JSON signature of ds/yhat/yhat_lower/yhat_upper/trend/components is bit-identical to their sequential result (D-12, SC5) — the speed-up (< half the sequential wall) is asserted on aarch64 release and printed elsewhere"
    - "`--http PORT --pool K` pools K pmcp routers behind a round-robin fallback (default 8); the stdio path is unaffected (D-12)"
    - "The spawned binary answers initialize -> tools/list (exactly one tool, strict schema) -> tools/call over stdio and refuses an unknown key (SC1 stdio; D-06 template tests/e2e_stdio.rs)"
    - "contracts/forecast-tool-boundary-v1.yaml pins BOTH servers' bounds and the shared response shape and validates with 0 errors and non-zero counts (D-03, D-15)"
  artifacts:
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "fit-server and Chronos bounds, date/freq/interval/cap/constant-y rules, unknown-field refusal, horizon gate, shared response shape, pool equality"
      contains: "constants:"
    - path: "crates/aprender-mcp-forecast/src/lib.rs"
      provides: "pooled_app(pool, name, version); e2e module with >= 14 refusal cases + NP happy path; pool_equality test"
      contains: "pub fn pooled_app"
    - path: "crates/aprender-mcp-forecast/src/main.rs"
      provides: "--http PORT [--pool K] (default 8)"
      contains: "--pool"
    - path: "crates/aprender-mcp-forecast/tests/e2e_stdio.rs"
      provides: "spawned-binary stdio round trip (dark in CI until ci.yml line 471 lists it — 06-08 checkpoint)"
      contains: "CARGO_BIN_EXE_aprender-mcp-forecast"
    - path: "crates/aprender-forecast/src/types.rs"
      provides: "bounds_match_contract + chronos_bounds_match_contract unit tests (fit-server and Chronos bounds against the one contract)"
      contains: "bounds_match_contract"
  key_links:
    - from: "crates/aprender-mcp-forecast/src/lib.rs"
      to: "crates/aprender-mcp-forecast/src/lib.rs"
      via: "pooled_app builds K x http_app(build_server(..)) and dispatches via axum Router::fallback + tower ServiceExt::oneshot round-robin"
      pattern: "fetch_add\\(1, Ordering::Relaxed\\) % routers\\.len\\(\\)"
    - from: "crates/aprender-forecast/src/types.rs"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "test_support::constant_u64(\"forecast-tool-boundary-v1\", ..) == MIN_POINTS / MAX_POINTS / MAX_HORIZON and == chronos::CHRONOS_MIN_POINTS / CHRONOS_MAX_POINTS / CHRONOS_MAX_HORIZON (06-05, a declared dependency)"
      pattern: "constant_u64\\(\"forecast-tool-boundary-v1\""
    - from: "crates/aprender-mcp-forecast/tests/e2e_stdio.rs"
      to: "crates/aprender-mcp-forecast/src/main.rs"
      via: "Command::new(env!(\"CARGO_BIN_EXE_aprender-mcp-forecast\")) with no args (stdio default)"
      pattern: "CARGO_BIN_EXE_aprender-mcp-forecast"
  prohibitions:
    - statement: "The tool boundary refuses, never defaults: no silent coercion of an unknown freq, model, growth, seasonality_mode or field (D-11)"
      status: resolved
      verification: test
    - statement: "Never one shared fit across requests; per-request seed (default 42) is what makes equality-under-load hold (forecast-mcp-thin-server.md)"
      status: resolved
      verification: test
    - statement: "Do not serve the demo page from another port — same-origin nest under /mcp only"
      status: resolved
      verification: judgment
    - statement: "Do not edit .github/workflows/ci.yml in this plan — the stdio integration target stays dark until the 06-08 human checkpoint"
      status: resolved
      verification: test
---

<objective>
Harden and scale the fit server: the complete D-11 refusal set as in-process e2e cases (SC1), the boundary
contract that both servers are held to (D-03/D-15), the spike-010 router pool behind `--pool K` with the
equality-under-load test (D-12, SC5), and the spawned-binary stdio round trip from the SetFit template (SC1
stdio).

Purpose: pmcp 2.19.3's streamable-HTTP router holds one Arc<Mutex<Server>> across the whole tool future —
without the pool eight concurrent fits are 1.0x sequential (measured); without the refusal cases a client
cannot learn a knob was ignored.
Output: one contract, ~16 e2e cases, `pooled_app`, `--pool`, `pool_equality`, `tests/e2e_stdio.rs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F2 pmcp API, F4, F5, Pattern 2, Security Domain)
@.planning/phases/06-native-time-series-forecasting-stack/06-PATTERNS.md  (sections: aprender-mcp-forecast lib.rs pool; e2e modules; tests/e2e_stdio.rs; Refusal pattern; Strict schema test)
@.claude/skills/spike-findings-aprender/references/forecast-mcp-thin-server.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md
@.planning/phases/06-native-time-series-forecasting-stack/06-05-SUMMARY.md  (chronos.rs: CHRONOS_MIN_POINTS / CHRONOS_MAX_POINTS / CHRONOS_MAX_HORIZON and the verbatim refusal messages the boundary contract pins — the reason this plan is Wave 3)

<interfaces>
<!-- Boundary constants both servers are tested against (D-11); pinned in the contract's top-level `constants:` and read by test_support::constant_u64 -->
constants: fit_min_points 10, fit_max_points 20000, fit_max_horizon 3650, chronos_min_points 4, chronos_max_points 20000, chronos_max_horizon 1024, chronos_native_horizon 64, default_seed 42, default_interval_width_x100 80, pool_default 8, pool_speedup_min_x10 20 (i.e. >= 2.0x on aarch64 release), concurrency_requests 8
Refusal messages (verbatim substrings the e2e cases assert; from sources/004 lib.rs:174-190 + dates.rs): "at least" | "exceeds max_points" | "ds has" | "strictly ascending" | "calendar" | "is constant" | "horizon must be" | "use D, W or MS" | "interval_width must be in (0, 1)" | "needs cap" | "must exceed max(y)" | "prophet or neuralprophet" | "additive or multiplicative" | "linear, logistic or flat"
pmcp 2.19.3: router_with_config(Arc<tokio::sync::Mutex<Server>>, RouterConfig) — the lock spike 010 measured is streamable_http_server.rs:2094/:2122
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contracts/forecast-tool-boundary-v1.yaml — both servers' bounds, refusal rules, shared response shape, pool equality</name>
  <reversibility rating="costly">The boundary constants and response field set are what clients and the Chronos server (06-07) are tested against; changing them is a versioned contract edit and a client-visible change.</reversibility>
  <files>contracts/forecast-tool-boundary-v1.yaml, crates/aprender-forecast/src/types.rs</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (lines 1-30, 80-100, 1010-1022, 1103-1115, 1243-1262, 1276-1310)
    - crates/aprender-contracts/src/schema/types.rs (lines 202-230, 248-281, 289-321, 326-367, 448-491, 497-525)
    - crates/aprender-forecast/src/types.rs and src/forecast.rs (the bounds and every refusal branch as ported)
    - crates/aprender-forecast/src/chronos.rs (06-05 — a declared dependency of this plan: `CHRONOS_MIN_POINTS 4`, `CHRONOS_MAX_POINTS 20_000`, `CHRONOS_MAX_HORIZON 1_024`, `validate()` and its verbatim refusal messages the contract must also pin; sources/007 lib.rs:25-27, 100-118 is their spike origin)
    - crates/aprender-mcp-setfit/src/lib.rs (lines 157-161: the "(contracts/<file> item N)" citation style in refusal messages)
  </read_first>
  <action>
    (0) Before any edit, record the plan-start SHA once: `test -s /tmp/p06-06-base.sha || git rev-parse HEAD > /tmp/p06-06-base.sha`
    — Task 3's `.github` guard diffs the committed range from it as well as the working tree.
    Create `contracts/forecast-tool-boundary-v1.yaml`: `contract: forecast-tool-boundary`, `metadata` (`kind: kernel`;
    description: the stateless `forecast` tool (D-01), one shape shared by every server so a client switches
    by endpoint (D-03), refuse-never-default (D-11), the pool rationale (D-12) and the EMPIRICAL coverage
    sentence (D-16); `references`: MCP specification 2025-06-18 tools/call, pmcp 2.19.3 `streamable_http_server.rs`
    lock lines, `references/forecast-mcp-thin-server.md`, `sources/010-*/RUN-OUTPUT.md`), `equations`:
    `fit_server_bounds`, `chronos_server_bounds`, `date_validity` (YYYY-MM-DD, real calendar, strictly ascending,
    unique), `freq_allowlist` (D, W, MS; H refused), `interval_width_open_unit`, `logistic_requires_cap_above_max_y`,
    `constant_y_refused_before_fit`, `unknown_field_refused` (additionalProperties false), `unknown_enum_refused`
    (model / growth / seasonality_mode), `horizon_gate_with_warning` (Chronos: > native 64 needs
    allow_long_horizon; response carries warning), `shared_response_shape` (both responses carry model, freq,
    n_history, predict_seconds, ds, yhat, yhat_lower, yhat_upper, diagnostics; fit server adds fit_seconds,
    trend, components; Chronos adds context_used, quantiles, warning), `refusals_are_validation_errors`
    (pmcp::Error::validation, never internal, never a default), `pool_equality_under_load` (signature of
    ds/yhat/bands/trend/components identical alone vs under 8 concurrent; speed-up >= 2.0x on aarch64 release),
    `default_seed_42`; top-level `constants` per the interfaces block; `proof_obligations` (>= 1 per
    equation; types `invariant|bound|determinism|completeness`), `falsification_tests` FALSIFY-BOUNDARY-001..
    naming the e2e test fns of Task 2/3 and 06-07's chronos e2e, `kani_harnesses` (house comment; harness
    names `dates` bounded grid test from 06-03 by path `prophet::parity::future_days_strictly_increasing_bounded`),
    `qa_gate` F-FORECAST-BOUNDARY-001. Validate with `pv`. Then in `crates/aprender-forecast/src/types.rs`
    add `#[cfg(test)] mod tests` with `bounds_match_contract`: `MIN_POINTS == constant_u64("forecast-tool-boundary-v1", "fit_min_points")`,
    same for `fit_max_points`, `fit_max_horizon`; and `chronos_bounds_match_contract`:
    `crate::chronos::CHRONOS_MIN_POINTS == constant_u64("forecast-tool-boundary-v1", "chronos_min_points")`,
    `CHRONOS_MAX_POINTS == chronos_max_points`, `CHRONOS_MAX_HORIZON == chronos_max_horizon` — unconditional:
    06-05 is a declared dependency of this plan (Wave 3), so `crate::chronos` exists; 06-07 additionally asserts
    `chronos_native_horizon` against the config fixture through the server crate.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-06-t1a.log 2>&1; rc=$?; tail -3 /tmp/p06-06-t1a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-06-t1a.log && CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-06-t1b.log 2>&1; rc2=$?; cat /tmp/p06-06-t1b.log; test $rc2 -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-06-t1b.log</automated>
    <fails_when>validate non-zero / no "0 error(s)"; or any status count is 0</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib bounds_match_contract > /tmp/p06-06-t1c.log 2>&1; rc=$?; tail -5 /tmp/p06-06-t1c.log; test $rc -eq 0 && grep -E 'test result: ok\. 2 passed; 0 failed' /tmp/p06-06-t1c.log > /dev/null</automated>
    <fails_when>non-zero exit or not exactly "2 passed" (a fit-server or Chronos bound disagrees with the contract, or a test name drifted so the filter matched fewer)</fails_when>
  </verify>
  <done>Contract valid and non-hollow; the fit server's three bounds and the Chronos door's three bounds are asserted equal to the contract.</done>
  <acceptance_criteria>
    - `contracts/forecast-tool-boundary-v1.yaml` contains `fit_min_points: 10`, `fit_max_points: 20000`, `fit_max_horizon: 3650`, `chronos_min_points: 4`, `chronos_max_horizon: 1024`, `chronos_native_horizon: 64`, `pool_default: 8`, `default_seed: 42`, `shared_response_shape:`, `pool_equality_under_load:`, `FALSIFY-BOUNDARY-001`, `DECLARED, NOT EXECUTED`
    - `crates/aprender-forecast/src/types.rs` contains `fn bounds_match_contract`, `fn chronos_bounds_match_contract`, `crate::chronos::CHRONOS_MIN_POINTS` and `constant_u64("forecast-tool-boundary-v1"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The full D-11 refusal set + strict schema + NP happy path as in-process e2e cases</name>
  <files>crates/aprender-mcp-forecast/src/lib.rs</files>
  <read_first>
    - crates/aprender-mcp-forecast/src/lib.rs (the e2e module and Client harness from 06-01)
    - .claude/skills/spike-findings-aprender/sources/004-forecast-mcp-thin-server/tests/e2e.rs (lines 68-100: the three spike refusal cases, the MS+multiplicative, logistic+holidays and NP cases)
    - crates/aprender-mcp-setfit/src/lib.rs (lines 305-320: strict-schema unit test; 226-229 module allow)
    - crates/aprender-forecast/src/forecast.rs (every refusal message — assert substrings, never rewrite them)
  </read_first>
  <action>
    Extend `#[cfg(test)] mod e2e` in the server lib.rs. Add a shared `async fn serve() -> Client` bring-up
    (single router, as 06-01) and `fn peyton() -> (Vec<String>, Vec<f64>)` from the included CSV. Add a helper
    `async fn refused(c: &mut Client, args: serde_json::Value, needle: &str)` asserting the JSON-RPC reply has
    `error` or `result.isError == true` AND the stringified reply contains `needle`. Cases (one `#[tokio::test]`
    each, names `refuses_<case>`): unknown field (`{"bogus": 1}` merged into valid args -> reply is an error;
    needle "bogus" or "unknown field"), five points ("at least"), 20 001 synthetic daily points ("exceeds
    max_points"), ds/y length mismatch ("ds has"), unsorted ds ("strictly ascending"), duplicate ds ("strictly
    ascending"), impossible date "2008-02-30" ("calendar"), constant y ("is constant"), horizon 0 ("horizon must
    be"), horizon 3651 ("horizon must be"), freq "H" ("use D, W or MS"), interval_width 1.0 ("interval_width
    must be in (0, 1)"), logistic without cap ("needs cap"), logistic with cap <= max(y) ("must exceed max(y)"),
    unknown model "arima" ("prophet or neuralprophet"), unknown seasonality_mode ("additive or multiplicative"),
    unknown growth ("linear, logistic or flat"). Happy paths: `neuralprophet_lag_free_happy_path` (Peyton,
    model neuralprophet, horizon 30 -> model == "neuralprophet", 30 ds, bands ordered, diagnostics.selected_lr
    present, diagnostics.band mentions "residual"), `prophet_ms_multiplicative_happy_path` (air_passengers CSV,
    freq MS, seasonality_mode multiplicative -> components has multiplicative_terms), `prophet_logistic_with_holidays_happy_path`
    (Peyton, growth logistic with cap above max(y), holidays [superbowl dates] -> components has superbowl).
    Add the unit test `the_tool_schema_is_strict_and_requires_ds_y_horizon` (schemars `schema_for!(ForecastArgs)`:
    `additionalProperties == false`, `required` contains exactly ds, y, horizon; properties include freq, model,
    n_lags, seed, holidays). Every refusal must be Validation-class: assert the error `code` pmcp uses for
    validation (read it off one failing reply and pin it) — an `internal` code is a bug.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-06-t2.log 2>&1; rc=$?; tail -30 /tmp/p06-06-t2.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-06-t2.log > /dev/null && test "$(grep -c '^test e2e::refuses_' /tmp/p06-06-t2.log)" -ge 14 && test "$(grep -c '^test e2e::.*happy_path' /tmp/p06-06-t2.log)" -ge 4</automated>
    <fails_when>non-zero exit; any failure; fewer than 14 `refuses_` cases or fewer than 4 happy paths ran</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib the_tool_schema_is_strict > /tmp/p06-06-t2b.log 2>&1; rc=$?; tail -3 /tmp/p06-06-t2b.log; test $rc -eq 0 && grep -E 'test result: ok\. 1 passed' /tmp/p06-06-t2b.log > /dev/null</automated>
    <fails_when>non-zero exit or not exactly 1 passed</fails_when>
  </verify>
  <done>>= 14 refusal e2e cases and 4 happy paths pass; the schema is strict; every refusal is validation-class.</done>
  <acceptance_criteria>
    - lib.rs contains `fn refuses_unknown_field`, `fn refuses_too_few_points`, `fn refuses_too_many_points`, `fn refuses_impossible_date`, `fn refuses_constant_y`, `fn refuses_horizon_above_max`, `fn refuses_unknown_freq`, `fn refuses_interval_width_out_of_range`, `fn refuses_logistic_without_cap`, `fn refuses_logistic_cap_below_max_y`, `fn refuses_unknown_model`, `fn neuralprophet_lag_free_happy_path`, `fn the_tool_schema_is_strict_and_requires_ds_y_horizon`
    - The e2e module asserts the validation error code (grep for the pinned numeric code or `"validation"` in the module)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Router pool (`pooled_app`, `--pool K`), the equality-under-load test, and the spawned-binary stdio e2e</name>
  <files>crates/aprender-mcp-forecast/src/lib.rs, crates/aprender-mcp-forecast/src/main.rs, crates/aprender-mcp-forecast/tests/e2e_stdio.rs</files>
  <read_first>
    - .claude/skills/spike-findings-aprender/sources/010-forecast-server-concurrency/src/main.rs (lines 44-68: signature, max_abs_diff, the pool `app(pool)`; 70-135: sequential baseline, 3 concurrent rounds, the 16x NP stress)
    - .claude/skills/spike-findings-aprender/sources/010-forecast-server-concurrency/RUN-OUTPUT.md (1.0x with one router; 3.9x with 8; 8/8 and 16/16 identical)
    - crates/aprender-mcp-setfit/tests/e2e_stdio.rs (217 lines: KillOnDrop, request/send/recv_json, CARGO_BIN_EXE spawn, reader thread, tools/list assertions, unknown-key probe, the shutdown note — copy the harness, drop the ENV_MODEL gate)
    - crates/aprender-mcp-forecast/src/main.rs (the --http branch from 06-01)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F5: why the stdio target is dark in CI; Pattern 2)
  </read_first>
  <action>
    (1) lib.rs: `pub fn pooled_app(pool: usize, name: &str, version: &str) -> pmcp::Result<axum::Router>`
    per PATTERNS: `pool <= 1` returns `http_app(build_server(name, version)?)`; otherwise K independent
    `http_app(build_server(..)?)` routers in an `Arc<Vec<axum::Router>>`, an `Arc<AtomicUsize>` counter, and
    `axum::Router::new().fallback(move |req: axum::extract::Request| { ... routers[i].clone().oneshot(req).await.unwrap_or_else(|e| match e {}) })`
    (Infallible — not the banned method) with `i = next.fetch_add(1, Ordering::Relaxed) % routers.len()`;
    `use tower::ServiceExt`. Doc comment cites the pmcp lock lines and the 1.0x -> 3.9x measurement.
    (2) main.rs: `--http PORT [--pool K]` parses `--pool` (default 8, 0 or 1 = single router) in the hand-rolled
    argv loop; usage error -> `ExitCode::from(2)`; the stderr banner prints the pool size.
    (3) lib.rs `#[cfg(test)] mod pool_equality` with `#[tokio::test(flavor = "multi_thread", worker_threads = 4)] async fn eight_concurrent_requests_are_bit_identical_to_sequential`:
    serve `pooled_app(8, ..)`; the request set = 8 distinct requests (4 Prophet on synthetic 1000-point daily
    series with different seeds / horizons, 2 NP lag-free, 2 NP n_lags=30 on 500 points) — or the Peyton set
    when env `FORECAST_POOL_SERIES=peyton` is set (the release-profile `just forecast-pool-ratio` in 06-08 uses
    it); run each sequentially and record `signature(out)` (spike 010 fn: JSON of ds/yhat/yhat_lower/yhat_upper/
    trend/components) and the sequential wall; fire all 8 at once with `tokio::join!`/`futures` and assert
    every signature equals its baseline (identical == 8) and `max_abs_diff == 0.0`; compute `speedup = seq / conc`;
    `if cfg!(target_arch = "aarch64") && !cfg!(debug_assertions) { assert!(speedup >= 2.0, ..) } else { println!("speedup {speedup:.2}x (not asserted: {arch}/{profile})") }`.
    A second test `sixteen_concurrent_neuralprophet_fits_stay_identical` (NP n_lags=30, 500 points, 16 at once)
    asserts 16/16 identical and 0 errors.
    (4) `tests/e2e_stdio.rs`: the SetFit harness whole, spawning `env!("CARGO_BIN_EXE_aprender-mcp-forecast")`
    with no args; `initialize` -> `notifications/initialized` -> `tools/list` (exactly ONE tool, name
    `forecast`, `inputSchema.additionalProperties == false`) -> `tools/call` with 60 synthetic daily points,
    horizon 7 -> 7 `ds` and finite `yhat` -> `tools/call` with an unknown key -> error. No model env gate
    (the fit server needs none) — this test ALWAYS runs locally; it is dark in CI until `.github/workflows/ci.yml`
    line 471 lists it, which is 06-08's human checkpoint, not this task.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib pool_equality > /tmp/p06-06-t3a.log 2>&1; rc=$?; tail -10 /tmp/p06-06-t3a.log; test $rc -eq 0 && grep -E 'test result: ok\. 2 passed; 0 failed' /tmp/p06-06-t3a.log > /dev/null</automated>
    <fails_when>non-zero exit (a signature differed under load, or the aarch64-release speed-up bar failed) or not exactly 2 passed</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --test e2e_stdio > /tmp/p06-06-t3b.log 2>&1; rc=$?; tail -6 /tmp/p06-06-t3b.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-06-t3b.log > /dev/null</automated>
    <fails_when>non-zero exit (spawn failed, a JSON-RPC step errored, or the unknown key was accepted) or 0 passed</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-mcp-forecast --all-targets -- -D warnings > /tmp/p06-06-t3c.log 2>&1; rc=$?; tail -3 /tmp/p06-06-t3c.log; test $rc -eq 0 && cargo fmt --all -- --check > /dev/null 2>&1 && test "$(git diff --stat HEAD -- .github/workflows | wc -l)" -eq 0 && test -s /tmp/p06-06-base.sha && test "$(git diff --stat "$(cat /tmp/p06-06-base.sha)"..HEAD -- .github/workflows | wc -l)" -eq 0</automated>
    <fails_when>clippy/fmt non-zero, or any file under .github/workflows changed — in the working tree (staged or not) OR in any commit since the plan-start SHA Task 1 recorded (CI edits are a human check-in — 06-08). A missing /tmp/p06-06-base.sha is itself a failure: re-record it as the parent of this plan's first `(06-06)` commit</fails_when>
  </verify>
  <done>pooled_app + --pool land; 8/8 and 16/16 bit-identical under load; the stdio round trip passes against the spawned binary; ci.yml untouched.</done>
  <acceptance_criteria>
    - lib.rs contains `pub fn pooled_app(`, `use tower::ServiceExt`, `fetch_add(1, Ordering::Relaxed) % routers.len()`, `fn eight_concurrent_requests_are_bit_identical_to_sequential`, `fn sixteen_concurrent_neuralprophet_fits_stay_identical`, `target_arch = "aarch64"`, `debug_assertions`
    - main.rs contains `"--pool"` and a default of `8`
    - `tests/e2e_stdio.rs` contains `CARGO_BIN_EXE_aprender-mcp-forecast`, `"tools/list"`, `additionalProperties`, `"tools/call"`, and an unknown-key probe; it contains no environment-variable skip gate
    - `grep -c 'ignored' /tmp/p06-06-t3b.log` shows `0 ignored` in the summary (the stdio test is unconditional)
    - The SUMMARY records the measured speed-up ratio and the host/profile it was measured on
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K routers) | untrusted JSON-RPC; K concurrent blocking fits |
| stdio parent process -> server | protocol on stdout; refusals as JSON-RPC errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `ForecastArgs` unknown fields / enums | high | mitigate | `deny_unknown_fields`; every unknown enum value refused; 14+ e2e cases; schema strictness unit test |
| T-06-02 | Denial of Service | K concurrent fits | high | mitigate | the pool bounds in-flight fits to K (default 8) on the blocking pool; per-fit `FIT_BUDGET_SECS 15`, `MAX_POINTS`, `MAX_HORIZON`; README documents sizing K to the blocking-thread budget |
| T-06-07 | Repudiation | responses under load | medium | mitigate | per-request seed; `pool_equality` asserts bit-identical signatures (8/8, 16/16) |
| T-06-03 | Spoofing | loopback HTTP | medium | mitigate | `127.0.0.1` bind, `AllowedOrigins::localhost()` per router in the pool |
| T-06-SC | Tampering | cargo installs | high | accept | `tower` is already a workspace dependency (RESEARCH F9); nothing new enters the graph |
</threat_model>

<verification>
- `cargo test -p aprender-mcp-forecast --lib e2e::` >= 14 refusals + 4 happy paths, 0 failed
- `cargo test -p aprender-mcp-forecast --lib pool_equality` 2 passed (equality asserted everywhere; speed-up asserted only on aarch64 release)
- `cargo test -p aprender-mcp-forecast --test e2e_stdio` passes (local; dark in CI pending 06-08)
- `pv validate` / `pv status` on the boundary contract; `bounds_match_contract` green
</verification>

<success_criteria>
- SC1's refusal list is fully e2e-asserted as validation errors; the schema is strict
- SC5's equality-under-load holds (8/8, 16/16); `--pool` default 8 lands (D-12)
- D-03/D-15: both servers' bounds and the shared shape are pinned in one contract
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-06)
New file: `contracts/forecast-tool-boundary-v1.yaml` (equations fit_server_bounds, chronos_server_bounds, date_validity, freq_allowlist, interval_width_open_unit, logistic_requires_cap_above_max_y, constant_y_refused_before_fit, unknown_field_refused, unknown_enum_refused, horizon_gate_with_warning, shared_response_shape, refusals_are_validation_errors, pool_equality_under_load, default_seed_42; constants fit_*/chronos_*/default_seed/pool_default/...; FALSIFY-BOUNDARY-0NN; F-FORECAST-BOUNDARY-001); `crates/aprender-mcp-forecast/tests/e2e_stdio.rs`.
New symbols: `aprender_mcp_forecast::pooled_app`; CLI flag `--pool K` on `--http`; env var `FORECAST_POOL_SERIES` (test-only selector); tests `e2e::refuses_*` (>= 14), `e2e::{neuralprophet_lag_free_happy_path, prophet_ms_multiplicative_happy_path, prophet_logistic_with_holidays_happy_path}`, `tests::the_tool_schema_is_strict_and_requires_ds_y_horizon`, `pool_equality::{eight_concurrent_requests_are_bit_identical_to_sequential, sixteen_concurrent_neuralprophet_fits_stay_identical}`, `types::tests::{bounds_match_contract, chronos_bounds_match_contract}`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-06-SUMMARY.md` when done. Record the pinned validation error code, the measured speed-up (host/profile), the 8/8 and 16/16 identical counts, and that `tests/e2e_stdio.rs` is dark in CI pending 06-08.
</output>
