---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 14
type: execute
wave: 12
gap_closure: true
depends_on: [06-13]
files_modified:
  - crates/aprender-forecast/src/forecast.rs
  - crates/aprender-forecast/src/types.rs
  - crates/aprender-forecast/src/prophet.rs
  - crates/aprender-forecast/src/test_support.rs
  - crates/aprender-mcp-forecast/src/lib.rs
  - contracts/forecast-tool-boundary-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [SC1]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # ---- THE CLASS INVARIANT (round-3 directive item 2). Two halves, both checkable. ----
    - "CLASS INVARIANT, HALF 1 — KNOBS: every caller-settable field reachable through `aprender_forecast::forecast` (every field of `ForecastArgs` AND of `HolidayArg`) is listed in `contracts/forecast-tool-boundary-v1.yaml` `door_surface.knobs` with the arm that reads it and the enforcement that covers it, and a field added to either struct WITHOUT a matching `door_surface.knobs` entry turns `types::tests::every_request_knob_is_enumerated` red. The list is derived from `schemars::schema_for!`, not from a hand-kept copy, so it cannot silently fall behind the struct"
    - "CLASS INVARIANT, HALF 2 — COST AXES: every cost axis whose magnitude is a function of caller input and that is spent BEFORE or OUTSIDE `fit::FIT_BUDGET_SECS` is listed in `door_surface.cost_axes` with a `bound:` that names a key PRESENT in the same file's `constants:` block (or the literal `measured_at_structural_maximum` carrying a `measured_seconds` under the 2 s SC1 bar), and an axis whose `bound:` names a key that does not exist turns `types::tests::every_cost_axis_names_a_real_bound` red"
    - "The enumeration is by INSPECTION, not by symptom: it was produced by reading `forecast.rs`, `types.rs`, `prophet.rs`, `np.rs` and `dates.rs` after a `pmat query` semantic pass (CLAUDE.md Code Search Policy), and it names axes NO review finding pointed at — the SUMMARY records which axes the enumeration found that `06-REVIEW.md` did not"
    # ---- CR-01, as ONE instance of the invariant ----
    - "A logistic prophet request whose simulated changepoint mean `lambda = n_changepoints x (t_max - 1)` exceeds `MAX_LOGISTIC_CHANGEPOINT_LAMBDA` is REFUSED at the door, before `make_design`, with a validation-class message naming the observed lambda, both factors, the bound key and three concrete fixes"
    - "The door computes the SAME lambda `predict` will draw, because both read one function: `prophet::changepoint_count` is the single implementation of the effective changepoint count, `make_design` uses it, and a test asserts `design.changepoints_t.len() == changepoint_count(n, &spec)` across a sweep of point counts. The bound cannot be evaded by the door and the sampler disagreeing about `n_changepoints`"
    - "The review's measured request — 33 points at 1-day spacing, `horizon: 3650`, `freq: \"MS\"`, `growth: \"logistic\"`, `cap: 50`, 1 132 bytes, measured at 2.334 s on HEAD — is refused, and a two-sided control one step UNDER the bound is still accepted and still returns the full response shape"
    - "The bound VALUE is derived from measurement, not chosen: the SUMMARY carries at-the-bound walls at three compositions (freq D / W / MS at the tightest legal history span) on a release build, all under 2 s, in the same form `MAX_HOLIDAY_DESIGN_COST`'s doc comment carries its three"
    - "The RED side was OBSERVED through the live streamable-HTTP server: with `fit_max_logistic_changepoint_lambda` temporarily raised, the same request is ACCEPTED and returns a forecast; restored, it is refused. A bound with no observed red side is what `types::tests::cost_bounds_match_contract` already proves is insufficient (`06-REVIEW.md` WR-01)"
    # ---- WR-01 and IN-03, which belong to this plan because the completeness test is what catches them ----
    - "WR-01 closed: `prophet::POISSON_NORMAL_BRANCH_LAMBDA` is contract-owned — `constants.poisson_normal_branch_lambda` exists and `types::tests::cost_bounds_match_contract` asserts the Rust value equals it through a new `test_support::constant_f64`, so the branch threshold can no longer be moved in Rust alone"
    - "IN-03 closed: `cost_bounds_match_contract`'s doc no longer says 'four', and `DEFAULT_POOL` — a router-pool size, not a per-request cost ceiling — is asserted by its own `pool_default_matches_contract` rather than riding in the cost-bound table"
    - "No parity rung moves: `prophet::parity` reports exactly 32 passed / 0 failed before and after, and no fixture or tolerance in `prophet-parity-v1.yaml`, `neuralprophet-parity-v1.yaml` or `chronos-bolt-parity-v1.yaml` is touched"
  artifacts:
    - path: "contracts/forecast-tool-boundary-v1.yaml"
      provides: "the `door_surface:` enumeration (knobs + cost_axes), `constants.fit_max_logistic_changepoint_lambda`, `constants.poisson_normal_branch_lambda`, the `logistic_changepoint_cost_bounded` and `door_surface_is_complete` equations, their proof obligations and their FALSIFY-BOUNDARY entries"
      contains: "door_surface"
    - path: "crates/aprender-forecast/src/types.rs"
      provides: "MAX_LOGISTIC_CHANGEPOINT_LAMBDA plus the two completeness tests and the split-out pool assertion"
      contains: "MAX_LOGISTIC_CHANGEPOINT_LAMBDA"
    - path: "crates/aprender-forecast/src/forecast.rs"
      provides: "the lambda refusal in the prophet arm, before make_design, and its two-sided library control"
      contains: "max_logistic_changepoint_lambda"
    - path: "crates/aprender-forecast/src/prophet.rs"
      provides: "`changepoint_count` as the ONE implementation of the effective changepoint count, called by make_design and by the door"
      contains: "fn changepoint_count"
    - path: "crates/aprender-forecast/src/test_support.rs"
      provides: "constant_f64 and contract_value — the readers the mirror test and the two completeness tests need"
      contains: "fn constant_f64"
    - path: "crates/aprender-mcp-forecast/src/lib.rs"
      provides: "the red-side and near-miss e2e pair driving a live streamable-HTTP router with the review's exact payload"
      contains: "refuses_logistic_changepoint_lambda_over_bound"
    - path: "contracts/aprender/binding.yaml"
      provides: "binding rows resolving the two new equations to real definition sites"
      contains: "logistic_changepoint_cost_bounded"
  key_links:
    - from: "crates/aprender-forecast/src/forecast.rs"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "types::MAX_LOGISTIC_CHANGEPOINT_LAMBDA asserted equal to constants.fit_max_logistic_changepoint_lambda by cost_bounds_match_contract — the contract is the source, the code the mirror (D-15)"
      pattern: "fit_max_logistic_changepoint_lambda"
    - from: "crates/aprender-forecast/src/forecast.rs"
      to: "crates/aprender-forecast/src/prophet.rs"
      via: "the door calls prophet::changepoint_count, the same function make_design calls, so the door's lambda IS predict's lambda"
      pattern: "changepoint_count"
    - from: "crates/aprender-forecast/src/types.rs"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "every_request_knob_is_enumerated walks schema_for!(ForecastArgs)/schema_for!(HolidayArg) and requires a door_surface.knobs entry per property — the invariant is checked, not narrated"
      pattern: "every_request_knob_is_enumerated"
  prohibitions:
    - statement: "Never clamp inside `poisson` (`n_changes.min(CAP)`). That removes the wall and silently reintroduces the truncation 06-13 exists to delete. The refusal is at the door — this phase's own D-11 discipline"
      status: resolved
      verification: test
    - statement: "Never touch a parity fixture, a parity tolerance or `contracts/{prophet,neuralprophet,chronos-bolt}-parity-v1.yaml`'s numbers. A refusal added at the door must not move a fixture; if `prophet::parity` leaves 32/0, the bound is reaching the fixture regime and the BOUND is wrong, not the ladder"
      status: resolved
      verification: test
    - statement: "Never write the effective changepoint count twice. If the door computes `min(25, floor(n*0.8) - 1)` inline while `make_design` computes its own, the two can drift and the bound becomes evadable — `changepoint_count` is extracted first and both call it"
      status: resolved
      verification: test
    - statement: "Never hand-maintain the knob list. `door_surface.knobs` is CHECKED against `schemars::schema_for!` output in both directions (no missing entry, no phantom entry); a list that is only prose is exactly the aspirational invariant this round exists to replace"
      status: resolved
      verification: test
    - statement: "Do not touch `crates/aprender-compute`, `.github/workflows/*.yml`, or `crates/aprender-serve` — scope fence for this gap-closure run"
      status: resolved
      verification: judgment
---

<objective>
Round 3 of Phase 06 gap closure, TRACER plan. Stop fixing the probe and close the CLASS: enumerate the
forecast door's full caller-settable surface and its full cost surface by inspection, land that
enumeration as a machine-CHECKED artifact in the tool-boundary contract, and close `06-REVIEW.md`'s
CR-01 as the first instance of the invariant that enumeration makes checkable.

Purpose: every gap this phase has produced is one instance of "the door accepts a request whose cost
or whose semantics it never checks", and rounds 1 and 2 each fixed exactly the measured probe. CR-01
is round 2's own fix walking into round 3: 06-13 correctly removed Knuth's accidental saturation near
745, and the simulated changepoint count now tracks lambda — which nothing at the door bounds. A
1 132-byte accepted request went from 0.157 s to 2.334 s (14.8x), over the same 2 s SC1 bar
06-11/06-12 were created to defend. Bounding lambda alone would be round 3 of the same mistake, so the
bound falls out of an enumeration that also names the axes no review has pointed at yet.

Output: `door_surface:` in `contracts/forecast-tool-boundary-v1.yaml` with two completeness tests that
turn red on a new field or a phantom bound; `MAX_LOGISTIC_CHANGEPOINT_LAMBDA` refused at the door
before `make_design`; a red-side e2e pair through a live streamable-HTTP router using the review's
exact payload; and `POISSON_NORMAL_BRANCH_LAMBDA` finally contract-owned.
</objective>

<gap_closure_context>
Source: `.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md` (incremental code
review, diff base `ce3e5a8ea..HEAD`, 1 Critical + 4 Warnings + 4 Info) — **not** `06-VERIFICATION.md`,
which is dated `2026-09-07T00:26Z`, predates plans 06-10..06-13, and whose three gaps those four plans
already closed. Do not re-close them.

Findings this plan owns:

| Finding | Where it lands |
|---|---|
| **CR-01** — 06-13 unbounded the simulated changepoint count; a 1.1 KB accepted request walls at 2.33 s | Task 1 (enumeration + bound), Task 3 (red-side e2e + contract equation) |
| **WR-01** — `POISSON_NORMAL_BRANCH_LAMBDA` is the one new behavioural constant with no contract mirror | Task 2 (contract key + `constant_f64` mirror). The *test-strength* half of WR-01 (a tight sweep point the normal branch fails) is plan 06-17 Task 1 |
| **IN-03** — `cost_bounds_match_contract`'s doc says "four", the table has seven, one is not a cost bound | Task 2 |

The other six findings are dispositioned in the ledger below and land in 06-15, 06-16 and 06-17. No
finding is silently dropped.

## The measured trigger (from `06-REVIEW.md` CR-01, reproduced three times)

Release build, `--http --pool 1`, min of 2 runs, same host, pre-fix binary from `ce3e5a8ea`:

| request (all accepted, all inside every door bound) | pre-06-13 | HEAD | factor |
|---|---|---|---|
| 33 pts / 1 d spacing, `horizon: 3650`, `freq: "MS"`, `growth: "logistic"`, `cap: 50` | 0.157 s | **2.334 s** | **14.8x** |
| same but 30 d point spacing (lambda / 30) — *control* | 0.152 s | 0.209 s | 1.38x |
| same but `freq: "W"` | 0.164 s | 0.634 s | 3.87x |
| same but `freq: "D"` | 0.173 s | 0.231 s | 1.34x |
| same but `growth: "linear"` — *the arm that does not call `poisson`* | 0.147 s | 0.149 s | flat |

The mechanism is pinned by the control pair, not argued: the two logistic rows differ ONLY in history
spacing — identical point count, identical horizon, identical row count — so the 11x spread between
them is attributable to lambda and nothing else.

This plan does NOT modify, supersede or re-create 06-01 … 06-13 or their SUMMARYs.
</gap_closure_context>

<review_dispositions_ledger>
## Review Dispositions Ledger — all 9 findings in `06-REVIEW.md`

Repeated verbatim in every round-3 plan so no plan can be read in isolation and conclude a finding
was dropped.

| ID | Severity | Disposition | Landing site |
|---|---|---|---|
| CR-01 | Critical | **INCORPORATED** | 06-14 T1 (enumeration + `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`), T3 (red-side e2e + `logistic_changepoint_cost_bounded`); 06-16 (the sweep gate that would have caught it) |
| WR-01 | Warning | **INCORPORATED, SPLIT** | 06-14 T2 (contract mirror for `POISSON_NORMAL_BRANCH_LAMBDA` via `constant_f64`); 06-17 T1 (the tight sub-threshold sweep point that the normal branch actually fails, so lowering the constant can no longer leave the suite green) |
| WR-02 | Warning | **INCORPORATED** | 06-17 T1 (sample-variance assertion with its own contract-owned tolerance; the zero-variance stub proven RED) |
| WR-03 | Warning | **INCORPORATED** | 06-15 T1 (aggregate-dates refusal moved into the holiday loop; the post-loop exact-total refusal kept) |
| WR-04 | Warning | **INCORPORATED** | 06-16 T1/T2/T3 (one parameterized SC1 sweep over freq x growth x holiday, wired into `just`, run without `--ignored`; the three geometry-hard-coded benches folded in; the superlative reworded) |
| IN-01 | Info | **INCORPORATED** | 06-16 T2 (a shared numeric-shape validator replacing the `awk … (v + 0 < 2.0)` coercion at ALL FIVE bar sites, with a must-match/must-not-match case table per CLAUDE.md rule 7) |
| IN-02 | Info | **INCORPORATED, NARROWED** | 06-17 T2 (`hol_sets.get(hi).is_some_and(…)` so a mismatched slice is a miss rather than an out-of-bounds panic, plus a semver note). The signature is NOT reverted: reverting re-opens 06-11's measured design-build fix, and the review itself records the hazard as pre-existing rather than a regression |
| IN-03 | Info | **INCORPORATED** | 06-14 T2 (doc count corrected; `DEFAULT_POOL` split into `pool_default_matches_contract`) |
| IN-04 | Info | **INCORPORATED IN PART, REST REJECTED WITH RATIONALE** | 06-17 T2 takes the two substantive items: `debug_assert_eq!(w.len(), out * inp)` at `bolt.rs`'s `trueno::blis::transpose` call, and a semver note for the `safetensors::load` removal + the `feature_row` signature change. REJECTED, no action: "five source files fall outside the stated 9-file scope" is an observation about the REVIEW's own scope statement, not a defect in the tree; the `dates.rs` `parse_date` rewrite the same finding examined was found sound, so there is nothing to change. Recorded here so it cannot be re-raised as new |
</review_dispositions_ledger>

<door_surface_enumeration>
## The enumeration (round-3 directive item 1) — produced by inspection, to be verified by Task 1

This table is the planner's reading of `forecast.rs`, `types.rs`, `prophet.rs`, `np.rs`, `dates.rs`
and `fit.rs` at HEAD. **Task 1 re-derives it and corrects it** — a planner's table is a hypothesis,
and the checked-in `door_surface:` block is the artifact. Where Task 1's reading disagrees, Task 1's
reading wins and the SUMMARY records the delta.

### (a) Caller-settable knobs reachable through `aprender_forecast::forecast`

| # | Knob | Owner | Arm(s) that read it | Enforcement at HEAD | Verdict |
|---|---|---|---|---|---|
| K-01 | `ds` | `ForecastArgs` | both | `len(ds)==len(y)`, `MIN_POINTS`, `MAX_POINTS`, `parse_date` per entry, strictly ascending, `MAX_SPAN_DAYS` | enforced |
| K-02 | `y` | `ForecastArgs` | both | length tie to `ds`, all finite, non-constant | enforced |
| K-03 | `horizon` | `ForecastArgs` | both | `1..=MAX_HORIZON` | enforced (COUNT only — see C-05) |
| K-04 | `freq` | `ForecastArgs` | prophet: `future_days`; np: refused unless `"D"` | enum `D`/`W`/`MS` | enforced as an ENUM; its COST effect is C-05, and C-05 feeds C-06 |
| K-05 | `model` | `ForecastArgs` | dispatch | enum `prophet`/`neuralprophet` | enforced |
| K-06 | `growth` | `ForecastArgs` | prophet | enum `linear`/`logistic`/`flat` | enforced; selects the C-06 arm |
| K-07 | `cap` | `ForecastArgs` | prophet, logistic only | logistic-only (06-10), finite, `> max(y)` | enforced |
| K-08 | `seasonality_mode` | `ForecastArgs` | prophet | enum `additive`/`multiplicative` | enforced |
| K-09 | `interval_width` | `ForecastArgs` | both | open `(0, 1)` | enforced; no cost |
| K-10 | `holidays` | `ForecastArgs` | prophet only | prophet-only for NP; per-holiday + aggregate bounds | enforced (see K-13) |
| K-11 | `n_lags` | `ForecastArgs` | neuralprophet only | prophet refuses; `<= 365`; `< n_train_grid` | enforced as a RANGE; its COST effect is C-08 |
| K-12 | `seed` | `ForecastArgs` | both | any `u64` legal; no cost axis | enforced by construction |
| K-13 | `holidays[].name` | `HolidayArg` | prophet | **NONE** | **UNENFORCED** — arbitrary-length `String`, cloned TWICE per design column by `prophet::columns` (`name` and `component`) and then used as the sort key of `hcols.sort_by`. 1 000 columns x an N-byte name is a ~2 000:1 amplification off one payload occurrence. Cost axis C-07. Closed by 06-15 T2 |
| K-14 | `holidays[].dates` | `HolidayArg` | prophet | `MAX_HOLIDAY_DATES`, `MAX_HOLIDAY_DATES_TOTAL`, `parse_date` | enforced (but the aggregate check fires LATE — WR-03, 06-15 T1) |
| K-15 | `holidays[].lower_window` | `HolidayArg` | prophet | `<= 0`, `>= -MAX_HOLIDAY_WINDOW`, `MAX_HOLIDAY_COLUMNS` | enforced |
| K-16 | `holidays[].upper_window` | `HolidayArg` | prophet | `>= 0`, `<= MAX_HOLIDAY_WINDOW`, `MAX_HOLIDAY_COLUMNS` | enforced |

### (b) Cost axes that are a function of caller input, with where they are SPENT

`FIT_BUDGET_SECS` is entered inside `fit::fit_prophet`. `make_design` runs before it and `predict`
runs after it, which is the STRUCTURAL reason two of the three prior rounds landed there. The
`spent_outside_budget` column is the one that matters.

| # | Cost axis | Magnitude | Spent in | Outside `FIT_BUDGET_SECS`? | Bound at HEAD |
|---|---|---|---|---|---|
| C-01 | history parse | `len(ds)` | door, `parse_date` loop | yes | `fit_max_points` |
| C-02 | NP imputed daily grid | `span_days` | `np::NpData::new` | yes | `fit_max_span_days` |
| C-03 | holiday date parse + membership sets | `sum(len(dates))` | door + `prophet::holiday_day_sets` | yes | `fit_max_holiday_dates_total` (late — WR-03) |
| C-04 | holiday design feature cells | `(points + horizon) * holiday_columns` | `make_design` + `predict` | yes | `fit_max_holiday_design_cost` |
| C-05 | future span in days | `horizon * freq_step` (1 / 7 / ~30.4) | `dates::future_days`, then `t_max` | yes | **NONE** — feeds C-06 |
| C-06 | logistic simulated changepoints | `uncertainty_samples * n_changepoints * (t_max - 1)` | `predict`, `Growth::Logistic` arm | yes | **NONE — CR-01. Closed by THIS plan** |
| C-07 | holiday column name bytes | `holiday_columns * 2 * len(name)` + an `O(C log C)` string sort | `prophet::columns` | yes | **NONE. Closed by 06-15 T2** |
| C-08 | NeuralProphet training work | `n_lrs * epochs * n_samples * (n_lags + 1)` | `np::train` — **no budget of any kind exists on this path** | yes | factor-wise only. Measured and closed by 06-15 T3 |
| C-09 | linear/flat uncertainty simulation | `uncertainty_samples * n_future` | `predict` | yes | `fit_max_horizon`; measured flat at 0.149 s |
| C-10 | percentile sorts | `n_rows * uncertainty_samples * log(uncertainty_samples)` | `predict` | yes | `fit_max_horizon` |
| C-11 | seasonality design cells | `(points + horizon) * <= 34` | `make_design` + `predict` | yes | `fit_max_points` / `fit_max_horizon`; `auto_seasonalities` is not caller-settable (max 34 columns: yearly order 10, weekly 3, daily 4) |
| C-12 | NP recursive AR predict | `horizon * n_lags` | `np::predict_ar_recursive` | yes | `fit_max_horizon` x 365 |
| C-13 | response serialization | `horizon * (5 + n_components)` | door | yes | `fit_max_horizon` |
| C-14 | L-BFGS fit | `iters * rows * K` | `fit::fit_prophet` | **no — inside** | `MAX_ITERS_PER_ROUND` + the cooperative budget |

`uncertainty_samples` (1 000) is a `Spec` field, NOT a `ForecastArgs` field — it is not caller-settable
and is recorded as a fixed multiplier of C-06/C-09/C-10 rather than as a knob.

### The reachable range of C-06, for the bound's derivation

`lambda = changepoint_count(n) * (t_max - 1)` with `t_max = (fut.last - ds.first) / (ds.last - ds.first)`.
`changepoint_count` is at most 25 (`Spec::default_linear`) and needs `floor(n * 0.8) - 1 >= 25`, i.e.
`n >= 33` points. The tightest legal span for 33 daily points is 32 days; the widest legal future span
is `3650` `MS` steps ~= 111 000 days. So the STRUCTURAL MAXIMUM is
`25 * (111000 / 32 - 1)` ~= **86 700**, which is exactly the review's measured configuration. Every
larger-history request has a proportionally smaller `t_max`, so high-lambda requests are necessarily
short-history requests — which is why `MAX_POINTS`, `MAX_SPAN_DAYS` and `MAX_HORIZON` are each
satisfied and their product is still unbounded, the same shape as `MAX_HOLIDAY_DESIGN_COST`.

Interpolating the review's four measured points (lambda ~2 825 -> 0.231 s, ~2 866 -> 0.209 s,
~19 925 -> 0.634 s, ~86 700 -> 2.334 s) gives ~2.5e-5 s per unit of lambda over a ~0.15 s floor, so
the 2 s bar sits near lambda ~74 000. **20 000 is the proposed value** — the largest round value with
roughly 3x measured headroom (0.634 s at ~19 925). Task 1 MEASURES it at three compositions and lowers
it if any composition fails to clear 2 s.
</door_surface_enumeration>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md  (CR-01, WR-01, IN-03 — the measured numbers, the control pair, the suggested sketch)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-11 refuse-never-default; D-15 the contract is the source and the code the mirror)
@.planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md  (how the holiday PRODUCT bound was derived, measured and worded — the template this plan follows)
@.planning/phases/06-native-time-series-forecasting-stack/06-12-SUMMARY.md  (how the over/near-miss e2e pair and the temporarily-raised-constant RED observation were run)
@.planning/phases/06-native-time-series-forecasting-stack/06-13-SUMMARY.md  (what 06-13 changed in `poisson`, and why CR-01 is its consequence)
@CLAUDE.md  (Verification Discipline rules 1/2/5/6/7; Code Search Policy: `pmat query`, not grep; DOGFOOD `pv`, never bash; `unwrap()` banned; `unsafe_code = "forbid"`)

<interfaces>
**The door** (`crates/aprender-forecast/src/forecast.rs`). `pub fn forecast(args: &ForecastArgs) ->
Result<ForecastResponse, ForecastError>`. Shared validation runs first (:34-125), then a
`match model_name.as_str()` with a `"prophet"` arm (:127-330ish) and a `"neuralprophet"` arm. Inside
the prophet arm the order at HEAD is: `seasonality_mode` enum, `growth` enum, the 06-10 cap refusal,
the cap validity block, the holiday loop, the 06-11/06-12 PRODUCT refusals, then `Spec` assembly, then
`make_design`, `fit_prophet`, `predict`. The new lambda refusal goes AFTER `Spec` is assembled (it
needs `spec` for the changepoint count) and BEFORE `make_design`.

Already in scope at that point: `ds: Vec<i64>` (parsed, strictly ascending, `len >= MIN_POINTS` so
`ds.last() > ds.first()` and `t_scale_days > 0` — no division guard is needed and adding one would
imply a case that cannot occur), `fut: Vec<i64>` (built by `dates::future_days` at :83, so the future
span is ALREADY KNOWN before the prophet arm is entered), `args.horizon`, `growth`, `spec`.

**The changepoint count** (`crates/aprender-forecast/src/prophet.rs`, inside `make_design` at
approximately :262-272):
```
hist_size = (n as f64 * spec.changepoint_range).floor() as usize
n_cp      = spec.n_changepoints;  if n_cp + 1 > hist_size { n_cp = hist_size.saturating_sub(1) }
changepoints_t = if n_cp > 0 { <n_cp entries> } else { vec![0.0] }   // note: len 1, not 0
```
Extract this as `pub fn changepoint_count(n: usize, spec: &Spec) -> usize` returning
`changepoints_t.len()` — i.e. `max(n_cp, 1)` under the branch above — and have `make_design` use it.
The `vec![0.0]` branch is why the function must return the LENGTH, not `n_cp`.

**The lambda `predict` draws** (`crates/aprender-forecast/src/prophet.rs`, `Growth::Logistic` arm of
`predict`, approximately :868-878): `t_max = t[n-1]` where `t` is built from the `ds_days` argument —
which is `fut` — against `d.start_days` (= `ds[0]`) and `d.t_scale_days` (= `ds.last() - ds[0]`), and
`s_cnt = d.changepoints_t.len() as f64`, then `lambda = s_cnt * (t_max - 1.0)` recomputed identically
on every one of the 1 000 simulation rows.

**The constants and their mirror** (`crates/aprender-forecast/src/types.rs`): every bound is a `pub
const` with a long doc comment recording WHY the existing bounds did not cover it and WHAT was
measured. `types::tests::cost_bounds_match_contract` (:228-266) drives a `for (name, key, value)`
table of seven `u64` entries through `test_support::constant_u64`.

**The contract readers** (`crates/aprender-forecast/src/test_support.rs`): `contract_doc` (private,
memoized `Arc<serde_yaml::Value>`), `constant_u64` (:139-150, `constants.<key>` via `as_u64`),
`equation_tolerance` (:120-137, `equations.<eq>.float_tolerance` via `as_f64`). This plan adds
`constant_f64` and `contract_value`; both are one-liners over `contract_doc`.

**`pv` and unknown top-level keys.** `constants:` is ALREADY an unknown top-level key — the comment
above it at :61-63 records that "the parser drops unknown top-level keys" and that this is deliberate.
`door_surface:` is the same shape and is therefore safe for `pv validate`. Confirm it, do not assume
it: `pv validate` must report `0 error(s)` after the block lands.

**The e2e harness** (`crates/aprender-mcp-forecast/src/lib.rs` `#[cfg(test)] mod e2e`): `serve()`
(:396) returns a `Client` on a live in-process streamable-HTTP router; `synth(n)` (:444) builds a
valid daily series; `valid_args()` (:457); `with(extra)` (:500) merges one perturbation;
`refused(&mut c, args, needle)` (:464) asserts error-or-`isError`, that the message contains `needle`,
the pinned `REFUSAL_CODE`, and the `VALIDATION_PREFIX`; `tool_output(&r)` and
`assert_shared_shape(&out, horizon)` for the accept side. The 06-12 pair
`refuses_holiday_design_cost_over_bound` / `accepts_holiday_design_cost_just_under_bound` (:807-869)
is the exact template: a straddling pair one step apart, with an in-test `assert!` proving the OVER
geometry really is over and the UNDER geometry really is under.

**Contract ids.** `falsification_tests` in `forecast-tool-boundary-v1.yaml` runs to
`FALSIFY-BOUNDARY-018` at HEAD; the equations block runs `fit_server_bounds`,
`holiday_design_cost_bounded`, `chronos_server_bounds`, … `pool_speedup_host_gated`. READ the file and
take the next unused ids rather than trusting these sentences.

**Binding rows** (`contracts/aprender/binding.yaml`, Phase 6 block from :1610): `contract:` bare
filename, `equation:`, `module_path:` (a CRATE path — `aprender_forecast::forecast`,
`aprender_forecast::types`), `function:`, quoted `signature:`, `status: implemented`, `notes:`. The
header comment at :1610 states a row count ("55 rows") that must be updated when rows are added.
`make contract-audit-phase6` RESOLVES each row's `function` to a definition site in the file
`module_path` maps to, so a `#[cfg(test)]` test function is a valid target only if it is in that file.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the door's whole surface, land it as `door_surface:`, and close CR-01 through it</name>
  <files>crates/aprender-forecast/src/prophet.rs, crates/aprender-forecast/src/types.rs, crates/aprender-forecast/src/forecast.rs, contracts/forecast-tool-boundary-v1.yaml</files>
  <precondition>No other cargo build is running against this workspace's shared target directory, and `df -h .` shows headroom — this branch halted three times on ENOSPC during waves 1-7 and every round-3 plan runs one at a time.</precondition>
  <reversibility rating="costly">Adding a door refusal narrows the advertised tool boundary of a published crate at 0.63.0: requests a client could send yesterday are refused today. Reversible in code (delete the check, delete the constant) but it is a client-visible contract change, so `contracts/forecast-tool-boundary-v1.yaml` gets a minor version bump and `pv diff` is consulted. Not one-way — no data is destroyed and no fixture moves.</reversibility>
  <read_first>
    - crates/aprender-forecast/src/forecast.rs — the WHOLE file. The shared validation prefix, both
      model arms, and specifically where `fut` is built (`dates::future_days`, ~:83), where the 06-10
      cap refusal sits, where the 06-11/06-12 product refusals sit, and where `make_design` is called
    - crates/aprender-forecast/src/types.rs — every `pub const` and its doc comment (the derivation
      style this task must match), `ForecastArgs`, `HolidayArg`, and
      `types::tests::cost_bounds_match_contract`
    - crates/aprender-forecast/src/prophet.rs — `Spec` and `Spec::default_linear`;
      `columns`; `holiday_day_sets`; `feature_row`; `make_design` in full, especially the
      `hist_size` / `n_cp` / `changepoints_t` block; `predict`'s uncertainty block, especially the
      `Growth::Logistic` arm and the `lambda` line; `poisson` and `POISSON_NORMAL_BRANCH_LAMBDA`
    - crates/aprender-forecast/src/dates.rs — `future_days`, all three freq arms, and the `MS` month
      arithmetic that produces the ~30.4x span multiplier
    - crates/aprender-forecast/src/np.rs — `NpData::new` (the imputed daily grid), `auto_epochs`,
      `auto_batch`, `train`'s `samples` / `batch` / `epochs` / `total_steps` block. Read it for the
      ENUMERATION only; 06-15 T3 owns any change here
    - crates/aprender-forecast/src/fit.rs — `FIT_BUDGET_SECS` and `MAX_ITERS_PER_ROUND` and their doc
      comments, so `spent_outside_budget` is decided from what the code does, not from the plan
    - contracts/forecast-tool-boundary-v1.yaml — the `constants:` block and the comment above it
      recording that unknown top-level keys are dropped by the parser; `fit_server_bounds` and
      `holiday_design_cost_bounded` as the equation style to match
    - .planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md — CR-01 in full,
      including the four measured rows, the control pair and the suggested sketch
    - This plan's `<door_surface_enumeration>` section — the hypothesis this task must confirm or
      correct
  </read_first>
  <action>
    (1) RE-DERIVE THE ENUMERATION, semantically first. Per CLAUDE.md's Code Search Policy run
    `pmat query` rather than grep for the semantic pass — for example
    `pmat query "forecast door validation bound refusal" --path crates/aprender-forecast/ --limit 10`
    and `pmat query "uncertainty simulation sample loop" --path crates/aprender-forecast/ --limit 10` —
    then CONFIRM every hit by reading the source. A query-derived list is a hint, not the enumeration.
    Produce two lists: every caller-settable field reachable through `forecast` (every field of
    `ForecastArgs` and of `HolidayArg`, across BOTH model arms and ALL THREE growth arms), and every
    cost axis whose magnitude is a function of caller input, each tagged with whether it is spent
    before, inside, or after `fit::FIT_BUDGET_SECS`. Compare against this plan's table. Where the
    source disagrees with the plan, the SOURCE WINS — record the delta in the SUMMARY, and if the
    enumeration finds an unbounded axis this plan does not already route to 06-15, STOP and report it
    rather than absorbing it silently.

    (2) EXTRACT `changepoint_count` FIRST, as a pure refactor. Add
    `pub fn changepoint_count(n: usize, spec: &Spec) -> usize` to `prophet.rs` next to `make_design`,
    returning the LENGTH `make_design` would give `changepoints_t` — which is `1`, not `0`, in the
    `n_cp == 0` branch, because that branch is `vec![0.0]`. Rewrite `make_design` to call it. Add
    `prophet::tests` (or the nearest existing sibling module) case
    `changepoint_count_equals_the_design_it_describes` sweeping `n` over at least
    `[10, 12, 20, 32, 33, 34, 100, 3000]` and asserting
    `make_design(&ds, &y, &spec).changepoints_t.len() == changepoint_count(n, &spec)` for each. This
    must be behaviour-preserving: run the whole crate suite and confirm `prophet::parity` is still
    exactly 32 passed / 0 failed before going further.

    (3) ADD THE CONSTANT, with a derivation doc comment in the house style. In `types.rs` add
    `pub const MAX_LOGISTIC_CHANGEPOINT_LAMBDA: f64 = 20_000.0;` — an `f64`, because lambda is one —
    with a doc comment carrying, in the same shape as `MAX_HOLIDAY_DESIGN_COST`'s: why the existing
    bounds did not cover it (`fit_max_points`, `fit_max_span_days` and `fit_max_horizon` are each
    satisfied and their RATIO is not bounded; `fit_max_horizon` bounds the COUNT of future steps while
    `dates::future_days` multiplies that count by 7 for `"W"` and by ~30.4 for `"MS"`); why
    `FIT_BUDGET_SECS` cannot substitute (it is entered inside `fit_prophet`, and this cost is in
    `predict`, which the door calls with no budget at all); the review's four measured rows; the
    structural maximum ~86 700 and how it is reached; and the three at-the-bound compositions this
    task measures in step (6).

    (4) REFUSE AT THE DOOR, before `make_design`. In `forecast.rs`'s prophet arm, after `spec` is
    assembled and before `make_design`, when `growth == Growth::Logistic`, compute
    `t_scale = (ds[ds.len() - 1] - ds[0]) as f64` (positive: `ds` is strictly ascending and
    `MIN_POINTS` is 10), `t_max = (fut[fut.len() - 1] - ds[0]) as f64 / t_scale`, and
    `lambda = changepoint_count(ds.len(), &spec) as f64 * (t_max - 1.0)` — via `prophet::changepoint_count`,
    NOT a second inline copy of the arithmetic. If `lambda > MAX_LOGISTIC_CHANGEPOINT_LAMBDA` return
    `ForecastError::Validation` whose message names: the observed lambda, both factors that produced
    it, the key `max_logistic_changepoint_lambda` and its value, and three concrete fixes — shorten
    the horizon, use freq `D`, or send a longer history. Follow the wording shape of the
    `max_holiday_design_cost` refusal, which names the observed cost, how it was computed, the bound,
    and what to reduce. Do NOT clamp inside `poisson`.

    (5) TWO-SIDED LIBRARY CONTROL. In `forecast::tests` add
    `a_logistic_request_over_the_changepoint_lambda_bound_is_refused` (the review's geometry: 33 daily
    points, `horizon: 3650`, `freq: "MS"`, `growth: "logistic"`, `cap` above `max(y)`) and
    `a_logistic_request_just_under_the_changepoint_lambda_bound_is_accepted`, the near-miss that must
    still fit and still return `horizon` rows — the positive control that keeps the bound from
    over-refusing. Each test computes its own lambda with `changepoint_count` and `assert!`s that its
    geometry really is on the side it claims, so a later change to the constant cannot leave a test
    named "over" sitting under the bound. Add a third case proving the LINEAR arm at the same geometry
    is still accepted: the bound must be keyed on `Growth::Logistic`, and a case that only tests the
    logistic arm would pass even if the refusal fired for everyone.

    (6) MEASURE THE VALUE, three compositions, release. At the bound (lambda within a few percent of
    `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`), at the tightest legal history span, measure the total wall for
    `freq: "D"`, `freq: "W"` and `freq: "MS"` on a RELEASE build and record all three in the SUMMARY
    and in the constant's doc comment. All three must clear 2 s. If any does not, LOWER the constant to
    the largest round value whose three compositions all clear, and say so — the value is derived from
    the measurement, never the other way round. 06-16 turns this measurement into a permanent gate; for
    THIS task a temporary release-profile harness (or `--release --ignored` run of the existing
    `logistic_band_wall`, whose `LOGISTIC_BENCH_POINTS` / `LOGISTIC_BENCH_HORIZON` knobs already exist)
    is sufficient, and CLAUDE.md rule 2 applies: print and quote a `profile=release` marker, never label
    the run by intent.

    (7) LAND `door_surface:` IN THE CONTRACT. Add a new top-level `door_surface:` block to
    `contracts/forecast-tool-boundary-v1.yaml`, beside `constants:` and with a comment block explaining
    the same way `constants:` does that it is a machine-readable mirror the parser drops. Two lists:
    `knobs:`, each entry carrying `field`, `owner` (`ForecastArgs` or `HolidayArg`), `arms`, and
    `enforced_by` (a prose statement naming the actual checks); and `cost_axes:`, each entry carrying
    `axis`, `formula`, `spent_in`, `spent_outside_fit_budget` (a bool), `bound` (a key that must exist
    in `constants:`, or the literal `measured_at_structural_maximum`) and `checked_at`. Every knob from
    (1) and every cost axis from (1) gets an entry — including K-13 / C-07 and C-08, which this plan
    does NOT close: their `bound` is the sentinel `unbounded_pending_06_15`, which Task 2's completeness
    test treats as a FAILURE, so 06-15 cannot be skipped without the suite going red. That is the point:
    a known-open axis is a red test, not a note.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-14-t1a.log 2>&1; rc=$?; tail -8 /tmp/p06-14-t1a.log; test $rc -eq 0 && grep -q 'test result: ok\. 32 passed; 0 failed' /tmp/p06-14-t1a.log</automated>
    <fails_when>non-zero exit, or the summary line is not exactly `test result: ok. 32 passed; 0 failed` — the `changepoint_count` extraction was not behaviour-preserving, or the new door refusal is reaching the parity fixture regime (in which case the BOUND is wrong, not the ladder)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib changepoint_count_equals_the_design_it_describes -- --nocapture > /tmp/p06-14-t1b.log 2>&1; rc=$?; tail -12 /tmp/p06-14-t1b.log; test $rc -eq 0 && grep -q '1 passed' /tmp/p06-14-t1b.log</automated>
    <fails_when>non-zero exit, or `0 passed` in the summary (the test name did not match anything, so nothing was checked) — the door and `make_design` disagree about the effective changepoint count, which makes the lambda bound evadable</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast::tests -- --nocapture > /tmp/p06-14-t1c.log 2>&1; rc=$?; tail -20 /tmp/p06-14-t1c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit, or the summary reports fewer than the three new cases plus the pre-existing `forecast::tests` cases — specifically, a failure in `a_logistic_request_just_under_the_changepoint_lambda_bound_is_accepted` means the bound over-refuses, and a failure in the linear-arm case means the refusal is not keyed on `Growth::Logistic`</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-14-t1d.log 2>&1; rc=$?; tail -10 /tmp/p06-14-t1d.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-14-t1d.log</automated>
    <fails_when>non-zero exit, or the log does not carry `0 error(s)` — `pv` did not accept the new top-level `door_surface:` key, contradicting the file's own comment that unknown top-level keys are dropped. Do not work around this with a shell script (CLAUDE.md: dogfood `pv`); if `pv` genuinely rejects the key, restructure the block or extend the schema, and report which</fails_when>
    <automated>test -f /tmp/p06-14-t1-wall.log && test "$(grep -c 'profile=release' /tmp/p06-14-t1-wall.log)" -ge 3</automated>
    <fails_when>a count of 0, or the file does not exist — the three at-the-bound compositions were measured on a debug build or not measured at all. This crate carries `[profile.dev.package.aprender-forecast] opt-level = 3`, so a debug wall looks plausible and still is not the SC1 bar (CLAUDE.md rule 2). Redirect the measurement run to `/tmp/p06-14-t1-wall.log`</fails_when>
  </verify>
  <acceptance_criteria>
    - `contracts/forecast-tool-boundary-v1.yaml` carries a top-level `door_surface:` block with a
      `knobs:` list of exactly 16 entries (12 `ForecastArgs` fields + 4 `HolidayArg` fields — or
      whatever count Task 1's re-derivation establishes, with the delta recorded) and a `cost_axes:`
      list covering every axis found, each with `axis`, `formula`, `spent_in`,
      `spent_outside_fit_budget`, `bound` and `checked_at`
    - `contracts/forecast-tool-boundary-v1.yaml` `constants:` carries
      `fit_max_logistic_changepoint_lambda` with the measured value
    - `crates/aprender-forecast/src/types.rs` declares `pub const MAX_LOGISTIC_CHANGEPOINT_LAMBDA: f64`
      whose doc comment states the structural maximum (~86 700), the review's four measured rows, and
      this task's three at-the-bound compositions with their release walls
    - `crates/aprender-forecast/src/prophet.rs` declares `pub fn changepoint_count(n: usize, spec: &Spec) -> usize`
      and `make_design` calls it — `grep -n 'fn changepoint_count' crates/aprender-forecast/src/prophet.rs`
      prints exactly one line (quote its number), and
      `grep -c 'changepoint_count(' crates/aprender-forecast/src/prophet.rs` is at least 2
    - `crates/aprender-forecast/src/forecast.rs` calls `changepoint_count` and its refusal message
      contains the literal `max_logistic_changepoint_lambda`:
      `grep -c 'max_logistic_changepoint_lambda' crates/aprender-forecast/src/forecast.rs` is at least 1
    - The refusal is positioned before the design build: in `forecast.rs`, the line number of the first
      `max_logistic_changepoint_lambda` occurrence is LESS than the line number of the `make_design(`
      call. State both line numbers in the SUMMARY
    - The three at-the-bound release measurements (freq D / W / MS at the tightest legal history span)
      are all under 2.000 s and are quoted in the SUMMARY with their `profile=release` marker
    - `prophet::parity` reports exactly 32 passed / 0 failed, and `git diff --stat HEAD` (an explicit
      revision, so a COMMITTED change cannot make the check pass vacuously) shows no change to
      `contracts/prophet-parity-v1.yaml`, `contracts/neuralprophet-parity-v1.yaml`,
      `contracts/chronos-bolt-parity-v1.yaml` or anything under `crates/aprender-forecast/tests/fixtures/`
  </acceptance_criteria>
  <done>The door's whole surface is enumerated by inspection and checked into the contract; the effective changepoint count has ONE implementation that both the door and `make_design` read; a logistic request whose lambda exceeds a measured, contract-owned bound is refused before `make_design`; the near-miss and the linear arm are both still accepted; no parity rung moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the class invariant CHECKED — two completeness tests, plus WR-01 and IN-03</name>
  <files>crates/aprender-forecast/src/test_support.rs, crates/aprender-forecast/src/types.rs, crates/aprender-forecast/src/prophet.rs, contracts/forecast-tool-boundary-v1.yaml</files>
  <read_first>
    - crates/aprender-forecast/src/test_support.rs — `contract_doc`, `contract_path`,
      `equation_tolerance`, `constant_u64`, and the module's own `#[cfg(test)] mod tests`
    - crates/aprender-forecast/src/types.rs — `cost_bounds_match_contract` and its doc comment
      (the one that says "four"), `bounds_match_contract`, `chronos_bounds_match_contract`, and
      `the_advertised_schema_is_strict_and_names_the_three_required_fields` (the existing
      `schemars::schema_for!` call site — reuse its shape)
    - crates/aprender-forecast/src/prophet.rs — `POISSON_NORMAL_BRANCH_LAMBDA` and its doc comment
    - contracts/forecast-tool-boundary-v1.yaml — the `door_surface:` block Task 1 just added, and
      `constants:`
  </read_first>
  <behavior>
    - `every_request_knob_is_enumerated` — RED if a field is added to `ForecastArgs` or `HolidayArg`
      without a `door_surface.knobs` entry (missing direction), and RED if `door_surface.knobs` names
      a field that no longer exists on either struct (phantom direction). Both directions, because a
      one-directional check lets the list rot in the other one
    - `every_cost_axis_names_a_real_bound` — RED if any `cost_axes[].bound` names a key absent from
      `constants:`; RED for the sentinel `unbounded_pending_06_15`; GREEN for
      `measured_at_structural_maximum` only when the entry also carries a `measured_seconds` strictly
      below 2.0
    - `cost_bounds_match_contract` — RED if any Rust cost constant, including the new f64 ones, differs
      from its contract key. A `poisson_normal_branch_lambda: 31` in the YAML alone turns it red
    - `pool_default_matches_contract` — RED if `DEFAULT_POOL` differs from `constants.pool_default`.
      Separate, because a router-pool size is not a per-request cost ceiling
  </behavior>
  <action>
    (1) TWO NEW READERS in `test_support.rs`. `pub(crate) fn contract_value(contract: &str) ->
    std::sync::Arc<serde_yaml::Value>` returning `contract_doc(contract)` so a test can walk arbitrary
    structure; and `pub(crate) fn constant_f64(contract: &str, key: &str) -> f64` reading
    `constants.<key>` as `as_f64()`, falling back to `as_u64()`/`as_i64()` cast, and panicking naming
    the key when absent — the same failure shape `constant_u64` has. Both get doc comments saying WHY
    they exist: `constant_u64` cannot express a threshold that is conceptually a real number, and the
    completeness tests need the parsed tree rather than one scalar.

    (2) WRITE THE TWO COMPLETENESS TESTS RED FIRST, in `types::tests`. Derive the field list from
    `schemars::schema_for!(ForecastArgs)` and `schemars::schema_for!(HolidayArg)` — read
    `schema["properties"]`'s object keys, converting through `serde_json::to_value` exactly as
    `the_advertised_schema_is_strict_and_names_the_three_required_fields` already does. Assert set
    equality against the `field`+`owner` pairs in `door_surface.knobs`, with an assertion message that
    prints the symmetric difference in BOTH directions so a failure names the field rather than the
    count. Observe RED before the block is complete (delete one `knobs:` entry, run, see it named,
    restore) — that observation is the evidence the test is load-bearing, per CLAUDE.md rule 7 and this
    phase's own D-15 practice. Then
    `every_cost_axis_names_a_real_bound`: for each `cost_axes[]`, its `bound` must be a key present in
    `constants:`, or `measured_at_structural_maximum` with a numeric `measured_seconds` under 2.0; the
    sentinel `unbounded_pending_06_15` must FAIL with a message naming the axis and the plan that owes
    it. Confirm the sentinel really is red at this point (C-07 and C-08 both carry it) and then mark
    the two tests `#[ignore]`-free but expected-red until 06-15 lands — no: do NOT ship a red suite.
    Instead, at this point in the round the two pending axes carry `bound: unbounded_pending_06_15`
    and `every_cost_axis_names_a_real_bound` is written and OBSERVED red against them, then the axes'
    `bound` is temporarily set to `measured_at_structural_maximum` with the HONEST measured seconds for
    each (Task 1 or this task measures them; if either exceeds 2.0 s the axis stays
    `unbounded_pending_06_15` and this plan's own suite is red, which is a STOP-and-report, not a value
    to fudge). 06-15 replaces those entries with real bound keys. Record in the SUMMARY exactly which
    form each pending axis shipped with and what its measured seconds were.

    (3) WR-01 — CONTRACT-OWN THE BRANCH THRESHOLD. Add `poisson_normal_branch_lambda: 30` to
    `constants:` with a comment saying it is the sampler's exact/approximate branch point and that the
    Rust constant is its mirror. Extend `cost_bounds_match_contract` with an f64 arm asserting
    `prophet::POISSON_NORMAL_BRANCH_LAMBDA == constant_f64("forecast-tool-boundary-v1",
    "poisson_normal_branch_lambda")`. Update the constant's doc comment in `prophet.rs` to say it is
    contract-owned and name the key. Prove the mirror by mutation: change the YAML value alone, observe
    the test red naming the key, restore, observe green — and record both observations. Note in the
    SUMMARY that this closes only the OWNERSHIP half of WR-01; the half about the sweep being unable to
    DETECT a lowered threshold is 06-17 Task 1.

    (4) IN-03 — FIX THE NARRATION AND SPLIT THE POOL. Reword `cost_bounds_match_contract`'s doc comment
    so it no longer states a count that drifts: describe what the table is (the contract-mirrored
    per-request cost ceilings) rather than how many rows it has. Move `DEFAULT_POOL` out of that table
    into its own `#[test] fn pool_default_matches_contract`, with a doc comment saying a router-pool
    size is a deployment default, not a per-request cost ceiling, and that it lives in this crate only
    because this crate owns the memoized contract reader.

    (5) EQUATIONS AND FALSIFY ENTRIES for the invariant itself. Add a `door_surface_is_complete`
    equation to `forecast-tool-boundary-v1.yaml` stating the two halves of the class invariant as a
    formula over the knob set and the axis set, with invariants recording WHY it exists (three rounds
    of point fixes, each finding the next instance of the same class) and its `preconditions`. Add the
    matching `proof_obligations` entry and the next unused `FALSIFY-BOUNDARY-0NN` id, whose `test:` is
    the two completeness test commands and whose `if_fails:` says the door surface has grown a knob or
    an axis that nothing enumerates, which is the state that produced CR-01. READ the file for the next
    unused id.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib types::tests -- --nocapture > /tmp/p06-14-t2a.log 2>&1; rc=$?; tail -25 /tmp/p06-14-t2a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit. A failure in `every_request_knob_is_enumerated` names the field on one side of the symmetric difference; a failure in `every_cost_axis_names_a_real_bound` names the axis and the bound key it claimed; a failure in `cost_bounds_match_contract` names the constant and the contract key that disagree</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib types::tests::every_request_knob_is_enumerated types::tests::every_cost_axis_names_a_real_bound types::tests::pool_default_matches_contract > /tmp/p06-14-t2b.log 2>&1; rc=$?; tail -8 /tmp/p06-14-t2b.log; test $rc -eq 0 && grep -q '3 passed' /tmp/p06-14-t2b.log</automated>
    <fails_when>non-zero exit, or the summary reports fewer than `3 passed` — one of the three test names does not exist, so the filter silently matched nothing and the run proves nothing (this is the `0 passed` trap: a green cargo exit with an empty filter)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- status contracts/forecast-tool-boundary-v1.yaml > /tmp/p06-14-t2c.log 2>&1; rc=$?; tail -20 /tmp/p06-14-t2c.log; test $rc -eq 0 && grep -q 'door_surface_is_complete' /tmp/p06-14-t2c.log</automated>
    <fails_when>non-zero exit, or `door_surface_is_complete` absent from the status output — the equation was added to the file but is not being read as an equation (wrong nesting level), so no obligation or FALSIFY entry is attached to it</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/test_support.rs` declares `constant_f64` and `contract_value`:
      `grep -n 'fn constant_f64' crates/aprender-forecast/src/test_support.rs` and
      `grep -n 'fn contract_value' crates/aprender-forecast/src/test_support.rs` each print exactly one
      line — quote both line numbers rather than a count
    - `crates/aprender-forecast/src/types.rs` declares the three test names
      `every_request_knob_is_enumerated`, `every_cost_axis_names_a_real_bound` and
      `pool_default_matches_contract`, and `DEFAULT_POOL` no longer appears inside
      `cost_bounds_match_contract`'s table
    - `contracts/forecast-tool-boundary-v1.yaml` `constants:` carries `poisson_normal_branch_lambda`
    - BOTH mutation controls are recorded in the SUMMARY with their observed output: (a) one
      `door_surface.knobs` entry deleted -> `every_request_knob_is_enumerated` red naming that field ->
      restored -> green; (b) `poisson_normal_branch_lambda` changed in the YAML alone ->
      `cost_bounds_match_contract` red naming the key -> restored -> green. A mirror test with no
      observed red side is exactly what WR-01 says is insufficient
    - The `door_surface_is_complete` equation, its proof obligation and its FALSIFY-BOUNDARY entry all
      exist, and `pv status` lists the equation
    - Every `cost_axes[]` entry has a `bound` that is either a real `constants:` key or
      `measured_at_structural_maximum` with `measured_seconds` under 2.0; the SUMMARY states which form
      C-07 and C-08 shipped with and what their measured seconds were
  </acceptance_criteria>
  <done>The class invariant is a pair of tests that go red on a new field, a phantom field, or an axis claiming a bound that does not exist — not a paragraph. `POISSON_NORMAL_BRANCH_LAMBDA` is contract-owned with an observed red side, and `cost_bounds_match_contract` no longer misdescribes itself or carries a non-cost constant.</done>
</task>

<task type="auto">
  <name>Task 3: The red side, through a live server, with the review's exact payload — and the contract equation that owns it</name>
  <files>crates/aprender-mcp-forecast/src/lib.rs, contracts/forecast-tool-boundary-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - crates/aprender-mcp-forecast/src/lib.rs `mod e2e` — `serve`, `synth`, `valid_args`, `with`,
      `refused`, `tool_output`, `assert_shared_shape`, and the whole 06-12 block
      `refuses_holiday_design_cost_over_bound` / `accepts_holiday_design_cost_just_under_bound` /
      `refuses_holiday_dates_total_over_bound` including their comment blocks — this is the template
    - crates/aprender-forecast/src/forecast.rs — the refusal Task 1 added, so the e2e needle matches
      the shipped message exactly
    - crates/aprender-forecast/src/types.rs — `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`, and how
      `aprender_forecast::types::MAX_HOLIDAY_DESIGN_COST` is referenced from the e2e crate
    - contracts/forecast-tool-boundary-v1.yaml — `holiday_design_cost_bounded` and
      `FALSIFY-BOUNDARY-018` in full; they are the shape this task's equation and entry must match
    - contracts/aprender/binding.yaml — the Phase 6 block header comment (it states a row count that
      must be updated) and two or three existing `forecast-tool-boundary-v1.yaml` rows
    - .planning/phases/06-native-time-series-forecasting-stack/06-12-SUMMARY.md — how the
      temporarily-raised-constant RED observation was actually run and recorded
  </read_first>
  <action>
    (1) THE STRADDLING PAIR, through a live streamable-HTTP router. Add
    `refuses_logistic_changepoint_lambda_over_bound` sending the review's measured payload verbatim in
    shape — 33 points at 1-day spacing, `horizon: 3650`, `freq: "MS"`, `growth: "logistic"`,
    `cap: 50` — and observing a validation-class refusal quoting `max_logistic_changepoint_lambda`. Add
    `accepts_logistic_changepoint_lambda_just_under_bound`, the same request with the ONE field changed
    that pulls lambda under the bound (prefer widening the history spacing, which is the review's own
    control axis, so the pair differs in the mechanism the bound is about and not in an unrelated
    dimension), asserting the full response shape via `assert_shared_shape` plus `components` and
    `diagnostics`. Both tests compute their own lambda from
    `aprender_forecast::prophet::changepoint_count` and `assert!` their geometry really is on the side
    they claim, exactly as the 06-12 pair does with `MAX_HOLIDAY_DESIGN_COST`. Record the payload's
    byte length in a comment: the point of CR-01 is that 1 132 bytes bought 2.334 s.

    (2) OBSERVE THE RED SIDE. With `constants.fit_max_logistic_changepoint_lambda` and
    `types::MAX_LOGISTIC_CHANGEPOINT_LAMBDA` temporarily raised past the structural maximum (~86 700),
    run the pair and observe the OVER case ACCEPTED — the server returns a forecast — then restore both
    and observe it refused. Record both runs in the SUMMARY. Note the wall time of the accepted run: it
    is the 2.33 s the review measured, and quoting it is what makes the bound's justification a
    measurement rather than a repeat of someone else's number.

    (3) THE CONTRACT EQUATION. Add `logistic_changepoint_cost_bounded` to
    `contracts/forecast-tool-boundary-v1.yaml`, in `holiday_design_cost_bounded`'s style: a `formula`
    over `changepoint_count(len(ds)) * (t_max - 1) <= fit_max_logistic_changepoint_lambda` with `t_max`
    spelled out in terms of `ds` and `future_days(freq, horizon)`; a `domain` naming untrusted
    unauthenticated JSON-RPC input; a `codomain` naming either a forecast or a refusal that states the
    observed lambda, both factors, the bound and the three fixes; and `invariants` recording (a) the
    Rust constant is asserted EQUAL to the contract key, (b) the check runs before `make_design` and
    could not have been a `FIT_BUDGET_SECS` setting because `predict` runs outside that budget
    entirely, (c) `fit_max_horizon` bounds the COUNT of future steps while `dates::future_days`
    multiplies it by 7 for `"W"` and ~30.4 for `"MS"`, which is why three isolated bounds left this
    product open, and (d) the pre-06-13 saturation near 745.13 was an ACCIDENTAL cap whose removal was
    correct and whose replacement is this bound, not a re-clamp. Add the matching `proof_obligations`
    entry and the next unused `FALSIFY-BOUNDARY-0NN` id listing every assertion that falsifies it
    across all three layers (constants mirror, library two-sided control, server two-sided pair) and
    stating that the server refusal was OBSERVED RED under a raised constant.

    (4) BINDING ROWS AND THE COUNT. Add binding rows for `logistic_changepoint_cost_bounded` and
    `door_surface_is_complete`, each resolving to a real definition site in the file its `module_path`
    maps to — `aprender_forecast::forecast` / `forecast` for the first, `aprender_forecast::types` for
    the second (point it at a function that exists in `types.rs`; `make contract-audit-phase6` resolves
    `function` against `fn <name>` in that file, so a `#[cfg(test)]` test function in `types.rs` is a
    valid target). Update the Phase 6 block header's stated row count to the new total. Run
    `make contract-audit-phase6` and require zero BIND- and zero RESOLVE- findings with all four
    `Total equations:` summaries present.

    (5) SEMVER. Run `pv diff` against the pre-change contract, materialising the old revision to a real
    file first — `git show HEAD:contracts/forecast-tool-boundary-v1.yaml > /tmp/ftb-old.yaml` then
    `pv diff /tmp/ftb-old.yaml contracts/forecast-tool-boundary-v1.yaml`. `pv diff` takes TWO FILESYSTEM
    PATHS and never a git revision; passing a revision reports a missing-file error that reads like a
    missing contract. Apply the suggested bump to `metadata.version` and record the before/after.
  </action>
  <verify>
    <!-- planner-discipline-allow: BIND- -->
    <!-- The audit-finding prefix is negative-grepped against a LOG FILE this command generates, never
         against a source file this plan writes, so naming it in the action cannot self-invalidate the
         gate. It has to be named: "zero audit findings" is not a checkable string. -->
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e:: > /tmp/p06-14-t3a.log 2>&1; rc=$?; tail -15 /tmp/p06-14-t3a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — either the new refusal did not fire through the transport (the bound is in the library but the server path bypasses it), or the near-miss was refused (the bound over-refuses), or an existing e2e case regressed because the new refusal fires earlier than an older one and stole its message</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib e2e::refuses_logistic_changepoint_lambda_over_bound e2e::accepts_logistic_changepoint_lambda_just_under_bound > /tmp/p06-14-t3b.log 2>&1; rc=$?; tail -8 /tmp/p06-14-t3b.log; test $rc -eq 0 && grep -q '2 passed' /tmp/p06-14-t3b.log</automated>
    <fails_when>non-zero exit, or the summary reports fewer than `2 passed` — one of the two names does not exist and the filter matched nothing, so a green cargo exit proves nothing about either side of the bound</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --test e2e_stdio > /tmp/p06-14-t3c.log 2>&1; rc=$?; tail -10 /tmp/p06-14-t3c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the spawned-binary stdio path regressed. This target is dark in CI, so a failure here is only visible if it is run deliberately</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-14-t3d.log 2>&1; rc=$?; tail -20 /tmp/p06-14-t3d.log; test $rc -eq 0 && test "$(grep -c 'Total equations:' /tmp/p06-14-t3d.log)" -ge 4 && test "$(grep -c 'BIND-' /tmp/p06-14-t3d.log)" -eq 0</automated>
    <fails_when>non-zero exit, or fewer than four `Total equations:` summaries — one of the four Phase 6 contracts was not audited, so its BIND- count is not evidence of anything. Any `BIND-` or `RESOLVE-` line in the log means a new equation has no binding row, or a row's `function` does not resolve to a definition site in the file its `module_path` names</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-14-t3e.log 2>&1; rc=$?; tail -20 /tmp/p06-14-t3e.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — any warning is an error here. Watch for `clippy::disallowed_methods` (`unwrap()` is banned by `.clippy.toml`; use `expect()` or `ok_or_else(|| …)?`) and for `clippy::cast_precision_loss` on the new `as f64` casts, which the workspace lint set may or may not allow for this crate</fails_when>
    <automated>cargo fmt --all -- --check > /tmp/p06-14-t3f.log 2>&1; rc=$?; tail -20 /tmp/p06-14-t3f.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the log names the file and the offending span</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-mcp-forecast/src/lib.rs` declares `refuses_logistic_changepoint_lambda_over_bound`
      and `accepts_logistic_changepoint_lambda_just_under_bound`, both `#[tokio::test]`, both driving
      `serve()`
    - The OVER case's needle is the literal `max_logistic_changepoint_lambda`, so the assertion is on
      the shipped refusal text rather than on "it errored"
    - Both tests carry an in-test `assert!` computing their own lambda from
      `aprender_forecast::prophet::changepoint_count` and proving their geometry is on the side the
      test name claims
    - The RED observation is recorded in the SUMMARY with both runs: the constant raised past ~86 700
      -> the OVER request ACCEPTED, with its measured wall time -> restored -> refused
    - `contracts/forecast-tool-boundary-v1.yaml` carries `equations.logistic_changepoint_cost_bounded`,
      a matching `proof_obligations` entry, and a new `FALSIFY-BOUNDARY-0NN` entry naming all three
      layers of assertion
    - `contracts/aprender/binding.yaml` carries rows whose `equation:` values are
      `logistic_changepoint_cost_bounded` and `door_surface_is_complete`, and the Phase 6 block header's
      stated row count matches the actual number of rows in that block
    - `pv diff` was run on two real filesystem paths (never a git revision) and its suggested semver
      bump was applied to `metadata.version`; the before/after versions are in the SUMMARY
  </acceptance_criteria>
  <done>CR-01 is closed at all three layers — constant mirror, library two-sided control, live-server two-sided pair — with the refusal OBSERVED red under a raised constant, and the contract owns the rule with an equation, an obligation, a FALSIFY entry and resolving binding rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> `/mcp` (K pooled routers, default K=8) | untrusted, unauthenticated JSON-RPC `tools/call` arguments reach the whole forecast door; `growth`, `freq`, `horizon` and the history spacing jointly select and size the logistic uncertainty simulation |
| MCP client -> stdio (`aprender-mcp-forecast`) | the same door behind a different transport; the transport re-checks nothing (D-11), so a bound missing from the door is missing from both surfaces |
| `door_surface:` block -> future maintainers | the enumeration is a claim about the code; if it is not machine-checked it becomes a stale document that licenses the next unbounded axis |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. The
genuine ASVS L1 surface is INPUT VALIDATION and RESOURCE EXHAUSTION.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-31 | Denial of Service | `prophet::predict`'s `Growth::Logistic` uncertainty arm, reached from `forecast::forecast` with no budget | **critical** | mitigate | A 1 132-byte unauthenticated request, inside every existing door bound, buys 2.334 s of CPU against a default pool of K=8 — measured, reproduced three times, over the 2 s SC1 bar. Eight such requests saturate the pool. Task 1 refuses at the door before `make_design`, on a contract-owned bound derived from three at-the-bound release measurements; Task 3 proves the refusal fires through the live transport and observes the red side |
| T-06-32 | Denial of Service | the door's un-enumerated surface as a whole | **high** | mitigate | Three consecutive rounds each closed one measured probe and the next adversarial pass found the next instance of the same class. The mitigation is structural, not another point fix: `door_surface:` enumerates every knob and every cost axis, and two tests make the enumeration go red on a new field, a phantom field, or an axis claiming a bound that does not exist. Two axes the enumeration found (C-07 holiday-name amplification, C-08 unbudgeted NP training) are routed to 06-15 and carry a measured-or-sentinel bound until then |
| T-06-33 | Tampering | `prophet::POISSON_NORMAL_BRANCH_LAMBDA` | medium | mitigate | The one behavioural constant with no contract mirror: it decides whether a request gets an exact Poisson or a normal approximation, and it can be moved in Rust alone. Task 2 contract-owns it and proves the mirror by mutating the YAML value and observing the test red. The complementary hole — that the current sweep cannot DETECT a lowered threshold — is 06-17 Task 1 |
| T-06-34 | Repudiation | the refusal message for an over-lambda request | low | accept | The refusal names the observed lambda, both factors, the bound key and three fixes, but it does not distinguish "your horizon is too long" from "your history is too short" — the caller must reason from the two factors. Accepted: naming both factors and their product is strictly more information than every neighbouring refusal in this door gives, and a decision tree in an error string is worse for clients to parse than the arithmetic |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan. Every symbol it uses (`schemars::schema_for!`, `serde_yaml`, `prophet::Rng`, the pmcp test client) is already a dependency of these crates. No new dependency enters the graph, so `RESEARCH.md`'s Package Legitimacy Audit is not engaged. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib` — 0 failed, and `prophet::parity` exactly 32 passed / 0 failed
- `cargo test -p aprender-forecast --lib types::tests` — the two completeness tests and the split pool
  assertion all present and green; both mutation controls observed and recorded
- `cargo test -p aprender-mcp-forecast --lib e2e::` — 0 failed, including the new straddling pair
- `cargo test -p aprender-mcp-forecast --test e2e_stdio` — 0 failed (dark in CI; run deliberately)
- `pv validate contracts/forecast-tool-boundary-v1.yaml` — `0 error(s)`, with the new top-level
  `door_surface:` key present
- `pv status contracts/forecast-tool-boundary-v1.yaml` — lists `logistic_changepoint_cost_bounded` and
  `door_surface_is_complete`
- `make contract-audit-phase6` — rc=0, zero BIND-, zero RESOLVE-, four `Total equations:` summaries
- `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings`
  and `cargo fmt --all -- --check` — rc=0
- Three at-the-bound release walls (freq D / W / MS at the tightest legal history span), all under 2 s,
  each printed with a `profile=release` marker
</verification>

<success_criteria>
- The door's caller-settable surface and its cost surface are BOTH enumerated by inspection, checked
  into `contracts/forecast-tool-boundary-v1.yaml`, and machine-verified in two directions
- CR-01 is closed as an INSTANCE of that enumeration, not as a standalone patch: the bound's existence
  is derivable from the `door_surface` entry for C-06
- Every new bound has an observed red side, and both mutation controls (knob deletion, contract-value
  change) are recorded with their output
- No parity fixture, parity tolerance or `*-parity-v1.yaml` numeric was touched, and `prophet::parity`
  is 32/0 before and after
- The two axes this plan does not close (C-07, C-08) are visible in the contract with an honest
  measured-or-sentinel bound and are routed to 06-15 by name — not omitted
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-14)

**New Rust constants** (`crates/aprender-forecast/src/types.rs`):
`MAX_LOGISTIC_CHANGEPOINT_LAMBDA` (`f64`).

**New Rust functions**:
`aprender_forecast::prophet::changepoint_count` (extracted from `make_design`);
`crates/aprender-forecast/src/test_support.rs` `constant_f64` and `contract_value` (both
`pub(crate)`).

**New test names**:
`prophet` — `changepoint_count_equals_the_design_it_describes`;
`types::tests` — `every_request_knob_is_enumerated`, `every_cost_axis_names_a_real_bound`,
`pool_default_matches_contract`;
`forecast::tests` — `a_logistic_request_over_the_changepoint_lambda_bound_is_refused`,
`a_logistic_request_just_under_the_changepoint_lambda_bound_is_accepted`, plus the linear-arm control
case;
`aprender-mcp-forecast` `mod e2e` — `refuses_logistic_changepoint_lambda_over_bound`,
`accepts_logistic_changepoint_lambda_just_under_bound`.

**New contract keys** (`contracts/forecast-tool-boundary-v1.yaml`):
top-level `door_surface:` with `knobs:` and `cost_axes:`;
`constants.fit_max_logistic_changepoint_lambda`; `constants.poisson_normal_branch_lambda`;
`equations.logistic_changepoint_cost_bounded`; `equations.door_surface_is_complete`; two new
`proof_obligations` entries; two new `falsification_tests` ids in the `FALSIFY-BOUNDARY-0NN` series
(the next unused ones — the highest at HEAD is 018).

**New sentinel value** used inside `door_surface.cost_axes[].bound`: `unbounded_pending_06_15` (must
FAIL `every_cost_axis_names_a_real_bound`) and the accepted alternative form
`measured_at_structural_maximum` + `measured_seconds`.

**New binding keys** (`contracts/aprender/binding.yaml`): the rows whose `equation:` values are
`logistic_changepoint_cost_bounded` and `door_surface_is_complete`; the Phase 6 block header's stated
row count is updated.

**New log token**: `profile=release` on the at-the-bound wall lines this task measures (the marker
CLAUDE.md rule 2 requires; the permanent `SC1 WALL:` token is 06-16's).

**Symbols later round-3 plans create** (not pre-existing, do not drift-check against HEAD): 06-15 —
`MAX_HOLIDAY_NAME_LEN`, `constants.fit_max_holiday_name_len`, the NP-training cost bound and its key,
`holiday_name_cost_bounded`, `np_train_cost_bounded`; 06-16 —
`crates/aprender-forecast/src/sc1_wall.rs`, `just forecast-sc1-sweep`, the `SC1 WALL:` token, the
shared numeric-shape validator script; 06-17 — the `variance_tolerance` key on
`poisson_sampler_domain` and `FALSIFY-PROPHET-014`.

**Symbols earlier gap-closure plans created** (present at HEAD, safe to drift-check): 06-10 —
`cap_is_logistic_only`, `FALSIFY-BOUNDARY-017`, `checked_cap`, `COVERAGE.md`; 06-11 —
`MAX_HOLIDAY_DESIGN_COST`, `MAX_HOLIDAY_DATES_TOTAL`, `holiday_day_sets`, `HOLIDAY DESIGN WALL:`,
`forecast-holiday-bench`; 06-12 — `holiday_design_cost_bounded`, `FALSIFY-BOUNDARY-018`,
`refuses_holiday_design_cost_over_bound`, `accepts_holiday_design_cost_just_under_bound`,
`HOLIDAY DESIGN OK`; 06-13 — `poisson`, `POISSON_NORMAL_BRANCH_LAMBDA`, `poisson_sampler_domain`,
`FALSIFY-PROPHET-013`, `logistic_band_wall`, `POISSON MEAN:`, `LOGISTIC BAND WALL:`.

Everything else named in this plan (`forecast`, `make_design`, `predict`, `Spec`, `Growth`,
`future_days`, `parse_date`, `FIT_BUDGET_SECS`, `MAX_ITERS_PER_ROUND`, `constant_u64`,
`equation_tolerance`, `serve`, `synth`, `refused`, `assert_shared_shape`, `contract-audit-phase6`,
`pv validate`, `pv status`, `pv diff`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md` when done. Record:
the re-derived enumeration and every delta from this plan's table (including any axis found that this
plan does not route to 06-15 — that is a STOP-and-report, and the SUMMARY must say whether it
happened); the three at-the-bound release walls with their `profile=release` markers and the value
they justify; the line numbers proving the refusal precedes `make_design`; both mutation controls
(knob deletion; `poisson_normal_branch_lambda` changed in the YAML alone) with their observed output;
the RED observation through the live server, with the accepted request's measured wall; the
`prophet::parity` count before and after; the form C-07 and C-08 shipped with in `cost_axes` and their
measured seconds; `pv diff`'s suggested semver bump and the contract version before/after; and the
Review Dispositions Ledger with this plan's three findings marked closed.
</output>
