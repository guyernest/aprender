---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-core/tests/monorepo_invariants.rs
  - crates/aprender-mcp-setfit-lambda/README.md
  - crates/aprender-contrastive-data/README.md
  - crates/aprender-mcp-setfit/README.md
autonomous: false
requirements: [SC5]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A human decided the FALSIFY-MONO-011 [[bin]] allowlist treatment for thin MCP deployment units BEFORE any executor edited crates/aprender-core/tests/monorepo_invariants.rs (RESEARCH Open Question 1; D-06 adds two [[bin]] crates)"
    - "After the decision is applied, `cargo test -p aprender-core --test monorepo_invariants` passes with the four pre-existing SetFit deployment crates and the two Phase 6 servers accounted for by name, and the migration-debt ratchet still rejects growth of the debt register"
    - "The two crates readme_contract reports as having no README (aprender-mcp-setfit-lambda, aprender-contrastive-data) have one containing the paiml/aprender link; aprender-mcp-setfit's README gains the link (FALSIFY-README-CRATE-001/002 turn green; the two COUNT failures stay red until plan 06-09 re-derives them)"
    - "SC5 'every gate is green' is reachable: nothing in this plan adds to the five drift failures measured on this branch, and three of them are closed here"
  artifacts:
    - path: "crates/aprender-core/tests/monorepo_invariants.rs"
      provides: "the decided allowlist treatment for deployment-unit binaries, with the policy sentence updated to match"
      contains: "aprender-mcp-forecast"
    - path: "crates/aprender-mcp-setfit-lambda/README.md"
      provides: "crate README with the monorepo link"
      contains: "paiml/aprender"
    - path: "crates/aprender-contrastive-data/README.md"
      provides: "crate README with the monorepo link"
      contains: "paiml/aprender"
    - path: "crates/aprender-mcp-setfit/README.md"
      provides: "existing README now linking the monorepo"
      contains: "paiml/aprender"
  key_links:
    - from: "crates/aprender-core/tests/monorepo_invariants.rs"
      to: "crates/aprender-mcp-forecast/Cargo.toml"
      via: "the allowlist names the package whose [[bin]] cargo metadata reports"
      pattern: "aprender-mcp-forecast"
    - from: "crates/aprender-core/tests/readme_contract.rs"
      to: "crates/aprender-mcp-setfit-lambda/README.md"
      via: "test at :245 requires README.md beside every crates/*/Cargo.toml; test at :275 requires the substring paiml/aprender"
      pattern: "paiml/aprender"
  prohibitions:
    - statement: "No executor edits ALLOWLIST_BASELINE or the allowlist on its own judgement — the ratchet is a policy sentence, not a constant (RESEARCH Pitfall 2)"
      status: resolved
      verification: judgment
    - statement: "Do not delete a [[bin]] to satisfy the ratchet — removing a binary before its capability is reachable elsewhere removes the capability (the test's own comment)"
      status: resolved
      verification: judgment
    - statement: "Do not hardcode a crate or contract count in any README edit — counts are re-derived in plan 06-09 (CLAUDE.md 'Derive the numbers')"
      status: resolved
      verification: judgment
---

<objective>
Make CI's two drift gates reachable for Phase 6. `monorepo_invariants::test_no_unauthorized_binaries`
(FALSIFY-MONO-011) is a shrink-only ratchet at 27 entries that ALREADY fails on four SetFit deployment
crates and will fail on `aprender-mcp-forecast` and `aprender-mcp-chronos` too; the treatment is a
policy decision only a human may make. `readme_contract` fails on two missing crate READMEs and one
README without the monorepo link — those are closed here; the two COUNT rows are closed in 06-09 once
every crate and contract exists.

Purpose: nothing else in the phase can turn `monorepo_invariants` green, and SC5 requires every gate
green. Surfacing the decision first means no executor invents one (RESEARCH Open Question 1).
Output: the decided allowlist edit, three README fixes, and a recorded rationale.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F1 gates, F7, Pitfall 2, Pitfall 5, Open Question 1)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: DECISION — FALSIFY-MONO-011 [[bin]] allowlist treatment for thin MCP deployment units</name>
  <decision>How FALSIFY-MONO-011 (`crates/aprender-core/tests/monorepo_invariants.rs:235-395`) treats thin MCP deployment-unit binaries — the four SetFit crates already failing plus the two Phase 6 servers</decision>
  <context>
    The allowlist is documented as a MIGRATION DEBT REGISTER ("every entry is a capability reachable only by its own binary and not yet through `apr <subcommand>`") with `ALLOWLIST_BASELINE = 27` asserted shrink-only and a stale-entry check. Measured on this branch (RESEARCH F7): it fails today on `aprender-mcp-setfit`, `aprender-mcp-setfit-lambda`, `aprender-mcp-setfit-train`, `aprender-setfit-train-lambda`. Phase 6 adds `aprender-mcp-forecast` and `aprender-mcp-chronos` (D-06, publish = false, `[[bin]]` each). An `apr forecast` subcommand is explicitly deferred (CONTEXT), so "migrate to an apr subcommand" is not available to Phase 6. These binaries are network-protocol deployment units (pmcp.run / Lambda), not CLI capabilities awaiting migration — the ratchet's category does not describe them. Undoing any option later is a one-line test edit, but the POLICY sentence it encodes is what the human owns.
  </context>
  <options>
    <option id="deployment-unit-class">
      <name>(recommended) A second, separately-ratcheted category</name>
      <pros>Keeps `allowed_bins` + `ALLOWLIST_BASELINE = 27` meaning exactly "migration debt"; adds `DEPLOYMENT_UNIT_BINS` (six names, each with a one-line comment: template / Lambda wrapper / training server / its Lambda / forecast server / chronos server) with its own `DEPLOYMENT_UNIT_BASELINE = 6` shrink-only assertion, its own stale-entry check, and a policy sentence: "publish = false thin MCP servers whose capability IS a protocol surface; adding one requires a CONTEXT decision, never a same-PR edit". Turns the gate green without redefining debt.</pros>
      <cons>Slightly more test code (a second set + two asserts); two baselines to explain.</cons>
    </option>
    <option id="single-list-33">
      <name>Fold the six into the existing allowlist, baseline 27 -> 33</name>
      <pros>Smallest diff (RESEARCH's recommendation); one list.</pros>
      <cons>Blurs "migration debt" with "deployment unit" — the ratchet's policy sentence becomes untrue for six entries unless rewritten; the shrink-only assertion now guards a mixed set.</cons>
    </option>
    <option id="phase6-only-29">
      <name>Add only the two Phase 6 servers (baseline 29); leave the SetFit four for their own ticket</name>
      <pros>Phase 6 touches only what it adds.</pros>
      <cons>`monorepo_invariants` stays RED in CI (the SetFit four still fail), so SC5 "every gate is green" is unreachable in this phase; the debt is real either way.</cons>
    </option>
    <option id="halt">
      <name>HALT planning of the two server crates pending an `apr forecast` design</name>
      <pros>Honors the ratchet literally.</pros>
      <cons>Contradicts D-06 (three crates, SetFit trio pattern) and the CONTEXT deferral of `apr forecast`; blocks SC1/SC4/SC5.</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: deployment-unit-class, single-list-33, phase6-only-29, halt — optionally with wording changes for the policy sentence</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply the decided allowlist treatment and prove the gate's ratchet still bites</name>
  <reversibility rating="costly">The edit is one test file, but it encodes repo policy on which crates may ship binaries; reversing it later means re-litigating six crates' status, not a refactor.</reversibility>
  <files>crates/aprender-core/tests/monorepo_invariants.rs</files>
  <read_first>
    - crates/aprender-core/tests/monorepo_invariants.rs (lines 230-395 in full: the comment block, `allowed_bins`, the `cargo metadata`-derived `with_bins`, the violations assert, `ALLOWLIST_BASELINE`, the stale-entry check)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F7 item 1 — the verbatim failure text; Open Question 1)
    - The human's answer to Task 1 (recorded in this plan's SUMMARY before any edit)
  </read_first>
  <action>
    Implement exactly the chosen option; do not improve on it.
    - `deployment-unit-class`: add `let deployment_unit_bins: HashSet<&str> = [ ... six names, one comment each ... ].into();`
      immediately after `allowed_bins`, a policy comment block modelled on the existing one but stating
      the deployment-unit rule, `const DEPLOYMENT_UNIT_BASELINE: usize = 6;` with a shrink-only assert
      whose message names the CONTEXT-decision requirement, extend `violations` to exclude names in
      EITHER set, and extend the stale check so a deployment unit that stops shipping a `[[bin]]` (and
      whose `crates/<name>` dir exists) is reported stale. Leave `allowed_bins` and `ALLOWLIST_BASELINE = 27`
      byte-identical.
    - `single-list-33`: append the six names in a new commented block inside `allowed_bins`, set
      `ALLOWLIST_BASELINE = 33`, and rewrite the ratchet comment so the policy sentence is true for the
      mixed set (say explicitly which six are deployment units and that they are not migration debt).
    - `phase6-only-29`: append only `aprender-mcp-forecast` and `aprender-mcp-chronos`, baseline 29, and
      record in the SUMMARY that `monorepo_invariants` remains RED on the SetFit four (SC5 blocked by
      out-of-phase debt).
    - `halt`: make no edit; return `## CHECKPOINT REACHED` to the orchestrator with the decision recorded.
    Whichever option: the stale-entry check must not fire for `aprender-mcp-chronos` before plan 06-07
    creates that directory (the check is `!with_bins.contains(c) && crates_dir.join(c).exists()` — a
    missing directory is not stale; verify by reading, not assuming). Then prove the ratchet still bites
    with a two-sided control: temporarily add a seventh bogus name to the new/extended set, run the test,
    observe the shrink-only assertion fail with the baseline message, revert. Record both observations.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --test monorepo_invariants > /tmp/p06-02-t2.log 2>&1; rc=$?; tail -8 /tmp/p06-02-t2.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-02-t2.log > /dev/null</automated>
    <fails_when>non-zero exit (FALSIFY-MONO-011 or any sibling invariant still failing), or the summary lacks "N passed; 0 failed" with N >= 1</fails_when>
  </verify>
  <done>monorepo_invariants passes in this worktree; the chosen option is implemented verbatim; the control mutation was observed RED and reverted; if `phase6-only-29` or `halt` was chosen the SUMMARY states plainly that SC5 is blocked.</done>
  <acceptance_criteria>
    - `crates/aprender-core/tests/monorepo_invariants.rs` contains `aprender-mcp-forecast` and `aprender-mcp-chronos` (except under `halt`)
    - Under `deployment-unit-class`: the file contains `DEPLOYMENT_UNIT_BASELINE` and `const ALLOWLIST_BASELINE: usize = 27;` unchanged
    - Under `single-list-33`: the file contains `const ALLOWLIST_BASELINE: usize = 33;`
    - The SUMMARY records the control run: the bogus seventh entry produced a failure whose message contains `shrink-only`, then the revert produced `0 failed`
    - `git diff --stat` for this task lists exactly one file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Close the three README drift failures Phase 6 can close now (CRATE-001 missing READMEs, CRATE-002 missing link)</name>
  <files>crates/aprender-mcp-setfit-lambda/README.md, crates/aprender-contrastive-data/README.md, crates/aprender-mcp-setfit/README.md</files>
  <read_first>
    - crates/aprender-core/tests/readme_contract.rs (lines 240-290: which crates need a README and the exact substring check `paiml/aprender`; lines 120-180: the two COUNT rows this task must NOT touch)
    - crates/aprender-mcp/README.md (line 3 — the link sentence to mirror)
    - crates/aprender-mcp-setfit/README.md (lines 1-30 — add the link without changing the run instructions)
    - crates/aprender-mcp-setfit-lambda/Cargo.toml and src/lib.rs (lines 1-60: what the crate does — `bootstrap` bin, `EMBEDDED_MODEL`, `resolve_model`, `APRENDER_SETFIT_MODEL`) and build.rs
    - crates/aprender-contrastive-data/Cargo.toml (description at line 6) and src/lib.rs (crate docs: the D-04 dependency allowlist / no fs-net-path rule from Phase 2)
  </read_first>
  <action>
    Write `crates/aprender-mcp-setfit-lambda/README.md`: title, one paragraph (the AWS Lambda custom-runtime
    wrapper of `aprender-mcp-setfit`: `bootstrap` binary, stateless streamable-HTTP loopback, model embedded
    at build via `APRENDER_SETFIT_MODEL` or read at runtime), a "Build" section naming the env var, and the
    sentence `Part of the [aprender](https://github.com/paiml/aprender) monorepo.` Write
    `crates/aprender-contrastive-data/README.md`: title, the Cargo description sentence, the Phase 2 boundary
    rule (no fs / net / path symbols; dependency allowlist enforced by `make contrastive-data-boundary`), the
    `apr data select` / `apr data pairs` entry points, and the same monorepo sentence. Edit
    `crates/aprender-mcp-setfit/README.md`: add the monorepo sentence after the opening paragraph (keep the
    existing `paiml/rust-mcp-sdk` link). No counts, no version numbers, no paths that do not exist.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --test readme_contract --no-fail-fast > /tmp/p06-02-t3.log 2>&1; rc=$?; tail -12 /tmp/p06-02-t3.log; test "$(grep -c '^test .* \.\.\. ' /tmp/p06-02-t3.log)" -ge 10 && ! grep -q 'FALSIFY-README-CRATE-00[12]' /tmp/p06-02-t3.log</automated>
    <fails_when>fewer than 10 test lines ran (the target did not execute), or the output still names a crate-README failure (missing README or missing link). The two COUNT rows are expected to still fail here — rc is deliberately not asserted; plan 06-09 asserts rc = 0</fails_when>
  </verify>
  <done>Three READMEs exist/contain the link; the two crate-README checks no longer appear in the failure list; the count checks remain the only readme_contract failures.</done>
  <acceptance_criteria>
    - `test -f crates/aprender-mcp-setfit-lambda/README.md && test -f crates/aprender-contrastive-data/README.md`
    - `grep -c 'paiml/aprender' crates/aprender-mcp-setfit-lambda/README.md crates/aprender-contrastive-data/README.md crates/aprender-mcp-setfit/README.md` is >= 1 for each of the three files
    - `/tmp/p06-02-t3.log` contains `FALSIFY-README-005` and `FALSIFY-README-007` (the count rows, still red — proof the run reached them) and nothing matching the crate-README ids
    - `git diff --stat` for this task lists exactly the three README files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo policy -> CI gate | a test constant encodes which crates may ship user-facing binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Elevation of Privilege | `test_no_unauthorized_binaries` allowlist | medium | mitigate | edit only after a `gate="blocking-human"` decision; two-sided control proves the ratchet still fails on growth; stale-entry check retained |
| T-06-09 | Repudiation | README claims | low | mitigate | no counts or measurements written here; every path cited exists (readme_contract FALSIFY-DOCS-CLAUDE-001 style) |
| T-06-SC | Tampering | cargo installs | high | accept | this plan installs nothing and adds no dependency |
</threat_model>

<verification>
- `cargo test -p aprender-core --test monorepo_invariants` exits 0 (unless `phase6-only-29`/`halt` was chosen — then recorded RED)
- `cargo test -p aprender-core --test readme_contract --no-fail-fast` shows only the two COUNT failures (FALSIFY-README-005 / -007)
- SUMMARY records the human's choice verbatim and the control mutation observation
</verification>

<success_criteria>
- The allowlist treatment is a recorded human decision, implemented exactly, with the ratchet proven to still bite
- Three of the five measured drift failures are closed; the remaining two are the count rows owned by 06-09
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-02)
New files: `crates/aprender-mcp-setfit-lambda/README.md`, `crates/aprender-contrastive-data/README.md`.
New symbols (option-dependent, in `crates/aprender-core/tests/monorepo_invariants.rs`): `deployment_unit_bins`, `DEPLOYMENT_UNIT_BASELINE` (option deployment-unit-class); or the six added `allowed_bins` names with `ALLOWLIST_BASELINE = 33` (single-list-33); or two names with baseline 29 (phase6-only-29).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-02-SUMMARY.md` when done. Record the human's decision verbatim, the diff applied, the two-sided control observation (bogus entry RED with the `shrink-only` message; revert GREEN), and which readme_contract rows remain red (expected: the two counts).
</output>
