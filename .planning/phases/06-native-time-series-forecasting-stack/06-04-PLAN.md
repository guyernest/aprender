---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - contracts/neuralprophet-parity-v1.yaml
  - crates/aprender-forecast/src/np.rs
  - crates/aprender-forecast/src/forecast.rs
  - crates/aprender-forecast/src/lib.rs
autonomous: true
requirements: [SC3]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`model: neuralprophet` dispatches to the ported np.rs (D-02, D-10): Huber from graph ops with a constant mask, clear_graph after every step, lr selected by TRAIN loss from {0.01, 0.03, 0.1} lag-free / {0.03, 0.1} with lags, auto batch/epochs, Fourier on days since 1900-01-01 — the port and the arm moved here from 06-01 Task 3 by REVIEW-06-06 so the tracer stays one honest end-to-end slice"
    - "The tracer stub sentence `ported in plan 06-04` no longer appears in crates/aprender-forecast/src/forecast.rs"
    - "NeuralProphet-lite data preparation matches the committed NeuralProphet 0.9.0 oracle (np_oracle_peyton.json): soft-normalisation shift/scale, t0/span, the 11-segment changepoints_t, and the auto batch/epochs formulas (SC3, D-10)"
    - "The lag-free model reaches 365-day-ahead holdout MAE <= 0.47 on Peyton with lr selected by TRAIN loss from {0.01, 0.03, 0.1} (SC3; Python NP 0.4611; Rust measured 0.4510)"
    - "With n_lags = 30 the AR-Net's one-step test MAE beats the naive one-step MAE recorded in the oracle (naive_1step_mae_test 0.3461; Rust measured 0.2509) (SC3)"
    - "weighted_huber is graph-connected: after backward() every parameter has Some(grad) with a non-zero entry; the tape is empty after train() (D-10)"
    - "Every bar is read from contracts/neuralprophet-parity-v1.yaml at test time (D-15); the contract validates with 0 errors and non-zero counts"
    - "The NeuralProphet-specific door refusals (freq other than D, n_lags > 365, n_lags >= the series span) are pmcp-visible validation errors with fix-naming messages (D-11)"
  artifacts:
    - path: "contracts/neuralprophet-parity-v1.yaml"
      provides: "frozen NP tolerances: data-prep 1e-9, lag-free MAE 0.47, AR beats naive, connectivity + training-rule invariants"
      contains: "lag_free_holdout_mae"
    - path: "crates/aprender-forecast/src/np.rs"
      provides: "the verbatim NeuralProphet-lite port (NpData, NpModel, weighted_huber, one_cycle_lr, auto_batch, auto_epochs, TrainConfig, TrainLog, train, predict_ts, predict_trend, predict_ar_1step, predict_ar_recursive) + #[cfg(test)] mod parity: data prep vs oracle, lag-free MAE, AR-Net vs naive, Huber connectivity, mini-batch + tape invariants"
      contains: "pub fn weighted_huber"
    - path: "crates/aprender-forecast/src/forecast.rs"
      provides: "the verbatim `\"neuralprophet\"` arm replacing 06-01's refusing stub, its dispatch smoke test, and unit tests for the three NP-specific refusals"
      contains: "supports freq D only"
  key_links:
    - from: "crates/aprender-forecast/src/np.rs"
      to: "contracts/neuralprophet-parity-v1.yaml"
      via: "test_support::equation_tolerance"
      pattern: "equation_tolerance\\(\"neuralprophet-parity-v1\""
    - from: "crates/aprender-forecast/src/np.rs"
      to: "crates/aprender-forecast/tests/fixtures/np_oracle_peyton.json"
      via: "test_support::load_json(\"np_oracle_peyton.json\") — keys trend_seasonality.{data_params, changepoints_t, batch, epochs, mae_365ahead_on_test_rows, n_test_rows}, ar30_hidden32.mae_1step_test, naive_1step_mae_test"
      pattern: "np_oracle_peyton"
  prohibitions:
    - statement: "Never core nn::loss::SmoothL1Loss — detached from the graph (D-10); the phase files a core ticket in 06-09 instead of fixing it here (RESEARCH Open Question 4)"
      status: resolved
      verification: test
    - statement: "Never full-batch training (MAE collapses to 2.6); never select lr or epochs by test error"
      status: resolved
      verification: test
    - statement: "NeuralProphet quantile regression and n_forecasts > 1 are NOT in scope (CONTEXT deferred) — the band stays residual-sd based and the diagnostics say so"
      status: resolved
      verification: judgment
    - statement: "Do not rewrite np.rs or the neuralprophet arm idiomatically while porting — port them verbatim from sources/004 (D-08); every deviation re-opens the SC3 bars this same plan then has to meet"
      status: resolved
      verification: judgment
    - statement: "Do not run this plan concurrently with 06-03 (the other Wave 2 plan): they share one Cargo target directory, one build lock and one 55 GiB-free disk (REVIEW-06-05). 06-05 can no longer overlap it at all — it is Wave 3 on a declared 06-04 dependency because both edit crates/aprender-forecast/src/lib.rs"
      status: resolved
      verification: test
---

<objective>
Land NeuralProphet-lite end to end: port `np.rs` verbatim and replace 06-01's refusing `"neuralprophet"`
stub with the spike's real arm (Task 1 — moved here from 06-01 by REVIEW-06-06), then prove the port to its
oracle (SC3) as CI-reachable `--lib` tests with the bars frozen in `contracts/neuralprophet-parity-v1.yaml`
(D-15), pinning the D-10 training rules (graph-connected Huber, mini-batches, train-loss lr selection, tape
hygiene) as invariant tests so a future "optimisation" cannot silently break them.

Purpose: NP-lite is the best point forecaster in the spike-006 benchmark (mean MASE 1.029) precisely
because of those rules; the tests make each rule falsifiable. Keeping the port in the same plan as its
oracle means NeuralProphet is implemented once, against its evidence, instead of twice — first as a
tracer smoke path and then as a parity path (codex's argument for the cut).
Output: the np.rs port, the restored arm, one contract, ~7 parity/invariant tests in np.rs, 1 dispatch
smoke test and 3 refusal unit tests in forecast.rs.

**EXECUTE SEQUENTIALLY within Wave 2.** This wave holds 06-03 and 06-04. They have zero `files_modified`
overlap but share one Cargo target directory, one `target/.package-cache` build lock and one disk that
`df -h .` reports at 94 % (55 GiB free) after three prior ENOSPC halts. Run them ONE AT A TIME. Task 1
carries a precondition that halts if another cargo build is already running (REVIEW-06-05 — both reviewers
independently; this is an instruction, not a note). 06-05 now DEPENDS on this plan and runs after it in
Wave 3: Task 1 adds `pub mod np;` to `crates/aprender-forecast/src/lib.rs` and 06-05 appends its three
module lines to the same block.
</objective>

<review_dispositions>
## Cross-AI review dispositions carried by this plan (06-REVIEWS.md, `plans_commit: 88da44dcd`)

**REVIEW-06-06 — INCORPORATED (structural, the replan's one sanctioned scope change).** The `np.rs` port,
the verbatim `"neuralprophet"` arm and the dispatch smoke test moved here from 06-01 Task 3; the three NP
refusal tests join them in Task 1 because they assert that arm's own messages. 06-01 keeps a refusing stub
naming this plan. Graph consequences: this plan stays Wave 2 on `depends_on: [06-01]`; 06-05 gains
`06-04` in `depends_on` and moves to Wave 3 (both plans now edit `crates/aprender-forecast/src/lib.rs`, and
a shared file is an implicit dependency); 06-06 gains `06-04` too, because its
`neuralprophet_lag_free_happy_path` e2e needs the real arm, and cascades to Wave 4 behind 06-05.

**REVIEW-06-05 — INCORPORATED, two ways.** The 06-04/06-05 file conflict this replan created (both append
to `crates/aprender-forecast/src/lib.rs`) is now a declared `depends_on` edge — 06-05 moved to Wave 3 — so
the executor cannot run them together. For the remaining wave-2 pair (06-03, 06-04), which has no file
overlap but shares a target directory and a 94 %-full disk, Task 1's precondition makes serialisation
machine-checkable rather than advisory.
</review_dispositions>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F3 SmoothL1Loss quote, F4, Pitfall 3)
@.claude/skills/spike-findings-aprender/references/neuralprophet-autograd.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md  (the tracer: the forecast door and its refusing `neuralprophet` stub — Task 1; test_support helpers, the fixtures and the profile decision — Task 2)

<interfaces>
<!-- Core API the np.rs port calls (moved here with the port; every name verified present, RESEARCH F3) -->
From crates/aprender-core/src/autograd/: no_grad, clear_graph, graph_tape_len, Tensor::{new, from_vec, zeros, requires_grad, shape, numel, data, grad, backward}; ops add/sub/mul/mul_scalar/pow/abs/mean/relu/matmul/transpose/broadcast_add/view
From crates/aprender-core/src/nn/: Linear::{new, without_bias, set_bias}, Module::forward; nn::optim::{AdamW::new(params, lr).weight_decay(wd), Optimizer::{step, zero_grad}}
<!-- Frozen tolerances (spike-002 RUN-OUTPUT measured values in parentheses) -->
equations.<name>.float_tolerance:
  data_prep_vs_oracle_abs        1.0e-9   (shift 5.262690, scale 4.582609, t0 2007-12-10, span 2596 d, 11 changepoints_t — matched NP to 1e-9 / 1e-16)
  lag_free_holdout_mae           0.47     (SC3 literal; Python NP 0.4611; Rust 0.4510, seeds 0.4508–0.4549)
  ar_net_vs_naive_margin         0.0      (MAE(n_lags=30, hidden [32]) < oracle.naive_1step_mae_test; measured 0.2509 vs 0.3461)
  huber_grad_min_abs             0.0      (at least one gradient entry strictly non-zero)
constants: lr_sweep_lag_free "0.01,0.03,0.1", lr_sweep_with_lags "0.03,0.1", auto_epochs_cap_with_lags 320, min_rows_for_mini_batch 64
Oracle keys: trend_seasonality{lr, epochs 80, batch 64, secs, n_train_rows_after_impute, data_params{...shift/scale...}, seasonality, changepoints_t, final_train_loss, final_train_mae, mae_365ahead_on_test_rows, n_test_rows, yhat_by_ds}, ar30_linear{... mae_1step_test}, ar30_hidden32{... mae_1step_test}, naive_1step_mae_test, mean_forecast_mae_test
Split: the oracle trains on the first n_train rows such that the last n_test_rows (365 daily) are held out — read n_train from `n_train_rows_after_impute` semantics as spike-002 main.rs does (lines 60-75), do not guess
</interfaces>
</context>

<tasks>

<!-- planner-region-allow: ported in plan 06-04 -->
<!-- 06-01 Task 1 deliberately writes the stub sentence; this task (a later plan, declared dependency) removes it. The negative grep below is the intended post-Task-1 state, not a sibling conflict. -->
<!-- planner-region-allow: SmoothL1Loss -->
<!-- The file-wide ban on `SmoothL1Loss` in np.rs/forecast.rs is intentional and satisfiable: no task in this plan ever writes that identifier into either SOURCE file. The literal appears elsewhere in this plan only as prose — Task 2's contract description names the detachment as the REASON for the D-10 rule, and Task 3 repeats the same negative grep as an acceptance criterion. Both are bans, not requirements, so there is no sibling conflict to region-scope around. -->
<task type="auto" tdd="true">
  <name>Task 1: Port np.rs verbatim, replace 06-01's refusing stub with the real `neuralprophet` arm, and assert dispatch + the three NP refusals (D-02, D-10, D-11)</name>
  <precondition>No other Wave 2 plan (06-03) and no Wave 3 plan is executing: `pgrep -fl 'cargo (test|build|check|clippy|nextest)' | grep -v $$ | wc -l` is 0 before this task starts, and `df -g . | awk 'NR==2{print $4}'` reports at least 20 GiB available (REVIEW-06-05: three plans, one target dir, one build lock, 94 % disk). Halt and report if either fails — do not proceed and hope.</precondition>
  <files>crates/aprender-forecast/src/np.rs, crates/aprender-forecast/src/forecast.rs, crates/aprender-forecast/src/lib.rs</files>
  <read_first>
    - .claude/skills/spike-findings-aprender/sources/004-forecast-mcp-thin-server/src/np.rs (384 lines — the evolved copy; port verbatim; `weighted_huber` at ~206-208 is the D-10 loss built from ops with a constant mask)
    - .claude/skills/spike-findings-aprender/sources/004-forecast-mcp-thin-server/src/lib.rs (lines 226-262: the verbatim `"neuralprophet"` arm the stub replaces — lr sweep {0.03,0.1}/{0.01,0.03,0.1} by train loss, TrainConfig fields, residual-sd band, diagnostics keys)
    - .claude/skills/spike-findings-aprender/references/neuralprophet-autograd.md (sections 1-5 and What to Avoid)
    - crates/aprender-forecast/src/forecast.rs (as written by 06-01 Task 1 — the stub arm returning `ForecastError::Validation("model neuralprophet is ported in plan 06-04")`, and the existing `#[cfg(test)]` module)
    - crates/aprender-forecast/src/lib.rs (as written by 06-01 — where `pub mod np;` goes)
    - crates/aprender-core/src/nn/loss.rs (lines 171-195: why SmoothL1Loss must NOT be used — `SmoothL1Loss::forward` extracts raw data and constructs a fresh `Tensor`, severing the graph)
  </read_first>
  <behavior>
    - `forecast(&args)` with `model: Some("neuralprophet")`, 120 synthetic daily points, `n_lags: None`, horizon 14 -> `Ok`, `response.model == "neuralprophet"`, 14 `ds`, `yhat_lower < yhat_upper` pointwise, `diagnostics["selected_lr"]` one of 0.01/0.03/0.1
    - After that call, `graph_tape_len() == 0` (tape hygiene, D-10)
    - `freq: Some("W")` with `model: neuralprophet` -> `Err(Validation)` whose message contains "supports freq D only"
    - `n_lags: Some(366)` -> `Err(Validation)` whose message contains "365"
    - `n_lags: Some(120)` on a 120-point daily series -> `Err(Validation)` whose message contains "smaller than the series span"
  </behavior>
  <action>
    Create `crates/aprender-forecast/src/np.rs` from sources/004 `np.rs` verbatim (D-08): `Rng`, `NpSeason`,
    `np_auto_seasonalities`, `season_dim`, `fourier_feats` (days since 1900-01-01), `trend_feats`,
    `sample_weight`, `NpData::new(ds_days, y, n_train_rows, n_changepoints, cp_range)`, `NpModel`,
    `weighted_huber`, `one_cycle_lr`, `auto_batch`, `auto_epochs`, `TrainConfig`, `TrainLog`, `train`,
    `predict_ts`, `predict_trend`, `predict_ar_1step`, `predict_ar_recursive`. Only clippy/fmt edits
    (RESEARCH Pitfall 1); keep `use aprender::autograd::{clear_graph, graph_tape_len, no_grad, Tensor};`
    and never import the core loss module. Add `pub mod np;` to `src/lib.rs`.

    In `src/forecast.rs` replace 06-01's stub arm body with the verbatim `"neuralprophet"` arm from
    sources/004 lib.rs:226-262: refuse `freq != "D"` ("neuralprophet supports freq D only" — reword the
    spike's "in this spike" phrasing, keep the rest), refuse `n_lags > 365`, refuse `n_lags >= d.n_train_grid`,
    lr sweep `[0.03, 0.1]` with lags / `[0.01, 0.03, 0.1]` without, selected by lowest final TRAIN loss,
    `epochs = auto_epochs(n).min(320)` with lags, `weight_decay 1e-3`, `huber_beta 0.3`, `newer_w 2.0`,
    per-request `seed` (default 42), residual-sd band via `normal_quantile((1 + interval_width) / 2)`,
    diagnostics keys `n_lags, ar_layers, epochs, batch, steps, params, selected_lr, final_train_loss,
    residual_sd, band, seasonalities`. Delete the stub sentence entirely — the file-wide negative grep in the
    verify is the acceptance state.

    Then add to forecast.rs's existing `#[cfg(test)]` module, on a 120-point synthetic daily series
    (sine + trend via `prophet::Rng`): `neuralprophet_arm_dispatches_and_returns_a_band` (the `<behavior>`
    happy path plus `graph_tape_len() == 0`), and the three D-11 refusals
    `neuralprophet_refuses_non_daily_freq`, `neuralprophet_refuses_n_lags_above_365`,
    `neuralprophet_refuses_n_lags_at_or_above_span`, each asserting the variant is
    `ForecastError::Validation(..)` and never `Internal`. Correctness against the NP oracle is Task 3;
    this task proves dispatch, refusals and tape hygiene only.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast:: > /tmp/p06-04-t1a.log 2>&1; rc=$?; tail -12 /tmp/p06-04-t1a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-04-t1a.log > /dev/null && test "$(grep -c '^test forecast::tests::neuralprophet_' /tmp/p06-04-t1a.log)" -ge 4</automated>
    <fails_when>non-zero exit; a "0 passed" summary (the module filter matched nothing — a filter matching nothing exits 0, which is why the count check is here); or fewer than 4 `forecast::tests::neuralprophet_` lines (the dispatch smoke test plus the three refusals did not all run)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets -- -D warnings > /tmp/p06-04-t1b.log 2>&1; rc=$?; tail -3 /tmp/p06-04-t1b.log; test $rc -eq 0 && cargo fmt --all -- --check > /tmp/p06-04-t1c.log 2>&1; rc2=$?; test $rc2 -eq 0 && ! grep -q 'ported in plan 06-04' crates/aprender-forecast/src/forecast.rs && ! grep -q 'SmoothL1Loss' crates/aprender-forecast/src/np.rs crates/aprender-forecast/src/forecast.rs</automated>
    <fails_when>non-zero clippy or fmt --check exit; the 06-01 stub sentence is still present in forecast.rs (the arm was not filled); or `SmoothL1Loss` appears in either file (D-10 — the core loss is detached from the graph)</fails_when>
  </verify>
  <done>np.rs is a verbatim, lint-clean port; the neuralprophet arm is the spike's, the stub sentence is gone; the smoke test proves dispatch, a residual band and an empty tape after the fit; the three D-11 refusals are Validation-class.</done>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/np.rs` contains `pub fn weighted_huber(`, `pub fn one_cycle_lr(`, `pub fn auto_batch(`, `pub fn auto_epochs(`, `clear_graph()`, and `1900` (the Fourier epoch)
    - `crates/aprender-forecast/src/lib.rs` contains `pub mod np;`
    - `crates/aprender-forecast/src/forecast.rs` contains `"neuralprophet" =>`, `[0.03, 0.1]`, `[0.01, 0.03, 0.1]`, `.min(320)`, `weight_decay: 1e-3`, `huber_beta: 0.3`
    - forecast.rs contains `fn neuralprophet_arm_dispatches_and_returns_a_band`, `fn neuralprophet_refuses_non_daily_freq`, `fn neuralprophet_refuses_n_lags_above_365`, `fn neuralprophet_refuses_n_lags_at_or_above_span`
    - The smoke test asserts `graph_tape_len() == 0` after the forecast call (grep `graph_tape_len` in forecast.rs)
    - `grep -c 'ForecastError::Validation(' crates/aprender-forecast/src/forecast.rs` counts at least 3 occurrences inside the tests module (each refusal asserts the variant, not just the message)
    - The SUMMARY records every clippy lint that fired on the ported np.rs and how each was resolved (the D-08 audit trail 06-01 started)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write contracts/neuralprophet-parity-v1.yaml (setfit-apr-v1 shape) with the frozen SC3 bars and D-10 invariants</name>
  <reversibility rating="costly">Loosening a frozen bar later is a `pv diff`-flagged contract edit, never an inline relaxation.</reversibility>
  <files>contracts/neuralprophet-parity-v1.yaml</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (lines 1-30, 80-100, 1010-1022, 1103-1115, 1243-1262, 1276-1310 — the shape)
    - crates/aprender-contracts/src/schema/types.rs (lines 248-281 Equation `float_tolerance`; 289-321 ProofObligation; 326-367 ObligationType lowercase; 448-491 FalsificationTest; 497-510 KaniHarness)
    - .claude/skills/spike-findings-aprender/sources/002-neuralprophet-autograd/RUN-OUTPUT.md (lines 14-70: the measured data-prep matches, MAE table, seed/epoch/full-batch ablations)
    - contracts/prophet-parity-v1.yaml if already present in the tree (plan 06-03 runs in the same wave — do not depend on it; mirror the shape from setfit-apr-v1 directly)
  </read_first>
  <action>
    Create `contracts/neuralprophet-parity-v1.yaml`: `contract: neuralprophet-parity`, `metadata` (version 1.0.0,
    created 2026-09-05, `kind: kernel`, description stating SC3, the oracle env `neuralprophet==0.9.0` with
    `pandas<3 numpy<2.3 torch<2.6`, and the D-10 rules with the reason for each — full-batch collapse to
    MAE 2.6, SmoothL1Loss detachment, train-loss selection; `references`: Triebe et al. 2021 "NeuralProphet:
    Explainable Forecasting at Scale", `.claude/skills/spike-findings-aprender/references/neuralprophet-autograd.md`,
    `sources/002-neuralprophet-autograd/RUN-OUTPUT.md`), `equations` with `float_tolerance` per the interfaces
    block plus invariant equations `fourier_epoch_1900`, `mini_batch_never_full` (auto_batch(n) < n for n >= 64),
    `lr_selected_by_train_loss`, `tape_cleared_per_step`, `huber_built_from_ops`; top-level `constants` as
    listed; `proof_obligations` (>= 1 per equation; `type: bound|invariant|equivalence`), `falsification_tests`
    FALSIFY-NP-001.. each naming `cargo test -p aprender-forecast --lib np::parity::<fn>`, `kani_harnesses`
    (house comment; `harness` names the bounded runnable `np::parity::auto_batch_is_mini_batch_over_grid`),
    `qa_gate` F-FORECAST-NP-001. Validate with `cargo run -p aprender-contracts-cli --bin pv -- validate` and
    read `pv status` counts.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/neuralprophet-parity-v1.yaml > /tmp/p06-04-t1a.log 2>&1; rc=$?; tail -3 /tmp/p06-04-t1a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-04-t1a.log</automated>
    <fails_when>non-zero exit or no "0 error(s)" line</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/neuralprophet-parity-v1.yaml > /tmp/p06-04-t1b.log 2>&1; rc=$?; cat /tmp/p06-04-t1b.log; test $rc -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-04-t1b.log</automated>
    <fails_when>non-zero exit or any count printed as 0 (hollow contract)</fails_when>
  </verify>
  <done>Contract validates, non-hollow, every SC3 bar and D-10 rule frozen.</done>
  <acceptance_criteria>
    - `contracts/neuralprophet-parity-v1.yaml` contains `kind: kernel`, `lag_free_holdout_mae:`, `float_tolerance: 0.47`, `ar_net_vs_naive_margin:`, `data_prep_vs_oracle_abs:`, `mini_batch_never_full:`, `constants:`, `FALSIFY-NP-001`, `DECLARED, NOT EXECUTED`, `neuralprophet==0.9.0`
    - `! grep -q '^falsification:' contracts/neuralprophet-parity-v1.yaml`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: np::parity — oracle data prep, lag-free MAE, AR-Net vs naive, Huber connectivity, training-rule invariants</name>
  <files>crates/aprender-forecast/src/np.rs</files>
  <read_first>
    - crates/aprender-forecast/src/np.rs (as ported in Task 1 of THIS plan: NpData::new signature, TrainConfig fields, train() return (NpModel, TrainLog), predict_ts / predict_ar_1step / predict_ar_recursive, weighted_huber, auto_batch, auto_epochs)
    - .claude/skills/spike-findings-aprender/sources/002-neuralprophet-autograd/src/main.rs (lines 30-130: how the oracle split is formed, the lr sweep, how test MAE is computed for lag-free and for the AR one-step case, the connectivity probe at 48-60)
    - crates/aprender-forecast/src/test_support.rs (load_json, read_csv, equation_tolerance, constant_u64)
    - crates/aprender-core/src/autograd/mod.rs (graph_tape_len, clear_graph) and tensor.rs (grad())
  </read_first>
  <action>
    Append `#[cfg(test)] mod parity` to np.rs (module-level `#[allow(clippy::disallowed_methods)]` only if
    `json!` is used). Tests:
    - `data_prep_matches_np_oracle`: build `NpData::new(&ds, &y, n_train, 10, 0.8)` from
      `tests/fixtures/peyton_manning.csv` with the oracle's split; assert `shift`, `scale`, `t0` (as days),
      `t_span`, and all 11 `cps` vs `trend_seasonality.data_params` / `changepoints_t` within
      `equation_tolerance("neuralprophet-parity-v1", "data_prep_vs_oracle_abs")`; assert
      `auto_batch(n_train) == trend_seasonality.batch` and `auto_epochs(n_train) == trend_seasonality.epochs`.
    - `lag_free_365_day_mae_within_contract`: lr sweep over `constants.lr_sweep_lag_free` selected by lowest
      final TRAIN loss (never by test), `seed 42`, `predict_ts` on the 365 held-out days, MAE vs the CSV's
      held-out y <= `lag_free_holdout_mae`; also assert the selected lr is in the sweep and log the oracle's
      `mae_365ahead_on_test_rows` in the message.
    - `ar_net_30_lags_beats_naive_one_step`: `n_lags 30`, `ar_layers [32]`, sweep `lr_sweep_with_lags`,
      `epochs = auto_epochs(n).min(320)`; one-step MAE on the held-out rows via `predict_ar_1step` <
      `oracle.naive_1step_mae_test - ar_net_vs_naive_margin`.
    - `weighted_huber_is_graph_connected`: a tiny `NpModel`, one forward, `weighted_huber(pred, target, w, 0.3).backward()`;
      every parameter's `grad()` is `Some` and at least one entry has abs > `huber_grad_min_abs`.
    - `auto_batch_is_mini_batch_over_grid`: for n in 64..=20_000 step 97: `auto_batch(n) < n` and
      `8 <= auto_batch(n) <= 2048` (the kani-harness evidence).
    - `train_clears_the_tape`: after `train()`, `graph_tape_len() == 0`.
    - `full_batch_is_not_what_auto_batch_returns`: `auto_batch(2540) == 64` (NP's own value for Peyton).
    Debug-profile note: the AR case is 1.2 s release; the root-manifest profile was decided in 06-01 Task 2
    (Wave 1 — already in place for every later plan; no plan after Wave 1 edits the root Cargo.toml), and without
    an override it may run 30-40 s in debug — acceptable under nextest's 1200 s kill; record the measured wall
    and the profile state (`grep -c '^\[profile\.dev\.package\.aprender-forecast\]' Cargo.toml`) in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib np::parity > /tmp/p06-04-t3.log 2>&1; rc=$?; tail -15 /tmp/p06-04-t3.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-04-t3.log > /dev/null && test "$(grep -c '^test np::parity::' /tmp/p06-04-t3.log)" -ge 7</automated>
    <fails_when>non-zero exit, any failed/ignored test, or fewer than 7 `test np::parity::` lines</fails_when>
  </verify>
  <done>Seven NP tests pass reading the contract; SC3's two accuracy bars and the D-10 invariants are asserted.</done>
  <acceptance_criteria>
    - np.rs contains test fns `data_prep_matches_np_oracle`, `lag_free_365_day_mae_within_contract`, `ar_net_30_lags_beats_naive_one_step`, `weighted_huber_is_graph_connected`, `auto_batch_is_mini_batch_over_grid`, `train_clears_the_tape`
    - `grep -c 'equation_tolerance("neuralprophet-parity-v1"' crates/aprender-forecast/src/np.rs` is >= 3
    - `! grep -q 'SmoothL1Loss' crates/aprender-forecast/src/np.rs`
    - `! grep -Eq '0\.47[^0-9]' crates/aprender-forecast/src/np.rs` (the MAE bar is contract-read, not literal)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request `n_lags` / `freq` -> AR-Net training | caller-controlled model size and epoch count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Denial of Service | NP training with `n_lags` | high | mitigate | `n_lags <= 365`, `n_lags < span`, `epochs = auto_epochs(n).min(320)`, `MAX_POINTS 20_000`; runs on `spawn_blocking` |
| T-06-12 | Tampering | autograd tape shared per thread | medium | mitigate | `clear_graph()` after every step (`train_clears_the_tape`); fits never interleave on one thread (spawn_blocking); pool equality test in 06-06 |
| T-06-SC | Tampering | cargo installs | high | accept | no new dependency |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib forecast::` green with >= 4 `forecast::tests::neuralprophet_` lines (dispatch + three refusals); the `ported in plan 06-04` stub sentence is gone
- `cargo test -p aprender-forecast --lib np::parity` >= 7 passed, 0 failed, 0 ignored
- `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets -- -D warnings` clean; `cargo fmt --all -- --check` clean
- `pv validate` 0 errors; `pv status` non-zero counts
- This plan ran alone (its Task 1 precondition held): no concurrent cargo build, >= 20 GiB free
</verification>

<success_criteria>
- `model: neuralprophet` dispatches through the verbatim spike arm (D-02, D-10) and the tape is empty after a fit — the tracer's one stub is filled here, not in 06-01 (REVIEW-06-06)
- SC3 met: lag-free MAE <= 0.47, AR-Net beats naive, data prep matches the oracle, Huber connected
- D-10 rules are invariant tests; D-15 bars contract-read; D-11 NP refusals asserted
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-04)
New module: `crates/aprender-forecast/src/np.rs` (moved here from 06-01 by REVIEW-06-06) and its `pub mod np;` line in `crates/aprender-forecast/src/lib.rs`.
New symbols: `np::{Rng, NpSeason, np_auto_seasonalities, season_dim, fourier_feats, trend_feats, sample_weight, NpData, NpModel, TrainConfig, TrainLog, weighted_huber, one_cycle_lr, auto_batch, auto_epochs, train, predict_ts, predict_trend, predict_ar_1step, predict_ar_recursive}` (spike names, verbatim); the real `"neuralprophet"` match arm inside `forecast::forecast` (replacing 06-01's refusing stub).
New file: `contracts/neuralprophet-parity-v1.yaml` (equations data_prep_vs_oracle_abs, lag_free_holdout_mae, ar_net_vs_naive_margin, huber_grad_min_abs, fourier_epoch_1900, mini_batch_never_full, lr_selected_by_train_loss, tape_cleared_per_step, huber_built_from_ops; FALSIFY-NP-0NN; F-FORECAST-NP-001).
New tests: `np::parity::{data_prep_matches_np_oracle, lag_free_365_day_mae_within_contract, ar_net_30_lags_beats_naive_one_step, weighted_huber_is_graph_connected, auto_batch_is_mini_batch_over_grid, train_clears_the_tape, full_batch_is_not_what_auto_batch_returns}`; `forecast::tests::{neuralprophet_arm_dispatches_and_returns_a_band, neuralprophet_refuses_non_daily_freq, neuralprophet_refuses_n_lags_above_365, neuralprophet_refuses_n_lags_at_or_above_span}`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-04-SUMMARY.md` when done. Record the clippy lints that fired on the ported `np.rs` and how each was resolved (D-08 audit trail), the measured lag-free MAE and AR one-step MAE, the selected lrs, the debug wall time of the AR test, the root-manifest profile state it was measured under, and that the 06-01 stub sentence is gone (REVIEW-06-06).
</output>
