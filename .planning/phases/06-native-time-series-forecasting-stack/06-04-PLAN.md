---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - contracts/neuralprophet-parity-v1.yaml
  - crates/aprender-forecast/src/np.rs
  - crates/aprender-forecast/src/forecast.rs
autonomous: true
requirements: [SC3]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "NeuralProphet-lite data preparation matches the committed NeuralProphet 0.9.0 oracle (np_oracle_peyton.json): soft-normalisation shift/scale, t0/span, the 11-segment changepoints_t, and the auto batch/epochs formulas (SC3, D-10)"
    - "The lag-free model reaches 365-day-ahead holdout MAE <= 0.47 on Peyton with lr selected by TRAIN loss from {0.01, 0.03, 0.1} (SC3; Python NP 0.4611; Rust measured 0.4510)"
    - "With n_lags = 30 the AR-Net's one-step test MAE beats the naive one-step MAE recorded in the oracle (naive_1step_mae_test 0.3461; Rust measured 0.2509) (SC3)"
    - "weighted_huber is graph-connected: after backward() every parameter has Some(grad) with a non-zero entry; the tape is empty after train() (D-10)"
    - "Every bar is read from contracts/neuralprophet-parity-v1.yaml at test time (D-15); the contract validates with 0 errors and non-zero counts"
    - "The NeuralProphet-specific door refusals (freq other than D, n_lags > 365, n_lags >= the series span) are pmcp-visible validation errors with fix-naming messages (D-11)"
  artifacts:
    - path: "contracts/neuralprophet-parity-v1.yaml"
      provides: "frozen NP tolerances: data-prep 1e-9, lag-free MAE 0.47, AR beats naive, connectivity + training-rule invariants"
      contains: "lag_free_holdout_mae"
    - path: "crates/aprender-forecast/src/np.rs"
      provides: "#[cfg(test)] mod parity: data prep vs oracle, lag-free MAE, AR-Net vs naive, Huber connectivity, mini-batch + tape invariants"
      contains: "equation_tolerance(\"neuralprophet-parity-v1\""
    - path: "crates/aprender-forecast/src/forecast.rs"
      provides: "unit tests for the three NP-specific refusals"
      contains: "supports freq D only"
  key_links:
    - from: "crates/aprender-forecast/src/np.rs"
      to: "contracts/neuralprophet-parity-v1.yaml"
      via: "test_support::equation_tolerance"
      pattern: "equation_tolerance\\(\"neuralprophet-parity-v1\""
    - from: "crates/aprender-forecast/src/np.rs"
      to: "crates/aprender-forecast/tests/fixtures/np_oracle_peyton.json"
      via: "test_support::load_json(\"np_oracle_peyton.json\") — keys trend_seasonality.{data_params, changepoints_t, batch, epochs, mae_365ahead_on_test_rows, n_test_rows}, ar30_hidden32.mae_1step_test, naive_1step_mae_test"
      pattern: "np_oracle_peyton"
  prohibitions:
    - statement: "Never core nn::loss::SmoothL1Loss — detached from the graph (D-10); the phase files a core ticket in 06-09 instead of fixing it here (RESEARCH Open Question 4)"
      status: resolved
      verification: test
    - statement: "Never full-batch training (MAE collapses to 2.6); never select lr or epochs by test error"
      status: resolved
      verification: test
    - statement: "NeuralProphet quantile regression and n_forecasts > 1 are NOT in scope (CONTEXT deferred) — the band stays residual-sd based and the diagnostics say so"
      status: resolved
      verification: judgment
---

<objective>
Prove the NeuralProphet-lite port to its oracle (SC3) as CI-reachable `--lib` tests with the bars frozen in
`contracts/neuralprophet-parity-v1.yaml` (D-15), and pin the D-10 training rules (graph-connected Huber,
mini-batches, train-loss lr selection, tape hygiene) as invariant tests so a future "optimisation" cannot
silently break them.

Purpose: NP-lite is the best point forecaster in the spike-006 benchmark (mean MASE 1.029) precisely
because of those rules; the tests make each rule falsifiable.
Output: one contract, ~7 parity/invariant tests in np.rs, 3 refusal unit tests in forecast.rs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F3 SmoothL1Loss quote, F4, Pitfall 3)
@.claude/skills/spike-findings-aprender/references/neuralprophet-autograd.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md  (np.rs port and test_support helpers)

<interfaces>
<!-- Frozen tolerances (spike-002 RUN-OUTPUT measured values in parentheses) -->
equations.<name>.float_tolerance:
  data_prep_vs_oracle_abs        1.0e-9   (shift 5.262690, scale 4.582609, t0 2007-12-10, span 2596 d, 11 changepoints_t — matched NP to 1e-9 / 1e-16)
  lag_free_holdout_mae           0.47     (SC3 literal; Python NP 0.4611; Rust 0.4510, seeds 0.4508–0.4549)
  ar_net_vs_naive_margin         0.0      (MAE(n_lags=30, hidden [32]) < oracle.naive_1step_mae_test; measured 0.2509 vs 0.3461)
  huber_grad_min_abs             0.0      (at least one gradient entry strictly non-zero)
constants: lr_sweep_lag_free "0.01,0.03,0.1", lr_sweep_with_lags "0.03,0.1", auto_epochs_cap_with_lags 320, min_rows_for_mini_batch 64
Oracle keys: trend_seasonality{lr, epochs 80, batch 64, secs, n_train_rows_after_impute, data_params{...shift/scale...}, seasonality, changepoints_t, final_train_loss, final_train_mae, mae_365ahead_on_test_rows, n_test_rows, yhat_by_ds}, ar30_linear{... mae_1step_test}, ar30_hidden32{... mae_1step_test}, naive_1step_mae_test, mean_forecast_mae_test
Split: the oracle trains on the first n_train rows such that the last n_test_rows (365 daily) are held out — read n_train from `n_train_rows_after_impute` semantics as spike-002 main.rs does (lines 60-75), do not guess
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contracts/neuralprophet-parity-v1.yaml (setfit-apr-v1 shape) with the frozen SC3 bars and D-10 invariants</name>
  <reversibility rating="costly">Loosening a frozen bar later is a `pv diff`-flagged contract edit, never an inline relaxation.</reversibility>
  <files>contracts/neuralprophet-parity-v1.yaml</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (lines 1-30, 80-100, 1010-1022, 1103-1115, 1243-1262, 1276-1310 — the shape)
    - crates/aprender-contracts/src/schema/types.rs (lines 248-281 Equation `float_tolerance`; 289-321 ProofObligation; 326-367 ObligationType lowercase; 448-491 FalsificationTest; 497-510 KaniHarness)
    - .claude/skills/spike-findings-aprender/sources/002-neuralprophet-autograd/RUN-OUTPUT.md (lines 14-70: the measured data-prep matches, MAE table, seed/epoch/full-batch ablations)
    - contracts/prophet-parity-v1.yaml if already present in the tree (plan 06-03 runs in the same wave — do not depend on it; mirror the shape from setfit-apr-v1 directly)
  </read_first>
  <action>
    Create `contracts/neuralprophet-parity-v1.yaml`: `contract: neuralprophet-parity`, `metadata` (version 1.0.0,
    created 2026-09-05, `kind: kernel`, description stating SC3, the oracle env `neuralprophet==0.9.0` with
    `pandas<3 numpy<2.3 torch<2.6`, and the D-10 rules with the reason for each — full-batch collapse to
    MAE 2.6, SmoothL1Loss detachment, train-loss selection; `references`: Triebe et al. 2021 "NeuralProphet:
    Explainable Forecasting at Scale", `.claude/skills/spike-findings-aprender/references/neuralprophet-autograd.md`,
    `sources/002-neuralprophet-autograd/RUN-OUTPUT.md`), `equations` with `float_tolerance` per the interfaces
    block plus invariant equations `fourier_epoch_1900`, `mini_batch_never_full` (auto_batch(n) < n for n >= 64),
    `lr_selected_by_train_loss`, `tape_cleared_per_step`, `huber_built_from_ops`; top-level `constants` as
    listed; `proof_obligations` (>= 1 per equation; `type: bound|invariant|equivalence`), `falsification_tests`
    FALSIFY-NP-001.. each naming `cargo test -p aprender-forecast --lib np::parity::<fn>`, `kani_harnesses`
    (house comment; `harness` names the bounded runnable `np::parity::auto_batch_is_mini_batch_over_grid`),
    `qa_gate` F-FORECAST-NP-001. Validate with `cargo run -p aprender-contracts-cli --bin pv -- validate` and
    read `pv status` counts.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/neuralprophet-parity-v1.yaml > /tmp/p06-04-t1a.log 2>&1; rc=$?; tail -3 /tmp/p06-04-t1a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-04-t1a.log</automated>
    <fails_when>non-zero exit or no "0 error(s)" line</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/neuralprophet-parity-v1.yaml > /tmp/p06-04-t1b.log 2>&1; rc=$?; cat /tmp/p06-04-t1b.log; test $rc -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-04-t1b.log</automated>
    <fails_when>non-zero exit or any count printed as 0 (hollow contract)</fails_when>
  </verify>
  <done>Contract validates, non-hollow, every SC3 bar and D-10 rule frozen.</done>
  <acceptance_criteria>
    - `contracts/neuralprophet-parity-v1.yaml` contains `kind: kernel`, `lag_free_holdout_mae:`, `float_tolerance: 0.47`, `ar_net_vs_naive_margin:`, `data_prep_vs_oracle_abs:`, `mini_batch_never_full:`, `constants:`, `FALSIFY-NP-001`, `DECLARED, NOT EXECUTED`, `neuralprophet==0.9.0`
    - `! grep -q '^falsification:' contracts/neuralprophet-parity-v1.yaml`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: np::parity — oracle data prep, lag-free MAE, AR-Net vs naive, Huber connectivity, training-rule invariants</name>
  <files>crates/aprender-forecast/src/np.rs</files>
  <read_first>
    - crates/aprender-forecast/src/np.rs (as ported in 06-01: NpData::new signature, TrainConfig fields, train() return (NpModel, TrainLog), predict_ts / predict_ar_1step / predict_ar_recursive, weighted_huber, auto_batch, auto_epochs)
    - .claude/skills/spike-findings-aprender/sources/002-neuralprophet-autograd/src/main.rs (lines 30-130: how the oracle split is formed, the lr sweep, how test MAE is computed for lag-free and for the AR one-step case, the connectivity probe at 48-60)
    - crates/aprender-forecast/src/test_support.rs (load_json, read_csv, equation_tolerance, constant_u64)
    - crates/aprender-core/src/autograd/mod.rs (graph_tape_len, clear_graph) and tensor.rs (grad())
  </read_first>
  <action>
    Append `#[cfg(test)] mod parity` to np.rs (module-level `#[allow(clippy::disallowed_methods)]` only if
    `json!` is used). Tests:
    - `data_prep_matches_np_oracle`: build `NpData::new(&ds, &y, n_train, 10, 0.8)` from
      `tests/fixtures/peyton_manning.csv` with the oracle's split; assert `shift`, `scale`, `t0` (as days),
      `t_span`, and all 11 `cps` vs `trend_seasonality.data_params` / `changepoints_t` within
      `equation_tolerance("neuralprophet-parity-v1", "data_prep_vs_oracle_abs")`; assert
      `auto_batch(n_train) == trend_seasonality.batch` and `auto_epochs(n_train) == trend_seasonality.epochs`.
    - `lag_free_365_day_mae_within_contract`: lr sweep over `constants.lr_sweep_lag_free` selected by lowest
      final TRAIN loss (never by test), `seed 42`, `predict_ts` on the 365 held-out days, MAE vs the CSV's
      held-out y <= `lag_free_holdout_mae`; also assert the selected lr is in the sweep and log the oracle's
      `mae_365ahead_on_test_rows` in the message.
    - `ar_net_30_lags_beats_naive_one_step`: `n_lags 30`, `ar_layers [32]`, sweep `lr_sweep_with_lags`,
      `epochs = auto_epochs(n).min(320)`; one-step MAE on the held-out rows via `predict_ar_1step` <
      `oracle.naive_1step_mae_test - ar_net_vs_naive_margin`.
    - `weighted_huber_is_graph_connected`: a tiny `NpModel`, one forward, `weighted_huber(pred, target, w, 0.3).backward()`;
      every parameter's `grad()` is `Some` and at least one entry has abs > `huber_grad_min_abs`.
    - `auto_batch_is_mini_batch_over_grid`: for n in 64..=20_000 step 97: `auto_batch(n) < n` and
      `8 <= auto_batch(n) <= 2048` (the kani-harness evidence).
    - `train_clears_the_tape`: after `train()`, `graph_tape_len() == 0`.
    - `full_batch_is_not_what_auto_batch_returns`: `auto_batch(2540) == 64` (NP's own value for Peyton).
    Debug-profile note: the AR case is 1.2 s release; with 06-03's override (same wave, may not have landed
    in this worktree) it may run 30-40 s in debug — acceptable under nextest's 1200 s kill; record the
    measured wall in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib np::parity > /tmp/p06-04-t2.log 2>&1; rc=$?; tail -15 /tmp/p06-04-t2.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-04-t2.log > /dev/null && test "$(grep -c '^test np::parity::' /tmp/p06-04-t2.log)" -ge 7</automated>
    <fails_when>non-zero exit, any failed/ignored test, or fewer than 7 `test np::parity::` lines</fails_when>
  </verify>
  <done>Seven NP tests pass reading the contract; SC3's two accuracy bars and the D-10 invariants are asserted.</done>
  <acceptance_criteria>
    - np.rs contains test fns `data_prep_matches_np_oracle`, `lag_free_365_day_mae_within_contract`, `ar_net_30_lags_beats_naive_one_step`, `weighted_huber_is_graph_connected`, `auto_batch_is_mini_batch_over_grid`, `train_clears_the_tape`
    - `grep -c 'equation_tolerance("neuralprophet-parity-v1"' crates/aprender-forecast/src/np.rs` is >= 3
    - `! grep -q 'SmoothL1Loss' crates/aprender-forecast/src/np.rs`
    - `! grep -Eq '0\.47[^0-9]' crates/aprender-forecast/src/np.rs` (the MAE bar is contract-read, not literal)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: NeuralProphet-specific door refusals as unit tests (D-11)</name>
  <files>crates/aprender-forecast/src/forecast.rs</files>
  <read_first>
    - crates/aprender-forecast/src/forecast.rs (the neuralprophet arm restored in 06-01 Task 2: the three refusals and their messages)
  </read_first>
  <action>
    In forecast.rs's existing `#[cfg(test)]` module add three tests using a 120-point synthetic daily series:
    `neuralprophet_refuses_non_daily_freq` (`freq: Some("W")`, `model: Some("neuralprophet")` -> Err Validation
    whose message contains "supports freq D only"), `neuralprophet_refuses_n_lags_above_365` (`n_lags: Some(366)`
    -> message contains "365"), `neuralprophet_refuses_n_lags_at_or_above_span` (`n_lags: Some(120)` on 120 daily
    points -> message contains "smaller than the series span"). Each asserts the variant is `Validation`, not
    `Internal`.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib forecast::tests::neuralprophet_refuses > /tmp/p06-04-t3.log 2>&1; rc=$?; tail -6 /tmp/p06-04-t3.log; test $rc -eq 0 && grep -E 'test result: ok\. 3 passed; 0 failed' /tmp/p06-04-t3.log > /dev/null</automated>
    <fails_when>non-zero exit or the summary is not exactly "3 passed; 0 failed" (a name drift makes the filter match fewer)</fails_when>
  </verify>
  <done>The three NP refusals are asserted as Validation errors with fix-naming messages.</done>
  <acceptance_criteria>
    - forecast.rs contains `fn neuralprophet_refuses_non_daily_freq`, `fn neuralprophet_refuses_n_lags_above_365`, `fn neuralprophet_refuses_n_lags_at_or_above_span`
    - Each test matches on `ForecastError::Validation(` (grep count >= 3 in the tests module)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request `n_lags` / `freq` -> AR-Net training | caller-controlled model size and epoch count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Denial of Service | NP training with `n_lags` | high | mitigate | `n_lags <= 365`, `n_lags < span`, `epochs = auto_epochs(n).min(320)`, `MAX_POINTS 20_000`; runs on `spawn_blocking` |
| T-06-12 | Tampering | autograd tape shared per thread | medium | mitigate | `clear_graph()` after every step (`train_clears_the_tape`); fits never interleave on one thread (spawn_blocking); pool equality test in 06-06 |
| T-06-SC | Tampering | cargo installs | high | accept | no new dependency |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib np::parity` >= 7 passed, 0 failed, 0 ignored
- `cargo test -p aprender-forecast --lib forecast::tests::neuralprophet_refuses` exactly 3 passed
- `pv validate` 0 errors; `pv status` non-zero counts
</verification>

<success_criteria>
- SC3 met: lag-free MAE <= 0.47, AR-Net beats naive, data prep matches the oracle, Huber connected
- D-10 rules are invariant tests; D-15 bars contract-read; D-11 NP refusals asserted
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-04)
New file: `contracts/neuralprophet-parity-v1.yaml` (equations data_prep_vs_oracle_abs, lag_free_holdout_mae, ar_net_vs_naive_margin, huber_grad_min_abs, fourier_epoch_1900, mini_batch_never_full, lr_selected_by_train_loss, tape_cleared_per_step, huber_built_from_ops; FALSIFY-NP-0NN; F-FORECAST-NP-001).
New tests: `np::parity::{data_prep_matches_np_oracle, lag_free_365_day_mae_within_contract, ar_net_30_lags_beats_naive_one_step, weighted_huber_is_graph_connected, auto_batch_is_mini_batch_over_grid, train_clears_the_tape, full_batch_is_not_what_auto_batch_returns}`; `forecast::tests::{neuralprophet_refuses_non_daily_freq, neuralprophet_refuses_n_lags_above_365, neuralprophet_refuses_n_lags_at_or_above_span}`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-04-SUMMARY.md` when done. Record the measured lag-free MAE and AR one-step MAE, the selected lrs, and the debug wall time of the AR test.
</output>
