---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 17
type: execute
wave: 15
gap_closure: true
depends_on: [06-16]
files_modified:
  - crates/aprender-forecast/src/prophet.rs
  - crates/aprender-forecast/src/test_support.rs
  - crates/aprender-forecast/src/bolt.rs
  - crates/aprender-forecast/README.md
  - contracts/prophet-parity-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [SC1]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WR-02 closed: the sampler's falsification test bars its VARIANCE against lambda as well as its mean, with a contract-owned tolerance, so `fn poisson(_rng, lambda) -> lambda.round() as usize` — a zero-variance stub that collapses every logistic band to the deterministic trend — no longer passes the test that names itself the sampler's domain check. The stub is OBSERVED red, not argued about"
    - "WR-01's second half closed: the sweep can now DETECT the branch threshold being lowered. At the sub-threshold lambdas the test bars the observed ZERO MASS against `exp(-lambda)`, and the normal approximation produces about 3.3x too many zeros at lambda = 5 (2.21 % versus the exact 0.674 %) — roughly 26 standard errors at the sweep's sample size — while its MEAN stays inside the existing 0.01 bar at 0.000310. Lowering `POISSON_NORMAL_BRANCH_LAMBDA` to 3.2 is OBSERVED turning the suite red"
    - "Every new bar is at least 4 standard errors from its own sampling noise, and the arithmetic is written down. A variance estimate at N = 20 000 has a relative standard error near 1.05 %, so the review's suggested 1-2 % bar would be a 1-2 sigma flake — the plan states this deviation from the review's suggestion and either widens the bar or raises N until the 4-sigma rule holds"
    - "IN-02 closed as narrowed: `prophet::feature_row` no longer indexes `hol_sets[hi]` — a mismatched slice is a MISS rather than an out-of-bounds panic in library code — and the 0.63.0 signature change is written down as a public-API note. The signature is NOT reverted: reverting re-opens 06-11's measured design-build fix, and the review itself records the hazard as pre-existing rather than a regression"
    - "IN-04's two substantive items closed: `bolt::transpose`'s `expect` on `trueno::blis::transpose` is preceded by a `debug_assert_eq!` making its safety claim checkable rather than asserted in prose, and the removal of `safetensors::load` from a published crate's public surface is written down as a semver note"
    - "The round is closed on evidence: every gate green in one recorded sweep — both crates' lib tests, the spawned-binary stdio e2e, `just forecast-sc1-sweep`, `just forecast-bench`, `just forecast-holiday-bench`, clippy with `-D warnings`, `cargo fmt --all --check`, `pv validate` on all four Phase 6 contracts, and `make contract-audit-phase6` with zero BIND- and zero RESOLVE-"
    - "All nine `06-REVIEW.md` findings are dispositioned and the disposition is VERIFIABLE from the tree, not just from a table: the SUMMARY names, for each finding, the symbol or file that closes it and the command that proves it"
    - "No parity rung moved across the whole round: `prophet::parity` is 32 passed / 0 failed and `np::parity` is at its pre-round count, and no fixture or tolerance in the three `*-parity-v1.yaml` files was edited except the ADDITION of `poisson_sampler_domain`'s new tolerance keys"
  artifacts:
    - path: "crates/aprender-forecast/src/prophet.rs"
      provides: "the variance and zero-mass assertions on the sampler sweep, the added sub-threshold lambda, and the bounds-safe feature_row lookup"
      contains: "poisson_mean_and_variance_track_lambda"
    - path: "contracts/prophet-parity-v1.yaml"
      provides: "variance_tolerance and zero_mass_tolerance on poisson_sampler_domain, the strengthened invariants, and a new FALSIFY-PROPHET entry for the distributional bars"
      contains: "variance_tolerance"
    - path: "crates/aprender-forecast/src/test_support.rs"
      provides: "equation_float — a reader for a named float key on an equation, so a second tolerance need not be smuggled in as a literal"
      contains: "fn equation_float"
    - path: "crates/aprender-forecast/src/bolt.rs"
      provides: "the debug_assert that makes transpose's expect-safety claim checkable at its own call site"
      contains: "debug_assert_eq!"
    - path: "crates/aprender-forecast/README.md"
      provides: "the public-API change notes for the 0.63.0 surface: feature_row's new parameter and the removal of safetensors::load"
      contains: "Public API changes"
    - path: "contracts/aprender/binding.yaml"
      provides: "the binding row for the strengthened sampler equation, and the Phase 6 header row count reconciled for the whole round"
      contains: "poisson_sampler_domain"
  key_links:
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "contracts/prophet-parity-v1.yaml"
      via: "equation_float(\"prophet-parity-v1\", \"poisson_sampler_domain\", \"variance_tolerance\") read at test time — the second bar lives in the contract exactly as the first does (D-15)"
      pattern: "variance_tolerance"
    - from: "crates/aprender-forecast/src/prophet.rs"
      to: "crates/aprender-forecast/src/prophet.rs"
      via: "the zero-mass assertion at the sub-threshold lambdas is what makes POISSON_NORMAL_BRANCH_LAMBDA's value load-bearing in a test rather than only in a mirror assertion"
      pattern: "zero_mass_tolerance"
  prohibitions:
    - statement: "Never set a statistical bar without writing down how many standard errors it is from its own sampling noise. A variance estimate at N = 20 000 has ~1.05 % relative standard error, so a 2 % bar is a 1.9-sigma flake; if the review's suggested 1-2 % is kept, N must rise until the bar clears 4 sigma. State the arithmetic either way (CLAUDE.md rule 6)"
      status: resolved
      verification: test
    - statement: "Never loosen an existing parity tolerance to accommodate this change. `poisson_sampler_domain`'s existing `float_tolerance` (0.01, the mean bar) is not touched; the new keys are ADDITIONS beside it"
      status: resolved
      verification: test
    - statement: "Never revert `feature_row`'s signature. The `hol_sets` parameter is 06-11's measured design-build fix; the fix here is to make the lookup total, not to undo the hoist. The review records the indexing hazard as pre-existing in the `spec.holidays[hi]` form the change replaced"
      status: resolved
      verification: test
    - statement: "Never leave a finding in the ledger marked incorporated without a symbol or file in the tree that closes it and a command that proves it. Task 3 verifies the ledger against the tree; a row that cannot be verified is reopened, not re-asserted"
      status: resolved
      verification: test
    - statement: "Do not touch `crates/aprender-compute`, `.github/workflows/*.yml`, or `crates/aprender-serve` — scope fence. Wiring the new gate into CI is a human decision, not this plan's"
      status: resolved
      verification: judgment
---

<objective>
Close the last four `06-REVIEW.md` findings — the sampler test that a zero-variance stub passes, the
threshold constant no test can detect being lowered, and the two hygiene items — and close the round
with one recorded evidence sweep across every gate.

Purpose: WR-02 and WR-01's second half are the same defect wearing two hats. `poisson_sampler_domain`
declares a codomain of "a non-negative count whose mean AND VARIANCE are both lambda", and
`FALSIFY-PROPHET-013`'s `if_fails` describes the failure in variance terms — but the test bars only
`|mean - lambda| <= 0.01 * lambda`. `fn poisson(_rng, lambda) -> lambda.round() as usize` yields
`rel = 0.0` at all six sweep points and passes outright, and it would collapse every logistic band to
the deterministic trend. Symmetrically, the branch threshold can be lowered to 3.2 — replacing the
exact sampler with a normal approximation for EVERY real logistic request, in the regime the contract
itself says the approximation is not standard — and the whole suite stays green, because the mean is
the one statistic the normal approximation gets right. One statistic is not a domain check.

Output: variance and zero-mass bars on the same sweep, both contract-owned, both with their sigma
arithmetic written down; the zero-variance stub and the lowered threshold each OBSERVED red; a total
`feature_row` lookup; a checkable safety claim at `bolt::transpose`; public-API notes; and the round's
closing evidence sweep.
</objective>

<gap_closure_context>
Source: `.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md`, WR-02, WR-01's second
half, IN-02 and IN-04. `06-VERIFICATION.md` is STALE — it predates 06-10..06-13, which closed all
three of its gaps.

**WR-01's measurement, reproduced by the reviewer in a standalone scratch binary** (the threshold
effectively lowered below 5.0, so every lambda in `LAMBDAS` takes the normal branch, same `Rng`, same
N = 20 000, same seed):

```
lambda=     5.0 mean=    4.9985 rel=0.000310
lambda=    29.0 mean=   29.0210 rel=0.000726
lambda=    31.0 mean=   31.0664 rel=0.002144
lambda=   100.0 mean=  100.0811 rel=0.000811
lambda=   900.0 mean=  900.0384 rel=0.000043
lambda=  2839.0 mean= 2839.5755 rel=0.000203
```

Every point is under the 0.01 bar. The guard catches loosening in the "make it Knuth again" direction
ONLY.

**The discriminator this plan installs.** The normal approximation's low tail is visibly wrong where
Poisson's is not. At lambda = 5 the exact zero mass is `exp(-5)` = 0.006738; the clamped normal branch
draws `round(5 + sqrt(5) * Z).max(0)`, which is 0 whenever `Z < (0.5 - 5) / sqrt(5)` = -2.0125, i.e.
0.0221 — **3.28x too many zeros**. At N = 20 000 the standard error on p = 0.006738 is 0.00058, so the
gap is roughly 26 sigma. At lambda = 3 the exact mass is 0.049787 against a normal-branch 0.0745,
about 1.50x and roughly 16 sigma. Both are far outside sampling noise, and both are in the regime the
only logistic parity fixture actually runs in (its measured lambda is 3.1239).

| Finding | Where it lands |
|---|---|
| **WR-02** — the falsification test asserts only the first moment; a zero-variance stub passes it | Task 1 |
| **WR-01, second half** — the new test cannot detect a change to `POISSON_NORMAL_BRANCH_LAMBDA` | Task 1 (the ownership half was 06-14 T2) |
| **IN-02** — `feature_row`'s `hol_sets` parameter trades a compile-time invariant for a runtime panic | Task 2, narrowed |
| **IN-04** — `bolt.rs`'s unchecked `expect` safety claim; the `safetensors::load` public removal | Task 2, in part; the scope observation rejected with rationale |

This plan does NOT modify, supersede or re-create 06-01 … 06-16 or their SUMMARYs.
</gap_closure_context>

<review_dispositions_ledger>
## Review Dispositions Ledger — all 9 findings in `06-REVIEW.md`

Repeated verbatim in every round-3 plan. **Task 3 of THIS plan verifies every row against the tree.**

| ID | Severity | Disposition | Landing site |
|---|---|---|---|
| CR-01 | Critical | **INCORPORATED** | 06-14 T1 (`door_surface` enumeration + `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`), T3 (red-side e2e + `logistic_changepoint_cost_bounded`); 06-16 T1 (the sweep gate, observed failing on the CR-01 configuration) |
| WR-01 | Warning | **INCORPORATED, SPLIT** | 06-14 T2 (contract mirror via `constant_f64`); **06-17 T1 (this plan)** — the zero-mass bar at the sub-threshold lambdas, with a lowered threshold observed red |
| WR-02 | Warning | **INCORPORATED** | **06-17 T1 (this plan)** — variance bar with a contract-owned tolerance; the zero-variance stub observed red |
| WR-03 | Warning | **INCORPORATED** | 06-15 T1 (aggregate-dates refusal moved into the holiday loop; exact-total refusal kept) |
| WR-04 | Warning | **INCORPORATED** | 06-16 T1/T2/T3 (one parameterized SC1 sweep, in `just`, no ignore flag; three blind benches folded in; superlative reworded) |
| IN-01 | Info | **INCORPORATED** | 06-16 T2 (shared numeric-shape validator at all five bar sites, with a case table that runs on every gate invocation) |
| IN-02 | Info | **INCORPORATED, NARROWED** | **06-17 T2 (this plan)** — `hol_sets.get(hi).is_some_and(…)` and a public-API note. The signature is NOT reverted: reverting re-opens 06-11's measured design-build fix, and the review records the hazard as pre-existing in the `spec.holidays[hi]` form it replaced |
| IN-03 | Info | **INCORPORATED** | 06-14 T2 (doc count corrected; `DEFAULT_POOL` split into `pool_default_matches_contract`) |
| IN-04 | Info | **INCORPORATED IN PART, REST REJECTED WITH RATIONALE** | **06-17 T2 (this plan)** takes the two substantive items: `debug_assert_eq!(w.len(), out * inp)` at `bolt::transpose`, and the semver note for the `safetensors::load` removal. **REJECTED, no action:** "five source files fall outside the stated 9-file scope" is an observation about the REVIEW's own scope statement, not a defect in the tree; and the `dates.rs` `parse_date` rewrite the same finding examined was explicitly found SOUND by the reviewer, so there is nothing to change there. Recorded so it cannot be re-raised as new |
</review_dispositions_ledger>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md  (WR-02, WR-01, IN-02, IN-04 in full — including the six-row reproduced sweep and the zero-variance stub)
@.planning/phases/06-native-time-series-forecasting-stack/06-13-SUMMARY.md  (what the sampler sweep asserts today, why N = 20 000 was chosen, and the standard-error argument behind the existing 0.01 mean bar)
@.planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md  (WR-01's ownership half: `constants.poisson_normal_branch_lambda` and its mirror)
@.planning/phases/06-native-time-series-forecasting-stack/06-15-SUMMARY.md  (which branch C-08 took; the door bounds this plan's gates must respect)
@.planning/phases/06-native-time-series-forecasting-stack/06-16-SUMMARY.md  (the SC1 sweep gate this plan runs as part of its closing evidence)
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md  (D-15: tolerances live in contracts and are read by tests, never written twice)
@CLAUDE.md  (Verification Discipline rules 1/2/4/6/7; dogfood `pv`; `unwrap()` banned; `unsafe_code = "forbid"`)

<interfaces>
**The sampler sweep** (`crates/aprender-forecast/src/prophet.rs` `#[cfg(test)] mod sampler`, from
~:940). `const N: usize = 20_000; const N_F: f64 = 20_000.0; const SEED: u64 = 20_260_907;`
`const LAMBDAS: [f64; 6] = [5.0, 29.0, 31.0, 100.0, 900.0, 2839.0];` and
`poisson_mean_tracks_lambda_across_its_whole_domain`, which seeds `Rng::new(SEED + i)` per lambda,
SUMS the draws without keeping them, computes `rel`, prints `POISSON MEAN: lambda=… n=… mean=… rel=…`
for all six FIRST and asserts in a second pass. Keeping the draws in a `Vec<usize>` is the only
structural change the new statistics need. The sibling
`wp_log_r_logistic_fixture_lambda_is_far_below_the_branch_threshold` asserts only
`3.1239 < POISSON_NORMAL_BRANCH_LAMBDA`, which passes for any threshold above 3.13 — that is the hole
Task 1 fills from the other side.

**The tolerance readers** (`crates/aprender-forecast/src/test_support.rs`): `equation_tolerance(contract,
equation)` reads `equations.<eq>.float_tolerance` specifically. A second bar on the same equation needs
a reader keyed on the KEY as well, hence `equation_float(contract, equation, key)`. 06-14 added
`constant_f64` and `contract_value` in this file; follow their doc-comment style.

**The equation** (`contracts/prophet-parity-v1.yaml`, `poisson_sampler_domain` at ~:178-193). Its
`codomain` already says "a non-negative count whose mean and variance are both lambda"; its
`float_tolerance` is 0.01; and its invariant block already records the 745.13 numerical fact, the
in-bounds reachability, the parity argument, why the ladder was silent, that the bar is on the
distribution and never on the stream, and the tolerance's sigma arithmetic. The new keys go BESIDE
`float_tolerance`, and a new invariant records why one statistic was not enough.

**`feature_row`** (`crates/aprender-forecast/src/prophet.rs` ~:187-207):
`pub fn feature_row(day: i64, spec: &Spec, cols: &[Column], hol_sets: &[HashSet<i64>], out: &mut Vec<f64>)`
indexes `hol_sets[hi]` where `(hi, off) = c.holiday.expect("holiday col")` — an index derived from a
DIFFERENT argument (`cols`) than the slice it indexes. `Design`'s fields are all `pub`, so
`d.spec.holidays.clear()` followed by `predict(&d, …)` reaches it. Its two callers are `make_design`
(~:272) and `predict` (~:766), both of which build `hol_sets` from the same `spec` immediately before
the row loop.

**`bolt::transpose`** (`crates/aprender-forecast/src/bolt.rs` ~:271-283): calls
`trueno::blis::transpose(out, inp, w, &mut t).expect("transpose: w.len() == out * inp, guaranteed by
every caller")`. The claim `w.len() == out * inp` is enforced by no type and asserted at no call site.

**`safetensors::load`** (`crates/aprender-forecast/src/safetensors.rs`): `pub fn load(path: &str)` was
DELETED in the reviewed range. The reviewer found no remaining callers in `crates/` or `src/`, so the
deletion is correct as dead code — but it is a breaking removal from a published crate's public
surface.

**The crate README** (`crates/aprender-forecast/README.md`) is the crate-local place for the two API
notes; the repository root `CHANGELOG.md` is where a release captures them. Add the README section;
mention the CHANGELOG in the SUMMARY as the release-time follow-up rather than editing 172 KB of
shared history here.

**Contract ids.** `contracts/prophet-parity-v1.yaml`'s `falsification_tests` runs to
`FALSIFY-PROPHET-013` at HEAD (06-13's). READ the file for the next unused id.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-02 + WR-01 half 2 — bar the sampler's VARIANCE and its ZERO MASS, and observe both new red sides</name>
  <files>crates/aprender-forecast/src/test_support.rs, crates/aprender-forecast/src/prophet.rs, contracts/prophet-parity-v1.yaml, contracts/aprender/binding.yaml</files>
  <precondition>No other cargo build is running against this workspace's shared target directory, and `df -h .` shows headroom — this branch halted three times on ENOSPC during waves 1-7 and every round-3 plan runs one at a time.</precondition>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs `#[cfg(test)] mod sampler` in full (~:940-1050) — the
      module doc block, `N`, `N_F`, `SEED`, `LAMBDAS`, the measure-all-then-assert structure of
      `poisson_mean_tracks_lambda_across_its_whole_domain`, and
      `wp_log_r_logistic_fixture_lambda_is_far_below_the_branch_threshold`, whose one-sided assertion is
      the hole this task fills from the other side
    - crates/aprender-forecast/src/prophet.rs `poisson`, `POISSON_NORMAL_BRANCH_LAMBDA` and `Rng` — the
      two branches, and specifically that the normal branch ROUNDS and CLAMPS at zero, which is where
      the low-tail discrepancy comes from
    - crates/aprender-forecast/src/test_support.rs — `equation_tolerance` and the two readers 06-14
      added (`constant_f64`, `contract_value`), for the doc-comment and panic style `equation_float`
      must match
    - contracts/prophet-parity-v1.yaml `poisson_sampler_domain` in full (~:178-193) — the `codomain`
      that already promises variance, every existing invariant, and `float_tolerance: 0.01` which must
      not move — plus its `proof_obligations` entry (~:255) and `FALSIFY-PROPHET-013`
    - .planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md WR-01 and WR-02 in full,
      including the six-row reproduced sweep under a lowered threshold and the zero-variance stub
    - .planning/phases/06-native-time-series-forecasting-stack/06-13-SUMMARY.md — the standard-error
      argument behind N = 20 000 and the existing 0.01 mean bar, which the new bars' arithmetic sits
      beside
  </read_first>
  <behavior>
    - `fn poisson(_rng: &mut Rng, lambda: f64) -> usize { lambda.round() as usize }` — a zero-variance
      stub that would collapse every logistic band to the deterministic trend — FAILS the sweep. It
      passes today at every one of the six lambdas
    - `POISSON_NORMAL_BRANCH_LAMBDA` lowered to 3.2, so every real logistic request in the fixture
      regime takes the normal approximation, FAILS the sweep. It passes today, with every mean inside
      the 0.01 bar
    - The unmodified sampler PASSES every bar at every lambda, with margin, and the margins are printed
    - Every bar is at least 4 standard errors from its own sampling noise, and the arithmetic is in the
      contract's invariant block, not only in the SUMMARY
  </behavior>
  <action>
    (1) A READER FOR A SECOND BAR. Add `pub(crate) fn equation_float(contract: &str, equation: &str,
    key: &str) -> f64` to `test_support.rs`, reading `equations.<equation>.<key>` as an f64 and
    panicking naming both the equation and the key when absent — the same failure shape
    `equation_tolerance` has. Do NOT generalise `equation_tolerance` away; it has eleven call sites and
    a churn there is a diff across the whole parity ladder for no benefit.

    (2) KEEP THE DRAWS. Change the sweep loop to collect each lambda's draws into a `Vec<usize>` (or
    accumulate sum, sum-of-squares and a zero count) instead of only summing. Everything else about
    the loop's structure — per-lambda `Rng::new(SEED + i)`, measure-and-print-all-first-then-assert —
    stays, because aborting at the first bad lambda hides the shape of the failure at the rest
    (CLAUDE.md rule 6, and the existing test's own comment says so).

    (3) THE VARIANCE BAR, WITH ITS SIGMA ARITHMETIC. Compute the sample variance and assert
    `|var - lambda| / lambda <= equation_float("prophet-parity-v1", "poisson_sampler_domain",
    "variance_tolerance")`. THE BAR MUST CLEAR 4 SIGMA. For a Poisson the fourth central moment is
    `lambda + 3*lambda^2`, so the relative standard error of the variance estimate is about
    `sqrt(1/lambda + 2) / sqrt(N)` — approximately **1.05 %** at N = 20 000, essentially flat across
    the sweep. The review suggested "a 1-2 % bar is real rather than a fudge"; at this N a 2 % bar is
    1.9 sigma and would flake. Resolve it one of two ways and say which: set
    `variance_tolerance: 0.05` (4.8 sigma at N = 20 000), or raise N for the sweep until a 2 % bar
    clears 4 sigma (N >= 80 000) and restate the mean bar's own arithmetic accordingly. Either is
    correct; shipping a 2 % bar at N = 20 000 without noticing is not. Record the chosen arithmetic in
    the contract invariant and note the deviation from the review's suggestion with its reason.
    Note also that `round()` inflates a NORMAL-branch variance by about 1/12, which is 0.27 % at
    lambda = 31 and smaller above — negligible — while the Knuth branch draws exact integers and has no
    such term.

    (4) THE ZERO-MASS BAR, WHICH IS WHAT DETECTS A LOWERED THRESHOLD. Add `3.0` to `LAMBDAS` (seven
    points; the only logistic parity fixture's own measured lambda is 3.1239, so 3.0 is the regime the
    ladder actually runs in). For every sweep lambda where the expected zero count is large enough to
    measure — `N * exp(-lambda) >= 30`, which today selects 3.0 and 5.0 — assert
    `|p_zero - exp(-lambda)| / exp(-lambda) <= equation_float(…, "zero_mass_tolerance")`. Derive the
    bar from the two gaps: the clamped normal branch gives 0.0221 at lambda = 5 against an exact
    0.006738 (3.28x, ~26 sigma) and 0.0745 at lambda = 3 against 0.049787 (1.50x, ~16 sigma), so a bar
    near 0.15 is roughly 2.6 sigma from sampling noise at lambda = 5 and far under both defect gaps.
    Compute the actual standard errors and set the bar so it clears 4 sigma of noise while staying
    under half the smaller defect gap; if those two constraints cannot both hold at N = 20 000, raise
    N and say so. Skip the check where the expected count is too small to mean anything, and PRINT
    which lambdas were checked and which were skipped — a silently-skipped assertion is the vacuous
    case this whole round is about.

    (5) OBSERVE BOTH RED SIDES. (a) Temporarily replace `poisson`'s body with
    `lambda.round() as usize` and run the sweep: the variance bar must fail at every lambda and the
    zero-mass bar at 3.0 and 5.0, while the MEAN bar still passes at all seven — record that
    juxtaposition, because it is the precise statement of WR-02. Restore. (b) Temporarily set
    `POISSON_NORMAL_BRANCH_LAMBDA` to 3.2 (and `constants.poisson_normal_branch_lambda` with it, or
    the 06-14 mirror test fires first and masks the point) and run: the zero-mass bar must fail at
    lambda = 5.0 while every mean stays inside 0.01, reproducing the review's six-row table and then
    catching it. Restore. Record both outputs verbatim. Rename the test to
    `poisson_mean_and_variance_track_lambda_across_its_whole_domain` so the name states what it now
    checks.

    (6) THE CONTRACT OWNS BOTH BARS. Add `variance_tolerance` and `zero_mass_tolerance` beside
    `float_tolerance` on `poisson_sampler_domain`, leaving `float_tolerance` at 0.01 untouched. Extend
    the invariant block with: why one statistic was not a domain check (the zero-variance stub passes
    the mean bar at every point); why the zero mass is the statistic that detects a lowered threshold
    (the normal approximation's low tail, with both numbers); and the sigma arithmetic for both new
    bars. Update the `proof_obligations` entry whose `formal` currently quantifies only the mean, and
    add the next unused `FALSIFY-PROPHET-0NN` whose `prediction` names the two observed red sides and
    whose `if_fails` says the sampler's spread — not merely its centre — has left its domain, which is
    what makes `yhat_lower` / `yhat_upper` narrower than the `interval_width` they advertise. Add or
    update the binding row for `poisson_sampler_domain` and reconcile the Phase 6 header's stated row
    count for the whole round.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture > /tmp/p06-17-t1a.log 2>&1; rc=$?; tail -30 /tmp/p06-17-t1a.log; test $rc -eq 0 && test "$(grep -c '^POISSON MEAN: lambda=' /tmp/p06-17-t1a.log)" -ge 7</automated>
    <fails_when>non-zero exit, or a count other than 7 `POISSON MEAN:` lines — lambda = 3.0 was not added to the sweep, or the sweep aborted partway instead of measuring all seven first. If `rtk` swallows `println!` through the Bash tool, read the redirected FILE (which this command already does) rather than concluding the test did not print</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::sampler::poisson_mean_and_variance_track_lambda_across_its_whole_domain > /tmp/p06-17-t1b.log 2>&1; rc=$?; tail -8 /tmp/p06-17-t1b.log; test $rc -eq 0 && grep -q '1 passed' /tmp/p06-17-t1b.log</automated>
    <fails_when>non-zero exit, or `0 passed` — the renamed test does not exist under that path, so the filter matched nothing and a green cargo exit proves nothing about either new bar</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-17-t1c.log 2>&1; rc=$?; tail -8 /tmp/p06-17-t1c.log; test $rc -eq 0 && grep -q 'test result: ok\. 32 passed; 0 failed' /tmp/p06-17-t1c.log</automated>
    <fails_when>non-zero exit, or a summary that is not exactly `test result: ok. 32 passed; 0 failed` — adding bars to the sampler must not move a parity rung; if one moved, something in the sampler itself was changed rather than only its assertions</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/prophet-parity-v1.yaml > /tmp/p06-17-t1d.log 2>&1; rc=$?; tail -10 /tmp/p06-17-t1d.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-17-t1d.log</automated>
    <fails_when>non-zero exit, or the log does not carry `0 error(s)` — the two new tolerance keys were added at the wrong nesting level or the strengthened obligation is malformed. Fix the contract; do not route around `pv` with a shell script</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/test_support.rs` declares `equation_float`:
      `grep -n 'fn equation_float' crates/aprender-forecast/src/test_support.rs` prints exactly one
      line (quote its number), and
      `equation_tolerance` still exists with its eleven call sites unchanged
    - `contracts/prophet-parity-v1.yaml` `poisson_sampler_domain` carries `variance_tolerance` and
      `zero_mass_tolerance` beside an unchanged `float_tolerance: 0.01`
    - The sweep reads both new bars from the contract, not from literals:
      `grep -c 'equation_float' crates/aprender-forecast/src/prophet.rs` is at least 2
    - `LAMBDAS` has seven entries including `3.0`, and the sweep prints seven `POISSON MEAN:` lines
    - The zero-mass check prints which lambdas it checked and which it skipped, with the
      `N * exp(-lambda) >= 30` reason
    - BOTH red observations are recorded verbatim in the SUMMARY: (a) the zero-variance stub — variance
      bar failing at every lambda, zero-mass failing at 3.0 and 5.0, MEAN bar still passing at all
      seven; (b) `POISSON_NORMAL_BRANCH_LAMBDA` at 3.2 — zero-mass failing at 5.0 while every mean
      stays inside 0.01. Both restored afterwards, with the green run also recorded
    - The chosen sigma arithmetic for both new bars is written into the contract's invariant block, and
      the SUMMARY states whether the review's suggested 1-2 % variance bar was kept (with a raised N)
      or widened, and why
    - `prophet::parity` is exactly 32 passed / 0 failed, and `float_tolerance: 0.01` is byte-identical
      to HEAD
  </acceptance_criteria>
  <done>The test that names itself the sampler's domain check now bars the sampler's spread and its low tail as well as its centre, both bars are contract-owned and at least 4 sigma from their own noise, and the two failures the review demonstrated — a zero-variance stub and a lowered branch threshold — have each been observed turning it red.</done>
</task>

<task type="auto">
  <name>Task 2: IN-02 and IN-04 — make the two unchecked safety claims checkable, and write down the public-API changes</name>
  <files>crates/aprender-forecast/src/prophet.rs, crates/aprender-forecast/src/bolt.rs, crates/aprender-forecast/README.md</files>
  <read_first>
    - crates/aprender-forecast/src/prophet.rs `feature_row` and `holiday_day_sets` in full, plus BOTH
      call sites (`make_design`'s row loop and `predict`'s row loop) so the change is evaluated against
      how the slice is actually built
    - crates/aprender-forecast/src/bolt.rs `transpose` and every call site of it in the crate, so the
      `w.len() == out * inp` claim is checked against what callers really pass
    - crates/aprender-forecast/src/safetensors.rs as it stands, to confirm `load` is gone and no
      caller remains
    - crates/aprender-forecast/README.md — its existing structure, so the new section fits
    - .planning/phases/06-native-time-series-forecasting-stack/06-11-SUMMARY.md — why `feature_row`
      gained the parameter, so the fix does not undo the measured design-build improvement
    - .planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md IN-02 and IN-04 in full
  </read_first>
  <action>
    (1) MAKE THE LOOKUP TOTAL, NOT THE SIGNATURE OLD. In `feature_row`, replace `hol_sets[hi].contains(&(day - off))`
    with `hol_sets.get(hi).is_some_and(|s| s.contains(&(day - off)))`, so a slice that does not line up
    with `cols` produces a MISS rather than an out-of-bounds panic in library code. Add a comment
    stating what the change buys and what it does not: the two in-crate callers always build
    `hol_sets` from the same `spec` immediately before the loop, so this cannot change any shipped
    result — a mismatch was previously a panic and is now a zero column, and neither is correct output
    for a caller who built the slice wrong. Do NOT revert the parameter: it is 06-11's measured
    design-build fix, and the review records the same indexing hazard as pre-existing in the
    `spec.holidays[hi]` form it replaced.

    (2) PROVE IT IS NOW TOTAL, AND PROVE IT CHANGED NOTHING. Add a test that calls `feature_row` with
    an intentionally short `hol_sets` (for example an empty slice against a `cols` list carrying
    holiday columns) and asserts it RETURNS with the holiday entries zero rather than panicking — run
    it against the pre-change code first and record the panic. Add a second test asserting the shipped
    path is unchanged: for a spec with holidays, the row produced with a correctly-built `hol_sets` is
    identical before and after (the parity ladder already covers this at the fixture level, so a
    direct row-level equality on one constructed case is enough).

    (3) THE `bolt::transpose` CLAIM. Add `debug_assert_eq!(w.len(), out * inp, "…")` immediately before
    the `trueno::blis::transpose(…).expect(…)` call, so the safety argument the `expect` message states
    in prose is checked in every debug and test build. Keep the `expect` — the `debug_assert` is the
    check, the `expect` is the message when a release build is wrong anyway. Say in a comment that this
    is the checkable form of the claim, not a substitute for it. Do NOT change the function's return
    type to `Result`: the review offers that as an alternative, and it is a public-API change to a
    published crate for a condition every in-crate caller already satisfies. Record that choice and its
    reason.

    (4) THE PUBLIC-API NOTES. Add a `## Public API changes` section to
    `crates/aprender-forecast/README.md` covering, with the reason for each: `prophet::feature_row`
    gaining a fourth parameter (`hol_sets`) — the caller hoists the membership-set construction out of
    the row loop, which is what 06-11 measured — and the removal of `safetensors::load`, which had no
    remaining callers in `crates/` or `src/` and was deleted as dead code but is a breaking removal
    from a published surface. State that both are breaking for external callers of the 0.63.0 crate and
    that the release-time capture belongs in the repository root `CHANGELOG.md`. Do not edit the
    172 KB root `CHANGELOG.md` here; name it in the SUMMARY as the release follow-up.

    (5) CONFIRM THE DELETION IS STILL CORRECT, by search rather than by trust. Use `pmat query` for the
    semantic pass per CLAUDE.md's Code Search Policy — for example
    `pmat query "safetensors load from path" --limit 10` — and confirm by reading that nothing in
    `crates/` or `src/` calls the removed function. If a caller HAS appeared since the review, that is a
    finding: report it rather than restoring the function silently.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-17-t2a.log 2>&1; rc=$?; tail -15 /tmp/p06-17-t2a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit. A failure in the new short-slice test means `feature_row` still indexes rather than `get`s; a failure anywhere in `prophet::parity` means the lookup change altered a shipped row, which it must not</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib prophet::parity > /tmp/p06-17-t2b.log 2>&1; rc=$?; tail -8 /tmp/p06-17-t2b.log; test $rc -eq 0 && grep -q 'test result: ok\. 32 passed; 0 failed' /tmp/p06-17-t2b.log</automated>
    <fails_when>non-zero exit, or a summary that is not exactly `test result: ok. 32 passed; 0 failed` — the `get`-based lookup returned a miss where the index returned a hit, which would mean `hol_sets` and `cols` were never actually aligned and a real result just changed</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib bolt:: > /tmp/p06-17-t2c.log 2>&1; rc=$?; tail -12 /tmp/p06-17-t2c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the new `debug_assert_eq!` fires in a test build, which means some in-crate caller does NOT satisfy `w.len() == out * inp` and the `expect` message's claim was false. That is a real finding: report it, do not weaken the assert</fails_when>
    <automated>test "$(grep -c 'Public API changes' crates/aprender-forecast/README.md)" -ge 1</automated>
    <fails_when>a count of 0 — the section was not added, so the two breaking changes to a published crate's surface remain undocumented</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-17-t2d.log 2>&1; rc=$?; tail -20 /tmp/p06-17-t2d.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — any warning is an error. `is_some_and` on an `Option<&HashSet<_>>` can trip a pedantic lint depending on how the closure is written; fix the code, never the lint level</fails_when>
  </verify>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/prophet.rs` `feature_row` uses a total lookup:
      `grep -c 'hol_sets.get(' crates/aprender-forecast/src/prophet.rs` is at least 1, and the
      function's parameter list is unchanged from HEAD (the signature is NOT reverted)
    - A test exists that calls `feature_row` with a short `hol_sets` and asserts it returns rather than
      panicking, and the pre-change panic is recorded in the SUMMARY
    - `crates/aprender-forecast/src/bolt.rs` carries a `debug_assert_eq!` immediately before the
      `trueno::blis::transpose` call, and the `expect` is retained
    - `crates/aprender-forecast/README.md` carries a `## Public API changes` section naming both
      `prophet::feature_row`'s new parameter and the `safetensors::load` removal, each with its reason
      and each marked breaking for external callers of 0.63.0
    - The `pmat query` confirmation that `safetensors::load` has no remaining caller is recorded, with
      the query used
    - `prophet::parity` is exactly 32 passed / 0 failed
  </acceptance_criteria>
  <done>Two safety claims that were asserted in prose are now checked by the compiler's own debug machinery or made total, without reverting a measured performance fix or widening a published API, and both breaking changes to the 0.63.0 surface are written down where a consumer would look.</done>
</task>

<task type="auto">
  <name>Task 3: Close the round on evidence — one recorded sweep of every gate, and the ledger verified against the tree</name>
  <files>crates/aprender-forecast/README.md, contracts/aprender/binding.yaml</files>
  <read_first>
    - .planning/phases/06-native-time-series-forecasting-stack/06-14-SUMMARY.md,
      06-15-SUMMARY.md and 06-16-SUMMARY.md — the three prior round-3 SUMMARYs, for every symbol,
      recipe and contract key the ledger must resolve to
    - .planning/phases/06-native-time-series-forecasting-stack/06-REVIEW.md — all nine findings, one
      last time, read against the tree rather than against the ledger table
    - contracts/aprender/binding.yaml — the Phase 6 block header's stated row count, which four plans
      have now each incremented
    - Makefile `contract-audit-phase6` and `PHASE6_CONTRACTS` — what the audit actually checks, so its
      green is read correctly
  </read_first>
  <action>
    (1) ONE RECORDED SWEEP, EVERY GATE, IN ORDER. Run each of the following, redirecting to its own log
    and reading `rc=$?` from the redirect and NEVER through a pipe (CLAUDE.md rule 1 — this exact
    mistake shipped twice in this repository), and record the rc and the decisive line from each:
    `cargo test -p aprender-forecast --lib`; `cargo test -p aprender-mcp-forecast --lib`;
    `cargo test -p aprender-mcp-forecast --test e2e_stdio`; `cargo test --release -p aprender-forecast
    --lib sc1_wall:: -- --nocapture`; `just forecast-sc1-sweep`; `just forecast-bench`;
    `just forecast-holiday-bench`; `bash scripts/check_assert_measurement_under_cases.sh`;
    `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings`;
    `cargo fmt --all -- --check`; `pv validate` on each of the four Phase 6 contracts;
    `make contract-audit-phase6`. If any is red, fix it in the plan that owns the file and re-run —
    do not record a red gate as a known issue.

    (2) VERIFY THE LEDGER AGAINST THE TREE, NOT AGAINST ITSELF. For each of the nine findings, name in
    the SUMMARY (a) the symbol, file or contract key in the tree that closes it, and (b) a command
    whose output proves it. For the two REJECTED items inside IN-04, state the rejection and its
    rationale instead. A row that cannot be resolved to something in the tree is REOPENED — say so and
    stop, rather than restating the table. This is the round's own version of the check that produced
    it: three prior rounds each believed they had closed the class.

    (3) RECONCILE THE BINDING ROW COUNT ONCE. Four plans have each added rows and each updated the
    Phase 6 block header's stated count. Count the rows in the block for real and set the header to
    that number, so the comment is not carrying four sequential guesses. Re-run
    `make contract-audit-phase6` afterwards.

    (4) STATE WHAT THE ROUND DID AND DID NOT CLOSE. In the SUMMARY, state plainly: which cost axes are
    now bounded at the door and by which constants; which are closed by a recorded measurement rather
    than a bound, with the number; that `every_request_knob_is_enumerated` and
    `every_cost_axis_names_a_real_bound` are the two tests that make the class invariant checkable and
    both are green; and what is still OPEN. The three `human_verification` items from
    `06-VERIFICATION.md` — the browser MCP handshake and chart rendering on both demo pages, the
    x86_64 `quantiles_abs_f32_nonaarch64` measurement (D-ITEM-06-04), and the decision on SC4's Chronos
    ladder staying dark in CI (D-ITEM-06-03) — are NOT closed by this round and remain open. Wiring
    `just forecast-sc1-sweep` into CI is likewise a human decision this round does not take; note it as
    the natural next one, since a gate nobody runs is the shape of WR-04.

    (5) A HONEST STATEMENT ABOUT THE CLASS. Round 3 closed every axis the enumeration could find. Say
    in the SUMMARY what would falsify the claim that the class is closed: a caller-settable knob or a
    caller-driven cost axis that exists and is absent from `door_surface`. Name the two tests that
    would go red if such a thing were added through the structs, and name the one way it could still
    slip in — an axis introduced inside `prophet.rs` or `np.rs` that no `ForecastArgs` field names, for
    which the only guard is the `sc1_wall` sweep noticing the wall move. That is the residual risk, and
    stating it is more useful than declaring victory.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib > /tmp/p06-17-t3a.log 2>&1; rc=$?; tail -12 /tmp/p06-17-t3a.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the log's `test result:` lines name the failing module</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --lib > /tmp/p06-17-t3b.log 2>&1; rc=$?; tail -12 /tmp/p06-17-t3b.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — most likely an e2e refusal whose needle no longer matches a shipped message after four plans of message edits</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-mcp-forecast --test e2e_stdio > /tmp/p06-17-t3c.log 2>&1; rc=$?; tail -10 /tmp/p06-17-t3c.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the spawned-binary stdio path regressed. This target is dark in CI, so it is only ever visible when run deliberately, which is why it is in this sweep</fails_when>
    <automated>just forecast-sc1-sweep > /tmp/p06-17-t3d.log 2>&1; rc=$?; tail -30 /tmp/p06-17-t3d.log; test $rc -eq 0 && grep -q 'SC1 SWEEP OK' /tmp/p06-17-t3d.log</automated>
    <fails_when>non-zero exit, or no `SC1 SWEEP OK` summary — a composition crossed the 2 s bar (the log names it), the validator case table failed, or the recipe checked zero lines</fails_when>
    <automated>for c in forecast-tool-boundary-v1 prophet-parity-v1 neuralprophet-parity-v1 chronos-bolt-parity-v1; do CARGO_INCREMENTAL=0 cargo run --release -p aprender-contracts-cli --bin pv -- validate "contracts/$c.yaml"; done > /tmp/p06-17-t3e.log 2>&1; rc=$?; tail -20 /tmp/p06-17-t3e.log; test $rc -eq 0 && test "$(grep -c '0 error(s)' /tmp/p06-17-t3e.log)" -ge 4</automated>
    <fails_when>non-zero exit, or fewer than four `0 error(s)` lines — one of the four Phase 6 contracts is malformed, or the loop exited early and validated fewer than four files</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-17-t3f.log 2>&1; rc=$?; tail -25 /tmp/p06-17-t3f.log; test $rc -eq 0 && test "$(grep -c 'Total equations:' /tmp/p06-17-t3f.log)" -ge 4 && test "$(grep -c 'BIND-' /tmp/p06-17-t3f.log)" -eq 0</automated>
    <fails_when>non-zero exit, fewer than four `Total equations:` summaries (a contract was not audited, so its count is not evidence of anything), or any `BIND-` line (an equation added this round has no binding row, or a row does not resolve to a definition site in the file its `module_path` names)</fails_when>
    <automated>cargo fmt --all -- --check > /tmp/p06-17-t3g.log 2>&1; rc=$?; tail -20 /tmp/p06-17-t3g.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — the log names the file and the offending span</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings > /tmp/p06-17-t3h.log 2>&1; rc=$?; tail -25 /tmp/p06-17-t3h.log; test $rc -eq 0</automated>
    <fails_when>non-zero exit — any warning is an error on these two crates. A bare `cargo clippy` would lint only the nearly-empty root facade, which is why both packages are named explicitly</fails_when>
  </verify>
  <acceptance_criteria>
    - Every command in step (1) is recorded in the SUMMARY with its rc and its decisive line, and every
      rc is 0. No gate is recorded as a known issue
    - For each of the nine `06-REVIEW.md` findings the SUMMARY names a symbol, file or contract key in
      the tree AND a command whose output proves it — except the two REJECTED items inside IN-04, for
      which it names the rejection and its rationale
    - The Phase 6 binding block header's stated row count equals the actual number of rows in the
      block, counted for real rather than incremented a fourth time, and
      `make contract-audit-phase6` is green after the reconciliation
    - The SUMMARY states, in one place: every cost axis that is now bounded at the door and by which
      constant; every axis closed by a recorded measurement instead, with the number; that both
      class-invariant tests are green; and what remains OPEN — the three `human_verification` items
      (browser MCP handshake and chart rendering, the x86_64 `quantiles_abs_f32_nonaarch64`
      measurement / D-ITEM-06-04, the SC4-dark-in-CI decision / D-ITEM-06-03) plus the un-taken
      decision to wire `just forecast-sc1-sweep` into CI
    - The residual-risk statement from step (5) is present and names the one path by which a new cost
      axis could still enter without either completeness test noticing
    - `git diff --stat ce3e5a8ea..HEAD` (the review's own diff base, pinned explicitly so a COMMITTED
      change cannot make the check pass vacuously) shows no change to any file under
      `crates/aprender-forecast/tests/fixtures/`, and no change to a numeric tolerance in
      `contracts/{prophet,neuralprophet,chronos-bolt}-parity-v1.yaml` other than the ADDITION of
      `variance_tolerance` and `zero_mass_tolerance`
  </acceptance_criteria>
  <done>Every gate in the round is green in one recorded sweep, every one of the nine review findings resolves to something in the tree with a command that proves it, the binding row count is a count rather than a guess, and what the round did NOT close is written down as plainly as what it did.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| forecast response -> client | `yhat_lower` / `yhat_upper` are consumed as an interval with a stated width; a sampler with the right mean and the wrong spread produces bands that are quietly narrower than advertised |
| `POISSON_NORMAL_BRANCH_LAMBDA` -> the sampler's algorithm | a single constant decides whether a request gets an exact draw or an approximation, in a regime the contract itself says the approximation is not standard for |
| `Design`'s public fields -> `predict` | every field is `pub`, so a caller can construct a `Design` whose `spec.holidays` and `cols` disagree and reach `feature_row` with a mismatched slice |
| test suite -> the claim "this is correct" | a bar on one statistic that a degenerate implementation satisfies is a green suite that means nothing |

Scope note: a stateless local numeric door with no authentication, no persistence and no PII. This
plan's threats are to OUTPUT INTEGRITY and to the integrity of the evidence, not to availability.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-43 | Tampering | `prophet::poisson` and its falsification test | **high** | mitigate | The test that names itself the sampler's domain check bars only the first moment, so an implementation with the right mean and ZERO variance passes it — and that implementation would collapse every logistic band to the deterministic trend while the client is told the interval has the width it asked for. An output whose semantics change without the caller being told is an integrity defect. Task 1 adds a variance bar with a contract-owned tolerance and OBSERVES the stub red |
| T-06-44 | Tampering | `POISSON_NORMAL_BRANCH_LAMBDA` | **high** | mitigate | Lowering the constant to 3.2 replaces the exact sampler with a normal approximation for every real logistic request in the fixture regime, and the entire suite stays green because the mean is exactly what the approximation gets right. 06-14 made the constant contract-owned; Task 1 makes it DETECTABLE, by barring the zero mass at the sub-threshold lambdas where the approximation's low tail is 3.28x wrong, and observes the lowered threshold red |
| T-06-45 | Denial of Service | `prophet::feature_row` | medium | mitigate | `hol_sets[hi]` indexes with an index derived from a different argument; `Design`'s fields are all `pub`, so a mismatched slice is an out-of-bounds panic in library code rather than a type error. Task 2 makes the lookup total. Narrowed deliberately: the parameter is 06-11's measured fix and reverting it re-opens the design-build cost |
| T-06-46 | Tampering | `bolt::transpose`'s `expect` | medium | mitigate | The safety argument `w.len() == out * inp` is enforced by no type and asserted at no call site, so a caller that violates it panics in a library instead of writing a partial result. Task 2 adds a `debug_assert_eq!` making the claim checkable in every debug and test build, without widening a published API to `Result` for a condition every in-crate caller already satisfies |
| T-06-47 | Repudiation | the round's own completion claim | medium | mitigate | Three prior rounds each believed they had closed the class. Task 3 verifies every ledger row against the TREE rather than against the table, records every gate's rc read from a redirect and never through a pipe, and states the residual risk — the one path by which a new cost axis could enter without either completeness test noticing — rather than declaring victory |
| T-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this plan; every symbol used is already a dependency of these crates. No new dependency enters the graph, so the package-legitimacy gate is not engaged. If that changes, a blocking human checkpoint is required before any install |
</threat_model>

<verification>
- `cargo test -p aprender-forecast --lib prophet::sampler -- --nocapture` — seven `POISSON MEAN:`
  lines, every mean, variance and applicable zero-mass bar green, with margins printed
- Both RED observations recorded: the zero-variance stub (variance and zero-mass red, mean still
  green) and `POISSON_NORMAL_BRANCH_LAMBDA` at 3.2 (zero-mass red at lambda 5, every mean green)
- `cargo test -p aprender-forecast --lib prophet::parity` — exactly 32 passed, 0 failed
- `cargo test -p aprender-forecast --lib` and `cargo test -p aprender-mcp-forecast --lib` — 0 failed
- `cargo test -p aprender-mcp-forecast --test e2e_stdio` — 0 failed
- `just forecast-sc1-sweep`, `just forecast-bench`, `just forecast-holiday-bench` — rc=0 with their OK
  tokens
- `bash scripts/check_assert_measurement_under_cases.sh` — rc=0
- `pv validate` on all four Phase 6 contracts — four `0 error(s)`
- `make contract-audit-phase6` — rc=0, four `Total equations:` summaries, zero BIND-, zero RESOLVE-
- `cargo clippy -p aprender-forecast -p aprender-mcp-forecast --all-targets --no-deps -- -D warnings`
  and `cargo fmt --all -- --check` — rc=0
</verification>

<success_criteria>
- The sampler's domain check bars its spread and its low tail, not only its centre, and both new
  failures the review demonstrated have been observed turning it red
- Every statistical bar in this plan is at least 4 standard errors from its own sampling noise, with
  the arithmetic in the contract rather than only in a SUMMARY
- Both unchecked safety claims are checkable, without reverting a measured fix or widening a published
  API, and both breaking 0.63.0 surface changes are documented
- All nine review findings resolve to something in the tree, each with a command that proves it; the
  two rejected items carry their rationale
- What the round did not close is stated as plainly as what it did, including the residual path by
  which a new cost axis could still enter unnoticed
- No parity fixture, and no existing parity tolerance, was modified
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-17)

**New Rust function**: `crates/aprender-forecast/src/test_support.rs` `equation_float` (`pub(crate)`).

**Renamed test**: `prophet::sampler::poisson_mean_tracks_lambda_across_its_whole_domain` becomes
`poisson_mean_and_variance_track_lambda_across_its_whole_domain`. Any reference to the old name — in
`contracts/prophet-parity-v1.yaml`'s `FALSIFY-PROPHET-013` `test:`/`prediction:` fields, in
`contracts/aprender/binding.yaml`, or in a SUMMARY — must be updated with it; a `FALSIFY` entry whose
`test:` names a function that no longer exists is a gate that runs nothing.

**New test names**: the short-`hol_sets` totality case and the row-equality control for `feature_row`
(names at the implementer's discretion).

**Changed sweep data**: `LAMBDAS` gains `3.0` and becomes a seven-element array.

**New contract keys** (`contracts/prophet-parity-v1.yaml`):
`equations.poisson_sampler_domain.variance_tolerance` and
`equations.poisson_sampler_domain.zero_mass_tolerance` (both ADDED beside an unchanged
`float_tolerance: 0.01`); a strengthened `proof_obligations` entry for the same equation; and the next
unused `falsification_tests` id in the `FALSIFY-PROPHET-0NN` series (the highest at HEAD is 013).

**New README section**: `## Public API changes` in `crates/aprender-forecast/README.md`.

**New source assertion**: the `debug_assert_eq!` in `crates/aprender-forecast/src/bolt.rs`.

**Symbols 06-14, 06-15 and 06-16 created** (present after waves 12-14, not at HEAD — do not
drift-check against HEAD): `MAX_LOGISTIC_CHANGEPOINT_LAMBDA`, `prophet::changepoint_count`,
`test_support::constant_f64`, `test_support::contract_value`, `door_surface:` with
`knobs:`/`cost_axes:`, `constants.fit_max_logistic_changepoint_lambda`,
`constants.poisson_normal_branch_lambda`, `constants.fit_max_holiday_name_len`,
`MAX_HOLIDAY_NAME_LEN`, `np::train_cost`, `crates/aprender-forecast/src/sc1_wall.rs`,
`just forecast-sc1-sweep`, `SC1 WALL:`, `SC1 SWEEP OK`, `scripts/assert_measurement_under.sh`,
`scripts/check_assert_measurement_under_cases.sh`, `logistic_changepoint_cost_bounded`,
`door_surface_is_complete`, `holiday_name_cost_bounded`, `np_train_cost_bounded`, `sc1_wall_swept`,
`every_request_knob_is_enumerated`, `every_cost_axis_names_a_real_bound`,
`pool_default_matches_contract`, and conditionally `MAX_NP_TRAIN_COST` /
`constants.fit_max_np_train_cost`.

Everything else named in this plan (`poisson`, `POISSON_NORMAL_BRANCH_LAMBDA`,
`poisson_sampler_domain`, `FALSIFY-PROPHET-013`, `equation_tolerance`, `feature_row`,
`holiday_day_sets`, `make_design`, `predict`, `Design`, `bolt::transpose`, `safetensors`,
`contract-audit-phase6`, `PHASE6_CONTRACTS`, `pv validate`) already exists at HEAD.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-17-SUMMARY.md` when done. Record:
the seven-point sweep output with mean, variance and zero-mass margins; the sigma arithmetic for both
new bars and whether the review's suggested 1-2 % variance bar was kept with a raised N or widened,
with the reason; BOTH red observations verbatim — the zero-variance stub (variance and zero-mass red,
mean green at all seven) and `POISSON_NORMAL_BRANCH_LAMBDA` at 3.2 (zero-mass red at lambda 5, every
mean inside 0.01) — and the restored green run; the pre-change `feature_row` panic and the post-change
return; the `pmat query` that confirmed `safetensors::load` has no remaining caller; every gate from
Task 3 step (1) with its rc and decisive line; the nine-row ledger with a tree symbol and a proving
command per row; the reconciled binding row count; the plain statement of what the round bounded, what
it closed by measurement, and what remains OPEN (the three `human_verification` items and the un-taken
CI-wiring decision); and the residual-risk statement naming the one path by which a new cost axis
could still enter without either completeness test noticing.
</output>
