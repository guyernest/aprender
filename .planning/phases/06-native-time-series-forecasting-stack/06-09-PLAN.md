---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 09
type: execute
wave: 6
depends_on: [06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08]
files_modified:
  - Makefile
  - contracts/aprender/binding.yaml
  - README.md
  - CLAUDE.md
  - .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [SC5]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every Phase 6 contract is validated by something: all four YAMLs are in the Makefile $(CONTRACTS) list, PHASE6_CONTRACTS exists, and contract-audit-phase6 is a BLOCKING tier3 gate that fails on an empty list, on a missing 'Total equations:' summary, or on any BIND- line (D-15; RESEARCH Pitfall 4)"
    - "Every equation in the four contracts has a binding row in contracts/aprender/binding.yaml (bare filename, module_path under aprender_forecast:: / aprender_mcp_forecast:: / aprender_mcp_chronos::, status implemented) so `pv audit` reports zero BIND- findings (D-15)"
    - "README.md's crate and contract counts are RE-DERIVED with the CLAUDE.md commands (cargo metadata; find contracts) and written from the command output, never from a number in a plan (CLAUDE.md 'Derive the numbers')"
    - "CLAUDE.md's Realizar-first table carries the Forecasting exception row and paragraph (D-07, RESEARCH F8 text) and every backticked path in them exists — FALSIFY-DOCS-CLAUDE-001 stays green"
    - "The two CI-wired drift gates are GREEN: cargo test -p aprender-core --test monorepo_invariants and --test readme_contract both exit 0 (SC5 'every gate is green'; five failures on this branch at research time -> zero)"
    - "The human's CI decision from 06-08 is read from its SUMMARY's single bare-token `CI decision:` line (matching `^CI decision: (apply-now|apply-stdio-only|defer)$`; the reply as typed and any runner mount path live on separate `CI decision (verbatim):` / `CI mount:` lines) BEFORE any ci.yml edit (no line -> no edit) and applied exactly (apply-now / apply-stdio-only -> the approved ci.yml hunks, with the `CI mount:` path substituted for the placeholder host path when that line exists; defer -> no ci.yml change) and the outcome is recorded in deferred-items.md alongside the deferred Lambda wrappers, the SmoothL1Loss core ticket, the X64 tolerance assumption, and Chronos-2"
    - "The closing sweep is green on the three new crates: clippy -D warnings (all targets), fmt --check, cargo check --workspace --exclude aprender-profile, and CI's nextest lib leg scoped to the three crates"
  artifacts:
    - path: "Makefile"
      provides: "$(CONTRACTS) +4, PHASE6_CONTRACTS, contract-audit-phase6 target wired into tier3"
      contains: "PHASE6_CONTRACTS"
    - path: "contracts/aprender/binding.yaml"
      provides: "binding rows for every Phase 6 equation"
      contains: "prophet-parity-v1.yaml"
    - path: "README.md"
      provides: "re-derived crate and contract counts"
      contains: "workspace crates"
    - path: "CLAUDE.md"
      provides: "Forecasting Realizar-first exception row + paragraph"
      contains: "Time-Series Forecasting"
    - path: ".planning/phases/06-native-time-series-forecasting-stack/deferred-items.md"
      provides: "D-ITEM entries: Lambda wrappers, SmoothL1Loss ticket, CI decision outcome, X64 tolerance, Chronos-2, model: auto"
      contains: "SmoothL1Loss"
  key_links:
    - from: "Makefile"
      to: "contracts/forecast-tool-boundary-v1.yaml"
      via: "$(CONTRACTS) continuation list + PHASE6_CONTRACTS"
      pattern: "contracts/forecast-tool-boundary-v1\\.yaml"
    - from: "CLAUDE.md"
      to: "crates/aprender-forecast"
      via: "the exception row cites the crate and contract paths; readme_contract checks every backticked path exists"
      pattern: "crates/aprender-forecast"
    - from: "README.md"
      to: "crates/aprender-core/tests/readme_contract.rs"
      via: "format!(\"| Workspace crates | **{crate_count}** workspace crates |\") and format!(\"**{contract_count}** provable contracts\")"
      pattern: "\\*\\*[0-9]+\\*\\* provable contracts"
  prohibitions:
    - statement: "Never hardcode a count from a plan or from memory into README.md — run the derivation command and paste its output (CLAUDE.md 'Derive the numbers; do not quote them')"
      status: resolved
      verification: test
    - statement: "Never cite a path in CLAUDE.md that does not exist in the same commit (FALSIFY-DOCS-CLAUDE-001)"
      status: resolved
      verification: test
    - statement: "Never edit .github/workflows/ci.yml beyond the hunks the human approved in 06-08; under defer, do not touch it"
      status: resolved
      verification: test
    - statement: "Do not fix core nn::loss::SmoothL1Loss in this phase — file the ticket (RESEARCH Open Question 4; touching aprender-core pulls its 14 285-test lib into every cycle)"
      status: resolved
      verification: judgment
    - statement: "Do not open the upstream NEON PR — human checkpoint, not a Phase 6 task (D-05)"
      status: resolved
      verification: judgment
---

<objective>
Close the phase against SC5 "every gate is green": wire the four contracts into the Makefile so they are
validated by something and bound so `pv audit` finds no BIND- lines (D-15), re-derive the README counts and
land the CLAUDE.md Realizar-first exception row (D-07), turn the two CI-wired drift gates green, apply the
human's CI decision from 06-08, and record every deferral where the next milestone will look for it.

Purpose: a contract that exists but is validated by nothing is decoration (Makefile:1920-1927); a README
count copied from a plan is the drift CLAUDE.md warns about; an unrecorded deferral is a silent gap.
Output: Makefile + binding rows, README/CLAUDE.md edits, deferred-items.md, the green sweep.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F4 binding shape, F7, F8 verbatim row + paragraph, Pitfall 4, Pitfall 5, Open Questions 3-4)
@.planning/phases/06-native-time-series-forecasting-stack/06-PATTERNS.md  (sections: Makefile; README/CLAUDE.md drift edits)
@.planning/phases/06-native-time-series-forecasting-stack/06-02-SUMMARY.md  (which readme_contract rows were left red)
@.planning/phases/06-native-time-series-forecasting-stack/06-08-SUMMARY.md  (the CI decision and the patch)

<interfaces>
CLAUDE.md row to insert after the SetFit row (RESEARCH F8, verbatim):
| Time-Series Forecasting (Prophet / NeuralProphet fit+predict, Chronos-Bolt zero-shot) | **Primary** (`crates/aprender-forecast`; thin pmcp servers `crates/aprender-mcp-forecast` and `crates/aprender-mcp-chronos` are transport only) | Never | Compute (`blis::gemm_blis`, NEON 8x6 kernel) |
CLAUDE.md paragraph to insert after the SetFit exception paragraph (RESEARCH F8, verbatim, with the final sentence's glob replaced by the two real files `contracts/forecast-tool-boundary-v1.yaml` and `contracts/chronos-bolt-parity-v1.yaml`):
"**The Forecasting row is a second deliberate EXCEPTION (Phase 6 D-07), for a different reason.** Prophet and NeuralProphet "inference" IS a fit — L-BFGS on a Stan-shaped objective and AdamW on the autograd — which is training-side machinery by this table's own first row, and the fit runs inside every stateless `forecast` call (D-01). Chronos-Bolt is an 8.65M-parameter T5 with no tokenizer, no KV cache and no LLM kernels; its ONLY conformance-proven implementation is the spike-005/007 port (parity 9.5e-7 against `chronos-forecasting` 2.3.1), so the evidence lives in `crates/aprender-forecast`. Serving a second, unproven port through realizar would violate OPS-03 (one implementation per operation). The thin servers own the tool boundary and transport only; `aprender-serve` is untouched. Tolerances and the tool boundary: `contracts/forecast-tool-boundary-v1.yaml`, `contracts/chronos-bolt-parity-v1.yaml`."
Binding row shape (contracts/aprender/binding.yaml:4-10): `- contract: <bare>.yaml / equation: <key> / module_path: <path> / function: <fn> / signature: '<sig>' / status: implemented` (STATE.md trap: bare filename; status in {implemented, partial, not_implemented, pending})
Makefile: $(CONTRACTS) ends at :1866 with `contracts/setfit-benchmark-claims-v1.yaml`; PHASE5_CONTRACTS at :1918; contract-audit-phase5 at :2191-2240 (the exact target to mirror); tier3 calls the audits at :353-375; .PHONY at :60
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the four contracts — $(CONTRACTS), PHASE6_CONTRACTS, contract-audit-phase6 in tier3, binding rows with zero BIND- findings</name>
  <files>Makefile, contracts/aprender/binding.yaml</files>
  <read_first>
    - Makefile (lines 60 .PHONY; 1815 PV_BIN; 1817-1866 CONTRACTS list; 1905-1927 PHASE4/5 lists and the "decoration" comment; 1929-1935 contract-validate; 2191-2240 contract-audit-phase5 — copy this target's structure exactly; 353-375 tier3 audit wiring)
    - contracts/aprender/binding.yaml (lines 1-20: header and row shape; grep for `aprender_contrastive_data` rows to see the Phase 2 precedent of module paths outside the `aprender` crate)
    - contracts/prophet-parity-v1.yaml, contracts/neuralprophet-parity-v1.yaml, contracts/chronos-bolt-parity-v1.yaml, contracts/forecast-tool-boundary-v1.yaml (every `equations:` key — each needs a row)
    - crates/aprender-forecast/src/{fit.rs, forecast.rs, prophet.rs, np.rs, bolt.rs, chronos.rs, dates.rs, types.rs}, crates/aprender-mcp-forecast/src/lib.rs, crates/aprender-mcp-chronos/src/lib.rs (the function each equation binds to — read the real signatures)
  </read_first>
  <action>
    (0) Before any edit, record the plan-start SHA once: `test -s /tmp/p06-09-base.sha || git rev-parse HEAD > /tmp/p06-09-base.sha`
    — Task 3's `.github` consistency gate diffs the committed range from it as well as the working tree.
    Makefile: append the four files to `$(CONTRACTS)` in the continuation-backslash style (after
    `contracts/setfit-benchmark-claims-v1.yaml`, which gains a trailing backslash); add
    `PHASE6_CONTRACTS := contracts/forecast-tool-boundary-v1.yaml contracts/prophet-parity-v1.yaml contracts/neuralprophet-parity-v1.yaml contracts/chronos-bolt-parity-v1.yaml`
    (four entries — one per D-15 concern; the comment explains why this phase has four where 4/5 had one:
    three ports with distinct oracles plus the boundary shared by two servers, none editing an existing
    contract) with the same "audited as a BLOCKING tier3 gate" comment; add `contract-audit-phase6` as a
    line-for-line mirror of `contract-audit-phase5` (log per contract under `target/contract-audit-phase6-N.log`,
    `set +e`/`set -e` around the audit, the `Total equations:` presence check, the BIND- line count, the
    empty-list guard, the message naming Phase 6); add it to `.PHONY` and to tier3 right after
    `@$(MAKE) contract-audit-phase5`. binding.yaml: one row per equation of the four contracts. Bind each to
    the function that realises it — e.g. `prophet-parity-v1.yaml / objective_at_python_map_abs -> aprender_forecast::prophet / Model::value_and_grad`,
    `fit_recipe -> aprender_forecast::fit / fit_prophet`, `neuralprophet-parity-v1.yaml / lag_free_holdout_mae -> aprender_forecast::np / train`,
    `huber_built_from_ops -> aprender_forecast::np / weighted_huber`, `chronos-bolt-parity-v1.yaml / quantiles_abs_f32 -> aprender_forecast::bolt / Bolt::predict`,
    `horizon_gate_native_64 -> aprender_forecast::chronos / validate`, `forecast-tool-boundary-v1.yaml / fit_server_bounds -> aprender_forecast::forecast / forecast`,
    `pool_equality_under_load -> aprender_mcp_forecast / pooled_app`, `counted_skip_when_unarmed -> build script (module_path aprender_forecast, function build_rs_cfg_emission, status implemented, notes naming build.rs)`.
    Signatures copied from the source. Run `make contract-validate` and `make contract-audit-phase6`; if the
    audit prints any BIND- line, fix the row (module path / function name), never the contract.
  </action>
  <verify>
    <automated>make contract-validate > /tmp/p06-09-t1a.log 2>&1; rc=$?; tail -8 /tmp/p06-09-t1a.log; test $rc -eq 0 && test "$(grep -c 'contracts/\(forecast-tool-boundary\|prophet-parity\|neuralprophet-parity\|chronos-bolt-parity\)-v1.yaml' /tmp/p06-09-t1a.log)" -eq 4</automated>
    <fails_when>non-zero exit (a contract failed pv validate) or the four Phase 6 files are not each echoed by the validate loop (one is missing from $(CONTRACTS) — validated by nothing)</fails_when>
    <automated>make contract-audit-phase6 > /tmp/p06-09-t1b.log 2>&1; rc=$?; tail -6 /tmp/p06-09-t1b.log; test $rc -eq 0 && grep -q 'Phase 6 binding audit: 4 contract(s) audited, zero BIND- findings' /tmp/p06-09-t1b.log && test "$(grep -c 'Total equations:' /tmp/p06-09-t1b.log)" -eq 4</automated>
    <fails_when>non-zero exit, the success banner absent, or fewer than four `Total equations:` summaries (an audit did not run — the target must fail on that, per the phase-5 pattern)</fails_when>
  </verify>
  <done>Four contracts validated by the Makefile loop; contract-audit-phase6 is blocking in tier3 and reports zero BIND- findings over four audited contracts.</done>
  <acceptance_criteria>
    - `grep -c 'PHASE6_CONTRACTS' Makefile` is >= 3 (definition, loop, .PHONY/tier3 mention) and `grep -q 'contract-audit-phase6' Makefile` and `sed -n 60p Makefile | grep -q contract-audit-phase6` (or the .PHONY line wherever it lives)
    - `grep -n 'contract-audit-phase6' Makefile` shows a line within 5 lines after the `contract-audit-phase5` tier3 call
    - `grep -c '^- contract: \(forecast-tool-boundary\|prophet-parity\|neuralprophet-parity\|chronos-bolt-parity\)-v1.yaml' contracts/aprender/binding.yaml` equals the total number of `equations:` keys across the four contracts (derive both counts and record them)
    - Every new binding row has `status: implemented`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Re-derive README counts, land the CLAUDE.md exception row and paragraph (D-07), turn both drift gates green</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (lines 38-48: the claims table rows for crates and contracts)
    - CLAUDE.md (lines 205-228: the Realizar-first table and the SetFit exception paragraph; the section header and "Derive the numbers" table at the top)
    - crates/aprender-core/tests/readme_contract.rs (lines 120-180: the exact format strings; 456-503: the backticked-path extraction rules and the >= 20 paths floor)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F7, F8)
  </read_first>
  <action>
    Derive, do not quote: run `cargo metadata --no-deps --format-version 1 | python3 -c "import json,sys;print(len(json.load(sys.stdin)['packages']))"`
    and `find contracts -name '*.yaml' | wc -l`, and write EXACTLY those two numbers into README.md's claims
    rows `| Workspace crates | **N** workspace crates |` and `| Provable contracts | **M** provable contracts |`
    (the test's format strings); update the "Sample" column of CLAUDE.md's derive-the-numbers table for the
    same two facts with today's date if that table exists (it does: "Workspace crates" and "Provable
    contracts" rows) — value AND date. Insert the F8 row into the Realizar-first table directly after the
    SetFit row and the F8 paragraph directly after the SetFit exception paragraph (interfaces block text;
    the final sentence names the two real contract files, not a glob). Before committing, list every
    backticked token you added that ends in .rs/.yaml/.yml/.toml/.sh/.md and `test -e` each. Then run both
    CI-wired drift gates.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --test readme_contract > /tmp/p06-09-t2a.log 2>&1; rc=$?; tail -6 /tmp/p06-09-t2a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-09-t2a.log > /dev/null</automated>
    <fails_when>non-zero exit (a count, a crate README, the link, or a CLAUDE.md path is still wrong) or the summary is not "N passed; 0 failed"</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --test monorepo_invariants > /tmp/p06-09-t2b.log 2>&1; rc=$?; tail -6 /tmp/p06-09-t2b.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed' /tmp/p06-09-t2b.log > /dev/null</automated>
    <fails_when>non-zero exit (FALSIFY-MONO-011 or a sibling invariant red — e.g. the human chose phase6-only-29 in 06-02, which leaves this red by decision; record, do not mask) or not "N passed; 0 failed"</fails_when>
    <automated>N=$(cargo metadata --no-deps --format-version 1 2>/dev/null | python3 -c "import json,sys;print(len(json.load(sys.stdin)['packages']))"); M=$(find contracts -name '*.yaml' | wc -l | tr -d ' '); grep -q "| Workspace crates | \*\*${N}\*\* workspace crates |" README.md && grep -q "\*\*${M}\*\* provable contracts" README.md && grep -q 'Time-Series Forecasting' CLAUDE.md && grep -q 'second deliberate EXCEPTION (Phase 6 D-07)' CLAUDE.md</automated>
    <fails_when>either README row does not match the freshly derived value, or the CLAUDE.md row/paragraph is absent</fails_when>
  </verify>
  <done>Both drift gates exit 0; README counts equal the derivation commands' output; the D-07 row and paragraph are in CLAUDE.md with every cited path present.</done>
  <acceptance_criteria>
    - README.md contains no crate/contract count other than the derived N and M in those two rows (`git diff README.md` touches exactly those two lines and, if present, the sample-date column)
    - CLAUDE.md contains `| Time-Series Forecasting (Prophet / NeuralProphet fit+predict, Chronos-Bolt zero-shot) | **Primary** (` and `contracts/forecast-tool-boundary-v1.yaml` and `contracts/chronos-bolt-parity-v1.yaml` and `crates/aprender-mcp-chronos`
    - `for p in crates/aprender-forecast crates/aprender-mcp-forecast crates/aprender-mcp-chronos contracts/forecast-tool-boundary-v1.yaml contracts/chronos-bolt-parity-v1.yaml; do test -e "$p"; done` exits 0
    - SUMMARY records N and M with the two commands and the date
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply the 06-08 CI decision, record every deferral, file the SmoothL1Loss core ticket, run the closing sweep</name>
  <precondition>`.planning/phases/06-native-time-series-forecasting-stack/06-08-SUMMARY.md` exists and carries exactly one line matching `^CI decision: (apply-now|apply-stdio-only|defer)$` — the bare token alone; 06-08 records the human's reply as typed on a separate `CI decision (verbatim):` line and the runner mount path, if one was given, on a separate `CI mount:` line (06-08 Task 3's blocking human checkpoint was answered — CLAUDE.md makes CI workflow edits check-in-before-acting), and `06-ci-chronos-step.patch` exists; without both, nothing under `.github/` may be edited</precondition>
  <files>.github/workflows/ci.yml, .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md</files>
  <read_first>
    - .planning/phases/06-native-time-series-forecasting-stack/06-08-SUMMARY.md (the bare-token `CI decision:` line — apply-now / apply-stdio-only / defer — plus the `CI decision (verbatim):` line and, if present, the `CI mount:` line naming the runner host path for hunk (a))
    - .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch (the approved hunks)
    - .github/workflows/ci.yml (lines 280-300 the nextest leg; 440-441 the comment "one PR at a time may edit this single physical line without hitting a merge-queue conflict" and line 471 itself) — READ ONLY unless the decision was apply-now or apply-stdio-only
    - .planning/phases/02-deterministic-pair-and-data-protocol/deferred-items.md if present (the D-ITEM entry format the project uses), else the STATE.md "Deferred Items" table format
    - crates/aprender-core/src/nn/loss.rs (lines 171-195: the SmoothL1Loss body to quote in the ticket)
    - .planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md (Deferred Ideas — every item must appear in deferred-items.md)
  </read_first>
  <action>
    (1) CI decision: FIRST read the recorded decision — `grep -Ec '^CI decision: (apply-now|apply-stdio-only|defer)$'`
    on 06-08's SUMMARY must print exactly 1; if it does not, STOP: no human decision is on record, nothing under
    `.github/` may be edited, and the gap goes back to the orchestrator as a checkpoint, not a guess (the human's
    reply as typed is on the separate `CI decision (verbatim):` line — informational; only the bare-token line
    decides). Then read the optional mount line: `grep -E '^CI mount: '` on the same SUMMARY (0 or 1 lines); when
    present, its value is the runner host path the human confirmed for hunk (a). If the decision is `apply-now`,
    copy the patch to `/tmp/p06-09-ci.patch` and — only if a `CI mount:` line exists — replace the placeholder host
    path `/srv/models` in hunk (a)'s `-v /srv/models:/work/models` with that value on the COPY (the committed patch
    file under `.planning/` is not edited); then apply BOTH hunks of the copy to `.github/workflows/ci.yml` with
    `git apply --check` first, then `git apply`; if `apply-stdio-only`, apply only the line-471 hunk (append
    `&& cargo test -p aprender-mcp-forecast --test e2e_stdio` before the trailing `cargo build --examples` — one
    physical line, no reflow; a `CI mount:` line, if any, does not bear on this hunk and is recorded, not used); if
    `defer`, touch nothing under `.github/`. Record the applied hunks (or "none — deferred") and the mount path
    used (or "none given") in deferred-items.md.
    (2) `deferred-items.md` (phase dir): D-ITEM-06-01 Lambda wrapper crates (`aprender-mcp-forecast-lambda`,
    `aprender-mcp-chronos-lambda`) deferred — each adds a `bootstrap` `[[bin]]` to the 06-02 allowlist decision
    and `cargo pmcp deploy`'s `*-lambda` discovery is ambiguous (RESEARCH Pitfall 11); D-ITEM-06-02 core
    `nn::loss::SmoothL1Loss` is detached from the graph (quote loss.rs:171-195) plus a connectivity test for
    every loss in `nn::loss` — filed as a ticket, not fixed here; D-ITEM-06-03 CI embedded-weights leg
    (state the 06-08 outcome; if deferred, the exact step text lives in the patch file); D-ITEM-06-04 the
    Chronos 1e-6 f32 tolerance was frozen on aarch64 with the NEON kernel — an X64 CI leg needs its own
    measurement before the bar is asserted there; D-ITEM-06-05 Chronos-2 tier, `model: auto`, `freq` H,
    country holidays, NP quantile regression / `n_forecasts > 1`, `apr forecast` CLI, pmcp router-lock
    upstream report, parallel-GEMM N-split, streaming safetensors loader, 8x12 NEON tile,
    `test_brick_profiler_reset_v2` flake, opening the NEON upstream PR (human checkpoint), deployment to
    pmcp.run/Lambda — each one line with its CONTEXT/RESEARCH source; D-ITEM-06-06 `tests/e2e_stdio.rs` dark
    in CI unless the 06-08 decision listed it. (3) File the SmoothL1Loss ticket: `gh issue create --title "nn::loss::SmoothL1Loss is detached from the autograd graph (Phase 6 spike 002)" --body-file <tmp>`
    with the loss.rs quote, the `weighted_huber` workaround location, and the proposed fix (compose
    `diff.abs()`, `diff.pow(2.0).mul_scalar(0.5 / beta)`, constant mask) + a connectivity test per loss; if
    `gh` is unauthenticated, record the exact command and body in deferred-items.md instead and say so in
    the SUMMARY (an auth gate, not a failure). (4) Closing sweep, each status captured on its own line:
    `cargo clippy -p aprender-forecast -p aprender-mcp-forecast -p aprender-mcp-chronos --all-targets -- -D warnings`;
    `cargo fmt --all -- --check`; `cargo check --workspace --exclude aprender-profile` (Darwin: STATE.md's
    pre-existing Linux-only compile_error); `cargo nextest run --profile ci -p aprender-forecast -p aprender-mcp-forecast -p aprender-mcp-chronos --lib`
    (CI's leg scoped to the three crates; unarmed — expect `skipped` counts for the weight tests and record
    them); `make contract-validate`; `make contract-audit-phase6`.
  </action>
  <verify>
    <automated>S=.planning/phases/06-native-time-series-forecasting-stack/06-08-SUMMARY.md; test -f "$S" || { echo "06-08 SUMMARY missing — no CI decision on record"; exit 1; }; test "$(grep -Ec '^CI decision: (apply-now|apply-stdio-only|defer)$' "$S")" -eq 1 || { echo "06-08 SUMMARY lacks exactly one bare-token 'CI decision:' line"; exit 1; }; D=$(grep -E '^CI decision: (apply-now|apply-stdio-only|defer)$' "$S" | sed 's/^CI decision: //'); echo "decision=$D"; test "$(grep -Ec '^CI mount: ' "$S")" -le 1 || { echo "06-08 SUMMARY carries more than one 'CI mount:' line"; exit 1; }; M=$(grep -E '^CI mount: ' "$S" | sed 's/^CI mount: //'); echo "mount=${M:-<none>}"; test -s /tmp/p06-09-base.sha || { echo "plan-start SHA missing"; exit 1; }; B=$(cat /tmp/p06-09-base.sha); F=$( { git diff --name-only "$B"..HEAD -- .github; git diff --name-only HEAD -- .github; } | sort -u); case "$D" in defer) test -z "$F" ;; apply-now) test "$F" = ".github/workflows/ci.yml" && grep -q 'chronos-gate' .github/workflows/ci.yml && test "$(grep -c 'e2e_stdio' .github/workflows/ci.yml)" -eq 1 && { test -z "$M" || grep -qF -- "-v $M:/work/models" .github/workflows/ci.yml; } ;; apply-stdio-only) test "$F" = ".github/workflows/ci.yml" && ! grep -q 'chronos-gate' .github/workflows/ci.yml && test "$(grep -c 'e2e_stdio' .github/workflows/ci.yml)" -eq 1 ;; *) exit 1 ;; esac</automated>
    <fails_when>the 06-08 SUMMARY is missing or does not carry exactly one bare-token `CI decision:` line — including a token with trailing text such as a mount path on the same line (no human decision on record — nothing under .github may be edited; the human check-in gate); more than one `CI mount:` line; the plan-start SHA is missing; under `defer` anything under .github changed (working tree, staged, or any commit since plan start); under `apply-*` a file other than ci.yml changed, `e2e_stdio` is not on exactly one line, or the `chronos-gate` step is present/absent contrary to the recorded option; under `apply-now` with a `CI mount:` line, ci.yml does not carry `-v <that path>:/work/models` (the human's path was not substituted for the placeholder)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast -p aprender-mcp-forecast -p aprender-mcp-chronos --all-targets -- -D warnings > /tmp/p06-09-t3a.log 2>&1; rc=$?; tail -3 /tmp/p06-09-t3a.log; test $rc -eq 0 && cargo fmt --all -- --check > /tmp/p06-09-t3b.log 2>&1; rc2=$?; test $rc2 -eq 0 && CARGO_INCREMENTAL=0 cargo check --workspace --exclude aprender-profile > /tmp/p06-09-t3c.log 2>&1; rc3=$?; tail -3 /tmp/p06-09-t3c.log; test $rc3 -eq 0</automated>
    <fails_when>any of clippy / fmt --check / workspace check exits non-zero (SC5 lint gate)</fails_when>
    <automated>env -u CHRONOS_MODEL_DIR CARGO_INCREMENTAL=0 cargo nextest run --profile ci -p aprender-forecast -p aprender-mcp-forecast -p aprender-mcp-chronos --lib > /tmp/p06-09-t3d.log 2>&1; rc=$?; tail -8 /tmp/p06-09-t3d.log; test $rc -eq 0 && grep -Eq 'Summary \[.*\] [1-9][0-9]* tests? run: [1-9][0-9]* passed(, [0-9]+ skipped)?' /tmp/p06-09-t3d.log && grep -Eq '[1-9][0-9]* skipped' /tmp/p06-09-t3d.log</automated>
    <fails_when>non-zero exit; no nextest Summary with a non-zero passed count; or ZERO skipped while unarmed (a weight-dependent test ran or passed vacuously — D-18)</fails_when>
    <automated>test -f .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md && grep -q 'SmoothL1Loss' .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md && grep -q 'D-ITEM-06-03' .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md && grep -qi 'lambda' .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md && grep -qi 'chronos-2' .planning/phases/06-native-time-series-forecasting-stack/deferred-items.md</automated>
    <fails_when>the deferred-items file is missing or lacks any of the four named entries</fails_when>
  </verify>
  <done>CI decision applied exactly; every CONTEXT deferral and every research open item recorded with a source; the SmoothL1Loss ticket filed (or its text recorded behind an auth gate); the three crates pass clippy/fmt/check/nextest with counted skips visible.</done>
  <acceptance_criteria>
    - Under `defer`: both `git diff --stat HEAD -- .github` and `git diff --stat <plan-start>..HEAD -- .github` are empty; under `apply-*`: the only changed file under `.github` (working tree and plan-start..HEAD) is `ci.yml`, its diff is exactly the approved hunk(s) (with the `CI mount:` path in place of `/srv/models` when that line exists), and `grep -c 'e2e_stdio' .github/workflows/ci.yml` is 1; the SUMMARY quotes 06-08's `CI decision:` line, its `CI decision (verbatim):` line and its `CI mount:` line (or "none given") verbatim
    - deferred-items.md contains `D-ITEM-06-01` .. `D-ITEM-06-06`, `SmoothL1Loss`, `aprender-mcp-forecast-lambda`, `apr forecast`, `model: auto`, `Chronos-2`, `test_brick_profiler_reset_v2`
    - SUMMARY records the `gh issue create` URL or the auth-gate fallback, and the nextest Summary line (passed / skipped counts)
    - `.planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md` (06-08) is referenced from the SUMMARY as the SC1/SC4/SC5 host-gated evidence
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Makefile / binding registry -> tier3 gate | what the repo's quality gate reaches |
| docs (README, CLAUDE.md) -> readme_contract | claims the CI-wired gate checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Repudiation | contracts validated by nothing | high | mitigate | `$(CONTRACTS)` + `PHASE6_CONTRACTS` + blocking `contract-audit-phase6` with the empty-list and `Total equations:` guards |
| T-06-18 | Repudiation | README counts drift | medium | mitigate | counts pasted from the derivation commands; the verify re-derives and compares |
| T-06-16 | Elevation of Privilege | CI workflow edit | high | mitigate | only the hunks the human approved in 06-08 are applied, via `git apply --check` first |
| T-06-SC | Tampering | cargo installs | high | accept | none |
</threat_model>

<verification>
- `make contract-validate` echoes all four Phase 6 files and exits 0; `make contract-audit-phase6` reports 4 audited, zero BIND-
- `cargo test -p aprender-core --test readme_contract` and `--test monorepo_invariants` exit 0
- clippy/fmt/check green on the three crates; nextest lib leg green with counted skips unarmed
- CI decision read from 06-08's single bare-token `CI decision:` line (runner mount path, if any, from its `CI mount:` line) and applied exactly (the first Task 3 verify gates every ci.yml edit on that line and on the plan-start..HEAD + working-tree `.github` diff); deferred-items complete
</verification>

<success_criteria>
- SC5: every gate green — both servers' e2e, the workspace lib tests for the new crates, clippy -D warnings, fmt, pv validate on every new contract, plus the two drift gates
- D-07 row landed with existing paths; D-15 contracts wired and bound; deferrals recorded with sources
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-09)
Makefile: `PHASE6_CONTRACTS`, target `contract-audit-phase6` (tier3, .PHONY), four `$(CONTRACTS)` entries. `contracts/aprender/binding.yaml`: one row per Phase 6 equation. README.md: re-derived counts. CLAUDE.md: the Forecasting Realizar-first row and exception paragraph. New file `.planning/phases/06-native-time-series-forecasting-stack/deferred-items.md` (D-ITEM-06-01..06). Possibly `.github/workflows/ci.yml` hunks (human-approved in 06-08). A GitHub issue for `SmoothL1Loss` (or its text in deferred-items).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-09-SUMMARY.md` when done. Record N and M with their commands, the binding-row count vs equation count, both drift-gate summary lines, the nextest Summary line, the CI decision as applied, and the issue URL or auth-gate note.
</output>
