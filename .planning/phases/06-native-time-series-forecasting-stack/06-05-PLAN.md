---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-04]
files_modified:
  - justfile
  - Cargo.lock
  - crates/aprender-forecast/Cargo.toml
  - crates/aprender-forecast/README.md
  - crates/aprender-forecast/build.rs
  - crates/aprender-forecast/src/lib.rs
  - crates/aprender-forecast/src/bolt.rs
  - crates/aprender-forecast/src/safetensors.rs
  - crates/aprender-forecast/src/chronos.rs
  - crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json
  - contracts/chronos-bolt-parity-v1.yaml
autonomous: false
requirements: [SC4]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`just fetch-chronos-tiny` downloads amazon/chronos-bolt-tiny at revision a0e552de83495b5c28c14c71c374f3e33280b340 into the gitignored /models/chronos-bolt-tiny/f32, verifies model.safetensors sha256 75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32 and config.json sha256 278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0, aborts on mismatch, and derives f16 into /models/chronos-bolt-tiny/f16 (D-18: weights never committed, pinned)"
    - "The recipe VERIFIES ON EVERY RUN, never only on download: with all files already present it re-hashes the f32 model, the config AND the derived f16, exits 0 when all three match their pins, and exits non-zero naming the offending file when one does not — so a pre-existing, cached or mounted weight file can never be used unverified (REVIEW-06-03, verified MEDIUM-HIGH: 06-08's chronos-gate previously invoked the recipe only when the f32 model was missing, and the sha256 checks live inside it). A tamper control proves the failing direction: a copy dir with one flipped byte makes the recipe exit non-zero"
    - "A weight-dependent test never passes vacuously: build.rs emits cfg(chronos_weights) only when CHRONOS_MODEL_DIR/model.safetensors exists; without it the run prints `N ignored` with the arming reason, with it the same tests run (D-18; RESEARCH F6 probe)"
    - "aprender_forecast::bolt reproduces chronos-forecasting 2.3.1: Peyton 64-step quantiles within 1e-6 absolute (f32), the six edge probes with identical attention masks, the 365-step 9-path re-quantiled rollout within the contract bar, f16 weights within 2 % of the series std (D-04, SC4)"
    - "Every multi-row product goes through trueno::blis::gemm_blis and single rows through dot8; the rayon branch is gone; aprender-compute is not modified (D-14, D-05). At the production defaults `Bolt::load` sets (`fast = true, attn_gemm = true, row1_gemv = false`) a test PROVES the single-row route is dot8 and not gemv — a grep showing `dot8` exists does not prove exclusive routing (REVIEW-06-04, codex HIGH downgraded to LOW by the orchestrator: the toggle survives in the shipped struct)"
    - "The shipped `Bolt` keeps BOTH weight layouts and that is deliberate, documented and measured: the row-major `[out, in]` copies (`q/k/v/o`, `wi/wo`, `wh/wo/wr`) feed `dot8` on single rows, the transposed `[in, out]` copies (`qt/kt/vt/ot`, `wit/wot`, `wht/wot/wrt`) feed `gemm_blis` on multi-row products. D-13's sentence 'Only transposed [in, out] weights are kept in memory' is AMENDED — as written it is incompatible with D-14 ('single rows through the 8-accumulator dot'), because `dot8` requires contiguous `[out, in]` rows. D-14 and the measured 9.54e-7 parity win; the memory cost is stated, not hidden — a test reports the total f32 elements held so the ~2x weight-array residency is a recorded number, and the SC4 '< 30 MB' bar is BINARY SIZE (embedded f16 bytes), which the dual in-memory layout does not touch (REVIEW-06-01, verified HIGH). GOVERNANCE: D-13 is a LOCKED CONTEXT decision, so the amendment is not the planner's to make alone — Task 2 is a blocking human `checkpoint:decision` that ratifies it (`amend-memory-clause`, recommended) or refuses it (`drop-untransposed`, under which this plan HALTS before Task 3 because the frozen 1.0e-6 parity bar was measured under the routing being removed and must be re-measured). The truth above holds under `amend-memory-clause`; the outcome is recorded in this plan's SUMMARY and as `D-ITEM-06-08` in `deferred-items.md` (06-09 Task 3)"
    - "The safetensors decoder decodes F32/F16/BF16 from one byte slice shared by embedded and on-disk weights"
    - "The Chronos door (chronos::forecast) refuses < 4 points, > 20 000 points, horizon 0 or > 1 024, horizon > 64 without allow_long_horizon (message names allow_long_horizon), all-null y, impossible dates — as ForecastError::Validation — and carries a warning naming the rollout when the flag is used (D-11, D-13)"
    - "Every Chronos tolerance is read from contracts/chronos-bolt-parity-v1.yaml, which validates with 0 errors and non-zero counts (D-15)"
    - "The f32 quantile bar is ARCHITECTURE-KEYED, and the aarch64 bar is unchanged: `quantiles_abs_f32` stays 1.0e-6 (the SC4 literal, measured 9.54e-7 on aarch64 with the spike-008 NEON kernel) and a second equation `quantiles_abs_f32_nonaarch64` carries a PROVISIONAL 5.0e-6 that the test selects on `!cfg!(target_arch = \"aarch64\")`, labelled in the contract as headroom for a different GEMM accumulation order and NOT as evidence — the first x86_64 run must record its measured max|delta| and tighten the bar to that measurement in a `pv diff`-visible edit (REVIEW-06-02, verified premises HIGH: all four CI jobs are `[self-hosted, X64, Linux, clean-room]` at .github/workflows/ci.yml:96,516,988,1039, and 1.0e-6 over 9.54e-7 is ~4.6 % margin)"
  artifacts:
    - path: "justfile"
      provides: "fetch-chronos-tiny recipe with the pinned revision and sha256s"
      contains: "a0e552de83495b5c28c14c71c374f3e33280b340"
    - path: "crates/aprender-forecast/build.rs"
      provides: "cargo::rustc-check-cfg=cfg(chronos_weights) + conditional cargo:rustc-cfg=chronos_weights"
      contains: "rustc-check-cfg=cfg(chronos_weights)"
    - path: "crates/aprender-forecast/src/bolt.rs"
      provides: "Chronos-Bolt T5 forward: Config, dot8, linear_fast (gemm_blis), t5_layer_norm, relative_bucket, transpose, Bolt::{load, forward, predict, forward_stages}, torch_quantile, 9-path rollout"
      contains: "trueno::blis::gemm_blis"
    - path: "crates/aprender-forecast/src/safetensors.rs"
      provides: "load_bytes(&[u8]) -> (Weights, dtype) for F32/F16/BF16"
      contains: "pub fn load_bytes"
    - path: "crates/aprender-forecast/src/chronos.rs"
      provides: "Model, ModelLoadError, load_model_from_bytes, load_model_from_dir, ChronosArgs, ChronosResponse, validate, forecast, load_csv; CHRONOS_MIN_POINTS/MAX_POINTS/MAX_HORIZON"
      contains: "pub fn forecast"
    - path: "crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json"
      provides: "the committed 1.1 KB config.json (NOT weights) so weights-free validation tests can build a Config"
      contains: "prediction_length"
    - path: "contracts/chronos-bolt-parity-v1.yaml"
      provides: "frozen Chronos tolerances, horizon policy, embedding/dtype invariants"
      contains: "quantiles_abs_f32"
  key_links:
    - from: "crates/aprender-forecast/src/bolt.rs"
      to: "crates/aprender-compute/src/blis/compute.rs"
      via: "gemm_blis(m, n, k, a, b, c, None) for rows > 1; dot8 for rows == 1"
      pattern: "gemm_blis\\("
    - from: "crates/aprender-forecast/src/chronos.rs"
      to: "crates/aprender-forecast/src/bolt.rs"
      via: "Model { bolt: Bolt, .. } ; forecast() calls model.bolt.predict(&context, horizon)"
      pattern: "bolt\\.predict\\("
    - from: "crates/aprender-forecast/build.rs"
      to: "crates/aprender-forecast/src/bolt.rs"
      via: "#[cfg_attr(not(chronos_weights), ignore = \"...run `just fetch-chronos-tiny` to arm\")] on every weight-dependent test"
      pattern: "cfg_attr\\(not\\(chronos_weights\\)"
    - from: "justfile"
      to: "models/chronos-bolt-tiny/"
      via: "hf_hub_download at the pinned revision + sha256 check + numpy f16 derivation"
      pattern: "hf_hub_download"
  prohibitions:
    - statement: "Never commit weights: *.safetensors is globally gitignored and /models/ root-anchored; only the 1.1 KB config.json is committed as a fixture (D-18)"
      status: resolved
      verification: test
    - statement: "Never download weights from the Hub at runtime or cold start (chronos-mcp-server.md) — fetch is a just recipe, never on a request path"
      status: resolved
      verification: judgment
    - statement: "Never trueno::Matrix::matmul or plain loops for MULTI-ROW products; never the rayon blis::gemm path in the servers (D-14). Single rows deliberately DO use the plain `linear`/`dot8` path — that IS D-14's second clause, not a violation of its first"
      status: resolved
      verification: test
    - statement: "Do not delete the untransposed `[out, in]` weight copies to satisfy D-13's memory sentence — `dot8` needs contiguous `[out, in]` rows, so deleting them forces every single-row product onto `gemv` (violating D-14) and changes the float accumulation order the 9.54e-7 parity measurement was taken under (REVIEW-06-01). This prohibition is CONDITIONAL on Task 2's blocking human decision returning `amend-memory-clause`; if the human returns `drop-untransposed` the prohibition is lifted and the plan halts for a re-plan and a re-measurement rather than proceeding"
      status: resolved
      verification: test
    - statement: "Do not port the median-only rollout — the 9-path re-quantiled scheme is chronos-forecasting 2.3.1 (off by 0.5 otherwise)"
      status: resolved
      verification: test
    - statement: "Do not subtract the mean in T5 layer norm or scale attention scores by 1/sqrt(d_kv)"
      status: resolved
      verification: judgment
    - statement: "Do not modify crates/aprender-compute (the spike-008 kernel is assumed, not changed; opening its upstream PR is a human checkpoint, D-05)"
      status: resolved
      verification: test
    - statement: "Never the println-then-return skip style for weight-dependent tests — it reports 0 ignored (D-18)"
      status: resolved
      verification: test
    - statement: "Chronos-2 is deferred (CONTEXT): no chronos2.rs, no 21-quantile path in this phase"
      status: resolved
      verification: judgment
---

<objective>
Land the Chronos-Bolt zero-shot forward and its door in `aprender-forecast` (D-07: the numeric path lives in
the library), with the weights mechanism D-18 demands: a pinned, sha256-checked `just fetch-chronos-tiny`
into the gitignored `/models/`, a `build.rs` that turns weight presence into a compile-time cfg so
weight-dependent tests are COUNTED skips when unarmed and real tests when armed, and the Bolt parity ladder
(SC4) against the committed chronos-forecasting 2.3.1 fixtures with every tolerance frozen in
`contracts/chronos-bolt-parity-v1.yaml` (D-15).

Purpose: the server crate (06-07) is transport only; everything it embeds and calls must already be proven
here, and the skip semantics must be proven in both directions before any CI leg depends on them.
Output: fetch recipe, build.rs, three ported/derived modules, the committed config fixture, one contract,
the gated ladder, the two-sided skip proof.

**THIS PLAN IS WAVE 3 AND RUNS ALONE.** It was Wave 2 alongside 06-03 and 06-04; the `--reviews` replan moved
it behind `06-04` because both plans append to the same `pub mod` block in
`crates/aprender-forecast/src/lib.rs` (this plan adds `bolt`, `safetensors`, `chronos`; 06-04 adds `np`), and
a shared file is a real dependency rather than a scheduling preference. The wave also shares one Cargo target
directory and one disk at 94 % (55 GiB free) after three prior ENOSPC halts, and this plan additionally
writes ~85 MB of weights into `models/`. Task 1's precondition still halts if another cargo build is running
(REVIEW-06-05 — the instruction, not a note).

**Task 2 is a blocking human `checkpoint:decision`, not executable work.** It ratifies (or refuses) the D-13
memory-clause amendment before Task 3 ports `bolt.rs`, exactly as 06-02 gates the FALSIFY-MONO-011 allowlist
and 06-08 gates the ci.yml leg. It is counted in `estimate.tasks: 4` for consistency with those two plans, but
it costs the executor no tokens — the 95 000 projection is unchanged and still covers three executable tasks
(1, 3, 4). If Task 1 (fetch recipe + build.rs + config fixture) overruns its share, the split candidate is
**Task 4** (the contract plus the gated Bolt parity ladder), lifted whole into its own wave-3 plan. Do NOT
thin the parity ladder or drop rungs to fit — the ladder's rigor is the phase's only evidence for SC4, and a
shortened ladder buys context by deleting the proof.
</objective>

<review_dispositions>
## Cross-AI review dispositions carried by this plan (06-REVIEWS.md, `plans_commit: 88da44dcd`)

**REVIEW-06-01 — INCORPORATED (verified HIGH, the review's top surviving finding).** Codex: the plan's
must-have truth "only transposed `[in, out]` weights are kept" is false of `sources/007-.../src/bolt.rs:102-105`,
which this plan instructs an executor to port verbatim (`Attn` holds `q/k/v/o` AND `qt/kt/vt/ot`; `Block`
holds `wi/wo` AND `wit/wot`; `Residual` holds `wh/wo/wr` AND `wht/wot/wrt`). The reviewers offered two exits —
drop the untransposed copies, or restate the truth. **Restate**, because dropping them is not available:
`proj(fast = true, row1_gemv = false, rows == 1)` routes to `linear()`, whose inner loop is `dot8` over
contiguous `[out, in]` rows (bolt.rs:20-31, 66-73). Deleting the untransposed copies would force single rows
onto `gemv` over the transposed copy — that violates D-14's own second clause AND changes the float
accumulation order under which the 9.54e-7 parity number was measured. So D-13's memory sentence is amended
in the truth above, in the contract (`weights_dual_layout_documented` replaces `weights_transposed_only`) and
in 06-07's prohibition list; Task 3 adds a test that STATES the resident element count instead of hiding it,
and Task 3 adds the routing proof the grep could never give (REVIEW-06-04). The amendment itself is ratified
by Task 2's blocking human `checkpoint:decision` — a locked CONTEXT decision is not the planner's to rewrite.

**REVIEW-06-02 — INCORPORATED (verified premises, HIGH).** The `1.0e-6` f32 quantile bar has ~4.6 % margin
over the aarch64-measured `9.54e-7`, and every CI job is x86_64, where `gemm_blis` does not run the NEON
microkernel and inner-product accumulation order differs across a 12-layer T5. The aarch64 bar is NOT
loosened (SC4 names 1e-6). Task 4 adds `quantiles_abs_f32_nonaarch64: 5.0e-6`, explicitly labelled
PROVISIONAL-UNMEASURED, selected by `cfg!(target_arch)`, with a proof obligation that the first x86_64 run
records its measured value and tightens the bar. 06-08's human checkpoint gains the quantified argument and
a `measure-x86-first` option so the decider can see the cheap mitigation.

**REVIEW-06-03 — INCORPORATED (verified MEDIUM-HIGH).** The fetch recipe is now verify-always, not
fetch-if-missing, it hashes the derived f16 as well, and it carries a tamper control. 06-08's `chronos-gate`
calls it unconditionally.

**REVIEW-06-05 — INCORPORATED.** This plan left Wave 2 for Wave 3 on a declared `06-04` dependency (the
shared `lib.rs` module block), so the executor cannot run it beside the plan it conflicts with; Task 1's
precondition additionally halts on any concurrent cargo build.

**REJECTED — the `cargo test -- a b` multi-filter finding (codex HIGH). REFUTED BY EXPERIMENT; do not
"fix" it.** Codex claims Task 4's `cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity`
passes only `bolt::parity` as a filter and forwards `chronos::parity` as an unsupported free argument, and
that the same defect sits in 06-08's `chronos-gate`. The orchestrator ran
`cargo test --lib -- alpha:: beta::` on cargo 1.98.0: both ran, and the summary read
`2 passed; 2 filtered out`. Modern libtest UNIONS multiple positional filters. **The two-filter commands
stay exactly as written in Task 4 and in `just chronos-gate`.** They are already non-vacuity-guarded: the
armed run asserts `>= 8` matching `^test (bolt|chronos)::parity::` lines, so if a filter were ever silently
dropped the count check goes red — which is the honest way to hold this, rather than splitting one command
into two on refuted evidence.
</review_dispositions>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F3 gemm_blis signature, F6 in full, F9, Pitfall 7, Pitfall 8, "Weight fetch recipe" code example, Assumptions A2/A3)
@.planning/phases/06-native-time-series-forecasting-stack/06-PATTERNS.md  (sections: prophet/np/bolt/safetensors/dates; build.rs; aprender-mcp-chronos lib.rs; justfile; Counted skip)
@.claude/skills/spike-findings-aprender/references/chronos-zero-shot-port.md
@.claude/skills/spike-findings-aprender/references/chronos-mcp-server.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md

<interfaces>
<!-- Frozen tolerances (spike-005/007 RUN-OUTPUT measured values in parentheses) -->
equations.<name>.float_tolerance:
  quantiles_abs_f32              1.0e-6   (Peyton 64 steps 9.54e-7 f32, also 9.54e-7 through the spike-007 server — SC4 literal; ~4.6 % margin, on aarch64 with the spike-008 kernel; ASSERTED ONLY WHERE MEASURED)
  quantiles_abs_f32_nonaarch64   5.0e-6   (PROVISIONAL, UNMEASURED — headroom for a different GEMM accumulation order where the NEON microkernel does not dispatch; every CI job is [self-hosted, X64, Linux, clean-room] at .github/workflows/ci.yml:96,516,988,1039. The first x86_64 run records its measured max|delta| and tightens this bar in a pv diff-visible edit — REVIEW-06-02)
  quantiles_rel_scale            4.0e-6   (max|Δ| / series scale for non-Peyton series; air 1.83e-4 at scale 119 = 1.5e-6)
  loc_scale_rel                  2.0e-6   (loc diff / |loc| and scale diff / scale; peyton 9.5e-7 / 1.8e-7; air scale 1.5e-5 at 119)
  hidden_states_abs              2.0e-5   (input_embeds <= 6.0e-7, encoder <= 1.4e-6, decoder hidden <= 7.6e-6)
  probe_quantiles_rel_scale      5.0e-6   (five_points 3.81e-6 abs / scale; huge_scale 1.00 abs at 1e6 = 1e-6; nan_gaps 1.91e-6; tiny_len_130 2.86e-6; air_rollout_24 1.22e-4 / 119)
  probe_constant_abs             1.0e-5   (constant series: scale floors to 1e-5 so relative is meaningless; 9.54e-7 abs measured)
  rollout_365_abs                5.0e-5   (1.72e-5 in spike 005; 1.91e-5 through the spike-007 server)
  f16_rel_std                    0.02     (SC4 literal; measured 9.55e-4 = 0.11 % of std)
constants: min_points 4, max_points 20000, max_horizon 1024, native_horizon 64, context_length 2048, quantile_levels 9, rollout_forwards_for_365 46
Weights pin: repo amazon/chronos-bolt-tiny, revision a0e552de83495b5c28c14c71c374f3e33280b340; f32 model.safetensors sha256 75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32 (33.0 MB); config.json sha256 278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0; derived tiny-f16 model.safetensors sha256 f9a033b42bc516e17ae5756317cb946121afdb59c94b4acfcd30fef93317cd4c (ADVISORY — re-derived, not downloaded; RESEARCH A3)
Fixture keys: chronos_bolt_tiny_fixture.json {config{...}, series{peyton, air, short100}{...loc, scale, attention_mask, input_embeds, encoder hidden, decoder hidden, quantiles_64, n_patches_plus_reg...}}; chronos_probes.json {chronos_version, cases{nan_gaps, five_points, constant, huge_scale, tiny_len_130_rollout, air_rollout_24}}; peyton_tiny_oracle.json {source, n, loc, scale, quantile_levels, quantiles_64, quantiles_365, torch_seconds_64, torch_seconds_365}
gemm_blis (verified): pub fn gemm_blis(m: usize, n: usize, k: usize, a: &[f32], b: &[f32], c: &mut [f32], profiler: Option<&mut BlisProfiler>) -> Result<(), TruenoError>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `just fetch-chronos-tiny` (pinned, sha256-checked, f16 derived) + build.rs cfg emission + the committed config fixture</name>
  <precondition>Either network access to huggingface.co, or the local HF cache snapshot ~/.cache/huggingface/hub/models--amazon--chronos-bolt-tiny/snapshots/a0e552de83495b5c28c14c71c374f3e33280b340/ exists (RESEARCH F6 verified it does on this box); `uv --version` and `just --version` succeed</precondition>
  <precondition>No Wave 2 plan (06-03, 06-04) is still executing: `pgrep -fl 'cargo (test|build|check|clippy|nextest)' | grep -v $$ | wc -l` is 0 before this task starts, and `df -g . | awk 'NR==2{print $4}'` reports at least 20 GiB available — this plan alone writes ~85 MB of weights plus a full debug build tree (REVIEW-06-05). Halt and report if either fails.</precondition>
  <files>justfile, Cargo.lock, crates/aprender-forecast/Cargo.toml, crates/aprender-forecast/build.rs, crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json, crates/aprender-forecast/README.md</files>
  <read_first>
    - justfile (lines 1-50: header contract "deployment surface", `set shell := ["bash", "-uc"]`, variable style, the shebang-recipe form at ~70)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F6 in full; "Weight fetch recipe" under Code Examples — the exact Python body; A2/A3 assumptions)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/tools/to_f16.py (the f16 derivation the recipe reproduces)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/build.rs (23 lines) and crates/aprender-train/build.rs (line 75: the in-tree rustc-check-cfg emitter)
    - .gitignore (lines 48-53: `*.safetensors` global, `/models/` root-anchored)
    - crates/aprender-forecast/Cargo.toml and README.md (as written by 06-01)
  </read_first>
  <action>
    (0) Before any edit, record the plan-start SHA once: `test -s /tmp/p06-05-base.sha || git rev-parse HEAD > /tmp/p06-05-base.sha`
    — Task 3's aprender-compute guard diffs the committed range from it as well as the working tree, so a
    change committed by an earlier task of this plan cannot slip past a working-tree-only diff.
    (1) justfile: add variables `chronos_rev := "a0e552de83495b5c28c14c71c374f3e33280b340"`,
    `chronos_dir := "models/chronos-bolt-tiny"` (RELATIVE — the `git check-ignore` and `git status` gates
    below need the repo-relative form) and `chronos_abs := justfile_directory() / chronos_dir` near the
    existing variables. Use `{{chronos_abs}}` wherever an ABSOLUTE path is required (the `CHRONOS_MODEL_DIR`
    / `CHRONOS_EMBED_DIR` values, the printed hints) — never `$PWD/{{chronos_dir}}`, because `$PWD` follows
    any `cd` inside a recipe body while `justfile_directory()` is fixed at the workspace root
    (REVIEW-06-U5, gemini LOW). Then a shebang recipe `fetch-chronos-tiny` implementing RESEARCH's Code
    Example: `uv run --python 3.12 --with huggingface_hub --with safetensors --with numpy python - "{{chronos_rev}}" "{{chronos_dir}}"`
    with the Python body that downloads `model.safetensors` and `config.json` via `hf_hub_download(repo_id, filename, revision=rev, local_dir=<f32 dir>)`
    ONLY WHEN THEY ARE ABSENT, and then — on EVERY run, download or not — verifies:
      (a) f32 `model.safetensors` sha256 == `75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32`,
      (b) f32 `config.json` sha256 == `278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0`,
      (c) the derived f16 `model.safetensors` sha256 == `f9a033b42bc516e17ae5756317cb946121afdb59c94b4acfcd30fef93317cd4c`.
    On (a) or (b) mismatch: `sys.exit` non-zero with a message naming the file, the expected sha and the
    computed sha — never a warning, never a silent overwrite, never a re-download over a mismatching file
    (a mismatch is a supply-chain event, not a cache miss). On (c) mismatch OR an absent f16: DELETE the f16
    directory and re-derive it deterministically from the ALREADY-VERIFIED f32 (convert every tensor to
    float16 with `safetensors.numpy`, metadata `format: pt`, `converted: f32->f16`, copy `config.json`
    beside it), then re-hash; if it still differs from (c), exit non-zero. This is REVIEW-06-03: the whole
    point is that a pre-existing, cached or CI-mounted file is verified rather than trusted, so the recipe
    is verify-always and the caller (06-08's `chronos-gate`) invokes it unconditionally. Print each
    computed sha next to its pin, and print the `CHRONOS_MODEL_DIR={{chronos_abs}}/f16` /
    `CHRONOS_EMBED_DIR=...` hints. Do not let `set -e` leak into callers (it is a recipe, not a sourced
    lib — `set -euo pipefail` inside the shebang is correct here). A header comment states: never run from
    CI's default gate; weights are Apache-2.0; `/models/` is gitignored; and that the f16 sha is
    RE-DERIVED locally, so (c) proves local integrity, not upstream provenance — (a) is what pins upstream.
    Run it once. Then copy `models/chronos-bolt-tiny/f32/config.json` to
    `crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json` (it is 1.1 KB of hyper-parameters,
    not weights — D-18 permits it; its sha is asserted by a test in Task 4).
    (2) `crates/aprender-forecast/build.rs` (auto-discovered; no manifest key needed): print
    `cargo::rustc-check-cfg=cfg(chronos_weights)`, `cargo:rerun-if-env-changed=CHRONOS_MODEL_DIR`, and
    `cargo:rustc-cfg=chronos_weights` iff `CHRONOS_MODEL_DIR` is set and `<dir>/model.safetensors` is a file
    (RESEARCH F6 snippet verbatim). It joins no `..` so `scripts/check_build_rs_paths.sh` skips it.
    (3) `crates/aprender-forecast/Cargo.toml`: add `half = { workspace = true }` (F16/BF16 decode in Task 2; `Cargo.lock`
    re-resolves the crate's dependency list — `half` is already in the lock, so no new registry package appears).
    (4) README.md: add a "Chronos-Bolt weights" section — the repo id, revision, both pinned sha256s, the
    advisory f16 sha, the Apache-2.0 license, `just fetch-chronos-tiny`, and the two env vars with their
    roles (`CHRONOS_MODEL_DIR` = runtime path + test arming; `CHRONOS_EMBED_DIR` = build-time embedding in
    the server crate).
  </action>
  <verify>
    <automated>just fetch-chronos-tiny > /tmp/p06-05-t1a.log 2>&1; rc=$?; tail -5 /tmp/p06-05-t1a.log; test $rc -eq 0 && test -f models/chronos-bolt-tiny/f32/model.safetensors && test -f models/chronos-bolt-tiny/f16/model.safetensors && test "$(shasum -a 256 models/chronos-bolt-tiny/f32/model.safetensors | cut -c1-64)" = "75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32" && test "$(shasum -a 256 models/chronos-bolt-tiny/f32/config.json | cut -c1-64)" = "278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0"</automated>
    <fails_when>non-zero recipe exit (download/sha failure), a missing file, or either sha256 differing from the pin</fails_when>
    <automated>git status --porcelain -- models/ > /tmp/p06-05-t1b.log 2>&1; rc=$?; test $rc -eq 0 && test ! -s /tmp/p06-05-t1b.log && git check-ignore -q models/chronos-bolt-tiny/f32/model.safetensors</automated>
    <fails_when>git status lists anything under models/ (weights would be committable), or check-ignore exits non-zero (the ignore rules do not cover the weights)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo build -p aprender-forecast > /tmp/p06-05-t1c.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t1c.log; test $rc -eq 0 && ! grep -q 'unexpected_cfgs' /tmp/p06-05-t1c.log && test -f crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json</automated>
    <fails_when>build fails, or an `unexpected_cfgs` warning appears (the check-cfg line is missing), or the config fixture was not committed</fails_when>
    <automated>just fetch-chronos-tiny > /tmp/p06-05-t1d.log 2>&1; rc=$?; echo "second run rc=$rc"; test $rc -eq 0 && test "$(grep -c '75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32' /tmp/p06-05-t1d.log)" -ge 1 && test "$(grep -c 'f9a033b42bc516e17ae5756317cb946121afdb59c94b4acfcd30fef93317cd4c' /tmp/p06-05-t1d.log)" -ge 1 && ! grep -qi 'download' /tmp/p06-05-t1d.log</automated>
    <fails_when>the SECOND, all-files-present run exits non-zero (not idempotent); or its output does not echo the f32 and f16 sha256 pins (it skipped verification instead of re-hashing — the exact REVIEW-06-03 defect: a cached file used unverified); or it downloaded again</fails_when>
    <automated>T=$(mktemp -d); mkdir -p "$T/f32" "$T/f16"; cp models/chronos-bolt-tiny/f32/model.safetensors models/chronos-bolt-tiny/f32/config.json "$T/f32/"; cp models/chronos-bolt-tiny/f16/model.safetensors models/chronos-bolt-tiny/f16/config.json "$T/f16/"; printf 'X' | dd of="$T/f32/model.safetensors" bs=1 seek=4096 conv=notrunc status=none; just chronos_dir="$T" fetch-chronos-tiny > /tmp/p06-05-t1e.log 2>&1; rc=$?; tail -6 /tmp/p06-05-t1e.log; rm -rf "$T"; test $rc -ne 0 && grep -qi 'sha256\|mismatch' /tmp/p06-05-t1e.log</automated>
    <fails_when>the tamper run exits 0, or its output never names a sha256 mismatch — a flipped byte in an already-present f32 weight file was accepted, which is precisely the trust-boundary hole REVIEW-06-03 reported. (The control runs entirely inside a mktemp copy dir passed as `chronos_dir`; the real `models/` tree is never modified, and the dir is removed before the status is read.)</fails_when>
  </verify>
  <done>Weights present locally and ignored by git; all three shas verified on a files-present re-run; a flipped byte makes the recipe fail; build.rs emits the cfg cleanly; the config fixture is committed; README documents the pin.</done>
  <acceptance_criteria>
    - `justfile` contains `fetch-chronos-tiny:`, `a0e552de83495b5c28c14c71c374f3e33280b340`, `75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32`, `278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0`, `f9a033b42bc516e17ae5756317cb946121afdb59c94b4acfcd30fef93317cd4c`, `hf_hub_download`, `float16`
    - `justfile` contains `chronos_abs := justfile_directory()` and `! grep -q '\$PWD/{{chronos_dir}}' justfile` (REVIEW-06-U5)
    - The recipe's Python body hashes all three files on every invocation: `grep -c 'sha256' justfile` is >= 3 and the verification block is NOT nested inside the download branch (read it — the download is conditional, the hashing is not)
    - `crates/aprender-forecast/build.rs` contains `cargo::rustc-check-cfg=cfg(chronos_weights)`, `rerun-if-env-changed=CHRONOS_MODEL_DIR`, `cargo:rustc-cfg=chronos_weights`, and no `".."`
    - `crates/aprender-forecast/Cargo.toml` contains `half = { workspace = true }`
    - `shasum -a 256 crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json | cut -c1-64` prints `278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0`
    - `crates/aprender-forecast/README.md` contains `amazon/chronos-bolt-tiny`, `Apache-2.0`, `CHRONOS_MODEL_DIR`, `CHRONOS_EMBED_DIR`
    - `ls models/chronos-bolt-tiny/f16/` lists `model.safetensors` and `config.json`; the recipe's printed f16 sha is recorded in the SUMMARY next to the advisory `f9a033b4...` value
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: DECISION — D-13's memory clause vs D-14's single-row routing (decided before Task 3 ports bolt.rs)</name>
  <decision>Whether D-13's sentence "Only transposed `[in, out]` weights are kept in memory" is AMENDED so the shipped `Bolt` keeps both weight layouts, or held as written</decision>
  <context>
    D-13 and D-14 are both LOCKED decisions in 06-CONTEXT.md and, as written, they cannot both hold. D-13's last
    sentence says only the transposed `[in, out]` copies are resident. D-14's second clause says "single rows
    through the 8-accumulator dot" — and `dot8` reads contiguous `[out, in]` rows (`linear()`, sources/007
    bolt.rs:20-31, 66-73), so the untransposed copy is exactly what it consumes. The code this plan ports
    verbatim under D-08 holds both (`Attn` has `q/k/v/o` AND `qt/kt/vt/ot`; `Block` `wi/wo` AND `wit/wot`;
    `Residual` `wh/wo/wr` AND `wht/wot/wrt` — bolt.rs:102-105), and every parity number this phase rests on was
    measured on that code.

    WHAT IS AND IS NOT AT STAKE. The measured aarch64 Peyton f32 quantile delta is 9.54e-7 against a frozen
    1.0e-6 bar (SC4's literal) — about 4.6 % of headroom, taken with the spike-008 NEON microkernel under the
    routing above. The cost of keeping both layouts is RESIDENT MEMORY: weight arrays are held roughly twice
    (~69 MB of f32 for tiny at 8.65 M params). It is NOT the SC4 "< 30 MB" bar, which measures the release
    BINARY carrying embedded f16 bytes and is untouched by an in-memory duplicate. The planner already wrote
    the amendment into this plan's must-have truth, into `contracts/chronos-bolt-parity-v1.yaml`
    (`weights_dual_layout_documented`) and into 06-07's prohibition list, and Task 3 adds a test that STATES
    the resident element count rather than hiding it. What the planner cannot do alone is amend a locked
    CONTEXT decision — this phase already routes its other two user-owned calls (06-02's FALSIFY-MONO-011
    allowlist, 06-08's ci.yml leg) through a blocking human decision, and this is the third.

    Reversibility: under `amend-memory-clause` the code is the spike's, so reverting later is a deletion plus a
    re-measurement. Under `drop-untransposed` the change is not a refactor — it moves every single-row product
    onto a different kernel, which changes float accumulation order through a 12-layer T5, so the number the
    whole SC4 ladder is bolted to stops being evidence until it is taken again.
  </context>
  <options>
    <option id="amend-memory-clause">
      <name>(recommended) Amend D-13's memory clause — the shipped Bolt keeps both layouts</name>
      <pros>Honors D-14 as written (single rows reach `dot8`; multi-row products reach `gemm_blis`); keeps the ported code byte-faithful under D-08; preserves the accumulation order under which the 9.54e-7 aarch64 parity was measured, so the frozen 1.0e-6 bar stays evidence rather than a guess; the cost is disclosed and asserted — Task 3's `weight_layout_is_dual_and_its_cost_is_stated` prints the total resident f32 element count, and `single_row_routing_is_dot8_at_production_defaults` proves the routing a grep could never prove; SC4's "< 30 MB" (binary size) is untouched.</pros>
      <cons>A locked decision's sentence is rewritten: weight arrays are resident roughly twice (~69 MB f32 for tiny), which matters on a 1-2 vCPU / small-memory Lambda even though no stated bar measures it; two layouts is more surface to keep consistent if a weight is ever mutated.</cons>
    </option>
    <option id="drop-untransposed">
      <name>Hold D-13 as written — drop the untransposed `[out, in]` copies</name>
      <pros>D-13's memory sentence is honored literally with no amendment and no governance debt; roughly halves weight-array residency.</pros>
      <cons>Forces every single-row product onto `gemv` over the transpose, which violates D-14's second clause — so this does not remove a conflict, it moves it onto the other locked decision; it changes float accumulation order, so the frozen `quantiles_abs_f32 = 1.0e-6` bar must be RE-MEASURED before it can be trusted and cannot be assumed to still hold at 9.54e-7; it deviates from the D-08 verbatim port. Choosing it means this plan HALTS at this task: Task 3 makes no edit, the executor returns `## CHECKPOINT REACHED`, and 06-05 (the contract equation and the ladder's bar) plus 06-07 (its prohibition list) are re-planned around a fresh measurement.</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: amend-memory-clause, drop-untransposed — optionally with wording changes for the amendment sentence. This plan's SUMMARY then records the outcome on separate column-0 lines (no bullet, no backticks, no trailing text): exactly one line `D-13 decision: <token>` where `<token>` is the bare token — `amend-memory-clause` or `drop-untransposed` — and nothing else on that line; and one line `D-13 decision (verbatim): <the reply as typed>`. Plan 06-09 Task 3 copies the bare token into `deferred-items.md` as `D-ITEM-06-08`, so a token with trailing text on that line reads as "no decision on record".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Port bolt.rs + safetensors.rs and build the Chronos door (chronos.rs) with a weights-free validate() seam</name>
  <files>crates/aprender-forecast/src/bolt.rs, crates/aprender-forecast/src/safetensors.rs, crates/aprender-forecast/src/chronos.rs, crates/aprender-forecast/src/lib.rs</files>
  <read_first>
    - The human's answer to Task 2 (recorded on this plan's SUMMARY's `D-13 decision:` line before any edit to bolt.rs). Under `drop-untransposed` this task makes NO edit — see the first paragraph of the action.
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/bolt.rs (323 lines — port verbatim EXCEPT the rayon branch: lines 48-51 `PARALLEL_GEMM` static and the `if PARALLEL_GEMM.load(...)` arm at 61 go; `linear_fast` keeps the single-threaded `gemm_blis` call)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/safetensors.rs (41 lines, verbatim)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/lib.rs (lines 25-138: consts, EMBEDDED statics [stay in the SERVER crate — not here], Model, load_model_from_bytes/dir, ForecastArgs/ForecastResponse [renamed here], forecast door; 175-189 load_csv)
    - crates/aprender-mcp-setfit/src/lib.rs (lines 94-112: the ModelLoadError enum shape with Display + Error)
    - crates/aprender-compute/src/blis/compute.rs (lines 838-846: gemm_blis signature) and crates/aprender-compute/src/lib.rs (line 88: `pub mod blis`)
    - crates/aprender-forecast/src/types.rs and dates.rs (as written by 06-01: ForecastError, parse_date, future_days, format_ymd)
  </read_first>
  <action>
    <!-- planner-discipline-allow: PARALLEL_GEMM -->
    <!-- planner-discipline-allow: Matrix::matmul -->
    FIRST, read the recorded decision. This plan's SUMMARY must carry exactly one line matching
    `^D-13 decision: (amend-memory-clause|drop-untransposed)$` from Task 2. If it does not, STOP: no human
    decision is on record, nothing in `src/bolt.rs` may be written, and the gap goes back to the orchestrator
    as a checkpoint, not a guess. Implement exactly the chosen option; do not improve on it.
    - Under `drop-untransposed`: make NO edit in this task and run no verify from it. Record in the SUMMARY
      that the frozen `quantiles_abs_f32 = 1.0e-6` bar was measured under the routing the option removes and
      is therefore no longer evidence, then return `## CHECKPOINT REACHED` to the orchestrator so 06-05's
      contract equation and ladder bar and 06-07's prohibition list are re-planned around a fresh
      measurement. Everything below is the `amend-memory-clause` branch.
    - Under `amend-memory-clause` (the recommended branch, and what the rest of this plan assumes):
    - `src/safetensors.rs` <- sources/007 verbatim: `Tensor { shape, data }`, `Weights = HashMap<String, Tensor>`,
      `load_bytes(bytes) -> Result<(Weights, String), String>` decoding F32/F16/BF16 via `half`. Clippy/fmt only.
    - `src/bolt.rs` <- sources/007 verbatim minus the `parallel` feature branch: delete the `PARALLEL_GEMM`
      static, its doc comment, and the rayon arm inside `linear_fast`, so every multi-row product is
      `trueno::blis::gemm_blis(rows, out, inp, x, wt, &mut y, None)` and single rows go through `dot8` (D-14).
      Keep `Config::from_json`, `t5_layer_norm` (RMS, no mean subtraction), `relative_bucket`, `transpose`,
      `Bolt::{load, attention, position_bias, forward, forward_stages, predict}` with the 9-path
      re-quantiled rollout and `torch_quantile`. Keep the `fast/attn_gemm/row1_gemv` toggles (the `--bench`
      variants in 06-07 use them) with the production defaults `fast = true, attn_gemm = true,
      row1_gemv = false` set in `Bolt::load`.
      BOTH WEIGHT LAYOUTS ARE KEPT, DELIBERATELY (REVIEW-06-01, ratified by Task 2's blocking human decision,
      and this replaces the earlier "only the
      `[in, out]` copies are stored — D-13" instruction, which was false of the code being ported): `Attn`
      keeps `q/k/v/o` AND `qt/kt/vt/ot`, `Block` keeps `wi/wo` AND `wit/wot`, `Residual` keeps `wh/wo/wr`
      AND `wht/wot/wrt`, exactly as sources/007 bolt.rs:102-105 has them. Write a module-level doc comment
      on `Attn`/`Block`/`Residual` stating WHY in these terms: the row-major `[out, in]` copy is what
      `dot8` reads (contiguous `inp`-length rows, `linear()` at the ported bolt.rs:20-31), and it is the
      single-row path D-14 mandates; the `[in, out]` transpose is what `gemm_blis` reads for rows > 1.
      Deleting either one forces the other path's kernel and changes the float accumulation order the
      9.54e-7 parity number was measured under. Also state the cost honestly: weight arrays are resident
      roughly twice (~69 MB of f32 for tiny at 8.65 M params), which is RESIDENT MEMORY and not the SC4
      "< 30 MB" bar, which measures the release BINARY carrying the embedded f16 bytes.
    - `src/chronos.rs` (new, from sources/007 lib.rs:25-138 + 175-189): `pub const CHRONOS_MIN_POINTS: usize = 4;`
      `CHRONOS_MAX_POINTS = 20_000`, `CHRONOS_MAX_HORIZON = 1_024`; `pub struct Model { pub bolt: Bolt, pub name: String, pub n_params: usize, pub dtype: String, pub source: String, pub load_seconds: f64 }`;
      `pub enum ModelLoadError { Io(std::io::Error), Decode(String), Config(String) }` with `Display` and
      `std::error::Error` (replaces the spike's `Result<_, String>`); `load_model_from_bytes(weights: &[u8], config: &[u8], source: &str) -> Result<Model, ModelLoadError>`;
      `load_model_from_dir(dir: &Path)`; `#[derive(Deserialize, JsonSchema)] #[serde(deny_unknown_fields)] pub struct ChronosArgs { ds: Vec<String>, y: Vec<Option<f64>>, horizon: usize, #[serde(default)] freq: Option<String>, #[serde(default)] allow_long_horizon: bool }`
      with the spike doc comments (they are the tool schema); `#[derive(Serialize)] pub struct ChronosResponse { model, freq, n_history, context_used, predict_seconds, ds, yhat, yhat_lower, yhat_upper, quantiles: serde_json::Map, #[serde(skip_serializing_if = "Option::is_none")] warning: Option<String>, diagnostics }`.
      THE ONE STRUCTURAL SEAM (a recorded D-08 deviation, justified by D-11 + D-18): extract the validation
      prefix of the spike's `forecast()` (its first 14 statements, through `future_days`) into
      `pub fn validate(cfg: &Config, args: &ChronosArgs) -> Result<Validated, ForecastError>` where
      `pub struct Validated { pub ds: Vec<i64>, pub fut: Vec<i64>, pub freq: String, pub context: Vec<f32>, pub n_missing: usize }`,
      with every message verbatim ("need at least 4 points", "horizon must be at least 1", "exceeds the
      maximum 1024", "pass allow_long_horizon: true to accept", "y needs at least 2 observed (non-null)
      values", the date messages from dates.rs). `pub fn forecast(model: &Model, args: &ChronosArgs) -> Result<ChronosResponse, ForecastError>`
      calls `validate(&model.bolt.cfg, args)?` then the unchanged zero-shot pass, the `warning` text naming
      the rollouts and the spike-006 MASE 1.7-vs-1.1 measurement, and the diagnostics keys
      `model, params, weights_dtype, weights_source, context_length, patch, native_horizon, rollouts, forwards, missing_values, quantile_levels`.
      `pub fn load_csv(text: &str) -> Result<(Vec<String>, Vec<Option<f64>>), String>` takes the CSV TEXT
      (callers use `include_str!`), not a path. File-scope `#![allow(clippy::disallowed_methods)]` (JsonSchema
      + json!).
    - `src/lib.rs`: `pub mod bolt; pub mod safetensors; pub mod chronos;`.
    Weights-free unit tests in `chronos.rs` (`mod tests`, NOT gated — they build `Config::from_json` from
    `tests/fixtures/chronos_bolt_tiny_config.json`): `config_fixture_sha_is_pinned` (sha256 of the fixture
    bytes == the pin — compute with a small in-test SHA-256? No new dependency: assert the byte length 1.1 KB
    band AND that `prediction_length == 64`, `context_length == 2048`, `quantiles.len() == 9`; the sha is
    asserted by the `shasum` acceptance criterion instead), `validate_refuses_too_few_points` ("at least 4"),
    `validate_refuses_horizon_zero`, `validate_refuses_horizon_above_max` ("1024"), `validate_refuses_long_horizon_without_flag`
    (message contains "allow_long_horizon"), `validate_accepts_long_horizon_with_flag` (horizon 365, flag true -> Ok),
    `validate_refuses_all_null_y` ("non-null"), `validate_refuses_impossible_date` ("calendar"),
    `validate_refuses_unsorted_ds`, `validate_refuses_freq_h` ("use D, W or MS"),
    `validate_refuses_trailing_content_in_ds` (a `ds` entry `"2008-02-01garbage"` -> Validation naming
    `YYYY-MM-DD`; the strict `parse_date` landed in 06-01, this proves the Chronos door inherits it —
    REVIEW-06-U2), `chronos_args_schema_is_strict` (schemars `additionalProperties == false`, required
    contains ds, y, horizon, properties contain allow_long_horizon).

    Two weights-free tests in `bolt.rs` (`#[cfg(test)] mod tests`, NOT gated — they need no model file):
    - `single_row_routing_is_dot8_at_production_defaults` (REVIEW-06-04): build small random `x` (1 row),
      `w` `[out, in]` and its `wt = transpose(&w, out, inp)`; assert
      `proj(true, false, &x, 1, inp, &w, &wt, out, None)` is BIT-IDENTICAL to `linear(&x, 1, inp, &w, out, None)`
      (the dot8 path) and, on this host, NOT bit-identical to `linear_fast(&x, 1, inp, &wt, out, None)`
      (the gemv path) for at least one of 20 seeded inputs — if the two kernels happened to agree on every
      input the assertion is vacuous, so assert the dot8 equality unconditionally and the gemv difference
      as "at least one of 20 differs", printing how many did. Also assert `Bolt::load`'s defaults by
      constructing a `Bolt` from the fixture config path used elsewhere is NOT possible without weights, so
      instead assert the source-level default with the acceptance criterion below. This is the routing proof
      a `grep dot8` cannot give: it shows WHICH kernel a single row actually reaches.
    - `weight_layout_is_dual_and_its_cost_is_stated` (REVIEW-06-01): a pure-arithmetic test over the fixture
      config's dims computing the f32 element count a loaded `Bolt` holds for BOTH layouts, asserting it is
      >= 2x the single-layout matrix element count, and `println!`ing the implied MB so the number appears
      in `-- --nocapture` runs and in the SUMMARY. The test's doc comment states the D-13 amendment.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib chronos::tests > /tmp/p06-05-t2a.log 2>&1; rc=$?; tail -20 /tmp/p06-05-t2a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-05-t2a.log > /dev/null && test "$(grep -c '^test chronos::tests::' /tmp/p06-05-t2a.log)" -ge 11</automated>
    <fails_when>non-zero exit, any failure/ignored, or fewer than 11 weights-free chronos tests ran (the ten refusal/schema cases plus the trailing-content date case)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib bolt::tests -- --nocapture > /tmp/p06-05-t2c.log 2>&1; rc=$?; tail -20 /tmp/p06-05-t2c.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-05-t2c.log > /dev/null && grep -q 'single_row_routing_is_dot8_at_production_defaults' /tmp/p06-05-t2c.log && grep -q 'weight_layout_is_dual_and_its_cost_is_stated' /tmp/p06-05-t2c.log</automated>
    <fails_when>non-zero exit (a single row did not reach dot8 at the production defaults, or the dual-layout element count was below 2x); or either test name is absent from the run (it was not written, and a filter matching nothing exits 0)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets -- -D warnings > /tmp/p06-05-t2b.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t2b.log; test $rc -eq 0 && cargo fmt --all -- --check > /dev/null 2>&1 && ! grep -q 'PARALLEL_GEMM' crates/aprender-forecast/src/bolt.rs && ! grep -q 'Matrix::matmul' crates/aprender-forecast/src/bolt.rs && test "$(git diff --stat HEAD -- crates/aprender-compute | wc -l)" -eq 0 && test -s /tmp/p06-05-base.sha && test "$(git diff --stat "$(cat /tmp/p06-05-base.sha)"..HEAD -- crates/aprender-compute | wc -l)" -eq 0</automated>
    <fails_when>clippy/fmt non-zero; the rayon static or a Matrix::matmul call survived in bolt.rs (D-14); or any file under crates/aprender-compute changed — in the working tree (staged or not) OR in any commit since the plan-start SHA Task 1 recorded (D-05). A missing /tmp/p06-05-base.sha is itself a failure: re-record it as the parent of this plan's first `(06-05)` commit, never as the current HEAD</fails_when>
  </verify>
  <done>Under `amend-memory-clause`: bolt/safetensors ported (rayon branch removed), the Chronos door split into validate()+forecast() with verbatim messages, >= 11 weights-free refusal/schema tests green, the single-row dot8 route and the dual-layout cost proven, aprender-compute untouched. Under `drop-untransposed`: no source file changed, the SUMMARY states why the 1.0e-6 bar is no longer evidence, and the executor returned `## CHECKPOINT REACHED` — the three verifies above do not apply and must not be reported as skipped-and-fine.</done>
  <acceptance_criteria>
    - This plan's SUMMARY carries exactly one line matching `^D-13 decision: (amend-memory-clause|drop-untransposed)$` plus one `D-13 decision (verbatim):` line, both written before any edit to bolt.rs (`grep -Ec '^D-13 decision: (amend-memory-clause|drop-untransposed)$'` on the SUMMARY prints 1)
    - Every criterion below is the `amend-memory-clause` branch; under `drop-untransposed` the only criterion is `git diff --name-only` for this task being empty
    - `crates/aprender-forecast/src/bolt.rs` contains `pub fn dot8(`, `trueno::blis::gemm_blis(`, `pub fn t5_layer_norm(`, `pub fn torch_quantile(` (or the spike's exact name), `pub fn transpose(`
    - `crates/aprender-forecast/src/bolt.rs` contains `row1_gemv: false` inside `Bolt::load` (the production default D-14 depends on) and `fn single_row_routing_is_dot8_at_production_defaults` and `fn weight_layout_is_dual_and_its_cost_is_stated`
    - `bolt.rs` keeps both layouts — `grep -c 'pub qt:' crates/aprender-forecast/src/bolt.rs` is 1 AND `grep -c 'pub q:' crates/aprender-forecast/src/bolt.rs` is 1 — and a doc comment above `pub struct Attn` explains the pairing (grep the file for `dot8` and `gemm_blis` within the struct doc block)
    - The SUMMARY records the D-13 amendment verbatim (both layouts ship; D-14 and the 9.54e-7 parity measurement are why) and the printed resident-MB figure from `weight_layout_is_dual_and_its_cost_is_stated`
    - `crates/aprender-forecast/src/chronos.rs` contains `pub const CHRONOS_MIN_POINTS: usize = 4;`, `pub const CHRONOS_MAX_HORIZON: usize = 1_024;`, `pub enum ModelLoadError`, `pub struct ChronosArgs`, `pub struct ChronosResponse`, `pub fn validate(`, `pub fn forecast(`, `allow_long_horizon: true to accept`, `skip_serializing_if = "Option::is_none"`
    - `crates/aprender-forecast/src/lib.rs` contains `pub mod bolt;`, `pub mod safetensors;`, `pub mod chronos;`
    - chronos.rs test module contains `fn validate_refuses_long_horizon_without_flag`, `fn validate_accepts_long_horizon_with_flag`, `fn validate_refuses_all_null_y`, `fn chronos_args_schema_is_strict`
    - The SUMMARY records the validate()/forecast() split as a D-08 deviation with its D-11/D-18 rationale
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: contracts/chronos-bolt-parity-v1.yaml + the gated Bolt parity ladder + the two-sided counted-skip proof (D-18)</name>
  <precondition>models/chronos-bolt-tiny/f32/model.safetensors and models/chronos-bolt-tiny/f16/model.safetensors exist (Task 1 ran)</precondition>
  <files>contracts/chronos-bolt-parity-v1.yaml, crates/aprender-forecast/src/bolt.rs, crates/aprender-forecast/src/chronos.rs</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (the shape; lines 1-30, 80-100, 1010-1022, 1103-1115, 1243-1262, 1276-1310)
    - crates/aprender-contracts/src/schema/types.rs (lines 248-281, 289-321, 326-367, 448-491, 497-525)
    - .claude/skills/spike-findings-aprender/sources/005-chronos-bolt-tiny-parity/src/main.rs (lines 23-113: the ladder — masks, loc/scale, embeddings, hidden states, quantiles_64, the 365 rollout with the median-only control, the six probes)
    - .claude/skills/spike-findings-aprender/sources/005-chronos-bolt-tiny-parity/RUN-OUTPUT.md (the measured table behind every tolerance in the interfaces block)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/tests/e2e.rs (lines 125-142: the f16-vs-f32 comparison, series std 0.8718)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F6: the exact cfg_attr form and the observed libtest/nextest output)
    - crates/aprender-forecast/src/test_support.rs
  </read_first>
  <action>
    <!-- planner-discipline-allow: println!("SKIP -->
    (1) `contracts/chronos-bolt-parity-v1.yaml`: `contract: chronos-bolt-parity`, `metadata` (`kind: kernel`;
    description states SC4, the oracle `chronos-forecasting==2.3.1`, the weights pin (repo, revision, both
    sha256s), the horizon policy from spike 006 (accept <= 64; `allow_long_horizon` to 1024 with a warning;
    MASE 1.7 past 64 vs 1.1 within), the embedding modes (tiny-f16 default 24 MB / small-f16 103 MB build
    option / runtime path), the EMPIRICAL coverage sentence (q10-q90 covered 0.65 on rolling origins, D-16),
    and the X64-tolerance assumption; `references`: Ansari et al. 2024 "Chronos: Learning the Language of
    Time Series", the `amazon-science/chronos-forecasting` 2.3.1 pipeline, `references/chronos-zero-shot-port.md`,
    `references/chronos-mcp-server.md`, `sources/005-*/RUN-OUTPUT.md`), `equations` with `float_tolerance`
    per the interfaces block plus invariants `horizon_gate_native_64`, `warning_beyond_native`,
    `rollout_scheme_nine_path_requantiled`, `weights_dual_layout_documented`, `gemm_routing_blis`,
    `single_row_routing_dot8`, `weights_never_committed`, `weights_verified_on_every_gate_run`,
    `counted_skip_when_unarmed`.
    TWO EQUATIONS THIS REVIEW ADDS OR RESHAPES:
    - `quantiles_abs_f32_nonaarch64`, `float_tolerance: 5.0e-6` (REVIEW-06-02). Its description must say,
      in these words, that the value is PROVISIONAL AND UNMEASURED: it is headroom for a different GEMM
      accumulation order on x86_64, where `gemm_blis` does not dispatch the spike-008 NEON microkernel, and
      it is NOT evidence of anything. Its proof obligation is that the FIRST x86_64 run records its measured
      max|delta| and the bar is then tightened to that measurement plus margin in a `pv diff`-visible contract
      edit. `quantiles_abs_f32` keeps `1.0e-6` — the SC4 literal, measured 9.54e-7 on aarch64 — and its
      description records the ~4.6 % margin and that every CI job is `[self-hosted, X64, Linux, clean-room]`
      (`.github/workflows/ci.yml:96,516,988,1039`). The parity tests select between the two on
      `cfg!(target_arch = "aarch64")` and PRINT which bar they used, so a green run always says where it ran.
    - `weights_dual_layout_documented` REPLACES the earlier `weights_transposed_only` (REVIEW-06-01). Its
      statement: the loaded `Bolt` holds both the row-major `[out, in]` copies (read by `dot8` on single
      rows) and the `[in, out]` transposes (read by `gemm_blis` on multi-row products); D-13's "only
      transposed weights are kept" sentence is amended because `dot8` requires contiguous `[out, in]` rows
      and D-14 mandates `dot8` for single rows. The description carries the resident-memory figure and notes
      that SC4's "< 30 MB" is a binary-size bar, not a residency bar.
    Also `weights_verified_on_every_gate_run` (REVIEW-06-03): the fetch recipe re-hashes f32 model, config
    and derived f16 on every invocation, never only on download, and `just chronos-gate` invokes it
    unconditionally; its falsification test is the tamper control in Task 1's verify.
    Top-level `constants` as listed; `proof_obligations` (>= 1 per equation),
    `falsification_tests` FALSIFY-CHRONOS-001.. naming `cargo test -p aprender-forecast --lib bolt::parity::<fn>` /
    `chronos::parity::<fn>` filters (and, for the skip proof, the two shell runs below), `kani_harnesses`
    (house comment; `harness` names `bolt::parity::relative_buckets_match_fixture_over_grid` as the bounded
    evidence), `qa_gate` F-FORECAST-CHRONOS-001 whose checks include "unarmed run prints >= 1 ignored with the
    arming reason" and "armed run prints 0 ignored". Validate with `pv validate`; `pv status` non-zero.
    (2) In `bolt.rs` add `#[cfg(test)] mod parity`; every weight-dependent test carries
    `#[cfg_attr(not(chronos_weights), ignore = "CHRONOS_MODEL_DIR unset or has no model.safetensors — run `just fetch-chronos-tiny` to arm")]`
    and loads the model from `std::env::var_os("CHRONOS_MODEL_DIR")` via `chronos::load_model_from_dir`
    (expect — when armed, absence is a defect). Every test that compares f32 quantiles resolves its bar
    through one shared helper `fn f32_quantile_bar() -> f64` that returns
    `equation_tolerance("chronos-bolt-parity-v1", if cfg!(target_arch = "aarch64") { "quantiles_abs_f32" } else { "quantiles_abs_f32_nonaarch64" })`
    and `println!`s which equation name and value it used together with `std::env::consts::ARCH`
    (REVIEW-06-02 — a green run must say where it ran and against which bar; the SUMMARY quotes that line).
    Tests: `peyton_ladder_matches_oracle_f32` (attention mask
    equal; loc/scale within `loc_scale_rel`; input_embeds first/last/REG, encoder first/REG, decoder hidden
    within `hidden_states_abs`; `quantiles_64` within `f32_quantile_bar()`, and the test message prints the
    measured max|delta| so an x86_64 run yields the number the provisional bar is waiting for),
    `air_and_short100_ladders_match_oracle`
    (same rungs; quantiles within `quantiles_rel_scale * scale`), `rollout_365_matches_oracle_and_median_only_does_not`
    (`peyton_tiny_oracle.json quantiles_365`: 9-path scheme within `rollout_365_abs`; the median-only control
    from spike 005 lines 70-82 must differ by > 0.1 — the control that pins the installed pipeline; `forwards == constants.rollout_forwards_for_365`),
    `edge_probes_match_oracle` (six cases: masks identical; quantiles within `probe_quantiles_rel_scale * scale`,
    except `constant` within `probe_constant_abs`), `relative_buckets_match_fixture_over_grid` (NOT gated —
    pure function vs the fixture's bucket table, the kani evidence). In `chronos.rs` add `#[cfg(test)] mod parity`
    (gated): `f16_weights_within_two_percent_of_std` (load `<CHRONOS_MODEL_DIR>/../f32` and `../f16` — both
    must exist when armed, the recipe creates both; `dtype` "F32"/"F16"; 64-step quantiles differ by <
    `f16_rel_std * 0.8718`), `door_reports_warning_and_forwards_for_365` (`allow_long_horizon: true`,
    `warning` contains "rollout", `diagnostics.forwards == 46`, `diagnostics.rollouts == 5`),
    `door_context_used_is_capped_at_2048` (a 3000-point series -> `context_used == 2048`).
    (3) The two-sided skip proof is the verify below; record both summaries verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/chronos-bolt-parity-v1.yaml > /tmp/p06-05-t3a.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t3a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-05-t3a.log && CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/chronos-bolt-parity-v1.yaml > /tmp/p06-05-t3b.log 2>&1; rc2=$?; cat /tmp/p06-05-t3b.log; test $rc2 -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-05-t3b.log</automated>
    <fails_when>validate non-zero / no "0 error(s)", or status shows any count of 0</fails_when>
    <automated>env -u CHRONOS_MODEL_DIR CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity > /tmp/p06-05-t3c.log 2>&1; rc=$?; tail -12 /tmp/p06-05-t3c.log; test $rc -eq 0 && grep -Eq 'test result: ok\. [0-9]+ passed; 0 failed; [1-9][0-9]* ignored' /tmp/p06-05-t3c.log && grep -q 'ignored, CHRONOS_MODEL_DIR unset' /tmp/p06-05-t3c.log</automated>
    <fails_when>non-zero exit; or the UNARMED summary shows 0 ignored (a weight test ran or silently passed — the D-18 failure mode); or the per-test "ignored, <reason>" line with the arming hint is absent</fails_when>
    <automated>CHRONOS_MODEL_DIR="$PWD/models/chronos-bolt-tiny/f32" CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity > /tmp/p06-05-t3d.log 2>&1; rc=$?; tail -15 /tmp/p06-05-t3d.log; test $rc -eq 0 && grep -Eq 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-05-t3d.log && test "$(grep -c '^test \(bolt\|chronos\)::parity::' /tmp/p06-05-t3d.log)" -ge 8</automated>
    <fails_when>non-zero exit (a parity bar failed); or the ARMED summary still shows ignored tests (the cfg did not arm — build.rs not re-run or the env var path wrong); or fewer than 8 parity tests ran</fails_when>
  </verify>
  <done>Contract valid and non-hollow; unarmed run: >= 1 ignored with the arming reason; armed run: 0 ignored, all Bolt/Chronos parity bars pass on f32 and f16.</done>
  <acceptance_criteria>
    - `contracts/chronos-bolt-parity-v1.yaml` contains `quantiles_abs_f32:`, `float_tolerance: 1.0e-6` (or `1e-6`), `rollout_365_abs:`, `f16_rel_std:`, `probe_constant_abs:`, `native_horizon: 64`, `rollout_forwards_for_365: 46`, `a0e552de83495b5c28c14c71c374f3e33280b340`, `FALSIFY-CHRONOS-001`, `DECLARED, NOT EXECUTED`, `0.65` (empirical coverage)
    - The contract contains `quantiles_abs_f32_nonaarch64:`, `5.0e-6` (or `5e-6`), the word `PROVISIONAL`, and `weights_dual_layout_documented:`; and `! grep -q 'weights_transposed_only' contracts/chronos-bolt-parity-v1.yaml` (the amended equation replaced it, REVIEW-06-01/02)
    - `bolt.rs` contains `fn f32_quantile_bar` and `quantiles_abs_f32_nonaarch64` and `std::env::consts::ARCH`
    - `grep -c 'cfg_attr(not(chronos_weights), ignore' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs` totals >= 7
    - `! grep -q 'println!("SKIP' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs`
    - bolt.rs contains `fn rollout_365_matches_oracle_and_median_only_does_not` and `fn edge_probes_match_oracle`; chronos.rs contains `fn f16_weights_within_two_percent_of_std` and `fn door_reports_warning_and_forwards_for_365`
    - `grep -c 'equation_tolerance("chronos-bolt-parity-v1"' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs` totals >= 6
    - SUMMARY quotes both `test result:` lines (unarmed with N ignored; armed with 0 ignored)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hugging Face Hub -> /models/ (dev/gate time only) | third-party model files enter the build; never on a request path |
| request `y`/`horizon` -> Bolt forward | caller-controlled context length and rollout count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | model weights supply chain | high | mitigate | pinned revision + sha256 for f32 and config, abort on mismatch; f16 derived locally; `*.safetensors` and `/models/` gitignored; never downloaded at runtime |
| T-06-02 | Denial of Service | Bolt forward / rollout | high | mitigate | `CHRONOS_MAX_POINTS 20_000`, context truncated to 2048 (`context_used`), `CHRONOS_MAX_HORIZON 1_024` (<= 15 rollouts of 9 paths), horizon > 64 gated behind `allow_long_horizon` |
| T-06-04 | Tampering | non-finite / all-null `y` | medium | mitigate | `is_finite` refusal; `>= 2 observed values` refusal; NaN handled as missing by the instance norm |
| T-06-13 | Repudiation | a parity claim that only ever passed because weights were absent | high | mitigate | `cfg_attr(..., ignore)` counted skips; the two-sided proof (unarmed >= 1 ignored / armed 0 ignored) in this plan's verify and in `just chronos-gate` (06-08) |
| T-06-SC | Tampering | cargo installs / python env | high | mitigate | no new registry crate (RESEARCH F9); `uv run --with huggingface_hub --with safetensors --with numpy` is a dev-time env for the fetch recipe only, never a runtime dependency |
</threat_model>

<verification>
- Task 2's blocking human decision is on record before any `bolt.rs` edit: the SUMMARY carries exactly one line matching `^D-13 decision: (amend-memory-clause|drop-untransposed)$`. Everything below is the `amend-memory-clause` branch; under `drop-untransposed` the plan halts at Task 3 with no source change and the phase re-plans 06-05/06-07 around a fresh parity measurement
- `just fetch-chronos-tiny` exits 0 with all three shas verified, and a SECOND files-present run still re-hashes and echoes the pins (REVIEW-06-03); a flipped byte in a copy dir makes it exit non-zero naming a sha256 mismatch; `git status` shows nothing under `models/`
- `pv validate` 0 errors; `pv status` non-zero counts
- Unarmed `cargo test ... -- bolt::parity chronos::parity`: `[1-9]+ ignored` with the arming reason; armed: `0 ignored`, >= 8 parity tests pass (the two positional filters are correct — see the rejected multi-filter finding in `<review_dispositions>`; the `>= 8` count is what would catch a dropped filter)
- `cargo test -p aprender-forecast --lib bolt::tests -- --nocapture` proves the single-row route is `dot8` at the production defaults and prints the dual-layout residency figure
- `cargo clippy -p aprender-forecast --all-targets -- -D warnings` clean; `crates/aprender-compute` untouched in the working tree AND across plan-start..HEAD
</verification>

<success_criteria>
- SC4's numeric bars hold in the library: f32 quantiles on Peyton within the arch-selected bar (1e-6 on aarch64, the provisional 5e-6 elsewhere with its measurement recorded), probes and rollout within contract, f16 within 2 % of std
- D-18's two constraints are proven (verify-always pinned fetch; counted skip both ways); D-13's horizon gate and warning are door-asserted and its memory sentence is amended with the reason on the record; D-14 routing proven by a kernel-equivalence test, not only by grep; D-05 aprender-compute unmodified
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-05)
New files: `justfile` recipe `fetch-chronos-tiny` (+ vars `chronos_rev`, `chronos_dir`); `crates/aprender-forecast/build.rs`; `crates/aprender-forecast/src/{bolt.rs, safetensors.rs, chronos.rs}`; `crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json`; `contracts/chronos-bolt-parity-v1.yaml`. `Cargo.lock`: `half` joins the `aprender-forecast` entry's dependency list (already-resolved workspace dependency; no new registry package).
New symbols: `bolt::{Config, linear, dot8, linear_fast, proj, t5_layer_norm, relative_bucket, transpose, Attn, Block, Stack, Residual, Bolt, torch_quantile}` (spike names); `safetensors::{Tensor, Weights, load_bytes}`; `chronos::{CHRONOS_MIN_POINTS, CHRONOS_MAX_POINTS, CHRONOS_MAX_HORIZON, Model, ModelLoadError, Validated, ChronosArgs, ChronosResponse, load_model_from_bytes, load_model_from_dir, validate, forecast, load_csv}`; cfg `chronos_weights`; env vars `CHRONOS_MODEL_DIR` (runtime + arming), `CHRONOS_EMBED_DIR` (documented; consumed by 06-07's build.rs); justfile variables `chronos_rev`, `chronos_dir`, `chronos_abs`.
New tests: `chronos::tests::{config_fixture_sha_is_pinned, validate_refuses_*, validate_refuses_trailing_content_in_ds, validate_accepts_long_horizon_with_flag, chronos_args_schema_is_strict}`; `bolt::tests::{single_row_routing_is_dot8_at_production_defaults, weight_layout_is_dual_and_its_cost_is_stated}`; `bolt::parity::{peyton_ladder_matches_oracle_f32, air_and_short100_ladders_match_oracle, rollout_365_matches_oracle_and_median_only_does_not, edge_probes_match_oracle, relative_buckets_match_fixture_over_grid}` plus the private helper `bolt::parity::f32_quantile_bar`; `chronos::parity::{f16_weights_within_two_percent_of_std, door_reports_warning_and_forwards_for_365, door_context_used_is_capped_at_2048}`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-05-SUMMARY.md` when done. Record FIRST, before any edit to `bolt.rs`, Task 2's outcome on its own column-0 lines: exactly one bare-token line `D-13 decision: <amend-memory-clause|drop-untransposed>` and one `D-13 decision (verbatim): <the reply as typed>` (06-09 Task 3 copies the bare token into `deferred-items.md` as `D-ITEM-06-08`). Then record: the printed f16 sha vs the advisory pin (A3), the second (files-present) recipe run proving verify-always and the tamper control's failure output (REVIEW-06-03), the validate()/forecast() D-08 deviation and rationale, both skip-proof summary lines verbatim, the measured max|Δ| per ladder rung on this host WITH the `f32_quantile_bar` line naming the arch and the equation used (REVIEW-06-02), the D-13 dual-layout amendment and the printed residency figure (REVIEW-06-01), the single-row `dot8` routing observation including how many of the 20 seeded inputs differed from the gemv path (REVIEW-06-04), and the fact that the two-positional-filter test commands were KEPT on refuted evidence.
</output>
