---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - justfile
  - Cargo.lock
  - crates/aprender-forecast/Cargo.toml
  - crates/aprender-forecast/README.md
  - crates/aprender-forecast/build.rs
  - crates/aprender-forecast/src/lib.rs
  - crates/aprender-forecast/src/bolt.rs
  - crates/aprender-forecast/src/safetensors.rs
  - crates/aprender-forecast/src/chronos.rs
  - crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json
  - contracts/chronos-bolt-parity-v1.yaml
autonomous: true
requirements: [SC4]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`just fetch-chronos-tiny` downloads amazon/chronos-bolt-tiny at revision a0e552de83495b5c28c14c71c374f3e33280b340 into the gitignored /models/chronos-bolt-tiny/f32, verifies model.safetensors sha256 75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32 and config.json sha256 278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0, aborts on mismatch, and derives f16 into /models/chronos-bolt-tiny/f16 (D-18: weights never committed, pinned)"
    - "A weight-dependent test never passes vacuously: build.rs emits cfg(chronos_weights) only when CHRONOS_MODEL_DIR/model.safetensors exists; without it the run prints `N ignored` with the arming reason, with it the same tests run (D-18; RESEARCH F6 probe)"
    - "aprender_forecast::bolt reproduces chronos-forecasting 2.3.1: Peyton 64-step quantiles within 1e-6 absolute (f32), the six edge probes with identical attention masks, the 365-step 9-path re-quantiled rollout within the contract bar, f16 weights within 2 % of the series std (D-04, SC4)"
    - "Every multi-row product goes through trueno::blis::gemm_blis and single rows through dot8; the rayon branch is gone; aprender-compute is not modified (D-14, D-05)"
    - "Only transposed [in, out] weights are kept in memory (D-13); the safetensors decoder decodes F32/F16/BF16 from one byte slice shared by embedded and on-disk weights"
    - "The Chronos door (chronos::forecast) refuses < 4 points, > 20 000 points, horizon 0 or > 1 024, horizon > 64 without allow_long_horizon (message names allow_long_horizon), all-null y, impossible dates — as ForecastError::Validation — and carries a warning naming the rollout when the flag is used (D-11, D-13)"
    - "Every Chronos tolerance is read from contracts/chronos-bolt-parity-v1.yaml, which validates with 0 errors and non-zero counts (D-15)"
  artifacts:
    - path: "justfile"
      provides: "fetch-chronos-tiny recipe with the pinned revision and sha256s"
      contains: "a0e552de83495b5c28c14c71c374f3e33280b340"
    - path: "crates/aprender-forecast/build.rs"
      provides: "cargo::rustc-check-cfg=cfg(chronos_weights) + conditional cargo:rustc-cfg=chronos_weights"
      contains: "rustc-check-cfg=cfg(chronos_weights)"
    - path: "crates/aprender-forecast/src/bolt.rs"
      provides: "Chronos-Bolt T5 forward: Config, dot8, linear_fast (gemm_blis), t5_layer_norm, relative_bucket, transpose, Bolt::{load, forward, predict, forward_stages}, torch_quantile, 9-path rollout"
      contains: "trueno::blis::gemm_blis"
    - path: "crates/aprender-forecast/src/safetensors.rs"
      provides: "load_bytes(&[u8]) -> (Weights, dtype) for F32/F16/BF16"
      contains: "pub fn load_bytes"
    - path: "crates/aprender-forecast/src/chronos.rs"
      provides: "Model, ModelLoadError, load_model_from_bytes, load_model_from_dir, ChronosArgs, ChronosResponse, validate, forecast, load_csv; CHRONOS_MIN_POINTS/MAX_POINTS/MAX_HORIZON"
      contains: "pub fn forecast"
    - path: "crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json"
      provides: "the committed 1.1 KB config.json (NOT weights) so weights-free validation tests can build a Config"
      contains: "prediction_length"
    - path: "contracts/chronos-bolt-parity-v1.yaml"
      provides: "frozen Chronos tolerances, horizon policy, embedding/dtype invariants"
      contains: "quantiles_abs_f32"
  key_links:
    - from: "crates/aprender-forecast/src/bolt.rs"
      to: "crates/aprender-compute/src/blis/compute.rs"
      via: "gemm_blis(m, n, k, a, b, c, None) for rows > 1; dot8 for rows == 1"
      pattern: "gemm_blis\\("
    - from: "crates/aprender-forecast/src/chronos.rs"
      to: "crates/aprender-forecast/src/bolt.rs"
      via: "Model { bolt: Bolt, .. } ; forecast() calls model.bolt.predict(&context, horizon)"
      pattern: "bolt\\.predict\\("
    - from: "crates/aprender-forecast/build.rs"
      to: "crates/aprender-forecast/src/bolt.rs"
      via: "#[cfg_attr(not(chronos_weights), ignore = \"...run `just fetch-chronos-tiny` to arm\")] on every weight-dependent test"
      pattern: "cfg_attr\\(not\\(chronos_weights\\)"
    - from: "justfile"
      to: "models/chronos-bolt-tiny/"
      via: "hf_hub_download at the pinned revision + sha256 check + numpy f16 derivation"
      pattern: "hf_hub_download"
  prohibitions:
    - statement: "Never commit weights: *.safetensors is globally gitignored and /models/ root-anchored; only the 1.1 KB config.json is committed as a fixture (D-18)"
      status: resolved
      verification: test
    - statement: "Never download weights from the Hub at runtime or cold start (chronos-mcp-server.md) — fetch is a just recipe, never on a request path"
      status: resolved
      verification: judgment
    - statement: "Never trueno::Matrix::matmul or plain loops for GEMM; never the rayon blis::gemm path in the servers (D-14)"
      status: resolved
      verification: test
    - statement: "Do not port the median-only rollout — the 9-path re-quantiled scheme is chronos-forecasting 2.3.1 (off by 0.5 otherwise)"
      status: resolved
      verification: test
    - statement: "Do not subtract the mean in T5 layer norm or scale attention scores by 1/sqrt(d_kv)"
      status: resolved
      verification: judgment
    - statement: "Do not modify crates/aprender-compute (the spike-008 kernel is assumed, not changed; opening its upstream PR is a human checkpoint, D-05)"
      status: resolved
      verification: test
    - statement: "Never the println-then-return skip style for weight-dependent tests — it reports 0 ignored (D-18)"
      status: resolved
      verification: test
    - statement: "Chronos-2 is deferred (CONTEXT): no chronos2.rs, no 21-quantile path in this phase"
      status: resolved
      verification: judgment
---

<objective>
Land the Chronos-Bolt zero-shot forward and its door in `aprender-forecast` (D-07: the numeric path lives in
the library), with the weights mechanism D-18 demands: a pinned, sha256-checked `just fetch-chronos-tiny`
into the gitignored `/models/`, a `build.rs` that turns weight presence into a compile-time cfg so
weight-dependent tests are COUNTED skips when unarmed and real tests when armed, and the Bolt parity ladder
(SC4) against the committed chronos-forecasting 2.3.1 fixtures with every tolerance frozen in
`contracts/chronos-bolt-parity-v1.yaml` (D-15).

Purpose: the server crate (06-07) is transport only; everything it embeds and calls must already be proven
here, and the skip semantics must be proven in both directions before any CI leg depends on them.
Output: fetch recipe, build.rs, three ported/derived modules, the committed config fixture, one contract,
the gated ladder, the two-sided skip proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F3 gemm_blis signature, F6 in full, F9, Pitfall 7, Pitfall 8, "Weight fetch recipe" code example, Assumptions A2/A3)
@.planning/phases/06-native-time-series-forecasting-stack/06-PATTERNS.md  (sections: prophet/np/bolt/safetensors/dates; build.rs; aprender-mcp-chronos lib.rs; justfile; Counted skip)
@.claude/skills/spike-findings-aprender/references/chronos-zero-shot-port.md
@.claude/skills/spike-findings-aprender/references/chronos-mcp-server.md
@.planning/phases/06-native-time-series-forecasting-stack/06-01-SUMMARY.md

<interfaces>
<!-- Frozen tolerances (spike-005/007 RUN-OUTPUT measured values in parentheses) -->
equations.<name>.float_tolerance:
  quantiles_abs_f32              1.0e-6   (Peyton 64 steps 9.54e-7 f32, also 9.54e-7 through the spike-007 server — SC4 literal; 5 % margin, on aarch64 with the spike-008 kernel; an X64 CI leg will need its own measurement — recorded as an assumption in the contract)
  quantiles_rel_scale            4.0e-6   (max|Δ| / series scale for non-Peyton series; air 1.83e-4 at scale 119 = 1.5e-6)
  loc_scale_rel                  2.0e-6   (loc diff / |loc| and scale diff / scale; peyton 9.5e-7 / 1.8e-7; air scale 1.5e-5 at 119)
  hidden_states_abs              2.0e-5   (input_embeds <= 6.0e-7, encoder <= 1.4e-6, decoder hidden <= 7.6e-6)
  probe_quantiles_rel_scale      5.0e-6   (five_points 3.81e-6 abs / scale; huge_scale 1.00 abs at 1e6 = 1e-6; nan_gaps 1.91e-6; tiny_len_130 2.86e-6; air_rollout_24 1.22e-4 / 119)
  probe_constant_abs             1.0e-5   (constant series: scale floors to 1e-5 so relative is meaningless; 9.54e-7 abs measured)
  rollout_365_abs                5.0e-5   (1.72e-5 in spike 005; 1.91e-5 through the spike-007 server)
  f16_rel_std                    0.02     (SC4 literal; measured 9.55e-4 = 0.11 % of std)
constants: min_points 4, max_points 20000, max_horizon 1024, native_horizon 64, context_length 2048, quantile_levels 9, rollout_forwards_for_365 46
Weights pin: repo amazon/chronos-bolt-tiny, revision a0e552de83495b5c28c14c71c374f3e33280b340; f32 model.safetensors sha256 75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32 (33.0 MB); config.json sha256 278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0; derived tiny-f16 model.safetensors sha256 f9a033b42bc516e17ae5756317cb946121afdb59c94b4acfcd30fef93317cd4c (ADVISORY — re-derived, not downloaded; RESEARCH A3)
Fixture keys: chronos_bolt_tiny_fixture.json {config{...}, series{peyton, air, short100}{...loc, scale, attention_mask, input_embeds, encoder hidden, decoder hidden, quantiles_64, n_patches_plus_reg...}}; chronos_probes.json {chronos_version, cases{nan_gaps, five_points, constant, huge_scale, tiny_len_130_rollout, air_rollout_24}}; peyton_tiny_oracle.json {source, n, loc, scale, quantile_levels, quantiles_64, quantiles_365, torch_seconds_64, torch_seconds_365}
gemm_blis (verified): pub fn gemm_blis(m: usize, n: usize, k: usize, a: &[f32], b: &[f32], c: &mut [f32], profiler: Option<&mut BlisProfiler>) -> Result<(), TruenoError>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: `just fetch-chronos-tiny` (pinned, sha256-checked, f16 derived) + build.rs cfg emission + the committed config fixture</name>
  <precondition>Either network access to huggingface.co, or the local HF cache snapshot ~/.cache/huggingface/hub/models--amazon--chronos-bolt-tiny/snapshots/a0e552de83495b5c28c14c71c374f3e33280b340/ exists (RESEARCH F6 verified it does on this box); `uv --version` and `just --version` succeed</precondition>
  <files>justfile, Cargo.lock, crates/aprender-forecast/Cargo.toml, crates/aprender-forecast/build.rs, crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json, crates/aprender-forecast/README.md</files>
  <read_first>
    - justfile (lines 1-50: header contract "deployment surface", `set shell := ["bash", "-uc"]`, variable style, the shebang-recipe form at ~70)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F6 in full; "Weight fetch recipe" under Code Examples — the exact Python body; A2/A3 assumptions)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/tools/to_f16.py (the f16 derivation the recipe reproduces)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/build.rs (23 lines) and crates/aprender-train/build.rs (line 75: the in-tree rustc-check-cfg emitter)
    - .gitignore (lines 48-53: `*.safetensors` global, `/models/` root-anchored)
    - crates/aprender-forecast/Cargo.toml and README.md (as written by 06-01)
  </read_first>
  <action>
    (0) Before any edit, record the plan-start SHA once: `test -s /tmp/p06-05-base.sha || git rev-parse HEAD > /tmp/p06-05-base.sha`
    — Task 2's aprender-compute guard diffs the committed range from it as well as the working tree, so a
    change committed by an earlier task of this plan cannot slip past a working-tree-only diff.
    (1) justfile: add variables `chronos_rev := "a0e552de83495b5c28c14c71c374f3e33280b340"` and
    `chronos_dir := "models/chronos-bolt-tiny"` near the existing variables, and a shebang recipe
    `fetch-chronos-tiny` implementing RESEARCH's Code Example: `uv run --python 3.12 --with huggingface_hub --with safetensors --with numpy python - "{{chronos_rev}}" "{{chronos_dir}}"`
    with the Python body that downloads `model.safetensors` and `config.json` via `hf_hub_download(repo_id, filename, revision=rev, local_dir=<f32 dir>)`,
    verifies both pinned sha256s (exit non-zero on mismatch — never silent), converts every tensor to
    float16 with `safetensors.numpy` into `<f16 dir>/model.safetensors` (metadata `format: pt`,
    `converted: f32->f16`), copies `config.json` beside it, prints the f16 sha next to the advisory spike
    value, and prints `CHRONOS_MODEL_DIR=$PWD/{{chronos_dir}}/f16` / `CHRONOS_EMBED_DIR=...` hints. The recipe
    must be idempotent (re-running with files present re-verifies and exits 0) and must not use `set -e`
    leaks into callers (it is a recipe, not a sourced lib — `set -euo pipefail` inside the shebang is correct
    here). A header comment states: never run from CI's default gate; weights are Apache-2.0; `/models/`
    is gitignored. Run it once. Then copy `models/chronos-bolt-tiny/f32/config.json` to
    `crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json` (it is 1.1 KB of hyper-parameters,
    not weights — D-18 permits it; its sha is asserted by a test in Task 3).
    (2) `crates/aprender-forecast/build.rs` (auto-discovered; no manifest key needed): print
    `cargo::rustc-check-cfg=cfg(chronos_weights)`, `cargo:rerun-if-env-changed=CHRONOS_MODEL_DIR`, and
    `cargo:rustc-cfg=chronos_weights` iff `CHRONOS_MODEL_DIR` is set and `<dir>/model.safetensors` is a file
    (RESEARCH F6 snippet verbatim). It joins no `..` so `scripts/check_build_rs_paths.sh` skips it.
    (3) `crates/aprender-forecast/Cargo.toml`: add `half = { workspace = true }` (F16/BF16 decode in Task 2; `Cargo.lock`
    re-resolves the crate's dependency list — `half` is already in the lock, so no new registry package appears).
    (4) README.md: add a "Chronos-Bolt weights" section — the repo id, revision, both pinned sha256s, the
    advisory f16 sha, the Apache-2.0 license, `just fetch-chronos-tiny`, and the two env vars with their
    roles (`CHRONOS_MODEL_DIR` = runtime path + test arming; `CHRONOS_EMBED_DIR` = build-time embedding in
    the server crate).
  </action>
  <verify>
    <automated>just fetch-chronos-tiny > /tmp/p06-05-t1a.log 2>&1; rc=$?; tail -5 /tmp/p06-05-t1a.log; test $rc -eq 0 && test -f models/chronos-bolt-tiny/f32/model.safetensors && test -f models/chronos-bolt-tiny/f16/model.safetensors && test "$(shasum -a 256 models/chronos-bolt-tiny/f32/model.safetensors | cut -c1-64)" = "75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32" && test "$(shasum -a 256 models/chronos-bolt-tiny/f32/config.json | cut -c1-64)" = "278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0"</automated>
    <fails_when>non-zero recipe exit (download/sha failure), a missing file, or either sha256 differing from the pin</fails_when>
    <automated>git status --porcelain -- models/ > /tmp/p06-05-t1b.log 2>&1; rc=$?; test $rc -eq 0 && test ! -s /tmp/p06-05-t1b.log && git check-ignore -q models/chronos-bolt-tiny/f32/model.safetensors</automated>
    <fails_when>git status lists anything under models/ (weights would be committable), or check-ignore exits non-zero (the ignore rules do not cover the weights)</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo build -p aprender-forecast > /tmp/p06-05-t1c.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t1c.log; test $rc -eq 0 && ! grep -q 'unexpected_cfgs' /tmp/p06-05-t1c.log && test -f crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json</automated>
    <fails_when>build fails, or an `unexpected_cfgs` warning appears (the check-cfg line is missing), or the config fixture was not committed</fails_when>
  </verify>
  <done>Weights present locally and ignored by git; both pins verified; build.rs emits the cfg cleanly; the config fixture is committed; README documents the pin.</done>
  <acceptance_criteria>
    - `justfile` contains `fetch-chronos-tiny:`, `a0e552de83495b5c28c14c71c374f3e33280b340`, `75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32`, `278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0`, `hf_hub_download`, `float16`
    - `crates/aprender-forecast/build.rs` contains `cargo::rustc-check-cfg=cfg(chronos_weights)`, `rerun-if-env-changed=CHRONOS_MODEL_DIR`, `cargo:rustc-cfg=chronos_weights`, and no `".."`
    - `crates/aprender-forecast/Cargo.toml` contains `half = { workspace = true }`
    - `shasum -a 256 crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json | cut -c1-64` prints `278f0086733031635fb1c861cb01c1bad6477420c7fcb19381a2993e335785e0`
    - `crates/aprender-forecast/README.md` contains `amazon/chronos-bolt-tiny`, `Apache-2.0`, `CHRONOS_MODEL_DIR`, `CHRONOS_EMBED_DIR`
    - `ls models/chronos-bolt-tiny/f16/` lists `model.safetensors` and `config.json`; the recipe's printed f16 sha is recorded in the SUMMARY next to the advisory `f9a033b4...` value
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Port bolt.rs + safetensors.rs and build the Chronos door (chronos.rs) with a weights-free validate() seam</name>
  <files>crates/aprender-forecast/src/bolt.rs, crates/aprender-forecast/src/safetensors.rs, crates/aprender-forecast/src/chronos.rs, crates/aprender-forecast/src/lib.rs</files>
  <read_first>
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/bolt.rs (323 lines — port verbatim EXCEPT the rayon branch: lines 48-51 `PARALLEL_GEMM` static and the `if PARALLEL_GEMM.load(...)` arm at 61 go; `linear_fast` keeps the single-threaded `gemm_blis` call)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/safetensors.rs (41 lines, verbatim)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/src/lib.rs (lines 25-138: consts, EMBEDDED statics [stay in the SERVER crate — not here], Model, load_model_from_bytes/dir, ForecastArgs/ForecastResponse [renamed here], forecast door; 175-189 load_csv)
    - crates/aprender-mcp-setfit/src/lib.rs (lines 94-112: the ModelLoadError enum shape with Display + Error)
    - crates/aprender-compute/src/blis/compute.rs (lines 838-846: gemm_blis signature) and crates/aprender-compute/src/lib.rs (line 88: `pub mod blis`)
    - crates/aprender-forecast/src/types.rs and dates.rs (as written by 06-01: ForecastError, parse_date, future_days, format_ymd)
  </read_first>
  <action>
    <!-- planner-discipline-allow: PARALLEL_GEMM -->
    <!-- planner-discipline-allow: Matrix::matmul -->
    - `src/safetensors.rs` <- sources/007 verbatim: `Tensor { shape, data }`, `Weights = HashMap<String, Tensor>`,
      `load_bytes(bytes) -> Result<(Weights, String), String>` decoding F32/F16/BF16 via `half`. Clippy/fmt only.
    - `src/bolt.rs` <- sources/007 verbatim minus the `parallel` feature branch: delete the `PARALLEL_GEMM`
      static, its doc comment, and the rayon arm inside `linear_fast`, so every multi-row product is
      `trueno::blis::gemm_blis(rows, out, inp, x, wt, &mut y, None)` and single rows go through `dot8` (D-14).
      Keep `Config::from_json`, `t5_layer_norm` (RMS, no mean subtraction), `relative_bucket`, `transpose`
      (only the `[in, out]` copies are stored — D-13), `Bolt::{load, attention, position_bias, forward,
      forward_stages, predict}` with the 9-path re-quantiled rollout and `torch_quantile`. Keep the
      `fast/attn_gemm/row1_gemv` toggles (the `--bench` variants in 06-07 use them) with the production
      defaults `fast = true, attn_gemm = true, row1_gemv = false` set in `Bolt::load`.
    - `src/chronos.rs` (new, from sources/007 lib.rs:25-138 + 175-189): `pub const CHRONOS_MIN_POINTS: usize = 4;`
      `CHRONOS_MAX_POINTS = 20_000`, `CHRONOS_MAX_HORIZON = 1_024`; `pub struct Model { pub bolt: Bolt, pub name: String, pub n_params: usize, pub dtype: String, pub source: String, pub load_seconds: f64 }`;
      `pub enum ModelLoadError { Io(std::io::Error), Decode(String), Config(String) }` with `Display` and
      `std::error::Error` (replaces the spike's `Result<_, String>`); `load_model_from_bytes(weights: &[u8], config: &[u8], source: &str) -> Result<Model, ModelLoadError>`;
      `load_model_from_dir(dir: &Path)`; `#[derive(Deserialize, JsonSchema)] #[serde(deny_unknown_fields)] pub struct ChronosArgs { ds: Vec<String>, y: Vec<Option<f64>>, horizon: usize, #[serde(default)] freq: Option<String>, #[serde(default)] allow_long_horizon: bool }`
      with the spike doc comments (they are the tool schema); `#[derive(Serialize)] pub struct ChronosResponse { model, freq, n_history, context_used, predict_seconds, ds, yhat, yhat_lower, yhat_upper, quantiles: serde_json::Map, #[serde(skip_serializing_if = "Option::is_none")] warning: Option<String>, diagnostics }`.
      THE ONE STRUCTURAL SEAM (a recorded D-08 deviation, justified by D-11 + D-18): extract the validation
      prefix of the spike's `forecast()` (its first 14 statements, through `future_days`) into
      `pub fn validate(cfg: &Config, args: &ChronosArgs) -> Result<Validated, ForecastError>` where
      `pub struct Validated { pub ds: Vec<i64>, pub fut: Vec<i64>, pub freq: String, pub context: Vec<f32>, pub n_missing: usize }`,
      with every message verbatim ("need at least 4 points", "horizon must be at least 1", "exceeds the
      maximum 1024", "pass allow_long_horizon: true to accept", "y needs at least 2 observed (non-null)
      values", the date messages from dates.rs). `pub fn forecast(model: &Model, args: &ChronosArgs) -> Result<ChronosResponse, ForecastError>`
      calls `validate(&model.bolt.cfg, args)?` then the unchanged zero-shot pass, the `warning` text naming
      the rollouts and the spike-006 MASE 1.7-vs-1.1 measurement, and the diagnostics keys
      `model, params, weights_dtype, weights_source, context_length, patch, native_horizon, rollouts, forwards, missing_values, quantile_levels`.
      `pub fn load_csv(text: &str) -> Result<(Vec<String>, Vec<Option<f64>>), String>` takes the CSV TEXT
      (callers use `include_str!`), not a path. File-scope `#![allow(clippy::disallowed_methods)]` (JsonSchema
      + json!).
    - `src/lib.rs`: `pub mod bolt; pub mod safetensors; pub mod chronos;`.
    Weights-free unit tests in `chronos.rs` (`mod tests`, NOT gated — they build `Config::from_json` from
    `tests/fixtures/chronos_bolt_tiny_config.json`): `config_fixture_sha_is_pinned` (sha256 of the fixture
    bytes == the pin — compute with a small in-test SHA-256? No new dependency: assert the byte length 1.1 KB
    band AND that `prediction_length == 64`, `context_length == 2048`, `quantiles.len() == 9`; the sha is
    asserted by the `shasum` acceptance criterion instead), `validate_refuses_too_few_points` ("at least 4"),
    `validate_refuses_horizon_zero`, `validate_refuses_horizon_above_max` ("1024"), `validate_refuses_long_horizon_without_flag`
    (message contains "allow_long_horizon"), `validate_accepts_long_horizon_with_flag` (horizon 365, flag true -> Ok),
    `validate_refuses_all_null_y` ("non-null"), `validate_refuses_impossible_date` ("calendar"),
    `validate_refuses_unsorted_ds`, `validate_refuses_freq_h` ("use D, W or MS"),
    `chronos_args_schema_is_strict` (schemars `additionalProperties == false`, required contains ds, y, horizon,
    properties contain allow_long_horizon).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib chronos::tests > /tmp/p06-05-t2a.log 2>&1; rc=$?; tail -20 /tmp/p06-05-t2a.log; test $rc -eq 0 && grep -E 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-05-t2a.log > /dev/null && test "$(grep -c '^test chronos::tests::' /tmp/p06-05-t2a.log)" -ge 10</automated>
    <fails_when>non-zero exit, any failure/ignored, or fewer than 10 weights-free chronos tests ran</fails_when>
    <automated>CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets -- -D warnings > /tmp/p06-05-t2b.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t2b.log; test $rc -eq 0 && cargo fmt --all -- --check > /dev/null 2>&1 && ! grep -q 'PARALLEL_GEMM' crates/aprender-forecast/src/bolt.rs && ! grep -q 'Matrix::matmul' crates/aprender-forecast/src/bolt.rs && test "$(git diff --stat HEAD -- crates/aprender-compute | wc -l)" -eq 0 && test -s /tmp/p06-05-base.sha && test "$(git diff --stat "$(cat /tmp/p06-05-base.sha)"..HEAD -- crates/aprender-compute | wc -l)" -eq 0</automated>
    <fails_when>clippy/fmt non-zero; the rayon static or a Matrix::matmul call survived in bolt.rs (D-14); or any file under crates/aprender-compute changed — in the working tree (staged or not) OR in any commit since the plan-start SHA Task 1 recorded (D-05). A missing /tmp/p06-05-base.sha is itself a failure: re-record it as the parent of this plan's first `(06-05)` commit, never as the current HEAD</fails_when>
  </verify>
  <done>bolt/safetensors ported (rayon branch removed), the Chronos door split into validate()+forecast() with verbatim messages, >= 10 weights-free refusal/schema tests green, aprender-compute untouched.</done>
  <acceptance_criteria>
    - `crates/aprender-forecast/src/bolt.rs` contains `pub fn dot8(`, `trueno::blis::gemm_blis(`, `pub fn t5_layer_norm(`, `pub fn torch_quantile(` (or the spike's exact name), `pub fn transpose(`
    - `crates/aprender-forecast/src/chronos.rs` contains `pub const CHRONOS_MIN_POINTS: usize = 4;`, `pub const CHRONOS_MAX_HORIZON: usize = 1_024;`, `pub enum ModelLoadError`, `pub struct ChronosArgs`, `pub struct ChronosResponse`, `pub fn validate(`, `pub fn forecast(`, `allow_long_horizon: true to accept`, `skip_serializing_if = "Option::is_none"`
    - `crates/aprender-forecast/src/lib.rs` contains `pub mod bolt;`, `pub mod safetensors;`, `pub mod chronos;`
    - chronos.rs test module contains `fn validate_refuses_long_horizon_without_flag`, `fn validate_accepts_long_horizon_with_flag`, `fn validate_refuses_all_null_y`, `fn chronos_args_schema_is_strict`
    - The SUMMARY records the validate()/forecast() split as a D-08 deviation with its D-11/D-18 rationale
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: contracts/chronos-bolt-parity-v1.yaml + the gated Bolt parity ladder + the two-sided counted-skip proof (D-18)</name>
  <precondition>models/chronos-bolt-tiny/f32/model.safetensors and models/chronos-bolt-tiny/f16/model.safetensors exist (Task 1 ran)</precondition>
  <files>contracts/chronos-bolt-parity-v1.yaml, crates/aprender-forecast/src/bolt.rs, crates/aprender-forecast/src/chronos.rs</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (the shape; lines 1-30, 80-100, 1010-1022, 1103-1115, 1243-1262, 1276-1310)
    - crates/aprender-contracts/src/schema/types.rs (lines 248-281, 289-321, 326-367, 448-491, 497-525)
    - .claude/skills/spike-findings-aprender/sources/005-chronos-bolt-tiny-parity/src/main.rs (lines 23-113: the ladder — masks, loc/scale, embeddings, hidden states, quantiles_64, the 365 rollout with the median-only control, the six probes)
    - .claude/skills/spike-findings-aprender/sources/005-chronos-bolt-tiny-parity/RUN-OUTPUT.md (the measured table behind every tolerance in the interfaces block)
    - .claude/skills/spike-findings-aprender/sources/007-chronos-mcp-thin-server/tests/e2e.rs (lines 125-142: the f16-vs-f32 comparison, series std 0.8718)
    - .planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md (F6: the exact cfg_attr form and the observed libtest/nextest output)
    - crates/aprender-forecast/src/test_support.rs
  </read_first>
  <action>
    <!-- planner-discipline-allow: println!("SKIP -->
    (1) `contracts/chronos-bolt-parity-v1.yaml`: `contract: chronos-bolt-parity`, `metadata` (`kind: kernel`;
    description states SC4, the oracle `chronos-forecasting==2.3.1`, the weights pin (repo, revision, both
    sha256s), the horizon policy from spike 006 (accept <= 64; `allow_long_horizon` to 1024 with a warning;
    MASE 1.7 past 64 vs 1.1 within), the embedding modes (tiny-f16 default 24 MB / small-f16 103 MB build
    option / runtime path), the EMPIRICAL coverage sentence (q10-q90 covered 0.65 on rolling origins, D-16),
    and the X64-tolerance assumption; `references`: Ansari et al. 2024 "Chronos: Learning the Language of
    Time Series", the `amazon-science/chronos-forecasting` 2.3.1 pipeline, `references/chronos-zero-shot-port.md`,
    `references/chronos-mcp-server.md`, `sources/005-*/RUN-OUTPUT.md`), `equations` with `float_tolerance`
    per the interfaces block plus invariants `horizon_gate_native_64`, `warning_beyond_native`,
    `rollout_scheme_nine_path_requantiled`, `weights_transposed_only`, `gemm_routing_blis`, `weights_never_committed`,
    `counted_skip_when_unarmed`; top-level `constants` as listed; `proof_obligations` (>= 1 per equation),
    `falsification_tests` FALSIFY-CHRONOS-001.. naming `cargo test -p aprender-forecast --lib bolt::parity::<fn>` /
    `chronos::parity::<fn>` filters (and, for the skip proof, the two shell runs below), `kani_harnesses`
    (house comment; `harness` names `bolt::parity::relative_buckets_match_fixture_over_grid` as the bounded
    evidence), `qa_gate` F-FORECAST-CHRONOS-001 whose checks include "unarmed run prints >= 1 ignored with the
    arming reason" and "armed run prints 0 ignored". Validate with `pv validate`; `pv status` non-zero.
    (2) In `bolt.rs` add `#[cfg(test)] mod parity`; every weight-dependent test carries
    `#[cfg_attr(not(chronos_weights), ignore = "CHRONOS_MODEL_DIR unset or has no model.safetensors — run `just fetch-chronos-tiny` to arm")]`
    and loads the model from `std::env::var_os("CHRONOS_MODEL_DIR")` via `chronos::load_model_from_dir`
    (expect — when armed, absence is a defect). Tests: `peyton_ladder_matches_oracle_f32` (attention mask
    equal; loc/scale within `loc_scale_rel`; input_embeds first/last/REG, encoder first/REG, decoder hidden
    within `hidden_states_abs`; `quantiles_64` within `quantiles_abs_f32`), `air_and_short100_ladders_match_oracle`
    (same rungs; quantiles within `quantiles_rel_scale * scale`), `rollout_365_matches_oracle_and_median_only_does_not`
    (`peyton_tiny_oracle.json quantiles_365`: 9-path scheme within `rollout_365_abs`; the median-only control
    from spike 005 lines 70-82 must differ by > 0.1 — the control that pins the installed pipeline; `forwards == constants.rollout_forwards_for_365`),
    `edge_probes_match_oracle` (six cases: masks identical; quantiles within `probe_quantiles_rel_scale * scale`,
    except `constant` within `probe_constant_abs`), `relative_buckets_match_fixture_over_grid` (NOT gated —
    pure function vs the fixture's bucket table, the kani evidence). In `chronos.rs` add `#[cfg(test)] mod parity`
    (gated): `f16_weights_within_two_percent_of_std` (load `<CHRONOS_MODEL_DIR>/../f32` and `../f16` — both
    must exist when armed, the recipe creates both; `dtype` "F32"/"F16"; 64-step quantiles differ by <
    `f16_rel_std * 0.8718`), `door_reports_warning_and_forwards_for_365` (`allow_long_horizon: true`,
    `warning` contains "rollout", `diagnostics.forwards == 46`, `diagnostics.rollouts == 5`),
    `door_context_used_is_capped_at_2048` (a 3000-point series -> `context_used == 2048`).
    (3) The two-sided skip proof is the verify below; record both summaries verbatim in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- validate contracts/chronos-bolt-parity-v1.yaml > /tmp/p06-05-t3a.log 2>&1; rc=$?; tail -3 /tmp/p06-05-t3a.log; test $rc -eq 0 && grep -q '0 error(s)' /tmp/p06-05-t3a.log && CARGO_INCREMENTAL=0 cargo run -p aprender-contracts-cli --bin pv -- status contracts/chronos-bolt-parity-v1.yaml > /tmp/p06-05-t3b.log 2>&1; rc2=$?; cat /tmp/p06-05-t3b.log; test $rc2 -eq 0 && ! grep -Eq 'Proof obligations: 0|Falsification tests: 0|Kani harnesses: 0' /tmp/p06-05-t3b.log</automated>
    <fails_when>validate non-zero / no "0 error(s)", or status shows any count of 0</fails_when>
    <automated>env -u CHRONOS_MODEL_DIR CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity > /tmp/p06-05-t3c.log 2>&1; rc=$?; tail -12 /tmp/p06-05-t3c.log; test $rc -eq 0 && grep -Eq 'test result: ok\. [0-9]+ passed; 0 failed; [1-9][0-9]* ignored' /tmp/p06-05-t3c.log && grep -q 'ignored, CHRONOS_MODEL_DIR unset' /tmp/p06-05-t3c.log</automated>
    <fails_when>non-zero exit; or the UNARMED summary shows 0 ignored (a weight test ran or silently passed — the D-18 failure mode); or the per-test "ignored, <reason>" line with the arming hint is absent</fails_when>
    <automated>CHRONOS_MODEL_DIR="$PWD/models/chronos-bolt-tiny/f32" CARGO_INCREMENTAL=0 cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity > /tmp/p06-05-t3d.log 2>&1; rc=$?; tail -15 /tmp/p06-05-t3d.log; test $rc -eq 0 && grep -Eq 'test result: ok\. [1-9][0-9]* passed; 0 failed; 0 ignored' /tmp/p06-05-t3d.log && test "$(grep -c '^test \(bolt\|chronos\)::parity::' /tmp/p06-05-t3d.log)" -ge 8</automated>
    <fails_when>non-zero exit (a parity bar failed); or the ARMED summary still shows ignored tests (the cfg did not arm — build.rs not re-run or the env var path wrong); or fewer than 8 parity tests ran</fails_when>
  </verify>
  <done>Contract valid and non-hollow; unarmed run: >= 1 ignored with the arming reason; armed run: 0 ignored, all Bolt/Chronos parity bars pass on f32 and f16.</done>
  <acceptance_criteria>
    - `contracts/chronos-bolt-parity-v1.yaml` contains `quantiles_abs_f32:`, `float_tolerance: 1.0e-6` (or `1e-6`), `rollout_365_abs:`, `f16_rel_std:`, `probe_constant_abs:`, `native_horizon: 64`, `rollout_forwards_for_365: 46`, `a0e552de83495b5c28c14c71c374f3e33280b340`, `FALSIFY-CHRONOS-001`, `DECLARED, NOT EXECUTED`, `0.65` (empirical coverage)
    - `grep -c 'cfg_attr(not(chronos_weights), ignore' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs` totals >= 7
    - `! grep -q 'println!("SKIP' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs`
    - bolt.rs contains `fn rollout_365_matches_oracle_and_median_only_does_not` and `fn edge_probes_match_oracle`; chronos.rs contains `fn f16_weights_within_two_percent_of_std` and `fn door_reports_warning_and_forwards_for_365`
    - `grep -c 'equation_tolerance("chronos-bolt-parity-v1"' crates/aprender-forecast/src/bolt.rs crates/aprender-forecast/src/chronos.rs` totals >= 6
    - SUMMARY quotes both `test result:` lines (unarmed with N ignored; armed with 0 ignored)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hugging Face Hub -> /models/ (dev/gate time only) | third-party model files enter the build; never on a request path |
| request `y`/`horizon` -> Bolt forward | caller-controlled context length and rollout count |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | model weights supply chain | high | mitigate | pinned revision + sha256 for f32 and config, abort on mismatch; f16 derived locally; `*.safetensors` and `/models/` gitignored; never downloaded at runtime |
| T-06-02 | Denial of Service | Bolt forward / rollout | high | mitigate | `CHRONOS_MAX_POINTS 20_000`, context truncated to 2048 (`context_used`), `CHRONOS_MAX_HORIZON 1_024` (<= 15 rollouts of 9 paths), horizon > 64 gated behind `allow_long_horizon` |
| T-06-04 | Tampering | non-finite / all-null `y` | medium | mitigate | `is_finite` refusal; `>= 2 observed values` refusal; NaN handled as missing by the instance norm |
| T-06-13 | Repudiation | a parity claim that only ever passed because weights were absent | high | mitigate | `cfg_attr(..., ignore)` counted skips; the two-sided proof (unarmed >= 1 ignored / armed 0 ignored) in this plan's verify and in `just chronos-gate` (06-08) |
| T-06-SC | Tampering | cargo installs / python env | high | mitigate | no new registry crate (RESEARCH F9); `uv run --with huggingface_hub --with safetensors --with numpy` is a dev-time env for the fetch recipe only, never a runtime dependency |
</threat_model>

<verification>
- `just fetch-chronos-tiny` exits 0 with both pins verified; `git status` shows nothing under `models/`
- `pv validate` 0 errors; `pv status` non-zero counts
- Unarmed `cargo test ... -- bolt::parity chronos::parity`: `[1-9]+ ignored` with the arming reason; armed: `0 ignored`, >= 8 parity tests pass
- `cargo clippy -p aprender-forecast --all-targets -- -D warnings` clean; `crates/aprender-compute` untouched in the working tree AND across plan-start..HEAD
</verification>

<success_criteria>
- SC4's numeric bars hold in the library: 1e-6 abs f32 quantiles on Peyton, probes and rollout within contract, f16 within 2 % of std
- D-18's two constraints are proven (pinned fetch; counted skip both ways); D-13's horizon gate and warning are door-asserted; D-14 routing enforced by grep gates; D-05 aprender-compute unmodified
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-05)
New files: `justfile` recipe `fetch-chronos-tiny` (+ vars `chronos_rev`, `chronos_dir`); `crates/aprender-forecast/build.rs`; `crates/aprender-forecast/src/{bolt.rs, safetensors.rs, chronos.rs}`; `crates/aprender-forecast/tests/fixtures/chronos_bolt_tiny_config.json`; `contracts/chronos-bolt-parity-v1.yaml`. `Cargo.lock`: `half` joins the `aprender-forecast` entry's dependency list (already-resolved workspace dependency; no new registry package).
New symbols: `bolt::{Config, linear, dot8, linear_fast, proj, t5_layer_norm, relative_bucket, transpose, Attn, Block, Stack, Residual, Bolt, torch_quantile}` (spike names); `safetensors::{Tensor, Weights, load_bytes}`; `chronos::{CHRONOS_MIN_POINTS, CHRONOS_MAX_POINTS, CHRONOS_MAX_HORIZON, Model, ModelLoadError, Validated, ChronosArgs, ChronosResponse, load_model_from_bytes, load_model_from_dir, validate, forecast, load_csv}`; cfg `chronos_weights`; env vars `CHRONOS_MODEL_DIR` (runtime + arming), `CHRONOS_EMBED_DIR` (documented; consumed by 06-07's build.rs).
New tests: `chronos::tests::{config_fixture_sha_is_pinned, validate_refuses_*, validate_accepts_long_horizon_with_flag, chronos_args_schema_is_strict}`; `bolt::parity::{peyton_ladder_matches_oracle_f32, air_and_short100_ladders_match_oracle, rollout_365_matches_oracle_and_median_only_does_not, edge_probes_match_oracle, relative_buckets_match_fixture_over_grid}`; `chronos::parity::{f16_weights_within_two_percent_of_std, door_reports_warning_and_forwards_for_365, door_context_used_is_capped_at_2048}`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-05-SUMMARY.md` when done. Record: the printed f16 sha vs the advisory pin (A3), the validate()/forecast() D-08 deviation and rationale, both skip-proof summary lines verbatim, the measured max|Δ| per ladder rung on this host, and the X64 tolerance assumption.
</output>
