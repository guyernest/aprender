---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 06-native-time-series-forecasting-stack
plan: 08
type: execute
wave: 6
depends_on: [06-06, 06-07]
files_modified:
  - justfile
  - crates/aprender-forecast/examples/mase_rolling_origin.rs
  - .planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md
  - .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch
autonomous: false
requirements: [SC1, SC4, SC5]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The host-gated success criteria are MEASURED on the aarch64 release build and recorded with host, commit and date — never asserted from the spike's numbers: Peyton 3 000-point-class round trip < 2 s (SC1), 2 048-point Chronos forward < 100 ms (SC4), embedded tiny-f16 release binary < 30 MB (SC4), cold start to first forecast over stdio < 150 ms (SC4), 8 concurrent requests < half the sequential wall (SC5)"
    - "`just chronos-gate` is the phase's embedded-weights gate (D-18 clause 2, local form): it ALWAYS invokes `just fetch-chronos-tiny` (never `if missing`), so every f32/config/f16 sha256 is re-verified on every gate run including against a CI-mounted or cached weights directory; then embedded build, armed tests of both Chronos crates, and it FAILS unless both summaries show 0 ignored — status captured on its own line, never through a pipe (CLAUDE.md Verification Discipline #1; REVIEW-06-03, verified MEDIUM-HIGH: gating the fetch on file absence let a pre-existing weight file bypass the only integrity check there is)"
    - "`just forecast-pool-ratio` is where the SC5 speed-up bar is ASSERTED: it runs the release-profile pool test up to three times, greps the `POOL SPEEDUP:` line each run, and passes if the BEST of the three ratios is >= 2.0 — the unit test itself only prints (REVIEW-06-04, both reviewers: a wall-clock ratio inside libtest is flake-prone; a best-of-3 in a benchmark recipe is the honest home for it)"
    - "The spike-006 rolling-origin MASE harness ships as a compiled example (`cargo build --examples` is on CI's line 471), not a per-commit test (D-16)"
    - "No task in this plan edits .github/workflows/ci.yml: the exact step text is written as a patch file FIRST, the human decides at a blocking checkpoint, and plan 06-09 applies exactly the approved hunks (CLAUDE.md check-in-before-acting); if declined, the CI leg for D-18 is recorded as an open item, not silently dropped"
    - "The two demo pages perform initialize -> tools/list -> tools/call and chart a forecast (checked by a human at end of phase; the servers are started by the executor)"
  artifacts:
    - path: "justfile"
      provides: "recipes chronos-embed-build, chronos-gate, chronos-bench, chronos-coldstart, forecast-bench, forecast-pool-ratio, mase-rolling-origin"
      contains: "chronos-gate:"
    - path: "crates/aprender-forecast/examples/mase_rolling_origin.rs"
      provides: "spike-006 harness: 4 series x 17 windows, MASE/coverage/WQL3 for Prophet, NP-lite, naive, seasonal naive, Chronos-Bolt (weights optional at runtime)"
      contains: "seasonal naive"
    - path: ".planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md"
      provides: "the measured table: gate | bar | measured | host | commit | date | command"
      contains: "| gate |"
    - path: ".planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch"
      provides: "the proposed ci.yml step text (unified diff) for the human decision"
      contains: "chronos"
  key_links:
    - from: "justfile"
      to: "crates/aprender-mcp-chronos/src/main.rs"
      via: "chronos-coldstart runs the release binary with --coldstart N; chronos-bench runs --bench"
      pattern: "--coldstart"
    - from: "justfile"
      to: "crates/aprender-mcp-forecast/src/lib.rs"
      via: "forecast-pool-ratio runs `cargo test --release -p aprender-mcp-forecast --lib pool_equality -- --nocapture` with FORECAST_POOL_SERIES=peyton and reads the printed ratio"
      pattern: "FORECAST_POOL_SERIES=peyton"
    - from: "crates/aprender-forecast/examples/mase_rolling_origin.rs"
      to: "crates/aprender-forecast/tests/fixtures/chronos_holdout_oracle.json"
      via: "cross-checks Rust Chronos MAEs against the Python oracle per window (when weights are present)"
      pattern: "chronos_holdout_oracle"
  prohibitions:
    - statement: "Never label a run by intent — every number in 06-EVIDENCE.md cites the command, host (`uname -m`), profile and commit that produced it (CLAUDE.md Verification Discipline #2)"
      status: resolved
      verification: judgment
    - statement: "Never read `$?` through a pipe in a gate recipe (Verification Discipline #1)"
      status: resolved
      verification: test
    - statement: "Do not claim accuracy from one split or one series — the MASE harness uses rolling origins with naive and seasonal-naive rows and a horizon slice (forecaster-evaluation-and-routing.md)"
      status: resolved
      verification: judgment
    - statement: "Do not edit .github/workflows/ci.yml before the human decision; if declined, do not edit it at all"
      status: resolved
      verification: test
    - statement: "model: auto routing is deferred (CONTEXT) — the MASE example documents the measured routing rule, it does not implement a router"
      status: resolved
      verification: judgment
---

<objective>
Turn the timing, size and concurrency success criteria into host-gated `just` recipes with measured evidence,
ship the D-16 rolling-origin harness as an example, make `just chronos-gate` the phase's embedded-weights
gate (D-18), and put the CI-workflow question to the human as a concrete patch (CLAUDE.md: modifying
`.github/workflows/*.yml` is check-in-before-acting).

Purpose: SC1/SC4/SC5 carry numbers the X64 CI box and debug builds cannot assert (RESEARCH Open Question 5);
they must be measured where they hold and recorded as evidence, and the one CI edit D-18 wants must be a
human's call.
Output: seven recipes, one example, 06-EVIDENCE.md, the ci.yml patch file, and the decision.
</objective>

<review_dispositions>
## Cross-AI review dispositions carried by this plan (06-REVIEWS.md, `plans_commit: 88da44dcd`)

**REVIEW-06-03 — INCORPORATED (verified MEDIUM-HIGH).** `chronos-gate` previously ran `just fetch-chronos-tiny`
only when `{{chronos_dir}}/f32/model.safetensors` was missing, and every sha256 check lives inside that
recipe — so a cached, pre-mounted or tampered weight file was used unverified, which matters most for
exactly the runner-local `/models` mount this plan's own CI patch proposes across a trust boundary. The
recipe is now verify-always (06-05 Task 1) and this gate invokes it UNCONDITIONALLY.

**REVIEW-06-04 — INCORPORATED (verified MEDIUM).** The `speedup >= 2.0` assertion left 06-06's unit test and
landed here as a best-of-3 assertion in `forecast-pool-ratio`, which greps the `POOL SPEEDUP:` line the test
now prints. CI keeps the bit-identical equality assertion; the wall-clock ratio is a host-gated benchmark
claim and is recorded in 06-EVIDENCE.md with its arch, profile, worker and CPU counts.

**REVIEW-06-02 — INCORPORATED (verified premises, HIGH) — the decider needs this.** Task 3's checkpoint now
carries the quantified x86_64 risk and a fourth option. The 1.0e-6 f32 Chronos bar has ~4.6 % margin over the
aarch64-measured 9.54e-7, every CI job is `[self-hosted, X64, Linux, clean-room]`
(`.github/workflows/ci.yml:96,516,988,1039`), and on x86_64 `gemm_blis` does not dispatch the spike-008 NEON
microkernel, so inner-product accumulation order differs through a 12-layer T5. 06-05 added the arch-keyed
provisional bar; this checkpoint adds `measure-x86-first` so the human can buy the number before betting a
red CI leg on it.

**REVIEW-06-U5 — INCORPORATED (gemini LOW).** Recipes use `{{chronos_abs}}` (from `justfile_directory()`),
never `$PWD/{{chronos_dir}}`.

**REJECTED — the `cargo test -- a b` multi-filter finding (codex HIGH), for `chronos-gate` as for 06-05.
REFUTED BY EXPERIMENT.** `cargo test --lib -- alpha:: beta::` on cargo 1.98.0 ran both and reported
`2 passed; 2 filtered out`; modern libtest unions positional filters. The gate keeps the single two-filter
command, and its non-vacuity comes from the `0 ignored` + `>= 1 passed` checks it already makes on both logs.
</review_dispositions>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-native-time-series-forecasting-stack/06-CONTEXT.md
@.planning/phases/06-native-time-series-forecasting-stack/06-RESEARCH.md  (sections: F5 the ci.yml line 471 and the docker/self-hosted X64 runner; F6 "at least one CI leg"; Pitfall 8; Open Question 5; Validation Architecture manual rows)
@.planning/phases/06-native-time-series-forecasting-stack/06-VALIDATION.md  (Manual-Only Verifications table)
@.claude/skills/spike-findings-aprender/references/forecaster-evaluation-and-routing.md
@.claude/skills/spike-findings-aprender/references/chronos-mcp-server.md  (Constraints table: the bars' provenance)
@.planning/phases/06-native-time-series-forecasting-stack/06-06-SUMMARY.md
@.planning/phases/06-native-time-series-forecasting-stack/06-07-SUMMARY.md

<interfaces>
Bars (from ROADMAP SC1/SC4/SC5; spike-measured values in parentheses, Apple M4 Pro release):
  forecast-bench: prophet 3 000 daily points total < 2.0 s (0.215 s synthetic; Peyton 2 905 pts 1.41 s)
  chronos-bench: tiny-f16 forward at 2 048 context < 100 ms median (18.5 ms)
  chronos-embed-build: release binary size < 30 000 000 bytes (24.4 MB unstripped, 23.1 stripped)
  chronos-coldstart: median exec -> first forecast reply < 150 ms (52 ms; 85 cold cache)
  forecast-pool-ratio: sequential wall / concurrent wall >= 2.0 (3.9x)
  chronos-gate: both `test result:` lines contain `0 ignored` and rc == 0
CI facts: `.github/workflows/ci.yml:289` nextest --lib leg; `:471` the ONE --test line (its own comment at ci.yml:440-441: "one PR at a time may edit this single physical line without hitting a merge-queue conflict"); runner `[self-hosted, X64, Linux, clean-room]` inside docker `localhost:5000/sovereign-ci:stable` — network availability inside the clean-room container is UNKNOWN; propose a runner-local `/models` mount or an `actions/cache`-style step, not a bare download
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The just recipes (deployment surface) + the MASE rolling-origin example + the ci.yml proposal as a patch file</name>
  <precondition>models/chronos-bolt-tiny/{f32,f16}/model.safetensors exist (06-05); `uname -m` prints arm64 (the timing bars are aarch64-release only — on another host the recipes must still run and print, but the plan's evidence is taken here)</precondition>
  <files>justfile, crates/aprender-forecast/examples/mase_rolling_origin.rs, .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch</files>
  <read_first>
    - justfile (as extended by 06-05: header, variables, fetch-chronos-tiny)
    - .claude/skills/spike-findings-aprender/sources/006-chronos-vs-prophet-holdout/src/main.rs (190 lines: Series/splits, MASE denominator, seasonal-naive row, WQL3, horizon slice, the oracle cross-check at 150-170; uses fit.rs/np.rs/bolt.rs which are now `aprender_forecast::{fit, np, bolt, chronos}`)
    - .claude/skills/spike-findings-aprender/sources/006-chronos-vs-prophet-holdout/RUN-OUTPUT.md (the table the example must reproduce in shape)
    - crates/aprender-mcp-chronos/src/main.rs and crates/aprender-mcp-forecast/src/main.rs (the --bench / --coldstart flags the recipes wrap)
    - crates/aprender-mcp-forecast/src/lib.rs (pool_equality's FORECAST_POOL_SERIES selector and printed ratio line)
    - CLAUDE.md "Verification Discipline" 1-3 and "Shell Scripts: Use bashrs" (the recipes are bash bodies — `set -euo pipefail` inside each shebang recipe is correct; capture rc on its own line)
  </read_first>
  <action>
    (0) Before any edit, record the plan-start SHA once: `test -s /tmp/p06-08-base.sha || git rev-parse HEAD > /tmp/p06-08-base.sha`
    — every `.github` guard in this plan diffs the committed range from it as well as the working tree.
    (1) justfile recipes, each a `#!/usr/bin/env bash` shebang body with `set -euo pipefail`, redirecting cargo
    output to `target/p06-<name>.log` and capturing `rc=$?` on its own line before any `grep`/`tail`:
    Every absolute path below uses `{{chronos_abs}}` (defined in 06-05 as `justfile_directory() / chronos_dir`),
    never `$PWD/{{chronos_dir}}`: `$PWD` follows any `cd` inside a recipe body, `justfile_directory()` does not
    (REVIEW-06-U5).
    - `chronos-embed-build`: `CHRONOS_EMBED_DIR={{chronos_abs}}/f16 cargo build --release -p aprender-mcp-chronos`;
      prints the binary size via `wc -c < target/release/aprender-mcp-chronos` (portable; not `stat`); fails
      if size >= 30000000 (SC4 bar, with a comment citing the contract's qa_gate).
    - `chronos-gate`: runs `just fetch-chronos-tiny` UNCONDITIONALLY — never `if [ ! -f ... ]` (REVIEW-06-03).
      The recipe is idempotent and verify-always (06-05 Task 1): with the weights already present it
      re-hashes the f32 model, the config and the derived f16 against their pins and exits 0, and it exits
      non-zero naming the file on any mismatch. Gating this call on file absence is exactly what let a
      cached, pre-mounted or tampered weights directory reach the parity tests unverified — and the CI leg
      this plan proposes would mount weights across a trust boundary. Capture its status on its own line
      (`just fetch-chronos-tiny > "$LOG0" 2>&1; rc0=$?`) and fail the gate on `rc0 != 0` with the log tail,
      before any test runs. Then
      `CHRONOS_MODEL_DIR={{chronos_abs}}/f32 cargo test -p aprender-forecast --lib -- bolt::parity chronos::parity > log1 2>&1; rc1=$?`
      and `CHRONOS_EMBED_DIR={{chronos_abs}}/f16 CHRONOS_MODEL_DIR={{chronos_abs}}/f32 cargo test -p aprender-mcp-chronos --lib > log2 2>&1; rc2=$?`
      (the two positional filters on the first command are correct — see the rejected multi-filter finding in
      `<review_dispositions>`); fails unless rc0 == 0, rc1 == 0, rc2 == 0, AND both logs' `test result:` lines
      contain `0 ignored` AND a passed count >= 1 (grep after the rc capture). Prints `CHRONOS GATE: PASS`
      only on success.
    - `chronos-bench`: `chronos-embed-build`-style release build, then `--bench {{chronos_abs}}/f32 {{chronos_abs}}/f16`;
      parses the tiny-f16 "2048" row's median ms and fails if >= 100 (SC4).
    - `chronos-coldstart N="3"`: release build with embed, then `target/release/aprender-mcp-chronos --coldstart {{N}}`;
      fails if the printed median exec->forecast ms >= 150 (SC4).
    - `forecast-bench`: `cargo run --release -p aprender-mcp-forecast -- --bench 1000 3000`; fails if the
      3000-point prophet total seconds >= 2.0 (SC1).
    - `forecast-pool-ratio`: THIS RECIPE, not the unit test, asserts the SC5 ratio (REVIEW-06-04). Loop up to
      three attempts; each attempt runs
      `FORECAST_POOL_SERIES=peyton CARGO_INCREMENTAL=0 cargo test --release -p aprender-mcp-forecast --lib pool_equality -- --nocapture > "$LOG$i" 2>&1` with `rc=$?` on its own line,
      fails the whole recipe immediately if any attempt's `rc != 0` (that is a CORRECTNESS failure — a
      signature differed under load — and must never be retried away), extracts the ratio from the single
      `POOL SPEEDUP:` line with `grep -m1 '^POOL SPEEDUP: '` + `sed`/`awk` on the `<n>x` field, and records
      it. After the attempts, pass if the BEST ratio >= 2.0; print every attempt's ratio, the best, and the
      full `POOL SPEEDUP:` line of the best attempt (it carries arch, profile, workers and cpus). Fail with
      all three ratios listed if the best is below 2.0. Comment the retry policy: retries exist because CPU
      throttling and background load move the ratio, NOT because a failing assertion may be retried away —
      correctness failures are never retried.
    - `mase-rolling-origin`: `CHRONOS_MODEL_DIR={{chronos_abs}}/f32 cargo run --release -p aprender-forecast --example mase_rolling_origin`.
    Add a header comment block: "Phase 6 host-gated evidence recipes — timing/size bars hold on aarch64 release
    with the spike-008 NEON kernel; CI (X64, debug) asserts parity and refusals instead (06-RESEARCH Open Question 5)".
    Run `bashrs lint` on an extracted copy of each recipe body if bashrs supports stdin/files; otherwise record
    that just recipes are not in `scripts/`.
    (2) `crates/aprender-forecast/examples/mase_rolling_origin.rs` <- sources/006 main.rs adapted to the crate API:
    `aprender_forecast::{fit::fit_prophet, prophet, np, chronos::{load_model_from_dir, Model}, dates}`; CSVs from
    `tests/fixtures/` via `CARGO_MANIFEST_DIR`; the oracle from `tests/fixtures/chronos_holdout_oracle.json`;
    Chronos rows run only when `CHRONOS_MODEL_DIR` is set (print "chronos rows skipped: CHRONOS_MODEL_DIR unset"
    — an EXAMPLE, not a test, so this print is honest, not a vacuous green). Keep naive + seasonal-naive rows,
    MASE with period 7/12, cov80, width/σ, WQL3, the 1-64 vs 65+ horizon slice, and the Python cross-check.
    Header doc states the measured routing rule (monthly or <= 64 steps -> Chronos; else NP-lite/Prophet) as
    DOCUMENTATION of the spike-006 evidence — `model: auto` is deferred. Must compile under
    `cargo build --examples -p aprender-forecast` with clippy -D warnings (CI compiles all examples).
    (3) Write `.planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch` — a unified
    diff against the CURRENT `.github/workflows/ci.yml` (read it; do NOT apply it) with two hunks: (a) a new
    step named `Chronos embedded-weights gate` placed after the nextest lib leg, running `just chronos-gate`
    inside the same docker invocation pattern the leg uses, with the weights supplied by a runner-local mount
    (`-v /srv/models:/work/models`, path to be confirmed by the human) so the job needs no network, and an
    `if:` guard that fails loudly when the mount is absent rather than skipping; (b) on the single physical
    line 471, append `&& cargo test -p aprender-mcp-forecast --test e2e_stdio` immediately before the trailing
    `cargo build --examples --workspace --keep-going`. Verify the patch applies cleanly with
    `git apply --check` and then leave the tree unchanged (BOTH `git diff --stat HEAD -- .github` and
    `git diff --stat <plan-start>..HEAD -- .github` empty). The patch is the human's decision input for
    Task 3; plan 06-09 applies whichever hunks are approved.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo build --examples -p aprender-forecast > /tmp/p06-08-t1a.log 2>&1; rc=$?; tail -3 /tmp/p06-08-t1a.log; test $rc -eq 0 && CARGO_INCREMENTAL=0 cargo clippy -p aprender-forecast --all-targets -- -D warnings > /tmp/p06-08-t1b.log 2>&1; rc2=$?; tail -3 /tmp/p06-08-t1b.log; test $rc2 -eq 0</automated>
    <fails_when>the example fails to build or clippy reports under -D warnings</fails_when>
    <automated>just --list > /tmp/p06-08-t1c.log 2>&1; rc=$?; test $rc -eq 0 && for r in chronos-embed-build chronos-gate chronos-bench chronos-coldstart forecast-bench forecast-pool-ratio mase-rolling-origin; do grep -q "^ *$r" /tmp/p06-08-t1c.log || { echo "missing recipe $r"; exit 1; }; done; grep -c 'rc=\$?' justfile | awk '{ if ($1 < 6) { print "fewer than 6 rc captures in justfile"; exit 1 } }'</automated>
    <fails_when>just cannot parse the file, a recipe name is missing from --list, or fewer than 6 `rc=$?` captures exist (a gate reading status through a pipe)</fails_when>
    <automated>test -f .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch && git apply --check .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch > /tmp/p06-08-t1d.log 2>&1; rc=$?; cat /tmp/p06-08-t1d.log; test $rc -eq 0 && test "$(git diff --stat HEAD -- .github | wc -l)" -eq 0 && test -s /tmp/p06-08-base.sha && test "$(git diff --stat "$(cat /tmp/p06-08-base.sha)"..HEAD -- .github | wc -l)" -eq 0 && grep -q 'e2e_stdio' .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch && grep -q 'chronos-gate' .planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch</automated>
    <fails_when>the patch is missing, does not apply cleanly to the current ci.yml, anything under .github was modified — in the working tree (staged or not) or in any commit since the plan-start SHA (the patch was applied instead of proposed), the plan-start SHA file is missing, or a hunk is missing</fails_when>
  </verify>
  <done>Seven recipes parse and are listed; the example compiles lint-clean; every gate captures rc on its own line; the ci.yml proposal exists as a clean-applying patch with the tree untouched.</done>
  <acceptance_criteria>
    - `justfile` contains `chronos-gate:`, `chronos-embed-build:`, `chronos-bench:`, `chronos-coldstart`, `forecast-bench:`, `forecast-pool-ratio:`, `mase-rolling-origin:`, `0 ignored`, `30000000`, `FORECAST_POOL_SERIES=peyton`, `CHRONOS GATE: PASS`, `POOL SPEEDUP: `
    - REVIEW-06-03, checked on the region that decides it: `sed -n '/^chronos-gate/,/^[a-z]/p' justfile | grep -v '^[[:space:]]*#' | grep -c 'just fetch-chronos-tiny'` is 1 AND the same region contains no `if` / `test -f` / `[ ! -f` guard on the line before that call (read it — a conditional fetch is the defect)
    - REVIEW-06-04: the `forecast-pool-ratio` region contains `2.0` (the bar it now owns) and at least three attempt log paths or a loop over `1 2 3`; `06-06`'s lib.rs contains no `assert!(speedup` (checked there, not here)
    - `justfile` contains `{{chronos_abs}}` and `! grep -q '\$PWD/{{chronos_dir}}' justfile` (REVIEW-06-U5)
    - `crates/aprender-forecast/examples/mase_rolling_origin.rs` contains `seasonal naive`, `MASE`, `WQL3`, `chronos_holdout_oracle`, `CHRONOS_MODEL_DIR`, and a doc comment mentioning `model: auto` as deferred
    - `! grep -Eq '\| *(grep|tail|awk)[^;]*; *rc=\$\?' justfile` (no rc captured after a pipe)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Measure every host-gated bar on the aarch64 release build and record 06-EVIDENCE.md; start both servers for the end-of-phase demo-page check</name>
  <precondition>`uname -m` prints arm64 and `rustc -vV` shows host aarch64-apple-darwin (this box); models/chronos-bolt-tiny/{f32,f16} present</precondition>
  <files>.planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md</files>
  <read_first>
    - justfile (Task 1 recipes)
    - .planning/phases/06-native-time-series-forecasting-stack/06-VALIDATION.md (Manual-Only Verifications: the demo-page steps)
    - CLAUDE.md "Verification Discipline" #2 (prove the mechanism engaged — cite the trace/version line) and #6 (one input is an anecdote — run each timing recipe 3x)
  </read_first>
  <action>
    Run, in order, capturing each recipe's log: `just chronos-gate`, `just chronos-embed-build`, `just chronos-bench`
    (3 runs), `just chronos-coldstart 5`, `just forecast-bench` (3 runs), `just forecast-pool-ratio` (3 runs),
    `just mase-rolling-origin` (1 run; informational). Write `06-EVIDENCE.md` with a header (host `uname -m`,
    `sw_vers`/`uname -r`, `rustc -vV` host triple, `git rev-parse --short HEAD`, date) and one table
    `| gate | bar | measured (min/median/max over N) | pass | command | log |` for the six asserted bars plus
    the chronos-gate line (both `test result:` lines quoted, `0 ignored` each), and the MASE table pasted below
    it with the spike-006 table beside for comparison. A bar that FAILS is recorded as FAIL with the number —
    never edited to pass and never explained away in this file; the SUMMARY then names it as a finding for
    the human. Then start both servers for the end-of-phase human check: `cargo run --release -p aprender-mcp-forecast -- --http 8787 --pool 8 &`
    and `CHRONOS_EMBED_DIR=$PWD/models/chronos-bolt-tiny/f16 cargo run --release -p aprender-mcp-chronos -- --http 8788 &`;
    confirm `GET http://127.0.0.1:8787/` and `:8788/` return 200 with a body containing "tools/call" before
    handing over (never present a checkpoint with a broken environment).
  </action>
  <verify>
    <automated>just chronos-gate > /tmp/p06-08-t2a.log 2>&1; rc=$?; tail -10 /tmp/p06-08-t2a.log; test $rc -eq 0 && grep -q 'CHRONOS GATE: PASS' /tmp/p06-08-t2a.log && test "$(grep -c '0 ignored' /tmp/p06-08-t2a.log)" -ge 2 && test "$(grep -c '75068728d376d2bec670379eeef4bfb4d24c0cfe24d957451f8d19b447030a32' /tmp/p06-08-t2a.log)" -ge 1</automated>
    <fails_when>non-zero exit, no PASS line, fewer than two `0 ignored` summaries (a weight test was skipped while armed), or the f32 sha256 pin never appears in the gate's own output — that last one is the REVIEW-06-03 failing direction: it means the gate ran the tests without re-verifying already-present weights</fails_when>
    <automated>just chronos-embed-build > /tmp/p06-08-t2b.log 2>&1; rc=$?; tail -3 /tmp/p06-08-t2b.log; test $rc -eq 0 && just chronos-coldstart 5 > /tmp/p06-08-t2c.log 2>&1; rc2=$?; tail -5 /tmp/p06-08-t2c.log; test $rc2 -eq 0 && just chronos-bench > /tmp/p06-08-t2d.log 2>&1; rc3=$?; tail -5 /tmp/p06-08-t2d.log; test $rc3 -eq 0</automated>
    <fails_when>any of the three recipes exits non-zero — binary >= 30 MB, median cold start >= 150 ms, or 2048-context forward >= 100 ms on this host</fails_when>
    <automated>just forecast-bench > /tmp/p06-08-t2e.log 2>&1; rc=$?; tail -8 /tmp/p06-08-t2e.log; test $rc -eq 0 && just forecast-pool-ratio > /tmp/p06-08-t2f.log 2>&1; rc2=$?; tail -8 /tmp/p06-08-t2f.log; test $rc2 -eq 0 && grep -Eq '^POOL SPEEDUP: [0-9]+\.[0-9]{3}x .*profile=release' /tmp/p06-08-t2f.log && test -f .planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md && grep -c '^| ' .planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md | awk '{ if ($1 < 8) exit 1 }'</automated>
    <fails_when>forecast-bench exits non-zero (3 000-point total >= 2 s); pool-ratio exits non-zero (a signature differed under load, or the BEST of three ratios was < 2.0); the recipe's output carries no `POOL SPEEDUP:` line with `profile=release` (it measured a debug build, so the ratio means nothing — CLAUDE.md Verification Discipline #2: prove the mechanism engaged); or the evidence file is missing / has fewer than 8 table rows</fails_when>
    <human-check>Open http://127.0.0.1:8787/ (aprender-mcp-forecast, pool 8): the page's event log shows initialize -> tools/list -> tools/call; click "Load Peyton" then "Forecast" — a chart with yhat and a band appears and the log shows fit_seconds/predict_seconds. Repeat at http://127.0.0.1:8788/ (aprender-mcp-chronos, embedded tiny-f16): the forecast returns nine quantiles; set horizon 365 without the flag -> the page shows the allow_long_horizon refusal; with the flag -> a warning line. Type approved or describe what differs.</human-check>
  </verify>
  <done>All six bars measured on aarch64 release and recorded with provenance; chronos-gate PASS with 0 ignored twice; both demo servers running for the end-of-phase check.</done>
  <acceptance_criteria>
    - `06-EVIDENCE.md` contains `uname -m`, `arm64`, `rustc`, a 7+-character commit hash, a date, the strings `chronos-gate`, `0 ignored`, `< 30`, `< 150`, `< 100`, `< 2`, `>= 2.0`, and a `| gate |` header row
    - `06-EVIDENCE.md` quotes the best attempt's full `POOL SPEEDUP:` line (arch, profile, workers, cpus) and all three attempt ratios, and states that the bar is asserted by the recipe and not by any unit test (REVIEW-06-04)
    - `06-EVIDENCE.md` records the f32/config/f16 sha256 lines the gate's own `fetch-chronos-tiny` invocation printed, as the evidence that the gate verified the weights it then tested (REVIEW-06-03)
    - Each timing row records N >= 3 runs with min/median/max (cold start N = 5)
    - No row's "pass" column was derived from the spike numbers — every measured value has a `log` path under `target/`
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: DECISION — the ci.yml embedded-weights leg and e2e_stdio target (applied by 06-09 if approved)</name>
  <decision>Whether to edit `.github/workflows/ci.yml` now so one CI leg exercises the embedded-weights build (D-18 clause 2) and lists the two new integration targets, or defer the CI edit</decision>
  <context>
    CLAUDE.md makes modifying CI workflows a check-in-before-acting item. D-18 asks that "at least one CI leg actually exercises the embedded-weights build". Today: the CI runner is `[self-hosted, X64, Linux, clean-room]` inside a docker image whose network access is not established; ci.yml:440-441 documents line 471 as "one PR at a time may edit this single physical line without hitting a merge-queue conflict". Task 1 of this plan wrote the exact proposal as a unified diff to `.planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch` (verified to apply cleanly with `git apply --check`; the tree is untouched): (a) a new step `Chronos embedded-weights gate` after the nextest leg that runs `just chronos-gate` with `/models` provided by a runner-local cache mount (`-v /srv/models:/work/models`) or a prior `just fetch-chronos-tiny` cache step, tolerating no network inside the job; (b) `cargo test -p aprender-mcp-forecast --test e2e_stdio` appended to line 471. Locally, `just chronos-gate` already PASSES on this host (Task 2) — that is the phase's evidence either way.

    THE NUMERICAL RISK, QUANTIFIED (REVIEW-06-02, cross-AI review; premises independently verified by the
    orchestrator). The Chronos f32 quantile bar `quantiles_abs_f32 = 1.0e-6` was frozen on a measurement of
    `9.54e-7` taken on THIS aarch64 host with the spike-008 NEON microkernel — about 4.6 % of headroom. All
    four CI jobs run on `[self-hosted, X64, Linux, clean-room]` (`.github/workflows/ci.yml:96,516,988,1039`),
    where `trueno::blis::gemm_blis` does not dispatch that microkernel. Floating-point addition is not
    associative, so a different kernel changes the inner-product accumulation order through a 12-layer T5,
    and 4.6 % is a thin margin for that. Nobody has measured the x86_64 divergence — that is the whole point
    of the finding, and it is why gemini recommended `apply-stdio-only` or `defer`. TWO MITIGATIONS ARE
    ALREADY IN THE TREE, so `apply-now` is no longer a bare bet: 06-05 added
    `quantiles_abs_f32_nonaarch64 = 5.0e-6`, explicitly labelled PROVISIONAL-UNMEASURED, which the parity
    tests select on `!cfg!(target_arch = "aarch64")`; and every parity test prints the bar and arch it used,
    so the first x86_64 run yields the measured max|delta| the provisional bar is waiting for. A red CI leg
    is therefore unlikely but still possible, and the number it would produce is the number we want.
  </context>
  <options>
    <option id="apply-now">
      <name>Apply the patch now (both the chronos step and the e2e_stdio target)</name>
      <pros>D-18 clause 2 is met in CI; the stdio test stops being dark; the first x86_64 parity run produces the measured max|delta| that turns `quantiles_abs_f32_nonaarch64` from provisional into evidence.</pros>
      <cons>Edits ci.yml on this branch (the human's rule); needs the runner-local `/models` mount or cache to exist — the executor can add the step but cannot provision the runner; the x86_64 accumulation-order risk is unmeasured, so a red CI leg is possible (the provisional 5.0e-6 non-aarch64 bar makes it unlikely, not impossible).</cons>
    </option>
    <option id="measure-x86-first">
      <name>Measure x86_64 first, then decide (the cheap mitigation REVIEW-06-02 asks for)</name>
      <pros>Buys the missing number before betting a CI leg on it: run `just chronos-gate` ONCE on an x86_64 box (the sovereign runner, or any x86_64 Linux host with the weights) and read the printed `f32_quantile_bar` line plus the measured max|delta| from `peyton_ladder_matches_oracle_f32`; then tighten `quantiles_abs_f32_nonaarch64` to that measurement plus margin in a `pv diff`-visible contract edit and apply the patch with a bar that is evidence rather than headroom. Nothing about the local aarch64 evidence changes.</pros>
      <cons>Needs one x86_64 run before the phase can close the CI item; if no x86_64 host is reachable this collapses into `defer`.</cons>
    </option>
    <option id="apply-stdio-only">
      <name>Append only the `e2e_stdio` target to line 471; defer the weights leg</name>
      <pros>No runner provisioning needed; the fit server's stdio path gets CI coverage.</pros>
      <cons>D-18 clause 2 stays local-only (`just chronos-gate` evidence in 06-EVIDENCE.md), recorded as an open item.</cons>
    </option>
    <option id="defer">
      <name>Defer both; file as a follow-up ticket with the patch attached</name>
      <pros>No CI change in this phase; the local gate is the recorded evidence.</pros>
      <cons>Both the weights leg and the stdio test remain dark in CI; D-18 clause 2 is an acknowledged gap until the ticket lands.</cons>
    </option>
  </options>
  <resume-signal>Reply apply-now, apply-stdio-only, measure-x86-first, or defer (optionally followed by the runner mount path to use, e.g. "apply-now, mount /srv/models"). The SUMMARY then records the outcome on separate column-0 lines (no bullet, no backticks, no trailing text): exactly one line `CI decision: <token>` where `<token>` is the bare token — one of `apply-now`, `apply-stdio-only`, `measure-x86-first`, `defer` — and nothing else is on that line (plan 06-09 Task 3 gates every ci.yml edit on `^CI decision: (apply-now|apply-stdio-only|measure-x86-first|defer)$` and halts on 0 or 2+ matches, so a mount path appended to this line would read as "no decision on record"); one line `CI decision (verbatim): <the reply as typed>`; and one line `CI mount: <host path>` ONLY if a mount path was given (absent otherwise — 06-09 substitutes it for the patch's placeholder `/srv/models` before applying hunk (a)). Plan 06-09 applies exactly the approved hunks — no ci.yml edit happens in this plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence -> phase claims | numbers recorded here become the SC1/SC4/SC5 verdicts |
| CI workflow edit -> release gate surface | a CI step decides what ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Repudiation | evidence table | high | mitigate | every value carries command, host, profile, commit, log path; N >= 3 runs per timing bar; FAIL recorded as FAIL |
| T-06-15 | Tampering | gate recipes reading status through a pipe | high | mitigate | `rc=$?` on its own line in every recipe; the justfile grep gate in Task 1's verify |
| T-06-16 | Elevation of Privilege | CI workflow edit | high | mitigate | `gate="blocking-human"` decision with the patch written first; no edit under `defer` |
| T-06-06 | Tampering | weights in a CI cache/mount | high | mitigate | `just chronos-gate` re-verifies the sha256 pins via `fetch-chronos-tiny` when files are missing; the proposal names a mount, never a bare download |
| T-06-SC | Tampering | cargo installs | high | accept | none in this plan |
</threat_model>

<verification>
- `just chronos-gate` PASS with two `0 ignored` summaries; embed-build, coldstart, bench, forecast-bench, pool-ratio all exit 0 on the aarch64 release build
- `06-EVIDENCE.md` complete with provenance; `06-ci-chronos-step.patch` exists before the checkpoint
- `.github/` is unmodified by this plan under every option (working tree AND plan-start..HEAD); the SUMMARY carries exactly one bare-token line `CI decision: apply-now|apply-stdio-only|measure-x86-first|defer` (the reply as typed on its own `CI decision (verbatim):` line; the runner mount path, if any, on its own `CI mount:` line) that 06-09 Task 3 reads before it may touch ci.yml
</verification>

<success_criteria>
- SC1 (< 2 s), SC4 (< 100 ms forward, < 30 MB binary, < 150 ms cold start), SC5 (< half sequential wall) are measured and recorded, not asserted from the spike
- D-18 clause 2 is either met in CI (human-approved edit) or recorded as an explicit open item with the patch attached
- D-16's MASE harness ships as a compiled example
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06-08)
New files: `crates/aprender-forecast/examples/mase_rolling_origin.rs`; `.planning/phases/06-native-time-series-forecasting-stack/06-EVIDENCE.md`; `.planning/phases/06-native-time-series-forecasting-stack/06-ci-chronos-step.patch`.
New just recipes: `chronos-embed-build`, `chronos-gate`, `chronos-bench`, `chronos-coldstart N`, `forecast-bench`, `forecast-pool-ratio`, `mase-rolling-origin`.
Not modified here: `.github/workflows/ci.yml` — the proposed hunks (a `Chronos embedded-weights gate` step and/or `cargo test -p aprender-mcp-forecast --test e2e_stdio` on line 471) are applied by plan 06-09 only if approved at this plan's checkpoint.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-native-time-series-forecasting-stack/06-08-SUMMARY.md` when done. Record every measured bar (pass/fail with numbers) and the checkpoint outcome as separate column-0 lines (no bullet, no backticks, no trailing whitespace or text): exactly one line `CI decision: <token>` where `<token>` is exactly one of `apply-now`, `apply-stdio-only`, `measure-x86-first`, `defer` and nothing else is on that line (06-09 Task 3 greps `^CI decision: (apply-now|apply-stdio-only|measure-x86-first|defer)$` and requires exactly one match — anything appended to this line halts 06-09 with "no decision on record"); one line `CI decision (verbatim): <the human's reply as typed>`; and one line `CI mount: <host path>` only if the human named a runner mount path (omit the line otherwise). If `defer`/`measure-x86-first`/`apply-stdio-only`, also record the exact open item text for 06-09's deferred-items file. No diff is applied in this plan.
</output>
