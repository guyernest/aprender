---
phase: 03-faithful-two-stage-trainer-and-head
plan: 09
type: execute
wave: 6
depends_on: ["03-08"]
files_modified:
  - crates/aprender-train/Cargo.toml
  - crates/aprender-train/tests/ui.rs
  - crates/aprender-train/tests/ui/setfit_direct_state_construction.rs
  - crates/aprender-train/tests/ui/setfit_fit_head_before_tune.rs
  - crates/aprender-train/tests/ui/setfit_token_without_lock.rs
  - crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.rs
  - crates/aprender-train/tests/ui/setfit_probe_claims_setfit.rs
  - crates/aprender-train/tests/setfit_repro.rs
  - Makefile
autonomous: true
requirements: [TRN-01, TRN-06]

must_haves:
  truths:
    - "Every illegal lifecycle expression is a COMPILE error proven by trybuild: direct state construction, fit_head before tune_encoder, token without lock, pairs into fit_head, probe-as-SetFit — with .stderr snapshots naming the crate's real types and visibility"
    - "Two SEPARATE processes running the full pipeline from identical inputs produce identical composite hashes (selection IDs, pair order, batch boundaries, step count, loss trace, evidence hash, artifact hash, predictions) — including under DIFFERENT RAYON_NUM_THREADS values"
    - "The tier3 cross-process gate and the GEMM determinism gate are Make targets whose rc is captured directly, never through a pipe"
    - "cargo-mutants scoped to the new Phase 3 code reports >= 85% caught, or every survivor is individually justified and re-run to confirm survival (02-08 precedent)"
  artifacts:
    - path: "crates/aprender-train/tests/ui.rs"
      provides: "trybuild harness for the five non-constructibility proofs"
    - path: "crates/aprender-train/tests/setfit_repro.rs"
      provides: "In-process two-run equality + subprocess child mode for the cross-process gate"
    - path: "Makefile"
      provides: "setfit-repro-crossproc + gemm-thread-determinism targets wired into tier3"
  key_links:
    - from: "Makefile"
      to: "crates/aprender-train/tests/setfit_repro.rs"
      via: "tier3 target spawning two separate processes and comparing hash lines"
      pattern: "setfit-repro-crossproc"
    - from: "crates/aprender-train/tests/ui.rs"
      to: "tests/ui/*.rs"
      via: "trybuild compile_fail glob"
      pattern: "compile_fail"
  caveats:
    - "In-process two-run equality alone is structurally blind (shared pool/allocator/statics) — the cross-process form is the AUTHORITATIVE claim (D-16). Both must exist."
    - "cargo-mutants at --timeout 20, not 60: 02-08 measured 9% of mutants hanging; 60 s x hangs exceeds the wall budget."
    - "tier2 arm64 red (D-ITEM-02) and zero-test headline (D-ITEM-03) stand — verification is via the scoped commands and targets named here."
---

<objective>
Prove the phase's two structural claims end-to-end: TRN-01's legality by compile-fail evidence
(trybuild, Phase 2's house proof) and TRN-06's reproducibility by the authoritative cross-process
two-clean-runs gate at varying thread counts (D-16), then close the phase with the scoped
mutation run and final tier wiring.

Purpose: This is the phase's verification spine — everything earlier plans asserted becomes
either a compile error or a measured hash equality here.
Output: five UI cases + snapshots, in-process + cross-process repro gates, Make targets in tier3,
mutation evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key facts:
- trybuild pattern (verified): contrastive-data/tests/ui.rs harness (TestCases::new().compile_fail("tests/ui/*.rs")); each case a complete program using ONLY the public API, headed by obligation ID + expected diagnostic; snapshot rules: each .stderr must name REAL types/methods/visibility (a syntax-error snapshot proves nothing); re-baseline via TRYBUILD=overwrite then review the diff against the named-types list. trybuild = "1" is a workspace dep; aprender-train needs the dev-dependency line (match contrastive-data's form).
- Run trybuild as `cargo test -p aprender-train --test ui --features setfit`.
- Cross-process gate design (D-16 + Verification Discipline rule 1): a Make target runs the pipeline binary/test twice as SEPARATE processes (fresh pool, allocator, statics), each printing one composite hash line; statuses captured directly (`> /tmp/x.log 2>&1; rc=$?`); hashes compared with cmp/diff on extracted lines. Vary RAYON_NUM_THREADS between the two runs (1 vs host max) — same-pool-size-twice is the false-green STATE.md warns about.
- Composite hash contents (ROADMAP criterion 5): selection ordered IDs + semantic hash, per-epoch pair order digest, batch boundaries, step count, loss-trace hash, evidence table hash, artifact hash, predictions on the probe rows. All already deterministic by construction from earlier plans.
- Mutation scope (Ph1 D-25 / Ph2 D-26 precedent): new Phase 3 code only — crates/aprender-train/src/train/setfit/, crates/aprender-core/src/classification/multinomial.rs, crates/aprender-core/src/setfit/dropout_rng.rs. `cargo mutants --no-times --timeout 20 --in-place` with -f file scoping.
- 02-08 survivor discipline: 10/22 survivors were individually justified as unobservable and RE-RUN to confirm survival; the justified list goes in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: trybuild non-constructibility suite (five proofs)</name>
  <files>crates/aprender-train/Cargo.toml, crates/aprender-train/tests/ui.rs, crates/aprender-train/tests/ui/setfit_direct_state_construction.rs, crates/aprender-train/tests/ui/setfit_fit_head_before_tune.rs, crates/aprender-train/tests/ui/setfit_token_without_lock.rs, crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.rs, crates/aprender-train/tests/ui/setfit_probe_claims_setfit.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/tests/ui.rs (harness + snapshot rules doc :20-44)
    - crates/aprender-contrastive-data/tests/ui/split_constructed_directly.rs and its .stderr (case-file discipline: obligation header, public-API-only, expected diagnostic)
    - crates/aprender-contrastive-data/Cargo.toml [dev-dependencies] (the trybuild dep line form to copy)
    - crates/aprender-train/src/train/setfit/mod.rs, lock.rs (the real public surface the cases attack)
  </read_first>
  <action>
    Add the trybuild dev-dependency to aprender-train (same form as contrastive-data). Create the
    harness (compile_fail glob) with the snapshot-rules doc comment copied and adapted. Five
    cases, each a complete program using only the public API, headed by the requirement/decision
    it discharges and the expected diagnostic:
    (1) setfit_direct_state_construction — constructing SetFitRun<EncoderTuned> via struct
    literal / private field access (TRN-01/D-06; expect private-field or private-constructor
    diagnostics);
    (2) setfit_fit_head_before_tune — calling .fit_head() on SetFitRun<Prepared> (expect
    method-not-found-for-type diagnostic);
    (3) setfit_token_without_lock — constructing CanonicalTestToken directly (TRN-07/D-14;
    expect private constructor/field diagnostic);
    (4) setfit_pairs_into_fit_head — attempting to pass a pair collection to fit_head (TRN-05/
    D-08; expect wrong-number-of-arguments diagnostic — the signature takes zero non-self args);
    (5) setfit_probe_claims_setfit — converting FrozenProbeRun into any SetFitRun<_> (SAFE-03/
    D-11; expect no-From/Into/trait-not-implemented diagnostic).
    Generate .stderr snapshots (TRYBUILD=overwrite), then REVIEW each against the named-types
    rule: every snapshot must mention the real type or method name (SetFitRun, fit_head,
    CanonicalTestToken, FrozenProbeRun) — a case failing for an unrelated reason (typo, missing
    import) is a vacuous proof; fix the case, not the snapshot.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --test ui --features setfit > /tmp/p3-09t1.log 2>&1; rc=$?; tail -3 /tmp/p3-09t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --test ui --features setfit` exits 0 with 5 compile-fail cases
    - Each .stderr file names at least one real Phase 3 type/method (grep each for SetFitRun|fit_head|CanonicalTestToken|FrozenProbeRun >= 1 match)
    - No case file uses pub(crate) paths, #[doc(hidden)] items, or test-support doors — public API only (source review recorded in SUMMARY)
  </acceptance_criteria>
  <done>Illegal lifecycle expressions are compile errors with reviewed, type-naming evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Two-clean-runs gates — in-process and authoritative cross-process</name>
  <files>crates/aprender-train/tests/setfit_repro.rs, Makefile</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/mod.rs (full pipeline surface: prepare/tune_encoder/fit_head/verify_artifact, plus the PUBLIC reproducibility accessors 03-08 Task 2 landed — selection_semantic_hash/pair_order_digest/batch_boundaries/step_count/loss_trace_hash/evidence_table_hash/artifact_hash/probe_predictions; the ONLY surface the child may read, since pub(crate) internals are invisible to out-of-crate integration tests)
    - crates/aprender-train/src/train/setfit/test_fixtures.rs (whether the fixture is reachable from integration tests; if it is #[cfg(test)]-only, re-expose the needed constructor as #[doc(hidden)] test-support — record the choice)
    - crates/aprender-core/tests/gemm_thread_determinism.rs (the current_exe subprocess pattern from 03-02 to reuse)
    - Makefile tier3 block :305-320 (wiring style) and CLAUDE.md Verification Discipline rule 1
  </read_first>
  <action>
    setfit_repro.rs (integration test, --features setfit):
    - CHILD MODE (env SETFIT_REPRO_CHILD=1): build the fixture, run the full pipeline
      (prepare -> tune_encoder -> fit_head -> verify_artifact with SerdeJsonVerifier), print
      labeled lines: SELECTION=<semantic hash hex>, PAIRORDER=<sha256 of concatenated per-epoch
      order>, BATCHES=<count>:<sha256 of boundary list>, STEPS=<n>, LOSSTRACE=<hex>,
      EVIDENCE=<hex>, ARTIFACT=<hex>, PREDICTIONS=<sha256 of prediction bytes>, plus
      THREADS=<rayon::current_num_threads()>. Source every labeled line EXCLUSIVELY from the
      public reproducibility accessors 03-08 Task 2 landed; if any component lacks a public
      accessor, that is a 03-08 defect — add the accessor in mod.rs (public, read-only), never
      a test-support backdoor, never a widened pub(crate).
    - IN-PROCESS TEST: run the pipeline twice in one process, assert every component equal
      (fast tier2-class signal; D-16's non-authoritative half).
    - CROSS-PROCESS TEST: spawn two children via current_exe with RAYON_NUM_THREADS=1 and =host
      max; assert THREADS lines DIFFER (mechanism engaged — rule 2) and every other labeled
      line is identical.
    Makefile: `setfit-repro-crossproc` target running the cross-process test via
    `cargo test -p aprender-train --test setfit_repro --features setfit cross_process` with rc
    captured directly; wire into tier3 after contract-audit-phase3. Also add the
    `gemm-thread-determinism` target (deferred from 03-02): runs
    `cargo test -p aprender-core --test gemm_thread_determinism`, rc captured directly, wired
    into tier3 beside the repro gate. Evidence discipline for BOTH new targets: run standalone
    first (rc recorded), induce one failure (e.g. temporarily perturb a hash input in a scratch
    build or force a child env difference), observe red, revert, record both observations.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --test setfit_repro --features setfit > /tmp/p3-09t2.log 2>&1; rc=$?; tail -3 /tmp/p3-09t2.log; [ $rc -eq 0 ] || exit $rc; make setfit-repro-crossproc > /tmp/repro.log 2>&1; rc=$?; tail -2 /tmp/repro.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --test setfit_repro --features setfit` exits 0 (both in-process and cross-process tests)
    - The cross-process test asserts THREADS differs between children AND all eight labeled hashes match (both assertions present in source)
    - Makefile contains setfit-repro-crossproc and gemm-thread-determinism, both invoked from tier3, neither reading rc through a pipe (no `| tee` before rc capture in either recipe)
    - The induced-red observations for both targets are recorded in the SUMMARY with rc values
    - setfit_repro.rs reads composite-hash components only through the public accessors from 03-08 (no pub(crate) reach-ins, no #[doc(hidden)] backdoors for hash components; the file compiles as an ordinary out-of-crate integration test)
  </acceptance_criteria>
  <done>TRN-06's authoritative claim is a measured cross-process, cross-thread-count hash equality wired into tier3.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped mutation run + phase closing audit</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-08-SUMMARY.md (the measured --timeout 20 rationale and survivor-justification discipline)
    - crates/aprender-train/src/train/setfit/ (the mutation surface)
  </read_first>
  <action>
    Run cargo-mutants scoped to the Phase 3 surface, in three scoped invocations (mutants -f
    accepts file globs): (a) crates/aprender-train/src/train/setfit/**, (b)
    crates/aprender-core/src/classification/multinomial.rs, (c)
    crates/aprender-core/src/setfit/dropout_rng.rs — each with `cargo mutants --no-times
    --timeout 20 --in-place -f <scope>` plus the feature flags the scope needs (`--features
    setfit` legs via `-- --features setfit` test args as cargo-mutants requires; consult
    `cargo mutants --help` for the current arg-passing form and record the exact commands).
    Target: >= 85% caught per scope. For every survivor: justify individually (unobservable
    mutants per the 02-08 taxonomy: disjoint-bit ops, single-variant enums, accessors no
    constructible value distinguishes) and RE-RUN the justified set to confirm they still
    survive; kill the rest with targeted tests. Record the full table in the SUMMARY.

    Closing audit (the phase gate, all rc-captured directly):
    - `cargo test -p aprender-train --lib --features setfit` and `cargo test -p aprender-core
      --lib --features setfit` exit 0
    - `cargo test -p aprender-train --test ui --features setfit`, `--test setfit_repro` exit 0
    - `make contract-audit-phase3`, `make setfit-repro-crossproc`, `make
      gemm-thread-determinism`, `make setfit-feature-matrix` exit 0
    - `pv lint contracts/multinomial-head-v1.yaml contracts/setfit-train-lifecycle-v1.yaml`
      recorded
    - Darwin workspace form: `cargo check --workspace --exclude aprender-profile` exits 0
    - Known-red re-measured and recorded: `cargo package -p aprender-train --no-verify` still
      fails on the unpublished dep (expected; Pitfall 9)
    Mark TRN-01..TRN-07 + SAFE-03 complete via the requirements handler ONLY for those whose
    evidence is genuinely demonstrated by the audit (Phase 2 discipline: never check a box to
    make a table look finished; if any item is short, leave it unchecked and say why).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit > /tmp/p3-09t3.log 2>&1; rc=$?; tail -3 /tmp/p3-09t3.log; [ $rc -eq 0 ] || exit $rc; make contract-audit-phase3 > /tmp/cap3.log 2>&1; rc=$?; tail -2 /tmp/cap3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - Mutation table in SUMMARY: total mutants, caught, survivors each with justification, justified set re-run confirmation
    - Every closing-audit command's rc recorded in the SUMMARY (captured directly, no pipes)
    - The known-red package measurement recorded with its error message
    - REQUIREMENTS.md updates reflect only genuinely demonstrated items
  </acceptance_criteria>
  <done>The phase's evidence is complete, measured, and honestly booked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time claims → runtime behavior | trybuild is the proof the typestate claims are real |
| single-process green → reproducibility claim | cross-process gate is the authoritative boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-30 | Repudiation | in-process-only repro passing for the wrong reason (shared pool/statics) | mitigate | cross-process gate with DIFFERENT thread counts, mechanism-engaged assertion (THREADS lines differ) |
| T-3-31 | Tampering | vacuous trybuild snapshots (syntax-error proofs) | mitigate | named-types review rule per snapshot; acceptance grep per .stderr |
| T-3-32 | Repudiation | gates whose rc is read through a pipe (repo has shipped this twice) | mitigate | direct rc capture in both new Make targets; acceptance criterion greps recipes |
| T-3-33 | Tampering | mutation survivors waved through unjustified | mitigate | 02-08 discipline: individual justification + re-run confirmation, table in SUMMARY |
| T-3-SC | Tampering | package installs | accept | none this plan (trybuild is an existing workspace dep) |
</threat_model>

<verification>
- All scoped Phase 3 test commands exit 0 (train lib, core lib, ui, setfit_repro)
- tier3's four Phase 3 targets (contract-audit-phase3, setfit-repro-crossproc, gemm-thread-determinism, setfit-feature-matrix) exit 0 standalone
- Mutation >= 85% caught or survivors justified + re-run
</verification>

<success_criteria>
- TRN-01 proven by five reviewed compile-fail cases
- TRN-06 proven cross-process at differing thread counts with all eight components hash-equal
- Phase evidence booked honestly; known-red states re-measured and recorded
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-09-SUMMARY.md` when done.
Record: the mutation table, the induced-red evidence for both new gates, the closing-audit rc
list, and the exact cargo-mutants invocations used.
</output>
