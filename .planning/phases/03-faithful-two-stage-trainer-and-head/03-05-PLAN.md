---
phase: 03-faithful-two-stage-trainer-and-head
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/tune.rs
  - crates/aprender-train/src/train/setfit/evidence.rs
  - crates/aprender-train/src/train/setfit/test_fixtures.rs
autonomous: true
requirements: [TRN-03, TRN-06]

must_haves:
  truths:
    - "One tuning run produces: a step-ordered f32 loss trace and its SHA-256 hash, per-named-parameter init/grad/delta norms computed through reduce.rs only, the pre-clip global norm per step, and a canonical-bytes evidence table with a binding hash"
    - "Two in-process runs from identical inputs produce bitwise-identical loss traces, evidence-table hashes, and step counts (first TRN-06 signal)"
    - "The evidence table is keyed by HF dotted parameter names in BTreeMap order; no Duration, wall-clock, or HashMap-ordered field exists in any hashed structure"
    - "A measured relative-delta distribution exists for the weakest cell (smallest-shot, 1 epoch) AND for a 1e-30-LR control run, giving plan 03-06 the basis to freeze epsilon"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/tune.rs"
      provides: "pub(crate) run_tuning — the full stage-one loop, record-only (gate arms in 03-06)"
    - path: "crates/aprender-train/src/train/setfit/evidence.rs"
      provides: "UpdateEvidence table + EvidenceSummary + canonical bytes + table hash"
    - path: "crates/aprender-train/src/train/setfit/test_fixtures.rs"
      provides: "Deterministic tiny encoder + Selection + pair-config fixture shared by Phase 3 trainer tests (synthetic text only)"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "aprender-contrastive-data pair replay"
      via: "replayed pairs per epoch, permuted by epoch.rs"
      pattern: "epoch_pair_order"
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "SetFitMiniLm"
      via: "set_train_step + train-mode forward + pair_cosine_mse"
      pattern: "set_train_step"
    - from: "crates/aprender-train/src/train/setfit/evidence.rs"
      to: "crates/aprender-train/src/train/setfit/reduce.rs"
      via: "every norm/mean computed through fixed-order reductions"
      pattern: "in_index_order"
  caveats:
    - "This plan does NOT mint SetFitRun<EncoderTuned>. run_tuning is pub(crate) and record-only; the public tune_encoder transition with the armed gate is plan 03-06 (epsilon must be frozen in the contract BEFORE any pass/fail comparison — Ph1 D-14 discipline)."
    - "Fixture texts are SYNTHETIC — never TweetEval tweet text (provenance rule: no tweet text committed)."
---

<objective>
Build stage one: the encoder-tuning loop over Phase 2's replayed pair stream with keyed dropout ON
(D-15), fixed-order evidence capture (D-9/D-13), the reference optimizer stack (AdamW 2e-5, clip
1.0, warmup-linear-decay), and the canonical evidence table with its binding hash (D-12) — then
run the controlled instrumentation that measures the relative-delta distribution so 03-06 can
freeze epsilon before arming the gate.

Purpose: TRN-03's evidence has to be captured before it can be judged; TRN-06's loss trace has to
be born deterministic. This plan produces both, record-only.
Output: run_tuning + UpdateEvidence/EvidenceSummary + shared deterministic test fixture +
measured epsilon basis (real-run vs 1e-30-run separation) recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md):
- Loop shape (RESEARCH evidence-capture example): named params via SetFitMiniLm::trainable_parameters_mut() -> Vec<(String, &mut Tensor)> (HF dotted names — exactly D-09's granularity); init norms BTreeMap<String, f64> BEFORE step 0; per step: backward -> per-name grad norms (fixed-order, f64) -> clip_grad_norm_refs(params, 1.0) -> f32 pre-clip global norm recorded -> AdamW::step_refs -> scheduler step.
- AdamW::new(lr, beta1, beta2, epsilon, weight_decay) with reference constants (2e-5 from config, 0.9, 0.999, 1e-8, 0.0); step_refs(&mut [&mut Tensor]); aprender-train's Tensor is the re-exported aprender-core autograd Tensor (lib.rs:139) — compatible with trainable_parameters_mut.
- pair_cosine_mse(za, zb, labels: &[f32]) -> Result<Tensor, SetFitError> (setfit/loss.rs:71) — the stage-one objective, consumed not rebuilt.
- Pair stream: REPLAYED per epoch from aprender-contrastive-data (pairs are never stored; only the manifest hash persists — Ph2 D-09). Epoch e's order = epoch_pair_order(root_seed, e, n_pairs) from 03-03; batches = consecutive windows, last short, never resampled.
- Dropout ON during tuning via 03-02's keyed source; thread the step counter with set_train_step(step) before each forward.
- Loss trace: Vec<f32> in step order; hash = SHA-256 over the LE f32 bit patterns in step order (sha2 is already an unconditional aprender-train dep). NEVER hash OptimizationResult or anything carrying Duration (Pitfall 4).
- Evidence canonical form (ledger.rs analog): fixed-field-order wire struct + schema_version + serde_json + #[serde(deny_unknown_fields)]; Vec order IS the event order; BTreeMap iteration discharges name ordering structurally (Ph2 02-03 precedent). Summary fields (D-12): verdict, trainable/frozen counts, min/median/worst relative delta, worst-offending parameter name, epsilon + contract version used, SHA-256 of the complete per-parameter table. Full table emitted separately as machine-readable JSON.
- Embedding delta (RESEARCH Open Question 5): measured on the selected training rows themselves — encode before tuning and after, eval mode, PINNED batch composition (consecutive Selection-order windows of config batch size), per-row relative L2 delta, aggregate min/median/max. Pair-loss endpoint stat: mean of first k vs last k steps (k and margin frozen in 03-06's contract; this plan computes and stores both means).
- Test model: Phase 1's tests construct encoders without network. Read crates/aprender-core/src/setfit/encoder_tests.rs and setfit/mod.rs test modules for the existing tiny-model constructor path and reuse it; if it is not exposed across crates, expose a #[doc(hidden)] test-support constructor in aprender-core behind the setfit feature (smallest surface, documented as test-support).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic trainer test fixture (shared by all Phase 3 trainer tests)</name>
  <files>crates/aprender-train/src/train/setfit/test_fixtures.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/encoder_tests.rs (how Phase 1 constructs test encoders without network — the constructor path to reuse)
    - crates/aprender-core/src/setfit/mod.rs (SetFitMiniLm construction surface; #[cfg(all(test, feature = "setfit"))] gating precedent at ~:460)
    - crates/aprender-contrastive-data/tests/common/ (how Phase 2 tests build PreparedDataset/Selection from synthetic rows)
    - crates/aprender-contrastive-data/src/select.rs and pairs.rs (the public path: selection + PairConfig + replay)
  </read_first>
  <action>
    Create a #[cfg(test)]-gated (or #[doc(hidden)] test-support, matching what cross-module reuse
    requires) fixture module providing: (a) a tiny deterministic SetFitMiniLm via the Phase 1
    test-constructor path (small hidden size, deterministic weights from a documented seed —
    whatever the existing path provides; if aprender-core only exposes it to its own tests, add a
    #[doc(hidden)] pub fn behind the setfit feature there and note it in the SUMMARY); (b) a
    synthetic K=3 dataset (about 8 examples per class, SYNTHETIC texts like "class-a sample 0" —
    no TweetEval content) run through the real contrastive-data path to yield a PreparedDataset,
    Selection (smallest-shot), and PairConfig with a small budget; (c) a
    SetFitTrainConfig::reference_defaults-derived config shrunk for test speed (1 epoch, batch 4).
    The fixture must be pure: two calls yield structurally identical objects (assert
    selection.semantic_hash equality in a fixture self-test).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit fixture_ > /tmp/p3-05t1.log 2>&1; rc=$?; tail -3 /tmp/p3-05t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit fixture_` exits 0
    - The fixture self-test asserts two builds produce equal selection semantic hashes
    - grep of test_fixtures.rs for `abortion|tweet|stance` = 0 matches (synthetic text only)
  </acceptance_criteria>
  <done>Every later trainer test has one deterministic, network-free, synthetic-text fixture door.</done>
</task>

<task type="auto">
  <name>Task 2: run_tuning — the stage-one loop, record-only</name>
  <files>crates/aprender-train/src/train/setfit/tune.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/optim/adamw.rs (step_refs surface :184 region; constructor)
    - crates/aprender-train/src/optim/clip.rs (:65 — returns PRE-clip global norm)
    - crates/aprender-core/src/setfit/loss.rs (pair_cosine_mse :71)
    - crates/aprender-core/src/setfit/mod.rs (trainable_parameters_mut :440, frozen_parameters, apply_freeze, set_training, set_train_step from 03-02, encode_texts)
    - crates/aprender-contrastive-data/src/pairs.rs (replay surface: how to iterate the budgeted pair stream for a Selection)
    - crates/aprender-train/src/train/setfit/epoch.rs and reduce.rs (as landed by 03-03)
  </read_first>
  <action>
    Implement `pub(crate) fn run_tuning(encoder, selection, config) -> Result<TuneOutput,
    SetFitTrainError>` in tune.rs. Per epoch e in 0..config.epochs: derive the pair order via
    epoch_pair_order(root_seed, e, n_pairs); replay pairs from contrastive-data in that order;
    batch in consecutive windows of config.batch_size (last short, never resampled); for each
    batch: set_train_step(global_step), train-mode forward of both pair sides through the
    encoder, pair_cosine_mse, backward; capture per-name gradient norms via
    reduce::l2_norm_in_index_order BEFORE clipping; clip_grad_norm_refs(params,
    config.grad_clip_max_norm) recording the returned pre-clip norm; AdamW::step_refs (AdamW
    constructed once with config.encoder_lr, 0.9, 0.999, 1e-8, 0.0); WarmupLinearDecayLR::step
    + apply (total_steps = epochs * ceil(n_pairs/batch), warmup_steps = round(warmup_proportion
    * total_steps)); push the scalar loss (f32) onto the trace. Apply config.freeze policy via
    apply_freeze BEFORE capturing init norms (frozen params are exempt from evidence per D-09;
    zero-match is apply_freeze's typed error). TuneOutput carries: tuned encoder, init-norm
    BTreeMap, per-name grad-norm aggregates (max and index-order mean across steps) and final
    delta norms, pre-clip norm series, loss trace Vec<f32> + its SHA-256 (LE f32 bits, step
    order), step count, first-k/last-k loss means (k = min(5, steps/2) computed but NOT judged
    here), and the before/after eval-mode embeddings of the selected rows (pinned consecutive
    Selection-order batches, set_training(false) around both encodes, dropout off in eval).
    No pass/fail judgment anywhere in this plan.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit tune_ > /tmp/p3-05t2.log 2>&1; rc=$?; tail -3 /tmp/p3-05t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit tune_` exits 0
    - A test runs run_tuning twice on the fixture and asserts bitwise-equal loss-trace hashes and equal step counts (in-process two-run signal)
    - A test asserts step count == epochs * ceil(n_pairs / batch_size) on the fixture
    - tune.rs contains `clip_grad_norm_refs` and `step_refs` (reuse, not reimplementation); grep `par_iter` in train/setfit/ = 0 matches
    - A test asserts the loss trace is finite at every step
  </acceptance_criteria>
  <done>Stage one runs end-to-end deterministically on the fixture and records everything the gate will later judge.</done>
</task>

<task type="auto">
  <name>Task 3: Evidence table + canonical bytes + instrumentation runs for the epsilon basis</name>
  <files>crates/aprender-train/src/train/setfit/evidence.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/ledger.rs (to_canonical_bytes/from_bytes/ledger_hash — the canonical-record analog)
    - crates/aprender-contrastive-data/src/attestation.rs (deny_unknown_fields precedent :73,:87)
    - crates/aprender-train/src/train/setfit/tune.rs (TuneOutput as landed in Task 2)
  </read_first>
  <action>
    evidence.rs: `UpdateEvidence` — per-parameter rows {name, init_norm, delta_norm,
    relative_delta, grad_norm_max, grad_norm_mean, steps_observed} in BTreeMap name order;
    plus run-level fields {loss_trace_hash, step_count, first_k_mean, last_k_mean, k,
    embedding_delta_min/median/max, pre_clip_norm_max}. Wire struct with schema_version,
    #[serde(deny_unknown_fields)], fixed field order, serde_json canonical bytes; table_hash =
    SHA-256 of those bytes. `EvidenceSummary` (D-12): verdict enum (Unjudged in this plan;
    Pass/Fail arms in 03-06), trainable_count, frozen_count, min/median/worst relative_delta,
    worst_param_name, epsilon_used: Option (None while unjudged), contract_version, table_hash.
    All numeric aggregation through reduce.rs. Tests: canonical bytes bitwise-stable across two
    identical runs; deny_unknown_fields rejects an extended payload; a mutated table no longer
    matches its hash; NO field of any serialized struct is a Duration/timestamp (grep-style test
    or type-level assertion).

    Instrumentation (tests, measurement not judgment): (a) real fixture run at reference-like
    settings — record per-parameter relative_delta min/median/max and per-parameter-class
    breakdown (embeddings vs LayerNorm vs attention/FFN, by name prefix); (b) control run at
    encoder_lr = 1e-30 — record the same; (c) assert the two distributions are SEPARATED (max
    relative_delta of the 1e-30 run < min relative_delta of the real run, on the fixture) and
    print both spreads with --nocapture-friendly formatting. Copy the measured numbers into the
    SUMMARY under an "epsilon basis" heading — plan 03-06 freezes epsilon from them (rule it
    will apply: at least 10x above the 1e-30 run's max, at least 10x below the real run's min;
    flag in the SUMMARY if the fixture cannot support that margin).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit evidence_ > /tmp/p3-05t3.log 2>&1; rc=$?; tail -3 /tmp/p3-05t3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit evidence_` exits 0
    - evidence.rs uses BTreeMap for the per-parameter table (grep `BTreeMap` >= 1) and contains `deny_unknown_fields`
    - grep `Duration|Instant|SystemTime|chrono` in evidence.rs = 0 matches
    - The separation test passes and its two distributions (real vs 1e-30) appear in the SUMMARY as concrete numbers
    - A hash-binding test proves a single-field mutation changes table_hash
  </acceptance_criteria>
  <done>Evidence is canonical, hash-bound, Duration-free, and the epsilon freeze in 03-06 has a measured basis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence table → summary consumers | Phase 4 APR field and Phase 5 EVAL-03 row trust the summary |
| loss trace → reproducibility claims | TRN-06 verdicts hang off this hash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-16 | Tampering | forged/edited evidence summary | mitigate | summary carries SHA-256 of the full canonical table; mutation test proves binding (D-12) |
| T-3-17 | Repudiation | nondeterministic trace (thread pools, map order, timestamps) | mitigate | reduce.rs-only aggregation, BTreeMap order, Duration-free structs, two-run bitwise test |
| T-3-18 | Information Disclosure | dataset text leaking into committed fixtures | mitigate | synthetic-text-only fixture with grep gate (no TweetEval content committed) |
| T-3-19 | Tampering (V5) | freeze policy matching zero parameters silently | mitigate | apply_freeze's zero-match typed error engaged before init-norm capture |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit tune_` / `evidence_` / `fixture_` all exit 0
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0 (closure holds after the new modules)
</verification>

<success_criteria>
- Stage one runs deterministically twice in-process with identical hashes
- Evidence table canonical + hash-bound; summary schema matches D-12's field list
- Measured epsilon basis recorded (real vs 1e-30 separation numbers in SUMMARY)
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-05-SUMMARY.md` when done.
Record: the epsilon-basis numbers (both distributions), k used for endpoint means, and any
test-support surface added to aprender-core for the fixture encoder.
</output>
