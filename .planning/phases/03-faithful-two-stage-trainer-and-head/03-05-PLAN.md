---
phase: 03-faithful-two-stage-trainer-and-head
plan: 05
type: execute
wave: 2
depends_on: ["03-02", "03-03"]
files_modified:
  - crates/aprender-core/src/autograd/mod.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/tune.rs
  - crates/aprender-train/src/train/setfit/evidence.rs
  - crates/aprender-train/src/train/setfit/test_fixtures.rs
  - crates/aprender-train/tests/setfit_calibration.rs
autonomous: true
requirements: [TRN-03, TRN-06]

must_haves:
  truths:
    - "The step body runs in exactly this order and a source-asserted test pins it: set scheduled LR -> clear grads and tape -> forward A (branch 0) -> forward B (branch 1) -> loss -> backward -> per-name grad norms -> clip -> AdamW step -> scheduler advance -> clear tape. Gradients never accumulate across batches and step 0 runs at the warmup LR, not the peak LR"
    - "The pair ordinals and batch boundaries actually CONSUMED by the loop are hashed in-band as the loop consumes them — never recomputed afterwards from configuration, so two runs that use the same wrong order cannot reproduce a correct-looking digest"
    - "Relative delta is `||dTheta|| / max(||theta_init restricted to the delta's support||, s_class)` and is finite for a zero-initialized parameter; `||dTheta|| > 0` is a separate strict predicate"
    - "Two in-process runs from identical inputs produce bitwise-identical loss traces, consumed-pair digests, batch-boundary digests, evidence-table hashes, and step counts (first TRN-06 signal)"
    - "A measured relative-delta distribution exists across a calibration MATRIX (>= 3 seeds x >= 2 shot/epoch boundary configurations) plus a 1e-30-LR control, per parameter CLASS, giving plan 03-06 the basis to freeze per-class epsilon and record its calibration regime"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/tune.rs"
      provides: "pub(crate) run_tuning — the full stage-one loop with in-band execution digests, record-only (gate arms in 03-06)"
    - path: "crates/aprender-train/src/train/setfit/evidence.rs"
      provides: "UpdateEvidence table + EvidenceSummary + canonical bytes + binding table hash"
    - path: "crates/aprender-train/src/train/setfit/test_fixtures.rs"
      provides: "Deterministic encoder + Selection + pair-config fixture shared by Phase 3 trainer tests (synthetic text only)"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "aprender-contrastive-data pair replay"
      via: "PairSampler::pair_at over the epoch permutation, with each consumed ordinal fed to the in-band digest"
      pattern: "pair_at"
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "SetFitMiniLm"
      via: "set_forward_ordinal(2*step + branch) + train-mode forward + pair_cosine_mse"
      pattern: "set_forward_ordinal"
    - from: "crates/aprender-train/src/train/setfit/evidence.rs"
      to: "crates/aprender-train/src/train/setfit/reduce.rs"
      via: "every norm/mean computed through fixed-order reductions"
      pattern: "in_index_order"
  caveats:
    - "This plan does NOT mint SetFitRun<EncoderTuned>. run_tuning is pub(crate) and record-only; the public tune_encoder transition with the armed gate is plan 03-06 (per-class epsilon must be frozen in the contract BEFORE any pass/fail comparison — Ph1 D-14 discipline)."
    - "Fixture texts are SYNTHETIC — never TweetEval tweet text (provenance rule: no tweet text committed)."
    - "The fixture encoder is a REAL slice of the pinned MiniLM (tests/fixtures/setfit/slice_config.json: hidden 64, 2 layers, 2 heads, intermediate 256, vocab 97, source_revision 1110a243..., real pretrained weights). It is NOT synthetic in its VALUES — only in its DIMENSIONS. Say so precisely in the SUMMARY; the calibration argument depends on the distinction."
---

<objective>
Build stage one: the encoder-tuning loop over Phase 2's replayed pair stream with keyed
branch-independent dropout ON (D-15 as amended), a correct optimizer step order, in-band recording
of the execution actually performed, fixed-order evidence capture (D-09/D-13), the reference
optimizer stack (AdamW 2e-5, clip 1.0, warmup-linear-decay), and the canonical evidence table with
its binding hash (D-12) — then run the calibration MATRIX that measures the per-class relative-delta
distribution so 03-06 can freeze per-class epsilon before arming the gate.

Purpose: TRN-03's evidence has to be captured before it can be judged; TRN-06's loss trace and
execution digests have to be born deterministic AND recorded rather than reconstructed. This plan
produces all of it, record-only.
Output: run_tuning + UpdateEvidence/EvidenceSummary + shared deterministic test fixture + the
measured calibration matrix recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md, re-verified against the repo this session):
- AdamW::new(lr, beta1, beta2, epsilon, weight_decay) with reference constants (2e-5 from config, 0.9, 0.999, 1e-8, 0.0); `step_refs(&mut [&mut Tensor])` (adamw.rs:184); `set_lr(&mut self, lr: f32)` (adamw.rs:261). aprender-train's Tensor is the re-exported aprender-core autograd Tensor (lib.rs:139) — compatible with trainable_parameters_mut.
- `clip_grad_norm_refs(&mut [&mut Tensor], max_norm) -> f32` returns the PRE-clip global norm (clip.rs:65).
- `pair_cosine_mse(za, zb, labels: &[f32]) -> Result<Tensor, SetFitError>` (setfit/loss.rs:71) — takes TWO [B,H] matrices, i.e. TWO separate encoder forwards per step. VERIFIED by reading.
- **Gradients ACCUMULATE.** `Tensor::accumulate_grad` adds into an existing `grad` (autograd/tensor.rs:245-260); `Tensor::zero_grad_(&mut self)` sets it to None (:236). Nothing clears it implicitly. VERIFIED by reading — this is why the loop must clear explicitly.
- **The tape is a thread-local that grows.** `GRAPH: RefCell<ComputationGraph>` (autograd/mod.rs:63-65) with `pub fn clear_graph()` (:117). Every recorded op appends to `self.tape`; nothing prunes it. VERIFIED by reading — the loop must clear per step or the tape grows without bound across an epoch.
- **`no_grad(f)` exists and is honored.** `autograd::no_grad` flips a thread-local `GRAD_ENABLED` (autograd/mod.rs:76-99) and the ops layer checks `is_grad_enabled()` (activation.rs 11 sites, ops/mod.rs 14 sites, embedding/normalize/pooling/similarity 1-2 each). VERIFIED by reading — the baseline encodes use it.
- **`PairSampler::pair_at(ordinal) -> LabeledPair` is O(1) random access** (pairs.rs:1020), and `LabeledPair` carries a `CanonicalPair` of `SelectedId`s plus a target. Permuting the replay stream is O(B), not O(B^2).
- Text lives in the dataset, not the Selection: `SelectedExample {id, label, exact_hash, normalized_hash}` (select.rs:81) has no text; `Split<Train>::rows() -> &[LabeledExample]` (split.rs:175) does. SetFitRun holds `dataset` (03-03 Task 1).
- `MiniLmTokenizer` truncates at `MAX_SEQUENCE_LENGTH = 256` (tokenizer.rs:52) and `encode_texts` takes no length parameter — max_length is an equality constraint, not a runtime setting.
- Loss trace: Vec<f32> in step order; hash = SHA-256 over the LE f32 bit patterns in step order (sha2 is already an unconditional aprender-train dep). NEVER hash OptimizationResult or anything carrying Duration (Pitfall 4).
- Evidence canonical form (ledger.rs analog): fixed-field-order wire struct + schema_version + serde_json + `#[serde(deny_unknown_fields)]`; Vec order IS the event order; BTreeMap iteration discharges name ordering structurally (Ph2 02-03 precedent).
- Test model: `SetFitMiniLm::from_slice_fixture(&fixtures_dir(), SEED)` (setfit/mod.rs:236) loads a real MiniLM slice from `crates/aprender-core/tests/fixtures/setfit/`; `fixtures_dir()` honours `APRENDER_SETFIT_FIXTURES` (model_tests.rs:403). It is `pub`, so cross-crate reuse needs no new door — confirm and record.

**Review fix 1 — no gradient clearing.** The first draft's loop was
`backward -> grad norms -> clip -> step_refs -> scheduler step`, with no `zero_grad`. Given
`accumulate_grad`, step N's gradient would be the sum of steps 0..N. This is a correctness defect,
not a style one.

**Review fix 2 — scheduler applied after the optimizer.** As drafted, AdamW was constructed at
`config.encoder_lr` and the scheduler stepped AFTER `step_refs`, so step 0 ran at the peak LR and
warmup never applied. The corrected order applies the scheduled LR BEFORE the step and advances
AFTER it. Note the reference consequence, pinned in 03-03: with warmup_steps > 0 the scheduled LR at
step 0 is exactly 0.0, matching HF's LambdaLR. That is fidelity, not an off-by-one — do not "fix" it.

**Review fix 3 — both pair-side forwards shared one step counter.** They now carry distinct forward
ordinals `2*step + 0` and `2*step + 1` (D-15's `block`), so branch A and branch B draw independent
dropout masks.

**Review fix 4 — pair-order and batch-boundary digests were RECOMPUTED from configuration.** Two
runs that consume the same WRONG order would then reproduce perfectly and report the expected
digest. The loop now hashes what it actually consumed, in band.

**Review fix 5 — `||dTheta|| / ||theta_init||` is undefined at zero init**, and a whole-table
denominator makes the sparse embedding class's ratio a function of vocabulary size, so a
fixture-calibrated epsilon cannot transfer. Both are fixed by the support-restricted,
floor-protected denominator below.

**Review fix 6 — initial norms alone cannot produce `||theta_final - theta_init||`.** Full initial
tensor snapshots are required; their memory cost is now planned and recorded.

**Review fix 7 — AdamW moment state is positional**, so the parameter registry order must be proven
stable across steps.

**Review fix 8 — device and max_length were validated but never consumed.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic trainer test fixture (shared by all Phase 3 trainer tests)</name>
  <files>crates/aprender-train/src/train/setfit/test_fixtures.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/mod.rs (from_slice_fixture at :236 — the public, network-free constructor path; confirm it is reachable cross-crate under the setfit feature)
    - crates/aprender-core/src/setfit/model_tests.rs (fixtures_dir at :403 — the APRENDER_SETFIT_FIXTURES override and default path)
    - crates/aprender-core/tests/fixtures/setfit/slice_config.json (the fixture's real dimensions — record them; the calibration argument in Task 3 depends on knowing them)
    - crates/aprender-contrastive-data/tests/common/ (how Phase 2 tests build PreparedDataset/Selection from synthetic rows)
    - crates/aprender-contrastive-data/src/select.rs and pairs.rs (the public path: selection + PairConfig + replay)
  </read_first>
  <action>
    Create a `#[cfg(test)]`-gated (or `#[doc(hidden)]` test-support where cross-module reuse
    requires it) fixture module providing:
    (a) the deterministic slice encoder via `SetFitMiniLm::from_slice_fixture(fixtures_dir(), seed)`
        — the fixture directory resolved with the same `APRENDER_SETFIT_FIXTURES`-then-default rule
        aprender-core uses. Record in the SUMMARY the resolved path and the slice dimensions read
        from slice_config.json, and state explicitly that the encoder's WEIGHT VALUES are real
        pretrained MiniLM values (only the dimensions are reduced). The calibration argument in
        Task 3 turns on that distinction and the review specifically challenged it;
    (b) a synthetic K=3 dataset (about 8 examples per class, SYNTHETIC texts like "class-a sample 0"
        — no TweetEval content) run through the real contrastive-data path to yield a
        PreparedDataset<Canonical>, a Selection (smallest shot count the fixture supports), and a
        PairConfig with a small budget;
    (c) a `SetFitTrainConfig::reference_defaults`-derived config shrunk for test speed (1 epoch,
        batch 4), plus a `calibration_variants()` helper returning the matrix Task 3 needs:
        seeds {1, 7, 42} x boundary configurations {(shots=min, epochs=1, batch=4),
        (shots=next, epochs=2, batch=8)} — the second configuration is the one that stresses the
        endpoint statistic and the sparse-embedding support fraction.
    The fixture must be pure: two calls yield structurally identical objects (assert
    selection.semantic_hash equality in a fixture self-test).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit fixture_ > /tmp/p3-05t1.log 2>&1; rc=$?; tail -3 /tmp/p3-05t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit fixture_` exits 0
    - The fixture self-test asserts two builds produce equal selection semantic hashes
    - `grep -c -iE 'abortion|tweet|stance' crates/aprender-train/src/train/setfit/test_fixtures.rs` returns 0 (synthetic text only)
    - `calibration_variants()` exists and yields >= 6 (seed, boundary-configuration) combinations (source assertion)
    - The SUMMARY records the resolved fixture path and the slice dimensions, and states that the weights are real pretrained values
  </acceptance_criteria>
  <done>Every later trainer test has one deterministic, network-free, synthetic-text fixture door, and the calibration matrix has a source.</done>
</task>

<task type="auto">
  <name>Task 2: run_tuning — the stage-one loop with a correct step order and in-band execution digests</name>
  <files>crates/aprender-core/src/autograd/mod.rs, crates/aprender-train/src/train/setfit/tune.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/tensor.rs (accumulate_grad :245, zero_grad_ :236, detach :280 — the accumulation semantics that force explicit clearing)
    - crates/aprender-core/src/autograd/mod.rs (GRAPH thread_local :63, no_grad :76, clear_graph :117 — and where the new graph_tape_len accessor lands)
    - crates/aprender-train/src/optim/adamw.rs (step_refs :184, set_lr :261, constructor)
    - crates/aprender-train/src/optim/clip.rs (:65 — returns PRE-clip global norm)
    - crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs (as landed by 03-03: get_lr, step, apply, warmup_steps_from_ratio)
    - crates/aprender-core/src/setfit/loss.rs (pair_cosine_mse :71)
    - crates/aprender-core/src/setfit/mod.rs (trainable_parameters_mut :440, frozen_parameters :451, apply_freeze, set_training :293, set_forward_ordinal from 03-02, encode_texts :287)
    - crates/aprender-contrastive-data/src/pairs.rs (PairSampler::pair_at :1020, iter_from :1035 — the O(1) ordinal access)
    - crates/aprender-train/src/train/setfit/epoch.rs and reduce.rs (as landed by 03-03)
    - crates/aprender-train/src/train/trainer/core.rs and crates/aprender-train/src/train/train_loop/basic.rs (THE EXISTING GENERIC TRAINER. 03-CONTEXT.md's canonical refs direct reading these "before deciding whether the SetFit trainer composes with it or sits beside it", and 03-PATTERNS.md had no analog assigned to tune.rs — the phase's largest new function. RESEARCH's Alternatives table already recommends SIT BESIDE IT and gives the reason: `Trainer` owns `Vec<Tensor>` params plus a `Box<dyn Optimizer>`, while SetFit needs BORROWED NAMED params from `SetFitMiniLm` and evidence capture between the optimizer step and the gradient clear. Read both files to confirm that shape still holds, then implement beside them reusing `AdamW`/`clip`/`scheduler` directly, and record the confirmation — or the contradiction — in the SUMMARY.)
  </read_first>
  <action>
    First add ONE accessor to aprender-core: `pub fn graph_tape_len() -> usize` in autograd/mod.rs,
    returning `with_graph(|g| g.len())`. `ComputationGraph::len` is already pub but the thread-local
    is not reachable from outside the crate, so there is currently no way to observe tape growth.
    This is the mechanism-engaged evidence for "the tape is cleared per step" here and for "no graph
    was built" in 03-07. Document it as a determinism/no-grad observation accessor.

    Implement `pub(crate) fn run_tuning(encoder, dataset, selection, config: &ResolvedSetFitConfig)
    -> Result<TuneOutput, SetFitTrainError>` in tune.rs.

    PRE-LOOP, in this order:
      1. Assert `config.device` resolved to CPU; a non-CPU device is
         `SetFitTrainError::UnsupportedDeviceForPhase3 { resolved }` (review fix 8 — the knob is now
         consumed, not merely validated).
      2. Assert `config.max_length == aprender::setfit::MAX_SEQUENCE_LENGTH`; a mismatch is a typed
         error naming both values (review fix 8, second half). Record max_length in TuneOutput so
         03-08's bundle carries it.
      3. `apply_freeze(config.freeze_policy)` — BEFORE anything else touches parameters, so the
         zero-match typed error fires before any snapshot or baseline encode (review fix: the LOW
         note that zero-match was deferred "until the first update").
      4. Compute the PARAMETER REGISTRY HASH: SHA-256 over the ordered dotted names from
         `trainable_parameters_mut()`. Store it. (Review fix 7.)
      5. SNAPSHOT the full initial trainable tensors — a `BTreeMap<String, Vec<f32>>` clone, not
         just norms. `||theta_final - theta_init||` is not computable from norms alone (review fix
         6). Record the snapshot's byte size in TuneOutput and in the SUMMARY: for the fixture it is
         small; for the full MiniLM it is ~22.7M params x 4 B ~ 91 MB, and peak memory during
         tuning is that snapshot plus the live parameters plus AdamW's two moment buffers. State the
         accounting; do not discover it in Phase 5.
      6. BASELINE ENCODE: `set_training(false)`, then encode the selected rows inside
         `autograd::no_grad(|| ...)` in pinned consecutive Selection-order windows of
         config.batch_size. Assert `graph_tape_len()` did not increase across the encode.

    PER EPOCH e in 0..config.epochs: derive the pair order via `epoch_pair_order(root_seed, e,
    n_pairs)`; consume it through `PairSampler::pair_at(ordinal)` (O(1) — do not materialize the
    stream); batch in consecutive windows of config.batch_size (last short, never resampled).

    COMPLEXITY BUDGET (W-07). `run_tuning` as specified is 6 pre-loop steps plus a 13-step
    per-batch body, which will breach the project's cyclomatic-complexity ceiling of 10 per function
    (CLAUDE.md / `.pmat-gates.toml`). Do NOT discover this at 03-10's gate. Decompose as you write,
    into named private functions with the step boundaries as the seams:
    `preflight(config, encoder) -> Result<Preflight, _>` (the 6 pre-loop steps),
    `snapshot_initial(params) -> BTreeMap<String, Vec<f32>>`,
    `baseline_encode(encoder, dataset, selection, batch_size) -> Result<Vec<Vec<f32>>, _>`,
    `run_batch(ctx, batch) -> Result<BatchRecord, _>` (steps a-m), and
    `absorb_batch_digests(hashers, batch, records)`. `run_tuning` itself becomes the epoch/batch
    iteration plus assembly of `TuneOutput`. The pinned ORDER is a property of `run_batch`'s body
    and the order-pin test targets it directly; decomposition must not change the order, and the
    test is what proves it did not.

    PER BATCH, EXACTLY THIS ORDER (review fixes 1, 2, 3 — pin it with the source-assertion test
    below; a reordering here is a silent correctness change):
      a. `scheduler.apply(&mut adamw)`   // set the SCHEDULED LR for the step about to be taken
      b. `zero_grad_()` on every trainable parameter, then `clear_graph()`
      c. `set_forward_ordinal(2*global_step + 0)`; `set_training(true)`; forward the A sentences
      d. `set_forward_ordinal(2*global_step + 1)`; forward the B sentences
      e. `pair_cosine_mse(za, zb, &targets)`
      f. `loss.backward()`
      g. per-name gradient norms via `reduce::l2_norm_in_index_order`, PRE-clip
      h. assert the parameter registry hash still equals the pre-loop value (review fix 7 — AdamW
         moment state is positional; a reordered registry silently pairs moments with the wrong
         parameters)
      i. `clip_grad_norm_refs(params, config.grad_clip_max_norm)`, recording the returned pre-clip
         global norm
      j. `adamw.step_refs(...)`
      k. `scheduler.step()`   // ADVANCE only after the step
      l. `clear_graph()`
      m. push the scalar loss (f32) onto the trace
    AdamW is constructed ONCE with (config.encoder_lr, 0.9, 0.999, 1e-8, 0.0); the scheduler owns
    the effective LR from step 0. total_steps = epochs * ceil(n_pairs / batch_size);
    warmup_steps = `warmup_steps_from_ratio(total_steps, config.warmup_ratio)` — call 03-03's single
    implementation, never re-derive it.

    IN-BAND EXECUTION DIGESTS (review fix 4 — the phase's most consequential structural fix).
    Maintain two streaming SHA-256 hashers fed AT THE POINT OF CONSUMPTION, never reconstructed:
      - `consumed_pair_digest`: for each pair actually pulled, absorb
        `epoch:u32 || batch_index_in_epoch:u32 || position_in_batch:u32 || pair_ordinal:u64 ||
         a_selected_index:u32 || b_selected_index:u32 || target.to_bits():u32`, all little-endian.
      - `batch_boundary_digest`: at each batch, absorb
        `epoch:u32 || batch_index_in_epoch:u32 || global_step:u64 || batch_start_ordinal:u64 ||
         batch_len:u32`.
    Also retain the plain boundary LIST (`Vec<(u32, u64, u32)>` of (epoch, start_ordinal, len)) so a
    mismatch can be diffed into something readable. It is NOT merely a debugging aid: 03-08's public
    `batch_boundaries()` accessor returns it, so it must survive into `UpdateEvidence` and onward
    into `PassedEvidence` (W-06 — as first drafted it lived only in `TuneOutput`, which meant the
    accessor had nothing recorded to return and an executor would have recomputed it, reintroducing
    exactly the false-green that the in-band digests remove). These recorded values — not any later
    recomputation — are what TuneOutput carries, what the evidence table hashes, what 03-08's
    accessors return, and what 03-10 compares across processes.

    TuneOutput carries: the tuned encoder; the initial-tensor snapshot map; per-name grad-norm
    aggregates (max and index-order mean across steps) and final delta norms AND the delta SUPPORT
    (the set/count of elements with a non-zero delta, needed for the support-restricted denominator
    in Task 3); the pre-clip norm series; the loss trace Vec<f32> and its SHA-256 (LE f32 bits, step
    order); step count; parameter registry hash; `consumed_pair_digest`; `batch_boundary_digest`;
    the boundary list; max_length; first-k/last-k loss means (k = min(5, steps/2), computed but NOT
    judged here); and the before/after eval-mode embeddings of the selected rows (pinned consecutive
    Selection-order batches, `no_grad`, `set_training(false)` around both encodes).
    No pass/fail judgment anywhere in this plan.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit tune_ > /tmp/p3-05t2.log 2>&1; rc=$?; tail -3 /tmp/p3-05t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit tune_` exits 0
    - STEP-ORDER PIN: a test named `tune_step_order_is_pinned` asserts, on a 3-step fixture run, that (i) after `zero_grad_` every trainable parameter's `grad()` is None at the top of each step, (ii) the observed LR at global_step 0 equals the scheduler's `get_lr()` at current_step 0 (exactly 0.0 with warmup_steps > 0) and NOT `config.encoder_lr`, and (iii) `graph_tape_len()` at the top of each step is 0
    - GRADIENT-ACCUMULATION NEGATIVE: a test runs two batches with the `zero_grad_` call skipped (via a test-only flag on the internal loop fn) and asserts the second batch's gradient norm DIFFERS from the correct run — proving the clearing is load-bearing and would be caught if removed
    - A test runs run_tuning twice on the fixture and asserts bitwise-equal loss-trace hashes, equal `consumed_pair_digest`, equal `batch_boundary_digest`, and equal step counts
    - DIGEST-IS-RECORDED-NOT-RECOMPUTED: a test perturbs the CONSUMPTION order (a test-only reversed-batch variant of the internal loop fn) with configuration held identical, and asserts `consumed_pair_digest` CHANGES. A digest that survives a real reordering is a recomputed digest and the test says so
    - A test asserts step count == epochs * ceil(n_pairs / batch_size)
    - A test asserts the two branches at one step used different forward ordinals (source or observed: `set_forward_ordinal` called with `2*s` and `2*s+1`)
    - tune.rs contains `clip_grad_norm_refs`, `step_refs`, `zero_grad_`, `clear_graph` and `warmup_steps_from_ratio` (reuse, not reimplementation); `grep -c 'par_iter' crates/aprender-train/src/train/setfit/` returns 0
    - A non-CPU resolved device and a max_length != 256 each produce their typed error (two tests)
    - A test asserts the loss trace is finite at every step
    - autograd/mod.rs exports `pub fn graph_tape_len` (grep >= 1)
  </acceptance_criteria>
  <done>Stage one runs end-to-end deterministically with a correct, pinned optimizer step order, and records the execution it actually performed rather than the one its configuration implies.</done>
</task>

<task type="auto">
  <name>Task 3: Evidence table + support-restricted relative delta + the calibration matrix</name>
  <files>crates/aprender-train/src/train/setfit/evidence.rs, crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/tests/setfit_calibration.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/ledger.rs (to_canonical_bytes/from_bytes/ledger_hash — the canonical-record analog)
    - crates/aprender-contrastive-data/src/attestation.rs (deny_unknown_fields precedent :73,:87)
    - crates/aprender-train/src/train/setfit/tune.rs (TuneOutput as landed in Task 2, including the delta support counts)
    - crates/aprender-core/src/setfit/encoder.rs (the HF dotted parameter names — the prefix vocabulary the class mapping keys off: `embeddings.*_embeddings.weight`, `*.LayerNorm.*`, `*.dense.*`/`*.attention.*`)
  </read_first>
  <action>
    RELATIVE DELTA — the amended D-10 formula (review fix 5). Define exactly one function:

      relative_delta(param) = ||dTheta||_2 / max(denom(param), s_class(param))

    where `dTheta = theta_final - theta_init` (needs the Task 2 snapshot), and

      denom(param) = for the SPARSE class (embedding tables): the L2 norm of theta_init restricted
                     to the SUPPORT of dTheta (the rows with any non-zero delta);
                     for every other class: the L2 norm of the whole theta_init.

    Two properties follow, and both are load-bearing:
      - It is FINITE at zero initialization, because the denominator is floored at the contracted
        positive `s_class`. `NaN > eps` is false, so the un-floored form would have rejected every
        run containing a zero-init bias.
      - For the sparse class it is INVARIANT to vocabulary size, because a few-shot batch touches a
        handful of rows regardless of how many rows the table has. This is what makes a
        fixture-calibrated epsilon transferable to the production encoder, and it is the direct
        answer to the review's strongest objection. State the argument in the module doc and record
        the observed support fraction per run in the evidence table so the claim stays checkable.
    A separate strict predicate `||dTheta||_2 > 0` is recorded alongside (a parameter that did not
    move at all is never SetFit, whatever the ratio says).

    Parameter class is a pure function of the HF dotted name prefix, in ONE `classify_parameter`
    function with a case table test covering every name the fixture encoder emits (a name matching
    no class is a typed `UnclassifiedParameter { name }` error — fail closed, never a silent
    default bucket).

    evidence.rs: `UpdateEvidence` — per-parameter rows {name, class, init_norm, delta_norm,
    delta_support_count, delta_support_fraction, denom_used, scale_floor_used, relative_delta,
    grad_norm_max, grad_norm_mean, steps_observed} in BTreeMap name order; plus run-level fields
    {loss_trace_hash, consumed_pair_digest, batch_boundary_digest, batch_boundary_list (the
    Vec<(epoch, start_ordinal, len)> from TuneOutput — W-06: this is the recorded source for
    03-08's `batch_boundaries()` accessor; without it that accessor has nothing to return),
    parameter_registry_hash, step_count, first_k_mean, last_k_mean, k,
    embedding_delta_min/median/max, pre_clip_norm_max, calibration_regime_id}. Wire struct with schema_version, `#[serde(deny_unknown_fields)]`, fixed
    field order, serde_json canonical bytes; `table_hash` = SHA-256 of those bytes.

    `EvidenceSummary` (D-12): verdict enum (Unjudged in this plan; Pass/Fail arms in 03-06),
    trainable_count, frozen_count, per-class min/median/worst relative_delta, worst_param_name,
    epsilon_used: Option (None while unjudged), calibration_regime_id, contract_version, table_hash.
    Doc wording per amended D-12: the hash BINDS the summary to the table (tamper-evidence and
    linkage); it does not make it non-forgeable. Do not write "non-forgeable" anywhere.
    All numeric aggregation through reduce.rs.

    Tests: canonical bytes bitwise-stable across two identical runs; deny_unknown_fields rejects an
    extended payload; a mutated table no longer matches its hash; no field of any serialized struct
    is a Duration/timestamp; the `classify_parameter` case table; and — the zero-norm proof — a test
    that deliberately zeroes one parameter's initial tensor and asserts the resulting relative_delta
    is FINITE and non-NaN, plus a companion asserting a zeroed-AND-unchanged parameter fails the
    strict `||dTheta|| > 0` predicate.

    CALIBRATION MATRIX (review fix: one trace on one configuration is not a calibration).
    Measurement, not judgment.

    RUNTIME AND PLACEMENT (W-11). The matrix is >= 6 cells each with a 1e-30-LR control, so >= 12
    complete `run_tuning` passes over the real-weight MiniLM slice. That does not belong on the
    default `evidence_` filter a developer types dozens of times a day, and VALIDATION.md's
    "seconds-scale per scoped filter" claim would become false if it did. Put the matrix in its OWN
    integration test target, `crates/aprender-train/tests/setfit_calibration.rs`, gated behind
    `--features setfit`, invoked as
    `cargo test -p aprender-train --test setfit_calibration --features setfit`. The `evidence_`
    unit filter keeps the fast structural tests (canonical bytes, hash binding, zero-init,
    classify_parameter, support-restricted denominator) and stays seconds-scale. MEASURE the
    calibration target's wall time and record it in the SUMMARY; the wave-2 sampling row in
    VALIDATION.md names this target explicitly.

    Over `calibration_variants()` from Task 1 — >= 3 seeds x >= 2 shot/epoch boundary
    configurations — plus a 1e-30-LR control at every cell:
      (a) record, PER PARAMETER CLASS, the min/median/max relative_delta and the support fraction;
      (b) record the first-k/last-k endpoint means and their spread ACROSS seeds (the review's
          concern that a margin calibrated on one trace rejects correct noisy runs on other seeds);
      (c) assert SEPARATION per class and per cell: the max relative_delta of the 1e-30 run is below
          the min relative_delta of the real run;
      (d) print all distributions in a `--nocapture`-friendly table.
    Copy the measured numbers into the SUMMARY under an "epsilon basis" heading, organised BY CLASS
    and BY CELL, together with a proposed `calibration_regime_id` string encoding
    (architecture fingerprint, seed set, shot/epoch set). Plan 03-06 freezes per-class epsilon from
    this. The rule it will apply: at least 10x above the worst 1e-30 value across ALL cells, at
    least 10x below the best real value across ALL cells, per class. If any class cannot support
    that margin across the whole matrix, FLAG IT in the SUMMARY with the numbers — widening the
    fixture or the matrix is in scope; silently narrowing the margin is not.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit evidence_ > /tmp/p3-05t3.log 2>&1; rc=$?; tail -5 /tmp/p3-05t3.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --test setfit_calibration --features setfit > /tmp/p3-05t3b.log 2>&1; rc=$?; tail -5 /tmp/p3-05t3b.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo clippy -p aprender-train --lib --features setfit -- -D warnings > /tmp/p3-05t3c.log 2>&1; rc=$?; tail -3 /tmp/p3-05t3c.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit evidence_` exits 0
    - The zero-init test exists and asserts `relative_delta.is_finite()` for a parameter whose initial tensor is exactly zero; the companion asserts the strict `||dTheta|| > 0` predicate fails for zeroed-and-unchanged
    - `classify_parameter` has a case-table test covering every parameter name the fixture emits, and an unmatched name is a typed error (no default bucket)
    - The evidence row carries `delta_support_count` and `delta_support_fraction` (source assertion), and the sparse-class denominator is support-restricted (a test constructs a delta supported on 2 of N rows and asserts the denominator uses only those rows)
    - evidence.rs uses BTreeMap for the per-parameter table and contains `deny_unknown_fields`
    - `grep -c -E 'Duration|Instant|SystemTime|chrono' crates/aprender-train/src/train/setfit/evidence.rs` returns 0; `grep -c -i 'non-forgeable' crates/aprender-train/src/train/setfit/evidence.rs` returns 0
    - The separation assertion passes for EVERY cell of the calibration matrix and every class, and the SUMMARY contains the per-class per-cell distributions as concrete numbers plus the endpoint-margin spread across seeds
    - The matrix lives in `crates/aprender-train/tests/setfit_calibration.rs`, NOT on the `evidence_` unit filter, and the SUMMARY records the MEASURED wall time of both `cargo test --lib --features setfit evidence_` and `cargo test --test setfit_calibration --features setfit` (W-11: if the `evidence_` filter exceeds 30 s the split did not work)
    - `UpdateEvidence` carries `batch_boundary_list` (source assertion — W-06: it is 03-08's recorded source for `batch_boundaries()`)
    - `cargo clippy -p aprender-train --lib --features setfit -- -D warnings` exits 0
    - A hash-binding test proves a single-field mutation changes table_hash
    - The evidence record carries `consumed_pair_digest` and `batch_boundary_digest` sourced from TuneOutput (source assertion: they are moved, not recomputed, in evidence.rs)
  </acceptance_criteria>
  <done>Evidence is canonical, hash-bound, Duration-free, well-defined at zero init and dimension-invariant for the sparse class, and 03-06's epsilon freeze has a measured multi-seed multi-configuration basis.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence table → summary consumers | Phase 4 APR field and Phase 5 EVAL-03 row trust the summary |
| loss trace and execution digests → reproducibility claims | TRN-06 verdicts hang off these hashes |
| configuration → recorded execution | a digest derived from intent rather than from what ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-16 | Tampering | edited evidence summary diverging from its table | mitigate | summary carries SHA-256 of the full canonical table; mutation test proves binding (D-12 as amended: binding, not non-forgeability) |
| T-3-17 | Repudiation | nondeterministic trace (thread pools, map order, timestamps) | mitigate | reduce.rs-only aggregation, BTreeMap order, Duration-free structs, two-run bitwise test |
| T-3-18 | Information Disclosure | dataset text leaking into committed fixtures | mitigate | synthetic-text-only fixture with grep gate (no TweetEval content committed) |
| T-3-19 | Tampering (V5) | freeze policy matching zero parameters silently | mitigate | apply_freeze's zero-match typed error engaged BEFORE the snapshot and BEFORE the baseline encode |
| T-3-38 | Repudiation | pair-order/batch digests recomputed from configuration, so a wrong order reproduces perfectly | mitigate | streaming digests absorbed at the point of consumption; a reversed-consumption test asserts the digest CHANGES |
| T-3-39 | Tampering | gradients accumulating across batches; warmup skipped by stepping the scheduler after the optimizer | mitigate | pinned step order with a source-asserted test; an explicit skip-zero_grad negative proving the clearing is load-bearing; LR at step 0 asserted equal to the scheduled value |
| T-3-42 | Repudiation | epsilon calibrated on one cell and applied to a different regime | mitigate | >= 3 seeds x >= 2 boundary configurations, per-class distributions, support-restricted denominator making the sparse class dimension-invariant, calibration_regime_id recorded in the evidence |
| T-3-54 | Tampering | reordered parameter registry pairing AdamW moments with the wrong tensors | mitigate | registry hash computed pre-loop and asserted before every optimizer step |
| T-3-55 | DoS | unbounded tape growth across an epoch | mitigate | clear_graph per step; graph_tape_len asserted 0 at the top of each step |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit tune_` / `evidence_` / `fixture_` all exit 0
- `cargo test -p aprender-core --lib autograd` exits 0 (the graph_tape_len addition)
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0 (closure holds after the new modules)
</verification>

<success_criteria>
- Stage one runs deterministically twice in-process with identical loss, pair and boundary digests
- The step order is correct and pinned, with the gradient-clearing and warmup properties proven by negatives
- Evidence table canonical + hash-bound, well-defined at zero init, dimension-invariant for the sparse class
- Measured calibration matrix recorded per class and per cell, with the endpoint-margin spread across seeds
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-05-SUMMARY.md` when done.
Record: the epsilon basis organised by class and by cell (real vs 1e-30, all seeds and boundary
configurations), the proposed calibration_regime_id, the endpoint k and its cross-seed spread, the
snapshot memory accounting for the fixture and the projected full-MiniLM figure, the observed
support fractions, and the RED observations from the skip-zero_grad and reversed-consumption
negatives.
</output>
