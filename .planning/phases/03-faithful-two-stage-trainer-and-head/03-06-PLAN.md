---
phase: 03-faithful-two-stage-trainer-and-head
plan: 06
type: execute
wave: 3
depends_on: ["03-04", "03-05"]
files_modified:
  - contracts/setfit-train-lifecycle-v1.yaml
  - contracts/aprender/binding.yaml
  - Makefile
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/tune.rs
  - crates/aprender-train/src/train/setfit/evidence.rs
  - crates/aprender-train/src/train/setfit/baseline.rs
autonomous: true
requirements: [TRN-03, SAFE-03]

must_haves:
  truths:
    - "SetFitRun<EncoderTuned> cannot exist without passing evidence: tune_encoder() runs the gate INSIDE the transition and fails typed when relative deltas, gradient finiteness, pair-loss endpoints, or embedding deltas miss the contracted thresholds"
    - "An all-frozen run fails with a typed no-trainable-parameters error (SAFE-03 automatic under D-09), and a 1e-30-LR run fails naming the worst-offending parameter — both red in every cargo test alongside a passing control"
    - "Epsilon, k, and the endpoint margin are frozen in setfit-train-lifecycle-v1.yaml BEFORE the gate ever judges a run, derived from 03-05's measured basis (Ph1 D-14 discipline)"
    - "FrozenProbeRun is a differently-named type with no conversion to SetFitRun<*>; its emitted kind string is 'frozen_linear_probe' and it binds linear-probe-classifier-v1.yaml"
  artifacts:
    - path: "contracts/setfit-train-lifecycle-v1.yaml"
      provides: "Lifecycle contract: evidence gate equation with frozen epsilon, endpoint k/margin, dropout + epoch-shuffle RNG derivations, loss-trace hash definition, 12-knob default provenance"
    - path: "crates/aprender-train/src/train/setfit/baseline.rs"
      provides: "FrozenProbeRun — the SAFE-03 baseline that never claims SetFit"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "contracts/setfit-train-lifecycle-v1.yaml"
      via: "#[contract] annotation on the evidence-validation gate function"
      pattern: "setfit-train-lifecycle-v1"
    - from: "crates/aprender-train/src/train/setfit/baseline.rs"
      to: "contracts/linear-probe-classifier-v1.yaml"
      via: "binding.yaml entry with real module path"
      pattern: "linear-probe-classifier-v1"
  caveats:
    - "Epsilon must be committed to the contract in the SAME commit or an EARLIER one than the commit that arms the gate — never after a pass/fail comparison has run. Task order inside this plan enforces it; do not reorder."
    - "If 03-05's measured basis cannot support the 10x/10x margin rule on the fixture, STOP and record the numbers — widening the fixture is in scope, silently narrowing the margin is not."
---

<objective>
Arm the SetFit-identity gate: freeze the contracted thresholds from 03-05's measured basis, author
the lifecycle contract (the phase's frozen-numbers home), turn run_tuning into the public
tune_encoder() transition that mints SetFitRun<EncoderTuned> only when evidence passes (D-10,
D-11), prove the gate bites with in-band negatives (frozen and 1e-30-LR runs red in every cargo
test), and ship the FrozenProbeRun baseline type that makes SAFE-03 structural.

Purpose: TRN-03 — a run cannot identify as SetFit until named gradients, parameter deltas,
embedding deltas, and pair-loss behavior pass; SAFE-03 — non-updating baselines stay labeled.
Output: armed transition, contract + bindings + Makefile growth, negatives, baseline type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key facts:
- Gate criteria (D-09/D-10 + RESEARCH Open Question 5): per trainable parameter (trainable set = all parameters minus FreezeGroup exemptions): finite non-zero gradient norms AND relative delta ||dTheta||/||Theta_init|| > epsilon. Run-level: every loss step finite; last-k mean < first-k mean by the contracted relative margin; embedding-delta aggregate above the contracted floor. Empty trainable set => unpassable (typed NoTrainableParameters — SAFE-03 automatic).
- Epsilon freeze rule (03-05 SUMMARY carries the basis): at least 10x above the 1e-30 run's max relative delta, at least 10x below the real run's min. k and margin from the measured trace endpoints.
- In-band negative discipline (Ph1 D-24 / Ph2 D-25, negative_leaky.rs template): the negative (rejected, message NAMES the offender), the control (same setup minus the poison passes), the mirror (untouched path unchanged). Red in every cargo test.
- Contract discipline: contrastive-pair-protocol-v1.yaml header style (names requirements, implementing crate, why-the-contract-exists — which claims are fakeable and need negatives); derivations frozen INLINE byte-for-byte; kani declarations must state not-executed + name the bounded proptest; every test command carries --lib.
- 12-knob defaults provenance (RESEARCH knob table): declare in-contract which reference each default traces to (setfit 1.1.3 -> SentenceTransformerTrainer -> HF TrainingArguments). Assumption A1 (HF defaults not overridden on the setfit path): attempt confirmation via Phase 2's pinned setfit reference env (crates/aprender-contrastive-data/tests/setfit_reference/ — it pins setfit 1.1.3); if the env cannot be reconstructed offline, record A1 explicitly as an assumption in the contract notes with the cheap-confirmation recipe.
- Makefile: PHASE3_CONTRACTS exists since 03-04 — append the new contract there AND to $(CONTRACTS).
- Binding traps: bare filename, status enum; cross-crate module paths under target_crate aprender are ACCEPTED (Phase 2 measured).
- linear-probe-classifier-v1.yaml exists with frozen-encoder invariants already written (RESEARCH verified) — read it before writing baseline.rs so the type satisfies what it binds.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author setfit-train-lifecycle-v1.yaml with the frozen numbers</name>
  <files>contracts/setfit-train-lifecycle-v1.yaml, contracts/aprender/binding.yaml, Makefile</files>
  <read_first>
    - .planning/phases/03-faithful-two-stage-trainer-and-head/03-05-SUMMARY.md (the epsilon basis — the measured distributions this task freezes)
    - contracts/contrastive-pair-protocol-v1.yaml (header + inline-statement discipline to copy)
    - crates/aprender-core/src/setfit/dropout_rng.rs and crates/aprender-train/src/train/setfit/epoch.rs (the derivations to freeze inline, including tags and golden constants)
    - Makefile PHASE3_CONTRACTS block (as created by 03-04)
    - crates/aprender-contrastive-data/tests/reference_fixtures.rs (:140 region — the setfit 1.1.3 pin, A1 confirmation route)
  </read_first>
  <action>
    Author the contract with equations for: (1) evidence_gate — per-parameter relative-delta
    inequality with the FROZEN numeric epsilon (write the derivation from 03-05's measured basis
    inline: both distributions, the 10x/10x rule, the chosen number); (2) pair_loss_endpoint —
    last-k vs first-k relative-margin inequality with frozen k and margin and the measured trace
    that motivated them; (3) embedding_delta_floor with its frozen number; (4)
    dropout_mask_derivation — tag apr-setfit-dropout-v1, SHA-256 key lanes, stream/ordinal
    mapping, threshold convention, golden constants from 03-02 restated inline; (5)
    epoch_shuffle_derivation — tag apr-setfit-train-v1, Fisher-Yates bounded-draw form, golden
    from 03-03; (6) loss_trace_hash — SHA-256 over LE f32 bits in step order, exclusion of all
    wall-clock fields; (7) knob_defaults — the 12 defaults each with its reference citation
    (attempt A1 confirmation via the pinned setfit reference env; if not reconstructible
    offline, record A1 as an explicit assumption plus the confirmation recipe). Falsification
    tests name the Task 2 negatives with --lib commands. Add the contract to $(CONTRACTS) and
    PHASE3_CONTRACTS. pv validate + pv lint via PV_BIN. Add binding.yaml entries for the gate
    function (Task 2's validate fn, status: not_implemented until Task 2 lands — then flip to
    implemented in Task 2's commit; or land Task 1+2 bindings together if committing as one
    unit, state which in the SUMMARY).
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/setfit-train-lifecycle-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/setfit-train-lifecycle-v1.yaml` exits 0
    - The contract contains a numeric epsilon literal, numeric k, and numeric margin (no TBD/placeholder tokens; grep -i 'tbd\|placeholder' = 0 matches)
    - The contract contains `apr-setfit-dropout-v1` and `apr-setfit-train-v1` derivations inline
    - Makefile $(CONTRACTS) and PHASE3_CONTRACTS both list setfit-train-lifecycle-v1.yaml
    - Every falsification command in the contract contains `--lib`
    - A1 is either confirmed with evidence or recorded as an explicit in-contract assumption with the confirmation recipe
  </acceptance_criteria>
  <done>The phase's frozen numbers exist in a pv-valid contract before any pass/fail comparison runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arm tune_encoder() — gate inside the transition + in-band negatives</name>
  <files>crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/src/train/setfit/tune.rs, crates/aprender-train/src/train/setfit/evidence.rs</files>
  <read_first>
    - contracts/setfit-train-lifecycle-v1.yaml (the frozen numbers just committed — the gate implements THESE, no others)
    - crates/aprender-contrastive-data/tests/negative_leaky.rs (module doc lines 1-35: negative + control + mirror discipline)
    - crates/aprender-train/src/train/setfit/tune.rs and evidence.rs (as landed by 03-05)
  </read_first>
  <behavior>
    - negative 1: fixture run with FreezeGroup covering everything -> tune_encoder returns Err naming NoTrainableParameters (SAFE-03 automatic); message includes trainable_count 0
    - negative 2: fixture run at encoder_lr = 1e-30 -> Err(EvidenceBelowEpsilon-class) naming the worst-offending parameter and its measured relative delta vs the contracted epsilon
    - control: reference-defaults fixture run -> Ok(SetFitRun<EncoderTuned>) whose EvidenceSummary verdict is Pass, epsilon_used equals the contract literal, table_hash binds
    - mirror: the passing run's loss-trace hash equals a second identical run's (unchanged by the gate's presence)
  </behavior>
  <action>
    Write the four tests first; watch both negatives fail-to-fail before the gate exists (i.e.
    they must be RED until the gate is armed — record the RED observation). Then implement:
    `impl SetFitRun<Prepared> { pub fn tune_encoder(self) -> Result<SetFitRun<EncoderTuned>,
    SetFitTrainError> }` — calls run_tuning, builds UpdateEvidence, then a single
    `validate_evidence(&UpdateEvidence, &Thresholds) -> Result<PassedEvidence, SetFitTrainError>`
    gate function (ONE validating function — both any future doors call it; annotate it
    #[provable_contracts_macros::contract("setfit-train-lifecycle-v1", equation =
    "evidence_gate")]). Thresholds are constants mirroring the contract literals with a test
    asserting the Rust constants equal the YAML literals (read the contract file in the test or
    duplicate-and-assert; state the mechanism in code comments). Implement LifecycleState for
    EncoderTuned with Evidence = PassedEvidence (the evidence field EXISTS only in this state —
    Ph2 02-03 absent-field precedent; no Option). EvidenceSummary verdict arms
    (Pass/Fail{reason}). Error variants carry named fields: worst parameter name, observed
    relative delta, contracted epsilon.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit evidence_gate > /tmp/p3-06t2a.log 2>&1; rc=$?; tail -3 /tmp/p3-06t2a.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit negative_ > /tmp/p3-06t2b.log 2>&1; rc=$?; tail -3 /tmp/p3-06t2b.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - Both scoped test commands exit 0 and cover the negative/control/mirror quartet
    - tune.rs or mod.rs contains `#[provable_contracts_macros::contract("setfit-train-lifecycle-v1"` on the single validate fn
    - The EncoderTuned state's evidence is a non-Option field (grep `Option<PassedEvidence>` = 0 matches)
    - The 1e-30 negative's asserted error message contains a parameter name (dotted HF name) and a numeric delta
    - binding.yaml entry for evidence_gate has status: implemented and `make contract-audit-phase3` exits 0
  </acceptance_criteria>
  <done>EncoderTuned is proof of passed evidence; the two SAFE-03-class negatives are red in every cargo test.</done>
</task>

<task type="auto">
  <name>Task 3: FrozenProbeRun — the baseline that never claims SetFit</name>
  <files>crates/aprender-train/src/train/setfit/baseline.rs, crates/aprender-train/src/train/setfit/mod.rs, contracts/aprender/binding.yaml</files>
  <read_first>
    - contracts/linear-probe-classifier-v1.yaml (the invariants the type must satisfy — read BEFORE designing)
    - crates/aprender-contrastive-data/src/split.rs (:55-70 — the CompatibilityTest-vs-Test distinct-marker precedent)
    - crates/aprender-core/src/classification/multinomial.rs (the head the probe reuses, from 03-04)
  </read_first>
  <action>
    baseline.rs: `FrozenProbeRun` — a separate type (NOT a SetFitRun state, NOT convertible to
    one; no From/Into impls exist) that: takes an encoder + Selection + head regularization,
    encodes the selected rows in eval mode WITHOUT any tuning step, fits
    MultinomialLogisticRegression on those frozen embeddings, and reports through its own
    `FrozenProbeReport` whose kind field is the literal string "frozen_linear_probe" (never
    "setfit"). Its documentation states the SAFE-03 rationale and that Phase 5 runs baselines
    through this door. Bind to linear-probe-classifier-v1.yaml: #[contract] annotation on the
    probe's fit function against whichever existing equation fits (read the contract first);
    binding.yaml entries with real module path; if the existing contract's equations do not
    line up with this surface, bind what fits, set the rest pending, and record the mismatch in
    the SUMMARY (do not force-fit or rewrite that contract here).
    Tests: probe produces a working classifier on the fixture (finite probabilities, sum to 1);
    the report kind equals "frozen_linear_probe"; grep-style test that baseline.rs never
    constructs a SetFitRun (no `SetFitRun<` token in baseline.rs outside comments).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit baseline_ > /tmp/p3-06t3.log 2>&1; rc=$?; tail -3 /tmp/p3-06t3.log; [ $rc -eq 0 ] || exit $rc; make contract-audit-phase3 > /tmp/cap3.log 2>&1; rc=$?; tail -2 /tmp/cap3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit baseline_` exits 0
    - baseline.rs contains the literal "frozen_linear_probe" and grep -v '^//' baseline.rs | grep -c 'SetFitRun<' = 0
    - binding.yaml contains at least one entry with contract: linear-probe-classifier-v1.yaml and a module path under the setfit train module
    - `make contract-audit-phase3` exits 0 with the new bindings
  </acceptance_criteria>
  <done>The baseline is runnable, labeled, contract-bound, and structurally incapable of claiming SetFit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training run → SetFit identity claim | the phase's core integrity boundary (SAFE-03) |
| measured basis → frozen thresholds | epsilon committed before judgment or the gate is theater |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-20 | Spoofing | frozen probe / centroid / 1e-30-LR run labeled SetFit | mitigate | gate inside the transition; empty-trainable unpassable; distinct FrozenProbeRun type; two in-band negatives red in every cargo test |
| T-3-21 | Tampering | epsilon loosened after a failing comparison | mitigate | epsilon lives in the pv-validated contract; loosening requires a contract edit pv diff flags; Rust-constant-equals-YAML-literal test |
| T-3-22 | Repudiation | gate passing for the wrong reason (vacuous negatives) | mitigate | negative + control + mirror quartet; negatives observed RED before the gate existed (recorded) |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `pv validate contracts/setfit-train-lifecycle-v1.yaml` exits 0
- `cargo test -p aprender-train --lib --features setfit` (full setfit module) exits 0 including negatives
- `make contract-audit-phase3` exits 0
</verification>

<success_criteria>
- EncoderTuned unmintable without passing evidence; negatives red in every cargo test
- Frozen thresholds contract-committed before first judgment, derived from measured basis
- FrozenProbeRun runnable, bound, and never SetFit-labeled
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-06-SUMMARY.md` when done.
Record: the frozen epsilon/k/margin and their derivation, the RED observations for both
negatives, the A1 outcome, and any linear-probe contract mismatches.
</output>
