---
phase: 03-faithful-two-stage-trainer-and-head
plan: 08
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - crates/aprender-train/src/train/setfit/verify.rs
  - crates/aprender-train/src/train/setfit/lock.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - contracts/setfit-train-lifecycle-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [TRN-01, TRN-07]

must_haves:
  truths:
    - "SetFitRun<ArtifactReloadedAndVerified> exists only after: close the in-memory model, reload from bytes through the ReloadVerify implementation, re-encode probe rows, re-predict, and compare (exact for the serde impl); a corrupted byte stream fails typed before any comparison"
    - "The ReloadVerify trait is format-agnostic (bytes in, verified bundle out, artifact_hash exposed) so Phase 4's APR implementation swaps the format, not the check (D-07)"
    - "A CanonicalTestToken can be minted ONLY from a SelectionLock whose committed artifact hash matches the model under evaluation — lock, keep tuning, then test INVALIDATES (typed error), never passes (D-14)"
    - "The lock record commits to configuration, artifact hash, canonical-validation metric, and selection-run hashes via canonical bytes + SHA-256; a forged or mutated record fails hash verification"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/verify.rs"
      provides: "ReloadVerify trait + serde round-trip implementation + verify_artifact transition"
    - path: "crates/aprender-train/src/train/setfit/lock.rs"
      provides: "SelectionLock hash-committing record + CanonicalTestToken typestate + ValidationMetric witness"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/verify.rs"
      to: "crates/aprender-train/src/train/setfit/mod.rs"
      via: "verify_artifact(self) consuming HeadFitted, minting ArtifactReloadedAndVerified"
      pattern: "ArtifactReloadedAndVerified"
    - from: "crates/aprender-train/src/train/setfit/lock.rs"
      to: "aprender-contrastive-data Split<Validation>"
      via: "ValidationMetric constructible only against the canonical validation split"
      pattern: "Split<Validation>"
  caveats:
    - "The serde JSON artifact is the PHASE 3 implementation of the trait — Phase 4 replaces the format with setfit-apr-v1 behind the SAME trait. Do not optimize the serde format; do not name it APR anywhere."
    - "serde_json f32 round-trips exactly (ryu shortest representation) — the serde impl's comparison is EXACT equality; the trait still carries a tolerance parameter because Phase 4's format may need it (contracted there)."
---

<objective>
Close the lifecycle: the ReloadVerify seam (Phase 3 owns the boundary, Phase 4 owns the format —
D-07) with a serde implementation that proves the close -> reload -> re-encode -> re-predict ->
compare invariant, minting ArtifactReloadedAndVerified; and the hash-committing SelectionLock
with the artifact-hash-matching CanonicalTestToken that makes lock-then-tune-then-test invalidate
(D-14, TRN-07).

Purpose: TRN-01's final state and TRN-07's mechanical selection-integrity check — the thing
Phase 5's "any post-test-selected cell invalidates the report" hangs off.
Output: verify.rs + lock.rs + transitions, contract growth for verify/lock equations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key facts:
- Round-trip analog: ledger.rs to_canonical_bytes/from_bytes (serialize-with-schema-version, parse-with-version-check, UnsupportedSchemaVersion typed error). No in-repo trait seam for verify-by-reload exists — this plan designs it as Phase 4's drop-in (RESEARCH "No Analog Found").
- Token analog: select.rs SelectedId — private constructor, accessors cannot mint, the type is proof of provenance. Append-only discipline from ledger.rs: "an append-only log that can be rewritten is not evidence."
- Artifact hash: SHA-256 over the artifact's canonical bytes (sha2 unconditional in aprender-train). One hashing convention repo-wide — canonical-bytes-then-SHA-256.
- The bundle the serde impl serializes: encoder tensors (f32), head weights (f32) + ordered labels, resolved SetFitTrainConfig, EvidenceSummary (with its table hash), schema_version. deny_unknown_fields on the wire struct.
- Phase 2's access ledger records which splits were touched; the lock is the COMPLEMENT that distinguishes honest selection from selection redone after a test peek (D-14 rationale — do not conflate them).
- Split<Validation> and Split<CompatibilityTest> are distinct types (Ph2 D-16/D-19): a compatibility selection run is non-constructible; ValidationMetric therefore witnesses CANONICAL validation by type.
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReloadVerify trait + serde bundle implementation</name>
  <files>crates/aprender-train/src/train/setfit/verify.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/ledger.rs (canonical bytes + schema-version round-trip shape :82-140)
    - crates/aprender-contrastive-data/src/attestation.rs (deny_unknown_fields wire-struct precedent)
    - crates/aprender-train/src/train/setfit/mod.rs (HeadFitted surface from 03-07: accessors for encoder state, head, labels, config, evidence)
    - crates/aprender-core/src/setfit/mod.rs (how encoder parameters are enumerated for serialization: named_parameters surface)
  </read_first>
  <action>
    Define `trait ReloadVerify` — format-agnostic, designed as Phase 4's drop-in seam: methods
    for serialize(bundle) -> Result<Vec<u8>>, reload(&[u8]) -> Result<Bundle>, and
    artifact_hash(&[u8]) -> [u8; 32] (SHA-256 of the bytes, default-implemented); a tolerance
    parameter type for the compare step (the serde impl passes exact-zero tolerances; Phase 4's
    APR impl will pass contracted ones). Bundle = the model's complete deterministic state:
    encoder named tensors (f32, BTreeMap name order), head weights + ordered labels, resolved
    config, EvidenceSummary, schema_version. Implement `SerdeJsonVerifier` per the ledger.rs
    round-trip shape: schema_version check with UnsupportedSchemaVersion-class typed error,
    deny_unknown_fields, canonical field order. Tests: round-trip byte-stability (serialize
    twice -> identical bytes); reload of truncated/corrupted bytes -> typed error naming the
    parse failure; version-bumped payload -> typed version error; f32 exactness (a tensor of
    adversarial values — subnormals, -0.0, values near f32::MAX — survives the JSON round-trip
    bit-exactly).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit verify_ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit verify_` exits 0
    - verify.rs contains `trait ReloadVerify` and an impl whose name contains `SerdeJson`
    - The wire struct carries schema_version and deny_unknown_fields (grep both >= 1)
    - The f32-exactness test includes a negative-zero and a subnormal value
    - grep `apr\b\|Apr\b|APR\b` in verify.rs type names = 0 (the serde impl never claims to be the APR format)
  </acceptance_criteria>
  <done>The seam exists with an implementation Phase 4 can swap by format alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: verify_artifact transition -> ArtifactReloadedAndVerified</name>
  <files>crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/src/train/setfit/verify.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/verify.rs (Task 1's trait/impl)
    - crates/aprender-train/src/train/setfit/test_fixtures.rs (pipeline fixture)
  </read_first>
  <behavior>
    - full pipeline on the fixture: prepare -> tune_encoder -> fit_head -> verify_artifact(SerdeJsonVerifier) returns Ok(SetFitRun<ArtifactReloadedAndVerified>) carrying the artifact hash
    - the reloaded model's re-encoded probe embeddings and re-predicted probabilities EXACTLY equal the pre-close values (serde impl: exact); labels exactly equal
    - a verifier handed deliberately corrupted bytes (flip one byte post-serialize via a test-only injection point on the verifier) -> typed VerifyFailed/parse error; the transition returns Err and no ArtifactReloadedAndVerified value exists
    - the in-memory model is CLOSED before reload: the transition consumes self, drops the live encoder/head, and rebuilds ONLY from bytes (assert via a canary: mutate nothing, but the test verifies predictions come from the reloaded bundle by corrupting the pre-close copy path if any exists — at minimum, source-assert the transition builds its comparison object from reload() output)
  </behavior>
  <action>
    Write the behavior tests first. Implement `impl SetFitRun<HeadFitted> { pub fn
    verify_artifact<V: ReloadVerify>(self, verifier: &V) -> Result<SetFitRun<
    ArtifactReloadedAndVerified>, SetFitTrainError> }`: capture probe predictions (the selected
    rows) BEFORE serialization; serialize the bundle; drop the in-memory state (consume);
    reload from bytes; re-encode + re-predict from the reloaded bundle; compare embeddings,
    logits/probabilities, and labels at the verifier's tolerance (exact here); mint the final
    state with Evidence extended by {artifact_hash, verify report}. Implement LifecycleState
    for ArtifactReloadedAndVerified (absent-field pattern). Every failure is a typed variant
    naming what diverged (field, index, expected, observed). Annotate the compare gate with
    #[provable_contracts_macros::contract("setfit-train-lifecycle-v1", equation =
    "reload_verify_roundtrip")] — the equation lands in Task 3's contract growth.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit verify_artifact 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit verify_artifact` exits 0
    - The corrupted-bytes test asserts a typed error and that the error names the failure site
    - mod.rs implements LifecycleState for ArtifactReloadedAndVerified with a non-Option evidence type
    - The transition's comparison object is constructed from reload() output (source assertion: the compare call's arguments trace to the reloaded bundle, not the pre-close model)
  </acceptance_criteria>
  <done>The last lifecycle state is mintable only through a real close-reload-verify round trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SelectionLock + CanonicalTestToken + contract growth</name>
  <files>crates/aprender-train/src/train/setfit/lock.rs, crates/aprender-train/src/train/setfit/mod.rs, contracts/setfit-train-lifecycle-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - crates/aprender-contrastive-data/src/select.rs (SelectedId private-constructor token pattern :103-118)
    - crates/aprender-contrastive-data/src/ledger.rs (append-only + canonical-bytes discipline)
    - crates/aprender-contrastive-data/src/split.rs (Split<Validation> type — the witness ValidationMetric requires)
    - contracts/setfit-train-lifecycle-v1.yaml (current equations — grow, do not rewrite)
  </read_first>
  <behavior>
    - lock flow: an ArtifactReloadedAndVerified run mints a SelectionLock committing {schema_version, config hash, artifact_hash, validation metric name+value, selection semantic hash, ledger hash}; canonical bytes stable; lock_hash binds (mutation test)
    - token minting: lock.mint_test_token(candidate_artifact_hash) succeeds when the hash matches the lock's committed hash, and returns a typed StaleLock error naming both hashes when it does not (the lock-then-tune-then-test sequence)
    - ValidationMetric is constructible only with a &Split<Validation> argument (compile-visible witness; the compatibility profile emits no Split<Validation> per Ph2 D-19, so a compatibility-selected metric is non-constructible)
    - CanonicalTestToken has no public constructor and its accessors cannot mint (SelectedId pattern); tests can only obtain one through the lock
  </behavior>
  <action>
    Write the behavior tests first. Implement lock.rs: `ValidationMetric` (name String, value
    f64, constructor takes &Split<Validation> plus the metric — the split argument is the
    type-level witness; document why per D-14/Ph2 D-19); `SelectionLock` with private fields,
    minted by `SetFitRun<ArtifactReloadedAndVerified>::create_selection_lock(metric)` (pulls
    config hash, artifact hash, selection semantic hash, ledger hash from the run's own state);
    canonical bytes + lock_hash per the ledger.rs shape (schema_version, deny_unknown_fields,
    no timestamps); `CanonicalTestToken` mintable ONLY by
    `SelectionLock::mint_test_token(&self, candidate: [u8;32])` on hash match, private fields,
    ordinal-style accessors that cannot mint. Provide the canonical-test door Phase 5 will
    consume: `CanonicalTestAccess::grant(token, &Split<Test>) -> CanonicalTestGrant` — a typed
    witness pairing the token with the canonical test split (compatibility split is a different
    type, unexpressible here).

    Contract growth: append equations selection_lock_commitment (the hash-committed field list
    and the match-required-to-mint invariant) and reload_verify_roundtrip (Task 2's gate) to
    setfit-train-lifecycle-v1.yaml; falsification tests name the StaleLock and corrupted-bytes
    tests with --lib commands; pv validate; binding.yaml entries (bare filename, implemented).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit lock_ 2>&1 | tail -3 && cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/setfit-train-lifecycle-v1.yaml && make contract-audit-phase3 > /tmp/cap3.log 2>&1; rc=$?; tail -2 /tmp/cap3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit lock_` exits 0 including a StaleLock test asserting both hashes appear in the rendered error
    - lock.rs has no `pub fn new` on CanonicalTestToken and no public field on it (source assertion)
    - ValidationMetric's constructor signature contains `Split<Validation>` (source assertion)
    - `pv validate contracts/setfit-train-lifecycle-v1.yaml` exits 0 with the two new equations present
    - `make contract-audit-phase3` exits 0
  </acceptance_criteria>
  <done>Canonical test access is mechanically gated on a hash-matching lock; stale locks invalidate instead of passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| serialized bytes → reloaded model | artifacts crossing the persistence boundary (V6 integrity) |
| selection decision → canonical test access | THE domain access-control boundary (V4-analogous, D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-26 | Tampering | corrupted/forged artifact bytes accepted | mitigate | schema-version + deny_unknown_fields + typed parse errors + exact round-trip compare before the state mints |
| T-3-27 | Repudiation | lock-then-tune-then-test passing | mitigate | token mintable only on artifact-hash match; StaleLock typed error names both hashes; falsification test in contract |
| T-3-28 | Tampering | rewritten lock record | mitigate | canonical bytes + SHA-256 binding, mutation test; append-only discipline (no removal API) |
| T-3-29 | Spoofing | metric from compatibility/test split posing as canonical validation | mitigate | ValidationMetric requires &Split<Validation> by type; compatibility profile cannot produce one (Ph2 D-19) |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit verify_` / `verify_artifact` / `lock_` all exit 0
- `pv validate contracts/setfit-train-lifecycle-v1.yaml` exits 0; `make contract-audit-phase3` exits 0
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0
</verification>

<success_criteria>
- All four lifecycle states now reachable exactly in order, each with typed-failure exits
- D-07 seam proven with the serde impl; Phase 4 swaps format only
- D-14 lock mechanics proven: match mints, stale invalidates, forgery fails hash
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-08-SUMMARY.md` when done.
Record: the bundle schema (fields + version), the trait signature Phase 4 must implement, and
the lock record's committed field list.
</output>
