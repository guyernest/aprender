---
phase: 03-faithful-two-stage-trainer-and-head
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-train/Cargo.toml
  - crates/aprender-train/src/train/mod.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/config.rs
  - crates/aprender-train/src/train/setfit/reduce.rs
  - crates/aprender-train/src/train/setfit/epoch.rs
  - crates/aprender-train/src/optim/scheduler/mod.rs
  - crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs
  - Makefile
autonomous: true
requirements: [TRN-01, TRN-02]

must_haves:
  truths:
    - "cargo check -p aprender-train --no-default-features --features setfit exits 0 AND the maximal CPU-safe feature combination with setfit also exits 0 (the setfit feature is dependency-CLOSED across the 37-module crate)"
    - "Every one of the 12 config knobs rejects its invalid forms with a typed error naming the knob and the observed value, BEFORE any training state exists — and the SAME validation runs on the deserialization path, so a JSON payload cannot smuggle an invalid value past it"
    - "SetFitRun<Prepared> can only be minted through prepare(); its fields are private and its state is a type parameter, not a runtime field"
    - "Epoch e's pair permutation is a pure function of (root_seed, e) under the trainer's own frozen domain tag, golden-pinned independently"
    - "warmup_steps = ceil(warmup_ratio * total_steps), pinned to the HF reference formula, and the scheduler's LR at step 0 with warmup_steps > 0 is exactly 0.0"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/config.rs"
      provides: "SetFitTrainConfig (requested, serializable, validated) + ResolvedSetFitConfig (adds the resolved device) — 12 knobs, reference defaults, serde routed through TryFrom"
    - path: "crates/aprender-train/src/train/setfit/mod.rs"
      provides: "LifecycleState sealed trait, four state markers, SetFitRun<Prepared> + prepare()"
    - path: "crates/aprender-train/src/train/setfit/reduce.rs"
      provides: "Fixed-order f64-accumulating reductions (sum/mean/l2_norm)"
    - path: "crates/aprender-train/src/train/setfit/epoch.rs"
      provides: "Philox epoch-shuffle permutation under tag apr-setfit-train-v1"
    - path: "crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs"
      provides: "WarmupLinearDecayLR — the reference schedule (linear warmup then linear decay to 0)"
  key_links:
    - from: "crates/aprender-train/Cargo.toml"
      to: "aprender-contrastive-data"
      via: "optional dep under the new setfit feature"
      pattern: "dep:aprender-contrastive-data"
    - from: "crates/aprender-train/src/train/setfit/config.rs"
      to: "crates/aprender-train/src/train/device.rs"
      via: "resolve_device reused in place for the device knob"
      pattern: "resolve_device"
  caveats:
    - "KNOWN-RED from this plan through the publish cascade: cargo package -p aprender-train fails on manifest RESOLUTION of the unpublished aprender-contrastive-data path dep — --no-verify does NOT dodge it (Phase 2 control-verified). Publish cascade order becomes aprender-contrastive-data -> {apr-cli, aprender-train}. /gsd:verify-work must read this as the expected state."
    - "tier2 is RED on arm64 (24 pre-existing clippy errors, D-ITEM-02) and its headline test step runs zero tests (D-ITEM-03) — verify via the SCOPED commands in this plan, never via 'run tier2, see green'."
    - "aprender-train has cuda/gpu/nvml/wasm features; a literal `--all-features` leg is NOT buildable on the CPU profile. The correct substitute is the maximal CPU-SAFE combination, with a no-setfit control leg proving any pre-existing breakage is not this phase's (see Task 1)."
    - "CARGO_INCREMENTAL=0 for all builds."
---

<objective>
Open the trainer's home: the `setfit` feature in aprender-train (dependency-closed across a crate
that also carries GPU/LoRA/distill/server), the sealed lifecycle typestate skeleton with
SetFitRun<Prepared> and prepare() (D-05, D-06), the 12-knob fail-closed configuration split into a
requested/serializable form and a resolved runtime form (TRN-02), and the three determinism
primitives the training loop will consume: the WarmupLinearDecayLR reference scheduler (RESEARCH
Pitfall 6 — the repo has no warmup-then-decay schedule and that IS the reference), fixed-order
reductions (D-13), and the Philox epoch-shuffle domain (Phase 2 D-14's explicit hand-off).

Purpose: Everything wave 2 builds (the tuning loop, evidence capture) programs against these
contracts. Interface-first: the state markers and config exist before the transitions.
Output: compiling feature-gated train/setfit module tree; validated config with FALSIFY case
tables and a validated deserialization path; scheduler + reduce + epoch primitives with goldens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

**BRANCH PRECONDITION (phase-3 review fix).** `gsd/phase-3-two-stage-trainer` MUST already exist
and be checked out before this plan starts; it is created ONCE before wave 1, not by any plan. This
plan CHECKS, never creates. See `<wave_1_concurrency>`.

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md, re-verified this session):
- aprender-train deps: `aprender = { path = "../aprender-core", package = "aprender-core" }` (feature ref form is `aprender/setfit`); sha2 "0.10", serde_json, serde, **serde_yaml "0.9"** (Cargo.toml:96) already unconditional; NO aprender-contrastive-data, NO aprender-rand, NO setfit feature today; `default = ["tui"]`.
- aprender-train's feature list includes `cuda`, `gpu`, `nvml`, `wasm`, `hub-publish` — a literal `--all-features` check cannot build on the CPU profile. CPU-safe features: `cpu-fallback, gguf, monitor, tui, citl, server, tracing, ruchy-sessions, parquet, hub, viz, kani, dhat-heap`.
- The typestate house style to copy: contrastive-data/src/split.rs — marker types + role trait, private fields + PhantomData, gated pub(crate) constructors, validation INSIDE the only door. Ph2 02-03 precedent: state-specific data is an associated type on the state trait (absent field beats Option+expect — that is what makes the trybuild proof provable).
- Config precedents: device.rs (pure parser split from environment probe; typed error whose Display cites the contract; FALSIFY-headed grammar case tables) and pairs.rs resolve_budget (cross-field ladder, each rung names what to change; the cap BINDS, never clamps).
- resolve_device (train/device.rs:110) already fails closed: explicit cuda/cuda:N without CUDA -> DeviceError::CudaNotAvailable; grammar rejects cuda:01; index <= 15; auto falls back to CPU.
- `MiniLmTokenizer` truncates at `pub const MAX_SEQUENCE_LENGTH: usize = 256` (setfit/tokenizer.rs:52) and `SetFitMiniLm::encode_texts(&self, texts)` takes NO max-length parameter. VERIFIED by reading. The max_length knob therefore constrains what may be REQUESTED; it does not reconfigure truncation.
- The trainer's text source is the PreparedDataset, not the Selection: `SelectedExample` carries `{id, label, exact_hash, normalized_hash}` and NO text (select.rs:81-89); text lives in `Split<Train>::rows() -> &[LabeledExample]` (split.rs:175). VERIFIED by reading — SetFitRun must therefore hold the dataset, not just the selection.
- Scheduler analog: warmup_cosine_decay.rs — struct {lr_max, lr_min, warmup_steps, total_steps, current_step}, `LRScheduler` trait `{get_lr, step}` (scheduler/mod.rs:23-30) plus an inherent `apply<O: Optimizer>(&self, optimizer)` (:33), zero-division guards. Do NOT copy LinearWarmupLR (constant after warmup — the deviation Pitfall 6 flags).
- Epoch shuffle: copy contrastive-data/src/rng.rs construction with OWN tag b"apr-setfit-train-v1\0"; Fisher-Yates with bounded draws; NonZeroU64 bound; golden constants derived independently.
- `PairSampler::pair_at(ordinal) -> LabeledPair` is O(1) random access (pairs.rs:1020) — permuting the replay stream is O(B), not O(B^2). Recorded here because the review raised the complexity question; it is answered by the existing API.
- Makefile already has `setfit-feature-matrix` (aprender-core legs + tokenizers-leak negative, wired into tier3 at :344); extend it with aprender-train legs.

**Review fix 1 — serde bypassed validation.** Deriving `Deserialize` directly on
`SetFitTrainConfig` creates a second construction path; `deny_unknown_fields` rejects unknown KEYS,
not invalid VALUES, so a payload with `encoder_lr: -1.0` deserialized cleanly straight past the
12-knob table. Fixed by `#[serde(try_from = "...")]` over a private wire type.

**Review fix 2 — device probing was mixed into a serializable config.** Requested configuration and
resolved runtime device are now distinct types.

**Review fix 3 — the warmup-step rounding rule was not pinned to the reference.** HF's
`TrainingArguments.get_warmup_steps` uses `math.ceil(num_training_steps * warmup_ratio)`. `round`
was a guess; `ceil` is the reference.
</context>

<wave_1_concurrency>
Wave 1 runs 03-01, 03-02 and 03-03 against ONE shared worktree on ONE branch. Rules (identical in
all three plans):

1. NEVER run `git checkout`, `git checkout -b`, `git switch`, `git stash`, `git reset`, `git clean`.
2. Stage with an EXPLICIT PATHSPEC limited to this plan's `files_modified`:
   `git add -- <path> ...`. Never `git add -A`, `git add .`, or `git commit -a`.
3. An `index.lock` commit failure is a benign peer collision: sleep 2 s, retry, up to 5 times.
4. If `git branch --show-current` is not `gsd/phase-3-two-stage-trainer` at ANY point, STOP.
</wave_1_concurrency>

<tasks>

<task type="auto">
  <name>Task 1: setfit feature wiring + sealed typestate skeleton + feature-matrix legs</name>
  <files>crates/aprender-train/Cargo.toml, crates/aprender-train/src/train/mod.rs, crates/aprender-train/src/train/setfit/mod.rs, Makefile</files>
  <read_first>
    - crates/aprender-train/Cargo.toml ([features] block and [dependencies] — current state, dep name forms, the GPU/wasm features that make a literal --all-features leg unbuildable)
    - crates/aprender-contrastive-data/src/split.rs (the typestate house style: marker+trait+PhantomData+private fields, module doc explaining why identity is a type parameter; Split<Role>::rows at :175)
    - crates/aprender-contrastive-data/src/select.rs (Selection surface; SelectedExample has NO text field — confirm before designing SetFitRun's fields)
    - crates/aprender-contrastive-data/src/prepared.rs (PreparedDataset<Canonical>: train() :290, validation() :295, test() :300, validation_witness() :327, label_names() :285)
    - crates/aprender-train/src/train/mod.rs (module registration style)
    - Makefile setfit-feature-matrix target at :344 (the block to extend)
    - Root Cargo.toml [workspace.dependencies] (whether aprender-contrastive-data has a workspace entry; if not, use the path+version form matching the aprender dep line)
  </read_first>
  <action>
    Check the branch precondition (`git branch --show-current` must print
    `gsd/phase-3-two-stage-trainer`; STOP otherwise). Do NOT create or switch branches.

    Cargo.toml: add `aprender-contrastive-data` (optional, path ../aprender-contrastive-data +
    workspace version, matching the existing `aprender` dep line's form) and
    `aprender-rand = { workspace = true, optional = true }`. New feature:
    `setfit = ["aprender/setfit", "dep:aprender-contrastive-data", "dep:aprender-rand"]`.

    train/setfit/mod.rs (registered from train/mod.rs behind `#[cfg(feature = "setfit")]`):
    - Sealed `LifecycleState` trait (private supertrait module — external impls impossible) with
      associated type `Evidence`. Declare all four marker types now: `Prepared`, `EncoderTuned`,
      `HeadFitted`, `ArtifactReloadedAndVerified`. Implement LifecycleState ONLY for `Prepared`
      (Evidence = ()) in this plan; later plans implement the others as their evidence types land
      (interface-first: the markers are the contract, the impls arrive with the transitions).
    - `SetFitRun<S: LifecycleState>` with ALL fields private:
        `encoder: SetFitMiniLm`,
        `dataset: PreparedDataset<Canonical>`  <-- REQUIRED and previously missing: SelectedExample
            carries no text, so without the dataset neither the tuning loop (03-05) nor the
            encode-once head input (03-07) can obtain a single string to encode,
        `selection: Selection`,
        `config: ResolvedSetFitConfig`,
        `evidence: S::Evidence`,
        `_state: PhantomData<S>`.
      No public constructor; the only door is
      `SetFitRun::<Prepared>::prepare(encoder, dataset, selection, config) ->
       Result<SetFitRun<Prepared>, SetFitTrainError>`, which resolves the device, re-validates
      cross-field facts that need the Selection (pair budget vs selection capacity via
      contrastive-data's capacity fns), and asserts every selected id resolves to a row in
      `dataset.train()` (a selection whose ids are not in the dataset is a typed error, not a later
      panic).
    - `SetFitTrainError` enum skeleton with the variants this plan needs (Config(SetFitConfigError),
      Device(DeviceError), Capacity variants, SelectionRowMissing{id}); later plans extend it.

    Makefile: extend `setfit-feature-matrix` with THREE aprender-train legs:
      (a) `cargo check -p aprender-train --no-default-features --features setfit`
      (b) `cargo check -p aprender-train --features setfit`   (default + setfit)
      (c) the MAXIMAL CPU-SAFE leg:
          `cargo check -p aprender-train --features setfit,cpu-fallback,gguf,monitor,tui,citl,server,tracing,ruchy-sessions,parquet,hub,viz`
      Review fix: Codex asked for an `--all-features` leg. A literal `--all-features` cannot build
      here — `cuda`, `gpu`, `nvml` and `wasm` need toolchains the CPU profile does not have — so
      leg (c) is the honest substitute: every CPU-buildable feature co-enabled with setfit, which
      is exactly the D-05 risk surface ("the setfit feature now has to propagate through a
      37-module crate that also carries GPU/LoRA/distill/server").
      CONTROL for leg (c): before wiring it, run the SAME feature list WITHOUT `setfit` and record
      its rc. If the control is already red, the breakage is pre-existing and leg (c) is wired with
      the setfit-diff subset that IS green plus a recorded deferred-item note — Phase 3 does not
      inherit an unrelated red, and it does not hide one either. Record both rcs in the SUMMARY.
    Run the target standalone first and capture rc directly (`> /tmp/sfm.log 2>&1; rc=$?` — never
    through a pipe).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo check -p aprender-train --no-default-features --features setfit > /tmp/p3-03t1a.log 2>&1; rc=$?; tail -3 /tmp/p3-03t1a.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo check -p aprender-train --features setfit > /tmp/p3-03t1b.log 2>&1; rc=$?; tail -3 /tmp/p3-03t1b.log; [ $rc -eq 0 ] || exit $rc; make setfit-feature-matrix > /tmp/p3-03t1c.log 2>&1; rc=$?; tail -3 /tmp/p3-03t1c.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints `gsd/phase-3-two-stage-trainer` (checked, not created)
    - All three feature legs exit 0 (or leg (c) is wired at the recorded green subset with both control rcs in the SUMMARY)
    - crates/aprender-train/Cargo.toml contains a `setfit = [` feature line listing `aprender/setfit`, `dep:aprender-contrastive-data`, `dep:aprender-rand`
    - train/setfit/mod.rs declares all four marker types and a sealed LifecycleState trait with `type Evidence`
    - SetFitRun has a `dataset` field of type `PreparedDataset<Canonical>` (source assertion — without it 03-05 and 03-07 have no text to encode)
    - `grep -c 'pub ' ` inside the SetFitRun struct block returns 0 (all fields private) and the state is `PhantomData<S>`
    - Makefile setfit-feature-matrix block contains `-p aprender-train` at least 3 times, and the literal string `--all-features` does NOT appear (it is unbuildable here; the SUMMARY records why)
    - `cargo package -p aprender-train --no-verify` FAILS with a no-matching-package error naming aprender-contrastive-data — capture the message; this is the EXPECTED known-red (Pitfall 9), record it in the SUMMARY
  </acceptance_criteria>
  <done>Feature-closed module tree exists; states are types; the run holds its own text source; the known-red is measured and recorded, not discovered later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetFitTrainConfig — 12 knobs, fail-closed, validated deserialization, resolved/requested split</name>
  <files>crates/aprender-train/src/train/setfit/config.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/device.rs (typed-error Display citing contract; pure parser vs probe split; FALSIFY-headed test tables; resolve_device at :110)
    - crates/aprender-contrastive-data/src/pairs.rs (PairConfig::new + resolve_budget two-stage validation; the binds-not-clamps doc)
    - crates/aprender-core/src/setfit/mod.rs (FreezeGroup, apply_freeze zero-match typed error — the freeze knob's existing validation)
    - crates/aprender-core/src/setfit/tokenizer.rs (MAX_SEQUENCE_LENGTH = 256 at :52 — the max_length knob's only legal value)
    - crates/aprender-contrastive-data/src/attestation.rs (deny_unknown_fields wire-struct precedent)
  </read_first>
  <behavior>
    THE KNOB TABLE (review fix: the twelve fields, their validation phase and their consumer are
    now enumerated unambiguously; every row has at least one rejected form, including root_seed):

    | # | Field | Type | Validation phase | Rejected forms | Consumer |
    |---|-------|------|------------------|----------------|----------|
    | 1 | encoder_lr | f64 | construction | 0.0, negative, NaN, inf | AdamW (03-05) |
    | 2 | epochs | u32 | construction | 0 | loop bound (03-05) |
    | 3 | batch_size | u32 | construction | 0 | window size (03-05) |
    | 4 | warmup_ratio | f64 | construction | < 0.0, > 1.0, NaN | warmup_steps (03-05) |
    | 5 | grad_clip_max_norm | f32 | construction | 0.0, negative, NaN | clip_grad_norm_refs (03-05) |
    | 6 | max_length | u32 | construction | any value != 256 -> MaxLengthNotSupported{requested, pinned} | asserted against the tokenizer at prepare(); recorded in the artifact bundle (03-08) |
    | 7 | pair_config | PairConfig | delegated | ZeroBudget / BudgetExceedsHardCap surface through unchanged | pair replay (03-05) |
    | 8 | freeze_policy | Vec<FreezeGroup> | structural at construction; zero-match at apply_freeze | malformed group; zero-match deferred and documented | apply_freeze (03-05, BEFORE any snapshot or baseline encode) |
    | 9 | head_regularization | enum | construction | Lambda(x) with x < 0 or NaN; SklearnEquivalentC{c} with c <= 0 or non-finite | head fit (03-07) |
    | 10 | root_seed | u64 | construction | ABSENT (no default — a missing root_seed in the wire form is MissingRootSeed); every present u64 is accepted, and a test asserts that acceptance explicitly | every RNG domain |
    | 11 | device | DeviceRequest (String) | construction (grammar) + prepare() (probe) | `cuda:01` grammar reject; explicit `cuda` on a CPU host -> CudaNotAvailable; a resolved non-CPU device -> UnsupportedDeviceForPhase3 | run_tuning asserts the resolved device is CPU (03-05) |
    | 12 | lr_schedule | fixed marker | construction | any variant other than WarmupLinearDecay | WarmupLinearDecayLR (03-05) |

    - cross-field: a warmup_ratio yielding warmup_steps > total_steps is rejected once total steps are computable (at prepare(), where n_pairs is known)
    - reference defaults: `SetFitTrainConfig::reference_defaults(root_seed)` yields lr 2e-5, epochs 1, batch 16, warmup_ratio 0.1, clip 1.0, max_length 256 — asserted with those exact values
    - AdamW hyperparameters (0.9, 0.999, 1e-8, weight_decay 0.0) are FIXED constants documented as reference-recipe values, not knobs
    - DESERIALIZATION (review fix): `serde_json::from_str::<SetFitTrainConfig>` on a payload with `encoder_lr: -1.0` FAILS with the same knob-naming error as the constructor; likewise for a payload missing `root_seed`, an out-of-range `warmup_ratio`, and `max_length: 512`. A payload with an unknown key still fails via deny_unknown_fields. A valid payload round-trips: serialize -> deserialize -> serialize gives byte-identical output
  </behavior>
  <action>
    Implement TWO types (review fix 2):
      - `SetFitTrainConfig` — the REQUESTED, serializable form. Holds the device as a
        `DeviceRequest` string-shaped value, never a probed `Device`. This is the canonical
        serialized form 03-08's artifact bundle embeds and hashes.
      - `ResolvedSetFitConfig` — `SetFitTrainConfig` plus the probed `Device` and the computed
        `warmup_steps`/`total_steps` once they are knowable. Produced by `prepare()`. NOT
        `Deserialize` (a resolved runtime fact must never arrive from a file); it IS `Serialize`,
        and 03-08's bundle carries BOTH forms so provenance records what was asked for and what was
        resolved.

    Validation is ONE function per knob (pure, no environment access) plus a cross-field pass,
    exactly the device.rs parser/probe split. `SetFitConfigError` variants carry named fields for
    expected AND observed values; Display cites contract id `setfit-train-lifecycle-v1` (authored in
    plan 03-06) and requirement TRN-02.

    DESERIALIZATION (the review fix, implement it this way, not by deriving Deserialize):
    define a PRIVATE wire struct `SetFitTrainConfigWire` deriving `Deserialize` with
    `#[serde(deny_unknown_fields)]` and declaration-order fields, then
    `#[serde(try_from = "SetFitTrainConfigWire")]` on `SetFitTrainConfig` with
    `impl TryFrom<SetFitTrainConfigWire> for SetFitTrainConfig` routing through the SAME public
    validating constructor. There must be exactly ONE validation implementation and zero ways to
    reach the type without it. Make `root_seed` non-optional in the wire struct so absence is a
    typed error rather than a silent default. No Duration, timestamp, or HashMap-ordered field
    anywhere (serde/serde_json are already unconditional aprender-train deps).

    The head-regularization knob is native lambda per D-04 (as amended): an enum with `Lambda(f64)`
    and `SklearnEquivalentC { c: f64 }` which resolves at fit time to `lambda = 1/(2*c*n)` with
    n = UNIQUE SELECTED ROWS. Document both; the `1/(2*C*n)` relation is the plan 03-04/03-06
    contract equation — link to it, do not restate it wrongly here.

    Pair knobs delegate to Phase 2's PairConfig/resolve_budget — never reimplement capacity logic.
    Freeze knob holds Vec<FreezeGroup>; structural checks here, zero-match enforcement stays with
    apply_freeze, and the doc states that 03-05 must call apply_freeze BEFORE the initial parameter
    snapshot and BEFORE the pre-tuning baseline encode (review fix: the LOW note that zero-match was
    deferred merely "until the first update" — it must precede any evidence capture).

    Write the FALSIFY case tables FIRST (tests named per device.rs's convention, including the
    deserialization cases), watch representative rows go red, then implement.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit config_ > /tmp/p3-03t2.log 2>&1; rc=$?; tail -3 /tmp/p3-03t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit config_` exits 0 and runs >= 22 tests (the 12-knob table plus the deserialization cases)
    - `grep -c 'derive' config.rs` region check: `SetFitTrainConfig` does NOT derive `Deserialize` directly; it carries `#[serde(try_from = ` (source assertion, both halves)
    - At least four tests deserialize INVALID JSON payloads (negative encoder_lr, missing root_seed, warmup_ratio 1.5, max_length 512) and assert the knob-naming typed error — a payload that deserializes cleanly is the defect
    - config.rs contains `resolve_device` (reuse, not reimplementation) and contains no independent CUDA/string device parsing
    - `ResolvedSetFitConfig` exists, is Serialize, and is NOT Deserialize (source assertion)
    - Every SetFitConfigError variant carries at least one named field (no bare unit variants for knob rejections)
    - A NaN encoder_lr is rejected with an error naming `encoder_lr`; a `max_length: 512` request is rejected with an error naming both 512 and 256
    - `reference_defaults` test asserts the exact values 2e-5 / 1 / 16 / 0.1 / 1.0 / 256
    - A test asserts every u64 root_seed is accepted (property or three-value table) AND that an absent root_seed is rejected
  </acceptance_criteria>
  <done>All 12 knobs fail closed before any training state exists, through exactly one validation implementation that deserialization cannot route around.</done>
</task>

<task type="auto">
  <name>Task 3: WarmupLinearDecayLR (ceil warmup, LR 0 at step 0) + fixed-order reductions + epoch-shuffle domain</name>
  <files>crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs, crates/aprender-train/src/optim/scheduler/mod.rs, crates/aprender-train/src/train/setfit/reduce.rs, crates/aprender-train/src/train/setfit/epoch.rs</files>
  <read_first>
    - crates/aprender-train/src/optim/scheduler/warmup_cosine_decay.rs (the exact structural mirror — swap cosine for linear-to-zero; note the inherent `apply<O: Optimizer>` at :33)
    - crates/aprender-train/src/optim/scheduler/mod.rs (LRScheduler trait :23-30, registration style :1-21)
    - crates/aprender-contrastive-data/src/rng.rs (DomainKey/derive_key/draw/bounded, NonZeroU64 bound, domains table, golden discipline)
    - crates/aprender-core/src/optim/lbfgs.rs (stylistic kin for sequential indexed numeric loops)
  </read_first>
  <action>
    warmup_linear_decay.rs: mirror WarmupCosineDecayLR's structure exactly (same fields, constructor
    shape, `apply<O: Optimizer>`, zero-division guards for warmup_steps==0 and decay_steps==0) with
    the decay branch linear to lr_min (default 0.0). Register in scheduler/mod.rs (module decl +
    pub use + doc bullet). NOT feature-gated — it is a general scheduler.

    REFERENCE PINNING (review fix 3), both facts documented on the type with their reference
    citation and both asserted by tests:
      - `warmup_steps = ceil(warmup_ratio * total_steps)` — HF `TrainingArguments.get_warmup_steps`
        uses `math.ceil`. The plan's earlier `round` was a guess. Provide the computation as a
        `pub fn warmup_steps_from_ratio(total_steps: u64, ratio: f64) -> u64` so exactly one
        implementation exists and 03-05 cannot re-derive it differently.
      - `get_lr()` at `current_step == 0` with `warmup_steps > 0` is EXACTLY 0.0, matching HF's
        LambdaLR, whose lambda(0) = 0 so the first optimizer step genuinely runs at lr 0. Document
        that this is deliberate reference fidelity, not an off-by-one, so the 03-05 loop ordering
        is not "fixed" later by someone who thinks it is a bug.
    Unit tests: LR at step 0 (exactly 0.0), mid-warmup ramp value, peak at warmup end, linear
    descent value at mid-decay, exactly lr_min at total_steps, both guard branches, and a
    `warmup_steps_from_ratio` table including a case where ceil and round differ
    (e.g. total_steps=11, ratio=0.1 -> ceil 2, round 1).

    reduce.rs (feature-gated with the setfit module): `sum_in_index_order(&[f32]) -> f64`,
    `mean_in_index_order(&[f32]) -> f64`, `l2_norm_in_index_order(&[f32]) -> f64` — sequential
    index-order folds accumulating in f64 (one reduction tree, identical bit pattern at any thread
    count; f64 accumulation also sidesteps f32 cancellation in delta-norms for near-frozen
    parameters, exactly the regime D-10's epsilon discriminates). Never `par_iter().sum()` anywhere
    in train/setfit — this module is the only reduction door. Tests: golden values; a crafted
    cancellation series ([1e8, 1.0, -1e8] style) where f64 index-order accumulation preserves the
    1.0 that f32 accumulation loses — assert the exact f64 result.

    epoch.rs: `epoch_pair_order(root_seed: u64, epoch: u32, n_pairs: u64) -> Vec<u64>` —
    Fisher-Yates over 0..n_pairs using the rng.rs construction under the trainer's OWN frozen tag
    `b"apr-setfit-train-v1\0"`, domain string "epoch-shuffle", stream_id = epoch, ordinal = draw
    counter; bounds via the multiply-shift `bounded` form with NonZeroU64 (modulo forbidden).
    Document on the fn: batches are consecutive fixed-size windows of this order, last batch short,
    never resampled (RESEARCH Open Question 3); and that the permuted stream is consumed via
    `PairSampler::pair_at(ordinal)`, which is O(1) random access (pairs.rs:1020), so permutation
    costs O(B) and not O(B^2). Tests: permutation validity (each index exactly once), replay
    equality, different-epoch difference, and one golden permutation for (seed=42, epoch=0, n=8)
    derived INDEPENDENTLY (Python from the construction spec recorded in the test doc comment).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit reduce_ > /tmp/p3-03t3a.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3a.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit epoch_ > /tmp/p3-03t3b.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3b.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib warmup_linear > /tmp/p3-03t3c.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3c.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - All three scoped test commands exit 0
    - `warmup_linear_decay.rs` contains `pub fn warmup_steps_from_ratio` and its body uses a ceiling computation (source assertion: `ceil` appears, `round` does not appear in that fn)
    - A test asserts `get_lr()` == 0.0 exactly at current_step 0 with warmup_steps > 0, and a second asserts the ceil-vs-round divergence case
    - `warmup_linear_decay.rs` get_lr returns exactly lr_min at current_step == total_steps (test asserts)
    - epoch.rs contains the byte literal `apr-setfit-train-v1` and does not contain `apr-contrastive-v1`
    - `grep -c -E 'par_iter|rayon' crates/aprender-train/src/train/setfit/reduce.rs` returns 0
    - The epoch golden test's doc comment contains its independent derivation
    - scheduler/mod.rs exports WarmupLinearDecayLR (grep `WarmupLinearDecayLR` in mod.rs >= 1)
  </acceptance_criteria>
  <done>The three determinism primitives exist, golden-pinned where random, with the reference schedule the repo was missing and its rounding rule pinned to the reference rather than guessed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config → trainer | 12 untrusted knobs cross into training; V5 input validation surface |
| serialized config file → trainer | a second, unvalidated construction path into the same type |
| feature graph → build | setfit feature leaking GPU/tui deps breaks the CPU-only contract |
| shared worktree → three parallel wave-1 executors | concurrent git index/branch mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering (V5) | invalid knob values reaching the loop | mitigate | fail-closed typed validation at construction; FALSIFY case tables per knob; no silent clamping (binds-not-clamps) |
| T-3-09 | Spoofing | direct construction of SetFitRun states | mitigate | sealed trait + private fields + PhantomData; trybuild proof lands in 03-09 |
| T-3-10 | Repudiation | epoch order depending on ambient RNG state | mitigate | Philox pure-function-of-(seed,epoch) permutation, own frozen tag, independent golden |
| T-3-11 | DoS | zero-division in scheduler guards | mitigate | warmup_steps==0 / decay_steps==0 branches copied from the cosine analog, tested |
| T-3-36 | Tampering (V5) | serde deserialization bypassing the 12-knob validation | mitigate | private wire type + `#[serde(try_from)]` routing through the single validating constructor; four invalid-payload rejection tests |
| T-3-43 | Tampering | wave-1 git index/branch race | mitigate | branch pre-created and only CHECKED; explicit-pathspec staging; no checkout/stash/reset |
| T-3-50 | Tampering | resolved runtime device arriving from an untrusted file | mitigate | ResolvedSetFitConfig is Serialize-only, never Deserialize; device probing happens in prepare() |
| T-3-51 | Repudiation | warmup step count guessed rather than pinned to the reference | mitigate | single `warmup_steps_from_ratio` using ceil, cited to HF `get_warmup_steps`, with a ceil-vs-round divergence test |
| T-3-SC | Tampering | package installs | accept | no external packages; new dep edges are in-repo crates (audit table verified acyclic) |
</threat_model>

<verification>
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0
- `cargo test -p aprender-train --lib --features setfit config_` / `reduce_` / `epoch_` all exit 0
- `make setfit-feature-matrix` run standalone with rc captured directly, exits 0 (three aprender-train legs, control rcs recorded)
</verification>

<success_criteria>
- setfit feature dependency-closed in aprender-train across the maximal CPU-safe combination; known-red package state measured and recorded
- 12 knobs fail closed with typed, knob-naming errors through one validation path serde cannot bypass; reference defaults pinned
- Scheduler/reduce/epoch primitives green with independent goldens and a reference-pinned warmup rule
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-03-SUMMARY.md` when done.
Record: the measured cargo-package known-red message, both feature-matrix control rcs, the epoch
golden derivation, the warmup ceil-vs-round divergence case, and any cross-field validation rules
that needed Selection-time deferral.
</output>
