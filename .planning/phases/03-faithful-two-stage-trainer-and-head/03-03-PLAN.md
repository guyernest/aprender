---
phase: 03-faithful-two-stage-trainer-and-head
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-train/Cargo.toml
  - crates/aprender-train/src/train/mod.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/config.rs
  - crates/aprender-train/src/train/setfit/reduce.rs
  - crates/aprender-train/src/train/setfit/epoch.rs
  - crates/aprender-train/src/optim/scheduler/mod.rs
  - crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs
  - Makefile
autonomous: true
requirements: [TRN-01, TRN-02]

must_haves:
  truths:
    - "cargo check -p aprender-train --no-default-features --features setfit exits 0 (the setfit feature is dependency-CLOSED across the 37-module crate)"
    - "Every one of the 12 config knobs rejects its invalid forms with a typed error naming the knob and the observed value, BEFORE any training state exists"
    - "SetFitRun<Prepared> can only be minted through prepare(); its fields are private and its state is a type parameter, not a runtime field"
    - "Epoch e's pair permutation is a pure function of (root_seed, e) under the trainer's own frozen domain tag, golden-pinned independently"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/config.rs"
      provides: "SetFitTrainConfig — 12 knobs validated at construction, reference defaults"
    - path: "crates/aprender-train/src/train/setfit/mod.rs"
      provides: "LifecycleState sealed trait, four state markers, SetFitRun<Prepared> + prepare()"
    - path: "crates/aprender-train/src/train/setfit/reduce.rs"
      provides: "Fixed-order f64-accumulating reductions (sum/mean/l2_norm)"
    - path: "crates/aprender-train/src/train/setfit/epoch.rs"
      provides: "Philox epoch-shuffle permutation under tag apr-setfit-train-v1"
    - path: "crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs"
      provides: "WarmupLinearDecayLR — the reference schedule (linear warmup then linear decay to 0)"
  key_links:
    - from: "crates/aprender-train/Cargo.toml"
      to: "aprender-contrastive-data"
      via: "optional dep under the new setfit feature"
      pattern: "dep:aprender-contrastive-data"
    - from: "crates/aprender-train/src/train/setfit/config.rs"
      to: "crates/aprender-train/src/train/device.rs"
      via: "resolve_device reused in place for the device knob"
      pattern: "resolve_device"
  caveats:
    - "KNOWN-RED from this plan through the publish cascade: cargo package -p aprender-train fails on manifest RESOLUTION of the unpublished aprender-contrastive-data path dep — --no-verify does NOT dodge it (Phase 2 control-verified). Publish cascade order becomes aprender-contrastive-data -> {apr-cli, aprender-train}. /gsd:verify-work must read this as the expected state."
    - "tier2 is RED on arm64 (24 pre-existing clippy errors, D-ITEM-02) and its headline test step runs zero tests (D-ITEM-03) — verify via the SCOPED commands in this plan, never via 'run tier2, see green'."
    - "CARGO_INCREMENTAL=0 for all builds."
---

<objective>
Open the trainer's home: the `setfit` feature in aprender-train (dependency-closed across a crate
that also carries GPU/LoRA/distill/server), the sealed lifecycle typestate skeleton with
SetFitRun<Prepared> and prepare() (D-05, D-06), the 12-knob fail-closed SetFitTrainConfig
(TRN-02), and the three determinism primitives the training loop will consume: the
WarmupLinearDecayLR reference scheduler (RESEARCH Pitfall 6 — the repo has no warmup-then-decay
schedule and that IS the reference), fixed-order reductions (D-13), and the Philox epoch-shuffle
domain (Phase 2 D-14's explicit hand-off).

Purpose: Everything wave 2 builds (the tuning loop, evidence capture) programs against these
contracts. Interface-first: the state markers and config exist before the transitions.
Output: compiling feature-gated train/setfit module tree; validated config with FALSIFY case
tables; scheduler + reduce + epoch primitives with goldens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md, read directly this session):
- aprender-train deps: `aprender = { path = "../aprender-core", package = "aprender-core" }` (feature ref form is `aprender/setfit`); sha2 "0.10", serde_json, serde already unconditional; NO aprender-contrastive-data, NO aprender-rand, NO setfit feature today; `default = ["tui"]`.
- The typestate house style to copy: contrastive-data/src/split.rs — marker types + role trait, private fields + PhantomData, gated pub(crate) constructors, validation INSIDE the only door. Ph2 02-03 precedent: state-specific data is an associated type on the state trait (absent field beats Option+expect — that is what makes the trybuild proof provable).
- Config precedents: device.rs (pure parser split from environment probe; typed error whose Display cites the contract; FALSIFY-headed grammar case tables) and pairs.rs resolve_budget (cross-field ladder, each rung names what to change; the cap BINDS, never clamps).
- resolve_device (train/device.rs:110) already fails closed: explicit cuda/cuda:N without CUDA -> DeviceError::CudaNotAvailable; grammar rejects cuda:01; index <= 15; auto falls back to CPU.
- Reference defaults for the 12 knobs (RESEARCH knob table): encoder LR 2e-5, epochs 1, batch 16, warmup 0.1 of total steps, grad clip max_norm 1.0, LR schedule = linear warmup -> linear decay to 0, AdamW betas (0.9, 0.999) eps 1e-8 weight_decay 0.0, max length = Phase 1's pinned tokenizer truncation (reuse ENC-02 facts, not a new decision), pair policy/budget = Phase 2 PairConfig + resolve_budget, freeze = all-trainable default + FreezeGroup list, head regularization default = sklearn LogisticRegression() C=1.0 equivalence (lambda = 1/(2n), resolved at fit time), root seed user-supplied, device via resolve_device.
- Scheduler analog: warmup_cosine_decay.rs — struct {lr_max, lr_min, warmup_steps, total_steps, current_step}, LRScheduler trait (get_lr/step) at scheduler/mod.rs:23-30, zero-division guards. Do NOT copy LinearWarmupLR (constant after warmup — the deviation Pitfall 6 flags).
- Epoch shuffle: copy contrastive-data/src/rng.rs construction with OWN tag b"apr-setfit-train-v1\0"; Fisher-Yates with bounded draws; NonZeroU64 bound; golden constants derived independently.
- Makefile already has `setfit-feature-matrix` (aprender-core legs + tokenizers-leak negative, wired into tier3); extend it with aprender-train legs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: setfit feature wiring + sealed typestate skeleton</name>
  <files>crates/aprender-train/Cargo.toml, crates/aprender-train/src/train/mod.rs, crates/aprender-train/src/train/setfit/mod.rs, Makefile</files>
  <read_first>
    - crates/aprender-train/Cargo.toml ([features] block and [dependencies] — current state, dep name forms)
    - crates/aprender-contrastive-data/src/split.rs (the typestate house style: marker+trait+PhantomData+private fields, module doc explaining why identity is a type parameter)
    - crates/aprender-contrastive-data/src/select.rs (Selection — the input type SetFitRun holds)
    - crates/aprender-train/src/train/mod.rs (module registration style)
    - Makefile setfit-feature-matrix target (grep '^setfit-feature-matrix:' — the block to extend)
    - Root Cargo.toml [workspace.dependencies] (whether aprender-contrastive-data has a workspace entry; if not, use the path+version form matching the aprender dep line)
  </read_first>
  <action>
    Branch first, idempotently — wave 1 runs 03-01/03-02/03-03 in parallel and any of them may
    start before the others; every wave-1 plan must land on the ONE phase branch, never on
    gsd/phase-2-contract-gate:
    `git checkout gsd/phase-3-two-stage-trainer 2>/dev/null || git checkout -b gsd/phase-3-two-stage-trainer`
    (base: current HEAD of gsd/phase-2-contract-gate).

    Cargo.toml: add `aprender-contrastive-data` (optional, path ../aprender-contrastive-data +
    workspace version, matching the existing `aprender` dep line's form) and `aprender-rand =
    { workspace = true, optional = true }`. New feature:
    `setfit = ["aprender/setfit", "dep:aprender-contrastive-data", "dep:aprender-rand"]`.

    train/setfit/mod.rs (registered from train/mod.rs behind `#[cfg(feature = "setfit")]`):
    - Sealed `LifecycleState` trait (private supertrait module — external impls impossible) with
      associated type `Evidence`. Declare all four marker types now: `Prepared`, `EncoderTuned`,
      `HeadFitted`, `ArtifactReloadedAndVerified`. Implement LifecycleState ONLY for `Prepared`
      (Evidence = ()) in this plan; later plans implement the others as their evidence types land
      (interface-first: the markers are the contract, the impls arrive with the transitions).
    - `SetFitRun<S: LifecycleState>` with ALL fields private: encoder (SetFitMiniLm), selection
      (Selection), config (SetFitTrainConfig), evidence (S::Evidence), PhantomData<S>. No public
      constructor; the only door is `SetFitRun::<Prepared>::prepare(encoder, selection, config)
      -> Result<SetFitRun<Prepared>, SetFitTrainError>` which re-validates cross-field facts that
      need the Selection (pair budget vs selection capacity via contrastive-data's capacity fns).
    - `SetFitTrainError` enum skeleton with the variants this plan needs (Config(SetFitConfigError),
      Capacity variants); later plans extend it (error.rs-style extension is in-band, the enum is
      non_exhaustive-free but crate-internal growth is fine).
    Makefile: extend `setfit-feature-matrix` with two aprender-train legs:
    `cargo check -p aprender-train --no-default-features --features setfit` and
    `cargo check -p aprender-train --features setfit`. Run the target standalone first and capture
    rc directly (`> /tmp/sfm.log 2>&1; rc=$?` — never through a pipe).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo check -p aprender-train --no-default-features --features setfit && CARGO_INCREMENTAL=0 cargo check -p aprender-train --features setfit</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints gsd/phase-3-two-stage-trainer before this task's first commit
    - Both feature-closure legs exit 0
    - crates/aprender-train/Cargo.toml contains a `setfit = [` feature line listing `aprender/setfit`, `dep:aprender-contrastive-data`, `dep:aprender-rand`
    - train/setfit/mod.rs declares all four marker types and a sealed LifecycleState trait with `type Evidence`
    - SetFitRun's struct fields are all private (no `pub` field in the struct block) and state is PhantomData
    - Makefile setfit-feature-matrix block contains `-p aprender-train` (2 legs)
    - `cargo package -p aprender-train --no-verify` FAILS with a no-matching-package error naming aprender-contrastive-data — capture the message; this is the EXPECTED known-red (Pitfall 9), record it in the SUMMARY and must_haves caveat stands
  </acceptance_criteria>
  <done>Feature-closed module tree exists; states are types; the known-red is measured and recorded, not discovered later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetFitTrainConfig — 12 knobs, fail-closed, FALSIFY case tables</name>
  <files>crates/aprender-train/src/train/setfit/config.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/device.rs (typed-error Display citing contract; pure parser vs probe split; FALSIFY-headed test tables)
    - crates/aprender-contrastive-data/src/pairs.rs (PairConfig::new + resolve_budget two-stage validation; the binds-not-clamps doc)
    - crates/aprender-core/src/setfit/mod.rs (FreezeGroup, apply_freeze zero-match typed error — the freeze knob's existing validation)
  </read_first>
  <behavior>
    - Each of the 12 knobs has at least one rejected invalid form with a typed error naming the knob: encoder_lr (0, negative, NaN, inf), epochs (0), batch_size (0), warmup_proportion (<0, >1, NaN), grad_clip_max_norm (0, negative, NaN), max_length (0, above Phase 1's pinned tokenizer cap), pair config (delegated: ZeroBudget/BudgetExceedsHardCap surface through), freeze policy (structural validation; zero-match deferred to apply_freeze at tune time and documented), head regularization (lambda < 0, NaN), root_seed (any u64 accepted — no invalid form, test asserts acceptance), device (cuda:01 rejected via resolve_device grammar; explicit cuda on this host -> CudaNotAvailable), scheduler-implied cross-field (warmup fraction yielding warmup_steps > total_steps rejected once total steps are computable)
    - Valid reference-default construction succeeds: SetFitTrainConfig::reference_defaults(root_seed) with lr 2e-5, epochs 1, batch 16, warmup 0.1, clip 1.0
    - AdamW hyperparameters (0.9, 0.999, 1e-8, wd 0.0) are FIXED constants documented as reference-recipe values, not knobs (deviation from HF defaults would be a claims defect)
  </behavior>
  <action>
    Implement `SetFitTrainConfig` validated at construction (RESEARCH Open Question 2: one config
    type, NOT a fallible builder chain — a builder validates per-setter and misses cross-field
    rules). Pure per-knob validation functions separated from environment probes (device.rs
    split), then cross-field checks. `SetFitConfigError` variants carry named fields for expected
    AND observed values; Display cites contract id `setfit-train-lifecycle-v1` (authored in plan
    03-06) and requirement TRN-02. The head-regularization knob is native lambda per D-04:
    represent as an enum with a Lambda(f64) form and a SklearnDefaultC1 form that resolves to
    lambda = 1/(2n) at fit time (both documented; the 1/(2Cn) relation with sklearn's half inside
    r(W) is the plan 03-04/03-06 contract equation — do not restate it wrongly here, link to it).
    Pair knobs delegate to Phase 2's PairConfig/resolve_budget — never reimplement capacity
    logic. Freeze knob holds Vec<FreezeGroup>; structural checks here, zero-match enforcement
    stays with apply_freeze (document the split). Write the FALSIFY case tables first (tests
    named per device.rs's convention), watch representative rows go red, then implement. Finally, derive
    Serialize AND Deserialize on SetFitTrainConfig and every type it embeds (regularization
    enum, freeze policy, resolved device form), with #[serde(deny_unknown_fields)] on the
    deserializing structs, declaration-order fields, and no Duration/timestamp/HashMap-ordered
    field anywhere — this IS the canonical serialized form 03-08's artifact bundle embeds and
    hashes (serde/serde_json are already unconditional aprender-train deps).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit config_ > /tmp/p3-03t2.log 2>&1; rc=$?; tail -3 /tmp/p3-03t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit config_` exits 0 and runs >= 15 tests (the case tables)
    - config.rs contains `resolve_device` (reuse, not reimplementation) and contains no independent CUDA/string device parsing
    - Every SetFitConfigError variant carries at least one named field (no bare unit variants for knob rejections)
    - A NaN encoder_lr is rejected (test asserts the error names `encoder_lr` or equivalent knob identifier)
    - reference_defaults construction test passes with the exact values 2e-5 / 1 / 16 / 0.1 / 1.0 asserted
    - config.rs derives both Serialize and Deserialize and contains `deny_unknown_fields` (grep each >= 1 — the canonical config form 03-08's artifact bundle hashes is defined HERE, not improvised there)
  </acceptance_criteria>
  <done>All 12 knobs fail closed before any training state exists; defaults trace to the reference recipe.</done>
</task>

<task type="auto">
  <name>Task 3: WarmupLinearDecayLR + fixed-order reductions + epoch-shuffle domain</name>
  <files>crates/aprender-train/src/optim/scheduler/warmup_linear_decay.rs, crates/aprender-train/src/optim/scheduler/mod.rs, crates/aprender-train/src/train/setfit/reduce.rs, crates/aprender-train/src/train/setfit/epoch.rs</files>
  <read_first>
    - crates/aprender-train/src/optim/scheduler/warmup_cosine_decay.rs (the exact structural mirror — swap cosine for linear-to-zero)
    - crates/aprender-train/src/optim/scheduler/mod.rs (LRScheduler trait :23-30, registration style :1-21)
    - crates/aprender-contrastive-data/src/rng.rs (DomainKey/derive_key/draw/bounded, NonZeroU64 bound, domains table, golden discipline)
    - crates/aprender-core/src/optim/lbfgs.rs (stylistic kin for sequential indexed numeric loops)
  </read_first>
  <action>
    warmup_linear_decay.rs: mirror WarmupCosineDecayLR's structure exactly (same fields,
    constructor shape, apply<O: Optimizer>, zero-division guards for warmup_steps==0 and
    decay_steps==0) with the decay branch linear to lr_min (default 0.0). Register in
    scheduler/mod.rs (module decl + pub use + doc bullet). NOT feature-gated — it is a general
    scheduler. Unit tests: warmup ramp value at mid-warmup, peak at warmup end, linear descent
    value at mid-decay, 0 at total_steps, both guard branches.

    reduce.rs (feature-gated with the setfit module): `sum_in_index_order(&[f32]) -> f64`,
    `mean_in_index_order(&[f32]) -> f64`, `l2_norm_in_index_order(&[f32]) -> f64` — sequential
    index-order folds accumulating in f64 (one reduction tree, identical bit pattern at any
    thread count; f64 accumulation also sidesteps f32 cancellation in delta-norms for
    near-frozen parameters, exactly the regime D-10's epsilon discriminates). Never
    par_iter().sum() anywhere in train/setfit — this module is the only reduction door. Tests:
    golden values; a crafted cancellation series ([1e8, 1.0, -1e8] style) where f64 index-order
    accumulation preserves the 1.0 that f32 accumulation loses — assert the exact f64 result.

    epoch.rs: `epoch_pair_order(root_seed: u64, epoch: u32, n_pairs: u64) -> Vec<u64>` —
    Fisher-Yates over 0..n_pairs using the rng.rs construction under the trainer's OWN frozen tag
    `b"apr-setfit-train-v1\0"`, domain string "epoch-shuffle", stream_id = epoch, ordinal = draw
    counter; bounds via the multiply-shift `bounded` form with NonZeroU64 (modulo forbidden).
    Batching contract documented on the fn: batches are consecutive fixed-size windows of this
    order, last batch short, never resampled (RESEARCH Open Question 3). Tests: permutation
    validity (each index exactly once), replay equality, different-epoch difference, and one
    golden permutation for (seed=42, epoch=0, n=8) derived INDEPENDENTLY (Python from the
    construction spec recorded in the test doc comment).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit reduce_ > /tmp/p3-03t3a.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3a.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit epoch_ > /tmp/p3-03t3b.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3b.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib warmup_linear > /tmp/p3-03t3c.log 2>&1; rc=$?; tail -2 /tmp/p3-03t3c.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - All three scoped test commands exit 0
    - epoch.rs contains the byte literal `apr-setfit-train-v1` and does not contain `apr-contrastive-v1`
    - reduce.rs contains no `par_iter` and no `rayon` token
    - warmup_linear_decay.rs get_lr returns exactly 0.0 (or lr_min) at current_step == total_steps (test asserts)
    - The epoch golden test's doc comment contains its independent derivation
    - scheduler/mod.rs exports WarmupLinearDecayLR (grep `WarmupLinearDecayLR` in mod.rs >= 1)
  </acceptance_criteria>
  <done>The three determinism primitives exist, golden-pinned where random, with the reference schedule the repo was missing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user config → trainer | 12 untrusted knobs cross into training; V5 input validation surface |
| feature graph → build | setfit feature leaking GPU/tui deps breaks the CPU-only contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Tampering (V5) | invalid knob values reaching the loop | mitigate | fail-closed typed validation at construction; FALSIFY case tables per knob; no silent clamping (binds-not-clamps) |
| T-3-09 | Spoofing | direct construction of SetFitRun states | mitigate | sealed trait + private fields + PhantomData; trybuild proof lands in 03-09 |
| T-3-10 | Repudiation | epoch order depending on ambient RNG state | mitigate | Philox pure-function-of-(seed,epoch) permutation, own frozen tag, independent golden |
| T-3-11 | DoS | zero-division in scheduler guards | mitigate | warmup_steps==0 / decay_steps==0 branches copied from the cosine analog, tested |
| T-3-SC | Tampering | package installs | accept | no external packages; new dep edges are in-repo crates (audit table verified acyclic) |
</threat_model>

<verification>
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0
- `cargo test -p aprender-train --lib --features setfit config_` / `reduce_` / `epoch_` all exit 0
- `make setfit-feature-matrix` run standalone with rc captured directly, exits 0
</verification>

<success_criteria>
- setfit feature dependency-closed in aprender-train; known-red package state measured and recorded
- 12 knobs fail closed with typed, knob-naming errors; reference defaults pinned
- Scheduler/reduce/epoch primitives green with independent goldens
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-03-SUMMARY.md` when done.
Record: the measured cargo-package known-red message, the epoch golden derivation, and any
cross-field validation rules that needed Selection-time deferral.
</output>
