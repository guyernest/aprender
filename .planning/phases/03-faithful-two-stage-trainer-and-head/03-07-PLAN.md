---
phase: 03-faithful-two-stage-trainer-and-head
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - crates/aprender-train/src/train/setfit/head_input.rs
  - crates/aprender-train/src/train/setfit/negative.rs
  - crates/aprender-train/src/train/setfit/mod.rs
autonomous: true
requirements: [TRN-05]

must_haves:
  truths:
    - "fit_head consumes SetFitRun<EncoderTuned> and reads ONLY the dataset and Selection it already holds — its signature has no pair parameter, so pair multiplicity is inexpressible, not rejected"
    - "Encode-exactly-once is proven by an instrumented encode ledger: the ordered list of row IDs actually handed to the encoder is recorded, and the multiset of that list equals the Selection's ID set with multiplicity exactly one for every ID. Row COUNT alone cannot see a duplicated row paired with an omitted one"
    - "Head encoding runs inside autograd::no_grad with dropout off and eval mode, and the mechanism is PROVEN engaged: graph_tape_len() is unchanged across the encode and every trainable parameter's grad() is unchanged"
    - "The effective lambda resolves ONCE against the unique-row count and the SAME value is used by both the trusted fit and the adversarial control, so the control isolates multiplicity rather than confounding it with a lambda shift"
    - "HeadFitted's evidence includes both the passed encoder evidence and the HeadFitReport; two identical fit_head calls produce bitwise-identical stored f32 weights"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/head_input.rs"
      provides: "Selection -> encode-once pinned-batch embedding matrix with an encode ledger (D-08)"
    - path: "crates/aprender-train/src/train/setfit/negative.rs"
      provides: "In-band pair-weighted fitter that must FAIL its gate in every cargo test"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/mod.rs"
      to: "crates/aprender-core/src/classification/multinomial.rs"
      via: "fit_head calls MultinomialLogisticRegression on the encode-once matrix"
      pattern: "MultinomialLogisticRegression"
    - from: "crates/aprender-train/src/train/setfit/head_input.rs"
      to: "aprender-contrastive-data Selection"
      via: "examples() iteration in deterministic order, with each id appended to the encode ledger"
      pattern: "encode_ledger"
  caveats:
    - "Head lambda default resolves at fit time: SklearnEquivalentC{c:1.0} -> lambda = 1/(2*1.0*n) with n = UNIQUE selected rows — NEVER the pair count, and NEVER the duplicated row count in the adversarial path. A test pins n = selection length."
    - "Texts come from the run's PreparedDataset (SelectedExample carries no text — select.rs:81). head_dataset therefore takes the dataset as well as the Selection."
---

<objective>
Build stage two's structural enforcement: the encode-once head input (each unique selected row
encoded exactly once, eval/no-gradient mode, dropout off, pinned batches, with an encode ledger that
makes exactly-once a measured property rather than an inferred one) and the fit_head transition
whose signature makes pair multiplicity inexpressible (D-08, TRN-05), backed by the in-band
pair-weighted fitter that proves the property is load-bearing.

Purpose: TRN-05 and ROADMAP criterion 3 — pair multiplicity cannot reweight the head data, by
construction, with an adversarial witness red in every cargo test.
Output: head_input.rs with its ledger, fit_head -> SetFitRun<HeadFitted>, negative.rs set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts:
- Selection surface (verified): `examples() -> &[SelectedExample]` (select.rs:306, already deduplicated, deterministic order), `ordered_ids()` (:311), `len()` (:316), `semantic_hash()` (:327), `ledger_hash()` (:333). `SelectedExample { id: String, label: usize, exact_hash, normalized_hash }` (:81-89) — NO text field. `SelectedId`'s constructor is private — proof of membership.
- Text therefore comes from the run's `PreparedDataset<Canonical>`: `train() -> &Split<Train>` (prepared.rs:290) and `Split::rows() -> &[LabeledExample]` (split.rs:175). `head_dataset` takes both the dataset and the Selection.
- `label_names() -> &[String]` (prepared.rs:285) is the CANONICAL declared label map. Label order comes from THERE (review fix), not from incidental BTreeMap or class-iteration order.
- Encode path: `set_training(false)` (setfit/mod.rs:293) before encoding; keyed dropout is identity in eval per 03-02.
- **No-grad is real and available**: `autograd::no_grad(f)` (autograd/mod.rs:76) flips a thread-local honored by the ops layer (`is_grad_enabled()` checked in activation.rs, ops/mod.rs, embedding.rs, normalize.rs, pooling.rs, similarity.rs). `Tensor::detach()` (tensor.rs:280) returns an owned, graph-free copy. `graph_tape_len()` was added by 03-05. VERIFIED by reading — the earlier "use no-grad if available; otherwise record its absence" is resolved: it IS available, so it is used and proven.
- Pinned batch composition (D-08): consecutive windows of config.batch_size over Selection order, last batch short. Phase 1 proved padding invariance only WITHIN tolerance — pinning composition is what makes head embeddings bitwise reproducible.
- The in-band negative discipline (negative_leaky.rs template): the poison must be built from whatever surface remains expressible; if it can only be built from trusted types, the typestate is broken and THAT is the finding.
- HeadFitted evidence: (PassedEvidence, HeadFitReport) — the absent-field pattern continues; implement LifecycleState for HeadFitted here.

**Review fix 1 — row count plus a bitwise re-encode does not prove exactly-once.** An
implementation that encodes row 3 twice and skips row 5 keeps the count and stays deterministic. The
encode ledger below is the missing evidence.

**Review fix 2 — "use no-grad if available; otherwise record its absence" does not meet TRN-05.**
It is available. It is used, and the mechanism is proven engaged.

**Review fix 3 — the uniform-multiplicity control was confounded.** Resolving
`SklearnEquivalentC` against the DUPLICATED row count changes lambda, so the two heads differ even
when multiplicity is uniform, and the control proves nothing. Lambda is now resolved once from the
unique-row count and passed to both fits.

**Review fix 4 — label order was incidental.** It comes from `PreparedDataset::label_names()`.

**Review fix 5 — the plan referred to a "quartet" but specified three elements.** The set is
negative + control + mirror; the wording is corrected.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: head_input.rs — encode-once pinned-batch matrix with an encode ledger</name>
  <files>crates/aprender-train/src/train/setfit/head_input.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/select.rs (Selection/SelectedExample surface — examples() :306, and the absence of a text field)
    - crates/aprender-contrastive-data/src/prepared.rs (train() :290, label_names() :285 — the canonical label order)
    - crates/aprender-contrastive-data/src/split.rs (rows() :175 — where the text lives)
    - crates/aprender-core/src/setfit/mod.rs (set_training :293, training() :277, encode_texts :287)
    - crates/aprender-core/src/autograd/mod.rs (no_grad :76, graph_tape_len as added by 03-05)
    - crates/aprender-core/src/autograd/tensor.rs (detach :280, grad :231)
    - crates/aprender-train/src/train/setfit/test_fixtures.rs (the fixture from 03-05)
  </read_first>
  <behavior>
    - `head_dataset(encoder, dataset, selection, batch_size)` returns a HeadDataset with exactly `selection.examples().len()` rows, in Selection order, with class indices and an ordered label Vec<String> taken from `dataset.label_names()`
    - ENCODE LEDGER (the review fix): HeadDataset carries `encode_ledger: Vec<String>` — the row IDs in the exact order they were handed to the encoder, appended at the call site, one entry per row per encode call. Tests assert: (i) `encode_ledger.len() == selection.len()`; (ii) sorting the ledger equals sorting `selection.ordered_ids()` — every selected ID appears, EXACTLY ONCE, and no unselected ID appears; (iii) the ledger order equals Selection order. A duplicate-plus-omission defect fails (ii) even though the row count is unchanged
    - ENCODE-CALL COUNT: HeadDataset carries `encode_call_count: usize` and a test asserts it equals `ceil(n / batch_size)` — one encoder invocation per pinned window, no retries, no per-row fallback path
    - NO-GRAD PROVEN ENGAGED: a test snapshots `graph_tape_len()` and every trainable parameter's `grad()` before the encode and asserts both are unchanged after — the encode built no graph and touched no gradient. A second test asserts the returned embedding tensor reports `requires_grad_enabled() == false`
    - EVAL MODE: during encoding the encoder reports `training() == false` (assert via the mode surface)
    - determinism: calling head_dataset twice yields bitwise-identical matrices AND identical ledgers
    - a batch_size larger than the row count still encodes every row exactly once (single short batch; ledger still passes (i)-(iii))
    - DISTINCTNESS: rows built from distinct fixture texts produce distinct embeddings (a defect that encoded one row N times would otherwise satisfy every count-based check with identical rows)
  </behavior>
  <action>
    Write the behavior tests first. Implement
    `pub(crate) fn head_dataset(encoder: &mut SetFitMiniLm, dataset: &PreparedDataset<Canonical>,
    selection: &Selection, batch_size: u32) -> Result<HeadDataset, SetFitTrainError>`:

      - `set_training(false)` first; assert `encoder.training() == false` before any encode.
      - Build a `BTreeMap<&str, &str>` id -> text index once from `dataset.train().rows()`; a
        selected id with no matching row is `SetFitTrainError::SelectionRowMissing { id }` (not a
        panic, not a silent skip — a skip is exactly the defect the ledger exists to catch).
      - Iterate `selection.examples()` in order; batch into consecutive windows of batch_size;
        for each window, push every row's id onto `encode_ledger` IN ORDER, increment
        `encode_call_count`, then call `encode_texts` for that window ONCE, inside
        `autograd::no_grad(|| ...)`.
      - `detach()` the resulting embedding tensor into owned f32 rows before storing, so no graph
        node survives into HeadDataset even if a future encoder change starts recording under
        no_grad.
      - Collect embeddings row-aligned with the selection.
      - Label order: `dataset.label_names().to_vec()` — the canonical declared map (review fix 4).
        Assert `label_names().len()` covers every class index present in the selection; a class
        index outside that range is a typed error.
      - Leave the encoder in eval mode afterwards and document that transitions own the encoder.

    HeadDataset carries: embedding rows (f32), class index per row, ordered label Vec<String>,
    `n` (row count), `encode_ledger`, `encode_call_count`.

    Exactly-once is enforced by construction (one pass, one encode per window) and PROVEN by the
    ledger multiset assertion plus the distinctness and call-count assertions — not by the row count
    alone, which the review correctly identified as insufficient.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit head_input_ > /tmp/p3-07t1.log 2>&1; rc=$?; tail -3 /tmp/p3-07t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit head_input_` exits 0 with >= 8 tests
    - A test asserts the SORTED encode_ledger equals the SORTED selection.ordered_ids() (the multiset assertion — this is the exactly-once proof, and a count-only check does not satisfy this criterion)
    - A test asserts `encode_call_count == ceil(n / batch_size)`
    - A test asserts `graph_tape_len()` and every trainable parameter's `grad()` are unchanged across the encode, and another asserts the embedding tensor's `requires_grad_enabled()` is false
    - head_input.rs contains `no_grad` and `detach` (source assertion — the mechanism, not a comment about it)
    - `grep -c 'Pair' crates/aprender-train/src/train/setfit/head_input.rs` returns 0 outside comments (no pair-shaped parameter anywhere in the module's signatures)
    - A test asserts encoder `training()` is false at encode time
    - Label order is sourced from `label_names()` (source assertion) and a test pins the resulting Vec<String> against the fixture's declared labels
  </acceptance_criteria>
  <done>The head's input is an encode-once, order-pinned, eval-mode, graph-free matrix, and exactly-once is a measured multiset property rather than an inference from a row count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fit_head transition -> SetFitRun&lt;HeadFitted&gt;</name>
  <files>crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/src/train/setfit/head_input.rs</files>
  <read_first>
    - crates/aprender-core/src/classification/multinomial.rs (fit signature, regularization enum, HeadFitReport — as landed by 03-04)
    - crates/aprender-train/src/train/setfit/mod.rs (SetFitRun fields incl. dataset, EncoderTuned evidence from 03-06)
    - crates/aprender-train/src/train/setfit/config.rs (head regularization knob forms)
  </read_first>
  <behavior>
    - fixture pipeline: prepare -> tune_encoder -> fit_head returns Ok(SetFitRun<HeadFitted>); probabilities on the fixture rows are finite and sum to 1 within 1e-6
    - two identical pipelines produce bitwise-identical stored f32 head weights
    - LAMBDA RESOLUTION (review fix 3): `resolve_lambda(&Regularization, unique_row_count) -> f64` is a single named function. With SklearnEquivalentC{c:1.0} and a 24-row selection the effective lambda equals exactly 1.0/48.0. A test pins n = `selection.len()` and a second asserts that varying the PAIR BUDGET (and therefore the pair count) leaves the resolved lambda and the fitted weights bitwise unchanged — the review's suggested stronger form of the same check
    - a head fit that cannot converge (max_iter forced tiny via a test-only configuration door) surfaces the typed HeadFitError inside SetFitTrainError — never a panic or silent acceptance
    - fit_head takes zero non-self parameters
  </behavior>
  <action>
    Write tests first. Implement `impl SetFitRun<EncoderTuned> { pub fn fit_head(self) ->
    Result<SetFitRun<HeadFitted>, SetFitTrainError> }`: builds head_dataset from the run's OWN
    dataset, selection and config batch size (the signature takes nothing — everything comes from
    the consumed state; this is what makes multiplicity inexpressible); resolves the head
    regularization through `resolve_lambda(&self.config.head_regularization,
    head_dataset.n)` where `head_dataset.n` is the UNIQUE row count; fits
    MultinomialLogisticRegression; wraps HeadFitError into SetFitTrainError with a dedicated variant
    preserving the inner typed detail.

    `resolve_lambda` is `pub(crate)` precisely so Task 3's adversary can call it with the SAME
    unique-row count — the control must differ from the trusted fit in multiplicity ONLY.

    Implement LifecycleState for HeadFitted with Evidence = (PassedEvidence, HeadFitReport) —
    absent-field pattern, no Option. Expose read-only accessors on HeadFitted needed downstream
    (predict on probe texts for 03-08's verify; ordered labels; head report; the encode ledger and
    encode_call_count, so 03-09 can assert exactly-once across processes too) without exposing
    mutation.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit fit_head > /tmp/p3-07t2.log 2>&1; rc=$?; tail -3 /tmp/p3-07t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit fit_head` exits 0
    - `pub fn fit_head(self)` takes zero non-self parameters (source assertion)
    - `grep -c 'fn resolve_lambda' crates/aprender-train/src/train/setfit/` returns 1 (one implementation, shared with the adversary)
    - The lambda test asserts the exact value 1.0/48.0 for the 24-row C=1 case, and the pair-budget-invariance test asserts bitwise-identical head weights across two different pair budgets
    - `grep -c 'Option<(PassedEvidence' crates/aprender-train/src/train/setfit/` returns 0
    - Two-pipeline bitwise weight-equality test passes
    - HeadFitted exposes read-only accessors for the encode ledger and encode_call_count (grep >= 1 each), none returning &mut
  </acceptance_criteria>
  <done>Stage two is a consuming transition with no pair-shaped input anywhere in its signature, and lambda provably tracks unique rows rather than pairs.</done>
</task>

<task type="auto">
  <name>Task 3: negative.rs — the pair-weighted fitter red in every cargo test</name>
  <files>crates/aprender-train/src/train/setfit/negative.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/tests/negative_leaky.rs (the three-element discipline verbatim: negative names the offender, control passes with the poison removed, mirror unchanged)
    - crates/aprender-train/src/train/setfit/head_input.rs and mod.rs (what surface remains expressible to attack from; resolve_lambda's signature)
  </read_first>
  <action>
    Build the test-only adversary (`#[cfg(test)]` module or `#[doc(hidden)]` test-support in-crate):
    a pair-weighted fitter that constructs head data with row multiplicity following pair endpoint
    frequency (replay the fixture's pair stream, count endpoint occurrences, duplicate embedding rows
    accordingly) and fits the same MultinomialLogisticRegression.

    CRITICAL (review fix 3): BOTH fits — the exactly-once fit and the pair-weighted fit — use the
    lambda produced by `resolve_lambda(&reg, unique_row_count)`. The adversary must NOT re-resolve
    lambda against its duplicated row count; doing so changes the objective and the control becomes
    meaningless. Encode this as a source-assertion in the test module doc and as an explicit
    parameter: the adversary's fit function takes `lambda: f64`, it never takes a `Regularization`.

    The set (three elements: negative, control, mirror — there is no fourth), running in every
    cargo test:
    - negative: on a multiplicity-SKEWED fixture (unequal per-class pair counts — construct via
      unequal class sizes so endpoint frequencies diverge from uniqueness), the pair-weighted
      coefficients differ from the exactly-once coefficients beyond a stated tolerance, and the
      assertion message names the skewed class and the observed multiplicity ratio;
    - control: on a multiplicity-UNIFORM fixture the two fits agree within tolerance, WITH THE SAME
      LAMBDA — proving the difference in the negative comes from multiplicity and not from a lambda
      shift or an unrelated defect;
    - mirror: the exactly-once path's weights are byte-identical with and without the adversary
      module compiled in.

    IMPORTANT per the template's doc discipline: the adversary must be built from whatever surface is
    expressible (embedding rows + the public head fit — NOT through fit_head, which cannot express
    it). If the adversary CAN be routed through fit_head or any SetFitRun door, the typestate is
    broken — stop, and report that as the finding instead of shipping the negative. Document in
    negative.rs's module doc WHY the attack cannot be represented through the trusted surface (the
    D-08 claim, made falsifiable), and WHY both fits share one lambda.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit pair_weight > /tmp/p3-07t3.log 2>&1; rc=$?; tail -3 /tmp/p3-07t3.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo clippy -p aprender-train --lib --features setfit -- -D warnings > /tmp/p3-07t3b.log 2>&1; rc=$?; tail -3 /tmp/p3-07t3b.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit pair_weight` exits 0 running exactly 3 tests (negative, control, mirror)
    - The adversary's fit function takes `lambda: f64` and does NOT take a `Regularization` value (source assertion — this is what keeps the control unconfounded)
    - negative.rs module doc contains an explanation of why fit_head cannot express the attack (contains the token `fit_head`) and why both fits share one lambda
    - The negative's assertion message names the skewed class AND the multiplicity ratio
    - The control asserts agreement within tolerance and its SUMMARY entry records the observed coefficient distance in both the skewed and uniform cases (a control that is trivially green because both fits are identical objects proves nothing)
    - `cargo clippy -p aprender-train --lib --features setfit -- -D warnings` exits 0 (W-08)
    - negative.rs is not reachable from non-test builds: `cargo check -p aprender-train --features setfit` emits no code from it (cfg(test)) OR it is #[doc(hidden)] with a doc comment stating test-support-only — one of the two, stated in the SUMMARY
  </acceptance_criteria>
  <done>TRN-05's structural claim has a live adversarial witness: multiplicity WOULD reweight the head at identical lambda, and the trusted surface cannot express it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pair stream → head dataset | THE boundary TRN-05 exists to seal (multiplicity reweighting) |
| encoder mode → head embeddings | train-mode dropout or a live graph leaking into head inputs |
| adversarial control → the claim it is meant to isolate | a confounded control that silently proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-23 | Tampering | pair multiplicity reweighting the head dataset | mitigate | fit_head signature has no pair-shaped input (inexpressible); in-band pair-weighted adversary red in every cargo test at a shared lambda |
| T-3-24 | Repudiation | dropout-on or graph-connected encoding making head data irreproducible | mitigate | eval-mode assertion, `no_grad` + `detach`, graph_tape_len and grad-unchanged assertions, bitwise re-encode on pinned batches |
| T-3-25 | Tampering (V5) | lambda resolved against pair count or duplicated row count instead of unique rows | mitigate | single `resolve_lambda(reg, unique_rows)`; exact 1/48 pin; pair-budget-invariance test; the adversary takes a plain f64 lambda |
| T-3-44 | Tampering | a duplicated row paired with an omitted row, invisible to a row-count check | mitigate | encode ledger multiset assertion against `selection.ordered_ids()`, plus encode_call_count and embedding distinctness |
| T-3-58 | Spoofing | label order taken from incidental map/class iteration rather than the declared map | mitigate | order sourced from `PreparedDataset::label_names()`; pinned by test against the fixture's declared labels |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit head_input_` / `fit_head` / `pair_weight` all exit 0
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0
</verification>

<success_criteria>
- TRN-05 structural: encode-once proven by ledger multiset equality (not by row count), multiplicity inexpressible through the trusted surface
- No-grad and eval mode proven engaged, not asserted
- HeadFitted mintable only from EncoderTuned; deterministic weights; lambda tracks unique rows
- Adversarial set red/green as designed in every cargo test, with an unconfounded control
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-07-SUMMARY.md` when done.
Record: the no-grad mechanism evidence (tape length and grad snapshots before/after), the label-order
rule and the pinned label vector, the adversary's expressible-surface analysis, and the observed
coefficient distances in both the skewed and uniform control cases.
</output>
