---
phase: 03-faithful-two-stage-trainer-and-head
plan: 07
type: execute
wave: 4
depends_on: ["03-06"]
files_modified:
  - crates/aprender-train/src/train/setfit/head_input.rs
  - crates/aprender-train/src/train/setfit/negative.rs
  - crates/aprender-train/src/train/setfit/mod.rs
autonomous: true
requirements: [TRN-05]

must_haves:
  truths:
    - "fit_head consumes SetFitRun<EncoderTuned> and reads ONLY the Selection it already holds — its signature has no pair parameter, so pair multiplicity is inexpressible, not rejected"
    - "Each unique selected row is encoded exactly once, in eval mode with dropout disabled, in pinned consecutive Selection-order batches; re-encoding yields a bitwise-identical embedding matrix"
    - "The test-only pair-weighted fitter produces measurably different head coefficients on a multiplicity-skewed fixture, and that difference is detected by its gate in every cargo test (negative + control + mirror)"
    - "HeadFitted's evidence includes both the passed encoder evidence and the HeadFitReport; two identical fit_head calls produce bitwise-identical stored f32 weights"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/head_input.rs"
      provides: "Selection -> encode-once pinned-batch embedding matrix (D-08)"
    - path: "crates/aprender-train/src/train/setfit/negative.rs"
      provides: "In-band pair-weighted fitter that must FAIL its gate in every cargo test"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/mod.rs"
      to: "crates/aprender-core/src/classification/multinomial.rs"
      via: "fit_head calls MultinomialLogisticRegression on the encode-once matrix"
      pattern: "MultinomialLogisticRegression"
    - from: "crates/aprender-train/src/train/setfit/head_input.rs"
      to: "aprender-contrastive-data Selection"
      via: "examples() iteration in deterministic order"
      pattern: "examples\\(\\)"
  caveats:
    - "Head lambda default resolves at fit time: SklearnEquivalentC{c:1.0} -> lambda = 1/(2*1.0*n) with n = unique selected rows — NEVER the pair count. A test pins n = selection length."
---

<objective>
Build stage two's structural enforcement: the encode-once head input (each unique selected row
encoded exactly once, eval/no-gradient mode, dropout off, pinned batches) and the
fit_head transition whose signature makes pair multiplicity inexpressible (D-08, TRN-05), backed
by the in-band pair-weighted fitter that proves the property is load-bearing.

Purpose: TRN-05 and ROADMAP criterion 3 — pair multiplicity cannot reweight the head data, by
construction, with an adversarial witness red in every cargo test.
Output: head_input.rs, fit_head -> SetFitRun<HeadFitted>, negative.rs quartet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key facts:
- Selection surface (verified): examples() -> &[SelectedExample] (already deduplicated, deterministic order), ordered_ids(), ids_in_class, semantic_hash(). SelectedId's constructor is private — proof of membership.
- Encode path: set_training(false) before encoding (per-site training flags exist from Phase 1; keyed dropout is identity in eval per 03-02). "No-gradient" = eval mode + no backward call; read the encoder's eval path for whether an explicit no-grad/detach mechanism exists and use it if present (record which in the SUMMARY).
- Pinned batch composition (D-08): consecutive windows of config.batch_size over Selection order, last batch short. Phase 1 proved padding invariance only WITHIN tolerance — pinning composition is what makes head embeddings bitwise reproducible.
- The in-band negative discipline (negative_leaky.rs template): the poison must be built from whatever surface remains expressible; if it can only be built from trusted types, the typestate is broken and THAT is the finding. negative.rs is #[cfg(test)] or #[doc(hidden)] test-support — it runs in every cargo test.
- HeadFitted evidence: (PassedEvidence, HeadFitReport) — the absent-field pattern continues; implement LifecycleState for HeadFitted here.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: head_input.rs — encode-once pinned-batch matrix</name>
  <files>crates/aprender-train/src/train/setfit/head_input.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/select.rs (Selection/SelectedExample surface — the ONLY input type)
    - crates/aprender-core/src/setfit/mod.rs (set_training, encode_texts; eval/no-grad mechanics)
    - crates/aprender-train/src/train/setfit/test_fixtures.rs (the fixture from 03-05)
  </read_first>
  <behavior>
    - head_dataset(encoder, selection, batch_size) returns a matrix with exactly selection.examples().len() rows, in Selection order, with class indices and ordered label strings derived from the selection's label set
    - Calling it twice yields bitwise-identical matrices (pinned batches + eval mode + keyed-dropout-off)
    - During encoding, every dropout site reports training == false (assert via the encoder's mode surface)
    - A selection with a batch_size larger than the row count still encodes every row exactly once (single short batch)
  </behavior>
  <action>
    Write the behavior tests first. Implement `pub(crate) fn head_dataset(...) -> Result<HeadDataset,
    SetFitTrainError>`: set_training(false); iterate selection.examples() in order; batch in
    consecutive windows of batch_size; encode each batch once; collect embeddings row-aligned
    with the selection; restore the encoder's prior mode afterward only if the surrounding code
    expects it (transitions own the encoder — prefer leaving eval mode and documenting it).
    HeadDataset carries: embedding rows (f32), class index per row, ordered label Vec<String>
    (deterministic order: label order as declared by the selection's class structure — document
    and test the rule), and n = row count. Exactly-once is enforced by construction (one pass,
    one encode per window) and PROVEN by the bitwise re-encode test plus a row-count assertion.
    Use an explicit no-grad mechanism if the autograd surface provides one (check; record).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit head_input_ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit head_input_` exits 0
    - A test asserts rows == selection.examples().len() and a second identical call is bitwise-equal
    - head_input.rs's public-in-crate surface accepts &Selection and has NO parameter typed as a pair/stream type (grep `Pair` in head_input.rs signatures = 0 matches outside comments)
    - A test asserts encoder training mode is false at encode time
  </acceptance_criteria>
  <done>The head's input is an encode-once, order-pinned, eval-mode matrix — reproducible bitwise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fit_head transition -> SetFitRun&lt;HeadFitted&gt;</name>
  <files>crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/src/train/setfit/head_input.rs</files>
  <read_first>
    - crates/aprender-core/src/classification/multinomial.rs (fit signature, regularization enum, HeadFitReport — as landed by 03-04)
    - crates/aprender-train/src/train/setfit/mod.rs (SetFitRun fields, EncoderTuned evidence from 03-06)
    - crates/aprender-train/src/train/setfit/config.rs (head regularization knob forms)
  </read_first>
  <behavior>
    - fixture run: prepare -> tune_encoder -> fit_head returns Ok(SetFitRun<HeadFitted>); probabilities on the fixture rows are finite and sum to 1 within 1e-6
    - two identical pipelines produce bitwise-identical stored f32 head weights
    - lambda resolution: with SklearnEquivalentC{c:1.0} and a 24-row selection, the effective lambda equals 1/48 exactly (test pins n = selection length, NOT pair count)
    - a head fit that cannot converge (max_iter forced tiny via a test-only configuration door) surfaces the typed HeadFitError inside SetFitTrainError — never a panic or silent acceptance
  </behavior>
  <action>
    Write tests first. Implement `impl SetFitRun<EncoderTuned> { pub fn fit_head(self) ->
    Result<SetFitRun<HeadFitted>, SetFitTrainError> }`: builds head_dataset from the run's OWN
    selection and config batch size (the signature takes nothing — everything comes from the
    consumed state; this is what makes multiplicity inexpressible); resolves the config's head
    regularization (SklearnEquivalentC resolves against n = dataset rows); fits
    MultinomialLogisticRegression; wraps HeadFitError into SetFitTrainError with a dedicated
    variant preserving the inner typed detail. Implement LifecycleState for HeadFitted with
    Evidence = (PassedEvidence, HeadFitReport) — absent-field pattern, no Option. Expose
    read-only accessors on HeadFitted needed downstream (predict on probe texts for 03-08's
    verify; ordered labels; head report) without exposing mutation.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit fit_head 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit fit_head` exits 0
    - `pub fn fit_head(self)` takes zero non-self parameters (source assertion)
    - The lambda test asserts the exact value 1.0/48.0 for the 24-row C=1 case
    - HeadFitted's evidence tuple is non-Option (grep `Option<(PassedEvidence` = 0)
    - Two-pipeline bitwise weight-equality test passes
  </acceptance_criteria>
  <done>Stage two is a consuming transition with no pair-shaped input anywhere in its signature.</done>
</task>

<task type="auto">
  <name>Task 3: negative.rs — the pair-weighted fitter red in every cargo test</name>
  <files>crates/aprender-train/src/train/setfit/negative.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/tests/negative_leaky.rs (the three-element discipline verbatim: negative names the offender, control passes with poison removed, mirror unchanged)
    - crates/aprender-train/src/train/setfit/head_input.rs and mod.rs (what surface remains expressible to attack from)
  </read_first>
  <action>
    Build the test-only adversary (#[cfg(test)] module or #[doc(hidden)] test-support in-crate):
    a pair-weighted fitter that constructs head data with row multiplicity following pair
    endpoint frequency (replay the fixture's pair stream, count endpoint occurrences, duplicate
    embedding rows accordingly) and fits the same MultinomialLogisticRegression. The gate
    (running in every cargo test):
    - negative: on a multiplicity-SKEWED fixture (unequal per-class pair counts — construct via
      unequal class sizes so endpoint frequencies diverge from uniqueness), the pair-weighted
      coefficients differ from the exactly-once coefficients beyond a stated tolerance, and the
      assertion message names the skewed class;
    - control: on a multiplicity-UNIFORM fixture the two fits agree within tolerance (proves the
      difference in the negative comes from multiplicity, not from some unrelated defect);
    - mirror: the exactly-once path's weights are byte-identical with and without the adversary
      module compiled in.
    IMPORTANT per the template's doc discipline: the adversary must be built from whatever
    surface is expressible (embedding rows + the public head fit — NOT through fit_head, which
    cannot express it). If the adversary CAN be routed through fit_head or any SetFitRun door,
    the typestate is broken — stop, and report that as the finding instead of shipping the
    negative. Document in negative.rs's module doc WHY the attack cannot be represented through
    the trusted surface (the D-08 claim, made falsifiable).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit pair_weight 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --lib --features setfit pair_weight` exits 0 running >= 3 tests (negative, control, mirror)
    - negative.rs module doc contains an explanation of why fit_head cannot express the attack (contains the token `fit_head`)
    - The negative's assertion message names the skewed class or the multiplicity ratio
    - negative.rs is not reachable from non-test builds: `cargo check -p aprender-train --features setfit` emits no code from it (cfg(test)) OR it is #[doc(hidden)] with a doc comment stating test-support-only — one of the two, stated in the SUMMARY
  </acceptance_criteria>
  <done>TRN-05's structural claim has a live adversarial witness: multiplicity WOULD reweight the head, and the trusted surface cannot express it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pair stream → head dataset | THE boundary TRN-05 exists to seal (multiplicity reweighting) |
| encoder mode → head embeddings | train-mode dropout leaking into head inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-23 | Tampering | pair multiplicity reweighting the head dataset | mitigate | fit_head signature has no pair-shaped input (inexpressible); in-band pair-weighted adversary red in every cargo test |
| T-3-24 | Repudiation | dropout-on encoding making head data irreproducible | mitigate | eval-mode assertion test; bitwise re-encode test on pinned batches |
| T-3-25 | Tampering (V5) | lambda resolved against pair count instead of unique rows | mitigate | exact 1/48 pin test with n = selection length |
| T-3-SC | Tampering | package installs | accept | none this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit head_input_` / `fit_head` / `pair_weight` all exit 0
- `cargo check -p aprender-train --no-default-features --features setfit` exits 0
</verification>

<success_criteria>
- TRN-05 structural: encode-once proven bitwise, multiplicity inexpressible through the trusted surface
- HeadFitted mintable only from EncoderTuned; deterministic weights
- Adversarial quartet red/green as designed in every cargo test
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-07-SUMMARY.md` when done.
Record: the no-grad mechanism used (or its absence), the label-order rule chosen, and the
adversary's expressible-surface analysis.
</output>
