---
phase: 03-faithful-two-stage-trainer-and-head
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/aprender-core/src/classification/mod.rs
  - crates/aprender-core/src/classification/multinomial.rs
  - crates/aprender-core/src/classification/tests_multinomial_contract.rs
  - scripts/gen_multinomial_sklearn_fixture.py
  - contracts/multinomial-head-v1.yaml
  - contracts/aprender/binding.yaml
  - Makefile
autonomous: true
requirements: [TRN-04]

must_haves:
  truths:
    - "For any ordered K >= 2 label set, fit returns explicit convergence or a typed failure — never Result<(), String>, never silent non-convergence (sklearn warns; we fail, a declared deviation)"
    - "Predicted probabilities are finite, sum to one within 1e-6 per row, and argmax ties break to the lowest label index"
    - "The contracted sklearn relation pins the half inside r(W): lambda = 1/(2*C*n) against aprender's mean-NLL + lambda*||W||^2_F with unpenalized intercept, and a falsification test at n=24 separates the factor-2 error beyond tolerance"
    - "The head fits in f64 (via 03-01's widened L-BFGS) and stores f32; the hashed HeadFitReport carries no Duration or wall-clock field"
  artifacts:
    - path: "crates/aprender-core/src/classification/multinomial.rs"
      provides: "MultinomialLogisticRegression + HeadFitError + HeadFitReport"
      contains: "pub struct MultinomialLogisticRegression"
    - path: "contracts/multinomial-head-v1.yaml"
      provides: "Contract with both objective conventions fully expanded + falsification tests"
    - path: "scripts/gen_multinomial_sklearn_fixture.py"
      provides: "Pinned-env fixture generator asserting n_iter_ < max_iter, recording versions and get_params()"
  key_links:
    - from: "crates/aprender-core/src/classification/multinomial.rs"
      to: "crates/aprender-core/src/optim/lbfgs.rs"
      via: "f64 L-BFGS entry point from plan 03-01"
      pattern: "Lbfgs.*64|minimize"
    - from: "Makefile"
      to: "contracts/multinomial-head-v1.yaml"
      via: "$(CONTRACTS) line + PHASE3_CONTRACTS + blocking contract-audit-phase3 in tier3"
      pattern: "PHASE3_CONTRACTS"
  caveats:
    - "The binary LogisticRegression at classification/mod.rs:114 and its apr-stochastic-lr-v1 contract are LEFT UNTOUCHED (D-01). Any diff to the existing binary type is a defect."
    - "No class_weight knob (RESEARCH Open Question 8: reference head is LogisticRegression() with defaults; weighting would be an undeclared deviation). No Estimator impl this phase — the typed API is primary."
---

<objective>
Build the general multiclass head: `MultinomialLogisticRegression` in
`crates/aprender-core/src/classification/` — dataset-agnostic, SetFit as first consumer not owner
(D-01) — fit in f64 on the widened L-BFGS (D-02/D-03), regularized natively as mean-NLL +
lambda*||W||^2 with unpenalized intercept, with the sklearn relation contracted and falsified
including the factor-of-2 trap RESEARCH found inside sklearn's r(W) (Pitfall 1).

Purpose: TRN-04 — one deterministic L2 multinomial softmax head for any ordered K >= 2 labels,
finite outputs, explicit convergence or typed failure, reference-matching regularization.
Output: the head type + typed errors + HeadFitReport, sklearn fixture workflow, the
multinomial-head-v1 contract wired into the Makefile as the first Phase 3 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md):
- Solver surface: `LBFGS::new(max_iter, tol, m)`, `minimize(f, grad, x0) -> OptimizationResult`; status variants Converged | MaxIterations | Stalled | NumericalError | Running | UserTerminated. Use the f64 path from 03-01.
- Error mapping (RESEARCH prescriptive): Converged -> Ok(FittedHead); MaxIterations -> NotConverged{iterations, gradient_norm, tol}; Stalled -> Stalled{iterations, gradient_norm}; NumericalError -> NumericalError{iterations}; Running/UserTerminated unreachable from this call pattern -> internal-error class, never silently accepted. Pre-fit validation -> InvalidInput(HeadInputError): K < 2, label out of range, non-finite feature, lambda < 0, n == 0, dim mismatch.
- The contracted relation, both sides fully expanded (RESEARCH Code Examples): sklearn objective is (1/S)*sum NLL_i + (1/(S*C))*(1/2)*||W||^2_F with S = n unweighted; aprender is (1/n)*sum NLL_i + lambda*||W||^2_F; therefore lambda = 1/(2*C*n). Intercepts excluded from the penalty on BOTH sides; compare intercepts only after mean-centering (Pitfall 5 gauge freedom — probabilities are the gauge-free primary comparison).
- Falsification design: n=24 (8-shot K=3), C=1 -> lambda = 1/48 vs the wrong 1/24 — separable beyond tolerance. Document the RED value in the test comment (GLM precedent: glm_tests.rs:274-297 falsify_glm_irls_link_derivative).
- Softmax-NLL is the ONE algorithm Phase 3 hand-writes: use the log-sum-exp shift in f64 (naive exp overflows at logit ~709 in f64).
- Pitfall 7: sklearn WARNS on non-convergence and returns coefficients; the Python fixture generator must assert n_iter_ < max_iter or the fixture is garbage.
- Fixture env: uv 0.9.5 available; ambient sklearn 1.9.0 — the fixture env must be a PINNED venv with recorded exact versions and LogisticRegression().get_params() (A2 confirmation), never the ambient interpreter.
- Pitfall 4: OptimizationResult carries elapsed_time — define HeadFitReport {status, iterations, final_grad_norm, objective} and hash THAT.
- Makefile: $(CONTRACTS) is an explicit hardcoded list at :1037; PHASE2_CONTRACTS + blocking contract-audit-phase2 (:1086, :1156, invoked from tier3 at :314) is the block to mirror as PHASE3_CONTRACTS/contract-audit-phase3. Binding traps: contract: is the BARE filename; status: only implemented|partial|not_implemented|pending.
- Ordered labels: Vec<String>, index = W row; argmax lowest-index tie-break, contracted.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultinomialLogisticRegression — f64 fit, f32 store, typed errors</name>
  <files>crates/aprender-core/src/classification/multinomial.rs, crates/aprender-core/src/classification/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/classification/mod.rs (the binary sibling at :114 — site the new type ALONGSIDE, register the module, touch nothing else in the file beyond the module declaration/re-export)
    - crates/aprender-core/src/optim/lbfgs.rs (the f64 entry point as landed by 03-01 — program against its actual signature)
    - crates/aprender-core/src/optim/mod.rs (ConvergenceStatus variants)
  </read_first>
  <behavior>
    - fit(features f32 rows, class indices, ordered labels Vec<String>, regularization) with K=3 separable toy data returns Ok; predict_proba rows are finite and sum to 1 within 1e-6; predict returns lowest-index label on an exact probability tie (construct duplicate-row tie case)
    - K < 2 -> InvalidInput; label index >= K -> InvalidInput; NaN feature -> InvalidInput; lambda < 0 -> InvalidInput; n == 0 -> InvalidInput; feature-dim mismatch across rows -> InvalidInput
    - max_iter = 1 on non-trivial data -> Err(NotConverged{..}) carrying iterations and gradient_norm (sklearn would warn; we fail — declared deviation)
    - Two identical fits produce bitwise-identical stored f32 weights (deterministic: fixed x0 = zeros, no randomness anywhere in the fit path)
    - HeadFitReport contains no Duration field and serializes to canonical JSON stable across two runs
  </behavior>
  <action>
    Write the behavior tests first (in multinomial.rs's test module), then implement:
    softmax-NLL objective and analytic gradient in f64 with the log-sum-exp shift; parameter
    layout W (K x d) + intercept vector (K), flattened for L-BFGS x0 = zeros; penalty
    lambda*||W||^2_F EXCLUDING the intercept entries; mean-NLL (1/n scaling). Solve with the f64
    L-BFGS from 03-01 (defaults mirroring sklearn's lbfgs budget: max_iter 100, tol 1e-4 as
    named constants — the contract in Task 3 records their provenance from get_params()).
    Map ConvergenceStatus to HeadFitError exactly per the mapping in <context>. Store fitted
    weights downcast to f32 plus ordered labels; keep an f64 copy OUT of the public surface.
    Prediction: logits then softmax (log-sum-exp shifted), argmax lowest-index tie-break.
    Regularization enum: Lambda(f64) | SklearnEquivalentC{c: f64, resolves at fit time to
    1/(2*c*n)} — both documented against the contracted equation. NO class_weight parameter,
    NO Estimator impl (deliberate, cite RESEARCH Open Questions 6/8 in the module doc).
    Register `pub mod multinomial;` + re-export from classification/mod.rs without touching the
    binary LogisticRegression.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib multinomial 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib multinomial` exits 0 with >= 10 tests
    - multinomial.rs contains `fn fit` returning `Result<` with a typed error (grep `Result<(), String>` in multinomial.rs = 0 matches)
    - multinomial.rs contains a log-sum-exp shift (a max-subtraction before exp — assert via test on logits ~ 1000 producing finite probabilities)
    - `git diff --stat` for classification/mod.rs shows only module registration/re-export lines (binary LogisticRegression untouched)
    - HeadFitReport struct contains no `Duration` and no `elapsed` field (grep = 0 matches in multinomial.rs for `elapsed`)
  </acceptance_criteria>
  <done>Head fits deterministically in f64, stores f32, fails typed, predicts finite ordered-label probabilities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sklearn reference fixture + factor-of-2 falsification</name>
  <files>scripts/gen_multinomial_sklearn_fixture.py, crates/aprender-core/src/classification/tests_multinomial_contract.rs, crates/aprender-core/src/classification/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/glm/glm_tests.rs (the falsify_glm_irls_link_derivative shape at :274-297 — RED value documented in comment, correct value asserted)
    - crates/aprender-core/src/classification/tests_logreg_contract.rs (how contract test files are registered from classification/mod.rs)
  </read_first>
  <behavior>
    - falsify test: aprender fit at lambda = 1/(2*1*24) on the fixed 24x4 K=3 design matrix matches the sklearn-fixture probabilities within the frozen tolerance; the same fit at the WRONG lambda = 1/(1*24) diverges beyond tolerance (asserted as a second test proving the gate bites — the in-band control)
    - Coefficient comparison passes after intercept mean-centering (gauge fix); probability comparison passes raw (gauge-free)
  </behavior>
  <action>
    Python generator (scripts/gen_multinomial_sklearn_fixture.py): builds a DETERMINISTIC design
    matrix from a documented closed-form fill (no ambient RNG — e.g. X[i][j] from a spelled-out
    integer formula scaled to [-2, 2]), n=24, d=4, K=3, labels round-robin; runs
    LogisticRegression(C=1.0) (do NOT pass multi_class — deprecated in sklearn 1.5+); asserts
    n_iter_ < max_iter and aborts otherwise (Pitfall 7); emits a header recording python,
    sklearn, numpy exact versions and the full get_params() dict (A2 confirmation), then
    coefficients, intercepts, and predict_proba rows. Run it inside a uv-pinned venv
    (`uv venv && uv pip install scikit-learn==<ambient-verified 1.9.0>`), record the resolved
    versions in the emitted header. The script is the pinned developer workflow (SAFE-02 style);
    its OUTPUT constants are frozen inline into tests_multinomial_contract.rs with full
    provenance in doc comments (GLM precedent — no fixture files parsed at test time).

    Rust side (tests_multinomial_contract.rs, registered from classification/mod.rs like
    tests_logreg_contract.rs): the falsification pair described in <behavior>, named
    falsify_multinomial_001_sklearn_relation (+ _wrong_lambda_detected control), with the RED
    values the factor-2 error produces documented in the comment. Tolerances: freeze numeric
    tolerances in the test constants BEFORE comparing (Ph1 D-14 discipline) — start from 1e-4 on
    probabilities (both solvers converge to tol 1e-4); if that fails, tighten sklearn's tol in
    the generator rather than loosening the assertion, and record why in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib multinomial_contract 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib multinomial_contract` exits 0 and includes a test whose name contains `sklearn_relation`
    - scripts/gen_multinomial_sklearn_fixture.py contains `n_iter_` and raises/asserts on non-convergence; contains `get_params()`
    - tests_multinomial_contract.rs doc comments contain the recorded sklearn version string and the RED (factor-2) value
    - The wrong-lambda control test asserts divergence (a gate that passes both lambdas is vacuous — must be red-proven during development and stated in the SUMMARY)
  </acceptance_criteria>
  <done>The sklearn relation is a measured, falsified equivalence — not an assumption; the factor-2 trap is caught by construction.</done>
</task>

<task type="auto">
  <name>Task 3: multinomial-head-v1 contract + Makefile Phase 3 wiring</name>
  <files>contracts/multinomial-head-v1.yaml, contracts/aprender/binding.yaml, Makefile, crates/aprender-core/src/classification/multinomial.rs</files>
  <read_first>
    - contracts/contrastive-pair-protocol-v1.yaml (header/metadata discipline: names requirements, implementing crate, why-the-contract-exists; inline-statement discipline — no dangling cross-references)
    - contracts/aprender/binding.yaml (entry shape at :880-887; bare-filename and status-enum traps)
    - Makefile :1080-1100 (PHASE2_CONTRACTS block + the explicit-list warning) and :305-320 (tier3 contract-audit-phase2 invocation to mirror)
    - contracts/classifier-pipeline-v1.yaml and contracts/classification-finetune-v1.yaml (overlap check REQUIRED before authoring — if either already covers an equation, bind there instead of duplicating)
  </read_first>
  <action>
    Author contracts/multinomial-head-v1.yaml in KernelContract shape: equations for (a)
    softmax_nll_objective with BOTH conventions fully expanded including sklearn's half inside
    r(W) and the lambda = 1/(2*C*n) relation; (b) label_order_semantics (index = W row, argmax
    lowest-index tie-break); (c) convergence_error_mapping (ConvergenceStatus -> HeadFitError
    table, including the declared fail-where-sklearn-warns deviation). proof_obligations +
    falsification_tests naming the Task 1/2 tests with commands carrying `--lib` (Phase 2 lesson:
    bare filter forms pass vacuously). If declaring kani harnesses, state in-contract they are
    not executed and name the bounded runnable proptest twin (Phase 2 convention; cargo-kani is
    absent repo-wide). Add #[provable_contracts_macros::contract("multinomial-head-v1",
    equation = "softmax_nll_objective")] on the objective/fit gate function in multinomial.rs.

    binding.yaml: entries for the three equations (bare filename `multinomial-head-v1.yaml`,
    real module_path aprender::classification::multinomial, status: implemented).

    Makefile: add contracts/multinomial-head-v1.yaml to $(CONTRACTS); create
    `PHASE3_CONTRACTS := contracts/multinomial-head-v1.yaml` with the Phase 2 comment discipline;
    create `contract-audit-phase3` mirroring contract-audit-phase2 (blocking, scoped, loop reads
    the audit status — do not copy the repo-wide contract-audit's broken loop); wire it into
    tier3 immediately after the contract-audit-phase2 line. Evidence discipline: run the new
    target STANDALONE first with rc captured directly (`> /tmp/cap3.log 2>&1; rc=$?`), then
    induce one failure mode (temporarily set a binding status to a bogus value or unbind one
    equation), observe the target go red, revert, and record both observations in the SUMMARY.
    Validate with `cargo run --release -p aprender-contracts-cli --bin pv -- validate
    contracts/multinomial-head-v1.yaml` and run `pv lint contracts/multinomial-head-v1.yaml`.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/multinomial-head-v1.yaml && make contract-audit-phase3 > /tmp/cap3-verify.log 2>&1; rc=$?; tail -3 /tmp/cap3-verify.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/multinomial-head-v1.yaml` exits 0
    - `make contract-audit-phase3` exits 0 standalone, and its induced-failure probe was observed red then reverted (SUMMARY records both rc values)
    - Makefile contains `PHASE3_CONTRACTS` and tier3 invokes `contract-audit-phase3`
    - contracts/multinomial-head-v1.yaml contains the literal `1/(2` (the half is pinned in the equation text) and every falsification command contains `--lib`
    - multinomial.rs contains a `#[provable_contracts_macros::contract("multinomial-head-v1"` annotation
    - The overlap check verdict on classifier-pipeline-v1/classification-finetune-v1 is recorded in the SUMMARY (bound there instead, or why not)
  </acceptance_criteria>
  <done>The head's semantics are contract-defended and the audit gate is wired, blocking, and proven falsifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → head fit | untrusted features/labels/lambda cross into contracted numerics (V5) |
| Python fixture env → frozen test constants | reference values entering the falsification gate |
| PyPI → pinned venv | dev-workflow package installs (scikit-learn, numpy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-12 | Tampering (V5) | non-finite features / out-of-range labels / negative lambda | mitigate | InvalidInput typed rejection before solve; behavior tests per case |
| T-3-13 | Tampering | silent regularization drift (C vs lambda, sum vs mean, intercept, the half) | mitigate | contracted two-sided equation + factor-2 falsification with in-band wrong-lambda control |
| T-3-14 | Repudiation | non-converged reference fixture compared as truth | mitigate | generator asserts n_iter_ < max_iter and records versions/get_params (Pitfall 7 / A2) |
| T-3-15 | Repudiation | Duration in hashed fit report | mitigate | HeadFitReport carries only deterministic fields; grep gate on `elapsed` |
| T-3-SC | Tampering | pip installs into the fixture venv | mitigate | uv-pinned venv, exact versions recorded in emitted fixture header; scikit-learn/numpy are top-tier PyPI packages verified present ambient (1.9.0) before pinning — no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib multinomial` and `multinomial_contract` exit 0
- `pv validate contracts/multinomial-head-v1.yaml` exits 0; `make contract-audit-phase3` exits 0
- `cargo clippy -p aprender-core --lib -- -D warnings` exits 0
</verification>

<success_criteria>
- TRN-04 deliverable complete: deterministic K >= 2 head, finite outputs, explicit convergence or typed failure, reference-matching regularization proven by falsification
- Binary LogisticRegression byte-untouched
- Phase 3 contract wiring live and proven falsifiable
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-04-SUMMARY.md` when done.
Record: pinned fixture env versions + get_params(), the RED factor-2 values, the overlap-check
verdict, and the induced contract-audit failure evidence.
</output>
