---
phase: 03-faithful-two-stage-trainer-and-head
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - crates/aprender-core/src/classification/mod.rs
  - crates/aprender-core/src/classification/multinomial.rs
  - crates/aprender-core/src/classification/tests_multinomial_contract.rs
  - scripts/gen_multinomial_sklearn_fixture.py
  - contracts/multinomial-head-v1.yaml
  - contracts/aprender/binding.yaml
  - Makefile
autonomous: true
requirements: [TRN-04]

must_haves:
  truths:
    - "The hand-written analytic gradient is validated against central differences for K=2 and K=3, at lambda=0 and lambda>0, including the unpenalized intercept entries — the ONE new numerical algorithm in the phase is not trusted on the strength of a single reference fixture"
    - "For any ordered K >= 2 label set, fit returns explicit convergence or a typed failure — never Result<(), String>, never silent non-convergence (sklearn warns; we fail, a declared deviation)"
    - "Predicted probabilities are finite, sum to one within 1e-6 per row, and argmax ties break to the lowest label index; logits accumulate in f64 and a non-finite logit is a typed error, not an Inf that propagates"
    - "The contracted sklearn relation pins the half inside r(W): lambda = 1/(2*C*n) against aprender's mean-NLL + lambda*||W||^2_F with unpenalized intercept, and a falsification test at n=24 separates the factor-2 error beyond tolerance"
    - "The head fits in f64 (via 03-01's LbfgsF64) and stores f32; the hashed HeadFitReport carries no Duration or wall-clock field"
  artifacts:
    - path: "crates/aprender-core/src/classification/multinomial.rs"
      provides: "MultinomialLogisticRegression + HeadFitError + HeadFitReport"
      contains: "pub struct MultinomialLogisticRegression"
    - path: "contracts/multinomial-head-v1.yaml"
      provides: "Contract with both objective conventions fully expanded, the gradient obligation, and falsification tests"
    - path: "scripts/gen_multinomial_sklearn_fixture.py"
      provides: "PEP 723 self-pinning fixture generator asserting n_iter_ < max_iter, recording versions and get_params()"
  key_links:
    - from: "crates/aprender-core/src/classification/multinomial.rs"
      to: "crates/aprender-core/src/optim/lbfgs.rs"
      via: "LbfgsF64 entry point from plan 03-01"
      pattern: "LbfgsF64"
    - from: "Makefile"
      to: "contracts/multinomial-head-v1.yaml"
      via: "$(CONTRACTS) line + PHASE3_CONTRACTS + blocking contract-audit-phase3 in tier3"
      pattern: "PHASE3_CONTRACTS"
  caveats:
    - "The binary LogisticRegression at classification/mod.rs:114 and its apr-stochastic-lr-v1 contract are LEFT UNTOUCHED (D-01). Any diff to the existing binary type is a defect."
    - "No class_weight knob (RESEARCH Open Question 8: reference head is LogisticRegression() with defaults; weighting would be an undeclared deviation). No Estimator impl this phase — the typed API is primary."
    - "D-04 as amended reads lambda = 1/(2*C*n). CONTEXT.md was amended in the phase-3 review run so plan and decision now agree; do not 'restore' 1/(C*n)."
---

<objective>
Build the general multiclass head: `MultinomialLogisticRegression` in
`crates/aprender-core/src/classification/` — dataset-agnostic, SetFit as first consumer not owner
(D-01) — fit in f64 on the widened L-BFGS (D-02/D-03), regularized natively as mean-NLL +
lambda*||W||^2 with unpenalized intercept, with the analytic gradient validated by central
differences and the sklearn relation contracted and falsified including the factor-of-2 trap
RESEARCH found inside sklearn's r(W) (Pitfall 1).

Purpose: TRN-04 — one deterministic L2 multinomial softmax head for any ordered K >= 2 labels,
finite outputs, explicit convergence or typed failure, reference-matching regularization.
Output: the head type + typed errors + HeadFitReport, a finite-difference gradient suite, the
self-pinning sklearn fixture workflow, and the multinomial-head-v1 contract wired into the Makefile
as the first Phase 3 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md / 03-PATTERNS.md):
- Solver surface after 03-01: `LbfgsF64::new(max_iter, tol, m)`, `minimize(f, grad, x0: &Vector<f64>) -> OptimizationResultF64`; status variants Converged | MaxIterations | Stalled | NumericalError | Running | UserTerminated (the SAME `ConvergenceStatus` enum both widths share).
- Error mapping (RESEARCH prescriptive): Converged -> Ok(FittedHead); MaxIterations -> NotConverged{iterations, gradient_norm, tol}; Stalled -> Stalled{iterations, gradient_norm}; NumericalError -> NumericalError{iterations}; Running/UserTerminated unreachable from this call pattern -> internal-error class, never silently accepted.
- The contracted relation, both sides fully expanded (RESEARCH Code Examples): sklearn objective is (1/S)*sum NLL_i + (1/(S*C))*(1/2)*||W||^2_F with S = n unweighted; aprender is (1/n)*sum NLL_i + lambda*||W||^2_F; therefore lambda = 1/(2*C*n). Intercepts excluded from the penalty on BOTH sides.
- Falsification design: n=24 (8-shot K=3), C=1 -> lambda = 1/48 vs the wrong 1/24 — separable beyond tolerance. Document the RED value in the test comment (GLM precedent: glm_tests.rs:274-297 falsify_glm_irls_link_derivative).
- Softmax-NLL is the ONE algorithm Phase 3 hand-writes: use the log-sum-exp shift in f64 (naive exp overflows at logit ~709 in f64).
- Pitfall 7: sklearn WARNS on non-convergence and returns coefficients; the Python fixture generator must assert n_iter_ < max_iter or the fixture is garbage.
- Fixture env: uv 0.9.5 available; ambient sklearn 1.9.0. The generator must be SELF-PINNING (below), not dependent on a procedural venv recipe someone has to remember.
- Pitfall 4: OptimizationResult carries elapsed_time — define HeadFitReport {status, iterations, final_grad_norm, objective} and hash THAT.
- Makefile: $(CONTRACTS) is an explicit hardcoded list at :1037; PHASE2_CONTRACTS + blocking contract-audit-phase2 (:1086, :1156, invoked from tier3 at :314) is the block to mirror as PHASE3_CONTRACTS/contract-audit-phase3. Binding traps: contract: is the BARE filename; status: only implemented|partial|not_implemented|pending.
- Ordered labels: Vec<String>, index = W row; argmax lowest-index tie-break, contracted.

**Review fix 1 — no finite-difference gradient check existed.** Both the objective and its analytic
gradient are hand-written here. The sklearn fixture compares a converged OPTIMUM, which a subtly
wrong gradient can still reach in some configurations while being wrong everywhere else. Codex
rated this HIGH; it is the cheapest high-value test in the phase.

**Review fix 2 — input validation was incomplete.** Duplicate/empty ordered labels, zero feature
dimension, classes with no represented rows, non-finite or non-positive C, and prediction-time
dimension mismatch were all unhandled.

**Review fix 3 — finite inputs can still produce infinite dot products.** Testing logits around
1000 does not cover overflow during ACCUMULATION.

**Review fix 4 — the intercept gauge was left to the optimizer.** Make it an explicit, asserted
property rather than an accident.

**Review fix 5 — the fixture generator was procedurally pinned.** Make the script itself declare
and enforce its dependency versions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultinomialLogisticRegression — f64 fit, f32 store, typed errors, complete input validation</name>
  <files>crates/aprender-core/src/classification/multinomial.rs, crates/aprender-core/src/classification/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/classification/mod.rs (the binary sibling at :114 — site the new type ALONGSIDE, register the module, touch nothing else in the file beyond the module declaration/re-export)
    - crates/aprender-core/src/optim/lbfgs.rs (the LbfgsF64 entry point as landed by 03-01 — program against its actual signature)
    - crates/aprender-core/src/optim/mod.rs (ConvergenceStatus variants)
  </read_first>
  <behavior>
    - fit(features f32 rows, class indices, ordered labels Vec<String>, regularization) with K=3 separable toy data returns Ok; predict_proba rows are finite and sum to 1 within 1e-6; predict returns the lowest-index label on an exact probability tie (construct a duplicate-row tie case)
    - K=2 boundary (TRN-04's K >= 2 lower bound; ROADMAP criterion 4's shared binary/multiclass head): fit on a K=2 toy set returns Ok; predict_proba rows finite and sum to 1 within 1e-6; predict returns labels from the 2-label ordered set — the boundary is exercised as a working fit/predict path, not merely K<2 rejection
    - INPUT VALIDATION, one test per case, each a distinct typed InvalidInput variant naming the offender (review fix 2):
      K < 2; label index >= K; NaN feature; +inf feature; lambda < 0; NaN lambda; n == 0; feature dimension d == 0; ragged rows (dim mismatch across rows); DUPLICATE entries in ordered labels; EMPTY string in ordered labels; ordered_labels.len() != K; a class in 0..K with ZERO represented rows; SklearnEquivalentC with c <= 0.0; SklearnEquivalentC with non-finite c; predict/predict_proba called with a feature dimension different from the fitted d
    - ACCUMULATION OVERFLOW (review fix 3): a fitted head fed a feature row whose f32 values are near f32::MAX yields a typed NonFiniteLogit{row, class} error rather than an Inf probability. Logits accumulate in f64; the test constructs the row so that the f32 dot product would overflow but each individual value is finite
    - max_iter = 1 on non-trivial data -> Err(NotConverged{..}) carrying iterations and gradient_norm (sklearn would warn; we fail — declared deviation)
    - INTERCEPT GAUGE (review fix 4): after any successful fit from the zero initialization, the fitted intercept vector's mean is within 1e-9 of 0.0. This is a real property, not an optimizer accident: the NLL gradient w.r.t. the intercepts sums to zero across classes at every point, so a zero-initialized unpenalized intercept never leaves the centered gauge. Assert it; if it ever fails, the gradient is wrong and the test says so
    - Two identical fits produce bitwise-identical stored f32 weights (deterministic: fixed x0 = zeros, no randomness anywhere in the fit path)
    - HeadFitReport contains no Duration field and serializes to canonical JSON stable across two runs
  </behavior>
  <action>
    Write the behavior tests first, then implement: softmax-NLL objective and analytic gradient in
    f64 with the log-sum-exp shift; parameter layout W (K x d) + intercept vector (K), flattened for
    L-BFGS with x0 = zeros; penalty lambda*||W||^2_F EXCLUDING the intercept entries; mean-NLL (1/n
    scaling). Solve with `LbfgsF64` from 03-01 (defaults mirroring sklearn's lbfgs budget: max_iter
    100, tol 1e-4, as named constants — the contract in Task 3 records their provenance from
    get_params()). Map ConvergenceStatus to HeadFitError exactly per the mapping in `<context>`.

    Store fitted weights downcast to f32 plus ordered labels; keep an f64 copy OUT of the public
    surface. Prediction: accumulate logits in f64 from the f32 stored weights (review fix 3),
    check finiteness before the softmax and return `HeadFitError::NonFiniteLogit { row, class }`
    otherwise; then log-sum-exp-shifted softmax; argmax with lowest-index tie-break.

    Regularization enum: `Lambda(f64)` | `SklearnEquivalentC { c: f64 }` resolving at fit time to
    `1/(2*c*n)` with n = the number of ROWS PASSED TO FIT — both documented against the contracted
    equation, and the doc states explicitly that n is a row count, never a pair count (03-07 pins
    this).

    Validation runs BEFORE any solve, in one `validate_fit_inputs` function returning
    `HeadInputError`, so no partially-validated state can reach the optimizer. NO class_weight
    parameter, NO Estimator impl (deliberate, cite RESEARCH Open Questions 6/8 in the module doc).
    Register `pub mod multinomial;` + re-export from classification/mod.rs without touching the
    binary LogisticRegression.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib multinomial > /tmp/p3-04t1.log 2>&1; rc=$?; tail -3 /tmp/p3-04t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib multinomial` exits 0 with >= 24 tests
    - A K=2 fit + predict_proba test exists and passes (test name contains `k2` or `binary_boundary`)
    - Every one of the 16 input-validation cases listed in `<behavior>` has its own named test and asserts a DISTINCT typed variant (the SUMMARY lists the variant per case)
    - `grep -c 'Result<(), String>' crates/aprender-core/src/classification/multinomial.rs` returns 0
    - multinomial.rs contains a log-sum-exp shift (a max-subtraction before exp) — asserted via a test on logits ~1000 producing finite probabilities
    - A test asserts `NonFiniteLogit` is returned (not an Inf probability) for the near-f32::MAX row
    - A test asserts the fitted intercept mean is within 1e-9 of 0.0
    - `git diff --stat` for classification/mod.rs shows only module registration/re-export lines (binary LogisticRegression untouched)
    - `grep -c -E 'elapsed|Duration' crates/aprender-core/src/classification/multinomial.rs` returns 0
  </acceptance_criteria>
  <done>Head fits deterministically in f64, stores f32, fails typed on every enumerated bad input, predicts finite ordered-label probabilities with f64 logit accumulation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Central-difference gradient suite + self-pinning sklearn fixture + factor-of-2 falsification</name>
  <files>scripts/gen_multinomial_sklearn_fixture.py, crates/aprender-core/src/classification/tests_multinomial_contract.rs, crates/aprender-core/src/classification/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/glm/glm_tests.rs (the falsify_glm_irls_link_derivative shape at :274-297 — RED value documented in comment, correct value asserted)
    - crates/aprender-core/src/classification/tests_logreg_contract.rs (how contract test files are registered from classification/mod.rs)
    - crates/aprender-core/src/classification/multinomial.rs (the objective and gradient as landed in Task 1 — the gradient suite needs an in-crate handle on both)
  </read_first>
  <behavior>
    - GRADIENT SUITE (the review's highest-value addition). For each of the four configurations
      {K=2, K=3} x {lambda = 0.0, lambda = 0.07}, on a fixed small design (n=7, d=3, values from a
      spelled-out closed-form fill), at a NON-ZERO parameter point (zeros is a degenerate point
      where several wrong gradients coincide with the right one — perturb by a documented pattern):
        for every parameter index j, |g_analytic[j] - g_central[j]| / max(1.0, |g_analytic[j]|) < 1e-6
      where g_central[j] = (f(x + h e_j) - f(x - h e_j)) / (2h) with h = 1e-6 in f64. Central
      differences at h=1e-6 in f64 carry truncation ~h^2 = 1e-12 and roundoff ~eps/h ~ 2e-10, so a
      1e-6 relative band is comfortable and still catches a factor-of-2, a sign flip, a missing
      1/n, or a penalty applied to the intercept
    - INTERCEPT-EXCLUSION COVERAGE: the suite asserts specifically that the intercept-block entries
      of g_analytic match central differences at lambda = 0.07 — i.e. the penalty term contributes
      exactly 0 there. A gradient that penalizes the intercept passes every W-block check and fails
      only here
    - RED OBSERVATION: before asserting, run the suite against a deliberately broken gradient (drop
      the factor 2 on the penalty term: use lambda*W instead of 2*lambda*W) and record that it goes
      red in the lambda>0 configurations and green at lambda=0 — that asymmetry is what proves the
      suite is measuring the penalty term and not just the NLL
    - falsify test: aprender fit at lambda = 1/(2*1*24) on the fixed 24x4 K=3 design matrix matches the sklearn-fixture probabilities within the frozen tolerance; the same fit at the WRONG lambda = 1/(1*24) diverges beyond tolerance (asserted as a second test proving the gate bites — the in-band control)
    - Coefficient comparison passes after intercept mean-centering (gauge fix); probability comparison passes raw (gauge-free)
  </behavior>
  <action>
    GRADIENT SUITE FIRST — it is cheap, needs no Python, and gates everything else. Put it in
    tests_multinomial_contract.rs as `falsify_multinomial_002_gradient_central_difference` (plus the
    intercept-exclusion variant), reaching the objective and gradient through whatever in-crate
    handle Task 1 exposes (a `pub(crate)` pair of free functions is fine; do not widen the public
    API for a test). Record the RED observation described in `<behavior>` in the SUMMARY.

    PYTHON GENERATOR (review fix 5 — make it self-pinning): write
    scripts/gen_multinomial_sklearn_fixture.py as a PEP 723 script — a `# /// script` header block
    declaring `requires-python` and pinned `dependencies = ["scikit-learn==<resolved>",
    "numpy==<resolved>"]` — so `uv run scripts/gen_multinomial_sklearn_fixture.py` reproduces the
    environment from the file itself rather than from a recipe in a SUMMARY. The script:
    builds a DETERMINISTIC design matrix from a documented closed-form fill (no ambient RNG — X[i][j]
    from a spelled-out integer formula scaled to [-2, 2]), n=24, d=4, K=3, labels round-robin; runs
    `LogisticRegression(C=1.0)` (do NOT pass multi_class — deprecated in sklearn 1.5+); asserts
    `n_iter_ < max_iter` and aborts otherwise (Pitfall 7); emits a header recording the ACTUAL
    resolved python/sklearn/numpy versions (read at runtime from the interpreter, not copied from
    the header block, so a drifted environment is visible) and the full `get_params()` dict (A2
    confirmation), then coefficients, intercepts, and predict_proba rows. Its OUTPUT constants are
    frozen inline into tests_multinomial_contract.rs with full provenance in doc comments (GLM
    precedent — no fixture files parsed at test time).

    Rust falsification pair (registered from classification/mod.rs like tests_logreg_contract.rs):
    `falsify_multinomial_001_sklearn_relation` + `_wrong_lambda_detected` control, with the RED
    values the factor-2 error produces documented in the comment. Tolerances: freeze numeric
    tolerances in the test constants BEFORE comparing (Ph1 D-14 discipline) — start from 1e-4 on
    probabilities (both solvers converge to tol 1e-4); if that fails, tighten sklearn's tol in the
    generator rather than loosening the assertion, and record why in the SUMMARY.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib multinomial_contract > /tmp/p3-04t2.log 2>&1; rc=$?; tail -3 /tmp/p3-04t2.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib multinomial_contract` exits 0 and includes tests whose names contain `sklearn_relation` and `gradient_central_difference`
    - The gradient suite covers all four {K} x {lambda} configurations and at least one non-zero parameter point (source assertion: four configuration constants and a documented perturbation)
    - The intercept-exclusion gradient assertion exists as its own named test
    - The SUMMARY records the RED observation: the broken-gradient variant red at lambda>0, green at lambda=0
    - scripts/gen_multinomial_sklearn_fixture.py begins with a PEP 723 `# /// script` block naming pinned scikit-learn and numpy versions, contains `n_iter_` with an abort on non-convergence, and contains `get_params()`
    - The emitted fixture header records versions read from the RUNNING interpreter (source assertion: the script reads `sklearn.__version__` / `sys.version`, it does not echo its own header block)
    - tests_multinomial_contract.rs doc comments contain the recorded sklearn version string and the RED (factor-2) value
    - The wrong-lambda control test asserts divergence (a gate that passes both lambdas is vacuous — must be red-proven during development and stated in the SUMMARY)
  </acceptance_criteria>
  <done>The analytic gradient is validated independently of the reference optimum, and the sklearn relation is a measured, falsified equivalence with the factor-2 trap caught by construction.</done>
</task>

<task type="auto">
  <name>Task 3: multinomial-head-v1 contract + Makefile Phase 3 wiring</name>
  <files>contracts/multinomial-head-v1.yaml, contracts/aprender/binding.yaml, Makefile, crates/aprender-core/src/classification/multinomial.rs</files>
  <read_first>
    - contracts/contrastive-pair-protocol-v1.yaml (header/metadata discipline: names requirements, implementing crate, why-the-contract-exists; inline-statement discipline — no dangling cross-references)
    - contracts/aprender/binding.yaml (entry shape at :880-887; bare-filename and status-enum traps)
    - Makefile :1080-1100 (PHASE2_CONTRACTS block + the explicit-list warning) and :305-320 (tier3 contract-audit-phase2 invocation to mirror)
    - contracts/classifier-pipeline-v1.yaml and contracts/classification-finetune-v1.yaml (overlap check REQUIRED before authoring — if either already covers an equation, bind there instead of duplicating)
  </read_first>
  <action>
    Author contracts/multinomial-head-v1.yaml in KernelContract shape with equations for:
    (a) `softmax_nll_objective` — BOTH conventions fully expanded including sklearn's half inside
        r(W) and the `lambda = 1/(2*C*n)` relation, with n stated as the ROW count;
    (b) `analytic_gradient` — the closed form for the W block and the intercept block, stating
        explicitly that the penalty term contributes `2*lambda*W` to the W block and exactly 0 to
        the intercept block, and that it is validated by central differences (this equation is the
        review fix's contract home; without it the gradient suite is a test with no obligation);
    (c) `label_order_semantics` — index = W row, argmax lowest-index tie-break;
    (d) `convergence_error_mapping` — ConvergenceStatus -> HeadFitError table, including the
        declared fail-where-sklearn-warns deviation;
    (e) `logit_finiteness` — logits accumulate in f64 and a non-finite logit is a typed error.
    proof_obligations + falsification_tests naming the Task 1/2 tests with commands carrying `--lib`
    (Phase 2 lesson: bare filter forms pass vacuously). If declaring kani harnesses, state
    in-contract they are not executed and name the bounded runnable proptest twin (Phase 2
    convention; cargo-kani is absent repo-wide). Add
    `#[provable_contracts_macros::contract("multinomial-head-v1", equation =
    "softmax_nll_objective")]` on the objective/fit gate function and a second annotation for
    `analytic_gradient` on the gradient function in multinomial.rs.

    binding.yaml: entries for all five equations (bare filename `multinomial-head-v1.yaml`, real
    module_path aprender::classification::multinomial, status: implemented).

    Makefile: add contracts/multinomial-head-v1.yaml to $(CONTRACTS); create
    `PHASE3_CONTRACTS := contracts/multinomial-head-v1.yaml` with the Phase 2 comment discipline;
    create `contract-audit-phase3` mirroring contract-audit-phase2 (blocking, scoped, loop reads the
    audit status — do not copy the repo-wide contract-audit's broken loop); wire it into tier3
    immediately after the contract-audit-phase2 line. Evidence discipline: run the new target
    STANDALONE first with rc captured directly (`> /tmp/cap3.log 2>&1; rc=$?`), then induce one
    failure mode (temporarily set a binding status to a bogus value or unbind one equation), observe
    the target go red, revert, and record both observations in the SUMMARY. Validate with
    `cargo run --release -p aprender-contracts-cli --bin pv -- validate
    contracts/multinomial-head-v1.yaml` and run `pv lint contracts/multinomial-head-v1.yaml`.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/multinomial-head-v1.yaml > /tmp/p3-04t3a.log 2>&1; rc=$?; tail -3 /tmp/p3-04t3a.log; [ $rc -eq 0 ] || exit $rc; make contract-audit-phase3 > /tmp/cap3-verify.log 2>&1; rc=$?; tail -3 /tmp/cap3-verify.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/multinomial-head-v1.yaml` exits 0
    - `make contract-audit-phase3` exits 0 standalone, and its induced-failure probe was observed red then reverted (SUMMARY records both rc values)
    - Makefile contains `PHASE3_CONTRACTS` and tier3 invokes `contract-audit-phase3`
    - contracts/multinomial-head-v1.yaml contains the literal `1/(2` (the half is pinned in the equation text) and an `analytic_gradient` equation naming both `2*lambda*W` and the zero intercept contribution
    - `grep -v '^\s*#' contracts/multinomial-head-v1.yaml | grep -c -- '--lib'` equals the number of falsification test commands in the file
    - multinomial.rs contains two `#[provable_contracts_macros::contract("multinomial-head-v1"` annotations (objective and gradient)
    - The overlap check verdict on classifier-pipeline-v1/classification-finetune-v1 is recorded in the SUMMARY (bound there instead, or why not)
  </acceptance_criteria>
  <done>The head's semantics — including its hand-written derivative — are contract-defended and the audit gate is wired, blocking, and proven falsifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → head fit | untrusted features/labels/lambda cross into contracted numerics (V5) |
| hand-written derivative → optimizer | the phase's one new numerical algorithm, trusted by everything downstream |
| Python fixture env → frozen test constants | reference values entering the falsification gate |
| PyPI → pinned venv | dev-workflow package installs (scikit-learn, numpy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-12 | Tampering (V5) | non-finite features / out-of-range labels / negative lambda / duplicate or empty labels / unrepresented classes / dim mismatch | mitigate | one `validate_fit_inputs` gate before any solve; 16 enumerated cases each with a distinct typed variant and its own test |
| T-3-13 | Tampering | silent regularization drift (C vs lambda, sum vs mean, intercept, the half) | mitigate | contracted two-sided equation + factor-2 falsification with in-band wrong-lambda control |
| T-3-14 | Repudiation | non-converged reference fixture compared as truth | mitigate | generator asserts n_iter_ < max_iter and records runtime-read versions/get_params (Pitfall 7 / A2) |
| T-3-15 | Repudiation | Duration in hashed fit report | mitigate | HeadFitReport carries only deterministic fields; grep gate on `elapsed`/`Duration` |
| T-3-46 | Repudiation | wrong analytic gradient converging to a self-consistent but incorrect optimum | mitigate | central-difference suite over {K=2,K=3} x {lambda=0,>0} at a non-zero point, with a dedicated intercept-exclusion assertion and a recorded RED observation from a deliberately broken gradient |
| T-3-52 | DoS | finite f32 features accumulating to an infinite logit | mitigate | f64 logit accumulation + typed NonFiniteLogit{row,class} checked before softmax; near-f32::MAX row test |
| T-3-53 | Repudiation | intercept gauge drift making coefficient comparison meaningless | mitigate | explicit assertion that the fitted intercept mean stays within 1e-9 of 0, justified from the NLL gradient's zero column-sum |
| T-3-SC | Tampering | pip installs into the fixture env | mitigate | PEP 723 self-pinning script with exact versions; scikit-learn/numpy are top-tier PyPI packages verified present ambient (1.9.0) before pinning — no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib multinomial` and `multinomial_contract` exit 0
- `pv validate contracts/multinomial-head-v1.yaml` exits 0; `make contract-audit-phase3` exits 0
- `cargo clippy -p aprender-core --lib -- -D warnings` exits 0
</verification>

<success_criteria>
- TRN-04 deliverable complete: deterministic K >= 2 head, finite outputs, explicit convergence or typed failure, reference-matching regularization proven by falsification AND an independently validated gradient
- Binary LogisticRegression byte-untouched
- Phase 3 contract wiring live and proven falsifiable
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-04-SUMMARY.md` when done.
Record: the gradient-suite RED observation (broken gradient red at lambda>0, green at lambda=0), the
resolved fixture env versions + get_params(), the RED factor-2 values, the per-case typed-variant
table for the 16 input-validation cases, the overlap-check verdict, and the induced contract-audit
failure evidence.
</output>
