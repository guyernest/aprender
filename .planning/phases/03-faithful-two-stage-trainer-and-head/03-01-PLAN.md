---
phase: 03-faithful-two-stage-trainer-and-head
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-core/src/optim/lbfgs.rs
  - crates/aprender-core/src/optim/mod.rs
  - crates/aprender-core/src/optim/lbfgs_tests.rs
  - crates/aprender-core/src/optim/tests_lbfgs_contract.rs
  - crates/aprender-core/src/primitives/vector.rs
  - contracts/lbfgs-kernel-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [TRN-04]

must_haves:
  truths:
    - "The public `LBFGS` type keeps ZERO generic parameters and its exact current f32 signature; every existing call site compiles unchanged and the f32 golden trajectory (solution bits, status, iteration count, accepted objective sequence) is byte-identical before and after the widening"
    - "A distinct `LbfgsF64` entry point converges on the contract quadratic with gradient norm below 1e-10 — a tolerance the f32 path cannot reach, which is what proves the width is real rather than a cast"
    - "Non-finite input from EVERY channel (x0, objective return, gradient return, line-search trial point) yields ConvergenceStatus::NumericalError in BOTH widths — never a panic, never a silently-finite bogus result"
    - "pv validate passes on the bumped lbfgs-kernel-v1.yaml and pv diff against the materialized old revision reports an additive (minor) change"
  artifacts:
    - path: "crates/aprender-core/src/optim/lbfgs.rs"
      provides: "Private float-generic L-BFGS core; non-generic public LBFGS (f32) wrapper; separate LbfgsF64 entry point"
    - path: "crates/aprender-core/src/optim/tests_lbfgs_contract.rs"
      provides: "f64 twins of FALSIFY-LBFGS-001/002/003, the four-channel non-finite matrix in both widths, the f32 golden-trajectory regression, and proptest variants"
    - path: "contracts/lbfgs-kernel-v1.yaml"
      provides: "Version-bumped contract declaring the f64 entry point and the non-finite-channel obligation"
  key_links:
    - from: "crates/aprender-core/src/optim/lbfgs.rs"
      to: "contracts/lbfgs-kernel-v1.yaml"
      via: "provable_contracts_macros::contract annotation on the two-loop recursion"
      pattern: "lbfgs-kernel-v1"
  caveats:
    - "This is shared contracted code: blast radius extends beyond Phase 3 (D-03 consequence). Both reviewers rated this the phase's largest collateral risk. Run the FULL aprender-core optim suite, not just lbfgs tests, and capture the f32 golden trajectory BEFORE touching the file."
    - "Darwin: any workspace-wide cargo command must use --exclude aprender-profile (STATE.md, control-measured)."
    - "Set CARGO_INCREMENTAL=0 for all builds this phase (STATE.md ENOSPC mitigation, measured effective)."
---

<objective>
Widen the contracted L-BFGS solver to f64 so the multinomial head (plan 03-04) can fit in the
reference's precision (D-03: fit in f64, store f32). Near the optimum the softmax-NLL gradient
norm approaches f32 epsilon (~1.2e-7); an f32 Wolfe line search can stall and report progress it
did not make. This is the first-wave contracted change RESEARCH Open Question 4 prescribes.

Purpose: Unblocks TRN-04 (deterministic L2 multinomial head with explicit convergence) — the head
supplies only the objective and gradient; convergence semantics come from this solver.
Output: private float-generic core, non-generic public f32 `LBFGS` (zero API break, proven by a
recorded golden trajectory rather than by "it still compiles"), `LbfgsF64` entry point, f64
contract-test twins, the four-channel non-finite matrix, version-bumped lbfgs-kernel-v1.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

**BRANCH PRECONDITION (phase-3 review fix — was a wave-1 race).** The branch
`gsd/phase-3-two-stage-trainer` MUST already exist and be the checked-out branch before this plan
starts. It is created ONCE, before wave 1, not by any plan. Three concurrent executors each running
`git checkout -b` in one shared worktree can race on the branch ref, the index, and the commit
parent — Codex rated exactly that arrangement HIGH and it is the one point where the two reviewers
directly contradicted each other. This plan's first action is a CHECK, not a creation:
`git branch --show-current` must print `gsd/phase-3-two-stage-trainer`; if it does not, STOP and
report. See `<wave_1_concurrency>` below for the commit rules that keep three parallel executors
from corrupting a shared index.

Key verified facts (from 03-RESEARCH.md, re-verified against the repo this session):
- `LBFGS::new(max_iter, tol, m)`, `minimize(f, grad, x0) -> OptimizationResult`; `WolfeLineSearch::new(1e-4, 0.9, 50)`; f32-hardwired via `Vector<f32>` (lbfgs.rs is 341 lines).
- `OptimizationResult` carries `elapsed_time: Duration` (optim/mod.rs:122) — a semantic-hash poison; never let it into any hashed record downstream.
- `ConvergenceStatus` variants: Converged, MaxIterations, Stalled, NumericalError, Running, UserTerminated.
- `Vector<T>` is generic in storage but arithmetic impls are f32-only (`impl Vector<f32>` at primitives/vector.rs:81).
- Existing contract binding: `#[provable_contracts_macros::contract("lbfgs-kernel-v1", equation = "two_loop_recursion")]` on `step` (lbfgs.rs ~line 205).
- `tests_lbfgs_contract.rs` (133 lines): FALSIFY-LBFGS-001/002/003 (quadratic convergence, objective decrease, finite result) + two proptest variants, all Vector<f32>-typed. Naming convention: `falsify_lbfgs_NNN_<property>` with "FALSIFIED LBFGS-NNN: ..." messages.
- `pv diff` takes TWO FILESYSTEM PATHS — materialize old revision with `git show HEAD:contracts/lbfgs-kernel-v1.yaml > /tmp/lbfgs-kernel-old.yaml` first (CLAUDE.md rule).
- PV_BIN form: `cargo run --release -p aprender-contracts-cli --bin pv --` (Makefile:1035).

**Review fix — the shape is NOT a generic default type parameter.** Codex: "Replacing the public
struct with a generic alias/default parameter can still be a Rust semver break." Turning
`pub struct LBFGS` into `pub struct LBFGS<T = f32>` breaks any downstream `use` in a turbofish or
trait-bound position and changes the type's kind. The required shape is therefore: a PRIVATE
generic core, a public NON-generic `LBFGS` (f32) whose signature is byte-identical to today's, and
a public NON-generic `LbfgsF64` beside it.
</context>

<wave_1_concurrency>
Wave 1 runs 03-01, 03-02 and 03-03 against ONE shared worktree on ONE branch. Their
`files_modified` sets are disjoint, so the only shared mutable state is git's index. Rules, all
three plans carry them identically:

1. NEVER run `git checkout`, `git checkout -b`, `git switch`, `git stash`, `git reset`, or
   `git clean`. Those mutate worktree-global state and are what the race is made of.
2. Stage with an EXPLICIT PATHSPEC limited to this plan's `files_modified`:
   `git add -- <path> <path> ...`. Never `git add -A`, never `git add .`, never `git commit -a`.
3. A `git commit` that fails with `index.lock`/`Unable to create` is a benign collision with a
   peer executor: sleep 2 s and retry, up to 5 times, then report.
4. If `git branch --show-current` is not `gsd/phase-3-two-stage-trainer` at ANY point, STOP.

If the orchestrator prefers zero risk it may run wave 1 sequentially, or give each executor its own
`git worktree`; both are compatible with these rules and neither requires a plan edit.
</wave_1_concurrency>

<tasks>

<task type="auto">
  <name>Task 1: Capture the f32 golden trajectory, then genericize L-BFGS over the float type</name>
  <files>crates/aprender-core/src/optim/lbfgs.rs, crates/aprender-core/src/optim/mod.rs, crates/aprender-core/src/optim/lbfgs_tests.rs, crates/aprender-core/src/primitives/vector.rs</files>
  <read_first>
    - crates/aprender-core/src/optim/lbfgs.rs (entire file — the code being widened)
    - crates/aprender-core/src/optim/mod.rs (OptimizationResult at ~:122, ConvergenceStatus at ~:171, Optimizer trait)
    - crates/aprender-core/src/primitives/vector.rs (the f32-only arithmetic impl block at ~:81 — determine which ops L-BFGS actually uses)
    - crates/aprender-core/src/optim/tests_lbfgs_contract.rs (the surface that must keep passing)
    - crates/aprender-core/src/optim/lbfgs_tests.rs (the existing f32 unit suite — where the golden-trajectory regression lands)
  </read_first>
  <action>
    Verify the branch precondition first (`git branch --show-current` must print
    `gsd/phase-3-two-stage-trainer`; STOP otherwise). Export CARGO_INCREMENTAL=0 for the session.
    Do NOT create or switch branches — see `<wave_1_concurrency>`.

    STEP A — record the f32 golden trajectory BEFORE editing lbfgs.rs. Addresses the review
    concern that "genericization could change existing f32 iteration trajectories, statuses, or
    line-search behavior while all current convergence tests remain green": compiling is not
    evidence. Add `lbfgs_f32_golden_trajectory_is_unchanged` to lbfgs_tests.rs. On the two
    contract quadratics already used by tests_lbfgs_contract.rs plus one ill-conditioned case
    (diagonal Hessian with condition number ~1e4), record for each: the solution's exact f32 BIT
    PATTERNS (`f32::to_bits` per element), the `ConvergenceStatus` discriminant, `iterations`,
    `gradient_norm.to_bits()`, and the ordered sequence of accepted objective values as bit
    patterns. Run it against the UNCHANGED code, paste the observed constants into the test as
    frozen literals with a comment recording the commit SHA they were captured at, and confirm it
    is green BEFORE step B. It must stay green after step B with zero literal edits — an edit to
    those literals is the finding, not a maintenance chore.

    Recording the accepted-objective sequence needs visibility into accepted steps. Do it with a
    TEST-ONLY instrumentation channel, not a new public API: pass the objective closure a
    `RefCell<Vec<f32>>` the test owns and push on every call, then assert on the monotone
    subsequence. No `pub` item is added for this. (This also settles the LOW review note about
    whether strict-decrease checking needs an observable API: it does not.)

    STEP B — widen. Per RESEARCH Open Question 4's prescriptive shape: a PRIVATE generic core
    `LbfgsImpl<T>` plus a small PRIVATE float-ops trait (`sqrt`, `abs`, `mul_add`-free arithmetic,
    `is_finite`, `zero`, `one`, and the literal constants c1/c2/tolerances as associated consts)
    implemented for f32 and f64. Do NOT add num-traits or any other external dependency; zero new
    external crates is a phase invariant.

    The public surface is TWO NON-GENERIC types:
      - `pub struct LBFGS` — unchanged: same field set, same `new(max_iter: usize, tol: f32,
        m: usize)`, same `minimize(f, grad, x0: &Vector<f32>) -> OptimizationResult`. It is a thin
        wrapper delegating to `LbfgsImpl<f32>`. It gains NO generic parameter, NO default type
        parameter, and NO new public method.
      - `pub struct LbfgsF64` — new: `new(max_iter: usize, tol: f64, m: usize)`,
        `minimize(f, grad, x0: &Vector<f64>) -> OptimizationResultF64` where
        `OptimizationResultF64` exposes `solution: Vector<f64>`, `gradient_norm: f64`,
        `iterations: usize`, `status: ConvergenceStatus` (the SAME enum — do not fork it). It may
        carry `elapsed_time: Duration`; if it does, document it as a hash poison in exactly the
        words the f32 form uses.

    Vector<f64>: prefer plain indexed loops inside the generic internals (the file's existing style
    — `norm` and the two-loop recursion are already indexed loops) over widening `Vector`'s public
    arithmetic surface. Touch primitives/vector.rs ONLY if construction helpers (`zeros`,
    `from_vec`, `len`, `Index`/`IndexMut`) are missing for f64; add the minimum, nothing
    speculative.

    Wolfe line-search constants (c1=1e-4, c2=0.9, 50 iterations) and the stall/iteration-budget
    logic stay identical in both widths.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib optim:: > /tmp/p3-01t1.log 2>&1; rc=$?; tail -5 /tmp/p3-01t1.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo clippy -p aprender-core --lib -- -D warnings > /tmp/p3-01t1b.log 2>&1; rc=$?; tail -3 /tmp/p3-01t1b.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints `gsd/phase-3-two-stage-trainer` (checked, not created)
    - `cargo test -p aprender-core --lib optim::` exits 0 with zero edits to the pre-existing f32 lbfgs test bodies in this task
    - `lbfgs_f32_golden_trajectory_is_unchanged` exists, is green, and its frozen bit-pattern literals are byte-identical to the values captured before step B (SUMMARY records the capture SHA and that no literal was edited)
    - Source assertion: `grep -c 'pub struct LBFGS<' crates/aprender-core/src/optim/lbfgs.rs` returns 0 — the public f32 type has NO generic parameter and NO default type parameter
    - Source assertion: `grep -c 'pub struct LbfgsF64' crates/aprender-core/src/optim/lbfgs.rs` returns 1
    - `grep -v '^\s*//' crates/aprender-core/src/optim/lbfgs.rs | grep -c 'ConvergenceStatus'` >= 1 and no second status enum was introduced (`grep -c 'enum ConvergenceStatus' crates/aprender-core/src/optim/*.rs` stays 1)
    - `git diff crates/aprender-core/Cargo.toml` shows no new entry under [dependencies]
    - `cargo clippy -p aprender-core --lib -- -D warnings` exits 0 (no unwrap(), pedantic-clean)
  </acceptance_criteria>
  <done>Existing f32 API byte-compatible and its trajectory proven unchanged; f64 minimize path exists and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: f64 contract twins, four-channel non-finite matrix, contract bump, pv diff evidence</name>
  <files>crates/aprender-core/src/optim/tests_lbfgs_contract.rs, crates/aprender-core/src/optim/lbfgs_tests.rs, contracts/lbfgs-kernel-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - crates/aprender-core/src/optim/tests_lbfgs_contract.rs (naming convention and the three FALSIFY properties to twin)
    - crates/aprender-core/src/optim/lbfgs.rs (the LbfgsF64 signature as landed in Task 1)
    - contracts/lbfgs-kernel-v1.yaml (current version, equations, falsification_tests entries)
    - contracts/aprender/binding.yaml (entry shape: bare filename, status enum implemented|partial|not_implemented|pending — Phase 2 traps)
  </read_first>
  <behavior>
    - falsify_lbfgs_001_f64: LbfgsF64 on the same contract quadratic converges with gradient_norm < 1e-10 — a tolerance UNREACHABLE by the f32 path, which is what proves the width is real, not a cast
    - falsify_lbfgs_002_f64: the objective strictly decreases across accepted steps in f64
    - falsify_lbfgs_003_f64: solution and gradient_norm are finite for a pathological-but-finite input (ill-conditioned Hessian ~1e10, near-degenerate curvature pair)
    - NON-FINITE MATRIX (review fix — T-3-02 previously promised non-finite handling that no test exercised). Four input channels x two widths = eight cases, each asserting ConvergenceStatus::NumericalError and NO panic:
      (i) x0 contains NaN; (ii) x0 contains +inf; (iii) the objective closure returns NaN at the first trial point; (iv) the gradient closure returns a vector containing inf. Channel (iv) is also run with the non-finite appearing only at a LINE-SEARCH trial point (finite at x0) so the Wolfe loop's own guard is exercised, not just the entry check
    - proptest twin: random PSD quadratics in f64 converge to status Converged
    - Existing f32 falsify tests still pass byte-unmodified
  </behavior>
  <action>
    Add the f64 twins and the non-finite matrix to tests_lbfgs_contract.rs following the exact
    `falsify_lbfgs_NNN_<property>` / "FALSIFIED LBFGS-NNN: ..." conventions. Write the tests FIRST.
    For 001_f64, first assert against a deliberately f32-reachable tolerance (1e-6) and confirm the
    test cannot distinguish the widths, then tighten to 1e-10 and confirm it now would fail on the
    f32 path — record BOTH observations in the SUMMARY. That is the RED observation that makes the
    test non-vacuous.

    For the non-finite matrix, name the tests `falsify_lbfgs_003_nonfinite_<channel>_<width>` and
    assert on the STATUS discriminant, not on a message. If any channel currently panics rather
    than returning NumericalError, fix the solver to return NumericalError (that is the T-3-02
    mitigation this contract already claims) and record which channels needed the fix.

    Contract: materialize the old revision (`git show HEAD:contracts/lbfgs-kernel-v1.yaml >
    /tmp/lbfgs-kernel-old.yaml`), edit lbfgs-kernel-v1.yaml to declare (a) the f64 entry point and
    (b) a `nonfinite_input_status` equation stating that a non-finite value arriving from ANY of the
    four channels yields NumericalError in both widths. Every falsification command must carry
    `--lib` (Phase 2 lesson 02-06: a bare filter form passes vacuously). Bump the version
    additively (minor), then run `pv diff /tmp/lbfgs-kernel-old.yaml contracts/lbfgs-kernel-v1.yaml`
    via the PV_BIN form and record its semver suggestion in the SUMMARY. Run
    `pv validate contracts/lbfgs-kernel-v1.yaml`. If the contract declares kani harnesses, keep the
    Phase 2 convention: state in-contract that they are not executed and name the
    identically-bounded runnable proptest.

    binding.yaml: add an entry for the new `nonfinite_input_status` equation; keep the existing
    `two_loop_recursion` binding intact (the `#[contract]` annotation must survive the
    genericization — re-attach it to the generic step if the macro moved).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib lbfgs > /tmp/p3-01t2.log 2>&1; rc=$?; tail -3 /tmp/p3-01t2.log; [ $rc -eq 0 ] || exit $rc; cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/lbfgs-kernel-v1.yaml > /tmp/p3-01t2b.log 2>&1; rc=$?; tail -3 /tmp/p3-01t2b.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib lbfgs` exits 0 and the run output contains a test named `falsify_lbfgs_001_f64`
    - The run output contains at least 8 test names matching `nonfinite` (four channels x two widths)
    - `pv validate contracts/lbfgs-kernel-v1.yaml` exits 0
    - The `version:` field of contracts/lbfgs-kernel-v1.yaml differs from that of /tmp/lbfgs-kernel-old.yaml (additive bump), and the recorded `pv diff` suggestion is minor, not major
    - `grep -c 'two_loop_recursion' crates/aprender-core/src/optim/lbfgs.rs` >= 1 (binding annotation survived)
    - `grep -v '^\s*#' contracts/lbfgs-kernel-v1.yaml | grep -c -- '--lib'` equals the number of falsification test commands in the file (every one carries --lib)
  </acceptance_criteria>
  <done>f64 twins green, f32 originals untouched and green, non-finite handling proven in both widths, contract bumped with pv diff evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → solver | untrusted objective/gradient closures and x0 cross into contracted numerics |
| contract file → CI gate | contract semantics changes must be visible, not silent |
| pre-widening f32 behavior → post-widening f32 behavior | a shared solver silently changing trajectory for unrelated callers |
| shared worktree → three parallel wave-1 executors | concurrent git index/branch mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | lbfgs-kernel-v1.yaml semantics drift | mitigate | pv diff against materialized old revision recorded in SUMMARY; version bump required |
| T-3-02 | DoS | non-finite objective/gradient/x0/line-search inputs causing panic | mitigate | four-channel x two-width non-finite matrix asserting NumericalError; contract equation `nonfinite_input_status`; unwrap() banned repo-wide |
| T-3-03 | Repudiation | elapsed_time entering downstream hashes | mitigate | documented as hash poison on both result types; downstream plans define their own reports (Pitfall 4) |
| T-3-43 | Tampering | wave-1 git index/branch race across three concurrent executors | mitigate | branch pre-created and only CHECKED here; explicit-pathspec staging; no checkout/stash/reset; documented index.lock retry (see `<wave_1_concurrency>`) |
| T-3-48 | Tampering | silent f32 trajectory change for existing optim consumers | mitigate | frozen bit-pattern golden trajectory captured BEFORE the edit; public LBFGS proven non-generic by source assertion |
| T-3-SC | Tampering | package installs | accept | no external packages installed this plan (RESEARCH Package Legitimacy Audit: workspace-internal only) |
</threat_model>

<verification>
- `CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib optim::` exits 0 (the FULL optim suite, per the reviewers' shared blast-radius concern — not just the lbfgs filter)
- `pv validate contracts/lbfgs-kernel-v1.yaml` exits 0 via PV_BIN form
- `cargo clippy -p aprender-core --lib -- -D warnings` exits 0
</verification>

<success_criteria>
- Public f32 API non-generic and byte-compatible; trajectory proven unchanged by frozen bit patterns
- f64 minimize converges below 1e-10 gradient norm on the contract quadratic, with the RED observation recorded
- Non-finite handling proven across four channels in both widths
- Contract bumped, pv diff evidence in SUMMARY, binding intact
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-01-SUMMARY.md` when done.
Record: the pv diff semver suggestion, the capture SHA of the f32 golden trajectory, both RED
observations for the f64 tolerance test, which non-finite channels required a solver fix, and any
Vector<f64> helpers added.
</output>
