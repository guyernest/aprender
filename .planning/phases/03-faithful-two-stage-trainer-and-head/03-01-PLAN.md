---
phase: 03-faithful-two-stage-trainer-and-head
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-core/src/optim/lbfgs.rs
  - crates/aprender-core/src/optim/mod.rs
  - crates/aprender-core/src/optim/lbfgs_tests.rs
  - crates/aprender-core/src/optim/tests_lbfgs_contract.rs
  - crates/aprender-core/src/primitives/vector.rs
  - contracts/lbfgs-kernel-v1.yaml
  - contracts/aprender/binding.yaml
autonomous: true
requirements: [TRN-04]

must_haves:
  truths:
    - "Every existing f32 LBFGS call site compiles unchanged (zero public API break)"
    - "An f64 minimize path converges on the contract quadratic with gradient norm below an f64-level tolerance (tighter than f32 epsilon)"
    - "pv validate passes on the bumped lbfgs-kernel-v1.yaml and pv diff against the materialized old revision reports an additive (minor) change"
  artifacts:
    - path: "crates/aprender-core/src/optim/lbfgs.rs"
      provides: "Float-generic L-BFGS implementation with LBFGS f32 alias and f64 entry point"
    - path: "crates/aprender-core/src/optim/tests_lbfgs_contract.rs"
      provides: "f64 twins of FALSIFY-LBFGS-001/002/003 plus proptest variants"
    - path: "contracts/lbfgs-kernel-v1.yaml"
      provides: "Version-bumped contract declaring the f64 entry point"
  key_links:
    - from: "crates/aprender-core/src/optim/lbfgs.rs"
      to: "contracts/lbfgs-kernel-v1.yaml"
      via: "provable_contracts_macros::contract annotation on the two-loop recursion"
      pattern: "lbfgs-kernel-v1"
  caveats:
    - "This is shared contracted code: blast radius extends beyond Phase 3 (D-03 consequence). Run the full aprender-core optim test suite, not just lbfgs tests."
    - "Darwin: any workspace-wide cargo command must use --exclude aprender-profile (STATE.md, control-measured)."
    - "Set CARGO_INCREMENTAL=0 for all builds this phase (STATE.md ENOSPC mitigation, measured effective)."
---

<objective>
Widen the contracted L-BFGS solver to f64 so the multinomial head (plan 03-04) can fit in the
reference's precision (D-03: fit in f64, store f32). Near the optimum the softmax-NLL gradient
norm approaches f32 epsilon (~1.2e-7); an f32 Wolfe line search can stall and report progress it
did not make. This is the first-wave contracted change RESEARCH Open Question 4 prescribes.

Also establishes the Phase 3 working branch (Phase 2 branch/PR policy carried forward).

Purpose: Unblocks TRN-04 (deterministic L2 multinomial head with explicit convergence) — the head
supplies only the objective and gradient; convergence semantics come from this solver.
Output: Float-generic L-BFGS with f32 alias (zero API break), f64 entry point, f64 contract-test
twins, version-bumped lbfgs-kernel-v1.yaml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key verified facts (from 03-RESEARCH.md, read directly this session):
- `LBFGS::new(max_iter, tol, m)`, `minimize(f, grad, x0) -> OptimizationResult`; `WolfeLineSearch::new(1e-4, 0.9, 50)`; f32-hardwired via `Vector<f32>` (lbfgs.rs is 341 lines).
- `OptimizationResult` carries `elapsed_time: Duration` (optim/mod.rs:122) — a semantic-hash poison; never let it into any hashed record downstream.
- `ConvergenceStatus` variants: Converged, MaxIterations, Stalled, NumericalError, Running, UserTerminated.
- `Vector<T>` is generic in storage but arithmetic impls are f32-only (`impl Vector<f32>` at primitives/vector.rs:81).
- Existing contract binding: `#[provable_contracts_macros::contract("lbfgs-kernel-v1", equation = "two_loop_recursion")]` on `step` (lbfgs.rs ~line 205).
- `tests_lbfgs_contract.rs` (133 lines): FALSIFY-LBFGS-001/002/003 (quadratic convergence, objective decrease, finite result) + two proptest variants, all Vector<f32>-typed. Naming convention: `falsify_lbfgs_NNN_<property>` with "FALSIFIED LBFGS-NNN: ..." messages.
- `pv diff` takes TWO FILESYSTEM PATHS — materialize old revision with `git show HEAD:contracts/lbfgs-kernel-v1.yaml > /tmp/lbfgs-kernel-old.yaml` first (CLAUDE.md rule).
- PV_BIN form: `cargo run --release -p aprender-contracts-cli --bin pv --` (Makefile:1035).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 3 branch and genericize L-BFGS over the float type</name>
  <files>crates/aprender-core/src/optim/lbfgs.rs, crates/aprender-core/src/optim/mod.rs, crates/aprender-core/src/primitives/vector.rs</files>
  <read_first>
    - crates/aprender-core/src/optim/lbfgs.rs (entire file — the code being widened)
    - crates/aprender-core/src/optim/mod.rs (OptimizationResult at ~:122, ConvergenceStatus at ~:171, Optimizer trait)
    - crates/aprender-core/src/primitives/vector.rs (the f32-only arithmetic impl block at ~:81 — determine which ops L-BFGS actually uses)
    - crates/aprender-core/src/optim/tests_lbfgs_contract.rs (the surface that must keep passing)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-01-SUMMARY.md (branch/PR policy this phase inherits)
  </read_first>
  <action>
    First: create branch `gsd/phase-3-two-stage-trainer` from the current HEAD of
    `gsd/phase-2-contract-gate` (Phase 2's PR is not yet opened; Phase 3 stacks on it, per the
    02-01 policy: all Phase 3 plans commit to this ONE branch, no per-plan PRs, the human opens
    the phase PR). Export CARGO_INCREMENTAL=0 for the session.

    Then widen L-BFGS per RESEARCH Open Question 4's prescriptive shape: genericize the
    implementation over the float — an internal `LbfgsImpl<T>` (or equivalent generic core) with
    a small private float-ops trait implemented for f32 and f64 (do NOT add num-traits or any new
    external dependency; zero new external crates is a phase invariant). Keep the public `LBFGS`
    type as the f32 form with its exact current signature — every existing call site must compile
    unchanged. Expose an f64 entry point (e.g. `LbfgsF64` with `minimize(f, grad, x0:
    Vector<f64>) -> OptimizationResultF64`, or a generic with default type parameter that leaves
    `OptimizationResult` meaning the f32 form — pick whichever leaves the existing public surface
    byte-compatible). The f64 result must expose solution, gradient_norm, iterations, and status
    in f64/usize; it may carry elapsed_time but document it as a hash poison exactly as the f32
    form does.

    For Vector<f64> arithmetic: prefer plain indexed loops inside the generic L-BFGS internals
    (the file's existing style — `norm` and the two-loop recursion are already indexed loops)
    over widening `Vector`'s public arithmetic surface. Touch primitives/vector.rs only if
    construction helpers (`zeros`, `from_vec`, `len`, indexing) are missing for f64 — add the
    minimum, nothing speculative.

    Wolfe line-search constants (c1=1e-4, c2=0.9, 50 iters) stay identical in both widths.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib optim:: 2>&1 | tail -5 && CARGO_INCREMENTAL=0 cargo check -p aprender-core</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints gsd/phase-3-two-stage-trainer
    - `cargo test -p aprender-core --lib optim::` exits 0 with zero test-code changes to existing f32 lbfgs tests in this task (proves no API break)
    - lbfgs.rs contains a generic implementation and an f64-typed minimize entry point (grep: `f64` appears in a public fn signature or public type alias in lbfgs.rs)
    - No new entry appears in crates/aprender-core/Cargo.toml [dependencies]
    - `cargo clippy -p aprender-core --lib -- -D warnings` exits 0 (no unwrap(), pedantic-clean)
  </acceptance_criteria>
  <done>Existing f32 API untouched and green; f64 minimize path exists and compiles; branch established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: f64 contract-test twins, contract version bump, pv diff evidence</name>
  <files>crates/aprender-core/src/optim/tests_lbfgs_contract.rs, crates/aprender-core/src/optim/lbfgs_tests.rs, contracts/lbfgs-kernel-v1.yaml, contracts/aprender/binding.yaml</files>
  <read_first>
    - crates/aprender-core/src/optim/tests_lbfgs_contract.rs (naming convention and the three FALSIFY properties to twin)
    - contracts/lbfgs-kernel-v1.yaml (current version, equations, falsification_tests entries)
    - contracts/aprender/binding.yaml (entry shape: bare filename, status enum implemented|partial|not_implemented|pending — Phase 2 traps)
  </read_first>
  <behavior>
    - falsify_lbfgs_001_f64: f64 minimize on the same contract quadratic converges with gradient_norm < 1e-10 (a tolerance UNREACHABLE by the f32 path — this is what proves the width is real, not a cast)
    - falsify_lbfgs_002_f64: objective strictly decreases across accepted steps in f64
    - falsify_lbfgs_003_f64: result solution and gradient_norm are finite for a pathological-but-finite input
    - proptest twin: random PSD quadratics in f64 converge to status Converged
    - Existing f32 falsify tests still pass byte-unmodified
  </behavior>
  <action>
    Add the f64 twins to tests_lbfgs_contract.rs following the exact `falsify_lbfgs_NNN_<property>`
    / "FALSIFIED LBFGS-NNN: ..." conventions. Write the tests FIRST and watch 001_f64 fail against
    a deliberately-f32-truncated tolerance to prove the assertion bites, then assert the real
    f64 tolerance (record the RED observation in the SUMMARY).

    Contract: materialize the old revision (`git show HEAD:contracts/lbfgs-kernel-v1.yaml >
    /tmp/lbfgs-kernel-old.yaml`), edit lbfgs-kernel-v1.yaml to declare the f64 entry point and the
    new falsification tests (every test command must carry `--lib` — Phase 2 lesson 02-06: a bare
    filter form passes vacuously), bump the version additively (minor), then run
    `pv diff /tmp/lbfgs-kernel-old.yaml contracts/lbfgs-kernel-v1.yaml` via the PV_BIN form and
    record its semver suggestion in the SUMMARY. Run `pv validate contracts/lbfgs-kernel-v1.yaml`.
    If the contract declares kani harnesses, keep the Phase 2 convention: state in-contract that
    they are not executed and name the identically-bounded runnable proptest.

    binding.yaml: update/add entries for the f64 path only if a new equation was added; keep the
    existing `two_loop_recursion` binding intact (the #[contract] annotation must survive the
    genericization — re-attach it to the generic step if the macro moved).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib lbfgs 2>&1 | tail -3 && cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/lbfgs-kernel-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --lib lbfgs` exits 0 and the run includes tests matching `falsify_lbfgs_001_f64` (assert via test name in output)
    - `pv validate contracts/lbfgs-kernel-v1.yaml` exits 0
    - contracts/lbfgs-kernel-v1.yaml version field differs from /tmp/lbfgs-kernel-old.yaml version field (additive bump)
    - grep -c 'two_loop_recursion' crates/aprender-core/src/optim/lbfgs.rs >= 1 (binding annotation survived)
    - Every falsification test command added to the contract contains the literal string `--lib`
  </acceptance_criteria>
  <done>f64 twins green, f32 originals untouched and green, contract bumped with pv diff evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → solver | untrusted objective/gradient closures and x0 cross into contracted numerics |
| contract file → CI gate | contract semantics changes must be visible, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | lbfgs-kernel-v1.yaml semantics drift | mitigate | pv diff against materialized old revision recorded in SUMMARY; version bump required |
| T-3-02 | DoS | non-finite objective/gradient inputs causing panic | mitigate | NumericalError status path retained in both widths; falsify_lbfgs_003_f64 asserts finite handling; unwrap() banned repo-wide |
| T-3-03 | Repudiation | elapsed_time entering downstream hashes | mitigate | documented as hash poison on both result types; downstream plans define their own reports (Pitfall 4) |
| T-3-SC | Tampering | package installs | accept | no external packages installed this plan (RESEARCH Package Legitimacy Audit: workspace-internal only) |
</threat_model>

<verification>
- `CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib optim::` exits 0
- `pv validate contracts/lbfgs-kernel-v1.yaml` exits 0 via PV_BIN form
- `cargo clippy -p aprender-core --lib -- -D warnings` exits 0
</verification>

<success_criteria>
- f32 public API byte-compatible (no existing call-site or test edits required)
- f64 minimize converges below 1e-10 gradient norm on the contract quadratic
- Contract bumped, pv diff evidence in SUMMARY, binding intact
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-01-SUMMARY.md` when done.
Record: the pv diff semver suggestion, the RED observation from the f64 tolerance test, and any
Vector<f64> helpers added.
</output>
