---
phase: 03-faithful-two-stage-trainer-and-head
plan: 10
type: execute
wave: 7
depends_on: ["03-09"]
files_modified:
  - crates/aprender-train/Cargo.toml
  - crates/aprender-train/tests/ui.rs
  - crates/aprender-train/tests/ui/setfit_direct_state_construction.rs
  - crates/aprender-train/tests/ui/setfit_direct_state_construction.stderr
  - crates/aprender-train/tests/ui/setfit_fit_head_before_tune.rs
  - crates/aprender-train/tests/ui/setfit_fit_head_before_tune.stderr
  - crates/aprender-train/tests/ui/setfit_token_without_lock.rs
  - crates/aprender-train/tests/ui/setfit_token_without_lock.stderr
  - crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.rs
  - crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.stderr
  - crates/aprender-train/tests/ui/setfit_probe_claims_setfit.rs
  - crates/aprender-train/tests/ui/setfit_probe_claims_setfit.stderr
  - crates/aprender-train/tests/ui/setfit_metric_value_asserted.rs
  - crates/aprender-train/tests/ui/setfit_metric_value_asserted.stderr
  - crates/aprender-train/tests/ui/setfit_external_codec_impl.rs
  - crates/aprender-train/tests/ui/setfit_external_codec_impl.stderr
  - crates/aprender-train/tests/setfit_repro.rs
  - Makefile
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TRN-01, TRN-02, TRN-03, TRN-04, TRN-05, TRN-06, TRN-07, SAFE-03]

must_haves:
  truths:
    - "Every illegal lifecycle expression is a COMPILE error proven by trybuild: direct state construction, fit_head before tune_encoder, token without lock, pairs into fit_head, probe-as-SetFit, an asserted metric value, and an external codec impl — each with a .stderr snapshot naming the crate's real types and visibility"
    - "Two SEPARATE processes running the full pipeline from identical inputs produce identical composite hashes — including under DIFFERENT, FIXED RAYON_NUM_THREADS values, with the pool size proven to differ and the child spawned by its exact test name"
    - "The composite hash compares the RECORDED execution digests from 03-05, and a SEPARATE assertion checks those recorded values against an independently recomputed expected replay — the two checks are distinct, so a wrong-but-consistent order cannot pass both"
    - "The tier3 cross-process gate and the GEMM determinism gate are Make targets whose rc is captured directly, never through a pipe"
    - "cargo-mutants scoped to the new Phase 3 code reports an ADJUSTED score >= 85% after excluding proven-equivalent mutants; narrative justification alone does not waive the threshold"
    - "The closing audit runs the VALIDATION.md full suite and the scoped clippy gates, not just the scoped test filters"
  artifacts:
    - path: "crates/aprender-train/tests/ui.rs"
      provides: "trybuild harness for the seven non-constructibility proofs"
    - path: "crates/aprender-train/tests/setfit_repro.rs"
      provides: "In-process two-run equality + subprocess child mode for the cross-process gate"
    - path: "Makefile"
      provides: "setfit-repro-crossproc + gemm-thread-determinism targets wired into tier3"
  key_links:
    - from: "Makefile"
      to: "crates/aprender-train/tests/setfit_repro.rs"
      via: "tier3 target spawning two separate processes and comparing hash lines"
      pattern: "setfit-repro-crossproc"
    - from: "crates/aprender-train/tests/ui.rs"
      to: "tests/ui/*.rs"
      via: "trybuild compile_fail glob"
      pattern: "compile_fail"
  caveats:
    - "In-process two-run equality alone is structurally blind (shared pool/allocator/statics) — the cross-process form is the AUTHORITATIVE claim (D-16). Both must exist."
    - "cargo-mutants at --timeout 20, not 60: 02-08 measured 9% of mutants hanging; 60 s x hangs exceeds the wall budget."
    - "tier2 arm64 red (D-ITEM-02) and zero-test headline (D-ITEM-03) stand — verification is via the scoped commands and targets named here."
    - "This plan was 03-09 before the phase-3 review run; 03-08's Task 3 became the new 03-09, shifting this plan to 03-10 / wave 7. Two UI cases were added for the review's new invariants (asserted metric value, external codec impl)."
---

<objective>
Prove the phase's two structural claims end-to-end: TRN-01's legality by compile-fail evidence
(trybuild, Phase 2's house proof) and TRN-06's reproducibility by the authoritative cross-process
two-clean-runs gate at fixed differing thread counts (D-16), then close the phase with the scoped
mutation run, the full-suite audit and final tier wiring.

Purpose: This is the phase's verification spine — everything earlier plans asserted becomes either a
compile error or a measured hash equality here.
Output: seven UI cases + snapshots, in-process + cross-process repro gates with separated
recorded-vs-expected checks, Make targets in tier3, mutation evidence with an adjusted score.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-faithful-two-stage-trainer-and-head/03-CONTEXT.md

Key facts:
- trybuild pattern (verified): contrastive-data/tests/ui.rs harness (`TestCases::new().compile_fail("tests/ui/*.rs")`); each case a complete program using ONLY the public API, headed by obligation ID + expected diagnostic; snapshot rules: each .stderr must name REAL types/methods/visibility (a syntax-error snapshot proves nothing); re-baseline via `TRYBUILD=overwrite` then review the diff against the named-types list. `trybuild = "1"` is a workspace dep; aprender-train needs the dev-dependency line (match contrastive-data's form).
- Run trybuild as `cargo test -p aprender-train --test ui --features setfit`.
- Cross-process gate design (D-16 + Verification Discipline rule 1): a Make target runs the pipeline test twice as SEPARATE processes (fresh pool, allocator, statics), each printing one composite hash line; statuses captured directly (`> /tmp/x.log 2>&1; rc=$?`); hashes compared with cmp/diff on extracted lines.
- Composite hash contents (ROADMAP criterion 5): selection ordered IDs + semantic hash, the recorded per-epoch consumed-pair digest, the recorded batch-boundary digest, step count, loss-trace hash, evidence table hash, encode-ledger hash, parameter registry hash, artifact hash, predictions on the probe rows. All already deterministic by construction from earlier plans, and all reachable through 03-08's public accessors.
- Mutation scope (Ph1 D-25 / Ph2 D-26 precedent): new Phase 3 code only — `crates/aprender-train/src/train/setfit/`, `crates/aprender-core/src/classification/multinomial.rs`, `crates/aprender-core/src/setfit/dropout_rng.rs`. `cargo mutants --no-times --timeout 20 --in-place` with `-f` file scoping.
- 02-08 survivor discipline: 10/22 survivors were individually justified as unobservable and RE-RUN to confirm survival; the justified list goes in the SUMMARY.
- VALIDATION.md's declared full suite is `cargo test --workspace --lib --exclude aprender-profile` (Darwin form, STATE.md).

**Review fix 1 — the .stderr snapshots and REQUIREMENTS.md were absent from `files_modified`.** Both
are now listed.

**Review fix 2 — spawning `current_exe()` does not run custom child code just because an env var is
set.** A libtest binary runs the FILTERED test. The child is spawned with its exact test name plus
`--exact --nocapture`.

**Review fix 3 — "host max" threads can be 1 on a constrained runner**, making a `THREADS > 1`
assertion unsatisfiable. Fixed pool sizes 1 and 3 are used.

**Review fix 4 — the public test fixture may have required a production `#[doc(hidden)]`
constructor.** 03-05's fixture uses `SetFitMiniLm::from_slice_fixture`, which is already `pub`
(setfit/mod.rs:236); confirm that path reaches the integration test and record it, rather than adding
a production backdoor.

**Review fix 5 — the closing audit omitted the full workspace suite and scoped clippy.** Both added.

**Review fix 6 — comparing recomputed digests inherits 03-08's false-green problem.** The gate now
compares the RECORDED digests across processes AND, separately, checks the recorded values against an
independently recomputed expected replay. Two distinct assertions with distinct failure meanings.

**Review fix 7 — ">= 85% or justify survivors" waives the threshold by narrative.** The threshold now
applies to an ADJUSTED score.
</context>

<tasks>

<task type="auto">
  <name>Task 1: trybuild non-constructibility suite (seven proofs)</name>
  <files>crates/aprender-train/Cargo.toml, crates/aprender-train/tests/ui.rs, crates/aprender-train/tests/ui/setfit_direct_state_construction.rs, crates/aprender-train/tests/ui/setfit_direct_state_construction.stderr, crates/aprender-train/tests/ui/setfit_fit_head_before_tune.rs, crates/aprender-train/tests/ui/setfit_fit_head_before_tune.stderr, crates/aprender-train/tests/ui/setfit_token_without_lock.rs, crates/aprender-train/tests/ui/setfit_token_without_lock.stderr, crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.rs, crates/aprender-train/tests/ui/setfit_pairs_into_fit_head.stderr, crates/aprender-train/tests/ui/setfit_probe_claims_setfit.rs, crates/aprender-train/tests/ui/setfit_probe_claims_setfit.stderr, crates/aprender-train/tests/ui/setfit_metric_value_asserted.rs, crates/aprender-train/tests/ui/setfit_metric_value_asserted.stderr, crates/aprender-train/tests/ui/setfit_external_codec_impl.rs, crates/aprender-train/tests/ui/setfit_external_codec_impl.stderr</files>
  <read_first>
    - crates/aprender-contrastive-data/tests/ui.rs (harness + snapshot rules doc :20-44)
    - crates/aprender-contrastive-data/tests/ui/split_constructed_directly.rs and its .stderr (case-file discipline: obligation header, public-API-only, expected diagnostic)
    - crates/aprender-contrastive-data/Cargo.toml [dev-dependencies] (the trybuild dep line form to copy)
    - crates/aprender-train/src/train/setfit/mod.rs, lock.rs, evaluate.rs, verify.rs, baseline.rs (the real public surface the cases attack)
  </read_first>
  <action>
    Add the trybuild dev-dependency to aprender-train (same form as contrastive-data). Create the
    harness (compile_fail glob) with the snapshot-rules doc comment copied and adapted. SEVEN cases,
    each a complete program using only the public API, headed by the requirement/decision it
    discharges and the expected diagnostic:
    (1) `setfit_direct_state_construction` — constructing `SetFitRun<EncoderTuned>` via struct
        literal / private field access (TRN-01/D-06; expect private-field or private-constructor
        diagnostics);
    (2) `setfit_fit_head_before_tune` — calling `.fit_head()` on `SetFitRun<Prepared>` (expect
        method-not-found-for-type);
    (3) `setfit_token_without_lock` — constructing `CanonicalTestToken` directly (TRN-07/D-14; expect
        private constructor/field diagnostic);
    (4) `setfit_pairs_into_fit_head` — attempting to pass a pair collection to `fit_head` (TRN-05/
        D-08; expect wrong-number-of-arguments — the signature takes zero non-self args);
    (5) `setfit_probe_claims_setfit` — converting `FrozenProbeRun` into any `SetFitRun<_>` (SAFE-03/
        D-11; expect no-From/Into/trait-not-implemented);
    (6) NEW (review fix) `setfit_metric_value_asserted` — attempting to construct a
        `ValidationEvaluation` with a caller-chosen metric value (TRN-07/D-14 as amended; expect
        private-constructor/no-such-function). This is the compile-time proof that the "caller
        asserts the number" hole Codex found is now closed by construction;
    (7) NEW (review fix) `setfit_external_codec_impl` — an out-of-crate `impl SetFitCodec for
        MyCodec` (TRN-01/D-07 as amended; expect the sealed-supertrait diagnostic naming the private
        Sealed trait). This is the compile-time proof that an arbitrary implementor cannot enter the
        verification path.
    Generate .stderr snapshots (`TRYBUILD=overwrite`), then REVIEW each against the named-types rule:
    every snapshot must mention the real type or method name (SetFitRun, fit_head, CanonicalTestToken,
    FrozenProbeRun, ValidationEvaluation, SetFitCodec/Sealed) — a case failing for an unrelated reason
    (typo, missing import) is a vacuous proof; fix the case, not the snapshot.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --test ui --features setfit > /tmp/p3-10t1.log 2>&1; rc=$?; tail -3 /tmp/p3-10t1.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --test ui --features setfit` exits 0 with 7 compile-fail cases
    - Each .stderr file names at least one real Phase 3 type/method: `grep -lE 'SetFitRun|fit_head|CanonicalTestToken|FrozenProbeRun|ValidationEvaluation|SetFitCodec|Sealed' crates/aprender-train/tests/ui/*.stderr` lists all seven files
    - The external-codec case's .stderr names the private Sealed trait (grep that specific file)
    - No case file uses pub(crate) paths, #[doc(hidden)] items, or test-support doors — public API only (source review recorded in SUMMARY)
    - All seven .rs and all seven .stderr files are tracked by git (`git ls-files --others --exclude-standard crates/aprender-train/tests/ui/` is empty)
  </acceptance_criteria>
  <done>Illegal lifecycle expressions — including the two the review exposed — are compile errors with reviewed, type-naming evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Two-clean-runs gates — in-process, cross-process, and recorded-vs-expected separated</name>
  <files>crates/aprender-train/tests/setfit_repro.rs, Makefile</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/mod.rs (full pipeline surface: prepare/tune_encoder/fit_head/verify_artifact, plus the ten PUBLIC reproducibility accessors 03-08 Task 2 landed — the ONLY surface the child may read, since pub(crate) internals are invisible to out-of-crate integration tests)
    - crates/aprender-core/src/setfit/mod.rs (from_slice_fixture :236 is `pub` — confirm the fixture is reachable from an integration test WITHOUT adding a production backdoor, and record the confirmation)
    - crates/aprender-train/src/train/setfit/epoch.rs (epoch_pair_order — the independent recomputation source for the expected-replay check)
    - crates/aprender-core/tests/gemm_thread_determinism.rs (the current_exe + exact-test-name subprocess pattern from 03-02 to reuse)
    - Makefile tier3 block :305-320 (wiring style) and CLAUDE.md Verification Discipline rules 1 and 2
  </read_first>
  <action>
    setfit_repro.rs (integration test, `--features setfit`):

    CHILD MODE — a test named `setfit_repro_child`, a no-op unless `SETFIT_REPRO_CHILD=1`. It builds
    the fixture, runs the full pipeline (prepare -> tune_encoder -> fit_head ->
    verify_artifact(&SerdeJsonCodec)), and prints labeled lines:
      `SELECTION=<semantic hash hex>`, `PAIRORDER=<recorded consumed_pair_digest hex>`,
      `BATCHES=<count>:<recorded batch_boundary_digest hex>`, `STEPS=<n>`, `LOSSTRACE=<hex>`,
      `EVIDENCE=<hex>`, `LEDGER=<encode_ledger_hash hex>`, `REGISTRY=<parameter_registry_hash hex>`,
      `ARTIFACT=<hex>`, `PREDICTIONS=<sha256 of prediction bytes>`, `THREADS=<rayon::current_num_threads()>`.
    Source every labeled line EXCLUSIVELY from the public reproducibility accessors 03-08 landed. If
    any component lacks a public accessor, that is an 03-08 defect — add the accessor in mod.rs
    (public, read-only), never a test-support backdoor, never a widened pub(crate).

    IN-PROCESS TEST: run the pipeline twice in one process, assert every component equal (fast
    tier2-class signal; D-16's non-authoritative half).

    CROSS-PROCESS TEST (`setfit_repro_cross_process`): spawn two children via `current_exe()` with
    argv `["setfit_repro_child", "--exact", "--nocapture"]` (review fix 2 — a libtest binary runs the
    FILTERED test; an env var alone executes nothing) and `RAYON_NUM_THREADS` = 1 and 3 (review fix 3
    — fixed sizes; "host max" can be 1). Assert (a) the `THREADS` lines DIFFER (mechanism engaged,
    rule 2) and (b) every other labeled line is identical.

    RECORDED-VS-EXPECTED, A SEPARATE TEST (review fix 6 — do not fold this into the cross-process
    equality): `setfit_repro_recorded_matches_expected_replay` independently recomputes, from the
    run's config and selection, the expected per-epoch pair order via `epoch_pair_order` and the
    expected batch boundaries via the consecutive-window rule, digests them with the SAME absorption
    format 03-05 contracted, and asserts equality with the RECORDED digests. Keep the two checks
    distinct and say why in the test doc: cross-process equality proves the run is reproducible;
    this test proves the run consumed the order it was supposed to. A single fused check cannot tell
    "reproducibly wrong" from "correct".

    Makefile: `setfit-repro-crossproc` running
    `cargo test -p aprender-train --test setfit_repro --features setfit setfit_repro_cross_process`
    with rc captured directly; wire into tier3 after `contract-audit-phase3`. Also add the
    `gemm-thread-determinism` target (deferred from 03-02): runs
    `cargo test -p aprender-core --test gemm_thread_determinism`, rc captured directly, wired into
    tier3 beside the repro gate. Evidence discipline for BOTH new targets: run standalone first (rc
    recorded), induce one failure (perturb a hash input in a scratch build, or force both children to
    the same pool size so the THREADS assertion fires), observe red, revert, record both observations.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --test setfit_repro --features setfit > /tmp/p3-10t2.log 2>&1; rc=$?; tail -3 /tmp/p3-10t2.log; [ $rc -eq 0 ] || exit $rc; make setfit-repro-crossproc > /tmp/repro.log 2>&1; rc=$?; tail -2 /tmp/repro.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --test setfit_repro --features setfit` exits 0 (in-process, cross-process and recorded-vs-expected tests)
    - The child spawn argv contains the exact test name, `--exact` and `--nocapture` (source assertion, all three tokens)
    - The test source contains `RAYON_NUM_THREADS` set to the literals 1 and 3, and does NOT contain a "host max" / `current_num_threads()`-derived pool size for the spawn (source assertion)
    - The cross-process test asserts THREADS differs between children AND all ten labeled hashes match (both assertions present in source)
    - `setfit_repro_recorded_matches_expected_replay` exists as a SEPARATE test and its doc comment states why it is separate
    - Makefile contains setfit-repro-crossproc and gemm-thread-determinism, both invoked from tier3, neither reading rc through a pipe (`grep -c 'tee' ` in both recipes returns 0 before any rc capture)
    - The induced-red observations for both targets are recorded in the SUMMARY with rc values
    - setfit_repro.rs reads composite-hash components only through the public accessors from 03-08 (no pub(crate) reach-ins, no #[doc(hidden)] backdoors); the SUMMARY records that `from_slice_fixture` was reachable as an existing `pub fn` and that no production backdoor was added
  </acceptance_criteria>
  <done>TRN-06's authoritative claim is a measured cross-process, cross-thread-count hash equality over RECORDED execution, with a separate check that the recorded execution was the intended one.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped mutation run with an adjusted score + phase closing audit</name>
  <files>Makefile, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-08-SUMMARY.md (the measured --timeout 20 rationale and survivor-justification discipline)
    - .planning/phases/03-faithful-two-stage-trainer-and-head/03-VALIDATION.md (the declared full suite and the four tier3 targets the closing audit must run)
    - crates/aprender-train/src/train/setfit/ (the mutation surface)
  </read_first>
  <action>
    Run cargo-mutants scoped to the Phase 3 surface, in three scoped invocations (`-f` accepts file
    globs): (a) `crates/aprender-train/src/train/setfit/**`, (b)
    `crates/aprender-core/src/classification/multinomial.rs`, (c)
    `crates/aprender-core/src/setfit/dropout_rng.rs` — each with `cargo mutants --no-times --timeout
    20 --in-place -f <scope>` plus the feature flags the scope needs (consult `cargo mutants --help`
    for the current test-arg passing form and record the exact commands used).

    SCORING (review fix 7): report BOTH
      raw_score = caught / total
      adjusted_score = caught / (total - proven_equivalent)
    where `proven_equivalent` counts only survivors individually demonstrated unobservable per the
    02-08 taxonomy (disjoint-bit ops, single-variant enums, accessors no constructible value
    distinguishes) AND re-run to confirm they still survive. **The >= 85% threshold applies to
    adjusted_score.** A survivor that is merely "explained" but not proven equivalent counts against
    the score; kill it with a targeted test. Record the full table — mutant, scope, verdict,
    justification, re-run confirmation — in the SUMMARY.

    CLOSING AUDIT (the phase gate, every command rc-captured directly, never through a pipe):
    - `cargo test -p aprender-train --lib --features setfit` and `cargo test -p aprender-core --lib
      --features setfit` exit 0
    - `cargo test -p aprender-train --test ui --features setfit` and `--test setfit_repro` exit 0
    - `cargo test -p aprender-core --test gemm_thread_determinism` exits 0
    - FULL SUITE (review fix 5): `cargo test --workspace --lib --exclude aprender-profile` exits 0 —
      this is VALIDATION.md's declared full suite and its absence was a HIGH finding
    - SCOPED CLIPPY (review fix 5): `cargo clippy -p aprender-train --lib --features setfit --
      -D warnings` and `cargo clippy -p aprender-core --lib --features setfit -- -D warnings` exit 0
    - `make contract-audit-phase3`, `make setfit-repro-crossproc`, `make gemm-thread-determinism`,
      `make setfit-feature-matrix` exit 0
    - `pv lint contracts/multinomial-head-v1.yaml contracts/setfit-train-lifecycle-v1.yaml` recorded
      (plus contracts/gemm-partition-determinism-v1.yaml if 03-02's contingency fired)
    - Darwin workspace form: `cargo check --workspace --exclude aprender-profile` exits 0
    - Known-red re-measured and recorded: `cargo package -p aprender-train --no-verify` still fails on
      the unpublished dep (expected; Pitfall 9)
    Mark TRN-01..TRN-07 + SAFE-03 complete in .planning/REQUIREMENTS.md via the requirements handler
    ONLY for those whose evidence is genuinely demonstrated by the audit (Phase 2 discipline: never
    check a box to make a table look finished; if any item is short, leave it unchecked and say why).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit > /tmp/p3-10t3a.log 2>&1; rc=$?; tail -3 /tmp/p3-10t3a.log; [ $rc -eq 0 ] || exit $rc; CARGO_INCREMENTAL=0 cargo test --workspace --lib --exclude aprender-profile > /tmp/p3-10t3b.log 2>&1; rc=$?; tail -3 /tmp/p3-10t3b.log; [ $rc -eq 0 ] || exit $rc; make contract-audit-phase3 > /tmp/cap3.log 2>&1; rc=$?; tail -2 /tmp/cap3.log; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - Mutation table in SUMMARY: total mutants, caught, raw_score, proven_equivalent (each with its justification and re-run confirmation), adjusted_score — and adjusted_score >= 0.85 per scope, or the shortfall is stated as a shortfall rather than waived
    - Every closing-audit command's rc recorded in the SUMMARY, captured directly, no pipes
    - `cargo test --workspace --lib --exclude aprender-profile` and both scoped clippy commands appear in the recorded audit with rc 0
    - The known-red package measurement recorded with its error message
    - .planning/REQUIREMENTS.md updates reflect only genuinely demonstrated items, and the SUMMARY says which (if any) were left unchecked and why
  </acceptance_criteria>
  <done>The phase's evidence is complete, measured against the declared full suite, and honestly booked with an adjusted mutation score.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time claims → runtime behavior | trybuild is the proof the typestate claims are real |
| single-process green → reproducibility claim | cross-process gate is the authoritative boundary |
| reproducible → correct | a run can be perfectly reproducible and consume the wrong order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-30 | Repudiation | in-process-only repro passing for the wrong reason (shared pool/statics) | mitigate | cross-process gate at FIXED differing thread counts with a mechanism-engaged assertion (THREADS lines differ) and children spawned by exact test name |
| T-3-31 | Tampering | vacuous trybuild snapshots (syntax-error proofs) | mitigate | named-types review rule per snapshot; acceptance grep across all seven .stderr files; snapshots tracked by git |
| T-3-32 | Repudiation | gates whose rc is read through a pipe (repo has shipped this twice) | mitigate | direct rc capture in both new Make targets; acceptance criterion greps recipes for a pre-capture `tee` |
| T-3-33 | Tampering | mutation survivors waved through unjustified | mitigate | 02-08 discipline plus an ADJUSTED score: the 85% threshold applies after excluding only proven-equivalent, re-run-confirmed survivors |
| T-3-38 | Repudiation | comparing recomputed digests, so a reproducibly-wrong order passes | mitigate | cross-process comparison over RECORDED digests, plus a SEPARATE recorded-vs-expected-replay test with distinct failure meaning |
| T-3-62 | Repudiation | a closing audit narrower than the declared validation contract | mitigate | full workspace suite and both scoped clippy gates added to the audit, with rcs recorded |
| T-3-SC | Tampering | package installs | accept | none this plan (trybuild is an existing workspace dep) |
</threat_model>

<verification>
- All scoped Phase 3 test commands exit 0 (train lib, core lib, ui, setfit_repro, gemm_thread_determinism)
- `cargo test --workspace --lib --exclude aprender-profile` exits 0
- tier3's four Phase 3 targets (contract-audit-phase3, setfit-repro-crossproc, gemm-thread-determinism, setfit-feature-matrix) exit 0 standalone
- Adjusted mutation score >= 85% per scope
</verification>

<success_criteria>
- TRN-01 and the review's two new invariants proven by seven reviewed compile-fail cases
- TRN-06 proven cross-process at differing fixed thread counts with all ten recorded components hash-equal, and separately proven to have consumed the intended order
- Phase evidence booked honestly against the declared full suite; known-red states re-measured and recorded
</success_criteria>

<output>
Create `.planning/phases/03-faithful-two-stage-trainer-and-head/03-10-SUMMARY.md` when done.
Record: the mutation table with raw and adjusted scores, the induced-red evidence for both new gates,
the full closing-audit rc list, the exact cargo-mutants invocations used, and the confirmation that
the integration test reached the fixture through an existing `pub fn` with no production backdoor.
</output>
