---
phase: 04-apr-artifact-and-production-parity
plan: 16
type: execute
wave: 5
depends_on: ["04-05", "04-13"]
files_modified:
  - crates/aprender-train/src/train/setfit/apr_reload.rs
  - crates/aprender-train/src/train/setfit/mod.rs
autonomous: true
requirements: [APR-04, TRN-07]
must_haves:
  truths:
    - "A FRESH process can obtain a SetFitRun<ArtifactReloadedAndVerified> from artifact bytes plus the Phase 2 dataset and selection manifest — the state every lock, token and grant door requires"
    - "The reload mints that state ONLY by re-entering the existing trusted verify_artifact policy — there is no second minting path and no second tolerance (Pitfall 9)"
    - "The reload refuses an artifact whose recorded selection semantic hash, selection ledger hash or dataset fingerprint does not match the supplied selection and dataset — typed, each naming both values"
    - "The reload refuses to mint unless re-serialization reproduces the ORIGINAL file bytes: the minted artifact hash must equal artifact_sha256_hex of the input"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/apr_reload.rs"
      provides: "reload_verified_run_from_apr — the fresh-process door to a verified run"
      contains: "reload_verified_run_from_apr"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/apr_reload.rs"
      to: "SetFitRun::verify_artifact"
      via: "the reload re-enters the EXISTING trusted policy rather than minting on its own"
      pattern: "verify_artifact"
    - from: "crates/aprender-train/src/train/setfit/apr_reload.rs"
      to: "aprender::setfit::artifact::load_setfit_apr"
      via: "production loader ladder incl. probe replay runs before any run is assembled"
      pattern: "load_setfit_apr"
---

<objective>
Close a gap the cross-AI review exposed indirectly and that plan-time code reading confirmed
outright: `SetFitRun::verify_artifact` is the ONLY producer of
`SetFitRun<ArtifactReloadedAndVerified>` and it consumes a live, just-trained
`SetFitRun<HeadFitted>`. There is no path from artifact bytes to that state. But
`create_selection_lock`, `mint_test_token` and `CanonicalTestAccess::grant` are all methods on
`SetFitRun<ArtifactReloadedAndVerified>` — so a separate `apr eval` process, which is exactly
D-16/TRN-07's "a user can" tier, cannot reach any of them. 04-07 as previously written referenced a
"Ph3 bytes-reconstruction path" that does not exist.

The fix deliberately does NOT add a second minting policy. It rebuilds the run's parts from the
artifact and then re-enters the existing trusted `verify_artifact` policy verbatim, which closes,
hashes, drops, deserializes, closure-checks, rebuilds, re-probes and compares at `Tolerance::EXACT`
exactly as it does at train time. The reload therefore inherits every guarantee the policy already
proves, and adds two of its own: a provenance identity gate over all three recorded identifiers, and
a byte-identity gate — the re-serialized bytes must equal the ORIGINAL file, so a reload that would
have produced a different artifact refuses to mint.

Purpose: make TRN-07's positive tier reachable from a fresh process without weakening APR-04.

Output: apr_reload.rs, the crate-internal run-from-parts constructor, tests.

Branch: `gsd/phase-4-apr-parity`. Wave 5, parallel with 04-06 (apr-cli only) and 04-12
(one aprender-train integration-test file). HARD CONSTRAINT: this plan touches ONLY the new
apr_reload.rs and mod.rs. If the parts needed to assemble a `SetFitRun<HeadFitted>` cannot be
constructed honestly from the artifact — a value that would have to be invented rather than
recovered — STOP and surface it as a finding rather than defaulting it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
Verified on this branch:
- `SetFitRun<S> { encoder: SetFitMiniLm, dataset: PreparedDataset<Canonical>, selection: Selection, config: ResolvedSetFitConfig, evidence: S::Evidence, _state: PhantomData }` (mod.rs:488-495) — private fields, same crate
- `SetFitRun::<HeadFitted>::verify_artifact<C: SetFitCodec>(self, codec) -> Result<SetFitRun<ArtifactReloadedAndVerified>, SetFitTrainError>` (mod.rs:768) — PUBLIC, the trusted policy this plan re-enters
- `SetFitRun::<ArtifactReloadedAndVerified>::artifact_hash()`, `selection_semantic_hash()` (mod.rs:836+), `create_selection_lock` (lock.rs:470+)
- `SetFitTrainConfig::resolve(self) -> Result<ResolvedSetFitConfig, SetFitConfigError>` (config.rs:614) — PUBLIC; it PROBES this host, which is the honest behaviour on reload (a recorded device is a record of another run, not a claim about this one)
- `SetFitMiniLm::from_bundle_parts` (core, public); `MultinomialLogisticRegression::from_stored_coefficients` (core, public)
- `Selection::semantic_hash() -> [u8;32]`, `Selection::ledger_hash() -> [u8;32]`, `PreparedDataset::<Canonical>::validation_witness().dataset_fingerprint_hex()`
- Selection reconstruction from the persisted manifest (aprender-contrastive-data select.rs:470-511): the replay path reads `payload.ledger_hash`, REBUILDS an `AccessLedger` from `payload.access_ledger` and REFUSES a mismatch (`SelectionReplayMismatch { field: "access_ledger" }`), then assembles the `Selection` with that same recorded hash. The ledger hash is therefore a deterministic function of the manifest, identical in every process that loads it — which is why step 3 below COMPARES it
From 04-03/04-05 (on branch):
- `load_setfit_apr(bytes) -> Result<VerifiedSetFitModel, SetFitArtifactError>` — the FULL production ladder incl. probe replay
- `read_setfit_apr_parts(bytes) -> Result<SetFitAprParts, SetFitArtifactError>` — doc + HF tensors + head coefficients + tokenizer bytes
- `AprCodec` + `APR_FORMAT_ID`; `artifact_sha256_hex(bytes)`
From 04-13: `bundle.provenance` / `doc.provenance` carries dataset_fingerprint, validation_split_fingerprint, selection_semantic_hash, selection_ledger_hash, selection_root_seed, shots_per_class
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reload_verified_run_from_apr — rebuild the parts, re-enter the trusted policy</name>
  <files>crates/aprender-train/src/train/setfit/apr_reload.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/mod.rs lines 420-500 (LifecycleState impls and the SetFitRun struct — the exact evidence associated type each state carries), 700-810 (fit_head's construction of SetFitRun<HeadFitted> — the shape this plan's constructor mirrors, and verify_artifact itself)
    - crates/aprender-train/src/train/setfit/verify.rs lines 240-330 and 555-660 (Tolerance::EXACT, the probe/report types the HeadFitted evidence carries, and run_verify_policy's use of them — read to confirm which values are RECOVERABLE from an artifact and which are not)
    - crates/aprender-contrastive-data/src/select.rs lines 470-515 (the manifest replay path: it reads `payload.ledger_hash`, rebuilds the ledger from `payload.access_ledger` and refuses a mismatch, then assembles the Selection with the recorded hash — the fact that makes the ledger hash comparable across processes)
    - crates/aprender-train/src/train/setfit/apr_codec.rs (AprCodec from 04-05 — the codec the re-entered policy uses)
    - contracts/setfit-apr-v1.yaml item 13 (the selection-lock lifecycle this door exists to make possible)
  </read_first>
  <behavior>
    - Test: a fixture run trained and written to APR bytes reloads through reload_verified_run_from_apr into an ArtifactReloadedAndVerified run whose artifact_hash equals artifact_sha256_hex of the input bytes
    - Test: the reloaded run can reach create_selection_lock -> mint_test_token -> CanonicalTestAccess::grant (the TRN-07 chain, proven at library level here and at CLI level in 04-07)
    - Test: a selection whose semantic hash differs from the artifact's recorded provenance -> typed refusal naming both hashes
    - Test: a selection whose LEDGER hash differs from the artifact's recorded provenance -> typed refusal naming both hashes, distinct from the semantic-hash variant and distinct from the step-8 artifact-hash refusal
    - Test: a dataset whose fingerprint differs from the artifact's recorded provenance -> typed refusal naming both fingerprints
    - Test: a selection reconstructed twice from the SAME persisted manifest yields the same ledger hash, so the step-3 comparison passes on the honest reload path (the witness that this check is not process-dependent)
    - Test: corrupted artifact bytes -> the production loader's typed failure, surfaced without a run ever existing
    - Test: bytes whose re-serialization would differ from the input (induce by editing a byte and re-signing the container) -> typed refusal; no state is minted
  </behavior>
  <action>
    New `crates/aprender-train/src/train/setfit/apr_reload.rs` with
    `pub fn reload_verified_run_from_apr(bytes: &[u8], dataset: PreparedDataset<Canonical>, selection: Selection) -> Result<SetFitRun<ArtifactReloadedAndVerified>, SetFitTrainError>`,
    in this order:
    1. `load_setfit_apr(bytes)` FIRST — the production loader's full ladder including D-11 probe
       replay. A consumer that could not verify the artifact must never get as far as assembling a
       run. Map the typed `SetFitArtifactError` into the existing `SetFitTrainError` codec/bundle
       arm (via `CodecError::Artifact` from 04-05) so the caller keeps a typed source.
    2. `read_setfit_apr_parts(bytes)` for the doc, HF-named tensors, head coefficients and
       tokenizer bytes.
    3. Provenance identity gate, BEFORE any rebuild — all THREE recorded identifiers, each a
       separate typed refusal naming both observed values:
       (a) `doc.provenance.selection_semantic_hash` == `hex::encode(selection.semantic_hash())`;
       (b) `doc.provenance.selection_ledger_hash` == `hex::encode(selection.ledger_hash())`;
       (c) `doc.provenance.dataset_fingerprint` == `dataset.validation_witness().dataset_fingerprint_hex()`.
       The ledger hash IS compared, and the comment must say why rather than why not: the ledger
       hash is not live process state, it is a value carried by the persisted selection manifest —
       select.rs:479-502 reads `payload.ledger_hash`, rebuilds the ledger from
       `payload.access_ledger`, refuses any mismatch, and assembles the `Selection` with that same
       recorded hash. Two processes loading the same manifest therefore hold the same ledger hash,
       so comparing it distinguishes "the selection this artifact was trained under" from "a
       different selection over the same dataset with the same shots" — which the semantic hash
       alone does not. Do NOT write the earlier draft's rationale that a fresh process legitimately
       has its own ledger state: it is false for a manifest-loaded Selection and would have talked a
       later reader out of a check that works.
       State the relationship to step 8 explicitly in the same comment: the byte-identity gate would
       also catch a divergent ledger hash, because 04-13 makes `selection_ledger_hash` a serialized
       provenance field — but it would surface as an opaque artifact-hash mismatch. Naming the
       refusal here is what makes the diagnosis readable, and it fails before any rebuild work.
    4. Rebuild the encoder with `SetFitMiniLm::from_bundle_parts` (tokenizer bytes + architecture +
       HF tensors + `doc.root_seed`) and the head with
       `MultinomialLogisticRegression::from_stored_coefficients` over the head tensors and
       `doc.ordered_labels`.
    5. Resolve the configuration: deserialize `doc.requested_config` into `SetFitTrainConfig` (its
       Deserialize routes through the validating constructor) and call `resolve()`, which PROBES
       THIS host. Comment: the recorded `resolved_config.resolved_device` describes the machine that
       trained, not the machine reloading; restating it would be labelling a run by intent
       (CLAUDE.md rule 2). An explicitly unavailable device fails closed here, which is OPS-06 reuse.
    6. Assemble `SetFitRun<HeadFitted>` through a new `pub(crate) fn from_reloaded_parts(...)` added
       to mod.rs beside the existing state transitions, carrying the rebuilt encoder, the supplied
       dataset and selection, the resolved config, and the HeadFitted evidence assembled from
       `doc.ordered_labels`, the deserialized `doc.evidence` summary and the rebuilt head. Every
       value must be RECOVERED from the artifact or SUPPLIED by the caller — if the evidence type
       requires a value that is neither, STOP and surface it rather than defaulting it.
    7. `run.verify_artifact(&AprCodec)` — the EXISTING trusted policy, called unchanged. This is
       the whole point: there is one minting path in this crate and this door goes through it.
    8. Final gate: the minted run's `artifact_hash()` must equal `artifact_sha256_hex(bytes)`. A
       reload that re-serializes to different bytes has not reproduced the artifact, and must not
       mint — typed refusal naming both hashes.
    Module doc must state the trust argument plainly: this door does not add a verification policy,
    it re-enters one; the state it produces carries the same evidence the train-time path produces,
    plus the provenance identity gate in step 3 and the byte-identity gate in step 8; and it is
    `pub` because D-16's whole point is that a fresh process can reach the lock workflow.
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --lib setfit::apr_reload::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --lib setfit::apr_reload::` exits 0 with >= 8 tests passed
    - Source assertion: apr_reload.rs calls `verify_artifact` (grep >= 1) and contains NO construction of an `ArtifactReloadedAndVerified` value of its own (grep for `ArtifactReloadedAndVerified` shows it only in the return type and imports)
    - Source assertion: `load_setfit_apr` is called BEFORE the first `from_bundle_parts` in the function body (rung order visible in code)
    - Source assertion: `grep -c "selection_ledger_hash" crates/aprender-train/src/train/setfit/apr_reload.rs` >= 1 — the ledger hash IS compared, with its own typed refusal variant distinct from the semantic-hash and artifact-hash variants (three distinct variants, asserted with matches! in three separate tests)
    - Source assertion: the phrase "own ledger state" does NOT appear in apr_reload.rs — the earlier draft's incorrect rationale must not ship in any form
    - The comment beside the ledger comparison cites select.rs's manifest replay path as the reason the value is process-independent, and states that step 8 would otherwise surface the same divergence as an opaque hash mismatch
    - The TRN-07 chain test reaches `CanonicalTestAccess::grant` and asserts the returned grant's artifact_hash equals the artifact's
  </acceptance_criteria>
  <done>A fresh process can hold a verified run, and it holds one only because the one trusted policy said so — with every recorded identifier checked by name rather than absorbed into a hash mismatch</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact bytes + user-supplied dataset/selection -> verified run | the state that unlocks canonical test data is minted here; a mismatched pair must never produce one |
| reload path -> minting policy | a second minting path would be a second, weaker verification policy (Pitfall 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-46 | Elevation of Privilege | a second minting path with weaker evidence | mitigate | the door re-enters verify_artifact; grep asserts no local construction of the verified state |
| T-04-47 | Spoofing | reloading an artifact against a dataset/selection it was not trained on | mitigate | provenance identity gate on selection semantic hash, selection LEDGER hash and dataset fingerprint, before rebuild, each a distinct typed refusal naming both values |
| T-04-48 | Tampering | an artifact that reloads into something else | mitigate | step 8 requires the minted hash to equal the input file's hash; a reload that re-serializes differently refuses to mint |
| T-04-49 | Repudiation | restating the recorded device as this host's | mitigate | resolve() probes this host; the recorded device stays a record, not a claim |
| T-04-60 | Repudiation | a check declined on a rationale that is false | mitigate | the ledger hash is compared and its comment cites select.rs's manifest replay path; a grep asserts the earlier incorrect rationale text is absent from the shipped source |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Scoped filter green with counted tests; `cargo test -p aprender-train --features setfit --lib setfit::` shows no NEW failures against the known-red baseline
- `git diff --name-only` lists only apr_reload.rs and mod.rs (wave-5 ownership contract with 04-06 and 04-12)
</verification>

<success_criteria>
- TRN-07's lock -> token -> grant chain is reachable from artifact bytes in a fresh process, at library level, with no new verification policy
- Every provenance identifier the artifact records is checked by a named typed refusal, not left to the byte-identity gate to report as an opaque mismatch
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-16-SUMMARY.md` when done.
Record: the exact public signature, the three provenance refusal variants and their messages, the
HeadFitted evidence values and where each was recovered from, test counts, and any value that could
not be recovered honestly (a finding, if it occurred).
</output>
