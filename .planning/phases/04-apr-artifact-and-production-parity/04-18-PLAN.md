---
phase: 04-apr-artifact-and-production-parity
plan: 18
type: execute
wave: 11
gap_closure: true
depends_on: []          # patches code delivered by 04-01..04-17; no NEW plan precedes it in wave 11
files_modified:
  - crates/apr-cli/src/setfit_tag.rs
  - crates/apr-cli/src/setfit_tag_tests.rs
  - crates/apr-cli/src/commands/inspect.rs
  - crates/apr-cli/src/commands/inspect_tests.rs
  - crates/apr-cli/src/commands/predict_tests.rs
  - crates/apr-cli/src/dispatch_analysis.rs
autonomous: true
requirements: [APR-02, APR-05, OPS-03]
closes_findings: [WR-09, WR-10-adjacent-none, WR-08]

must_haves:
  truths:
    - "A hostile APR whose header declares a 4 GiB metadata block is refused by `apr inspect` without allocating 4 GiB"
    - "An artifact whose SetFit metadata block exceeds the 16 MiB cap is described the SAME WAY by `apr predict`, `apr eval --task classify` and `apr inspect` — none of them says it is not a SetFit artifact"
    - "`apr serve` keeps its pre-existing fall-through to the plain-APR path for the same file"
    - "A legitimate in-repo APR fixture is unaffected: its inspect output is byte-identical to what it was before the cap"
  artifacts:
    - path: "crates/apr-cli/src/setfit_tag.rs"
      provides: "MAX_TAG_METADATA_BYTES promoted to pub(crate); over-cap becomes a typed InvalidFormat error naming the cap, not Ok(None)"
      contains: "pub(crate) const MAX_TAG_METADATA_BYTES"
    - path: "crates/apr-cli/src/commands/inspect.rs"
      provides: "read_metadata bounded by the absolute cap BEFORE the vec! allocation, and the over-cap fact disclosed rather than silently defaulted"
      contains: "MAX_TAG_METADATA_BYTES"
    - path: "crates/apr-cli/src/setfit_tag_tests.rs"
      provides: "the over-cap refusal and the four-consumer agreement table"
  key_links:
    - from: "crates/apr-cli/src/commands/inspect.rs"
      to: "crates/apr-cli/src/setfit_tag.rs"
      via: "the ONE absolute metadata cap constant, referenced not re-declared"
      pattern: "crate::setfit_tag::MAX_TAG_METADATA_BYTES"
    - from: "crates/apr-cli/src/commands/serve/handlers.rs"
      to: "crates/apr-cli/src/setfit_tag.rs"
      via: ".ok().flatten() — the fall-through the new Err must not break"
      pattern: "read_setfit_tag\\(model_path\\)\\s*\\n?\\s*\\.ok\\(\\)"
  caveats:
    - "This plan does NOT close F-10. OPS-01, OPS-02, SC1, SC2, SC3 and the 'over a produced artifact' halves of SC4/SC5 remain OPEN after this run — the production-encoder calibration that unblocks them is Phase 5 work per the ROADMAP's explicit blocking note."
    - "This plan does NOT close the per-crate cargo-mutants gate (04-11 must-have 4) or the SAFE-02 'in CI' clause. Both stay human_verification items."
    - "WR-01, WR-03, WR-04, WR-05, WR-06 and IN-01/02/04/05/06/07 from 04-REVIEW.md remain open and untouched by this plan."
---

<objective>
Close the two round-2 review findings on the `apr-cli` artifact-detection surface: **WR-09**, an
unbounded attacker-controlled allocation in `apr inspect`, and **WR-08**, the fail-open that makes
three tools state contradictory things about one file.

Purpose: `apr inspect` is the tool CLAUDE.md mandates as diagnostic step 1. Today a 64-byte header
on a sparse file makes it allocate ~4 GiB and then report success, and the sibling detector's
`Ok(None)` fail-open makes `apr predict` tell an operator that a genuinely tagged classifier "is
not a SetFit classifier" while pointing them at `apr inspect`, which renders the full APR-05
section for the same bytes.

Output: one absolute metadata cap shared by both readers, a typed over-cap refusal that says
something true on every consumer, and an executed four-consumer agreement table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: work on the CURRENT branch `gsd/phase-2-contract-gate`. Do NOT create, rename or switch
branches. Do NOT open a PR.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEW.md
@.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md

Measured at HEAD `81652bb50` during planning — do not re-derive, but do re-confirm before editing:

- `crates/apr-cli/src/setfit_tag.rs:48` — `const MAX_TAG_METADATA_BYTES: u64 = 16 * 1024 * 1024;`
  is **private**. Promoting it is a prerequisite for the WR-09 fix.
- `crates/apr-cli/src/setfit_tag.rs:141-143` — the `if declared > MAX_TAG_METADATA_BYTES { return
  Ok(None); }` fail-open, whose justifying comment at 133-140 is true for `apr serve` only.
- `crates/apr-cli/src/commands/inspect.rs:535-566` — `read_metadata`. The bound at 555-561 is
  `metadata_offset + declared <= file_len` and nothing else; line 563 is
  `vec![0u8; header.metadata_size as usize]`.
- The three `read_setfit_tag` consumers, all confirmed present at HEAD:
  `crates/apr-cli/src/commands/predict.rs:114` (`?`-propagates),
  `crates/apr-cli/src/dispatch_analysis.rs:759` (`?`-propagates),
  `crates/apr-cli/src/commands/serve/handlers.rs:774` (`.ok().flatten()`, swallows).
  Because predict and dispatch already propagate with `?`, converting the over-cap case to `Err`
  needs **no change in either file** — which is why this fix is small.
- `crates/apr-cli/src/commands/inspect.rs` includes its tests with `include!("inspect_tests.rs")`
  and `include!("inspect_setfit_tests.rs")` at end of file (not `#[path] mod`).
- `MetadataInfo` (inspect.rs:66-132) uses `#[serde(skip_serializing_if = "Option::is_none")]` on
  every optional non-provenance field. A new optional field with the same attribute leaves every
  existing `--json` output byte-identical.
- Fixture builder to reuse: `crate::setfit_tag::test_support::write_setfit_shaped_apr(dir, name,
  model_type, doc)` at `setfit_tag.rs:197` — it writes a REAL APR v2 container through the
  production writer.

<interfaces>
Contracts the executor needs; do not go looking for them.

From `crates/apr-cli/src/setfit_tag.rs`:
  const MAX_TAG_METADATA_BYTES: u64 = 16 * 1024 * 1024;   // currently private
  pub(crate) const SETFIT_MODEL_TYPE: &str = "setfit";
  pub(crate) const SETFIT_CUSTOM_KEY: &str = "setfit";
  pub(crate) struct SetFitTag { pub(crate) doc: Option<serde_json::Value> }
  pub(crate) fn read_setfit_tag(path: &Path) -> Result<Option<SetFitTag>, CliError>;
  #[cfg(test)] pub(crate) mod test_support {
      pub(crate) fn write_setfit_shaped_apr(dir: &Path, name: &str, model_type: &str,
                                            doc: Option<&str>) -> PathBuf;
  }

From `crates/apr-cli/src/commands/inspect.rs`:
  fn read_metadata(reader: &mut BufReader<File>, header: &HeaderData) -> MetadataInfo;
  struct HeaderData { version, flags, tensor_count, metadata_offset: u64,
                      metadata_size: u32, tensor_index_offset, data_offset, checksum_valid }
  struct MetadataInfo { model_type: Option<String>, ..., #[serde(skip)] setfit_doc: Option<Value> }

From `crates/apr-cli/src/error.rs`:
  CliError::InvalidFormat(String)   // the variant `read_setfit_tag` already returns for a
                                    // block that runs past EOF (setfit_tag.rs:125)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-09 — bound `apr inspect`'s metadata allocation by the absolute cap, proven with a sparse hostile file</name>

  <read_first>
    - `crates/apr-cli/src/commands/inspect.rs` lines 60-135 (`MetadataInfo`) and 500-640
      (`read_header` tail, `read_metadata` in full) — the file being modified.
    - `crates/apr-cli/src/setfit_tag.rs` lines 24-31 (the module header's own argument that a
      file-length bound is not sufficient) and 118-152 (bound (2), cap (2b), read (3)) — the
      SOURCE OF TRUTH for the cap being reused. The comment at 133-140 already states the
      counter-example ("a 30 GB APR whose metadata block declares 20 MiB passes (2)").
    - `crates/apr-cli/src/commands/inspect_tests.rs` — the existing test conventions and the
      fixture helpers already in scope.
    - `CLAUDE.md` § "Verification Discipline" rules 1 and 3.
  </read_first>

  <behavior>
    - Test 1 (RED first): declare `metadata_size` JUST ABOVE `MAX_TAG_METADATA_BYTES`
      (`16 * 1024 * 1024 + 1`), NOT `0xFFFF_FFFF`. This is the only value that exercises the new
      cap: measured at HEAD, the pre-existing file-length check at `inspect.rs:555-561` runs
      first, so a 4 GiB declaration on any reasonably-sized temp file is refused by the OLD bound
      and the new one is never reached — the test would pass before the fix and prove nothing.
      The temp file must therefore be padded past `metadata_offset + 16 MiB` so the length check
      passes and the cap is what fires; use `File::set_len`, which is sparse and instant and costs
      no disk. Assert `MetadataInfo` comes back with `model_type == None` and the new over-cap
      disclosure set.
    - Test 2: a declared size of exactly `MAX_TAG_METADATA_BYTES` is NOT refused by the cap (the
      cap is `>`, not `>=`), so the boundary is pinned in the same direction as
      `setfit_tag.rs:141`.
    - Test 3 (non-vacuity control): a legitimate fixture written by
      `test_support::write_setfit_shaped_apr` still parses and still yields its `model_type`, so
      the cap cannot pass by refusing everything.
  </behavior>

  <files>crates/apr-cli/src/setfit_tag.rs, crates/apr-cli/src/commands/inspect.rs, crates/apr-cli/src/commands/inspect_tests.rs</files>

  <action>
Promote `MAX_TAG_METADATA_BYTES` in `crates/apr-cli/src/setfit_tag.rs:48` from `const` to
`pub(crate) const`, keeping the value `16 * 1024 * 1024` and its existing doc comment, and extend
that doc comment with one sentence recording that `commands/inspect.rs::read_metadata` now shares
it so the two readers cannot answer differently about the same block — the same "one detection
point means one bound" argument the constant's doc already makes for the deleted `serve` copy.

In `crates/apr-cli/src/commands/inspect.rs::read_metadata` (currently at line 535), insert an
absolute-cap check AFTER the existing `metadata_offset + declared <= m.len()` check at 555-561 and
BEFORE the `vec![0u8; header.metadata_size as usize]` allocation at 563. The check compares the
same `declared: u64` against `crate::setfit_tag::MAX_TAG_METADATA_BYTES` with `>`. Keep the
file-length check: it is tighter for a small file, the cap is tighter for a large one, and the
setfit_tag module header already argues both are needed. On over-cap, return the same
`MetadataInfo` shape the function already returns for an unparseable block, with the new
disclosure field set (Task 3 adds the field; for this task set it to `Some(declared)` and add the
field as part of this task's diff so the test can observe it).

Add the disclosure field to `MetadataInfo` as `metadata_over_cap_bytes: Option<u64>` carrying
`#[serde(skip_serializing_if = "Option::is_none")]`, so every existing `--json` output for a
legitimate artifact stays byte-identical and no `output_json` / `output_json_with_quality` call
site changes.

Replace the block comment at inspect.rs:548-554 rather than appending to it: its current text
argues that the stat'd length "is a sound bound that needs no invented constant", which is the
claim this change refutes. State instead that the file length bounds a small file and the absolute
cap bounds a large one, and cite the 30 GB counter-example by name.

Write the three tests into `crates/apr-cli/src/commands/inspect_tests.rs`. Build the hostile
header bytes directly rather than via `truncate -s`: a unit test that constructs `HeaderData` with
the hostile `metadata_size` and a `BufReader<File>` over a real temp file whose length satisfies
the file-length bound is deterministic, needs no sparse-filesystem support, and runs in every
`cargo test`. Do not use `unwrap()` — the crate bans it; use `expect(..)` with a reason.
  </action>

  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib inspect > /tmp/04-18-t1.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "test result:" /tmp/04-18-t1.log</automated>
  </verify>

  <acceptance_criteria>
    - `rtk proxy grep -c 'pub(crate) const MAX_TAG_METADATA_BYTES' crates/apr-cli/src/setfit_tag.rs`
      returns exactly 1.
    - `rtk proxy grep -c 'MAX_TAG_METADATA_BYTES' crates/apr-cli/src/commands/inspect.rs` returns
      at least 1, and the match is a REFERENCE to `crate::setfit_tag::MAX_TAG_METADATA_BYTES`, not
      a second `const` declaration: `rtk proxy grep -c 'const MAX_' crates/apr-cli/src/commands/inspect.rs`
      returns 0.
    - In `read_metadata`, the cap comparison appears at a lower line number than the
      `vec![0u8; header.metadata_size as usize]` allocation. Verify by reading the two line numbers
      out of `rtk proxy grep -n 'MAX_TAG_METADATA_BYTES\|vec!\[0u8' crates/apr-cli/src/commands/inspect.rs`
      and asserting the cap line is smaller.
    - `cargo test -p apr-cli --features setfit --lib inspect` exits 0 with a passed count of at
      least 123 (measured baseline at HEAD: 120 via `make setfit-cli-inspect-tests`, floor 110).
      Capture the status with `cmd > log 2>&1; rc=$?` — never through a pipe.
    - RED was observed before GREEN: run the new Test 1 against the pre-change `read_metadata`
      (stash the inspect.rs cap hunk, or temporarily comment the cap) and record in the SUMMARY the
      exact failure line it produced. A test that has only ever been seen passing is not evidence.
    - `make setfit-cli-inspect-tests` exits 0 and its `assert_tests_ran` floor of 110 is satisfied.
  </acceptance_criteria>

  <done>`apr inspect`'s metadata read is bounded by an absolute 16 MiB cap taken from the single shared constant, the cap fires before the allocation, a legitimate fixture is unaffected, and the RED-before-GREEN transition is recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-08 — make the over-cap case a typed refusal so predict and eval stop saying something false</name>

  <read_first>
    - `crates/apr-cli/src/setfit_tag.rs` in full — the file being modified; in particular the
      module header at 53-59 ("rather than silently falling back to 'plain APR', which would report
      a corrupt classifier as a healthy generic model"), which is the stated bias this task applies
      to the adjacent case, and the `Errors` doc at 79-84 that must gain the new variant reason.
    - `crates/apr-cli/src/setfit_tag_tests.rs` — the existing tag-detection test conventions.
    - `crates/apr-cli/src/commands/predict.rs` lines 38-47 (`SUPPORTED_FAMILIES`) and 110-121 (the
      `?`-propagating call site) — read to CONFIRM no change is needed there, not to change it.
    - `crates/apr-cli/src/dispatch_analysis.rs` lines 755-810 — same, the `?`-propagating call site
      and the flag-refusal table whose message ("does not carry the SetFit tag") is the false
      statement being eliminated.
    - `crates/apr-cli/src/commands/serve/handlers.rs` lines 755-782 — the `.ok().flatten()`
      fall-through that MUST keep working unchanged.
    - `04-REVIEW.md` § WR-08 — it offers two fixes; this plan takes the second ("simpler and
      consistent with the module's own stated bias").
  </read_first>

  <behavior>
    - Test 1 (RED first): an APR v2 container tagged `model_type = "setfit"` whose metadata block
      is in-bounds for the file but larger than `MAX_TAG_METADATA_BYTES` makes `read_setfit_tag`
      return `Err(CliError::InvalidFormat(..))` whose message names the declared size and the cap.
      Today it returns `Ok(None)`.
    - Test 2: a container tagged `qwen2` with an over-cap metadata block ALSO returns the same
      `Err` — the refusal is about the block, not about the tag, and `read_setfit_tag` cannot know
      the tag without reading the block. The message must therefore NOT claim the file is or is not
      a SetFit artifact.
    - Test 3: an in-bounds tagged artifact still returns `Ok(Some(SetFitTag { doc: Some(..) }))`,
      and an in-bounds untagged one still returns `Ok(None)` — the two pre-existing dispositions
      are unchanged, so the new error cannot pass by refusing everything.
    - Test 4: a block that runs past EOF still returns the PRE-EXISTING `InvalidFormat` naming the
      file length, and its message is distinguishable from the new one — two different malformations
      must not collapse into one diagnosis.
  </behavior>

  <files>crates/apr-cli/src/setfit_tag.rs, crates/apr-cli/src/setfit_tag_tests.rs, crates/apr-cli/src/commands/predict_tests.rs</files>

  <action>
In `crates/apr-cli/src/setfit_tag.rs`, replace the `return Ok(None);` at line 142 (inside the
`if declared > MAX_TAG_METADATA_BYTES` block at 141-143) with a
`return Err(CliError::InvalidFormat(..))` whose message names, in this order: the path, the
declared byte count, and the cap in bytes with its `16 MiB` rendering. The message must NOT assert
anything about whether the file is a SetFit artifact — that fact is unknown at this point because
the block has not been read.

Rewrite the comment block at 133-140 so it no longer says "the caller falls through to the
pre-existing APR path, which has its own and better diagnosis". Record instead what was measured:
that sentence was true for `apr serve` only, because `apr predict` has no plain-APR path and
`apr eval --task classify` refuses the flags with a statement about the SetFit tag that is false
for a file that carries one. State that `serve` keeps its fall-through via `.ok().flatten()` at
`serve/handlers.rs:774` and that the other two consumers now propagate a true statement.

Update the `# Errors` doc section at 79-84 to list the new reason: `CliError::InvalidFormat` for a
metadata block that is in-bounds for the file but over the identification cap, distinct from the
existing "does not fit inside the file" reason.

Change NOTHING in `predict.rs`, `dispatch_analysis.rs` or `serve/handlers.rs`. The first two
already `?`-propagate and the third already swallows with `.ok().flatten()`. Confirm this by
reading those three call sites and record the confirmation in the SUMMARY; if any of them turns
out not to propagate as recorded, stop and report rather than editing outside this plan's
`files_modified`.

Add Tests 1-4 to `crates/apr-cli/src/setfit_tag_tests.rs`. Build the over-cap fixture by writing a
real APR v2 container and then rewriting only the 4-byte `metadata_size` field in the header (the
declared value is what is under test; the block does not have to actually be that large, and
making it that large would put a 16 MiB write in every `cargo test`). Extend the file length with
zero padding so the pre-existing `end > file_len` check at 124 does not fire first — the ORDER of
the two checks is what Test 4 pins. Add one test to
`crates/apr-cli/src/commands/predict_tests.rs` asserting that `predict::run` on the over-cap
fixture surfaces the cap message and does NOT emit the `SUPPORTED_FAMILIES` "not a SetFit
classifier" text.
  </action>

  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib setfit_tag > /tmp/04-18-t2a.log 2>&1; rc=$?; echo "tag_rc=$rc"; cargo test -p apr-cli --features setfit --lib predict > /tmp/04-18-t2b.log 2>&1; rc2=$?; echo "predict_rc=$rc2"; grep -E "test result:" /tmp/04-18-t2a.log /tmp/04-18-t2b.log</automated>
  </verify>

  <acceptance_criteria>
    - `rtk proxy grep -c 'return Ok(None);' crates/apr-cli/src/setfit_tag.rs` is one LOWER than at
      HEAD (measured at HEAD: the over-cap arm is one of several `Ok(None)` returns; record both
      counts in the SUMMARY rather than asserting an absolute number).
    - The over-cap arm returns `Err`: `rtk proxy grep -n 'MAX_TAG_METADATA_BYTES' -A 6
      crates/apr-cli/src/setfit_tag.rs` shows `CliError::InvalidFormat` inside the `if declared >`
      block.
    - The fix needs no consumer LOGIC edits: `rtk proxy git diff --name-only` does NOT list
      `crates/apr-cli/src/commands/predict.rs` or `crates/apr-cli/src/commands/serve/handlers.rs`,
      and `rtk proxy git diff -- crates/apr-cli/src/dispatch_analysis.rs` is empty AT THE END OF
      THIS TASK. (Task 3 adds a one-keyword visibility seam to `dispatch_analysis.rs`; that is a
      test-reachability change, not a consumer edit, and it must not appear until then.)
    - `cargo test -p apr-cli --features setfit --lib setfit_tag` exits 0 and its `test result:` line
      shows a passed count strictly greater than the HEAD baseline (record the HEAD baseline by
      running the same command before editing).
    - `cargo test -p apr-cli --features setfit --lib predict` exits 0 with at least 35 passed
      (measured baseline 34 via `make setfit-cli-predict-tests`, floor 30).
    - Behaviour assertion, executed: for the over-cap fixture, the `apr predict` refusal text
      contains the substring `16 MiB` and does NOT contain the substring
      `not a SetFit classifier`. Assert both directions in the same test.
    - `make setfit-cli-predict-tests` exits 0.
  </acceptance_criteria>

  <done>An over-cap metadata block is one typed refusal that every consumer either propagates truthfully or deliberately swallows, no consumer file changed, and the two malformation diagnoses stay distinguishable.</done>
</task>

<task type="auto">
  <name>Task 3: the four-consumer agreement table — an executed proof that no two tools contradict each other about one file</name>

  <files>crates/apr-cli/src/commands/inspect.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/setfit_tag_tests.rs</files>

  <read_first>
    - `crates/apr-cli/src/setfit_tag_tests.rs` as modified by Task 2 — the fixture builder this
      task reuses.
    - `crates/apr-cli/src/commands/inspect.rs` `read_metadata` as modified by Task 1 — the
      `metadata_over_cap_bytes` disclosure this task renders.
    - `crates/apr-cli/src/commands/inspect_setfit.rs` lines 30-45 — the module's own statement of
      what the APR-05 section exists to avoid, which is the standard this table holds inspect to.
    - `crates/apr-cli/src/commands/inspect_output_json.rs` `output_json` signature (line 6) — read
      to CONFIRM the new field needs no call-site change, not to change it.
    - `CLAUDE.md` § "Verification Discipline" rule 5 (a guard that does not scan the surface where
      the DECISION is made is theater) — the four consumers are the four decision surfaces here.
  </read_first>

  <action>
Render the over-cap disclosure in `apr inspect`'s human-readable output. Find the metadata
rendering site in `crates/apr-cli/src/commands/inspect.rs` (the non-`--json` path that prints
model type, name, author and the provenance keys) and emit one line when
`metadata.metadata_over_cap_bytes` is `Some(n)`, naming `n` and the cap and saying explicitly that
the metadata block was NOT read, so no `model_type` and no SetFit section could be derived from it.
Do not print the line when the field is `None`, so existing output is unchanged.

**Two of the four surfaces are unreachable from a test module today — bump their visibility before
writing the table.** Measured at HEAD: `commands::inspect::read_metadata` is a private `fn` at
`inspect.rs:535`, and `dispatch_analysis::dispatch_classify_eval` is a private `fn` at
`dispatch_analysis.rs:744` in a file that exposes ZERO `pub(crate) fn`. Promote both to
`pub(crate) fn` and nothing else — no signature change, no body change, no re-export. Add a
one-line comment on each recording that the visibility exists so the four-consumer agreement table
can reach the decision surface, because a table that only tests the two reachable consumers would
be exactly the "guard that does not scan the surface where the DECISION is made" CLAUDE.md rule 5
calls theater. `dispatch_analysis.rs` is in this plan's `files_modified` for this reason and this
reason only.

Add ONE table-driven test to `crates/apr-cli/src/setfit_tag_tests.rs` that takes the single
over-cap fixture from Task 2 and drives all four decision surfaces against it in one function:
`setfit_tag::read_setfit_tag` (the shared detector), `commands::inspect::read_metadata` (its own
reader), `commands::predict::run`, and `dispatch_analysis::dispatch_classify_eval` (the
`apr eval --task classify` decision point). For each, assert the observable outcome, and assert the
CONJUNCTION that WR-08 names: no surface states or implies that the file is not a SetFit artifact,
and no surface renders a populated APR-05 section for it.

Assert `serve`'s disposition too, and assert it is DIFFERENT and deliberate: the fall-through is
what `serve` needs and it is documented as such. Drive it through the same
`.ok().flatten().is_some()` expression `serve/handlers.rs:774` uses rather than reimplementing it,
so a change to that expression turns this test red.

Name the test so its subject is unmistakable — for example
`four_consumers_agree_about_one_over_cap_file`. Add a doc comment recording the measured HEAD
behaviour this replaces: `apr predict` said "not a SetFit classifier … run apr inspect", and
`apr inspect` rendered the full APR-05 section for the same bytes.
  </action>

  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib four_consumers_agree > /tmp/04-18-t3.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "test result:|four_consumers" /tmp/04-18-t3.log</automated>
  </verify>

  <acceptance_criteria>
    - The named test exists and RAN: the `cargo test` log for the filter
      `four_consumers_agree` shows `1 passed` and NOT `0 passed`. A zero-match filter exits 0 in
      libtest, so assert the passed count, not the exit code alone.
    - The test drives four distinct surfaces AND each is genuinely reachable from where the test
      lands (`crates/apr-cli/src/setfit_tag_tests.rs`, module `crate::setfit_tag`). Verify by
      source assertion that the test body names all four identifiers — `read_setfit_tag`,
      `read_metadata`, `predict` and `dispatch_classify_eval` — and that it COMPILES, which is the
      real proof of reachability. Record the four identifiers in the SUMMARY.
    - The two visibility seams are minimal and are the only change to those declarations:
      `rtk proxy grep -c 'pub(crate) fn read_metadata' crates/apr-cli/src/commands/inspect.rs`
      returns 1, `rtk proxy grep -c 'pub(crate) fn dispatch_classify_eval' crates/apr-cli/src/dispatch_analysis.rs`
      returns 1, and `rtk proxy git diff -- crates/apr-cli/src/dispatch_analysis.rs` shows changes
      confined to that one declaration line plus its new comment. If the diff touches the flag
      table, the refusal messages or any call site, the seam has exceeded its purpose.
    - Mutation control, executed and reverted: revert the Task 2 `Err` to `Ok(None)` and confirm
      the table test turns RED naming the consumer that then lies. Record the exact assertion
      message. Revert the probe.
    - `cargo test -p apr-cli --features setfit --lib inspect` still exits 0 with at least 123
      passed, and `make setfit-cli-inspect-tests` exits 0 — i.e. the human-output change broke no
      existing golden.
    - `cargo test -p apr-cli --features setfit --lib` exits 0 with at least 6750 passed. Measured
      at HEAD `81652bb50` during planning with exactly that command: **6756 passed, 0 failed, 15
      ignored**. The `0fb47958f` figure of 6752 is stale by three commits — re-measure before
      editing and record your own number, and if it differs from 6756 the floor is yours to set
      just below it, not to inherit.
    - `rtk proxy git diff --name-only` does not list
      `crates/apr-cli/src/commands/inspect_output_json.rs` — the `skip_serializing_if` field must
      need no call-site change.
  </acceptance_criteria>

  <done>Two minimal `pub(crate)` visibility seams make all four decision surfaces reachable from a test; one test drives all four over one over-cap file, compiles (which is the proof of reachability), asserts they no longer contradict each other, and was proven able to fail by reverting the Task 2 refusal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied file path → `apr inspect` / `apr predict` / `apr eval` | The APR container is untrusted input. Its 64-byte header, including a `u32 metadata_size`, is attacker-controlled and — measured at `inspect.rs:521` — its checksum is computed but never branched on. |
| operator-supplied file path → `apr serve` startup | Same bytes, different disposition: `serve` deliberately falls through to the plain-APR path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-70 | Denial of Service | `commands/inspect.rs::read_metadata` | mitigate | The live finding. `metadata_size` up to `0xFFFF_FFFF` bounded only by file length; `truncate -s 4297M` is free on a sparse filesystem, so ~4 GiB is allocated and the command then SUCCEEDS. On a memory-limited container this OOM-kills the tool CLAUDE.md mandates as diagnostic step 1. Task 1 applies the absolute 16 MiB cap BEFORE the `vec![0u8; ..]`, sharing `setfit_tag::MAX_TAG_METADATA_BYTES` so the two readers cannot drift. |
| T-04-71 | Spoofing / Repudiation | `setfit_tag::read_setfit_tag` over-cap `Ok(None)` | mitigate | An attacker who inflates the SetFit document past 16 MiB makes `apr predict` and `apr eval` state that a genuinely tagged classifier is not one, while `apr inspect` renders its full APR-05 section — the operator is routed into the contradiction by the refusal's own text. Task 2 converts the arm to a typed `InvalidFormat` naming the cap; Task 3 pins the four-surface agreement. |
| T-04-72 | Information Disclosure | the new over-cap error message | accept | The message names the declared size, the cap and the path — all values the operator already supplied or can `stat`. No artifact content is read or echoed, because the refusal happens before the block is read. |
| T-04-73 | Tampering | header checksum computed but unused (`inspect.rs:521` sets `checksum_valid`, nothing branches on it) | accept | Out of this plan's scope and untouched by it. Recorded here so the cap is not mistaken for integrity: the cap bounds resource use, it does not authenticate the header. Belongs to whoever closes the APR-02 residual I-01. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: this plan adds no dependency. `crates/apr-cli/Cargo.toml` is not in `files_modified`; if a task finds itself needing a new crate, stop and report rather than installing. |
</threat_model>

<verification>
Run at the end of the plan, statuses captured with `cmd > log 2>&1; rc=$?` and never through a pipe:

1. `cargo test -p apr-cli --features setfit --lib` — rc=0, passed count ≥ 6750. Measured at HEAD
   `81652bb50` with exactly that command: **6756 passed, 0 failed, 15 ignored**; the 6752 in
   `04-VERIFICATION.md` is the `0fb47958f` baseline, stale by three commits. Same number and same
   provenance as the Task 3 criterion — they must not drift apart.
2. `make setfit-cli-inspect-tests` — rc=0, floor 110 satisfied.
3. `make setfit-cli-predict-tests` — rc=0, floor 30 satisfied.
4. `make setfit-cli-serve-tests` — rc=0, floor satisfied (proves the `serve` fall-through survived).
5. `make setfit-feature-matrix` — rc=0 (proves the `apr-cli --lib setfit` legs still compile and
   run with the feature both on and off).
6. Live binary probe, binary PINNED. The fixture recipe is concrete, so no byte offset has to be
   invented — all three facts were measured at HEAD:
   `crates/apr-format/tests/fixtures/golden_v2.apr` exists (1.1K); `HEADER_SIZE_V2` is 64; and
   `AprV2Header::from_bytes` (`crates/apr-format/src/v2/header_impl.rs:80`) reads `metadata_size`
   as a little-endian `u32` at byte offset **20..24** (with `metadata_offset` as a LE `u64` at
   12..20). So: copy the golden fixture, overwrite bytes 20..24 with the LE encoding of
   `16 * 1024 * 1024 + 1`, and `truncate -s 17M` the copy so the pre-existing length check passes
   and the new cap is what fires. CONFIRM the offset by reading `header_impl.rs` before patching —
   do not take it from this plan. Then `. scripts/apr_bin.sh || exit 1` and run
   `"$APR" inspect <fixture>` and `"$APR" predict <fixture> --text hi`, recording both exit codes
   and both messages verbatim. Never a bare `apr`, never a hardcoded path — four `apr` binaries
   once coexisted on this host. Rebuild first so `apr_bin.sh`'s HEAD-SHA check passes against the
   edited tree. If the fixture cannot be built as described, record the probe as an explicit
   NOT-RUN with the reason; do not report it as passing.
</verification>

<success_criteria>
- `apr inspect` refuses a metadata block over 16 MiB before allocating it, proven by a unit test
  that was seen RED against the pre-change code.
- The over-cap case is one typed refusal; `apr predict` and `apr eval --task classify` propagate a
  true statement; `apr serve` keeps its documented fall-through.
- One executed test drives all four decision surfaces over the same file and was proven able to
  fail.
- Every existing `--json` inspect output for a legitimate artifact is byte-identical.
- The SUMMARY states plainly that F-10, the per-crate mutation gate and the SAFE-02 "in CI" clause
  remain OPEN, and that this plan closes neither OPS-01 nor OPS-02.
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-18-SUMMARY.md` when done.

The SUMMARY must record, at minimum: the HEAD baseline test counts measured BEFORE editing; the
exact RED failure line for Task 1's Test 1 and Task 3's mutation control; the verbatim messages
from the two live `"$APR"` probes — or, if the over-cap fixture could not be built as described,
their explicit NOT-RUN reason; and an explicit statement of what remains open (F-10, the mutation
gate, the CI run).
</output>
