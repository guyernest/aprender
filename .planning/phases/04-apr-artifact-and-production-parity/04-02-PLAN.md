---
phase: 04-apr-artifact-and-production-parity
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - crates/aprender-core/src/setfit/artifact.rs
  - crates/aprender-core/src/setfit/mod.rs
autonomous: true
requirements: [APR-01]
must_haves:
  truths:
    - "Writing the same bundle view twice — including in two SEPARATE processes — produces byte-identical artifacts with identical SHA-256"
    - "The artifact contains exactly the canonical F32 tensor set plus one U8 tokenizer.blob tensor whose bytes equal the pinned tokenizer.json exactly"
    - "The serialized metadata carries model_type=setfit and EXACTLY ONE custom key (setfit) whose JSON object matches the contract field list"
    - "A missing HF->canonical name mapping is a typed error, not a silently dropped tensor"
  artifacts:
    - path: "crates/aprender-core/src/setfit/artifact.rs"
      provides: "write_setfit_apr + SetFitArtifactView + SetFitArtifactError + probe computation at write time"
      contains: "pub fn write_setfit_apr"
      min_lines: 250
  key_links:
    - from: "crates/aprender-core/src/setfit/artifact.rs"
      to: "aprender::format::v2 AprV2Writer"
      via: "add_f32_tensor / add_tensor(U8) / write()"
      pattern: "AprV2Writer"
    - from: "crates/aprender-core/src/setfit/artifact.rs"
      to: "contracts/setfit-apr-v1.yaml"
      via: "contract-resident probe strings + constants module"
      pattern: "setfit-apr-v1"
---

<objective>
Implement the `setfit-apr-v1` writer half in `aprender-core/src/setfit/artifact.rs`: a pure
deterministic function from a complete artifact view to APR v2 bytes — canonical tensor names,
U8 tokenizer blob, hybrid metadata with exactly one custom key, embedded synthetic probe records
computed from the tensors being written — and prove determinism cross-process.

Purpose: APR-01, and the byte-canonical closure obligation (verify.rs: serialize(deserialize(bytes))
must equal bytes EXACTLY or ReloadNotFromBytes fires). Every nondeterminism here becomes a flaky
train-time gate in plan 04-05.

Output: writer + determinism proofs. (Loader lands in 04-03; this plan reads back only metadata
for its own byte-equality tests via AprV2Reader.)

Branch: `gsd/phase-4-apr-parity` (created by the orchestrator from `gsd/phase-2-contract-gate` HEAD — check it exists, stop if not).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-RESEARCH.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
From crates/apr-format/src/v2/ (re-exported as aprender::format::v2), verified on this branch:
- `AprV2Writer::new(metadata: AprV2Metadata)` — sets LAYOUT_ROW_MAJOR automatically (writer.rs:30-57)
- `add_tensor(name, dtype: TensorDType, shape: Vec<usize>, data: Vec<u8>)` — U8 tokenizer blob goes here (TensorDType::U8 = 7)
- `add_f32_tensor(name, shape, data: &[f32])` — LE byte conversion built in
- `write() -> Result<Vec<u8>, V2FormatError>` — sorted index, deterministic offsets, CRC32 footer (writer.rs:317). Use write(), NOT write_into: the seam is bytes; file placement is the CLI's job
- `AprV2Metadata { model_type: String, created_at: Option<String> (MUST stay None), custom: HashMap<String, serde_json::Value> (#[serde(flatten)], header_impl.rs:298) }

From crates/aprender-core/src/setfit/mod.rs:
- `SetFitMiniLm::from_bundle_parts(tokenizer_bytes, arch: &EncoderArchitecture, tensors: BTreeMap<String,(Vec<usize>,Vec<f32>)>, root_seed) -> Result<Self, SetFitError>` (mod.rs:354-374)
- `tokenizer_sha256()`, `tokenizer_bytes()`, `encode_texts()`, `named_parameters()`
- `aprender::classification::MultinomialLogisticRegression` — `predict_proba`, `weights()`, `intercepts()`, `n_features()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: write_setfit_apr — deterministic writer with embedded probes</name>
  <files>crates/aprender-core/src/setfit/artifact.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - contracts/setfit-apr-v1.yaml (the field list, probe strings, name table, constants — the source of truth this writer implements)
    - crates/apr-format/src/v2/writer.rs (writer API + determinism analysis; padding is Vec::resize(_,0))
    - crates/apr-format/src/v2/header_impl.rs lines 132-300 (AprV2Metadata fields; custom flatten; created_at skip)
    - crates/aprender-core/src/setfit/mod.rs (from_bundle_parts, encode_texts, EncoderArchitecture — the probe computation door and the module you register into)
    - crates/aprender-train/src/train/setfit/bundle.rs lines 380-420 (f32_to_hex bit-pattern precedent for floats in JSON)
    - 04-RESEARCH.md "Code Examples > Deterministic one-key metadata construction" (copy the construction shape verbatim)
  </read_first>
  <behavior>
    - Test: writer output contains every canonical tensor name from the contract table plus tokenizer.blob, and nothing else
    - Test: metadata JSON parsed back has model_type == "setfit" and custom.len() == 1 with key "setfit"
    - Test: a view whose tensor map omits one encoder tensor -> typed IncompleteTensorSet error (no partial artifact)
    - Test: a view with an HF tensor name absent from the embedded name map -> typed UnmappedTensorName error
    - Test: non-finite f32 anywhere in tensors or probe expectations -> typed NonFiniteValue error BEFORE serialization
  </behavior>
  <action>
    In new `crates/aprender-core/src/setfit/artifact.rs` (register `pub mod artifact;` in setfit/mod.rs,
    gated on the existing `setfit` feature):

    1. Define `SetFitArtifactView` — the writer's complete input, mirroring SetFitBundle's field
       list (bundle.rs:317-356) so plan 04-05's codec can populate it 1:1: architecture record,
       tokenizer bytes, pooling/normalization/l2_epsilon/truncation/padding/max_length policy,
       root_seed, HF-named tensors (BTreeMap<String,(Vec<usize>,Vec<f32>)>), head weights/intercepts/
       n_features, ordered_labels, requested_config + resolved_config records, evidence summary +
       table hash. Everything in the artifact MUST be derivable from this view (closure obligation —
       no clock, no env, no host name; provenance fields come from the view, never from lookup).
    2. Define `SetFitArtifactError` (typed, non_exhaustive, no unwrap): UnmappedTensorName,
       IncompleteTensorSet, NonFiniteValue, TokenizerHashMismatch, ContainerWrite, ProbeComputation —
       plus loader variants arriving in 04-03.
    3. `pub fn write_setfit_apr(view: &SetFitArtifactView) -> Result<Vec<u8>, SetFitArtifactError>`:
       (a) validate completeness: view tensor names map 1:1 onto the contract's canonical set via the
       hf_name_map (missing mapping or missing/extra tensor = typed error);
       (b) compute the 6 probe records at write time: rebuild a model from the view's own parts via
       SetFitMiniLm::from_bundle_parts + MultinomialLogisticRegression from stored coefficients, run
       the contract's 6 synthetic strings, record expected embedding/logits/probabilities/label with
       floats as bit-pattern hex (deterministic CPU path makes recomputation stable — this is what
       keeps the codec closure exact);
       (c) reject any non-finite value in tensors or probe outputs (typed, before serialization —
       serde_json renders NaN as null, CR-03);
       (d) build the metadata doc as ONE serde_json::Map inserted under the single custom key
       "setfit", fields exactly per contract (schema, schema_version=1, ordered_labels,
       tokenizer_sha256, hf_name_map, preprocessing, resolved_config, requested_config, evidence,
       provenance, probes); model_type = "setfit"; created_at = None;
       (e) write canonical-named F32 tensors + `tokenizer.blob` U8 tensor via AprV2Writer; return bytes.
    4. Also expose `pub fn artifact_sha256_hex(bytes: &[u8]) -> String` as a trusted free function
       in core (sha2; the codec must never hash its own output — verify.rs:198-205 discipline).
    Unit tests per the behavior list, plus: tokenizer.blob bytes recovered via AprV2Reader::get_tensor_data
    equal the input tokenizer bytes exactly and hash to the recorded tokenizer_sha256.
    Use the tiny fixture encoder architecture from existing core setfit tests as the test model —
    do NOT require the real 90 MB pinned weights in unit tests.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::artifact::</automated>
  </verify>
  <acceptance_criteria>
    - artifact.rs contains `pub fn write_setfit_apr(` and `pub fn artifact_sha256_hex(`
    - `cargo test -p aprender-core --features setfit --lib setfit::artifact::` exits 0 with >= 8 tests passed (a zero-match filter is a CR-02 vacuous pass — assert the count in the SUMMARY)
    - grep assertion: `grep -c "created_at" crates/aprender-core/src/setfit/artifact.rs` shows it only as an explicit None or absent — never Some(...)
    - The five behavior tests exist and pass; each typed error is asserted with matches!, not message text
  </acceptance_criteria>
  <done>A complete view becomes valid setfit-apr-v1 bytes; every incomplete/unmappable/non-finite view is a typed error</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Determinism and hash-stability proofs (cross-process)</name>
  <files>crates/aprender-core/src/setfit/artifact.rs</files>
  <read_first>
    - 04-RESEARCH.md Pitfall 1 and Pitfall 2 (the empirically verified HashMap-order nondeterminism and the closure obligation's full breadth)
    - crates/aprender-core/src/setfit/artifact.rs (the writer you just built)
  </read_first>
  <behavior>
    - Test: write(view) == write(view) byte-for-byte within one process
    - Test: parse metadata JSON back and re-serialize the doc -> byte-identical metadata (write->parse->write)
    - Test CROSS-PROCESS: a child process re-invoked from the same test executable writes the same view and reports the same SHA-256 as the parent (this is the half that catches HashMap RandomState ordering — same-process tests cannot)
    - Test: serialized metadata JSON contains exactly one top-level custom key beyond the typed fields
  </behavior>
  <action>
    Add the determinism test suite to artifact.rs tests:
    1. Same-process double-write byte equality + identical artifact_sha256_hex.
    2. Write -> AprV2Reader parse -> rebuild metadata doc -> byte equality of the metadata segment.
    3. Cross-process: the test spawns `std::env::current_exe()` with env var
       `SETFIT_APR_DETERMINISM_CHILD=1` and a `--exact` filter for a helper test; the child builds
       the IDENTICAL fixture view (fixed constants — no RNG without fixed seed), writes, prints the
       hex hash to stdout; parent compares with its own. Guard the helper so it is a no-op unless
       the env var is set (standard re-exec pattern; no new dev-deps).
    4. A canonical fixture-view hash golden: pin the expected SHA-256 hex of the tiny fixture
       artifact as a committed constant; a drift in writer bytes fails this test loudly (bless
       deliberately, per Ph1 D-13 fixture discipline).
    In-band negative (house discipline): a test that builds metadata with TWO custom keys via raw
    AprV2Metadata (bypassing write_setfit_apr) and asserts the writer's own output never does —
    i.e., assert the public writer path cannot produce two keys, and document WHY two keys are
    forbidden (empirical nondeterminism) in the test comment.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::artifact::determinism</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --features setfit --lib setfit::artifact::determinism` exits 0 with >= 4 tests passed
    - The cross-process test spawns current_exe (source assertion: artifact.rs contains `current_exe`) and compares hashes, not exit codes
    - A committed golden hash constant exists (source assertion: a 64-char hex literal near a name containing GOLDEN or golden_sha256)
  </acceptance_criteria>
  <done>Determinism is proven where it can actually break — across processes — before the codec closure check ever runs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view -> artifact bytes | everything a consumer will trust is fixed here; nondeterminism or smuggled env data undermines the hash chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | metadata determinism | mitigate | one custom key + sorted Map + created_at None + cross-process byte-equality test |
| T-04-04 | Information Disclosure | probe records | mitigate | probes are the contract's 6 synthetic strings ONLY — no dataset text in the artifact (Pitfall 8); test asserts probe inputs equal the contract strings |
| T-04-05 | Tampering | non-finite floats -> null JSON | mitigate | typed NonFiniteValue rejection before serialization; bit-pattern hex for stored floats |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- `cargo test -p aprender-core --features setfit --lib setfit::artifact::` green with counted tests
- `cargo clippy -p aprender-core --features setfit -- -D warnings` clean for the new module (scoped — arm64 repo-wide tier2 is KNOWN-RED, D-ITEM-02)
- Recommend CARGO_INCREMENTAL=0 for the plan (ENOSPC history)
</verification>

<success_criteria>
- APR-01's artifact content list is fully written by one pure function of one complete view
- Determinism proven same-process, cross-process, and against a pinned golden hash
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-02-SUMMARY.md` when done.
Record: test counts per filter, the golden hash value, and the exact metadata field list as shipped.
</output>
