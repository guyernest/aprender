---
phase: 04-apr-artifact-and-production-parity
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/aprender-core/src/setfit/classify.rs
  - crates/aprender-core/src/setfit/mod.rs
autonomous: true
requirements: [OPS-04, OPS-06]
must_haves:
  truths:
    - "classify on ordered single or mixed-length batches returns, for every input: label, FULL ordered probability vector, optional logits, winning margin, token count and truncation facts, plus response-level latency, backend identity, and artifact hash"
    - "Backend identity in the response is read from the execution layer (trueno runtime detection), never echoed from configuration — a config value cannot reach the field"
    - "A non-finite value anywhere in a response is a typed error before serialization — null never appears in a numeric field"
    - "ClassifyResponse is ONE versioned type in core; CLI and HTTP will serialize this exact struct (D-08 field parity by construction)"
  artifacts:
    - path: "crates/aprender-core/src/setfit/classify.rs"
      provides: "ClassifyResponse envelope family + VerifiedSetFitModel::classify"
      contains: "pub struct ClassifyResponse"
      min_lines: 150
  key_links:
    - from: "crates/aprender-core/src/setfit/classify.rs"
      to: "aprender-compute (trueno) runtime backend detection"
      via: "execution-derived backend identity (D-12)"
      pattern: "backend"
    - from: "crates/aprender-core/src/setfit/classify.rs"
      to: "VerifiedSetFitModel"
      via: "classify is a method on the verified typestate only"
      pattern: "impl VerifiedSetFitModel"
---

<objective>
Define the D-08 shared response envelope ONCE in core and implement
`VerifiedSetFitModel::classify` — the single classification path every surface (Rust API, CLI,
HTTP) will serialize identically. Backend identity comes from execution (D-12); non-finite values
are typed errors (CR-03 lesson); truncation facts come from the real tokenizer batch.

Purpose: OPS-04's field list becomes a type-level fact; criterion 4's parity becomes structural.
OPS-06's honesty half: the backend field cannot misreport because it is not writable from config.

Output: classify.rs (envelope + classify + goldens + negatives).

Branch: `gsd/phase-4-apr-parity`. Runs in wave 4 parallel with 04-05 (zero file overlap — this plan
owns core setfit/classify.rs + mod.rs; 04-05 owns aprender-train files only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
From 04-03 (on branch):
- `VerifiedSetFitModel` (private-field typestate; accessors artifact_sha256(), ordered_labels(), doc_view())
From core setfit:
- `SetFitMiniLm::tokenize(texts) -> SentenceBatch` (ordered token facts incl. truncation), `encode_texts()`
- `MultinomialLogisticRegression::predict_proba` (full probability rows)
From aprender-compute (trueno) lib.rs:281-312:
- runtime CPU feature detection (detect_x86_backend / detect_arm_backend) selecting the active Backend — the ONLY legitimate source for the identity field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClassifyResponse envelope family (D-08) with non-finite rejection</name>
  <files>crates/aprender-core/src/setfit/classify.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/data_contrastive.rs lines 548-571 (PairsReport — the typed-report precedent; D-08 differs: owned, versioned, core-resident)
    - contracts/setfit-apr-v1.yaml (max_batch_texts = 256; tolerance + field obligations)
    - crates/aprender-train/src/train/setfit/verify.rs lines 336-341 (NaN-visible within idiom)
    - crates/aprender-core/src/setfit/mod.rs (SentenceBatch shape — the truncation facts source)
  </read_first>
  <behavior>
    - Test: serde_json serialization of a fixture ClassifyResponse contains EXACTLY the field names: schema_version, artifact_sha256, backend, latency_ms, results; each result: label, probabilities, logits (optional, absent when not requested), margin, token_count, truncated
    - Test: a response carrying NaN or infinity in probabilities/logits/margin fails construction with a typed NonFiniteResponse error — serialization is unreachable
    - Test: probabilities in each result sum to 1.0 within 1e-6 and have exactly ordered_labels.len() entries
  </behavior>
  <action>
    New `crates/aprender-core/src/setfit/classify.rs` (register `pub mod classify;` gated on setfit
    feature). Define the versioned envelope, all Serialize + Deserialize (HTTP needs both):
    - `ClassifyResponse { schema_version: u32 (const 1), artifact_sha256: String, backend: String, latency_ms: f64, results: Vec<ClassifyResult> }`
    - `ClassifyResult { label: String, probabilities: Vec<f64>, logits: Option<Vec<f64>>, margin: f64, token_count: u32, truncated: bool }`
    - `ClassifyRequestOptions { include_logits: bool }`
    - `ClassifyError` (typed): EmptyInput, BatchTooLarge { max: usize, got: usize } (contract
      max_batch_texts = 256), NonFiniteResponse { field: &'static str }.
    Construction goes through a validating constructor (PairsReport-style fully-typed report;
    deny non-finite via the partial_cmp idiom before the value can exist). Serialization goldens:
    a committed expected-JSON golden for a fixture response (exact bytes, keys in declared order)
    so any field rename/removal is a loud diff — this is the D-08 "schema bumps are deliberate and
    pv-diff-visible" enforcement at the Rust level.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::classify::envelope</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --features setfit --lib setfit::classify::envelope` exits 0 with >= 4 tests passed
    - classify.rs contains `pub struct ClassifyResponse` and `pub struct ClassifyResult` with exactly the fields listed above (source assertion)
    - The golden test compares serialized bytes against a committed literal, not a re-serialized struct (drift-visible)
  </acceptance_criteria>
  <done>The envelope is one owned versioned core type; a malformed or non-finite response is unrepresentable serialized output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VerifiedSetFitModel::classify with execution-derived backend identity</name>
  <files>crates/aprender-core/src/setfit/classify.rs</files>
  <read_first>
    - crates/aprender-compute/src/lib.rs lines 281-312 (runtime backend detection — the D-12 identity source)
    - crates/aprender-core/src/setfit/artifact.rs (VerifiedSetFitModel internals you extend)
  </read_first>
  <behavior>
    - Test: classify on a 3-text mixed-length batch returns 3 results in input order with full probability vectors over ordered_labels
    - Test: classify(&[]) -> typed EmptyInput; classify with 257 texts -> typed BatchTooLarge
    - Test: token_count/truncated facts for the contract's truncation-boundary probe string show truncated == true; a short string shows false
    - Test (D-12): the backend field value equals the value returned by the compute layer's runtime detection called directly in the test — and there is NO classify parameter that can set it
    - Test: each text classified singly yields probabilities within contract tolerance of its batched result (padding invariance at the response level)
  </behavior>
  <action>
    Implement `impl VerifiedSetFitModel { pub fn classify(&self, texts: &[String], opts: &ClassifyRequestOptions) -> Result<ClassifyResponse, ClassifyError> }`
    in classify.rs (method on the verified typestate ONLY — the door stays single):
    1. Validate: non-empty, len <= 256 (contract constant, same constants module as 04-03).
    2. Tokenize once via SetFitMiniLm::tokenize -> SentenceBatch; capture per-text token_count and
       truncated facts from the batch (real facts, not recomputed guesses).
    3. Encode -> head predict_proba -> per-text label (argmax over ordered_labels), full
       probability vector, optional logits (opts.include_logits), margin = top1 - top2 probability.
    4. latency_ms measured around the compute (std::time::Instant — measurement, not identity;
       latency is allowed to vary, it is not part of any byte-determinism obligation).
    5. backend: obtained by calling the trueno runtime detection through a private
       `execution_backend_identity()` helper — the field is populated ONLY from this call. Config,
       env vars, and caller parameters have no path to it (D-12 structural honesty; CLAUDE.md
       Verification Discipline rule 2). Document in a comment why there is no setter.
    6. artifact_sha256 from self — the same hash readiness and eval will report.
    Then validate + construct the envelope through Task 1's validating constructor.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::classify::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --features setfit --lib setfit::classify::` exits 0 with >= 9 tests passed (both tasks' suites)
    - Source assertion: `grep -c "fn set_backend\|backend:" crates/aprender-core/src/setfit/classify.rs` shows backend populated only inside classify via execution_backend_identity()
    - The truncation-fact test uses the contract's 64x-repeated probe string and asserts truncated == true with token_count == 256
  </acceptance_criteria>
  <done>One classify path, every OPS-04 field real, backend identity impossible to misreport from config</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller texts -> classify | untrusted input sizes/content enter the model here |
| execution layer -> response identity | the backend field must reflect what ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | DoS | unbounded batch | mitigate | contract max_batch_texts = 256 enforced in core classify (typed BatchTooLarge) — serve adds body bound in 04-08 |
| T-04-12 | Repudiation | backend misreporting | mitigate | identity read from trueno runtime detection only; no setter; test pins field == direct detection call |
| T-04-13 | Tampering | NaN -> null in JSON | mitigate | validating constructor rejects non-finite before serialization |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Full `cargo test -p aprender-core --features setfit --lib setfit::` green; scoped clippy clean
</verification>

<success_criteria>
- OPS-04's complete field list is a type; OPS-06's reporting honesty is structural
- CLI (04-07) and HTTP (04-08) can serialize this struct with zero surface-local response types
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-04-SUMMARY.md` when done.
Record: the exact serialized golden, test counts, and the backend identity value observed on this host.
</output>
