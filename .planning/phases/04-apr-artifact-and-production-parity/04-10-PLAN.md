---
phase: 04-apr-artifact-and-production-parity
plan: 10
type: execute
wave: 8
depends_on: ["04-09", "04-12", "04-15"]
files_modified:
  - Makefile
  - contracts/setfit-apr-v1.yaml
autonomous: true
requirements: [SAFE-02, SAFE-01]
must_haves:
  truths:
    - "Every new Phase 4 test surface has a NAMED Make target that compiles it, RUNS it, and fails if it ran nothing (assert_tests_ran guard — a zero-match filter can never print success)"
    - "Every suite a Phase 4 plan cites as its own evidence has a guarded target — including 04-14's setfit::config and setfit::evaluate suites, which would otherwise run only incidentally under the pre-existing broad train leg with no ran-something guard over them"
    - "Every cargo test invocation carries AT MOST ONE positional filter — libtest accepts one, and the previous plan's two-filter commands would not have run what they claimed (review M5)"
    - "setfit-feature-matrix covers all FOUR setfit crates across the three supported CPU profiles (no-default-features, setfit, default+setfit) with BUILD and RUN legs (review M10)"
    - "The matrix records, in-recipe, that apr-cli's unconditional parity dev-dependency unifies aprender-serve's setfit feature into every test build — so the apr-cli no-default-features RUN leg proves less than it reads and SAFE-02's gating evidence is the check leg"
    - "The parity gate, the spawned-serve smoke and the spawned CLI lifecycle are wired into tier3 — CR-01's lesson is structural: no feature-gated surface without a tier that runs it"
    - "The OPS-01 boundary gate exists as a Make target with a must-match/must-not-match case table (CLAUDE.md guard-regex rule 7)"
  artifacts:
    - path: "Makefile"
      provides: "setfit-apr-tests, setfit-classify-tests, setfit-bundle-tests, setfit-config-tests, setfit-evaluate-tests, setfit-codec-tests, setfit-reload-tests, setfit-lock-tests, setfit-lifecycle-tests, setfit-ui-tests, setfit-cli-*, setfit-serve-tests, setfit-parity, setfit-serve-smoke, setfit-cli-lifecycle, setfit-api-boundary + grown setfit-feature-matrix + tier wiring"
      contains: "setfit-parity"
    - path: "contracts/setfit-apr-v1.yaml"
      provides: "binding-status flips for equations whose modules now exist (the ONLY sanctioned edit — no semantic change)"
      contains: "setfit-apr-v1"
  key_links:
    - from: "Makefile setfit-* targets"
      to: "assert_tests_ran"
      via: "every name-filtered target parses the passed count (CR-02)"
      pattern: "assert_tests_ran"
---

<objective>
Wire every Phase 4 surface into named, guarded, tiered Make gates: scoped test targets with
ran-something guards, the four-crate SAFE-02 feature matrix across all three supported CPU
profiles, the parity/smoke/lifecycle tier3 targets, and the OPS-01 dependency-boundary gate. Local
(Makefile) half only — the ci.yml half is 04-11's human-gated edit, and the two must stay
command-identical.

**Review findings closed here.** M5: the previous plan's targets passed TWO positional filters to
`cargo test` (e.g. `... setfit::artifact:: setfit::classify::`), which libtest does not accept —
a gate that cannot run what it names is worse than no gate. Each suite is now its own invocation
with its own guard. M6: this plan edits `contracts/setfit-apr-v1.yaml` binding statuses, so the
contract is now DECLARED in `files_modified` instead of being an undeclared edit that broke the
file-ownership model. M10: the SAFE-02 matrix is spelled out per crate and per profile, with run
legs and not only build legs.

**Checker warnings closed here.** W-6: 04-14's two suites (`setfit::config` for `to_request`,
`setfit::evaluate` for the float-door guard re-run) are cited as that plan's evidence but had no
guarded target — they ran only incidentally under the pre-existing broad
`-p aprender-train --lib setfit::` leg, where no `assert_tests_ran` covers them. Both now have
their own target. W-7: the feature matrix records the apr-cli dev-dependency unification so the
`--no-default-features` run leg is not read as stronger evidence than it is.

Purpose: SAFE-02's local half; the CR-01/CR-02 lessons made structural. A gate that can go vacuous
is not a gate.

Output: Makefile targets + tier wiring + contract binding-status flips.

Branch: `gsd/phase-4-apr-parity`. Wave 8, sole plan in the wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md

<interfaces>
Makefile (verified):
- `assert_tests_ran` define at 1381-1390 (awk-parses "test result:" passed counts; fails below a minimum — use for EVERY new filtered target)
- `setfit-feature-matrix` at 382-394 (core-only legs today; grow it)
- `setfit-tests` at 1426 (the CR-01 tier3 pattern: compile AND run, counts asserted)
- tier2/tier3 recipe locations; PHASE4_CONTRACTS + contract-audit-phase4 landed by 04-01
- bashrs governs shell fragments; `$?` never read through a pipe (Verification rule 1); Makefile recipes invoking `apr` directly are scanned by scripts/check_apr_bin_pinned.sh — prefer cargo test forms that need no apr binary (the spawned tests resolve the binary through CARGO_BIN_EXE_apr, so no recipe needs to name `apr`)
Host caveats (RESEARCH Pitfall 6): no bare `cargo check --workspace` (Darwin KNOWN-RED via aprender-profile); arm64 tier2 clippy KNOWN-RED; CARGO_INCREMENTAL=0 for long runs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoped test targets — one filter per invocation, each with a ran-something guard</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 1381-1470 (assert_tests_ran + setfit-tests + setfit-repro-* — the exact guarded-target shape to clone)
    - The 04-02..04-09, 04-12..04-16 SUMMARYs (the MEASURED test counts each suite produced — every guard minimum comes from a measured count minus a small slack, never a guess). Note 04-14's two suites specifically: its acceptance criteria cite `setfit::config` (>= 4 new) and a re-run of `setfit::evaluate`'s float-door guard, and both counts are recorded there
    - 04-REVIEWS.md (the M5 finding: `cargo test ... filter_a filter_b` supplies two positional filters and does not do what the recipe claims)
  </read_first>
  <action>
    Add guarded targets. EVERY recipe line runs exactly ONE `cargo test` with AT MOST ONE positional
    filter, redirects into its own log with the status captured directly (`cmd > log 2>&1; rc=$?` —
    never through a pipe), then calls assert_tests_ran with a minimum derived from the measured
    count, then exits rc. A target that needs two suites runs two commands and guards each.
    Targets:
    - `setfit-apr-tests`: `cargo test -p aprender-core --features setfit --lib setfit::artifact::`
    - `setfit-classify-tests`: `cargo test -p aprender-core --features setfit --lib setfit::classify::`
    - `setfit-bundle-tests`: `cargo test -p aprender-train --features setfit --lib setfit::bundle`
    - `setfit-config-tests`: `cargo test -p aprender-train --features setfit --lib setfit::config`
      (04-14 Task 1's `to_request` suite. It has no target today and would otherwise be covered only
      by the pre-existing broad `-p aprender-train --lib setfit::` leg, which carries no
      ran-something guard over this filter — the CR-02 shape this phase exists to remove)
    - `setfit-evaluate-tests`: `cargo test -p aprender-train --features setfit --lib setfit::evaluate`
      (04-14 Task 2's re-run of `evaluate_source_exposes_no_public_api_taking_a_float_parameter`.
      That guard is named as acceptance evidence for a new public door; evidence that runs only
      incidentally is the same gap)
    - `setfit-codec-tests`: `cargo test -p aprender-train --features setfit --lib setfit::apr_codec::`
    - `setfit-reload-tests`: `cargo test -p aprender-train --features setfit --lib setfit::apr_reload::`
    - `setfit-lock-tests`: `cargo test -p aprender-train --features setfit --lib setfit::lock`
    - `setfit-lifecycle-tests`: `cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle`
    - `setfit-ui-tests`: `cargo test -p aprender-train --features setfit --test ui` (count grown by 04-03's new case)
    - `setfit-cli-train-tests`: `cargo test -p apr-cli --features setfit --lib setfit_train`
      PLUS a second invocation `... --lib setfit_train -- --ignored` for the e2e leg, guarded separately
    - `setfit-cli-predict-tests`: `cargo test -p apr-cli --features setfit --lib predict`
    - `setfit-cli-inspect-tests`: `cargo test -p apr-cli --features setfit --lib inspect`
    - `setfit-cli-eval-tests`: `cargo test -p apr-cli --features setfit --lib eval::setfit`
    - `setfit-cli-io-tests`: `cargo test -p apr-cli --features setfit --lib setfit_io`
    - `setfit-serve-tests`: `cargo test -p aprender-serve --features setfit --lib setfit`
    - `setfit-parity`: `cargo test -p apr-cli --features setfit,inference --test setfit_parity`
    - `setfit-serve-smoke`: `cargo test -p apr-cli --features setfit,inference --test setfit_parity -- --ignored spawned_serve_smoke` (min 1)
    - `setfit-cli-lifecycle`: two guarded invocations —
      `cargo test -p apr-cli --features setfit --test setfit_cli_lifecycle -- --ignored lifecycle` and
      `... -- --ignored tooling` (min 1 each; 04-15's file is auto-discovered and carries a
      module-level cfg guard, so with the feature off it would run zero tests — which is exactly
      what the guard catches)
    - `setfit-api-boundary`: `cargo tree -e normal` for aprender-core and for aprender-train,
      grep-count "apr-cli" == 0 — WITH a must-match/must-not-match case table in comments (rule 7):
      a deliberately wrong invocation that WOULD match (the tree of apr-cli itself contains apr-cli)
      proves the grep is live; the two real legs must not match. Use `grep -c` on tree output
      (machine output, no comment-line hazard) and compare explicitly — never a bare `|| true`.
    Add an aggregate `setfit-all-tests` that depends on the individual targets — all of them,
    including `setfit-config-tests` and `setfit-evaluate-tests` — so tier wiring names one
    prerequisite while every suite keeps its own guard.
  </action>
  <verify>
    <automated>make setfit-apr-tests && make setfit-classify-tests && make setfit-config-tests && make setfit-evaluate-tests && make setfit-codec-tests && make setfit-reload-tests && make setfit-cli-predict-tests && make setfit-serve-tests && make setfit-parity && make setfit-api-boundary</automated>
  </verify>
  <acceptance_criteria>
    - Every listed target exits 0 with visible counted output; each recipe names assert_tests_ran (or an explicit count compare for the boundary gate)
    - `setfit-config-tests` and `setfit-evaluate-tests` both exist, both call assert_tests_ran, and both are prerequisites of `setfit-all-tests` (source assertion: each name appears at least twice in the Makefile — its own target and the aggregate's prerequisite list)
    - Source assertion: every suite named as acceptance evidence by any 04-xx plan has a matching target — cross-check the target list against the `<automated>` commands of all 38 tasks and record any suite with no target in the SUMMARY
    - Source assertion: no recipe in the Makefile passes two positional filters to one `cargo test` — verify by inspecting each added line for a second bare token after the first filter (review M5)
    - Falsification (performed, recorded in SUMMARY, then reverted): raise one guard minimum above the real count — the target must FAIL; break one filter so it matches nothing — the guard must FAIL (the CR-02 proof, performed, not assumed)
    - `bashrs make lint Makefile` reports no new issues for the added recipes
    - No added recipe reads a status through a pipe
  </acceptance_criteria>
  <done>Every new suite has a named gate that runs exactly what it claims and provably cannot pass vacuously</done>
</task>

<task type="auto">
  <name>Task 2: SAFE-02 four-crate x three-profile matrix, tier wiring, contract binding statuses</name>
  <files>Makefile, contracts/setfit-apr-v1.yaml</files>
  <read_first>
    - Makefile lines 382-394 (current setfit-feature-matrix)
    - Makefile tier2/tier3 recipes (where setfit-tests and the phase2/3 audits sit today)
    - crates/apr-cli/Cargo.toml (04-09's unconditional `realizar` dev-dependency with features ["setfit"], and the comment it left there) plus 04-09-SUMMARY.md's unification note — the exact wording this recipe mirrors
    - contracts/setfit-apr-v1.yaml (the equations carrying `status: pending` from 04-01, and the audit's BIND-001 vs BIND-004 distinction)
    - 04-REVIEWS.md (the M10 finding: the matrix as written did not clearly cover all-features legs for all four crates, nor runtime legs for every matrix point)
  </read_first>
  <action>
    1. Rewrite `setfit-feature-matrix` to cover FOUR crates x THREE supported CPU profiles, with a
       BUILD leg for every cell and a RUN leg for every cell that has tests:
       crates: aprender-core, aprender-train, apr-cli, aprender-serve;
       profiles: (a) `--no-default-features`, (b) `--no-default-features --features setfit`
       (plus the minimum companions each crate needs, e.g. apr-cli's `training` and aprender-serve's
       `server`, which the setfit feature already pulls), (c) default features + `--features setfit`.
       Build legs use `cargo check -p <crate> ...`; run legs use `cargo test -p <crate> ... --lib`
       with a ran-something guard where the profile actually has setfit tests.
       Record IN A COMMENT beside the apr-cli cells the dev-dependency unification 04-09 introduced,
       in that plan's own wording: `crates/apr-cli/tests/setfit_parity.rs` requires an UNCONDITIONAL
       dev-dependency on aprender-serve with `features = ["setfit"]` (Cargo does not permit an
       optional dev-dependency), and dev-dep features unify with the normal dependency on the same
       package for any build that includes test targets. Therefore the apr-cli
       `--no-default-features` RUN leg still builds aprender-serve with setfit, which pulls
       `aprender/setfit` — that leg proves apr-cli's OWN gated code is absent, NOT that the setfit
       surface is absent from the graph. The apr-cli `--no-default-features` CHECK leg does not
       build dev-dependencies and is unaffected, so SAFE-02's gating evidence is read from the CHECK
       leg. Say this in the recipe rather than only in a SUMMARY: a reader running the matrix must
       not conclude more from the run leg than it establishes.
       Record IN A COMMENT why literal `--all-features` is NOT used: it enables gpu/cuda features
       that are not the supported CPU profile SAFE-02 names, so an `--all-features` leg would either
       fail on this host or prove something other than the requirement. If a measured attempt shows
       `--all-features` is in fact clean for a given crate, add it and say so — but measure first
       and record the result either way.
       Do NOT use bare `cargo check --workspace` (Darwin KNOWN-RED via aprender-profile);
       per-crate forms only.
    2. Tier wiring: tier3 gains `setfit-all-tests`, `setfit-parity`, `setfit-serve-smoke`,
       `setfit-cli-lifecycle`, `setfit-api-boundary` and the extended `setfit-feature-matrix`,
       beside `setfit-tests` and `contract-audit-phase4`. Keep the fast scoped core/train suites in
       tier2's orbit where the existing setfit-tests target already sits.
    3. Contract binding statuses: flip `status: pending` to bound/implemented in
       `contracts/setfit-apr-v1.yaml` for every equation whose module now exists, and re-run
       `make contract-audit-phase4`. This is the ONLY sanctioned edit to that file in this plan —
       binding status only, never a semantic change to a constant, tolerance, schema row, the
       nullable-path allowlist or an obligation. `pv diff` against the 04-01 version must show
       binding-status lines and nothing else; include that diff summary in the SUMMARY.
  </action>
  <verify>
    <automated>make setfit-feature-matrix && make contract-audit-phase4</automated>
  </verify>
  <acceptance_criteria>
    - `make setfit-feature-matrix` exits 0 and its output shows a leg for every crate x profile cell that the recipe declares, with run legs visibly executing tests
    - Source assertion: the recipe contains the dev-dependency unification comment and names `cargo check -p apr-cli --no-default-features` as the leg SAFE-02's gating evidence is read from (grep the recipe block for `dev-dep` and for `no-default-features`)
    - `grep -A 40 "^tier3" Makefile` names setfit-all-tests, setfit-parity, setfit-serve-smoke, setfit-cli-lifecycle, setfit-api-boundary and setfit-feature-matrix (source assertion)
    - `make contract-audit-phase4` exits 0 with zero BIND-001 and the binding statuses reflecting shipped modules
    - `pv diff` between the 04-01 contract and the current one lists only binding-status changes (recorded in the SUMMARY); if it lists anything else — the four-path nullable allowlist above all — revert that hunk
  </acceptance_criteria>
  <done>The feature matrix and the tiers cover every crate the setfit feature now spans, in every supported CPU profile, building AND running — and each leg's recipe says exactly what it proves</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate definitions -> developer trust | a vacuous or mis-invoked gate converts absence of evidence into evidence of absence |
| contract file -> audit | an undeclared contract edit breaks the file-ownership model the whole phase depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | Repudiation | zero-match filters exiting 0 | mitigate | assert_tests_ran on every filtered target + performed falsification (raise-min / break-filter, reverted) |
| T-04-31 | Repudiation | rc read through a pipe | mitigate | `> log 2>&1; rc=$?` form in every recipe (Verification rule 1 — shipped twice before) |
| T-04-32 | Spoofing | boundary grep matching nothing forever | mitigate | must-match/must-not-match case table executed once and recorded (rule 7) |
| T-04-56 | Repudiation | a recipe that cannot run what it names | mitigate | one positional filter per invocation, asserted by inspection of every added line (review M5) |
| T-04-57 | Tampering | undeclared contract edit | mitigate | contracts/setfit-apr-v1.yaml is declared in files_modified; the edit is binding-status only and pv diff is recorded |
| T-04-61 | Repudiation | a SAFE-02 run leg that proves less than it reads | mitigate | the dev-dep unification comment sits in the matrix recipe itself, and the gating evidence is explicitly relocated to the check leg dev-deps cannot reach |
| T-04-62 | Repudiation | a plan's cited evidence suite with no guarded target | mitigate | setfit-config-tests and setfit-evaluate-tests added; a cross-check of all 38 tasks' `<automated>` commands against the target list is an acceptance criterion |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- All targets green locally with the Darwin caveats honored (no bare --workspace check; arm64 tier2 clippy KNOWN-RED untouched; CARGO_INCREMENTAL=0 for the long runs)
- rtk hook: any porcelain-emptiness assertion goes through `rtk proxy`
</verification>

<success_criteria>
- SAFE-02's local half: four crates x three supported CPU profiles, build and run, plus named tier gates that run everything they claim
- Every suite any Phase 4 plan cites as evidence is covered by a guarded target, not by an unguarded broad leg
- Both CR-01 (unrun surface) and CR-02 (vacuous filter) are structurally impossible for the new surfaces, and the review's mis-invoked-filter class is closed by inspection
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-10-SUMMARY.md` when done.
Record: each target's measured count and guard minimum, the falsification transcript, the
all-38-tasks target cross-check result, the matrix cell list with results, the dev-dep unification
comment as shipped, tier placements, and the `pv diff` binding-status summary.
</output>
