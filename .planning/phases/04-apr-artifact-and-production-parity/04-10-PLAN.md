---
phase: 04-apr-artifact-and-production-parity
plan: 10
type: execute
wave: 8
depends_on: ["04-09"]
files_modified:
  - Makefile
autonomous: true
requirements: [SAFE-02, SAFE-01]
must_haves:
  truths:
    - "Every new Phase 4 test surface has a NAMED Make target that compiles it, RUNS it, and fails if it ran nothing (assert_tests_ran guard — a zero-match filter can never print success)"
    - "setfit-feature-matrix covers all FOUR setfit crates (core, train, apr-cli, aprender-serve) with check AND run legs per SAFE-02"
    - "The parity gate and spawned-serve smoke are wired into tier3; fast scoped suites into tier2's orbit — CR-01's lesson is structural: no feature-gated surface without a tier that runs it"
    - "The OPS-01 boundary gate exists as a Make target with a must-match/must-not-match case table (CLAUDE.md guard-regex rule 7)"
  artifacts:
    - path: "Makefile"
      provides: "setfit-apr-tests, setfit-cli-tests, setfit-serve-tests, setfit-parity, setfit-serve-smoke, setfit-api-boundary targets + grown setfit-feature-matrix + tier wiring"
      contains: "setfit-parity"
  key_links:
    - from: "Makefile setfit-* targets"
      to: "assert_tests_ran"
      via: "every name-filtered target parses the passed count (CR-02)"
      pattern: "assert_tests_ran"
---

<objective>
Wire every Phase 4 surface into named, guarded, tiered Make gates: scoped test targets with
ran-something guards, the four-crate SAFE-02 feature matrix, the parity + smoke tier3 targets, and
the OPS-01 dependency-boundary gate. Local (Makefile) half only — the ci.yml half is 04-11's
human-gated edit, and the two must stay command-identical.

Purpose: SAFE-02's local half; the CR-01/CR-02 lessons made structural. A gate that can go vacuous
is not a gate.

Output: Makefile targets + tier wiring.

Branch: `gsd/phase-4-apr-parity`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md

<interfaces>
Makefile (verified):
- `assert_tests_ran` define at 1381-1390 (awk-parses "test result:" passed counts; fails below a minimum — use for EVERY new filtered target)
- `setfit-feature-matrix` at 382-394 (core-only legs today; grow it)
- `setfit-tests` at 1426 (the CR-01 tier3 pattern: compile AND run, counts asserted)
- tier2/tier3 recipe locations; PHASE4_CONTRACTS + contract-audit-phase4 landed by 04-01
- bashrs governs shell fragments; `$?` never read through a pipe (Verification rule 1); Makefile recipes with `@apr` are scanned by scripts/check_apr_bin_pinned.sh — pin via scripts/apr_bin.sh if any recipe invokes apr directly (prefer cargo-run/test forms that need no apr binary)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoped test targets with ran-something guards</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 1381-1470 (assert_tests_ran + setfit-tests + setfit-repro-* — the exact guarded-target shape to clone)
    - 04-02..04-09 SUMMARYs (the actual test counts each suite produced — the guard minimums come from MEASURED counts, not guesses)
  </read_first>
  <action>
    Add guarded targets (each: run scoped cargo test into a log with rc captured directly —
    `cmd > log 2>&1; rc=$?` — then assert_tests_ran with a minimum derived from the measured count
    minus a small slack, then exit rc):
    - `setfit-apr-tests`: `cargo test -p aprender-core --features setfit --lib setfit::artifact:: setfit::classify::` (min from SUMMARYs, >= 25 expected)
    - `setfit-codec-tests`: `cargo test -p aprender-train --features setfit --lib setfit::apr_codec::` + `--test setfit_apr_lifecycle` + `--test ui` (ui already tier-wired by Phase 3 — extend the count for the new case)
    - `setfit-cli-tests`: `cargo test -p apr-cli --features setfit --lib` filters for setfit_train, predict, inspect, eval::setfit + the ignored e2e (`-- --include-ignored` on the setfit_train filter)
    - `setfit-serve-tests`: `cargo test -p aprender-serve --features setfit --lib setfit`
    - `setfit-parity`: `cargo test -p apr-cli --features setfit,inference --test setfit_parity` (min >= 8)
    - `setfit-serve-smoke`: the --ignored spawned_serve_smoke run (min 1)
    - `setfit-api-boundary`: cargo tree -e normal for aprender-core and aprender-train, grep-count
      "apr-cli" == 0 — WITH a must-match/must-not-match case table in comments (rule 7): a
      deliberately wrong invocation that WOULD match (e.g. tree of apr-cli itself contains
      apr-cli) proves the grep is live; the real legs must not match. Use `grep -c` on tree output
      (machine output, no comment-line hazard) and compare explicitly — never bare `|| true`.
  </action>
  <verify>
    <automated>make setfit-apr-tests && make setfit-codec-tests && make setfit-cli-tests && make setfit-serve-tests && make setfit-parity && make setfit-api-boundary</automated>
  </verify>
  <acceptance_criteria>
    - All six targets exit 0 with visible counted output; each target's recipe names assert_tests_ran (or an explicit count compare for the boundary gate)
    - Falsification (record in SUMMARY, then revert): temporarily raise one guard minimum above the real count — the target must FAIL; temporarily break one filter to match nothing — the guard must FAIL (the CR-02 proof, performed, not assumed)
    - `bashrs make lint Makefile` reports no new issues for the added recipes
  </acceptance_criteria>
  <done>Every new suite has a named gate that provably cannot pass vacuously</done>
</task>

<task type="auto">
  <name>Task 2: Grow the SAFE-02 feature matrix to four crates + tier wiring</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 382-394 (current setfit-feature-matrix)
    - Makefile tier2/tier3 recipes (where setfit-tests and the phase2/3 audits sit today)
  </read_first>
  <action>
    1. Extend `setfit-feature-matrix` with the two new crates, keeping the existing legs:
       `cargo check -p apr-cli --features setfit`, `cargo check -p apr-cli` (feature off),
       `cargo check -p aprender-serve --features setfit`, `cargo check -p aprender-serve`,
       plus the no-default-features legs the SAFE-02 matrix names for core/train if absent.
       (Do NOT use bare `cargo check --workspace` — Darwin KNOWN-RED via aprender-profile;
       per-crate forms only, per the Pitfall 6 caveat.)
    2. Tier wiring: tier3 gains setfit-apr-tests, setfit-codec-tests, setfit-cli-tests,
       setfit-serve-tests, setfit-parity, setfit-serve-smoke, setfit-api-boundary (beside
       setfit-tests and contract-audit-phase4); the extended setfit-feature-matrix stays where the
       current one is wired. Confirm contract-audit-phase4's bindings: flip any `status: pending`
       equations in contracts/setfit-apr-v1.yaml to bound/implemented for modules that now exist
       (contract EDIT is allowed here only for binding statuses — pv diff will show it; core
       semantic edits remain forbidden).
  </action>
  <verify>
    <automated>make setfit-feature-matrix && make contract-audit-phase4</automated>
  </verify>
  <acceptance_criteria>
    - `make setfit-feature-matrix` exits 0 and its output shows all four crates' legs ran
    - `grep -A 40 "^tier3" Makefile` names every new target (source assertion)
    - `make contract-audit-phase4` exits 0 with zero BIND-001 and the binding statuses reflecting shipped modules
  </acceptance_criteria>
  <done>The feature matrix and tiers cover every crate the setfit feature now spans — locally</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate definitions -> developer trust | a vacuous or unpinned gate converts absence of evidence into evidence of absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | Repudiation | zero-match filters exiting 0 | mitigate | assert_tests_ran on every filtered target + performed falsification (raise-min / break-filter, reverted) |
| T-04-31 | Repudiation | rc read through a pipe | mitigate | `> log 2>&1; rc=$?` form in every recipe (Verification rule 1 — shipped twice before) |
| T-04-32 | Spoofing | boundary grep matching nothing forever | mitigate | must-match/must-not-match case table executed once and recorded (rule 7) |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- All targets green locally with the Darwin caveats honored (no bare --workspace check; arm64 tier2 clippy KNOWN-RED untouched; CARGO_INCREMENTAL=0 for the long runs)
- rtk hook: any porcelain-emptiness assertion goes through `rtk proxy`
</verification>

<success_criteria>
- SAFE-02's local half: four-crate matrix + named tier gates that run everything they claim
- Both CR-01 (unrun surface) and CR-02 (vacuous filter) are structurally impossible for the new surfaces
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-10-SUMMARY.md` when done.
Record: each target's measured count + guard minimum, the falsification transcript, tier placements.
</output>
