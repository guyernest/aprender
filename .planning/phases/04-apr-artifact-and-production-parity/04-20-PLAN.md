---
phase: 04-apr-artifact-and-production-parity
plan: 20
type: execute
wave: 11
gap_closure: true
depends_on: []          # patches code delivered by 04-01..04-17; no NEW plan precedes it in wave 11
files_modified:
  - crates/apr-cli/src/commands/setfit_train.rs
  - crates/apr-cli/src/commands/eval/setfit.rs
  - crates/apr-cli/src/commands/eval/setfit_tests.rs
  - crates/apr-cli/tests/setfit_cli_lifecycle.rs
autonomous: true
requirements: [TRN-07]
closes_findings: [WR-10]

must_haves:
  truths:
    - "`apr eval --lock-out <FILE>` refuses an already-existing lock destination BEFORE it reads the dataset, before it loads any artifact, and before it evaluates any candidate"
    - "The refusal is ORDER-observable: with an existing lock destination AND a deliberately broken later input, the operator is told about the lock, not about the broken input"
    - "The write-time check in `write_lock` stays — it is what covers a file that appeared WHILE the run was going, and removing it must turn a test red"
    - "`--force` still overrides both checks, and the bespoke 'a lock is a COMMITMENT' wording is what the operator sees"
  artifacts:
    - path: "crates/apr-cli/src/commands/setfit_train.rs"
      provides: "refuse_existing_output promoted to pub(crate) so the sibling command can call the standalone gate it was factored out to support"
      contains: "pub(crate) fn refuse_existing_output"
    - path: "crates/apr-cli/src/commands/eval/setfit.rs"
      provides: "the lock-out pre-flight, sited between check_split_flags and the Phase 2 dataset ingest"
    - path: "crates/apr-cli/src/commands/eval/setfit_tests.rs"
      provides: "the ordering proof and the retained-write-time-check proof"
  key_links:
    - from: "crates/apr-cli/src/commands/eval/setfit.rs"
      to: "crates/apr-cli/src/commands/setfit_train.rs"
      via: "the ONE no-clobber gate, called early rather than re-implemented"
      pattern: "setfit_train::refuse_existing_output"
    - from: "crates/apr-cli/src/commands/eval/setfit.rs run()"
      to: "data_contrastive::read_attested_canonical"
      via: "the pre-flight must sit ABOVE this call, which is where the expensive work starts"
      pattern: "read_attested_canonical"
  caveats:
    - "OPS-02 is deliberately NOT claimed in `requirements`, even though the defect lives on the `apr eval` CLI surface. OPS-02's 'a user can complete the lifecycle' clause is blocked behind F-10 and no gap-closure plan in this run can advance it. The honest mapping is TRN-07, whose text is 'a selection-lock record is created before canonical test access is permitted' — this plan changes WHEN that record's creation is refused."
    - "This plan does NOT close F-10. OPS-01, OPS-02, SC1, SC2, SC3 and the 'over a produced artifact' halves of SC4/SC5 remain OPEN — the production-encoder calibration that unblocks them is Phase 5 work per the ROADMAP's explicit blocking note. TRN-07 also stays UNCHECKED: its missing positive 'a user can' tier needs a user-produced artifact, which F-10 prevents."
    - "This plan does NOT close the per-crate cargo-mutants gate (04-11 must-have 4) or the SAFE-02 'in CI' clause. Both stay human_verification items."
    - "WR-01 (the check → rename clobber window inside `atomic_write`) is a DIFFERENT and still-open finding. This plan narrows the window earlier; it does not close WR-01, and the SUMMARY must say so rather than implying the write path is now race-free."
    - "WR-03, WR-04, WR-06, IN-01, IN-02, IN-07 also live in `eval/setfit.rs` and stay open. Do not opportunistically fix them — they are out of the user's scope for this run."
---

<objective>
Close **WR-10**: `apr eval --lock-out` runs the entire multi-candidate sweep — a full bounded read,
the eight-rung load ladder and a classify pass over the whole validation split for every candidate
— before discovering that the lock file it was asked to write already exists.

Purpose: this inverts a discipline the sibling module states in its own header and was refactored
to support. `setfit_train.rs:12-20` says the four request-level checks run "BEFORE a single row of
`--data` is read", and that "refusing the output path last of the four is what stops a
twenty-minute run from ending in 'I will not overwrite that file'". `refuse_existing_output` was
made a standalone function precisely so it could be called early. `apr eval` never calls it early.

Output: a pre-flight sited above the dataset ingest, an ORDER-observable test that proves it, and
a retained write-time check proven still load-bearing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: work on the CURRENT branch `gsd/phase-2-contract-gate`. Do NOT create, rename or switch
branches. Do NOT open a PR.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEW.md
@.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md

Measured at HEAD `81652bb50` during planning — re-confirm before editing, do not re-derive:

- `crates/apr-cli/src/commands/eval/setfit.rs:197` — `pub(crate) fn run(args: &SetFitEvalArgs<'_>)`.
  Its step (1) comment already claims the right principle: *"THE FLAGS FIRST, before a byte of the
  dataset or the artifact is read. A run that spends a minute loading a corpus and then says
  '--selection-lock is required' has told the operator something it knew before it started."*
  Step (1) resolves `args.data` and `args.selection` and calls `check_split_flags(args)?` at :217.
  Step (2) at :220-228 calls `data_contrastive::read_attested_canonical`,
  `data_contrastive::read_selection_manifest` and `Selection::replay` — this is where the expensive
  work starts. Step (3) at :234 calls `reload_artifact`.
- `crates/apr-cli/src/commands/eval/setfit.rs:246` — `fn check_split_flags(args) -> Result<()>`,
  the natural home or immediate predecessor of the pre-flight. Its doc already states the governing
  rule: *"A flag silently ignored is worse than a flag refused."* It refuses `--selection-lock` on
  validation and `--lock-out` / `--candidate` / missing `--selection-lock` on test.
- `crates/apr-cli/src/commands/eval/setfit.rs:308` — `fn run_validation(...)`. The sweep is
  :318-341 (own artifact evaluated, then a `for path in args.candidates` loop where each iteration
  does a full `reload_artifact` + `evaluate_one`). `create_selection_lock` is at :347 and
  `write_lock(destination, &lock, args.force)?` at :352.
- `crates/apr-cli/src/commands/eval/setfit.rs:599` — `fn write_lock(destination, lock, force)`.
  Its first act is `if destination.exists() && !force` with the bespoke *"already exists. A
  selection lock is a COMMITMENT, so overwriting one is never implicit…"* message, and it then
  delegates to `crate::commands::setfit_train::atomic_write(destination, &lock.to_canonical_bytes(),
  force)`. The comment at :610-621 explains that the second check inside `atomic_write` is not
  redundant: it covers a file that appeared WHILE the run was going.
- `crates/apr-cli/src/commands/setfit_train.rs:199` — `fn refuse_existing_output(target, force)` is
  **private**. Its doc at :191-198 reads *"The no-clobber gate, callable on its own so it can run
  BEFORE training"* — a stated purpose the current visibility prevents any other module from using.
  `atomic_write` at :176 is already `pub(crate)` and calls it at :177.
- `crates/apr-cli/src/commands/setfit_train.rs:12-20` — the module header stating the ordering
  discipline this plan restores.
- Test wiring: `crates/apr-cli/src/commands/eval/setfit.rs:740-741` is
  `#[path = "setfit_tests.rs"] mod setfit_tests;`.
- `Makefile:2140` — `setfit-cli-eval-tests` runs
  `cargo test -p apr-cli --features setfit --lib eval::setfit` with `assert_tests_ran ... 13`.
  Measured at verification: 16 passed. Adding tests stays above the floor, so this plan does NOT
  touch the Makefile — a sibling wave-1 plan owns it.

<interfaces>
From `crates/apr-cli/src/commands/setfit_train.rs`:
  pub(crate) fn atomic_write(target: &Path, bytes: &[u8], force: bool) -> Result<()>;
  fn refuse_existing_output(target: &Path, force: bool) -> Result<()>;   // currently PRIVATE
      // returns CliError::ValidationFailed("Refusing to replace existing file {} (pass --force
      // to replace it)") when !force && target.exists()

From `crates/apr-cli/src/commands/eval/setfit.rs`:
  pub(crate) struct SetFitEvalArgs<'a> {
      artifact: &'a Path,
      data: Option<&'a Path>,
      selection: Option<&'a Path>,
      split: Split,
      lock_out: Option<&'a Path>,        // :99
      selection_lock: Option<&'a Path>,
      candidates: &'a [PathBuf],
      force: bool,                       // :105
      json: bool,
  }
  pub(crate) fn run(args: &SetFitEvalArgs<'_>) -> Result<()>;            // :197
  fn check_split_flags(args: &SetFitEvalArgs<'_>) -> Result<()>;        // :246
  fn write_lock(destination: &Path, lock: &SelectionLock, force: bool) -> Result<()>;  // :599
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the ordering proof — a RED test that observes the refusal happening after the expensive work</name>

  <read_first>
    - `crates/apr-cli/src/commands/eval/setfit.rs` lines 190-360 in one pass: `run`,
      `check_split_flags`, `reload_artifact` and `run_validation` including the candidate sweep.
      This is the control flow the test must pin, and the file being modified.
    - `crates/apr-cli/src/commands/eval/setfit_tests.rs` in full — the existing test conventions,
      what fixtures already exist, and how a `SetFitEvalArgs` is constructed in a test.
    - `crates/apr-cli/src/commands/setfit_train.rs` lines 12-20 (the ordering discipline being
      restored) and 176-207 (`atomic_write` and `refuse_existing_output`) — the SOURCE OF TRUTH
      for both the rule and the gate.
    - `04-REVIEW.md` § WR-10 — its concrete failure narrative names the exact invocation and the
      exact wasted work.
    - `crates/apr-cli/src/dispatch_analysis.rs` lines 744-800 — **the reason a naive live probe
      cannot work.** `:759` reads the SetFit tag BEFORE `commands::eval::setfit::run` is called, so
      an UNTAGGED artifact is refused at the flag table (`:787`) with *"--lock-out applies only to
      a setfit-apr-v1 artifact, and {path} does not carry the SetFit tag"* and the new pre-flight
      is never reached. Read this before designing any end-to-end evidence for this plan.
    - `crates/apr-cli/tests/setfit_cli_lifecycle.rs:731` — `write_tagged_decoy(dir, name)`, whose
      own doc comment says it exists to get past exactly that gate from a spawned process. Existing
      callers at `:806` and `:1047` show the spawn-and-assert shape to copy.
    - `CLAUDE.md` § "Verification Discipline" rule 1 (never read `$?` through a pipe) and rule 6
      (one failing input is an anecdote — this test varies the LATER input deliberately, which is
      what makes it an ordering proof rather than a refusal proof).
  </read_first>

  <behavior>
    - Test 1 (the ordering proof, RED at HEAD): construct a `--split validation` invocation whose
      `--lock-out` destination ALREADY EXISTS and whose `--data` directory does NOT exist. Today
      the dataset read at step (2) fails first, so the operator hears about `--data`. After the fix
      the operator hears the bespoke "a lock is a COMMITMENT" refusal. Assert on the MESSAGE, and
      assert the negative too: the error text must NOT name the missing dataset. Both directions in
      one test — a one-sided assertion would pass on a run that failed for a third reason.
    - Test 2 (second ordering witness, varying the broken input): same existing `--lock-out`, but
      this time `--data` is valid and the ARTIFACT path does not exist, so step (3) would fail.
      After the fix the lock refusal still wins. Two different later-stage failures both losing to
      the pre-flight is what distinguishes "the check moved earlier" from "the check happened to
      beat one particular error".
    - Test 3 (`--force` still overrides): the same existing-destination setup with `force: true`
      proceeds past the pre-flight and fails (or succeeds) on the LATER input, proving the
      pre-flight is gated on `force` exactly like the two checks it joins.
    - Test 4 (the write-time check is still load-bearing): a destination that does NOT exist at
      pre-flight time but is created before `write_lock` runs is still refused. Drive `write_lock`
      directly for this case rather than trying to win a race in `run` — the property under test is
      that the second check was not deleted as "now redundant".
  </behavior>

  <files>crates/apr-cli/src/commands/eval/setfit_tests.rs</files>

  <action>
Write Tests 1-4 into `crates/apr-cli/src/commands/eval/setfit_tests.rs` and run them BEFORE any
production change, so the RED is real rather than reconstructed. Record the exact failure output of
Tests 1 and 2 at HEAD in the SUMMARY — those two are the plan's evidence that the ordering was
wrong, and 02-09's checker finding (a task whose tests could not compile until the interface
existed, so it had no executable RED) is the failure mode to avoid here. These tests CAN run at
HEAD because every symbol they touch already exists; if any of them cannot compile at HEAD, stop
and report rather than manufacturing a RED afterwards.

Name the tests so the property is unmistakable, for example
`lock_out_refuses_an_existing_destination_before_the_dataset_is_read`,
`lock_out_refuses_an_existing_destination_before_the_artifact_is_loaded`,
`lock_out_force_proceeds_past_the_preflight`, and
`write_lock_still_refuses_a_destination_that_appeared_mid_run`.

For Tests 1 and 2 assert on the returned `CliError` variant AND on distinguishing substrings:
positively on `A selection lock is a COMMITMENT` (the bespoke wording that must survive), and
negatively on the substring the pre-fix path would have produced — the `--data` requirement text
for Test 1, and the artifact/load-failure text for Test 2. Read the exact current strings out of
the source rather than inventing them.

For Test 4, create the destination file after obtaining a `SelectionLock` and before calling
`write_lock`, then assert the refusal. Use the crate's existing helper for building a
`SelectionLock` in tests if one exists in `setfit_tests.rs`; if none does, construct the minimal
valid one through the public door the file already uses and say so in a comment.

Use `expect(..)` with a stated reason, never `unwrap()` — the project bans it, and although
`crates/apr-cli/src/lib.rs:9-16` currently makes that ban lint-inert (IN-06 / D-04-14-B, out of
scope here), the rule still applies by discipline.
  </action>

  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib eval::setfit > /tmp/04-20-t1.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "test result:|lock_out_refuses|write_lock_still" /tmp/04-20-t1.log</automated>
  </verify>

  <acceptance_criteria>
    - All four tests compile and RUN at HEAD before any production edit. Record the HEAD run's
      output: Tests 1 and 2 FAIL, Tests 3 and 4 PASS. Capture the status with
      `cmd > log 2>&1; rc=$?` — never through a pipe.
    - The two RED failures are recorded verbatim in the SUMMARY, including which unrelated error
      message the pre-fix code produced instead of the lock refusal. This is the ordering evidence;
      a paraphrase is not.
    - `cargo test -p apr-cli --features setfit --lib eval::setfit` at HEAD shows a `test result:`
      line with exactly 2 failed, and the two names are Tests 1 and 2. If any OTHER test fails,
      stop and report — the phase's known-red baseline does not include `eval::setfit`.
    - The tests assert both directions: `rtk proxy grep -c 'assert!(!' crates/apr-cli/src/commands/eval/setfit_tests.rs`
      increases by at least 2 relative to HEAD, corresponding to the two negative assertions in
      Tests 1 and 2. Record both counts.
  </acceptance_criteria>

  <done>Four named tests exist, ran at HEAD, and two of them failed for the reason WR-10 describes — with the pre-fix error message recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: call the standalone gate early — the pre-flight above the dataset ingest</name>

  <files>crates/apr-cli/src/commands/setfit_train.rs, crates/apr-cli/src/commands/eval/setfit.rs, crates/apr-cli/tests/setfit_cli_lifecycle.rs</files>

  <read_first>
    - `crates/apr-cli/src/commands/setfit_train.rs` lines 176-210 — `atomic_write` and
      `refuse_existing_output` with its doc comment, the file being modified. Note that
      `atomic_write` is already `pub(crate)` while the gate it calls is private, which is the
      asymmetry this task removes.
    - `crates/apr-cli/src/commands/eval/setfit.rs` lines 190-250 as read in Task 1 — the insertion
      point, and `check_split_flags`'s doc comment whose rule the pre-flight extends.
    - `crates/apr-cli/src/commands/eval/setfit.rs` lines 594-625 — `write_lock` and the comment
      block explaining why its check is not redundant with `atomic_write`'s. That argument now
      covers THREE checks and its wording must be updated to say so.
    - The Task 1 tests as written — the production change must turn exactly Tests 1 and 2 green
      without touching Tests 3 and 4.
  </read_first>

  <action>
Promote `refuse_existing_output` in `crates/apr-cli/src/commands/setfit_train.rs:199` from `fn` to
`pub(crate) fn`. Extend its doc comment: it currently justifies the double check between the
pre-flight and the write. There are now three call sites in two commands — `apr setfit train`'s
pre-flight, `apr eval --lock-out`'s pre-flight, and `atomic_write`'s write-time check — so state
that, and state that the write-time check remains the one that covers a file appearing mid-run.
Change nothing else in that file; the writer's behaviour is untouched.

In `crates/apr-cli/src/commands/eval/setfit.rs::run`, insert the pre-flight immediately after
`check_split_flags(args)?` at :217 and strictly BEFORE the step (2) block that calls
`data_contrastive::read_attested_canonical`. Guard it on `args.lock_out` being `Some` and pass
`args.force` through, so `--force` behaves identically at both the pre-flight and the write.

Reproduce the BESPOKE refusal, not the generic one. `refuse_existing_output` returns *"Refusing to
replace existing file {} (pass --force to replace it)"*, but `write_lock`'s message —
*"{} already exists. A selection lock is a COMMITMENT, so overwriting one is never implicit: pass
--force if you intend to replace the committed decision, and be aware that any test measurement
taken under the old lock no longer describes the selection this file records."* — says materially
more and is what the Task 1 tests assert on. Factor that message into one place so the pre-flight
and `write_lock` cannot drift into two wordings for one refusal: extract the existing check-and-
message from `write_lock` into a small private helper in `eval/setfit.rs` (for example
`refuse_existing_lock(destination, force)`), call it from both the pre-flight and from `write_lock`
at :600, and have that helper be the single producer of the COMMITMENT text.

Keep `write_lock`'s call to `crate::commands::setfit_train::atomic_write(destination, ..., force)`
exactly as it is. `atomic_write` still runs `refuse_existing_output` internally, and that third
check is what covers the mid-run appearance Task 1's Test 4 pins. Update the comment block at
:610-621 so it describes three checks in ordering terms — pre-flight (before any input is read),
write_lock (before bytes are produced), atomic_write (immediately before the rename) — rather than
the two it currently describes.

Update the step (1) comment in `run` so it names the new member of the group. It currently lists
only flag consistency; the pre-flight makes it *"the flags AND the output path first"*, which is
the same four-check shape `setfit_train.rs:12-20` states.

Do not touch `run_validation`, the candidate sweep, `create_selection_lock`, or any other finding
living in this file (WR-03, WR-04, WR-06, IN-01, IN-02, IN-07 are all out of scope for this run).
  </action>

  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib eval::setfit > /tmp/04-20-t2.log 2>&1; rc=$?; echo "eval_rc=$rc"; cargo test -p apr-cli --features setfit --lib setfit_train > /tmp/04-20-t2b.log 2>&1; rc2=$?; echo "train_rc=$rc2"; grep -E "test result:" /tmp/04-20-t2.log /tmp/04-20-t2b.log</automated>
  </verify>

  <acceptance_criteria>
    - Tests 1 and 2 from Task 1 now PASS, and Tests 3 and 4 still pass:
      `cargo test -p apr-cli --features setfit --lib eval::setfit` exits 0 with 0 failed and a
      passed count at least 20. HEAD baseline measured at 16 with that exact command; note the
      Makefile's own gate table at `Makefile:1918` still records `15 / 0`, so the table is stale by
      one and 04-21 corrects it. Re-measure the baseline yourself before editing.
    - Ordering is a SOURCE fact, not only a test fact: in
      `crates/apr-cli/src/commands/eval/setfit.rs`, the line number of the pre-flight call is
      strictly LESS than the line number of `read_attested_canonical`. Verify by reading both from
      `rtk proxy grep -n 'refuse_existing_lock\|read_attested_canonical\|check_split_flags' crates/apr-cli/src/commands/eval/setfit.rs`
      and asserting `check_split_flags < pre-flight < read_attested_canonical`. Record the three
      line numbers in the SUMMARY.
    - `rtk proxy grep -c 'pub(crate) fn refuse_existing_output' crates/apr-cli/src/commands/setfit_train.rs`
      returns exactly 1.
    - The COMMITMENT message has exactly ONE producer:
      `rtk proxy grep -c 'A selection lock is a COMMITMENT' crates/apr-cli/src/commands/eval/setfit.rs`
      returns exactly 1 in non-test source. If the count is 2, the pre-flight duplicated the string
      instead of sharing it.
    - The third check survives: delete the `refuse_existing_output(target, force)?;` line inside
      `atomic_write`, confirm Task 1's Test 4 turns RED, and revert. Record the assertion message.
      This is CLAUDE.md rule 4 in miniature — the pre-flight extended the gate's SCOPE, so the
      write-time proof must be re-run in the new arrangement rather than assumed to transfer.
    - `apr setfit train`'s own behaviour is unregressed:
      `cargo test -p apr-cli --features setfit --lib setfit_train` exits 0, and
      `make setfit-cli-train-tests` exits 0 with its floor satisfied.
    - `make setfit-cli-eval-tests` exits 0 with its `assert_tests_ran` floor of 13 satisfied.
    - End-to-end evidence comes from a SPAWNED test, not an ad-hoc shell probe, because the
      naive probe cannot reach the code under test. Measured at HEAD: `dispatch_analysis.rs:759`
      reads the SetFit tag before `eval::setfit::run`, so an untagged artifact is refused at `:787`
      with *"--lock-out applies only to a setfit-apr-v1 artifact, and {path} does not carry the
      SetFit tag"*, and no `*.apr` in this repo carries the tag (the nearest,
      `tests/fixtures/setfit/slice_model.apr`, has `model_type: "Bert"`) — F-10 guarantees none can
      be produced. Add the case to `crates/apr-cli/tests/setfit_cli_lifecycle.rs` instead, building
      its input with the in-file `write_tagged_decoy(dir, name)` helper at `:731`, which exists to
      pass that gate from a spawned process. The spawned case must: create the `--lock-out`
      destination first, point `--data` at a nonexistent directory, spawn the binary via
      `env!("CARGO_BIN_EXE_apr")` (never PATH, per the file's own rung-0 discipline), read the
      verdict off a reaped `ExitStatus`, and assert the COMMITMENT text appears while the `--data`
      text does not. If the decoy cannot get far enough to reach the pre-flight — a real
      possibility worth measuring rather than assuming, since the decoy is tagged but not loadable
      — record the case as an explicit NOT-RUN with the measured reason and rely on Task 1's two
      in-process ordering tests as the plan's evidence. Do NOT report an unreachable probe as
      passing.
  </acceptance_criteria>

  <done>The no-clobber gate runs before any input is read, proven by two in-process ordering tests with DIFFERENT later-stage failures and by a source-level ordering assertion; end-to-end evidence is either a spawned `setfit_cli_lifecycle` case built on `write_tagged_decoy`, or that case's explicit NOT-RUN reason — never an unreachable probe reported as passing. The write-time check is retained and proven still load-bearing by an induced deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator invocation → `apr eval --lock-out <FILE>` | The destination path is operator-supplied and may already hold a committed selection lock — an artifact whose whole purpose is to be a commitment that predates canonical test access. |
| filesystem ↔ the eval process during the sweep | Between the pre-flight and the rename, another process can create or replace the destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-78 | Denial of Service | `eval/setfit.rs::run_validation` candidate sweep | mitigate | The live finding, and the cheap half of the risk: an operator burns N full artifact loads and N validation sweeps before being told a fact known at argument-parse time. Task 2 sites the check above `read_attested_canonical`, restoring the discipline `setfit_train.rs:12-20` states. |
| T-04-79 | Tampering | an existing committed `SelectionLock` at the `--lock-out` destination | mitigate | A selection lock gates canonical test access (TRN-07 / D-16). Silently replacing one would invalidate every test measurement taken under it while leaving the file looking current. Three ordered checks now stand between an operator and that outcome, all gated on `--force`, and Task 2's acceptance criteria prove the third was not deleted as "now redundant". |
| T-04-80 | Tampering | the check → `fs::rename` window inside `atomic_write` | accept | **Explicitly NOT closed here.** WR-01 is the finding that `fs::rename` replaces the destination unconditionally, so a file created during the temp write is destroyed without `--force`; the correct fix is taking the destination with `O_CREAT|O_EXCL` first. This plan narrows the window by refusing earlier and does not claim to close it. Recorded so the SUMMARY cannot imply the write path is race-free. |
| T-04-81 | Information Disclosure | the refusal message | accept | It names the destination path the operator supplied and the `--force` remedy. It reveals nothing about the lock's contents, the artifact, or the dataset. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: no dependency is added and no `Cargo.toml` is in `files_modified`. If a task finds itself needing a new crate, stop and report. |
</threat_model>

<verification>
Run at the end of the plan, statuses captured with `cmd > log 2>&1; rc=$?` and never through a pipe:

1. `make setfit-cli-eval-tests` — rc=0, floor 13 satisfied, 0 failed. (The floor is raised to
   track the new count by 04-22 in wave 12; do NOT raise it here — 04-21 is editing the Makefile in
   this wave and this plan does not own it.)
2. `make setfit-cli-train-tests` — rc=0, floor satisfied (proves the visibility change regressed
   nothing in the writer).
3. `cargo test -p apr-cli --features setfit --lib` — rc=0, passed count at least 6750. Measured at
   HEAD `81652bb50` with exactly that command: **6756 passed, 0 failed, 15 ignored**. The 6752
   figure in `04-VERIFICATION.md` is the `0fb47958f` baseline and is stale by three commits.
   Re-measure and record your own number.
4. `make setfit-cli-lifecycle` — rc=0 (the spawned OPS-02 ladder; proves the CLI still behaves at
   the process tier). Note its rung 4 is EXPECTED to exit 6 with an `F10_CLOSED` canary message —
   that is the standing F-10 blocker, not a regression from this plan.
5. `make setfit-feature-matrix` — rc=0.
6. `cargo test -p apr-cli --features setfit --test setfit_cli_lifecycle -- --ignored` — rc=0, with
   the new spawned case's verdict and verbatim message recorded, or its explicit NOT-RUN reason.
   Note this target is the same one `make setfit-cli-lifecycle` drives, so if the case lands the
   Make gate covers it automatically; confirm which by reading the recipe rather than assuming.
</verification>

<success_criteria>
- `apr eval --lock-out` refuses an existing destination before reading the dataset and before
  loading any artifact.
- Two ordering tests with DIFFERENT later-stage failures both observe the lock refusal winning —
  one failing input would have been an anecdote.
- The write-time check is retained and was proven still load-bearing by deleting it and observing
  RED.
- The COMMITMENT wording has exactly one producer in non-test source.
- The SUMMARY states plainly that WR-01 remains OPEN and that this plan does not make the write
  path race-free; and that F-10, the per-crate mutation gate and the SAFE-02 "in CI" clause remain
  OPEN, with OPS-01 and OPS-02 still NOT MET.
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-20-SUMMARY.md` when done.

The SUMMARY must record: the HEAD run of Task 1's four tests with the two verbatim RED messages and
the unrelated errors the pre-fix code produced instead; the three line numbers proving the source
ordering; the induced-deletion RED for the retained write-time check; the spawned
`setfit_cli_lifecycle` case's verdict and verbatim message, OR its explicit NOT-RUN reason with the
measurement that established it; and the explicit open-items statement including WR-01.
</output>
