---
phase: 04-apr-artifact-and-production-parity
plan: 05
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - crates/aprender-train/src/train/setfit/apr_codec.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/aprender-train/src/train/setfit/verify.rs
autonomous: true
requirements: [APR-03]
must_haves:
  truths:
    - "verify_artifact(AprCodec) closes the in-memory model, reloads through the codec, passes the byte-canonical closure check (serialize(deserialize(bytes)) == bytes) and Tolerance::EXACT comparison, and mints ArtifactReloadedAndVerified"
    - "The bytes AprCodec produces load through the PRODUCTION core loader (load_setfit_apr) including probe replay — the train-time artifact IS a valid production artifact"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/apr_codec.rs"
      provides: "sealed AprCodec: SetFitCodec writing/reading setfit-apr-v1 via core"
      contains: "setfit-apr-v1"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/apr_codec.rs"
      to: "aprender::setfit::artifact::write_setfit_apr"
      via: "serialize delegates format semantics to core"
      pattern: "write_setfit_apr"
    - from: "crates/aprender-train/src/train/setfit/apr_codec.rs"
      to: "aprender::setfit::artifact::read_setfit_apr_parts"
      via: "deserialize parses through core's rungs 1-5 (one ladder, landed in 04-03)"
      pattern: "read_setfit_apr_parts"
    - from: "crates/aprender-train/src/train/setfit/verify.rs"
      to: "AprCodec"
      via: "run_verify_policy closure + EXACT comparison over the new codec"
      pattern: "AprCodec"
---

<objective>
Implement the `AprCodec` adapter behind Phase 3's sealed `SetFitCodec` seam — the ~100-line
bytes<->bundle adapter whose shape verify.rs's module docs literally prescribe — and prove APR-03:
the trusted policy closes the model, reloads the real `setfit-apr-v1` bytes, survives the
byte-canonical closure check at Tolerance::EXACT, and the SAME bytes load through the production
core loader.

Purpose: the format swaps behind the seam; the check does not change (Ph3 D-07 as amended).

Output: apr_codec.rs, seal visibility one-liner.

NOTE: OPS-01's public-API lifecycle proof (train -> save -> load -> embed -> classify -> inspect)
lives in 04-12 (wave 5), NOT here — its classify step needs 04-04's `VerifiedSetFitModel::classify`
+ `ClassifyResponse`, which land in the PARALLEL wave-4 plan and cannot be consumed in-wave.

Branch: `gsd/phase-4-apr-parity`. Wave 4, parallel with 04-04. HARD CONSTRAINT: this plan touches
aprender-train files ONLY — zero overlap with 04-04, which owns core setfit files this wave.
Everything this plan needs from core (write_setfit_apr, load_setfit_apr, read_setfit_apr_parts,
VerifiedSetFitModel::embed) landed in 04-02/04-03. If a core change seems needed, STOP and
surface — do not edit crates/aprender-core/**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
From verify.rs (verified on branch):
- `mod sealed { pub trait Sealed {} }` — PRIVATE to verify.rs today (lines 61-65); the one-line visibility decision: make it `pub(crate) mod sealed` so apr_codec.rs (same crate) can impl
- `trait SetFitCodec: sealed::Sealed { fn format_id(&self) -> &'static str; fn serialize(&self, &SetFitBundle) -> Result<Vec<u8>, CodecError>; fn deserialize(&self, &[u8]) -> Result<SetFitBundle, CodecError>; }` (lines 83-100)
- `CodecError::{Bundle { format_id, source: BundleError }, ForeignFormat { expected, got }}` — inner BundleError preserved, never stringified
- `run_verify_policy` closure check: serialize(deserialize(bytes)) == bytes or SetFitTrainError::ReloadNotFromBytes (close_round_trip, lines 496-529); Tolerance::EXACT at 250-253; format id stamped by SetFitBundle::from_run_parts(codec.format_id(), ..) (290-297)
From bundle.rs:317-356 — SetFitBundle pub(crate) fields (same crate: direct access):
schema_version, format_id, architecture, tokenizer_bytes_hex, pooling, normalization, l2_epsilon,
truncation_max_sequence_length, padding_mode, max_length, root_seed, tensors (BTreeMap<String,BundleTensor>),
head_weights_hex, head_intercepts_hex, head_n_features, ordered_labels, requested_config,
resolved_config (ResolvedConfigRecord), evidence (EvidenceSummary)
From 04-02/04-03 (core, on branch — this plan CONSUMES these, it does not edit core):
- `write_setfit_apr(&SetFitArtifactView) -> Result<Vec<u8>, SetFitArtifactError>`, `load_setfit_apr`, `SetFitArtifactView`, `SetFitArtifactDoc`
- `read_setfit_apr_parts(bytes: &[u8]) -> Result<(SetFitArtifactDoc, BTreeMap<String,(Vec<usize>,Vec<f32>)>, Vec<u8>), SetFitArtifactError>` — parse-only door, EXACTLY rungs 1-5 (landed in 04-03 Task 1; load_setfit_apr calls it internally, so there is ONE ladder)
- `VerifiedSetFitModel::{embed(texts) -> Result<Vec<Vec<f32>>, _>, artifact_sha256(), ordered_labels(), doc_view()}` — classify arrives via 04-04 in the parallel wave-4 plan (consumed by 04-12's wave-5 lifecycle test, NOT by this plan)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AprCodec — sealed adapter over core's writer/loader</name>
  <files>crates/aprender-train/src/train/setfit/apr_codec.rs, crates/aprender-train/src/train/setfit/mod.rs, crates/aprender-train/src/train/setfit/verify.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/verify.rs lines 20-29 (the prescribed adapter shape), 61-65 (seal), 149-192 (SerdeJsonCodec — copy the deliberate redundant format-id self-check and its comment), 496-529 (closure obligation)
    - crates/aprender-train/src/train/setfit/bundle.rs lines 317-420 (field list + hex encode/decode helpers — reuse them, do not re-implement hex)
    - crates/aprender-core/src/setfit/artifact.rs (READ ONLY — SetFitArtifactView/Doc field shapes you map onto, and read_setfit_apr_parts' signature from 04-03)
  </read_first>
  <behavior>
    - Test: AprCodec.serialize(bundle) produces bytes that AprCodec.deserialize returns to an == bundle (round trip at the bundle level)
    - Test: serialize(deserialize(bytes)) == bytes byte-for-byte (the closure the trusted policy will run) — including run TWICE for stability
    - Test: deserialize on SerdeJsonCodec-produced bytes -> CodecError (foreign format refused at the container parse; wrong magic)
    - Test: a bundle whose format_id is not "setfit-apr-v1" round-tripped through raw doc surgery -> ForeignFormat from the codec's own self-check
  </behavior>
  <action>
    1. In verify.rs: change `mod sealed` to `pub(crate) mod sealed` (the single sanctioned
       visibility edit — PATTERNS calls it a one-line decision; nothing else in verify.rs changes).
    2. New apr_codec.rs: `pub const APR_FORMAT_ID: &str = "setfit-apr-v1";`
       `pub struct AprCodec;` + `impl super::verify::sealed::Sealed for AprCodec {}` +
       `impl SetFitCodec for AprCodec`:
       - serialize: map SetFitBundle fields 1:1 into core's SetFitArtifactView (hex fields decoded
         via bundle.rs's existing helpers; tensors BundleTensor -> (shape, Vec<f32>)), call
         write_setfit_apr, map SetFitArtifactError into CodecError::Bundle with
         format_id = APR_FORMAT_ID (preserve the typed source — never stringify).
       - deserialize: parse bytes via core's `read_setfit_apr_parts` (landed in 04-03 — EXACTLY
         rungs 1-5; the probe-replay rung stays in the trusted policy's domain, the codec's job is
         bytes<->bundle; one ladder shared with load_setfit_apr, Pitfall 9), then rebuild
         SetFitBundle with format_id from the doc;
       - redundant format-id self-check per the SerdeJsonCodec comment (both checks exist on purpose).
       Note the closure consequence: probes are recomputed inside write_setfit_apr from the view's
       tensors on every serialize — deterministic CPU path, so recomputed probe records are
       byte-identical to the embedded ones. The closure test proves this.
    3. Register `pub mod apr_codec;` in setfit/mod.rs. SerdeJsonCodec stays untouched (it is
       Phase 3's interim format and its tests still bind contract equations — do not delete or
       rename anything existing; D-15-style non-regression).
    Wave-4 file-ownership rule: do NOT edit anything under crates/aprender-core/ — 04-04 runs in
    parallel and owns the core setfit files. read_setfit_apr_parts already exists; if it appears
    missing, the 04-03 dependency did not land — STOP and surface, do not add it here.
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --lib setfit::apr_codec::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --lib setfit::apr_codec::` exits 0 with >= 5 tests passed
    - apr_codec.rs contains `impl SetFitCodec for AprCodec` and the string "setfit-apr-v1"
    - deserialize calls core's parse door: `grep -c "read_setfit_apr_parts" crates/aprender-train/src/train/setfit/apr_codec.rs` >= 1
    - `git diff --name-only` contains NO path under crates/aprender-core/ (wave-4 ownership assertion)
    - `git diff crates/aprender-train/src/train/setfit/verify.rs` touches ONLY the sealed-module visibility line
  </acceptance_criteria>
  <done>The codec is a thin adapter; format semantics live in core; the seam's obligations hold</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: APR-03 round trip through the trusted policy + production-loader cross-check</name>
  <files>crates/aprender-train/src/train/setfit/apr_codec.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/verify.rs (verify_artifact / run_verify_policy call shape; how Phase 3 tests drive it with SerdeJsonCodec — mirror those tests with AprCodec)
    - crates/aprender-train/src/train/setfit/lock.rs + mod.rs (the bytes-reconstruction path Phase 3 D-08 shipped — the door 04-07's eval will also use)
  </read_first>
  <behavior>
    - Test: a completed tiny-fixture run -> verify_artifact(AprCodec) succeeds -> ArtifactReloadedAndVerified state exists; recorded artifact hash == artifact_sha256_hex of the bytes
    - Test: the SAME bytes pass core load_setfit_apr including probe replay (train-time artifact == production artifact)
    - Test: a mutated byte (one tensor byte flipped, CRC re-signed via a full rewrite) -> verify_artifact fails typed (EXACT tolerance catches it) — the in-band negative for this plan
    - Test: Tolerance::EXACT holds (A2 falsification — if this fails, STOP and surface; the fix is trusted-code tolerance keyed on codec, never a trait parameter)
  </behavior>
  <action>
    Extend apr_codec.rs tests (reusing Phase 3's tiny-fixture run builders — read how existing
    verify tests construct a HeadFitted run cheaply): drive the REAL verify_artifact with AprCodec,
    assert minting, closure, hash stability across two runs (determinism inherited from 04-02),
    and cross-check the emitted bytes through aprender::setfit::artifact::load_setfit_apr —
    the production loader, probe replay included. This is the APR-03 sentence made executable:
    "training closes the in-memory model, reloads the written APR through the production core
    loader, and verifies exact tokenizer/configuration/tensor state plus tolerance-bounded outputs."
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --lib setfit::apr_codec::round_trip</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --lib setfit::apr_codec::round_trip` exits 0 with >= 4 tests passed
    - One test asserts the minted state's recorded hash equals core's artifact_sha256_hex over the same bytes (one hash, two witnesses)
    - The negative test fails through the trusted policy (ReloadNotFromBytes or comparison failure), asserted via matches! on the typed error
  </acceptance_criteria>
  <done>APR-03 proven end-to-end with the shipped container; EXACT tolerance validated or loudly falsified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codec bytes -> trusted policy | the policy must not trust the codec's claims; closure + hash discipline enforce it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Spoofing | codec reporting a foreign payload as its own | mitigate | trusted decode re-checks format id + codec's redundant self-check (both, deliberately) |
| T-04-15 | Repudiation | codec hashing its own output | mitigate | artifact_hash stays a trusted free function; codec never computes digests (verify.rs:198-205) |
| T-04-16 | Tampering | nondeterministic serialize breaking closure | mitigate | 04-02's cross-process determinism + this plan's double-closure test |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Both scoped filters green with counted tests; `cargo test -p aprender-train --features setfit --lib setfit::` unaffected outside the new module (known-red baseline untouched)
- `git diff --name-only` for this plan's commits contains only crates/aprender-train/ paths (wave-4 parallelism contract with 04-04)
</verification>

<success_criteria>
- APR-03's full sentence executable and green; the format swapped behind the seam with the check unchanged
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-05-SUMMARY.md` when done.
Record: closure/EXACT outcomes (or the A2 falsification if it fired), test counts.
</output>
