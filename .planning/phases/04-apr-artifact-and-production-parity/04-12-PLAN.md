---
phase: 04-apr-artifact-and-production-parity
plan: 12
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - crates/aprender-train/tests/setfit_apr_lifecycle.rs
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "A Rust caller completes train -> save -> load -> embed -> classify -> inspect purely through public library APIs with zero apr-cli types (OPS-01), proven by an out-of-src integration test"
    - "aprender-core and aprender-train resolve zero dependencies on apr-cli (cargo tree)"
  artifacts:
    - path: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      provides: "public-API-only lifecycle integration test (OPS-01, incl. embed step)"
      contains: "load_setfit_apr"
  key_links:
    - from: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      to: "aprender::setfit::artifact::load_setfit_apr"
      via: "production core loader consumed out-of-crate through the public API"
      pattern: "load_setfit_apr"
    - from: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      to: "VerifiedSetFitModel::classify"
      via: "04-04's D-08 envelope consumed through the public API"
      pattern: "\\.classify\\("
---

<objective>
Prove OPS-01: a Rust caller completes the whole CPU lifecycle — train -> save -> load -> embed ->
classify -> inspect — through stable public library APIs only, with zero apr-cli types, plus the
cargo-tree dependency-direction evidence.

Purpose: this is the relocated 04-05 Task 3. It is its own wave-5 plan because its classify step
consumes `VerifiedSetFitModel::classify` + `ClassifyResponse` from 04-04 AND `AprCodec` from
04-05 — the two PARALLEL wave-4 plans — so it cannot compile inside either wave-4 worktree.
It runs in wave 5 parallel with 04-06: zero file overlap (04-06 owns apr-cli files only; this
plan owns exactly one aprender-train integration-test file).

Output: setfit_apr_lifecycle.rs integration test + boundary evidence recorded in SUMMARY.

Branch: `gsd/phase-4-apr-parity`. HARD CONSTRAINT: this plan creates ONE new file —
crates/aprender-train/tests/setfit_apr_lifecycle.rs. Do not edit src/ anywhere. If a public-API
gap blocks the test (a lifecycle step not reachable out-of-crate), STOP and surface — that gap is
itself an OPS-01 finding, not something to patch around by editing core or train sources here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md

<interfaces>
From 04-02/04-03 (core, landed):
- `aprender::setfit::artifact::load_setfit_apr` — production loader, full ladder + probe replay
- `VerifiedSetFitModel::{embed(texts), artifact_sha256(), ordered_labels(), doc_view()}`
From 04-04 (core, landed in wave 4 — this plan's first consumer of it outside core):
- `VerifiedSetFitModel::classify` returning the versioned `ClassifyResponse` envelope (D-08): label,
  full ordered probability vector, optional logits, margin, token/truncation facts, latency,
  backend identity, artifact hash
From 04-05 (train, landed in wave 4):
- `AprCodec` (setfit-apr-v1 codec) + `verify_artifact(AprCodec)` — the trusted door to artifact bytes
If `classify` or `AprCodec` appears missing, a wave-4 dependency did not land — STOP and surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPS-01 public-API lifecycle integration test + boundary evidence</name>
  <files>crates/aprender-train/tests/setfit_apr_lifecycle.rs</files>
  <read_first>
    - crates/aprender-train/tests/ (an existing integration test for import/style conventions — tests here see only the PUBLIC API, which is the point)
    - crates/aprender-train/src/train/setfit/mod.rs (the public lifecycle surface: config, run states, verify_artifact door)
    - crates/aprender-core/src/setfit/classify.rs (READ ONLY — ClassifyResponse field names for the envelope assertions)
  </read_first>
  <action>
    New integration test (public API only — this file compiling is itself evidence the API is
    usable out-of-crate): tiny-fixture SetFitTrainConfig -> Prepared -> EncoderTuned -> HeadFitted
    -> verify_artifact(AprCodec) -> take the artifact bytes -> aprender::setfit::artifact::
    load_setfit_apr -> VerifiedSetFitModel::embed on two texts — OPS-01's "embed" step: assert one
    embedding per text at the encoder's hidden dimension, every value finite, each L2 norm within
    tolerance of 1.0 -> VerifiedSetFitModel::classify on two texts -> assert envelope fields
    (labels from ordered_labels, probabilities sum to 1, artifact_sha256 matches) -> doc_view()
    recovers labels + seeds + evidence hash (inspect tier). The full OPS-01 chain — train -> save
    -> load -> embed -> classify -> inspect — with ZERO apr-cli imports; the file's import list is
    the OPS-01 assertion.
    Boundary evidence (recorded in SUMMARY, durable Make gate lands in 04-10):
    run `cargo tree -p aprender-core -e normal | grep -c "apr-cli"` and the same for
    aprender-train; both counts must be 0. Note: use `rtk proxy` if the rtk hook rewrites output.
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle` exits 0 with >= 1 test passed
    - Source assertion: `grep -c "apr_cli\|apr-cli" crates/aprender-train/tests/setfit_apr_lifecycle.rs` == 0
    - The lifecycle test exercises embed: `grep -c "\.embed(" crates/aprender-train/tests/setfit_apr_lifecycle.rs` >= 1
    - The lifecycle test exercises classify: `grep -c "\.classify(" crates/aprender-train/tests/setfit_apr_lifecycle.rs` >= 1
    - `cargo tree -p aprender-core -e normal | grep -c "apr-cli"` outputs 0 (recorded in SUMMARY)
  </acceptance_criteria>
  <done>A Rust caller demonstrably completes the whole lifecycle — embed step included — through stable public APIs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public library API -> out-of-crate caller | OPS-01 claims the lifecycle needs no CLI; the claim must be witnessed, not asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-36 | Repudiation | claiming a public-API-only lifecycle while CLI types leak in | mitigate | grep count == 0 on the test's imports + cargo tree apr-cli count 0 for BOTH library crates (durable Make gate follows in 04-10's setfit-api-boundary) |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Scoped integration-test filter green with a counted, nonzero test run
- `git diff --name-only` for this plan's commits contains ONLY crates/aprender-train/tests/setfit_apr_lifecycle.rs (wave-5 parallelism contract with 04-06, which owns apr-cli files)
- Both cargo tree boundary counts are 0 and recorded in the SUMMARY
</verification>

<success_criteria>
- OPS-01 proven by a compiling, passing public-API-only integration test covering train -> save -> load -> embed -> classify -> inspect + zero-dependency evidence
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-12-SUMMARY.md` when done.
Record: cargo tree counts (both 0), test count, and any public-API gap surfaced (should be none).
</output>
