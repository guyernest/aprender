---
phase: 04-apr-artifact-and-production-parity
plan: 12
type: execute
wave: 5
depends_on: ["04-04", "04-05"]
files_modified:
  - crates/aprender-train/tests/setfit_apr_lifecycle.rs
autonomous: true
requirements: [OPS-01]
must_haves:
  truths:
    - "A Rust caller completes train -> save -> load -> embed -> classify -> inspect purely through public library APIs with zero apr-cli types (OPS-01), proven by an out-of-src integration test"
    - "aprender-core and aprender-train resolve zero dependencies on apr-cli (cargo tree)"
    - "The classify step goes through the shared ClassifyRequestDocument and reads its results through the envelope's accessors — the same types the CLI and HTTP surfaces use"
  artifacts:
    - path: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      provides: "public-API-only lifecycle integration test (OPS-01, incl. embed step)"
      contains: "load_setfit_apr"
  key_links:
    - from: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      to: "aprender::setfit::artifact::load_setfit_apr"
      via: "production core loader consumed out-of-crate through the public API"
      pattern: "load_setfit_apr"
    - from: "crates/aprender-train/tests/setfit_apr_lifecycle.rs"
      to: "VerifiedSetFitModel::classify"
      via: "04-04's D-08 envelope consumed through the public API"
      pattern: "\\.classify\\("
---

<objective>
Prove OPS-01: a Rust caller completes the whole CPU lifecycle — train -> save -> load -> embed ->
classify -> inspect — through stable public library APIs only, with zero apr-cli types, plus the
cargo-tree dependency-direction evidence.

Purpose: this is the relocated 04-05 Task 3. It is its own wave-5 plan because its classify step
consumes `VerifiedSetFitModel::classify` + `ClassifyResponse` from 04-04 AND `AprCodec` from
04-05 — the two PARALLEL wave-4 plans — so it cannot compile inside either wave-4 worktree.
It runs in wave 5 parallel with 04-06 (apr-cli files) and 04-16 (aprender-train src files): this
plan owns exactly one aprender-train integration-test file and nothing else.

This revision also makes the test a second, independent witness for two review fixes: the envelope's
private-field accessor surface must be sufficient for a real out-of-crate consumer (if it is not,
that is an OPS-01 finding, not a reason to make fields public), and the artifact's provenance must
be recoverable through `doc_view()`.

Output: setfit_apr_lifecycle.rs integration test + boundary evidence recorded in SUMMARY.

Branch: `gsd/phase-4-apr-parity`. HARD CONSTRAINT: this plan creates ONE new file —
crates/aprender-train/tests/setfit_apr_lifecycle.rs. Do not edit src/ anywhere. If a public-API
gap blocks the test (a lifecycle step not reachable out-of-crate, or a response field with no
accessor), STOP and surface — that gap is itself an OPS-01 finding, not something to patch around
by editing core or train sources here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
From 04-02/04-03 (core, landed):
- `aprender::setfit::artifact::load_setfit_apr` — production loader, full ladder + probe replay
- `VerifiedSetFitModel::{embed(texts), artifact_sha256(), ordered_labels(), doc_view()}`
From 04-04 (core, landed in wave 4 — this plan's first consumer of it outside core):
- `ClassifyRequestDocument { texts, include_logits }` and `VerifiedSetFitModel::classify(&request)`
  returning the versioned `ClassifyResponse` (D-08). Fields are PRIVATE with read accessors:
  `artifact_sha256()`, `backend()`, `latency_ms()`, `results()`, and per result `label()`,
  `probabilities()`, `logits()`, `margin()`, `token_count()`, `truncated()`
From 04-05 (train, landed in wave 4):
- `AprCodec` (setfit-apr-v1 codec) + `verify_artifact(AprCodec)` — the trusted door to artifact bytes
If `classify` or `AprCodec` appears missing, a wave-4 dependency did not land — STOP and surface.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPS-01 public-API lifecycle integration test + boundary evidence</name>
  <files>crates/aprender-train/tests/setfit_apr_lifecycle.rs</files>
  <read_first>
    - crates/aprender-train/tests/ (an existing integration test for import/style conventions — tests here see only the PUBLIC API, which is the point)
    - crates/aprender-train/src/train/setfit/mod.rs (the public lifecycle surface: config, run states, verify_artifact door)
    - crates/aprender-core/src/setfit/classify.rs (READ ONLY — the ClassifyRequestDocument fields and the accessor names on ClassifyResponse/ClassifyResult)
    - crates/aprender-core/src/setfit/artifact.rs (READ ONLY — doc_view()'s SetFitArtifactDoc field names, including provenance)
  </read_first>
  <action>
    New integration test (public API only — this file compiling is itself evidence the API is
    usable out-of-crate): tiny-fixture `SetFitTrainConfig` -> Prepared -> EncoderTuned -> HeadFitted
    -> `verify_artifact(AprCodec)` -> take the artifact bytes ->
    `aprender::setfit::artifact::load_setfit_apr` -> `VerifiedSetFitModel::embed` on two texts
    (OPS-01's "embed" step: assert one embedding per text at the encoder's hidden dimension, every
    value finite, each L2 norm within tolerance of 1.0) -> build a `ClassifyRequestDocument` with
    two texts (one of them containing an embedded newline, so the shared document's fidelity is
    witnessed here too) and call `classify` -> assert envelope fields THROUGH ACCESSORS (labels
    drawn from ordered_labels, probabilities sum to 1 within 1e-6, artifact_sha256 matches, backend
    non-empty and free of any SIMD capability token, latency finite and non-negative) ->
    `doc_view()` recovers labels, seeds, evidence table hash and the provenance dataset fingerprint
    (inspect tier). The full OPS-01 chain — train -> save -> load -> embed -> classify -> inspect —
    with ZERO apr-cli imports; the file's import list is the OPS-01 assertion.
    Boundary evidence (recorded in SUMMARY, durable Make gate lands in 04-10):
    run `cargo tree -p aprender-core -e normal` and the same for aprender-train, and count
    "apr-cli" occurrences; both counts must be 0. Use `rtk proxy` if the rtk hook rewrites output.
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle` exits 0 with >= 1 test passed
    - Source assertion: `grep -c "apr_cli\|apr-cli" crates/aprender-train/tests/setfit_apr_lifecycle.rs` == 0
    - The lifecycle test exercises embed: `grep -c "\.embed(" ...` >= 1; and classify: `grep -c "\.classify(" ...` >= 1
    - The test reads every asserted response value through an accessor — no struct field access appears (which is the out-of-crate witness that the private-field envelope is usable)
    - `cargo tree -p aprender-core -e normal | grep -c "apr-cli"` outputs 0 (recorded in SUMMARY), same for aprender-train
  </acceptance_criteria>
  <done>A Rust caller demonstrably completes the whole lifecycle — embed and classify included — through stable public APIs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public library API -> out-of-crate caller | OPS-01 claims the lifecycle needs no CLI; the claim must be witnessed, not asserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-36 | Repudiation | claiming a public-API-only lifecycle while CLI types leak in | mitigate | grep count == 0 on the test's imports + cargo tree apr-cli count 0 for BOTH library crates (durable Make gate follows in 04-10's setfit-api-boundary) |
| T-04-51 | Repudiation | an envelope whose invariants force consumers to reach for fields | mitigate | the test reads exclusively through accessors; a missing accessor is surfaced as an OPS-01 finding rather than fixed by widening visibility |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Scoped integration-test filter green with a counted, nonzero test run
- `git diff --name-only` for this plan's commits contains ONLY crates/aprender-train/tests/setfit_apr_lifecycle.rs (wave-5 parallelism contract with 04-06 and 04-16)
- Both cargo tree boundary counts are 0 and recorded in the SUMMARY
</verification>

<success_criteria>
- OPS-01 proven by a compiling, passing public-API-only integration test covering train -> save -> load -> embed -> classify -> inspect + zero-dependency evidence
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-12-SUMMARY.md` when done.
Record: cargo tree counts (both 0), test count, the accessor names actually used, and any
public-API gap surfaced (should be none).
</output>
