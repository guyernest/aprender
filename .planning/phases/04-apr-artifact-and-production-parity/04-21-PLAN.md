---
phase: 04-apr-artifact-and-production-parity
plan: 21
type: execute
wave: 11
gap_closure: true
depends_on: []          # patches code delivered by 04-01..04-17; no NEW plan precedes it in wave 11
files_modified:
  - crates/aprender-serve/src/api/setfit_handlers.rs
  - Makefile
  - .planning/phases/04-apr-artifact-and-production-parity/deferred-items.md
autonomous: true
requirements: [OPS-05, SAFE-01]
closes_findings: [D-04-11-A-survivor-A, D-04-11-A-survivor-B]

must_haves:
  truths:
    - "The current production-mutant survivor set for `crates/aprender-serve/src/api/setfit_handlers.rs` is MEASURED at HEAD, not inherited from D-04-11-A"
    - "Survivor B (`>` → `==` at the batch bound) is diagnosed by VARYING the input, not by asserting the named test is mis-scoped"
    - "Each claimed kill is proven by re-running that specific mutant with `--re` and reading `caught`, not by asserting a test 'should' kill it"
    - "The seven `#[cfg(test)]` survivors stay triaged OUT as equivalent-by-construction, and the run is scoped so they are not counted"
    - "D-04-11-A's Survivor A is re-stated truthfully: `has_setfit_model` was DELETED at `b47acc4fe`, so the mutant no longer exists; its successor surface is named explicitly"
  artifacts:
    - path: "crates/aprender-serve/src/api/setfit_handlers.rs"
      provides: "an at-the-bound acceptance test and a residency assertion, each targeting a measured survivor"
    - path: "Makefile"
      provides: "the `setfit-serve-tests` floor raised to match the new measured count, so the gate stays non-vacuous"
      contains: "setfit-serve-tests"
    - path: ".planning/phases/04-apr-artifact-and-production-parity/deferred-items.md"
      provides: "D-04-21-A — the re-measured survivor set, the varied-input diagnosis, and the correction to D-04-11-A's Survivor A"
  key_links:
    - from: "crates/aprender-serve/src/api/setfit_handlers.rs setfit_classify_handler"
      to: "aprender::setfit::classify MAX_BATCH_TEXTS"
      via: "the transport bound, checked here AND in core (defense in depth, T-04-23) — the reason the boundary is unobservable from above"
      pattern: "request\\.texts\\.len\\(\\) > MAX_BATCH_TEXTS"
    - from: "crates/aprender-serve/src/api/router.rs readiness"
      to: "AppState::setfit_model()"
      via: "map_or over the slot — the successor of the deleted has_setfit_model"
      pattern: "state\\.setfit_model\\(\\)"
  caveats:
    - "This plan closes the TWO NAMED SURVIVORS ONLY. It does NOT close 04-11's must-have 4 — the per-crate cargo-mutants gate with four baselines and an explicitly computed aggregate adjusted score. D-04-11-B measured ≥10 h wall clock for 890 mutants; that stays a `human_verification` item and the SUMMARY must say so."
    - "This plan does NOT close F-10. OPS-01, OPS-02, SC1, SC2, SC3 and the 'over a produced artifact' halves of SC4/SC5 remain OPEN. OPS-05 stays PARTIAL — its fixture is synthetic because no user-producible artifact exists."
    - "This plan does NOT run CI. The SAFE-02 'in CI' clause (16 legs applied at `.github/workflows/ci.yml:378-397`, never executed) stays a `human_verification` item. Do NOT push, do NOT open a PR, do NOT edit any file under `.github/`."
    - "`aprender-serve --lib` whole-crate is a STANDING RED: D-04-08-A records 51 failures from one arithmetic overflow at `contract_gate.rs:428:21`, untouched by this phase. Every leg here is scoped `--features setfit --lib setfit`; do not widen it."
---

<objective>
Close the two REAL production mutation survivors recorded in D-04-11-A — but re-measure them first,
because three commits landed after the run that found them and one of those commits deleted the
function Survivor A mutates.

Purpose: Survivor B is the more valuable finding and is precisely what mutation testing is for — a
boundary mutant living beside a test whose NAME asserts boundary coverage
(`setfit_classify_refuses_a_batch_one_over_the_contract_bound`). D-04-11-A explicitly declined to
diagnose it from one input, citing CLAUDE.md rule 6. This plan varies the input.

Output: a measured survivor set at HEAD, a diagnosis derived from varied inputs, tests that kill
the specific mutants, and a per-mutant re-run proving each kill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: work on the CURRENT branch `gsd/phase-2-contract-gate`. Do NOT create, rename or switch
branches. Do NOT open a PR. Do NOT touch `.github/workflows/*.yml` — CLAUDE.md places those edits
outside autonomous scope.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md
@.planning/phases/04-apr-artifact-and-production-parity/04-VERIFICATION.md

Measured at HEAD `81652bb50` during planning — re-confirm before editing, do not re-derive:

- **D-04-11-A's Survivor A no longer exists.** `AppState::has_setfit_model` was ADDED at
  `82416fcc1` (plan 04-08) and DELETED at `b47acc4fe` (the cleanup pass), whose own commit body
  says *"Deleted `has_setfit_model` (zero callers)"*. `rtk proxy grep -rn "has_setfit_model" crates/`
  returns nothing at HEAD. The mutant D-04-11-A named cannot be re-run because the code is gone.
  Its SUCCESSOR surface — the readiness behaviour it protected — is now split across
  `AppState::setfit_model()` (`setfit_handlers.rs:65`) and the `#[cfg(feature = "setfit")]` arm of
  `AppState::model_loaded()` (`mod_app_state_gpu.rs:419-422`), consumed by
  `router.rs:249` via `state.setfit_model().map_or(...)`.
- **Survivor B has moved line.** D-04-11-A records `:158`; at HEAD the expression
  `if request.texts.len() > MAX_BATCH_TEXTS` is at `setfit_handlers.rs:152`, and
  `cargo mutants --list` names the mutant
  `crates/aprender-serve/src/api/setfit_handlers.rs:152:28: replace > with == in setfit_classify_handler`.
  Two siblings exist at the same site: `replace > with <` and `replace > with >=`.
- **The production mutant inventory at HEAD, enumerated with `--list` (not estimated):** 21
  production mutants at lines 58, 65 (×2), 82 (×2), 91 (×4), 107 (×4), 108, 137 (×4), 152 (×3),
  plus the `tests::` and `fixture::` mutants that D-04-11-A correctly triages out as
  equivalent-by-construction (a suite cannot detect the deletion of one of its own tests).
- **The planner's varied-input hypothesis, to be CONFIRMED or REFUTED by the executor, not assumed:**
  the named test posts `MAX_BATCH_TEXTS + 1 = 257` texts and asserts 400 plus the substrings `257`
  and `256`. Core re-checks the same bound — `crates/aprender-core/src/setfit/classify.rs:624` is
  `if request.texts.len() > MAX_BATCH_TEXTS`, returning `ClassifyError::BatchTooLarge { max, got }`
  — and `setfit_handlers.rs`'s own module header at lines 22-27 states this is deliberate
  ("defense in depth (T-04-23)"). Under the `==` mutant, `257 == 256` is false, the request falls
  through to `model.classify(&request)`, core refuses, `classify_error_response` maps it to 400,
  and the SAME two substrings appear. So the test would still pass. If that is what the executor
  measures, the diagnosis is **neither "the test is mis-scoped" nor "the test is unreached"** — it
  is that a redundant inner check makes the outer branch unobservable through the response for that
  input.
- **The distinguishing input follows from the diagnosis:** at exactly `MAX_BATCH_TEXTS` the correct
  `>` admits the request while both `==` and `>=` refuse it. `aprender-core` already has that
  acceptance test at `classify.rs:1729` (`(0..MAX_BATCH_TEXTS)`, asserting
  `response.results().len() == MAX_BATCH_TEXTS`); `aprender-serve` has no at-the-bound acceptance
  test, which is why the two mutants live.
- **All TEN** existing serve tests, re-measured at HEAD (an earlier draft of this plan said five
  and gave three wrong line numbers):
  `setfit_classify_returns_core_envelope_for_a_mixed_batch` (:624),
  `setfit_classify_treats_an_embedded_newline_as_one_text` (:686),
  `setfit_classify_refuses_an_empty_batch` (:707),
  `setfit_classify_refuses_a_batch_one_over_the_contract_bound` (:725),
  `setfit_classify_refuses_a_body_over_the_contract_limit` (:741),
  `setfit_classify_with_no_model_is_503_and_is_not_404` (:767),
  `setfit_readiness_is_503_with_no_model_and_reports_no_classifier` (:794),
  `setfit_readiness_is_200_and_reports_the_exact_artifact_hash` (:811),
  `setfit_classify_and_readiness_agree_on_the_artifact` (:838),
  `setfit_classify_is_the_only_surface_added_and_predict_is_untouched` (:862).
  Helpers: `app_with_model()` -> `(Router, Vec<u8>, String)` (:568), `app_without_model()` (:581),
  `post_classify(app, body)` (:585), `get_ready(app)` (:604).
- `Makefile:2163` — `setfit-serve-tests` runs
  `cargo test -p aprender-serve --features setfit --lib setfit` with `assert_tests_ran ... 9`.
  Measured at verification: 10 passed.
- **cargo-mutants invocation constraints, measured in 04-11 and recorded in D-04-11-B — all four
  still apply:** pass `--features` / `--cargo-arg`, never `-- --features` (otherwise `setfit` is
  compiled OUT and the whole run is vacuous); pass `--cargo-arg=--lib` or the baseline cannot build
  on `aprender-serve`; `--in-place` conflicts with `--jobs` in cargo-mutants 25.3.1; exclude
  `#[cfg(test)]` functions or ~7 of every 9 survivors are equivalent by construction.
  Installed version confirmed at HEAD: `cargo-mutants 25.3.1` at `~/.cargo/bin/cargo-mutants`.
- Useful flags confirmed present in 25.3.1: `-F/--re <EXAMINE_RE>` (regex matched against the names
  shown by `--list`), `-E/--exclude-re`, `-f/--file`, `--list`, `--baseline`.

<interfaces>
From `crates/aprender-core/src/setfit/classify.rs`:
  pub const MAX_BATCH_TEXTS: usize = 256;                    // :49
  pub const MAX_REQUEST_BODY_BYTES: u64;
  pub enum ClassifyError { EmptyInput, BatchTooLarge { max: usize, got: usize }, ... }
  impl VerifiedSetFitModel {
      pub fn classify(&self, request: &ClassifyRequestDocument) -> Result<ClassifyResponse, ClassifyError>;
      pub fn artifact_sha256(&self) -> &str;
  }

From `crates/aprender-serve/src/api/setfit_handlers.rs`:
  impl AppState {
      pub fn with_setfit_model(self, model: Arc<VerifiedSetFitModel>) -> Self;   // :57
      pub fn setfit_model(&self) -> Option<&Arc<VerifiedSetFitModel>>;           // :65
  }
  pub(crate) fn classify_body_limit_bytes() -> usize;                            // :82
  pub(crate) async fn setfit_classify_handler(State(AppState), Json(ClassifyRequestDocument))
      -> Result<Json<ClassifyResponse>, (StatusCode, Json<ErrorResponse>)>;      // :137

From `crates/aprender-serve/src/api/mod_app_state_gpu.rs`:
  pub fn model_loaded(&self) -> bool;                                            // :397
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: re-measure the survivor set at HEAD and diagnose Survivor B by varying the input</name>

  <files>(no source edits — this task MEASURES; it writes only .planning/phases/04-apr-artifact-and-production-parity/deferred-items.md)</files>

  <read_first>
    - `crates/aprender-serve/src/api/setfit_handlers.rs` lines 1-210 in one pass: the module header
      (especially 22-27, "Why the bounds are checked here AND in core"), the `AppState` accessors,
      `classify_body_limit_bytes`, `refuse`, `classify_error_response`, and
      `setfit_classify_handler` in full. This is the file under mutation.
    - `crates/aprender-serve/src/api/setfit_handlers.rs` lines **541-880** — the test module in
      full. Measured at HEAD, not estimated: the helpers are `app_with_model` (`:568`),
      `app_without_model` (`:581`), `post_classify` (`:585`) and `get_ready` (`:604`), and the
      module holds **TEN** `#[tokio::test]` functions, not five. The one that matters most here is
      `setfit_classify_returns_core_envelope_for_a_mixed_batch` (`:624`) — the only existing
      ACCEPTANCE-path test, and therefore the structural analog for Task 2's at-the-bound test; a
      range that stopped at 690 would have excluded it.
    - `crates/aprender-core/src/setfit/classify.rs` lines 600-640 (the inner re-check) and lines
      1700-1740 (core's own one-over and at-the-bound tests) — the SOURCE OF TRUTH for what the
      inner check does with the same input.
    - `crates/aprender-serve/src/api/router.rs` lines 230-270 — the readiness assembly and
      `state.setfit_model().map_or(...)`, the successor surface of the deleted `has_setfit_model`.
    - `crates/aprender-serve/src/api/mod_app_state_gpu.rs` lines 390-425 — `model_loaded` and its
      `#[cfg(feature = "setfit")]` arm with the OPS-05 rationale.
    - `.planning/.../deferred-items.md` § D-04-11-A and § D-04-11-B in full — the inherited claim
      being re-measured, and the four invocation constraints.
    - `CLAUDE.md` § "Verification Discipline" rules 1, 2 and 6.
  </read_first>

  <action>
Do not edit any source in this task. Measure.

First, enumerate. Run cargo-mutants in `--list` mode scoped to the one file, with
`--package aprender-serve --features setfit --cargo-arg=--lib` and `-f
crates/aprender-serve/src/api/setfit_handlers.rs`. Capture the full list to a log. Separate the
production mutants from the `tests::` and `fixture::` ones and record BOTH counts — D-04-11-A's
triage argument (a suite cannot detect the deletion of one of its own tests) is the reason the
second group is excluded, and the count is what makes the exclusion auditable rather than a
hand-wave.

Second, run. Execute the scoped mutation run over PRODUCTION mutants only, using
`-E` to exclude the `tests::` and `fixture::` names. Follow all four D-04-11-B constraints
verbatim: `--features setfit` and `--cargo-arg=--lib` as cargo-mutants arguments (never after a
bare `--`), and do not combine `--in-place` with `--jobs`. Use `--timeout 180`, the value
D-04-11-A used successfully. Capture the status with `cmd > log 2>&1; rc=$?`, never through a pipe.
The production set is ~21 mutants against a measured lower bound of ~40.5 s per mutant, so budget
roughly 15-25 minutes; if it exceeds 45 minutes, STOP, record the measured partial state and the
elapsed time, and report — do not silently rescope, which is the precedent D-04-11-B set and the
right call.

Third, record what actually survives at HEAD. Compare it against D-04-11-A's claim item by item.
Specifically confirm and record: that no `has_setfit_model` mutant appears (the function was
deleted at `b47acc4fe`); whether `replace AppState::setfit_model -> ... with None` survives — this
is the successor surface, and whether it survives is a measurement, not a prediction; and whether
all three of the line-152 comparison mutants (`==`, `<`, `>=`) survive or only some.

Fourth, diagnose Survivor B by varying the input, per CLAUDE.md rule 6. Take the mutant
`replace > with == in setfit_classify_handler` and determine, by EXECUTION rather than by reading,
what each of these inputs produces under the mutant and under the correct code: a batch of
`MAX_BATCH_TEXTS - 1`, a batch of exactly `MAX_BATCH_TEXTS`, and a batch of `MAX_BATCH_TEXTS + 1`
(the input the named test uses). The cheapest way to run those three under the mutant is to apply
the one-character change by hand in a scratch working copy, run the three cases, and revert; state
in the SUMMARY that this is what was done and that the mutant was subsequently re-run through
cargo-mutants for the authoritative verdict.

Name the cause from the measurements. If the measurement matches the planner's hypothesis — that
core's re-check at `classify.rs:624` produces the same 400 and the same two substrings for the
`+1` input, so the named test cannot distinguish the two implementations — then the diagnosis is
that a redundant inner check makes the outer branch unobservable through the response FOR THAT
INPUT, and the named test is neither mis-scoped nor unreached. If the measurement shows something
else, record what it actually shows. Either way, state the distinguishing input the measurements
identify.

Write the findings into `.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md`
as a new section `## D-04-21-A`, appended at the end of the file. Do not edit or delete the
existing D-04-11-A section — it was true when written, and the correction reads better as a dated
successor than as a silent rewrite. The new section must state: the re-measured survivor set with
both counts; that D-04-11-A's Survivor A is moot-by-deletion, naming the commit and the successor
surface; and the varied-input diagnosis for Survivor B with the three measured outcomes.
  </action>

  <verify>
    <automated>cargo mutants --list --package aprender-serve --features setfit --cargo-arg=--lib -f crates/aprender-serve/src/api/setfit_handlers.rs > /tmp/04-21-list.log 2>&1; rc=$?; echo "rc=$rc"; grep -c "setfit_handlers.rs" /tmp/04-21-list.log; grep -cE "tests::|fixture::" /tmp/04-21-list.log</automated>
  </verify>

  <acceptance_criteria>
    - The `--list` log exists and its production-mutant count is recorded alongside its
      test-mutant count. The planner measured 21 production mutants at HEAD; if the executor's
      count differs, the executor's measurement wins and the discrepancy is recorded.
    - `rtk proxy grep -rn "has_setfit_model" crates/` returns nothing, and the `--list` log contains
      zero occurrences of `has_setfit_model`. Both are recorded as the proof that D-04-11-A's
      Survivor A is moot-by-deletion rather than fixed.
    - The scoped mutation run COMPLETED (not interrupted), and its summary line — caught / missed /
      timeout / unviable counts — is quoted verbatim in the SUMMARY. If it did not complete, the
      SUMMARY says so with the elapsed time and no score is claimed. cargo-mutants prints its
      summary only at the end, which is exactly why 04-11's interrupted run produced no score.
    - The mechanism is proven engaged, not labelled by intent (CLAUDE.md rule 2): the run's own log
      shows a viable baseline (`baseline: ok`) and a non-zero caught count. A run with an unbuildable
      baseline would report nothing useful while looking like it ran.
    - The `setfit` feature was genuinely ON: the mutation log names the `--features setfit` argument
      in its cargo invocation, and the mutant list includes mutants from
      `#[cfg(feature = "setfit")]`-gated code. This is the F-04 vacuity check D-04-11-B's
      deviation 4 exists for.
    - Three varied inputs were EXECUTED against the mutated code, and all three outcomes (status
      code and message substrings, under mutant and under correct code) are tabulated in the
      SUMMARY. A diagnosis stated without the table does not satisfy this criterion.
    - `deferred-items.md` gains a `## D-04-21-A` section and the existing `## D-04-11-A` sections
      are byte-unchanged: `rtk proxy git diff -- .planning/phases/04-apr-artifact-and-production-parity/deferred-items.md`
      shows additions only, no deletions.
  </acceptance_criteria>

  <done>The survivor set is measured at HEAD rather than inherited, Survivor A's disappearance is proven by commit and by absence, Survivor B is diagnosed from three varied inputs, and all of it is recorded in a dated deferred-items section that leaves the prior record intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: kill the measured survivors — an at-the-bound acceptance test and a residency assertion</name>

  <read_first>
    - Task 1's recorded survivor set and diagnosis — the tests written here must target the mutants
      that were MEASURED to survive, not the ones D-04-11-A predicted.
    - `crates/aprender-serve/src/api/setfit_handlers.rs` lines **541-880** — the whole test module,
      so the new tests follow its shape and reuse `app_with_model` (`:568`) / `post_classify`
      (`:585`) / `get_ready` (`:604`) rather than building a second fixture. Copy the shape of
      `setfit_classify_returns_core_envelope_for_a_mixed_batch` (`:624`) specifically: it is the
      existing acceptance-path test, and Task 2's at-the-bound test is that same shape with a
      different batch size.
    - `crates/aprender-serve/src/api/setfit_handlers.rs` lines 175-200 — the `mod fixture` header
      explaining why the fixture is a deliberate duplicate of core's and why no golden hash is
      pinned; the new tests must keep that property (compare values measured in the same run).
    - `crates/aprender-core/src/setfit/classify.rs` lines 1725-1740 — core's at-the-bound
      acceptance test, the closest analog for the serve-tier one.
    - `Makefile` lines 2160-2180 — `setfit-serve-tests`, its scoped filter and its
      `assert_tests_ran` floor of 9.
  </read_first>

  <behavior>
    - Test 1 (kills `==` and `>=` at line 152): POST a batch of exactly `MAX_BATCH_TEXTS` texts and
      assert `200 OK` with `results().len() == MAX_BATCH_TEXTS`. Under the correct `>` the batch is
      admitted; under `==` or `>=` the transport layer refuses it with 400. The count must be read
      from the exported constant, never a literal `256`, so a contract change moves the test with
      the bound.
    - Test 2 (guards the diagnosis, not just the mutant): assert that the ONE-OVER case is refused
      by the TRANSPORT check specifically, distinguishably from core's inner refusal. If Task 1
      measured that the two produce indistinguishable responses, then this test is the one that
      makes the outer branch observable — for example by asserting a transport-attributable
      property of the refusal. If Task 1 measured that they are already distinguishable, record
      that and make the existing named test assert the distinction explicitly. Do NOT weaken or
      rename `setfit_classify_refuses_a_batch_one_over_the_contract_bound`; add beside it.
    - Test 3 (targets `AppState::setfit_model -> None`, IF Task 1 measured it surviving): assert
      that a state built with `with_setfit_model` reports the classifier through the readiness
      surface — the successor of the deleted `has_setfit_model`. If Task 1 measured that mutant
      already caught by `setfit_readiness_is_200_and_reports_the_exact_artifact_hash`, skip Test 3
      and record the measurement instead of adding a test that kills nothing.
  </behavior>

  <files>crates/aprender-serve/src/api/setfit_handlers.rs, Makefile</files>

  <action>
Add the tests to the existing `#[cfg(all(test, feature = "setfit"))]` test module in
`crates/aprender-serve/src/api/setfit_handlers.rs`, beside the TEN that already exist (measured at
HEAD; an earlier draft of this plan said five). Do not
create a new module: `Makefile:2163`'s `setfit-serve-tests` filters on `setfit`, and the
feature-matrix leg at `Makefile:701` uses the same scoping, so a test placed elsewhere stops being
gated.

Name Test 1 for the property, for example
`setfit_classify_admits_a_batch_at_exactly_the_contract_bound`. Give it a doc comment that records
WHY it exists: the boundary is checked here and again in core (T-04-23, module header lines 22-27),
so a refusal at `+1` is produced by either implementation and cannot distinguish them; only an
ACCEPTANCE at the bound can. Cite the mutant it kills by its cargo-mutants name so the next reader
can re-run it.

Build the request with `(0..MAX_BATCH_TEXTS)` and short texts, mirroring core's
`classify.rs:1729`. Assert `StatusCode::OK` and `results().len() == MAX_BATCH_TEXTS`, reading the
bound from the imported constant. Note that `MAX_REQUEST_BODY_BYTES` also bounds this request —
keep each text short enough that the body limit is not what is being tested, and add an assertion
that the serialized body is comfortably under `classify_body_limit_bytes()` so the test cannot
silently start measuring the wrong bound.

Write Test 2 and Test 3 as the behavior block conditions them: each is added only if Task 1
measured the corresponding mutant surviving. A test that kills nothing is noise and dilutes the
floor; record the measurement instead.

Raise `setfit-serve-tests`'s `assert_tests_ran` floor in the `Makefile` from 9 to one less than the
new measured passed count, preserving the existing pattern (the floor sits just below the measured
value so a silently-skipped test is caught while an added test does not break the gate), and update
its gate-table row at `Makefile:1920` to the new measured count and floor. Record the old floor, the
old measured count, the new measured count and the new floor in the SUMMARY. Change NOTHING else in
the Makefile — in particular do NOT touch the `setfit-cli-eval-tests` row at `:1918` or its floor at
`:2148`; those belong to 04-22 in wave 12 for the reason given in the acceptance criteria, and a
third edit here would both collide with the wave-12 sibling that lints this file and write a number
that decays the moment 04-20 lands.

Use `expect(..)` with a stated reason, never `unwrap()`.
  </action>

  <verify>
    <automated>cargo test -p aprender-serve --features setfit --lib setfit > /tmp/04-21-t2.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "test result:|at_exactly_the_contract_bound" /tmp/04-21-t2.log</automated>
  </verify>

  <acceptance_criteria>
    - Test 1 exists and RAN: the log for `-p aprender-serve --features setfit --lib setfit` names
      it and shows a `test result:` line with 0 failed and a passed count at least 11 (measured HEAD
      baseline 10). A zero-match filter exits 0 in libtest, so the named-test line is the evidence.
    - Test 1 is a real kill, proven by MUTATION not by argument: hand-apply `>` → `==` at
      `setfit_handlers.rs:152`, run the scoped suite, confirm Test 1 turns RED while
      `setfit_classify_refuses_a_batch_one_over_the_contract_bound` stays GREEN, and revert. That
      asymmetry is the whole finding and must be recorded verbatim in the SUMMARY.
    - Each claimed kill is confirmed by cargo-mutants itself, not by the hand-applied probe: re-run
      the specific mutants with `-F` matching their `--list` names (for the line-152 family, a
      regex over `replace > with` in `setfit_classify_handler`) and read `caught` in the summary.
      Quote the summary line. This is the criterion D-04-11-A's action item 1 asked for.
    - The `-F`-scoped re-run reports zero missed among the mutants this plan claims to have killed.
      Any mutant that remains missed is recorded as still-open in `D-04-21-A`, not silently omitted.
    - `make setfit-serve-tests` exits 0 with the NEW floor satisfied, and the new floor is strictly
      greater than 9. Verify the floor is not vacuous by temporarily filtering the suite to a
      zero-match name and confirming `assert_tests_ran` FAILS — the CR-02 class. Revert.
    - `rtk proxy git diff -- Makefile` shows exactly TWO changed lines, both about this plan's own
      suite: (a) the `assert_tests_ran` floor inside the `setfit-serve-tests` recipe; (b) the
      gate-table row at `Makefile:1920`
      (`#   setfit-serve-tests         serve setfit                  10 / 0         9`), whose
      measured count and floor both move — `Makefile:1927-1929` explicitly instructs *"Re-measure
      and RAISE the floors whenever a phase adds tests here"*, and the paragraph beneath it records
      what happened last time nobody did.
    - Do NOT touch the `setfit-cli-eval-tests` row at `Makefile:1918` or its floor at `:2148`. That
      row is stale (records 15; measured 16 at HEAD), but sibling plan 04-20 is in THIS WAVE and
      adds tests to `eval::setfit`, so any value written here is stale by four the moment 04-20
      lands. The correction and the floor raise belong to 04-22, which is wave 12, runs after both
      siblings and already owns the Makefile. Writing it here would be bookkeeping that decays on
      contact — the exact decay `Makefile:1927-1929` warns about.
    - `make setfit-parity` exits 0 (20 passed / 1 ignored baseline) and `make setfit-serve-smoke`
      exits 0 — the serve surface is unregressed at both the in-process and spawned tiers.
  </acceptance_criteria>

  <done>Every mutant this plan claims to kill was re-run by name through cargo-mutants and reported caught; the at-the-bound test was proven to be the one that kills it while the similarly-named one-over test stays green; and the gate floor moved with the count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated/authenticated HTTP client → `POST /v1/classify` | The request body is untrusted. Two bounds apply: `MAX_REQUEST_BODY_BYTES` bounds the PARSE via axum's body limit, `MAX_BATCH_TEXTS` bounds the WORK. Neither subsumes the other. |
| orchestrator / k8s probe → `GET /health/ready` | The readiness body carries `classifier_artifact_sha256` and `classifier_verified`, which a deployment uses to decide WHICH artifact answered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-82 | Denial of Service | `setfit_classify_handler` batch bound at `:152` | mitigate | The transport bound is the first of two defences against unbounded per-request work. A mutant living at that comparison means nothing pins the exact boundary AT the transport tier — the suite currently cannot tell a correct bound from one that is off by one in either direction, because core's re-check absorbs the difference for the only input tested. Task 2's at-the-bound acceptance test makes the outer branch observable. |
| T-04-83 | Repudiation | the test name `setfit_classify_refuses_a_batch_one_over_the_contract_bound` | mitigate | This is CLAUDE.md rule 2 ("never label a run by intent") one level down — at the test NAME rather than at the run. A name that asserts boundary coverage beside a living boundary mutant is a false attestation to any reader auditing coverage by name. Task 1 diagnoses it from varied inputs; Task 2 leaves the name intact and adds the test that earns it. |
| T-04-84 | Spoofing | readiness reporting no resident classifier (`AppState::setfit_model -> None`) | mitigate if measured surviving | A readiness path that always reported "no classifier resident" would make a healthy deployment unschedulable, and one that reported a classifier when none was loaded would admit a server that answers `/v1/classify` with 503. Whether the existing exact-hash readiness test already kills this is a MEASUREMENT (Task 1), not an assumption. |
| T-04-85 | Elevation of Privilege | `/v1/classify` auth and CORS layering | accept | Out of scope and already closed: CR-02 gated the route, WR-07 fixed the regression that gating introduced (`auth::layer_public_ops` exempts only CORS preflight and `/health*`, verified at `81652bb50`). This plan touches neither the router nor the auth layer. Recorded so the mutation work is not mistaken for a review of that surface. |
| T-04-86 | Tampering | mutation-run vacuity | mitigate | A run with `setfit` compiled out, or with an unbuildable baseline, reports plausible numbers about code that never ran — D-04-11-B measured both failure modes. Task 1's acceptance criteria require the log to show `baseline: ok`, a non-zero caught count, and the `--features setfit` argument in the cargo invocation. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: no dependency is added and no `Cargo.toml` is in `files_modified`. `cargo-mutants 25.3.1` is already installed at `~/.cargo/bin/cargo-mutants`; if it is absent at execution time, STOP and report — do not install a build tool as a side effect of a gap-closure plan. |
</threat_model>

<verification>
Run at the end of the plan, statuses captured with `cmd > log 2>&1; rc=$?` and never through a pipe:

1. `make setfit-serve-tests` — rc=0, NEW floor satisfied, 0 failed.
2. `make setfit-serve-smoke` — rc=0 (spawned server tier).
3. `make setfit-parity` — rc=0, 20 passed / 1 ignored (the three-surface gate; proves the serve leg
   still agrees with library and CLI).
4. `cargo test -p aprender-serve --features setfit --lib setfit` — rc=0, passed count recorded.
   Do NOT run `-p aprender-serve --lib` whole-crate: D-04-08-A records 51 standing failures from one
   overflow at `contract_gate.rs:428:21` that this phase did not touch.
5. The `-F`-scoped cargo-mutants re-run for every mutant this plan claims to kill — summary line
   quoted, `missed: 0` among them.
6. `make setfit-feature-matrix` — rc=0.
</verification>

<success_criteria>
- The survivor set is MEASURED at HEAD and differs from D-04-11-A's inherited claim in at least one
  respect that is stated plainly: `has_setfit_model` was deleted, so its mutant is moot.
- Survivor B's cause is named from three varied inputs with a tabulated result, not from one.
- Every claimed kill is confirmed by a `-F`-scoped cargo-mutants re-run reporting `caught`.
- The at-the-bound test was shown to turn RED under a hand-applied `>` → `==` while the
  similarly-named one-over test stayed GREEN.
- `setfit-serve-tests`'s floor moved with the count and was proven non-vacuous, and the Makefile
  diff is exactly those TWO lines (the `setfit-serve-tests` recipe floor at `:2177` and its row at
  `:1920`). Separately, the `setfit-cli-eval-tests` row at `:1918` and its floor at
  `:2148` are deliberately left to 04-22 in wave 12, because sibling 04-20 adds tests to that suite
  in THIS wave and any value written here would be stale by four on contact.
- The SUMMARY states plainly that 04-11's must-have 4 (the four-crate per-crate gate with an
  explicitly computed aggregate adjusted score) remains OPEN as a `human_verification` item; that
  the SAFE-02 "in CI" clause remains OPEN and no CI run was triggered; and that F-10 remains OPEN
  with OPS-01 and OPS-02 still NOT MET and OPS-05 still PARTIAL.
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-21-SUMMARY.md` when done.

The SUMMARY must record: both mutant counts from `--list`; the verbatim cargo-mutants summary line
for the full scoped run; the three-input diagnosis table for Survivor B; the asymmetric RED/GREEN
result of the hand-applied `>` → `==` probe; the `-F`-scoped re-run summary for each claimed kill;
the old and new `setfit-serve-tests` floors with their measured counts; the explicit note that the
`setfit-cli-eval-tests` row and floor were left to 04-22 and why; and the explicit open-items
statement.
</output>
