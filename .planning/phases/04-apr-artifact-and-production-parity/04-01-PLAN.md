---
phase: 04-apr-artifact-and-production-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/setfit-apr-v1.yaml
  - Makefile
  - CLAUDE.md
autonomous: true
requirements: [SAFE-01]
must_haves:
  truths:
    - "pv validate accepts contracts/setfit-apr-v1.yaml with zero errors"
    - "The D-03 hard cap, D-11 probe strings/count, and probe/parity tolerances exist in-contract BEFORE any comparison code lands (Ph1 D-14)"
    - "contracts/setfit-apr-v1.yaml is named in the Makefile $(CONTRACTS) explicit list — a contract not listed there is validated by nothing"
    - "The CLAUDE.md realizar-first table contains a SetFit row documenting the D-09 core-owned-inference exception"
  artifacts:
    - path: "contracts/setfit-apr-v1.yaml"
      provides: "Artifact schema, load-validation ladder, size cap, probe policy, parity + probe tolerances, canonical<->HF name table"
      contains: "268435456"
    - path: "Makefile"
      provides: "$(CONTRACTS) append + PHASE4_CONTRACTS list + contract-audit-phase4 target"
      contains: "contracts/setfit-apr-v1.yaml"
    - path: "CLAUDE.md"
      provides: "SetFit row in the realizar-first responsibility table"
      contains: "SetFit"
  key_links:
    - from: "Makefile"
      to: "contracts/setfit-apr-v1.yaml"
      via: "$(CONTRACTS) explicit list and PHASE4_CONTRACTS"
      pattern: "setfit-apr-v1\\.yaml"
    - from: "contracts/setfit-apr-v1.yaml"
      to: "contracts/setfit-encoder-conformance-v1.yaml"
      via: "referenced (never edited) tolerance provenance"
      pattern: "setfit-encoder-conformance"
---

<objective>
Commit the Phase 4 contract before any comparison code exists: `contracts/setfit-apr-v1.yaml`
owning the `setfit-apr-v1` artifact schema, the 7-rung load-validation ladder, the D-03 derived-plus-cap
size constants, the D-11 synthetic probe policy, the parity/probe tolerances, and the complete
canonical<->HF tensor name table — plus the Makefile wiring that makes the contract actually validated,
and the CLAUDE.md realizar-first SetFit row that D-09 mandates.

Purpose: Ph1 D-14 discipline — contract-resident constants are committed BEFORE the code that
compares against them. Every later plan (writer, loader, codec, parity) reads its constants from
this contract instead of inventing them inline.

Output: pv-valid contract, Makefile contract wiring, CLAUDE.md exception row.

**Branch base (stated per 02-01 policy):** Phase 4 builds on `gsd/phase-2-contract-gate` @ d66678e7a
(Phase 3 code-complete + UAT complete + CR-01..04 fixes). The orchestrator creates
`gsd/phase-4-apr-parity` from that HEAD before dispatching wave 1; all Phase 4 plans commit there and
wave merges land back on `gsd/phase-2-contract-gate` (Phase 3 precedent). No PR to `main` is opened
by any plan — that remains the human's call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-RESEARCH.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the canonical/HF tensor name table and author contracts/setfit-apr-v1.yaml</name>
  <files>contracts/setfit-apr-v1.yaml</files>
  <read_first>
    - contracts/tensor-names-v1.yaml (the bert alias entries and _fallback canonical forms — RESEARCH Open Question 1: derive, do not invent)
    - contracts/setfit-encoder-conformance-v1.yaml (the frozen tolerance table — probe tolerances must not contradict it; pooled-output family 7.62939453e-06, per-layer 1.52587891e-05)
    - contracts/setfit-train-lifecycle-v1.yaml (evidence record schema the artifact doc embeds; reference, never edit)
    - contracts/tensor-layout-v1.yaml and contracts/apr-model-lifecycle-v1.yaml (referenced contracts)
    - crates/aprender-core/src/setfit/mod.rs (EncoderArchitecture — the tensor set the encoder actually names; `named_parameters()` view)
    - crates/aprender-train/src/train/setfit/bundle.rs lines 317-356 (SetFitBundle field list — everything the artifact doc must be derivable from)
  </read_first>
  <action>
    Create `contracts/setfit-apr-v1.yaml` (one NEW contract per Ph1 D-23 — reference, never edit,
    the five contracts above). It owns, with concrete committed values:

    1. **Artifact schema (D-01/D-02):** typed metadata `model_type = "setfit"`; EXACTLY ONE custom
       metadata key `"setfit"` whose value is a schema-versioned JSON object with fields (snake_case,
       per RESEARCH Pattern 2): `schema` = "setfit-apr-v1", `schema_version` = 1, `ordered_labels`,
       `tokenizer_sha256`, `hf_name_map`, `preprocessing` (pooling, normalization, l2_epsilon as
       bit-pattern hex, truncation_max_sequence_length, padding_mode, max_length), `resolved_config`,
       `requested_config`, `evidence` (summary + table hash, Ph3 D-12), `provenance`, `probes`.
       Tokenizer bytes ride as a `U8` tensor named `tokenizer.blob` (byte-exact, no sidecars).
       F32 tensors stored under canonical tensor-names-v1 names, row-major (tensor-layout-v1).
       State the determinism obligation in-contract: `created_at` absent, one custom key, sorted map,
       serialize(deserialize(bytes)) == bytes (the verify.rs closure check).
    2. **The complete canonical<->HF name table** for the pinned MiniLM encoder: every HF dotted name
       the encoder's `named_parameters()` emits, mapped to its tensor-names-v1 canonical form
       (per-layer patterns expanded for the 6-layer MiniLM). Derive from tensor-names-v1.yaml bert
       entries + the EncoderArchitecture tensor set; a name with no mapping is a contract gap that
       must be resolved here, not papered over in code.
    3. **D-03 constants with derivation recorded:** hard cap `max_artifact_bytes = 268435456`
       (256 MiB; ~2.8x headroom over the ~90 MB legitimate artifact, mirroring the BundleLimits
       512MiB/182MB precedent); metadata bound = the container's existing 16 MiB MAX_METADATA_SIZE;
       structural rule: every TensorIndexEntry.size == product(shape) x dtype_width, with U8 width 1
       special-cased for tokenizer.blob.
    4. **D-11 probe policy:** probe inputs are FIXED synthetic contract-resident strings — never
       dataset text (Pitfall 8). Commit exactly these 6 strings:
       (a) "the quick brown fox jumps over the lazy dog";
       (b) "El rapido zorro marron salta sobre el perro perezoso — naive cafe, pi = 3.14159";
       (c) the phrase "few shot classification with contrastive pairs " repeated 64 times
           (truncation-boundary: exceeds the pinned 256-token bound);
       (d) "ok";
       (e) "Stance detection: I firmly support this position!!! #debate @user123 https://example.com";
       (f) "line one\nline two\ttabbed   spaced".
       Probe record = input string + expected embedding + expected logits + expected probabilities +
       expected label (floats as bit-pattern hex — CR-03/Pitfall 5). Probe count = 6, all replayed
       by every fresh consumer before prediction.
    5. **Tolerances (Ph1 D-14, committed before comparison):** probe embedding tolerance =
       7.62939453e-06 (the conformance contract's pooled-output family — cite it); probe/parity
       probability and logit tolerance = 1.0e-5 absolute; labels compared EXACTLY; the train-time
       round trip stays Tolerance::EXACT (A2). NaN-visible comparison mandated (partial_cmp idiom).
    6. **HTTP/classify bounds (Security V5):** max_batch_texts = 256, max_request_body_bytes = 1048576.
    7. **Load-validation ladder** as ordered obligations (raw-length cap -> header/CRC/row-major ->
       tag + one-key doc parse + schema version -> structural/tensor-set/tokenizer-hash ->
       non-finite scan -> rebuild -> probe replay), plus falsification tests naming the induced
       corruptions plan 04-03 will implement, and the in-band-negative obligation for the parity gate.

    Shape: fit the existing schema (usually KernelContract with equations, proof_obligations,
    falsification_tests). If `pv validate` rejects the shape, follow CLAUDE.md's three sanctioned
    options — restructure first; never a bash/yq workaround. Kani-declaration honesty rule applies:
    if a harness is declared, state in-contract it is not executed and name the bounded proptest.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/setfit-apr-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/setfit-apr-v1.yaml` exits 0
    - contracts/setfit-apr-v1.yaml contains the literal `268435456`, the string `tokenizer.blob`, all 6 probe strings, `7.62939453e-06`, and a `hf_name_map` (or equivalently named) table covering every encoder parameter name
    - The contract references setfit-train-lifecycle-v1, setfit-encoder-conformance-v1, tensor-names-v1, tensor-layout-v1, apr-model-lifecycle-v1 by name; `git diff --stat` shows NONE of those five files modified
  </acceptance_criteria>
  <done>pv-valid contract with every Phase 4 constant committed before comparison code exists</done>
</task>

<task type="auto">
  <name>Task 2: Wire the contract into the Makefile explicit lists and a scoped phase-4 audit</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 1138-1214 ($(CONTRACTS) explicit list ending at contracts/linear-probe-classifier-v1.yaml:1184; PHASE2_CONTRACTS:1190; PHASE3_CONTRACTS:1212)
    - Makefile lines 1283-1360 (contract-audit-phase2 and contract-audit-phase3 — the scoped blocking audit pattern; the repo-wide contract-audit is exit-0 broken, D-ITEM-04, do NOT rely on it)
  </read_first>
  <action>
    Three Makefile edits, following the Phase 2/3 shapes exactly:
    1. Append `contracts/setfit-apr-v1.yaml` to the $(CONTRACTS) explicit list (Ph2 lesson: an
       unlisted contract is validated by nothing).
    2. Add `PHASE4_CONTRACTS := contracts/setfit-apr-v1.yaml` following the PHASE2_CONTRACTS /
       PHASE3_CONTRACTS declarations.
    3. Add `contract-audit-phase4` modeled on `contract-audit-phase3` (blocking loop that READS the
       audit status — not the broken repo-wide form). Equations not yet bound to code are declared
       `status: pending` in the contract (BIND-004 warning, not BIND-001 error) until later plans
       land the modules; the audit target must pass on pending, fail on a BIND-001. Wire the target
       into tier3 beside contract-audit-phase3.
  </action>
  <verify>
    <automated>grep -v '^#' Makefile | grep -c "setfit-apr-v1.yaml" && make contract-audit-phase4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' Makefile | grep -c "setfit-apr-v1.yaml"` >= 2 (once in $(CONTRACTS), once in PHASE4_CONTRACTS)
    - `make contract-audit-phase4` exits 0 and its output shows the audit actually ran (nonzero contract count in output — not an empty loop)
    - tier3's recipe names contract-audit-phase4 (source assertion: `grep -A 30 "^tier3" Makefile` includes it)
  </acceptance_criteria>
  <done>The new contract is validated and audited by named blocking targets, not by nothing</done>
</task>

<task type="auto">
  <name>Task 3: Add the SetFit row to CLAUDE.md's realizar-first table (D-09 documented exception)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md section "CRITICAL: Realizar-First Architecture" (the responsibility table this row extends)
    - .planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md D-09 (the exception's exact rationale)
  </read_first>
  <action>
    Implementing locked decision D-09, which mandates this edit ("the CLAUDE.md realizar-first table
    gets a SetFit row"; the exception must live where the rule lives): add one row to the
    responsibility table — SetFit classification inference: aprender-core Primary (loader +
    VerifiedSetFitModel::classify), realizar/aprender-serve = HTTP transport only (route/AppState/
    readiness, calls core), trueno = compute. Add a 2-4 line note under the table stating WHY this
    is an exception: realizar-first exists because core's LLM inference was ~750x slower than
    realizar's kernels; SetFit is a 22M-param encoder whose ONLY conformance-proven implementation
    is core's Phase-1 fixture-verified graph path; serving a second port would violate OPS-03 and
    re-open every Phase 1 fixture. Change nothing else in the file.
  </action>
  <verify>
    <automated>grep -c "SetFit" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md's realizar-first section contains a SetFit table row naming aprender-core as the inference owner
    - The note names the reason (conformance evidence lives in core) — not just the ruling
    - `git diff CLAUDE.md` touches only the realizar-first section
  </acceptance_criteria>
  <done>A later reader who tries to "fix" SetFit serving back toward a realizar reimplementation finds the exception where the rule lives</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract file -> every later gate | constants committed here bound all later validation; a wrong cap or tolerance weakens every downstream check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | contracts/setfit-apr-v1.yaml | mitigate | pv validate in $(CONTRACTS) + contract-audit-phase4 blocking in tier3; pv diff flags any later edit |
| T-04-02 | Repudiation | tolerance provenance | mitigate | probe tolerances cite setfit-encoder-conformance-v1's frozen values in-contract, not re-derived numbers |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: none) — nothing to check |
</threat_model>

<verification>
- `pv validate contracts/setfit-apr-v1.yaml` exits 0; `pv lint contracts/` introduces no new errors for this file
- `make contract-audit-phase4` exits 0 with visible nonzero work
- Host caveats: run pv via `$(PV_BIN)` (cargo run form — pv is not on PATH); never bash/yq/python on contract YAML
</verification>

<success_criteria>
- Every constant later plans compare against (cap, probe strings, tolerances, batch bounds, name table) exists in the committed contract
- The contract is validated by named blocking targets
- D-09's exception is documented where the realizar-first rule lives
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-01-SUMMARY.md` when done.
Record: the pv validate output, the final constant values, and any contract-shape restructuring pv forced.
</output>
