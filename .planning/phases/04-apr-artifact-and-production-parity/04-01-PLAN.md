---
phase: 04-apr-artifact-and-production-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/setfit-apr-v1.yaml
  - Makefile
  - CLAUDE.md
autonomous: true
requirements: [SAFE-01, APR-01]
must_haves:
  truths:
    - "pv validate accepts contracts/setfit-apr-v1.yaml with zero errors"
    - "The contract carries the COMPLETE normative storage map: every APR-01 item -> its storage location (typed metadata key / doc field / named tensor), its SetFitBundle source field, its serialized representation, and its loader destination — settled BEFORE the writer exists (review B1/B2)"
    - "The contract carries a field-by-field bijection table proving every serialized doc field is either a SetFitBundle field or a deterministic function of bundle fields (review B3)"
    - "Classifier head coefficients have explicit canonical tensor names and structural rules — the head is NOT metadata (review B1)"
    - "The nullable-path allowlist is DERIVED from the Option-typed fields of the FIVE embedded sub-documents — ALL FOUR resulting paths, not one — so the null guard rejects a smuggled non-finite value without rejecting the pinned production artifact it exists to protect; resolved_config and provenance are WALKED and contribute zero paths, because an unwalked subtree cannot reject anything"
    - "The backend-identity grammar forbids capability-detection values; identity must name the kernel that executed (review B6)"
    - "The D-03 hard cap, D-11 probe strings/count, and probe/parity tolerances exist in-contract BEFORE any comparison code lands (Ph1 D-14)"
    - "contracts/setfit-apr-v1.yaml is named in the Makefile $(CONTRACTS) explicit list — a contract not listed there is validated by nothing"
    - "The CLAUDE.md realizar-first table contains a SetFit row documenting the D-09 core-owned-inference exception"
  artifacts:
    - path: "contracts/setfit-apr-v1.yaml"
      provides: "Normative storage map, doc<->bundle bijection table, canonical tensor names incl. head, nullable-path allowlist, validation ladder, size cap, bounded-read rule, probe policy, tolerances, backend-identity grammar, selection-lock artifact lifecycle, request/response schema obligations"
      contains: "268435456"
    - path: "Makefile"
      provides: "$(CONTRACTS) append + PHASE4_CONTRACTS list + contract-audit-phase4 target"
      contains: "contracts/setfit-apr-v1.yaml"
    - path: "CLAUDE.md"
      provides: "SetFit row in the realizar-first responsibility table"
      contains: "SetFit"
  key_links:
    - from: "Makefile"
      to: "contracts/setfit-apr-v1.yaml"
      via: "$(CONTRACTS) explicit list and PHASE4_CONTRACTS"
      pattern: "setfit-apr-v1\\.yaml"
    - from: "contracts/setfit-apr-v1.yaml"
      to: "contracts/setfit-encoder-conformance-v1.yaml"
      via: "referenced (never edited) tolerance provenance"
      pattern: "setfit-encoder-conformance"
    - from: "contracts/setfit-apr-v1.yaml"
      to: "crates/aprender-train/src/train/setfit/bundle.rs SetFitBundle"
      via: "the bijection table naming each bundle source field"
      pattern: "SetFitBundle"
---

<objective>
Commit the Phase 4 contract before any comparison code exists. Beyond the original scope (schema,
ladder, cap, probes, tolerances), this revision makes the contract the **single normative
reconciliation point** the cross-AI review found missing: one storage map covering every APR-01
item including classifier head coefficients, one settled `SetFitArtifactDoc` field list, and one
field-by-field bijection table proving byte-canonical closure is achievable.

Purpose: Ph1 D-14 discipline plus the review's pre-wave-2 blockers. Codex found that the writer
(04-02), the loader (04-03), and the codec (04-05) disagreed about what the artifact contains and
where the head lives. Three plans cannot agree on a schema that is written down in none of them.
After this plan, every later plan reads its schema from here instead of restating it.

Output: pv-valid contract, Makefile contract wiring, CLAUDE.md exception row.

**Review findings closed here:** B1 (head persistence unspecified), B2 (metadata schema disagrees
across plans), B3 (bijection unproven — the table lands here, the executable proof lands in 04-05),
B6 (backend identity grammar), plus MEDIUM items: D-02 typed-key amendment, tiny-fixture vs pinned
schema, token_count definition, bounded-read rule.

**Branch base (stated per 02-01 policy):** Phase 4 builds on `gsd/phase-2-contract-gate` @ d66678e7a
(Phase 3 code-complete + UAT complete + CR-01..04 fixes). The orchestrator creates
`gsd/phase-4-apr-parity` from that HEAD before dispatching wave 1; all Phase 4 plans commit there and
wave merges land back on `gsd/phase-2-contract-gate` (Phase 3 precedent). No PR to `main` is opened
by any plan — that remains the human's call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-RESEARCH.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author contracts/setfit-apr-v1.yaml — storage map, bijection table, name table, constants</name>
  <files>contracts/setfit-apr-v1.yaml</files>
  <read_first>
    - .planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md (Codex Concerns + Suggestions — this task closes B1/B2/B3/B6 and the D-02, fixture-schema, token_count MEDIUM items; the contract text must be traceable to them)
    - contracts/tensor-names-v1.yaml (bert alias entries + `_fallback` canonical forms; `global_roles` at line 104 and `layer_roles` at line 266 — derive the encoder table, do not invent; NOTE: there is NO role for a classification head over pooled embeddings, which is why the head names are SetFit-schema-owned below)
    - contracts/setfit-encoder-conformance-v1.yaml (frozen tolerance table — probe tolerances must not contradict it; pooled-output family 7.62939453e-06, per-layer 1.52587891e-05)
    - contracts/setfit-train-lifecycle-v1.yaml lines 651-690 (the `bundle_completeness` equation and its 19-field normative list — the bijection table's left-hand side, and the list 04-13 extends with field 20)
    - contracts/tensor-layout-v1.yaml and contracts/apr-model-lifecycle-v1.yaml (referenced contracts)
    - crates/aprender-train/src/train/setfit/bundle.rs lines 285-420 (SetFitBundle's 19 pub(crate) fields + ResolvedConfigRecord{resolved_device} + from_run_parts — the exact source-field names the bijection table cites)
    - crates/aprender-train/src/train/setfit/evidence.rs lines 195-215 and 450-480 (`first_null_path` and the `to_canonical_bytes` CR-03 comment — the house null-scan precedent item 3 extends, and the precondition it names)
    - crates/apr-format/src/v2/header_impl.rs lines 125-200 (AprV2Metadata's TYPED fields: model_type, name, description, author, license, data_source, data_license, version, source, original_format, created_at, total_size, param_count — the D-02 amendment rests on what is and is not typed here)
    - crates/aprender-core/src/setfit/mod.rs (EncoderArchitecture, named_parameters(), MAX_SEQUENCE_LENGTH re-export — the tensor set and the truncation bound)
  </read_first>
  <action>
    Create `contracts/setfit-apr-v1.yaml` (one NEW contract per Ph1 D-23 — reference, never edit,
    the five contracts above). Shape: fit the existing schema (usually KernelContract with
    `equations`, `proof_obligations`, `falsification_tests`). If `pv validate` rejects the shape,
    follow CLAUDE.md's three sanctioned options — restructure first; never a bash/yq workaround.
    Kani-declaration honesty rule applies: if a harness is declared, state in-contract it is not
    executed and name the bounded proptest.

    The contract owns, with concrete committed values:

    **1. Storage map (closes review B1 + B2).** ONE table with a row per APR-01 item and these
    columns: item | storage location | SetFitBundle source field | serialized representation |
    loader destination. Rows, exactly:
    - encoder tensors -> named F32 tensors under tensor-names-v1 canonical names | `tensors`
      (BTreeMap of HF-named BundleTensor) | LE f32 payload, row-major | rebuilt HF-named map fed to
      `SetFitMiniLm::from_bundle_parts`
    - classifier head weights -> named F32 tensor `setfit.head.weight`, shape
      `[num_labels, head_n_features]`, row-major | `head_weights_hex` | LE f32 payload |
      `MultinomialLogisticRegression::from_stored_coefficients`
    - classifier head intercepts -> named F32 tensor `setfit.head.bias`, shape `[num_labels]` |
      `head_intercepts_hex` | LE f32 payload | same
    - tokenizer bytes -> named U8 tensor `tokenizer.blob`, shape `[len]` | `tokenizer_bytes_hex` |
      raw bytes, byte-identical to the pinned upstream file | `from_bundle_parts` tokenizer input
    - family/detection tag -> TYPED metadata `model_type = "setfit"` | (constant) | JSON string |
      D-04 detection
    - everything else -> the ONE custom metadata key `"setfit"` holding the `SetFitArtifactDoc`
      object (field list in item 2)
    State explicitly, in-contract, that head coefficients are TENSORS and never metadata: an array
    of `K*d` floats inside a JSON document would be lossy, unaligned, invisible to `apr tensors`,
    and would inflate metadata past the container's 16 MiB `MAX_METADATA_SIZE`.

    **2. The normative `SetFitArtifactDoc` field list (closes B2).** Exactly these fields, in this
    order, snake_case, `deny_unknown_fields`, no others:
    `schema` (const "setfit-apr-v1"), `schema_version` (const 1), `bundle_schema_version` (u32),
    `format_id` (string), `architecture` (embedded sub-document), `tokenizer_sha256` (string),
    `preprocessing` {`pooling`, `normalization`, `l2_epsilon_hex`, `truncation_max_sequence_length`,
    `padding_mode`, `max_length`}, `root_seed` (u64), `head` {`n_features`, `num_labels`},
    `ordered_labels` (array), `requested_config` (embedded sub-document), `resolved_config`
    (embedded sub-document), `evidence` (embedded sub-document), `provenance`
    {`dataset_fingerprint`, `validation_split_fingerprint`, `selection_semantic_hash`,
    `selection_ledger_hash`, `selection_root_seed`, `shots_per_class`}, `hf_name_map` (object,
    HF dotted name -> canonical name), `probes` (array of probe records).
    Record that the FIVE embedded sub-documents (`architecture`, `requested_config`,
    `resolved_config`, `evidence`, `provenance`) are carried as JSON objects produced by
    `serde_json::to_value` over the corresponding aprender-train types, because `aprender-core`
    cannot name those types (train depends on core, never the reverse) — and that their key order is
    deterministic because `serde_json::Map` is BTreeMap-backed. `provenance` is one of the five and
    not an exception: it is `to_value(bundle.provenance)` exactly like the other four, so item 3's
    null walk covers it on the same terms.

    **3. The nullable-path allowlist (CR-03 hardening).** `serde_json`'s Value serializer maps a
    non-finite f64 to `null` SILENTLY, so a stray `null` IS the CR-03 signature and a blanket
    null-scan is the house guard for it. Keep that polarity — it is already the shipped precedent:
    `first_null_path` guards `UpdateEvidence::to_canonical_bytes` (evidence.rs:200-215, 455-478)
    and that comment records WHY a null-scan beats an enumerated float-field list, in its own
    words: an enumerated field list is the guard that silently stops covering the newest field,
    which is how these checks rot. Record in-contract the PRECONDITION that same comment names and
    that Phase 4 does NOT satisfy: a blanket scan is exact only over types with NO `Option` field,
    because only then can a `null` mean nothing but a non-finite float. `UpdateEvidence` has none.
    Three of the FIVE embedded sub-documents DO — so this contract owns the exception set.

    The allowlist is DERIVED from the types, never hand-listed: it is exactly the set of paths at
    which an `Option`-typed field of the five sub-documents can serialize. Enumerated on this
    branch (none of these types carries `skip_serializing_if`, so every `None` emits an explicit
    `null`), the allowlist is these FOUR paths and no others:
    - `architecture.vocab_remap` — `Option<Vec<u32>>` on `EncoderArchitecture`
      (crates/aprender-core/src/setfit/mod.rs:126-127). `None` is the FULL PIN — the production
      shape; `Some` occurs only on the slice-fixture path (setfit/import.rs:501 sets `None`, :620
      sets `Some`). An allowlist omitting this path refuses every pinned MiniLM artifact.
    - `requested_config.pair_config.budget` — `Option<u64>` on `PairConfigWire`
      (crates/aprender-train/src/train/setfit/config.rs:716)
    - `requested_config.pair_config.hard_cap` — `Option<u64>`, same struct, line 717
    - `evidence.epsilon_used` — `Option<f64>` on `EvidenceSummary`
      (crates/aprender-train/src/train/setfit/evidence.rs:599)
    `resolved_config` contributes NO nullable path: `ResolvedConfigRecord` holds a single `String`
    (bundle.rs:292-301). `provenance` likewise contributes NONE: `ProvenanceRecord` — bundle field
    20, landed by plan 04-13 — holds four `String`s, a `u64` and a `u32`, and 04-13 forbids an
    `Option` field on it. A `null` at any path outside that four-entry list is a typed
    `NonFiniteValue { path }` rejection at write time.

    THE WALKED SET IS FIVE SUB-DOCUMENTS; THE ALLOWLIST IS FOUR PATHS. Record this boundary
    deliberately, because two different numbers otherwise read as an oversight (checker warning
    W-A). The writer walks all five, INCLUDING the two that contribute zero allowlisted paths.
    Walking a zero-path subtree is not redundant — it is the whole point: an UNWALKED subtree
    cannot reject anything, so a future `Option` field added to `ResolvedConfigRecord` or
    `ProvenanceRecord` would fail SILENTLY on the first production artifact instead of loudly at
    the guard, which is the exact inversion of the fail-loud asymmetry this allowlist design rests
    on. State in-contract that any sub-document added to the doc later is WALKED from the moment it
    is added, contributing an empty allowlist entry until a written analysis says otherwise.

    Record the residual honestly instead of claiming the guard is total. At the three
    `Option<non-float>` paths a `null` is unambiguous — no f64 can produce one at a `Vec<u32>` or
    `u64` field. At `evidence.epsilon_used`, which is `Option<f64>`, `None` and a non-finite
    epsilon render identically and the writer cannot separate them. Today that residual is empty:
    `epsilon_used` has exactly one assignment in the tree and it is `None` (evidence.rs:655, with
    evidence.rs:1144 asserting it), i.e. the judged path does not exist yet; when it lands the
    value comes from the frozen per-class epsilon table, so a non-finite value there is a
    contract-authoring defect `pv validate` catches, not a run-time divergence. State exactly that,
    and state that no further path may enter the allowlist without the same written analysis.

    Two normative obligations follow, both binding on later plans:
    (a) the allowlist ships as ONE named constant listing the four paths, cited by the writer's
        walk rather than scattered as inline literals, and that walk is pointed at ALL FIVE
        sub-documents (04-02);
    (b) a COMPLETENESS GATE lives in the only crate that can name all five types — `aprender-train`
        — and asserts that serializing an all-`None` instance of each sub-document type produces
        exactly this four-path null set, with the `resolved_config` and `provenance` subtrees each
        asserted BY NAME to contribute an EMPTY set. A new `Option` field on any of the five then
        fails that gate by name, instead of failing on the first production artifact (04-13).
    Adding `skip_serializing_if` to any of these types is FORBIDDEN: it changes
    `SetFitBundle::to_canonical_bytes` output and breaks Phase 3's committed bundle closure and
    round-trip tests.

    Every float the doc ITSELF owns (`preprocessing.l2_epsilon_hex`, every probe expectation) is a
    bit-pattern hex STRING, never a JSON number — so no doc-owned field can reach the null path at
    all, and the allowlist governs only the five opaque sub-documents.

    **4. The bijection table (closes B3).** A second table, one row per doc field, with columns:
    doc field | bundle source | direction. Every row is either `= bundle.<field>` or
    `= f(bundle...)` naming the deterministic function. Specifically:
    `bundle_schema_version = bundle.schema_version`; `format_id = bundle.format_id`;
    `architecture = to_value(bundle.architecture)`; `tokenizer_sha256 = bundle.architecture.tokenizer_sha256`;
    `preprocessing.* = bundle.{pooling, normalization, l2_epsilon, truncation_max_sequence_length,
    padding_mode, max_length}`; `root_seed = bundle.root_seed`;
    `head.n_features = bundle.head_n_features`; `head.num_labels = bundle.ordered_labels.len()`;
    `ordered_labels = bundle.ordered_labels`; `requested_config = to_value(bundle.requested_config)`;
    `resolved_config = to_value(bundle.resolved_config)`; `evidence = to_value(bundle.evidence)`;
    `provenance = to_value(bundle.provenance)` (bundle field 20, landed by plan 04-13);
    `hf_name_map = f(bundle.tensors.keys(), this contract's name table)`;
    `probes = f(rebuild(bundle), this contract's probe inputs)`.
    And the reverse direction, one row per bundle field, proving `deserialize` recovers all 20:
    tensors from the canonical names inverted through `hf_name_map`; `tokenizer_bytes_hex` from the
    `tokenizer.blob` payload; `head_weights_hex`/`head_intercepts_hex` from the two head tensors;
    the rest read directly from the doc. State the closure obligation as an equation:
    `serialize(deserialize(bytes)) == bytes` at `Tolerance::EXACT`, and that `probes` being
    RECOMPUTED on every serialize is what makes them closure-safe (a deterministic CPU path
    reproduces identical records) while `provenance` being non-derivable is why bundle field 20
    exists. Record that closure alone does NOT subsume item 3's guard: a `null` at an allowlisted
    `Option` path round-trips to `None` and back to `null`, so closure holds while the value is
    gone — which is exactly why the null scan runs at write time and not only as a byte compare.

    **5. Canonical tensor name table.** Every HF dotted name the encoder's `named_parameters()`
    emits, mapped to its tensor-names-v1 canonical form, with per-layer patterns expressed as
    `{n}` templates so the set is a FUNCTION of `architecture.num_layers` (see item 6). Plus the
    three SetFit-schema-owned entries `setfit.head.weight`, `setfit.head.bias`, `tokenizer.blob`,
    each with a note recording the deliberate D-01 amendment: tensor-names-v1 has no role for a
    classification head or a tokenizer blob, so these names are owned by THIS contract, are
    reserved, and must not collide with any tensor-names-v1 canonical form.

    **6. Architecture-derived tensor-set rule (closes the tiny-fixture MEDIUM finding).** The
    expected tensor set is computed from the artifact's OWN declared `architecture` record by
    expanding the `{n}` templates over `architecture.num_layers`. There is NO test-only schema
    exception and no hardcoded 6-layer list: a fixture keeps the exact per-layer name topology at
    reduced dimensions/layer count and is validated by the same production rule. Separately record
    `reference_architecture` (the pinned MiniLM: 6 layers, hidden 384, 12 heads, vocab 30522,
    max positions 512) used ONLY for the ~90 MB size derivation below and as the table's worked
    expansion example. Record one further fixture obligation, because item 3 depends on it: the
    fixture must also keep the production NULLABILITY topology — `vocab_remap: None` is the pinned
    shape, so a fixture that sets `Some` exercises the one input shape at which the null guard
    cannot fire, and a test suite built only on it would earn every green count on the wrong shape.

    **7. D-03 constants with derivation recorded.** `max_artifact_bytes = 268435456` (256 MiB;
    ~2.8x headroom over the ~90 MB legitimate artifact, mirroring the BundleLimits 512MiB/182MB
    precedent); metadata bound = the container's existing 16 MiB `MAX_METADATA_SIZE`; structural
    rule: every `TensorIndexEntry.size == product(shape) * dtype_width`, with U8 width 1
    special-cased for `tokenizer.blob` and F32 width 4 for every other entry.

    **8. Bounded-read obligation (closes review B5).** State that the cap must bite BEFORE a
    caller allocates: any consumer reading an artifact from a file MUST (a) consult the declared
    file length and refuse over-cap before reading a byte, and (b) read through a reader bounded at
    `max_artifact_bytes + 1` so a length that lies (FIFO, growing file) still cannot exhaust memory.
    The in-memory cap in `load_setfit_apr` remains as defense in depth. Name the core API this
    contract binds: `read_setfit_apr_bytes_bounded(reader, declared_len)`.

    **9. D-11 probe policy.** Probe inputs are FIXED synthetic contract-resident strings — never
    dataset text (Pitfall 8). Commit exactly these 6 strings:
    (a) "the quick brown fox jumps over the lazy dog";
    (b) "El rapido zorro marron salta sobre el perro perezoso — naive cafe, pi = 3.14159";
    (c) the phrase "few shot classification with contrastive pairs " repeated 64 times
        (truncation-boundary: exceeds the pinned 256-token bound);
    (d) "ok";
    (e) "Stance detection: I firmly support this position!!! #debate @user123 https://example.com";
    (f) "line one\nline two\ttabbed   spaced".
    Probe record = input string + expected embedding + expected logits + expected probabilities +
    expected label, floats as bit-pattern hex. Probe count = 6, all replayed by every fresh
    consumer before prediction.

    **10. Tolerances (Ph1 D-14).** Probe embedding tolerance 7.62939453e-06 (the conformance
    contract's pooled-output family — cite it); probe/parity probability and logit tolerance
    1.0e-5 absolute; labels compared EXACTLY; the train-time round trip stays `Tolerance::EXACT`
    (A2). NaN-visible comparison mandated (the `matches!(delta.partial_cmp(&bound), Some(Less | Equal))`
    idiom); a bare `delta <= bound` is a contract violation because it silently accepts NaN.

    **11. Request/response schema obligations (closes the M1 + M2 review items).**
    - `ClassifyResponse` fields: `schema_version`, `artifact_sha256`, `backend`, `latency_ms`,
      `results[]{label, probabilities[], logits?[], margin, token_count, truncated}`.
    - Obligation: every field is PRIVATE with read accessors, the only constructor validates, and
      `Deserialize` routes through the same validating constructor via a private wire struct
      (`#[serde(try_from = ...)]`, the `SetFitTrainConfig` precedent). This is what makes
      "a non-finite value is unrepresentable in a ClassifyResponse" a true statement rather than a
      claim a public field can falsify.
    - `latency_ms` obligation: finite and >= 0.0. It is a MEASUREMENT: it is excluded from every
      equality comparison and no gate may assert it is strictly positive.
    - `token_count` DEFINITION: the number of positions the model actually consumed for that text
      — i.e. the attention-mask length after truncation. Under this definition a truncated text
      has `token_count == MAX_SEQUENCE_LENGTH` by construction, so gates assert
      `truncated == true` together with `token_count == MAX_SEQUENCE_LENGTH` (the exported
      constant, never a literal 256) and never a tokenizer-internal count.
    - ONE shared request document `ClassifyRequestDocument { texts: [string], include_logits: bool }`
      with `deny_unknown_fields`, used byte-identically by the CLI `--input` file and the HTTP
      request body. Rationale in-contract: a line-delimited CLI format cannot carry a text
      containing a newline, so the three parity surfaces would receive different ordered input
      sets while appearing to agree.
    - HTTP/classify bounds (Security V5): `max_batch_texts = 256`,
      `max_request_body_bytes = 1048576`.

    **12. Backend-identity grammar (closes review B6).** Identity format
    `<device>:<implementation>:<kernel>`; v1 value `cpu:setfit-core:autograd-trueno-matmul`.
    Normative rules, with the reason recorded: (a) the value MUST be produced by the encode
    invocation that ran and returned to the caller — the classify path may not construct it;
    (b) a CPU-feature DETECTION result (trueno `Backend::AVX2`/`NEON`/`select_backend*`) MUST NOT
    appear in this field, because detection reports what the host CAN do, not what ran — verified
    at plan time: `trueno::Matrix::matmul` dispatches on SIZE (`matmul_naive` below the 64
    threshold, `gemm_blis_parallel` above it, a GPU path when compiled in), so an AVX2 detection
    result is consistent with a scalar execution; (c) a config/env/CLI value MUST NOT reach the
    field. Record the v1 limitation honestly: trueno exposes no per-dispatch execution report, so
    the identity names the kernel ENTRY POINT the encoder invoked, not the innermost dispatch
    chosen; upgrading to per-dispatch reporting requires a trueno API and is a recorded deferred
    item. Cite CLAUDE.md Verification Discipline rule 2.

    **13. Selection-lock artifact lifecycle (closes review B4).** Normative: canonical-test
    evaluation requires a DURABLE selection lock created by a PRIOR validation evaluation.
    Validation evaluation atomically writes `SelectionLock::to_canonical_bytes()` to an operator-
    named path; test evaluation requires that path, reconstructs the lock, and reaches the test
    split only through `mint_test_token(run) -> CanonicalTestAccess::grant(token, run, dataset)`.
    Absent lock, an artifact hash that is not the locked chosen one (`StaleLock`), and a dataset
    whose fingerprint is not the locked one (`TokenDatasetMismatch`) are each typed refusals.
    Record the honest trust model: the lock file is an integrity-checked RECORD, not an
    unforgeable credential — its `lock_hash` is a digest OF its own bytes, so a hand-written file
    is possible; what the mechanism guarantees is that test access cannot be reached without a
    recorded, hash-committing selection decision naming candidates and a chosen artifact, and that
    every downstream door re-checks identity against the REAL run and the REAL dataset.

    **14. D-02 amendment note (closes the typed-key MEDIUM finding).** D-02 asked for well-known
    TYPED keys for family, schema, schema version, ordered labels and tokenizer hash.
    `AprV2Metadata`'s typed fields are `model_type`, `name`, `description`, `author`, `license`,
    `data_source`, `data_license`, `version`, `source`, `original_format`, `created_at`,
    `total_size`, `param_count`, `quantization`, `shard` — there is no typed slot for a schema id,
    a label list or a tokenizer digest, and adding container fields touches 39 files / 77 use
    sites (RESEARCH). Therefore: `model_type = "setfit"` is the ONLY typed key this writer sets,
    and `schema`, `schema_version`, `ordered_labels`, `tokenizer_sha256` are the FIRST-LEVEL fields
    of the single `"setfit"` custom document — still generically inspectable as JSON with zero
    SetFit knowledge. This is an explicit, recorded amendment of D-02(a), not a silent change.
    Record also that `license`, `data_source`, `data_license` serialize as explicit `null` by
    container design (no `skip_serializing_if`), that this is deterministic, that the writer
    sets none of them because a value not derivable from the bundle would break closure, and that
    these three typed-metadata nulls are OUTSIDE item 3's scope — the walk covers the five
    embedded sub-documents only, and must not be pointed at the container's typed fields.

    **15. Load-validation ladder** as ordered obligations: bounded read / raw-length cap ->
    header + CRC + row-major flag -> typed tag + one-key doc parse + schema version ->
    structural (architecture-derived tensor set, per-entry size, tokenizer hash) -> non-finite scan
    -> rebuild -> probe replay. Plus falsification tests naming the induced corruptions plan 04-03
    implements, and the in-band-negative obligation for the parity gate.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/setfit-apr-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/setfit-apr-v1.yaml` exits 0
    - The contract contains the literal `268435456`, the strings `tokenizer.blob`, `setfit.head.weight`, `setfit.head.bias`, `cpu:setfit-core:autograd-trueno-matmul`, `read_setfit_apr_bytes_bounded`, `ClassifyRequestDocument`, all 6 probe strings, and `7.62939453e-06`
    - The nullable-path allowlist names EXACTLY four paths: the contract contains the literals `architecture.vocab_remap`, `requested_config.pair_config.budget`, `requested_config.pair_config.hard_cap` and `evidence.epsilon_used`, plus the explicit statement that `resolved_config` AND `provenance` each contribute none
    - The contract states that the WALK covers FIVE sub-documents (`architecture`, `requested_config`, `resolved_config`, `evidence`, `provenance`) while the allowlist names FOUR paths, and gives the reason the two numbers differ — an unwalked subtree cannot reject anything, so its drift would fail silently (source assertions, both grep-safe regardless of YAML line wrapping: the token `provenance` appears inside the allowlist block, and `grep -ci "walked" contracts/setfit-apr-v1.yaml` >= 1)
    - The allowlist block records, for each of the four paths, the owning type and its `Option<...>` field type — the derivation is visible, not asserted (source assertion: `EncoderArchitecture`, `PairConfigWire` and `EvidenceSummary` each appear in that block, and `ResolvedConfigRecord` and `ProvenanceRecord` each appear as a zero-contribution entry)
    - The allowlist block states the `evidence.epsilon_used` residual (an `Option<f64>` null cannot be separated from a non-finite epsilon) and forbids adding `skip_serializing_if` to any of the five types
    - The storage map has a row for classifier head weights AND intercepts naming a TENSOR location (review B1 closed — assertable by grep for `setfit.head.weight` in the same table block)
    - The bijection table has one row per `SetFitArtifactDoc` field and one row per SetFitBundle field including `provenance`, and every row names either a bundle field or a named deterministic function (review B3 closed at the specification layer)
    - The contract states that a CPU-feature detection result must not appear in the backend field, and records the size-dispatch evidence (review B6)
    - The contract references setfit-train-lifecycle-v1, setfit-encoder-conformance-v1, tensor-names-v1, tensor-layout-v1, apr-model-lifecycle-v1 by name; `git diff --stat` shows NONE of those five files modified
  </acceptance_criteria>
  <done>Every schema question the review found unsettled has exactly one written answer, committed before any code that could quietly answer it differently</done>
</task>

<task type="auto">
  <name>Task 2: Wire the contract into the Makefile explicit lists and a scoped phase-4 audit</name>
  <files>Makefile</files>
  <read_first>
    - Makefile lines 1138-1214 ($(CONTRACTS) explicit list ending at contracts/linear-probe-classifier-v1.yaml:1184; PHASE2_CONTRACTS:1190; PHASE3_CONTRACTS:1212)
    - Makefile lines 1283-1360 (contract-audit-phase2 and contract-audit-phase3 — the scoped blocking audit pattern; the repo-wide contract-audit is exit-0 broken, D-ITEM-04, do NOT rely on it)
  </read_first>
  <action>
    Three Makefile edits, following the Phase 2/3 shapes exactly:
    1. Append `contracts/setfit-apr-v1.yaml` to the $(CONTRACTS) explicit list (Ph2 lesson: an
       unlisted contract is validated by nothing).
    2. Add `PHASE4_CONTRACTS := contracts/setfit-apr-v1.yaml` following the PHASE2_CONTRACTS /
       PHASE3_CONTRACTS declarations.
    3. Add `contract-audit-phase4` modeled on `contract-audit-phase3` (blocking loop that READS the
       audit status — not the broken repo-wide form). Equations not yet bound to code are declared
       `status: pending` in the contract (BIND-004 warning, not BIND-001 error) until later plans
       land the modules; the audit target must pass on pending, fail on a BIND-001. Wire the target
       into tier3 beside contract-audit-phase3. Capture the command's status directly
       (`cmd > log 2>&1; rc=$?`), never through a pipe (Verification rule 1).
    Then falsify the wiring gate itself, exactly once, and record the transcript: temporarily
    delete the PHASE4_CONTRACTS line, run this task's verify command, confirm it FAILS (count 1,
    below the required 2), and restore the line. A bare `grep -c` whose exit status is consumed
    directly would have PASSED at count 1 — that is the same defect class as CLAUDE.md
    Verification rule 1, which is why the verify command captures the count and compares it.
  </action>
  <verify>
    <automated>n=$(grep -v '^#' Makefile | grep -c "setfit-apr-v1.yaml"); test "$n" -ge 2 && make contract-audit-phase4</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0, and its count component is compared explicitly (`test "$n" -ge 2`) rather than relying on `grep -c`'s exit status, which is 0 for ANY count >= 1 and could not distinguish 1 from 2
    - The count is >= 2: once in $(CONTRACTS), once in PHASE4_CONTRACTS
    - Falsification performed and recorded in the SUMMARY: with the PHASE4_CONTRACTS line removed the verify command FAILS with the observed count 1; the line is restored and the command passes again
    - `make contract-audit-phase4` exits 0 and its output shows the audit actually ran (nonzero contract count in output — not an empty loop)
    - tier3's recipe names contract-audit-phase4 (source assertion: `grep -A 30 "^tier3" Makefile` includes it)
    - No added recipe reads a status through a pipe (source assertion on the new recipe block)
  </acceptance_criteria>
  <done>The new contract is validated and audited by named blocking targets, and the wiring gate has been shown able to fail</done>
</task>

<task type="auto">
  <name>Task 3: Add the SetFit row to CLAUDE.md's realizar-first table (D-09 documented exception)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md section "CRITICAL: Realizar-First Architecture" (the responsibility table this row extends)
    - .planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md D-09 (the exception's exact rationale)
  </read_first>
  <action>
    Implementing locked decision D-09, which mandates this edit ("the CLAUDE.md realizar-first table
    gets a SetFit row"; the exception must live where the rule lives): add one row to the
    responsibility table — SetFit classification inference: aprender-core Primary (loader +
    VerifiedSetFitModel::classify), realizar/aprender-serve = HTTP transport only (route/AppState/
    readiness, calls core), trueno = compute. Add a 2-4 line note under the table stating WHY this
    is an exception: realizar-first exists because core's LLM inference was ~750x slower than
    realizar's kernels; SetFit is a 22M-param encoder whose ONLY conformance-proven implementation
    is core's Phase-1 fixture-verified graph path; serving a second port would violate OPS-03 and
    re-open every Phase 1 fixture. Change nothing else in the file.
  </action>
  <verify>
    <automated>grep -c "SetFit" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md's realizar-first section contains a SetFit table row naming aprender-core as the inference owner
    - The note names the reason (conformance evidence lives in core) — not just the ruling
    - `git diff CLAUDE.md` touches only the realizar-first section
  </acceptance_criteria>
  <done>A later reader who tries to "fix" SetFit serving back toward a realizar reimplementation finds the exception where the rule lives</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract file -> every later gate | constants committed here bound all later validation; a wrong cap or tolerance weakens every downstream check |
| contract schema -> three implementing crates | writer, loader and codec each read this schema; a schema stated in none of them is the defect the review found |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | contracts/setfit-apr-v1.yaml | mitigate | pv validate in $(CONTRACTS) + contract-audit-phase4 blocking in tier3; pv diff flags any later edit |
| T-04-02 | Repudiation | tolerance provenance | mitigate | probe tolerances cite setfit-encoder-conformance-v1's frozen values in-contract, not re-derived numbers |
| T-04-37 | Repudiation | backend identity grammar | mitigate | contract forbids capability-detection values in the identity field and records the size-dispatch evidence that makes detection non-probative |
| T-04-38 | Spoofing | head coefficients smuggled as metadata | mitigate | storage map binds head weights/intercepts to named F32 tensors; the loader's tensor-set rule refuses an artifact missing them |
| T-04-58 | Denial of Service | a null guard that refuses every legitimate artifact | mitigate | the allowlist is derived from the FIVE walked sub-documents' `Option` fields (yielding four paths; `resolved_config` and `provenance` contribute none yet are still walked) with the owning type recorded per path; a completeness gate in aprender-train (04-13) fails by name when a new `Option` field appears on any of the five, and 04-02 must prove the full-pin (`vocab_remap: None`) shape is ACCEPTED |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: none) — nothing to check |
</threat_model>

<verification>
- `pv validate contracts/setfit-apr-v1.yaml` exits 0; `pv lint contracts/` introduces no new errors for this file
- `make contract-audit-phase4` exits 0 with visible nonzero work, and the wiring gate's falsification transcript is recorded
- Host caveats: run pv via `$(PV_BIN)` (cargo run form — pv is not on PATH); never bash/yq/python on contract YAML
</verification>

<success_criteria>
- Every constant and every schema decision later plans compare against exists in the committed contract
- The storage map, the doc field list, and the bijection table close review findings B1, B2 and B3 at the specification layer before any implementing code exists
- The nullable-path allowlist is complete against the shipped types, so the writer's CR-03 guard can fire on a smuggled non-finite value without refusing the pinned production artifact
- The contract is validated by named blocking targets; D-09's exception is documented where the realizar-first rule lives
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-01-SUMMARY.md` when done.
Record: the pv validate output, the final constant values, the storage map and bijection table as
shipped, the four allowlisted nullable paths with their owning types, the Makefile wiring-gate
falsification transcript, and any contract-shape restructuring pv forced.
</output>
