---
phase: 04-apr-artifact-and-production-parity
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - crates/aprender-core/src/setfit/artifact.rs
  - crates/aprender-core/src/setfit/mod.rs
  - crates/aprender-train/tests/ui/setfit_verified_model_constructed.rs
  - crates/aprender-train/tests/ui/setfit_verified_model_constructed.stderr
autonomous: true
requirements: [APR-02, APR-04, APR-05]
must_haves:
  truths:
    - "A valid artifact loads through one production door, replays all 6 embedded probes, and only then yields a VerifiedSetFitModel"
    - "Every corruption class — truncated, oversized, CRC-flipped, untagged, unknown-field, wrong schema version, missing tensor, extra tensor, size mismatch, NaN payload, tokenizer byte flip, perturbed probe — fails with a DISTINCT typed error before prediction"
    - "VerifiedSetFitModel cannot be constructed outside aprender-core — proven at compile time by trybuild from an out-of-crate test"
    - "The loader recovers every APR-05 identity field (revisions, hashes, policy, labels, head config, fingerprints, seeds, evidence, artifact hash, schema version) from the artifact alone"
    - "read_setfit_apr_parts exposes rungs 1-5 as a parse-only door that load_setfit_apr itself calls — ONE ladder, no second policy (04-05's AprCodec::deserialize dependency)"
    - "VerifiedSetFitModel::embed returns the encoder's L2-normalized sentence embeddings through the public API — the accessor for OPS-01's 'embed' step (lifecycle proof lands in 04-12 Task 1)"
  artifacts:
    - path: "crates/aprender-core/src/setfit/artifact.rs"
      provides: "load_setfit_apr validation ladder + probe replay + VerifiedSetFitModel typestate"
      contains: "pub fn load_setfit_apr"
    - path: "crates/aprender-core/src/setfit/artifact.rs"
      provides: "read_setfit_apr_parts parse-only door (rungs 1-5, shared with load_setfit_apr)"
      contains: "pub fn read_setfit_apr_parts"
    - path: "crates/aprender-train/tests/ui/setfit_verified_model_constructed.rs"
      provides: "compile-fail proof of out-of-crate non-constructibility"
      contains: "VerifiedSetFitModel"
  key_links:
    - from: "crates/aprender-core/src/setfit/artifact.rs"
      to: "SetFitMiniLm::from_bundle_parts"
      via: "rebuild rung after canonical->HF name inversion"
      pattern: "from_bundle_parts"
    - from: "crates/aprender-core/src/setfit/artifact.rs"
      to: "contracts/setfit-apr-v1.yaml"
      via: "cap + tolerances + probe strings constants"
      pattern: "268435456|max_artifact_bytes"
---

<objective>
Implement the production loader half of `setfit-apr-v1`: a 7-rung fail-closed validation ladder
ending in probe replay and the `VerifiedSetFitModel` typestate — the ONLY door to classification —
plus the induced-corruption negative suite and the out-of-crate trybuild non-constructibility proof.
Rungs 1-5 are additionally exposed as the parse-only door `read_setfit_apr_parts` (04-05's codec
dependency — landed here so wave 4 stays conflict-free).

Purpose: APR-02 (fail before prediction), APR-04's consumer side (nothing short of a verified model
reaches any consumer — structurally, because the classify-capable type cannot exist otherwise), and
APR-05's data recovery (everything inspect needs, recovered here once).

Output: `load_setfit_apr` + `read_setfit_apr_parts` + `VerifiedSetFitModel` (incl. `embed`
accessor) + negatives + trybuild case.

Branch: `gsd/phase-4-apr-parity` (check exists, stop if not).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
From 04-02 (already on branch):
- `write_setfit_apr(view) -> Result<Vec<u8>, SetFitArtifactError>`, `SetFitArtifactView`, `artifact_sha256_hex`
From apr-format v2 (aprender::format::v2):
- `AprV2Reader` — header parse, `AprV2Header::verify_checksum`, footer CRC, `get_tensor_data` (raw bytes), TensorIndexEntry { shape, dtype, size }
From core setfit:
- `SetFitMiniLm::from_bundle_parts(tokenizer_bytes, arch, tensors: BTreeMap<String,(Vec<usize>,Vec<f32>)>, root_seed)` — checks tokenizer hash FIRST (mod.rs:354-374)
- `SetFitMiniLm::encode_texts` — L2-normalized sentence embeddings (the embed accessor delegates here)
- `MultinomialLogisticRegression::from_stored_coefficients` (usage precedent: aprender-train verify.rs:552-555)
NaN-visible comparison idiom (verify.rs:336-341): matches!(delta.partial_cmp(&bound), Some(Less | Equal))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The validation ladder — rungs 1-5 (bytes-side, before any model exists)</name>
  <files>crates/aprender-core/src/setfit/artifact.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/artifact.rs (current writer state — extend, do not restructure)
    - crates/aprender-train/src/train/setfit/bundle.rs lines 437-524 (from_canonical_bytes_within — the fail-closed ladder precedent whose ORDER is load-bearing; copy the door discipline: the pub entry names only the contracted bound)
    - crates/apr-format/src/v2/reader_impl.rs (reader API, checksum verification, index access)
    - contracts/setfit-apr-v1.yaml (cap, structural rule, U8 width special case, schema_version)
  </read_first>
  <behavior>
    - Test: bytes.len() == cap passes rung 1; cap+1 fails typed ArtifactTooLarge BEFORE any parse (construct via vec![0u8; cap+1] — only len is read)
    - Test: flipped header CRC byte -> typed ContainerIntegrity error
    - Test: model_type != "setfit" -> typed NotASetFitArtifact (a SetFit-shaped tensor set WITHOUT the tag is refused here — D-04 explicit-tag-only)
    - Test: doc with an unknown field -> typed error (deny_unknown_fields); schema_version 2 -> typed UnsupportedSchemaVersion
    - Test: TensorIndexEntry.size != product(shape) x dtype_width -> typed InconsistentTensor; missing canonical tensor -> IncompleteTensorSet; extra tensor -> InconsistentTensorSet
    - Test: NaN bit pattern (0x7FC00000) injected in an F32 payload -> typed NonFiniteValue
    - Test: one flipped tokenizer.blob byte -> typed TokenizerHashMismatch
    - Test: read_setfit_apr_parts on valid writer-produced bytes returns the doc + full HF-keyed tensor map + tokenizer bytes; on a rung 1-5 corruption it fails with the SAME typed variant load_setfit_apr reports (one ladder, two doors)
  </behavior>
  <action>
    Add to artifact.rs: `pub fn load_setfit_apr(bytes: &[u8]) -> Result<VerifiedSetFitModel, SetFitArtifactError>`
    implementing rungs in this exact order (each rung a private helper so tests can target it; the
    pub doors are load_setfit_apr and the parse-only read_setfit_apr_parts below — nothing else,
    bundle.rs door discipline):
    1. Raw length vs the contract cap (constant from a `contract_constants` module in this file,
       value 268435456 with a comment citing contracts/setfit-apr-v1.yaml) — before serde or the
       reader touches anything.
    2. Header parse: magic, version, header CRC, LAYOUT_ROW_MAJOR flag present, footer CRC over
       content (AprV2Reader).
    3. Typed tag: model_type == "setfit"; parse the ONE "setfit" custom key into
       `SetFitArtifactDoc` — a single #[serde(deny_unknown_fields)] struct mirroring the contract
       field list; schema_version must equal 1 (refuse unknown, UnsupportedSchemaVersion pattern).
    4. Structural consistency: tensor index set == canonical expected set exactly (derive expected
       set from the doc's hf_name_map values + tokenizer.blob; missing = IncompleteTensorSet,
       extra = InconsistentTensorSet); every entry's size == product(shape) x dtype_width (U8 width
       1 for tokenizer.blob, F32 width 4 for the rest); tokenizer.blob SHA-256 == doc.tokenizer_sha256.
    5. Non-finite scan over every F32 payload (chunk as LE f32; typed NonFiniteValue naming the
       tensor).
    6. `pub fn read_setfit_apr_parts(bytes: &[u8]) -> Result<(SetFitArtifactDoc, BTreeMap<String, (Vec<usize>, Vec<f32>)>, Vec<u8>), SetFitArtifactError>`
       — the parse-only door: EXACTLY rungs 1-5 (no rebuild, no probe replay), returning the doc,
       the HF-named tensor map, and the tokenizer.blob bytes. load_setfit_apr MUST be implemented
       as read_setfit_apr_parts followed by rungs 6-7, so rungs 1-5 exist ONCE — one ladder, never
       two policies (Pitfall 9). This is 04-05's AprCodec::deserialize dependency; landing it here
       (wave 3, core-owned) keeps wave 4 conflict-free — 04-05 must not touch aprender-core files.
    Extend SetFitArtifactError with one distinct variant per rung; tests assert variants via
    matches!, never message text. All numeric bounds arithmetic uses checked/saturating ops.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::artifact::ladder</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --features setfit --lib setfit::artifact::ladder` exits 0 with >= 11 tests passed
    - Every negative test corrupts REAL bytes produced by write_setfit_apr (induced corruption), not hand-built fakes
    - Source assertion: load_setfit_apr's body performs the length check before constructing any reader (rung order is visible in code)
    - Source assertion: load_setfit_apr calls read_setfit_apr_parts for rungs 1-5 (grep "read_setfit_apr_parts" in artifact.rs shows the pub definition AND the call site inside load_setfit_apr — rungs 1-5 are not duplicated)
  </acceptance_criteria>
  <done>Every malformed/oversized/incomplete/inconsistent/non-finite artifact dies typed, before a model exists; rungs 1-5 exist once behind two doors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild + probe replay + VerifiedSetFitModel typestate (rungs 6-7)</name>
  <files>crates/aprender-core/src/setfit/artifact.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/verify.rs lines 311-362 (probe_model + compare_probes — the five-rung comparison shape: counts FIRST, then ids, embeddings, probabilities, labels; and the NaN-visible within idiom at 336-341)
    - crates/aprender-core/src/setfit/mod.rs lines 340-391 (from_bundle_parts + tokenizer accessors; encode_texts — the embed delegation target)
    - contracts/setfit-apr-v1.yaml (probe tolerance 7.62939453e-06 embeddings / 1.0e-5 probabilities+logits; exact labels)
  </read_first>
  <behavior>
    - Test: a valid artifact loads, probes replay, and classify-capable VerifiedSetFitModel exists with artifact_sha256() equal to artifact_sha256_hex(bytes)
    - Test: one probe expectation perturbed beyond tolerance (re-signed through the writer) -> typed ProbeReplayFailed naming the probe index
    - Test: probe comparison is NaN-visible — a NaN expected value never compares as within tolerance
    - Test: doc_view() exposes every APR-05 field recovered from the artifact
    - Test: embed on two texts returns two vectors of the encoder's hidden dimension, every value finite, each vector's L2 norm within tolerance of 1.0; embed(&[]) fails typed (no panic path)
  </behavior>
  <action>
    1. Rung 6 — rebuild: invert hf_name_map (canonical -> HF), reconstruct the
       BTreeMap<String,(Vec<usize>,Vec<f32>)> of HF-named tensors, call SetFitMiniLm::from_bundle_parts
       with the doc's architecture record + tokenizer.blob bytes + root_seed from the doc; rebuild
       the head via MultinomialLogisticRegression::from_stored_coefficients + ordered_labels.
       (Rungs 1-5 arrive via Task 1's read_setfit_apr_parts — do not re-parse.)
    2. Rung 7 — probe replay (D-11): encode the 6 embedded probe inputs through the rebuilt model,
       compare against embedded expectations with the contract tolerances using the `within`
       partial_cmp idiom (counts -> inputs -> embeddings -> probabilities -> labels order, mirroring
       compare_probes). Failure = typed ProbeReplayFailed BEFORE any classify capability exists.
    3. `pub struct VerifiedSetFitModel` — all fields private, NO public constructor, not Clone-able
       into an unverified state; accessors: `artifact_sha256(&self) -> &str`,
       `ordered_labels(&self) -> &[String]`, `doc_view(&self) -> &SetFitArtifactDoc` (APR-05
       recovery: revisions, hashes, pooling/truncation policy, label order, head config, data
       fingerprint, seeds, update evidence, schema version), and
       `embed(&self, texts: &[String]) -> Result<Vec<Vec<f32>>, SetFitArtifactError>` — delegates
       to the rebuilt encoder's encode path (the SAME L2-normalized embeddings rung 7's probe
       replay verifies); typed error on empty input, no unwrap/panic path. This accessor is
       OPS-01's "embed" step — the public-API lifecycle proof lands in 04-12 Task 1. The classify
       method arrives in 04-04 — leave a doc comment naming that plan, add nothing speculative.
    4. Export from setfit/mod.rs: artifact module's public names only (write_setfit_apr,
       load_setfit_apr, read_setfit_apr_parts, SetFitArtifactView, SetFitArtifactDoc,
       SetFitArtifactError, VerifiedSetFitModel, artifact_sha256_hex).
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::artifact::probe</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-core --features setfit --lib setfit::artifact::probe` exits 0 with >= 5 tests passed
    - Source assertion: `grep -n "pub fn new" crates/aprender-core/src/setfit/artifact.rs` shows NO public constructor for VerifiedSetFitModel
    - The perturbed-probe negative works on real writer-produced bytes with the probe expectation legitimately re-signed (whole-artifact rewrite), so it exercises rung 7, not rung 2's CRC
    - VerifiedSetFitModel has a `pub fn embed` accessor (source assertion: grep "pub fn embed" artifact.rs == 1 match)
  </acceptance_criteria>
  <done>Probe replay is the last rung; the only classify-capable type is mint-on-success; embed is reachable through the public API</done>
</task>

<task type="auto">
  <name>Task 3: Out-of-crate trybuild non-constructibility proof</name>
  <files>crates/aprender-train/tests/ui/setfit_verified_model_constructed.rs, crates/aprender-train/tests/ui/setfit_verified_model_constructed.stderr</files>
  <read_first>
    - crates/aprender-train/tests/ui/setfit_external_codec_impl.rs (the house case-file shape: WHY-comment naming the expected diagnostic, then a minimal complete program)
    - crates/aprender-train/tests/ui.rs (the runner — compile_fail("tests/ui/*.rs"); the new case is picked up automatically, no runner edit)
  </read_first>
  <action>
    Add ONE trybuild case in aprender-train's existing ui suite (aprender-train is out-of-crate
    relative to aprender-core, which is the tier APR-04 demands; aprender-core has NO ui runner and
    does not need one — this decision resolves the PATTERNS.md open check). The case attempts both
    illegal doors in one file: (a) struct-literal construction of
    `aprender::setfit::VerifiedSetFitModel { .. }` and (b) any path to obtain one without
    load_setfit_apr. Expected diagnostics: E0423/E0451 (private fields / cannot construct) — pin the
    exact compiler output in the committed .stderr. Header comment states the APR-04 obligation:
    "evaluation, registration, benchmarking, prediction, and serving accept only
    ArtifactReloadedAndVerified — out-of-crate code cannot mint the consumer-side witness type."
  </action>
  <verify>
    <automated>cargo test -p aprender-train --features setfit --test ui</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-train --features setfit --test ui` exits 0 and its output includes setfit_verified_model_constructed (the new case RAN — zero-match is CR-02 vacuous)
    - A committed .stderr pins the diagnostic; the .rs names the expected error codes in its comment
  </acceptance_criteria>
  <done>Non-constructibility is a compile-time fact proven from outside the crate, not a review promise</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile artifact bytes -> loader | the primary Phase 4 attack surface; every rung is a STRIDE mitigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | DoS | allocation bomb | mitigate | rung 1 raw-length cap before parse; rung 4 sizes from DECLARED index before payload reads (BundleLimits pattern) |
| T-04-07 | Tampering | corrupted-but-CRC-valid artifact | mitigate | CRC32 is not cryptographic — SHA-256 identity + rung 7 probe replay catch semantic corruption |
| T-04-08 | Spoofing | foreign APR masquerading as SetFit | mitigate | rung 3 explicit-tag-only + deny_unknown_fields + schema-version refusal |
| T-04-09 | Tampering | NaN weight poisoning | mitigate | rung 5 non-finite scan, typed failure before prediction; NaN-visible probe comparison |
| T-04-10 | Elevation of Privilege | classify without verification | mitigate | VerifiedSetFitModel private ctor + trybuild compile-fail proof |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- All three scoped filters green with counted tests; `cargo test -p aprender-core --features setfit --lib setfit::` full module green
- clippy scoped to aprender-core clean; no unwrap() (banned via .clippy.toml)
</verification>

<success_criteria>
- One production door, rung order load-bearing, one distinct typed error per corruption class
- Rungs 1-5 exist once (read_setfit_apr_parts), shared by the production loader and 04-05's codec
- Probe replay re-proves "the served model is the evaluated model" for every fresh process
- Out-of-crate non-constructibility pinned by trybuild
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-03-SUMMARY.md` when done.
Record: test counts per filter, the full error-variant list, and the trybuild diagnostic pinned.
</output>
