---
phase: 04-apr-artifact-and-production-parity
plan: 07
type: execute
wave: 6
depends_on: ["04-04", "04-06"]
files_modified:
  - crates/apr-cli/src/commands/predict.rs
  - crates/apr-cli/src/commands/inspect.rs
  - crates/apr-cli/src/commands/eval/mod.rs
  - crates/apr-cli/src/commands/mod.rs
  - crates/apr-cli/src/extended_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
autonomous: true
requirements: [OPS-02, OPS-03, APR-05]
must_haves:
  truths:
    - "apr predict (NEW generic command) auto-detects a SetFit APR via the typed tag and serializes the D-08 core envelope — the CLI defines NO response struct of its own"
    - "A SetFit-shaped APR WITHOUT the model_type tag is treated as a plain APR, never sniffed by tensor names (D-04 negative)"
    - "apr inspect on a SetFit APR recovers: encoder/tokenizer revision + hashes, pooling/truncation policy, label order, head configuration, data fingerprint, seeds, update evidence summary + table hash, artifact hash, schema version (APR-05 list, complete)"
    - "apr eval on the canonical test split reaches create_selection_lock -> mint_test_token -> CanonicalTestAccess::grant from OUTSIDE the crate — TRN-07's missing positive tier — and canonical-test eval WITHOUT a prior validation lock is a typed refusal"
  artifacts:
    - path: "crates/apr-cli/src/commands/predict.rs"
      provides: "generic apr predict with explicit-tag auto-detect routing to core"
      contains: "load_setfit_apr"
    - path: "crates/apr-cli/src/commands/eval/mod.rs"
      provides: "SetFit branch driving the Ph3 trusted evaluator + lock workflow"
      contains: "create_selection_lock"
  key_links:
    - from: "crates/apr-cli/src/commands/predict.rs"
      to: "aprender::setfit::classify::ClassifyResponse"
      via: "CLI serializes the core envelope directly (D-08)"
      pattern: "ClassifyResponse"
    - from: "crates/apr-cli/src/commands/eval/mod.rs"
      to: "entrenar lock.rs mint_test_token"
      via: "the ONLY canonical-test door (Ph3 D-14, consumed as shipped)"
      pattern: "mint_test_token"
---

<objective>
Deliver the generic consumer surfaces (D-06: generic-only, no setfit-namespaced aliases): a NEW
`apr predict` command, the SetFit auto-detect branch in `apr inspect` (APR-05 recovery), and the
SetFit branch in `apr eval` that closes TRN-07's positive half by driving the Phase 3 lock/token/
grant workflow as its real canonical-test path (D-16).

Purpose: OPS-03 — generic commands auto-detect via the explicit tag and call the ONE shared core
path; OPS-02's inspect/eval/predict legs; APR-05 complete; TRN-07 "a user can reach the lock".

Output: predict.rs, inspect SetFit branch, eval SetFit branch + lock workflow tests.

Branch: `gsd/phase-4-apr-parity`. Wave 6, parallel with 04-08 (zero file overlap: 04-08 owns
serve files + apr-cli Cargo.toml; this plan owns predict/inspect/eval + enum/dispatch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md

<interfaces>
From apr-cli (verified):
- Tag detection precedent: inspect.rs:504-532 (read_metadata seeks to the metadata offset, parses AprV2Metadata, reads custom keys cheaply — the SetFit branch reads meta.model_type == "setfit" + the one "setfit" custom key)
- `ExtendedCommands::Eval { file, dataset, task, data, device, ... }` (extended_commands.rs:84-124); dispatch at dispatch_analysis.rs:1257; NO Predict variant exists today (verified) — apr predict is NEW
- CliError taxonomy (error.rs) — FeatureDisabled(9) for setfit-gated paths when the feature is off
From core (04-03/04-04, on branch):
- `load_setfit_apr(bytes) -> VerifiedSetFitModel`; `doc_view()` (APR-05 fields); `classify(texts, opts) -> ClassifyResponse`
From aprender-train Ph3 substrate (consumed AS SHIPPED — Pitfall 9 forbids a second policy):
- lock.rs: `create_selection_lock` (:491), `mint_test_token` (:419), `CanonicalTestAccess::grant` (:552/:586) — token minting takes the verified run object; canonical test access requires artifact-hash match
- evaluate.rs: trusted validation evaluator
- The Ph3 bytes-reconstruction path (03-08) rebuilds ArtifactReloadedAndVerified from bytes + codec — the door eval uses to get a verified RUN (not just a core model) for lock/minting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generic apr predict — explicit-tag auto-detect, core envelope out</name>
  <files>crates/apr-cli/src/commands/predict.rs, crates/apr-cli/src/commands/mod.rs, crates/apr-cli/src/extended_commands.rs, crates/apr-cli/src/dispatch_analysis.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/inspect.rs lines 500-540 (the cheap header+metadata read to copy for detection — do not load tensors to detect)
    - crates/apr-cli/src/commands/data_contrastive.rs (adapter + report render conventions)
    - crates/aprender-core/src/setfit/classify.rs (the envelope you serialize — and must NOT duplicate)
  </read_first>
  <action>
    1. New top-level `Predict` variant in ExtendedCommands (NOT under the setfit namespace — D-06):
       `apr predict <FILE> --text <STR> (repeatable) | --input <FILE> (one text per line)`,
       `--logits` (include optional logits), honors global --json. Dispatch arm ->
       commands::predict::run.
    2. Detection: read header + metadata ONLY (inspect.rs precedent); if model_type == "setfit"
       (cfg setfit): read file bytes -> load_setfit_apr -> classify -> print. --json prints
       serde_json of the core ClassifyResponse VERBATIM; human mode renders a view over the SAME
       struct (label, top probability, margin per text; backend + hash + latency in a footer).
       The CLI file must not declare any response struct — grep-assertable.
    3. Non-SetFit APR or non-APR file: typed InvalidFormat naming the supported families for
       predict in v1 (setfit only) — with the D-04 negative guaranteed: detection reads ONLY the
       typed tag, never tensor names.
    4. Feature-off behavior: with the binary built without setfit, a tagged SetFit file yields
       CliError::FeatureDisabled (exit 9) naming the feature to enable.
    Tests: tagged artifact -> envelope JSON with all OPS-04 fields; untagged-but-SetFit-shaped APR
    (build via raw AprV2Writer with empty model_type) -> InvalidFormat; corrupted artifact ->
    the loader's typed error surfaces as ModelLoadFailed exit 6; --input file with 3 mixed-length
    lines -> 3 ordered results.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib predict</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --features setfit --lib predict` exits 0 with >= 4 tests passed
    - Source assertion: `grep -c "struct.*Response" crates/apr-cli/src/commands/predict.rs` == 0 (the CLI serializes core's envelope, defines nothing)
    - The untagged-APR negative exists and asserts InvalidFormat via matches!
  </acceptance_criteria>
  <done>apr predict exists, auto-detects by tag only, and emits the shared envelope byte-identically to the library surface</done>
</task>

<task type="auto">
  <name>Task 2: apr inspect SetFit branch — the complete APR-05 recovery</name>
  <files>crates/apr-cli/src/commands/inspect.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/inspect.rs (current metadata read + render + --json paths — extend in place, exact-in-place analog per PATTERNS)
    - crates/aprender-core/src/setfit/artifact.rs (SetFitArtifactDoc — the field source; artifact_sha256_hex for the file-level hash)
  </read_first>
  <action>
    When inspect's metadata read finds model_type == "setfit": parse the one "setfit" custom key
    (reuse core's SetFitArtifactDoc when the setfit feature is on; when off, render the raw JSON
    value with a note — inspect must not require the feature to at least SHOW identity fields).
    Add a SetFit section to both human and --json output with EXACTLY the APR-05 list: encoder +
    tokenizer revision and hashes, pooling/normalization/truncation policy, ordered labels, head
    configuration (n_features, K), data fingerprint(s), seeds, update-evidence summary + table
    hash, artifact SHA-256 (computed over the file bytes — the same value every other surface
    reports), and artifact schema version. Inspection is READ-ONLY metadata work: no tensor loads,
    no probe replay (inspect does not classify, so it does not need the verified state — document
    this boundary in a comment referencing APR-04).
    Tests: JSON output contains every APR-05 field for a fixture artifact; a plain (untagged) APR's
    inspect output is byte-unchanged from before this plan (non-regression golden).
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib inspect</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --features setfit --lib inspect` exits 0 with >= 3 new tests passed
    - The --json SetFit section contains keys for all ten APR-05 items (test asserts each key name)
    - Non-SetFit inspect output unchanged (golden comparison in test)
  </acceptance_criteria>
  <done>A user recovers every contracted identity field from the artifact alone, offline</done>
</task>

<task type="auto">
  <name>Task 3: apr eval SetFit branch — validation eval + the TRN-07 lock workflow (D-16)</name>
  <files>crates/apr-cli/src/commands/eval/mod.rs, crates/apr-cli/src/dispatch_analysis.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/eval/mod.rs (the existing --task classify routing for the LoRA baseline — the SetFit branch must not disturb it; find the dispatch point)
    - crates/aprender-train/src/train/setfit/lock.rs (create_selection_lock, mint_test_token, CanonicalTestAccess::grant signatures — the exact workflow, consumed as shipped)
    - crates/aprender-train/src/train/setfit/evaluate.rs (trusted evaluator API)
    - crates/aprender-train/src/train/setfit/verify.rs + mod.rs (the bytes-reconstruction door that yields an ArtifactReloadedAndVerified run from artifact bytes + AprCodec)
  </read_first>
  <action>
    In the Eval dispatch, when file is an APR with the setfit tag and --task classify:
    1. Read bytes -> the Ph3 bytes-reconstruction path with AprCodec -> ArtifactReloadedAndVerified
       run (NOT a second verification policy — the trusted path already exists; Pitfall 9).
    2. `--data <DIR>` = the Phase 2 prepared dataset dir (attested ingest doors), new
       `--split validation|test` flag, default validation.
    3. validation: evaluate through the trusted evaluator; emit one machine-readable row (--json):
       metrics, ordered labels, artifact_sha256, evidence table hash, dataset fingerprint, seed —
       the EVAL-03-shaped row Phase 5 will consume.
    4. test: the ONLY path is create_selection_lock (committing to the validation-selected
       candidate) -> mint_test_token(verified run) -> CanonicalTestAccess::grant -> evaluate.
       Attempting --split test without the validation step/lock precondition -> the crate's typed
       refusal surfaced as ValidationFailed with the remedy text naming the required flow. The
       adapter adds NO gating of its own and bypasses NONE (thin adapter over lock.rs/evaluate.rs).
    Tests: (a) an out-of-crate/integration-level test demonstrating a user REACHING
    lock -> token -> grant through commands::eval (the TRN-07 positive tier — this exact test is
    the evidence D-16 exists for; name TRN-07 in its doc comment); (b) --split test refused typed
    without the lock precondition; (c) validation row contains artifact_sha256 == the file's hash;
    (d) the existing LoRA --task classify path's tests still pass untouched.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit --lib eval::setfit</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --features setfit --lib eval::setfit` exits 0 with >= 3 tests passed, including one whose name or doc names TRN-07
    - Source assertion: eval's SetFit branch contains create_selection_lock AND mint_test_token AND grant call sites (grep), and zero new gating types defined in apr-cli
    - Existing eval tests (`cargo test -p apr-cli --lib eval`) remain green
  </acceptance_criteria>
  <done>Canonical-test evaluation is reachable by a user and ONLY through the Phase 3 lock workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user files -> generic commands | hostile artifacts and text inputs enter through predict/inspect/eval |
| eval -> canonical test data | the leakage boundary TRN-07/D-16 guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Spoofing | untagged APR treated as SetFit | mitigate | explicit-tag-only detection + untagged negative test (D-04) |
| T-04-21 | Info Disclosure | canonical test leakage via eval | mitigate | test split reachable only via lock->token->grant; typed refusal without lock; no adapter-side bypass (grep-asserted) |
| T-04-22 | Tampering | corrupted artifact reaching prediction | mitigate | all commands route through load_setfit_apr's full ladder incl. probe replay |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- All scoped filters green with counts; `cargo check -p apr-cli` (feature off) still green
</verification>

<success_criteria>
- OPS-03 structural: one implementation per operation, reached by tag-detection routing
- APR-05 complete and machine-readable; TRN-07's positive tier demonstrated with named evidence
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-07-SUMMARY.md` when done.
Record: test counts, the APR-05 JSON key list as shipped, and the TRN-07 evidence test name (04-11's
closing audit will cite it when checking the requirement box).
</output>
