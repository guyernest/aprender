---
phase: 04-apr-artifact-and-production-parity
plan: 11
type: execute
wave: 10
depends_on: ["04-10"]
files_modified:
  - .planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SAFE-02, TRN-07]
must_haves:
  truths:
    - "The proposed CI change exists as a TEXTUAL PATCH FILE before approval — .github/workflows/ci.yml is not touched in the working tree until the human approves (review M12: preparing a working-tree edit to a workflow is already acting)"
    - "The applied CI step compiles and RUNS the apr-cli and aprender-serve setfit surfaces, the parity harness, AND the OPS-01 lifecycle integration target — command-identical to the 04-10 tier targets"
    - "Every Phase 4 test target is either IN the CI step or has a written exclusion rationale in the patch — no target is omitted silently, which is the CR-01 defect class this phase claims to have made structural"
    - "Scoped cargo-mutants runs PER CRATE with its own baseline, and the aggregate adjusted score is computed explicitly from the per-crate numbers (review M13)"
    - "The closing audit re-checks every Phase 4 requirement against SHIPPED behavior before any box is checked; TRN-07 cites TWO tests with the label each one earned — 04-15's SPAWNED lifecycle test as the CROSS-PROCESS evidence, and 04-07's two-invocation test as the IN-PROCESS lock-file-mediated evidence. Neither is labelled by intent (CLAUDE.md Verification Discipline rule 2), and REQUIREMENTS.md outlives the phase"
  artifacts:
    - path: ".planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch"
      provides: "the exact proposed ci.yml diff, presented before any workflow file is modified"
      contains: "ci.yml"
    - path: ".github/workflows/ci.yml"
      provides: "extended setfit step (applied only after approval)"
      contains: "apr-cli --features setfit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "honestly updated traceability incl. TRN-07"
      contains: "TRN-07"
  key_links:
    - from: ".github/workflows/ci.yml setfit step"
      to: "Makefile setfit-* targets"
      via: "command-identical tier/CI pairing (the CR-01 fix's own comment mandates it)"
      pattern: "cargo test -p apr-cli --features setfit"
---

<objective>
Close the phase: the human-gated ci.yml extension (SAFE-02's CI half), the scoped mutation gate over
all new Phase 4 code, and the closing requirements audit that lets no box be checked beyond what
shipped.

**Review finding M12 changes how the gate works.** The previous plan had Task 1 prepare an
uncommitted working-tree edit to `.github/workflows/ci.yml` and then claimed nothing had been
"applied". Under this repo's check-in-before-acting rule — and CLAUDE.md explicitly lists modifying
`.github/workflows/*.yml` as requiring escalation — editing the file IS acting. The proposal is now
a patch FILE under `.planning/`, and `git apply` runs only after recorded approval. **M13** splits
the mutation run per crate, because cargo-mutants operates in a selected package context and a
single invocation spanning four crates does not reliably do what the recipe claims.

**Checker warning W-5 closed here.** The previous patch enumerated its additions and excluded only
the two SPAWNED tests, each with a documented flake rationale — but silently omitted
`cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle`, the OPS-01 proof
created by 04-12. That target is an INTEGRATION target: the existing CI legs run
`-p aprender-train --lib setfit::`, which never reaches it, and it is in-process, so the flake
rationale that covers the spawned tests does not apply to it. A feature-gated surface with a tier
target and no CI job is exactly CR-01. It is now in the patch, and the patch must account for every
Phase 4 target either by inclusion or by written rationale.

Purpose: Phase 3's CR-01 established that a feature-gated surface without a CI job that runs it is
theater; CLAUDE.md reserves workflow edits for human decision — so the edit is drafted exactly,
presented as text, and applied only on approval.

Output: the presented patch, the approved ci.yml change, per-crate mutation evidence, audited
traceability.

Branch: `gsd/phase-4-apr-parity`. NOT autonomous — contains a blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md

<interfaces>
- ci.yml setfit step at lines ~274-309 (verified): `set -e`, NO pipes (a `| tee ; grep` shape reads grep's status — Verification rule 1; the step's own comment records this). Its three existing legs are `cargo test -p aprender-core --lib setfit::`, `cargo test -p aprender-train --lib setfit::` and `cargo test -p aprender-train --test ui`. Note what `--lib` does NOT reach: `crates/aprender-train/tests/setfit_apr_lifecycle.rs` and `crates/apr-cli/tests/setfit_parity.rs` are integration targets and need their own `--test` legs
- Mutation constraints (measured, Phase 3 / 03-HUMAN-UAT): `--timeout >= 120` (aprender-core's test binary needs >20s to LINK), tree-copy mode — `--in-place` conflicts with `--jobs` in cargo-mutants 25.3.1; scope with `-f` globs to new files only (unscoped = 1181 mutants ~= 44.6h)
- Memory: most cargo-mutants survivors here are proven-equivalent, not test gaps — that is why the criterion says "adjusted"
- 04-09-SUMMARY.md records a dev-dependency feature-unification limitation on the apr-cli `--no-default-features` RUN leg; the closing audit must carry it as a recorded limitation, not silently absorb it
- The Phase 4 modules to mutate, by crate:
  aprender-core: src/setfit/artifact.rs, src/setfit/classify.rs
  aprender-train: src/train/setfit/apr_codec.rs, src/train/setfit/apr_reload.rs, plus the bundle/lock/config additions from 04-13 and 04-14
  apr-cli: src/commands/setfit_train.rs, src/commands/predict.rs, src/setfit_io.rs
  aprender-serve: src/api/setfit_handlers.rs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the ci.yml change as a PATCH FILE — do not touch the workflow</name>
  <files>.planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch</files>
  <read_first>
    - .github/workflows/ci.yml lines 260-320 (the setfit step: its comment records the CR-01 rationale and the no-pipes rule — the extension must keep both; note that its three legs are all `--lib` or `--test ui`, so no Phase 4 integration target is reached today)
    - Makefile (the 04-10 targets whose commands the CI step mirrors — tier and CI must be command-identical to stop drift, per the step's own comment). Enumerate the FULL 04-10 target list; the patch must account for every one
    - 04-REVIEWS.md (the M12 finding: preparing a working-tree edit to a workflow is already acting)
  </read_first>
  <action>
    Write a unified diff to
    `.planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch` describing the
    minimal extension of the existing setfit step. The workflow file itself is NOT modified by this
    task — `git status` must show `.github/workflows/ci.yml` clean when the task ends.
    The proposed step adds, in the same `bash -c 'set -e; ...'` no-pipes shape as the existing step,
    commands COPIED VERBATIM from the 04-10 targets (one positional filter per invocation):
    - the apr-cli setfit lib suites — setfit_train, predict, inspect, eval::setfit, setfit_io as
      SEPARATE invocations;
    - `cargo test -p aprender-serve --features setfit --lib setfit`;
    - `cargo test -p aprender-train --features setfit --lib setfit::config` and
      `cargo test -p aprender-train --features setfit --lib setfit::evaluate` if 04-10's targets
      show the pre-existing broad train leg does not already cover them under the CI feature set;
    - `cargo test -p aprender-train --features setfit --test setfit_apr_lifecycle` — the OPS-01
      proof. This one is easy to lose and was lost once: the existing CI legs are `--lib`, which
      never reaches an integration target, and the flake rationale below does not cover it because
      it spawns nothing and runs in-process. Its inclusion is the difference between OPS-01 having a
      CI job and OPS-01 having only a local tier target, which is the CR-01 shape;
    - `cargo test -p apr-cli --features setfit,inference --test setfit_parity`;
    - the check legs for the two new crates mirroring setfit-feature-matrix.
    COVERAGE OBLIGATION: enumerate every target 04-10 created and, for each, either include its
    command or write a one-line exclusion rationale INSIDE the patch as a comment. Exactly two
    exclusions are expected, both spawned-binary tier3 targets — `setfit-serve-smoke` and
    `setfit-cli-lifecycle` — with the rationale that a CI loopback-spawn is a new flake surface and
    that promoting them is the human's call at the checkpoint. Any THIRD exclusion means something
    was dropped: stop and surface it rather than shipping a patch whose omissions are invisible.
    No other workflow file or step appears in the patch.
    Verify the patch applies cleanly WITHOUT modifying the tree: `git apply --check` on the patch
    file, which reports applicability and changes nothing.
  </action>
  <verify>
    <automated>git apply --check .planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch && rtk proxy git status --porcelain -- .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `git apply --check` on the patch exits 0 (it applies cleanly)
    - `rtk proxy git status --porcelain -- .github/workflows/ci.yml` prints NOTHING — the workflow file is untouched (review M12)
    - The patch contains the literal `--test setfit_apr_lifecycle` (the OPS-01 integration leg) and the literal `--test setfit_parity`; a patch with only `--lib` legs cannot reach either integration target
    - Every 04-10 target is accounted for: the patch's added commands plus its written exclusion rationales cover the complete target list, and the SUMMARY prints that accounting as a two-column list (target -> included / excluded with reason)
    - Exactly TWO exclusions appear, both spawned-binary targets, each with its flake rationale in the patch text
    - The patch's added lines contain no `|` pipes and sit inside a `set -e` block (grep on the patch file)
    - Every added cargo command appears verbatim in the Makefile (source assertion: each command string is grep-findable in the Makefile)
  </acceptance_criteria>
  <done>The exact edit exists as inspectable text, it reaches every surface a tier target reaches, and the repo's CI policy has not been touched to produce it</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approval of the ci.yml patch</name>
  <what-built>A textual patch file proposing an extension of the existing CI setfit step to add the apr-cli and aprender-serve setfit legs, the OPS-01 lifecycle integration target and the parity harness — command-identical to the 04-10 Make targets, same no-pipes/set -e discipline. `.github/workflows/ci.yml` has NOT been modified.</what-built>
  <how-to-verify>
    1. Read `.planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch`.
    2. Confirm `rtk proxy git status --porcelain -- .github/workflows/ci.yml` is empty — nothing was applied to get here.
    3. Confirm the added commands match the Makefile targets (setfit-cli-*, setfit-serve-tests, setfit-config-tests, setfit-evaluate-tests, setfit-lifecycle-tests, setfit-parity, feature-matrix legs).
    4. Confirm the target-coverage accounting: every 04-10 target either appears as a command or carries a written exclusion rationale, and there are exactly two exclusions.
    5. Decide whether the spawned-serve smoke and the spawned CLI lifecycle should ALSO run in CI (default: no — tier3-local; the flake-surface tradeoff is documented in the patch comment).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved + smoke") or describe required changes</resume-signal>
  <files>.planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch</files>
  <read_first>
    - .planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch (the artifact under review)
  </read_first>
  <action>PAUSE. Present Task 1's patch file contents to the human verbatim, together with the target-coverage accounting list, and wait. Do not apply, stage, commit or otherwise modify the workflow file while waiting. On "approved", proceed to Task 3; on requested changes, revise the patch FILE and re-present; on rejection, stop and report.</action>
  <verify>
    <automated>rtk proxy git status --porcelain -- .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The human's response is recorded verbatim in the SUMMARY before any change touches .github/workflows/
    - `rtk proxy git status --porcelain -- .github/workflows/ci.yml` is still empty at the moment of approval
    - The target-coverage accounting was presented alongside the patch, not only the diff
    - No commit containing ci.yml exists on the branch before the recorded approval
  </acceptance_criteria>
  <done>Explicit human approval (or rejection) recorded; no CI change applied without it</done>
</task>

<task type="auto">
  <name>Task 3: Apply the approved patch; per-crate mutation gate; closing requirements audit</name>
  <files>.github/workflows/ci.yml, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/04-apr-artifact-and-production-parity/04-11-ci-setfit.patch (the approved text — apply exactly this)
    - .planning/REQUIREMENTS.md (the Phase 4 rows + TRN-07's qualifier text)
    - .planning/phases/04-apr-artifact-and-production-parity/04-15-SUMMARY.md (the SPAWNED `env!(CARGO_BIN_EXE_apr)` lifecycle test — five separate processes, the lock file written by step 3 and read by step 4. This is TRN-07's CROSS-PROCESS evidence)
    - .planning/phases/04-apr-artifact-and-production-parity/04-07-SUMMARY.md (the TRN-07 evidence test name from 04-07 Task 3 — two invocations of `commands::eval::run` with a lock FILE between them, IN-PROCESS. Its own action text says "ACROSS TWO INVOCATIONS", not two processes; do not relabel it)
    - All 04-xx SUMMARYs (the audit's evidence base), 04-09's dev-dep unification note included
  </read_first>
  <action>
    1. Apply the approved patch with `git apply` (incorporating requested changes into the patch
       file first if any, and re-presenting if the change is material). If the human rejected it,
       stop and report — do not route around the gate. Commit the resulting ci.yml change alone.
    2. Mutation gate, ONE INVOCATION PER CRATE (review M13 — cargo-mutants operates in a selected
       package context, so a single run spanning four crates does not reliably do what it claims):
       run `cargo mutants --no-times --timeout 180 --package <crate> -f '<crate-relative globs>' -- --features setfit`
       for aprender-core, aprender-train, apr-cli and aprender-serve in turn, with the file globs
       listed in this plan's interfaces section. Tree-copy mode (NO `--in-place` if `--jobs` is
       used), CARGO_INCREMENTAL=0. Record each crate's BASELINE result and mutant/caught/survivor
       counts separately. Triage every survivor individually: proven-equivalent (re-run to confirm
       survival, justify in writing — the Phase 2 02-08 standard) or a real gap (kill it with a
       targeted test). Compute the AGGREGATE adjusted score explicitly from the per-crate numbers
       and show the arithmetic; target >= 85%. If wall-clock projects beyond a reasonable budget,
       report the measured projection and stop for the human (the Phase 3 compute-budget precedent)
       rather than shrinking scope silently.
    3. Closing audit (the 02-09 standard — nothing checked to make the table look finished):
       re-verify each of APR-01..05, OPS-01..06, SAFE-01, SAFE-02 against SHIPPED behavior by
       running the relevant gate and citing the evidence (test name / target / SUMMARY line);
       update REQUIREMENTS.md checkboxes and the Traceability table for those genuinely delivered
       at the "a user can" tier. OPS-01's citation must name BOTH the local target and the CI leg,
       since the point of Task 1's addition is that it now has both. Check TRN-07 (D-16) citing BOTH
       evidence tests with the label each one earned, and update its Phase 3 qualifier text to
       record where the positive half landed. The CROSS-PROCESS citation is 04-15's spawned
       lifecycle test — five `env!(CARGO_BIN_EXE_apr)` invocations where the lock file written by
       step 3 is read by step 4 in a DIFFERENT process. The IN-PROCESS citation is 04-07 Task 3's
       test — two invocations of `commands::eval::run` with a lock FILE between them, inside ONE
       process; its own action text says "ACROSS TWO INVOCATIONS", not two processes. Do NOT write
       "cross-process" next to the 04-07 test: that is labelling a run by intent, which CLAUDE.md
       Verification Discipline rule 2 forbids, and REQUIREMENTS.md outlives this phase. If 04-15's
       SUMMARY does not record the spawned test as passing, TRN-07 is checked at the in-process
       tier only, with one line saying the cross-process tier is unproven.
       Any requirement NOT fully demonstrable stays unchecked with one line saying exactly what is
       missing. Record explicitly, as audit notes rather than silent omissions: the D-02 typed-key
       amendment (04-01 item 14), the D-01 head/tokenizer name extension (04-01 item 5), the
       nullable-path allowlist and its `evidence.epsilon_used` residual (04-01 item 3), the
       SetFitBundle field-20 addition (04-13), the backend-identity v1 limitation (04-01 item 12),
       the apr-cli dev-dependency feature-unification limit on SAFE-02's run leg (04-09), and any
       `apr qa` verdict 04-15 surfaced as a finding.
       GSD-handler hazard (5 prior occurrences, one a false completion claim): after ANY
       state/roadmap handler call, DIFF the file and revert unearned completion claims by hand.
  </action>
  <verify>
    <automated>grep -c "apr-cli --features setfit" .github/workflows/ci.yml && grep -A 2 "TRN-07" .planning/REQUIREMENTS.md | grep -c "\[x\]"</automated>
  </verify>
  <acceptance_criteria>
    - The committed ci.yml matches the approved patch byte-for-byte (post-approval changes only if the human asked); `git log` shows the ci.yml commit AFTER the recorded approval
    - The committed ci.yml contains `--test setfit_apr_lifecycle` (source assertion) — OPS-01's CI leg exists, not only its tier target
    - Mutation report in the SUMMARY has FOUR per-crate sections, each with its baseline status, total mutants, caught, survivors with per-survivor triage — plus the aggregate adjusted score and the arithmetic producing it (or a measured stop-and-report)
    - REQUIREMENTS.md: TRN-07 cites TWO tests by name — 04-15's spawned lifecycle test labelled CROSS-PROCESS and 04-07 Task 3's test labelled IN-PROCESS (lock-file-mediated, two invocations). Source assertion: in REQUIREMENTS.md the string `cross-process` appears on the same TRN-07 row as the 04-15 test name and does NOT appear on the 04-07 citation
    - REQUIREMENTS.md: OPS-01 cited with both its Make target and its CI leg; every Phase 4 checkbox change backed by a named gate/test in the SUMMARY; no phase-level completion claim the verifier has not earned
    - The audit notes list all seven recorded amendments/limitations by name
  </acceptance_criteria>
  <done>CI runs what the tiers run, new code is mutation-hardened per crate, and the table says only what is true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow edit -> repo CI policy | reserved for human decision by project rule; preparing the edit is already acting |
| audit -> requirement claims | false completion claims are this repo's measured recurring defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Elevation of Privilege | autonomous CI edit | mitigate | the proposal is a patch FILE under .planning/; ci.yml is provably untouched until approval (git status assertion at both Task 1 and Task 2) |
| T-04-34 | Repudiation | unearned requirement checkboxes | mitigate | 02-09 audit standard: per-requirement evidence citation; handler-output distrust (diff + hand-repair); recorded amendment list |
| T-04-35 | Tampering | mutation gate scoped to nothing | mitigate | per-crate invocations with explicit globs; each report includes its baseline and total mutant count (a near-zero count is itself a red flag) |
| T-04-63 | Repudiation | a tier-covered surface silently absent from CI | mitigate | the patch must account for EVERY 04-10 target by inclusion or written rationale, exactly two exclusions are permitted, and the accounting is presented at the human checkpoint |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- ci.yml changes land only post-approval; `make tier3` fully green before the audit concludes
- KNOWN-REDs stay known: cargo package -p apr-cli (publish cascade), Darwin workspace check, arm64 tier2 clippy — none may be "fixed" or misread as Phase 4 regressions
</verification>

<success_criteria>
- SAFE-02 complete: CPU feature matrix verified in CI without Python or network, human-approved through a text-first gate
- Every Phase 4 tier target is either in CI or excluded on the record, so no surface is tier-only by accident
- Phase 4 traceability honest, TRN-07 closed with two named evidence tests each carrying the label it earned (04-15 spawned = cross-process, 04-07 = in-process), per-crate mutation evidence on record, every amendment recorded rather than absorbed
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-11-SUMMARY.md` when done.
Record: the target-coverage accounting list, the approval transcript outcome, the four per-crate
mutation tables and the aggregate arithmetic, the per-requirement audit evidence list, and the seven
recorded amendment/limitation notes.
</output>
