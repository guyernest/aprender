---
phase: 04-apr-artifact-and-production-parity
plan: 11
type: execute
wave: 9
depends_on: ["04-10"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SAFE-02]
must_haves:
  truths:
    - "The CI setfit step compiles and RUNS the apr-cli and aprender-serve setfit surfaces plus the parity harness, command-identical to the tier targets — applied ONLY after explicit human approval of the exact diff (CLAUDE.md autonomy rule: workflows are check-in-before-acting)"
    - "Scoped cargo-mutants over the new Phase 4 modules reports an adjusted score >= 85% with survivors individually triaged (proven-equivalent vs gap)"
    - "The closing audit re-checks every Phase 4 requirement against SHIPPED behavior before any box is checked; TRN-07's positive half is checked citing the 04-07 evidence test by name"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "extended setfit step (human-approved)"
      contains: "apr-cli --features setfit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "honestly updated traceability incl. TRN-07"
      contains: "TRN-07"
  key_links:
    - from: ".github/workflows/ci.yml setfit step"
      to: "Makefile setfit-* targets"
      via: "command-identical tier/CI pairing (the CR-01 fix's own comment mandates it)"
      pattern: "cargo test -p apr-cli --features setfit"
---

<objective>
Close the phase: the human-gated ci.yml extension (SAFE-02's CI half), the scoped mutation gate
over all new Phase 4 code, and the closing requirements audit that lets no box be checked beyond
what shipped.

Purpose: Phase 3's CR-01 established that a feature-gated surface without a CI job that runs it is
theater; CLAUDE.md reserves workflow edits for human check-in — so the edit is prepared exactly,
presented, and applied only on approval (the same route 03-HUMAN-UAT item 4 took).

Output: approved ci.yml diff applied; mutation evidence; audited traceability.

Branch: `gsd/phase-4-apr-parity`. NOT autonomous — contains a blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md

<interfaces>
- ci.yml setfit step at lines ~274-309 (verified): `set -e`, NO pipes (a `| tee ; grep` shape reads grep's status — Verification rule 1; the step's own comment records this), runs core+train setfit tests + train ui
- Mutation constraints (measured, Phase 3 / 03-HUMAN-UAT): `--timeout >= 120` (aprender-core's test binary needs >20s to LINK), tree-copy mode — `--in-place` conflicts with `--jobs` in cargo-mutants 25.3.1; scope with `-f` globs to new files only (unscoped = 1181 mutants ~= 44.6h)
- Memory: most cargo-mutants survivors here are proven-equivalent, not test gaps — that is why the criterion says "adjusted"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the exact ci.yml diff (do not apply)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 260-320 (the setfit step: its comment records the CR-01 rationale and the no-pipes rule — the extension must keep both)
    - Makefile (the 04-10 targets whose commands the CI step mirrors — tier and CI must be command-identical to stop drift, per the step's own comment)
  </read_first>
  <action>
    Draft (as a unified diff in the checkpoint message and a prepared working-tree edit that is NOT
    committed) the minimal extension of the existing setfit step:
    `cargo test -p apr-cli --features setfit --lib` (the setfit-scoped filters exactly as
    setfit-cli-tests runs them); `cargo test -p aprender-serve --features setfit --lib setfit`;
    `cargo test -p apr-cli --features setfit,inference --test setfit_parity`; and check legs for
    the two new crates mirroring setfit-feature-matrix. Same `bash -c 'set -e; ...'` no-pipes
    shape as the existing step. Do NOT add the spawned smoke to CI in this diff (tier3-local; a CI
    loopback-spawn is a new flake surface — if the human wants it, that is their call at the
    checkpoint). No other workflow file or step is touched.
  </action>
  <verify>
    <automated>git diff --stat .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat .github/workflows/ci.yml` shows exactly one file changed and the diff is confined to the setfit step region
    - The drafted step contains no `|` pipes and starts with set -e (grep on the diff)
    - Nothing is committed yet (`rtk proxy git status --porcelain` shows the file modified, unstaged)
  </acceptance_criteria>
  <done>The exact edit exists and is inspectable — nothing applied to CI history</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approval of the ci.yml edit</name>
  <what-built>A prepared, uncommitted extension of the existing CI setfit step adding the apr-cli and aprender-serve setfit legs plus the parity harness — command-identical to the 04-10 Make targets, same no-pipes/set -e discipline.</what-built>
  <how-to-verify>
    1. Review `git diff .github/workflows/ci.yml` (only the setfit step region changes).
    2. Confirm the added commands match the Makefile targets (setfit-cli-tests, setfit-serve-tests, setfit-parity, feature-matrix legs).
    3. Decide whether the spawned-serve smoke should ALSO run in CI (default: no — tier3-local; flake-surface tradeoff documented in the diff comment).
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved + smoke") or describe required changes</resume-signal>
  <files>.github/workflows/ci.yml</files>
  <action>PAUSE. Present Task 1's prepared diff to the human verbatim and wait. Do not commit, stage, or modify the workflow file while waiting. On "approved", proceed to Task 3; on requested changes, revise the prepared diff and re-present; on rejection, stop and report.</action>
  <verify>
    <automated>rtk proxy git status --porcelain -- .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The human's response is recorded verbatim in the SUMMARY before any commit touches .github/workflows/
    - No commit containing ci.yml exists on the branch before the recorded approval
  </acceptance_criteria>
  <done>Explicit human approval (or rejection) recorded; no CI change applied without it</done>
</task>

<task type="auto">
  <name>Task 3: Apply approved edit; scoped mutation gate; closing requirements audit</name>
  <files>.github/workflows/ci.yml, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Phase 4 rows + TRN-07's qualifier text)
    - .planning/phases/04-apr-artifact-and-production-parity/04-07-SUMMARY.md (the TRN-07 evidence test name)
    - All 04-0x SUMMARYs (the audit's evidence base)
  </read_first>
  <action>
    1. Commit the approved ci.yml edit exactly as approved (incorporate requested changes first if
       any; if the human rejected it, stop and report — do not route around the gate).
    2. Mutation gate: `cargo mutants --no-times --timeout 180 -f 'crates/aprender-core/src/setfit/artifact.rs' -f 'crates/aprender-core/src/setfit/classify.rs' -f 'crates/aprender-train/src/train/setfit/apr_codec.rs' -f 'crates/apr-cli/src/commands/setfit_train.rs' -f 'crates/apr-cli/src/commands/predict.rs' -f 'crates/aprender-serve/src/api/setfit_handlers.rs' -- --all-features`
       — tree-copy mode (NO --in-place if --jobs is used), CARGO_INCREMENTAL=0. Triage every
       survivor individually: proven-equivalent (re-run to confirm survival, justify in writing —
       the Phase 2 02-08 standard) or a real gap (kill it with a targeted test). Adjusted score
       >= 85%. If wall-clock projects beyond a reasonable budget, report the measured projection
       and stop for the human (the Phase 3 compute-budget precedent) rather than shrinking scope
       silently.
    3. Closing audit (the 02-09 standard — nothing checked to make the table look finished):
       re-verify each of APR-01..05, OPS-01..06, SAFE-01, SAFE-02 against SHIPPED behavior by
       running the relevant gate and citing the evidence (test name / target / SUMMARY line);
       update REQUIREMENTS.md checkboxes and the Traceability table for those genuinely delivered
       at the "a user can" tier; check TRN-07 citing 04-07's named evidence test (D-16) and update
       its Phase 3 qualifier text to record where the positive half landed. Any requirement NOT
       fully demonstrable stays unchecked with one line saying exactly what is missing.
       GSD-handler hazard (5 prior occurrences, one a false completion claim): after ANY
       state/roadmap handler call, DIFF the file and revert unearned completion claims by hand.
  </action>
  <verify>
    <automated>grep -c "apr-cli --features setfit" .github/workflows/ci.yml && grep -A 2 "TRN-07" .planning/REQUIREMENTS.md | grep -c "\[x\]"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml committed matches the approved diff byte-for-byte (post-approval changes only if the human asked)
    - Mutation report committed in the SUMMARY: total mutants, caught, survivors with per-survivor triage, adjusted score >= 85% (or a measured stop-and-report)
    - REQUIREMENTS.md: TRN-07 checked with the evidence test cited; every Phase 4 checkbox change is backed by a named gate/test in the SUMMARY; no phase-level completion claim anywhere the verifier has not earned
  </acceptance_criteria>
  <done>CI runs what the tiers run; new code is mutation-hardened; the table says only what is true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow edit -> repo CI policy | reserved for human decision by project rule |
| audit -> requirement claims | false completion claims are this repo's measured recurring defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-33 | Elevation of Privilege | autonomous CI edit | mitigate | blocking human checkpoint BEFORE commit; prepared-diff-only until approval |
| T-04-34 | Repudiation | unearned requirement checkboxes | mitigate | 02-09 audit standard: per-requirement evidence citation; handler-output distrust (diff + hand-repair) |
| T-04-35 | Tampering | mutation gate scoped to nothing | mitigate | -f globs enumerated explicitly; report includes total mutant count (a near-zero count is itself a red flag) |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- ci.yml change lands only post-approval; `make tier3` fully green before the audit concludes
- KNOWN-REDs stay known: cargo package -p apr-cli (publish cascade), Darwin workspace check, arm64 tier2 clippy — none may be "fixed" or misread as Phase 4 regressions
</verification>

<success_criteria>
- SAFE-02 complete: CPU feature matrix verified in CI without Python or network, human-approved
- Phase 4 traceability honest, TRN-07 closed with named evidence, mutation evidence on record
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-11-SUMMARY.md` when done.
Record: the approval transcript outcome, the mutation table, and the per-requirement audit evidence list.
</output>
