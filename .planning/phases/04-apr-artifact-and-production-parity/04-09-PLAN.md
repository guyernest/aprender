---
phase: 04-apr-artifact-and-production-parity
plan: 09
type: execute
wave: 7
depends_on: ["04-07", "04-08"]
files_modified:
  - crates/apr-cli/tests/setfit_parity.rs
  - crates/apr-cli/tests/fixtures/setfit_parity/goldens.json
  - crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256
  - crates/apr-cli/Cargo.toml
autonomous: true
requirements: [SAFE-01, OPS-04, OPS-01]
must_haves:
  truths:
    - "One harness feeds the SAME input set (mixed-length batches + edge cases) through the library call, a spawned apr binary, and an in-process HTTP request, and compares pairwise: exact labels, contract-tolerance probabilities/logits, identical artifact hash in all three"
    - "Frozen goldens with a SHA-256 manifest pin the core surface — live compare proves the surfaces agree, goldens prove nobody moved"
    - "An in-band negative — a deliberately skewed surface variant — FAILS the comparison in every cargo test run"
    - "ONE tier3 smoke test spawns a real apr serve on a loopback port and completes startup -> readiness -> one classify round trip against the installed binary"
  artifacts:
    - path: "crates/apr-cli/tests/setfit_parity.rs"
      provides: "three-surface parity harness + in-band negative + spawned smoke"
      contains: "CARGO_BIN_EXE_apr"
    - path: "crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256"
      provides: "fixture manifest (Ph1 D-13 discipline)"
      contains: "goldens.json"
  key_links:
    - from: "crates/apr-cli/tests/setfit_parity.rs"
      to: "aprender::setfit (library leg), env!(CARGO_BIN_EXE_apr) (CLI leg), realizar router oneshot (HTTP leg)"
      via: "pairwise comparison at contract tolerances"
      pattern: "oneshot"
---

<objective>
Build the D-13/D-14 parity gate: the composite harness no single analog covers — library vs
spawned CLI vs in-process HTTP, pairwise, on one committed input set, plus frozen goldens, the
in-band skewed negative, and the single tier3 spawned-serve smoke test.

Purpose: SAFE-01's "core/CLI/HTTP parity failures" detector; the phase's final criterion-4 proof.
Sited in `crates/apr-cli/tests/` per RESEARCH A4 — verified at plan time: apr-cli already depends
on realizar (aprender-serve) optionally and serve never depends on apr-cli, so no cycle exists.

Output: setfit_parity.rs + goldens + manifest + Cargo.toml test target wiring.

Branch: `gsd/phase-4-apr-parity`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
- Library leg: aprender::setfit::{artifact::load_setfit_apr, classify::ClassifyResponse}
- CLI leg: spawn env!("CARGO_BIN_EXE_apr") — never PATH (shadowed-artifact rule; CLAUDE.md discipline 8); `predict <apr> --input <file> --json` prints the envelope
- HTTP leg: realizar (aprender-serve) create_router_with_config + AppState::with_setfit_model + tower::util::ServiceExt::oneshot (app_state_default.rs:42-52 pattern)
- Tolerances + input constants: contracts/setfit-apr-v1.yaml (exact labels; 1.0e-5 probabilities/logits; NaN-visible within idiom)
- Cargo: dev-dependencies may add realizar with features ["setfit"] (+ its server feature via the setfit feature def), tower with util, axum body helpers; [[test]] name = "setfit_parity", required-features = ["setfit", "inference"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness skeleton — one fixture artifact, one input set, three legs, pairwise compare</name>
  <files>crates/apr-cli/tests/setfit_parity.rs, crates/apr-cli/Cargo.toml</files>
  <read_first>
    - crates/aprender-serve/src/api/tests/app_state_default.rs (oneshot mechanics; do NOT copy its multi-status tolerance)
    - crates/aprender-core/src/setfit/artifact.rs tests (the tiny-fixture view builder recipe — the harness builds its fixture artifact the same way, at test start, into a tempdir; the ~90 MB real model is NOT used and nothing large is committed)
    - crates/apr-cli/Cargo.toml (dev-dependencies + existing [[test]] targets for the required-features declaration shape)
  </read_first>
  <action>
    1. Cargo.toml: `[[test]] name = "setfit_parity"` with `required-features = ["setfit", "inference"]`;
       dev-deps: realizar { workspace = true, features = ["setfit"] }, tower (util), plus body/JSON
       helpers already in the tree. (Verified no cycle: serve never deps on apr-cli.)
    2. setfit_parity.rs: build the tiny-fixture artifact bytes ONCE (core writer), write to a
       tempdir file, load once for the library leg.
       Input set (committed as a const in the test, drawn from the contract's probe strings PLUS
       extras): a 5-text mixed-length batch including the 64x truncation-boundary string, the
       unicode string, "ok", an emoji/URL string, and a whitespace/newline string; plus a
       single-text call. Both with and without --logits/include_logits.
    3. Three legs producing core `ClassifyResponse` values:
       (a) library: model.classify(...);
       (b) CLI: std::process::Command on env!("CARGO_BIN_EXE_apr") with ["predict", apr_path,
       "--input", input_file, "--json"] (+"--logits"); capture stdout; serde_json parse INTO
       ClassifyResponse (type-level parity — a CLI drift is a deserialization failure);
       assert exit status success via .status (never through a pipe — Verification rule 1);
       (c) HTTP: router oneshot POST /v1/classify; parse body INTO ClassifyResponse.
    4. Pairwise comparator `assert_parity(a, b)`: labels EXACT; probabilities/logits within the
       contract tolerance using the NaN-visible partial_cmp idiom; margins within tolerance;
       artifact_sha256 EXACT-equal across all three; token_count/truncated EXACT. latency_ms and
       backend are compared for PRESENCE and plausibility (backend equal strings — same process
       family; latency > 0) but not for value equality (measurement, not identity).
    Tests: lib-vs-CLI, lib-vs-HTTP, CLI-vs-HTTP on the batch input; single-text on all three;
    logits-on variant.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --features setfit,inference --test setfit_parity` exits 0 with >= 5 tests passed
    - Source assertion: the CLI leg parses stdout INTO aprender::setfit::classify::ClassifyResponse (grep "ClassifyResponse" in the CLI-leg fn) and uses CARGO_BIN_EXE_apr (never a PATH lookup)
    - All three legs report the identical artifact_sha256 (asserted in every pairwise test)
  </acceptance_criteria>
  <done>The three surfaces demonstrably agree on values, not just shapes</done>
</task>

<task type="auto">
  <name>Task 2: Frozen goldens + SHA-256 manifest + the in-band skewed negative</name>
  <files>crates/apr-cli/tests/setfit_parity.rs, crates/apr-cli/tests/fixtures/setfit_parity/goldens.json, crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256</files>
  <read_first>
    - crates/apr-cli/tests/setfit_parity.rs (the harness you extend)
    - 04-CONTEXT.md specifics: "A gate that can go vacuous is not a gate" + the Ph1 D-24/Ph2 D-25/Ph3 D-08 in-band-negative house discipline
  </read_first>
  <action>
    1. Goldens: run the library leg on the committed input set against the deterministic tiny
       fixture artifact; freeze the expected labels/probabilities/logits/margins/token facts into
       fixtures/setfit_parity/goldens.json with a goldens.sha256 manifest (Ph1 D-13). A test
       compares the live library leg against the goldens at contract tolerance — this is the
       "nobody moved" pin; blessing a new golden is a deliberate diff, and the manifest makes a
       silent edit visible.
    2. In-band negative (MUST run and FAIL-the-bad-case in every cargo test): a
       `skewed_surface(resp) -> ClassifyResponse` helper that perturbs one probability by 10x the
       contract tolerance; test asserts `assert_parity(good, skewed)` REJECTS it (returns/panics
       with the comparison error). A second negative: a golden with one flipped byte fails the
       manifest check. Comment names the discipline chain (Ph1 D-24 / Ph2 D-25 / Ph3 D-08 / this
       plan) so the pattern is traceable.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity golden</automated>
  </verify>
  <acceptance_criteria>
    - Golden + negative tests pass (>= 3 tests in this group; counted)
    - fixtures/setfit_parity/goldens.sha256 exists and the manifest-check test reads it
    - The skewed-negative test FAILS the gate on the skewed variant (i.e., the test PASSES by observing rejection — asserted via matches!/panic capture, not log inspection)
  </acceptance_criteria>
  <done>Drift is loud, and the gate is proven able to fail</done>
</task>

<task type="auto">
  <name>Task 3: The ONE tier3 spawned-serve smoke test</name>
  <files>crates/apr-cli/tests/setfit_parity.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/serve/handlers.rs (startup flags: addr/port config for a loopback bind)
    - 04-CONTEXT.md D-14 (why exactly one spawned test: all-spawned = flaky-gate failure mode; in-process-only = installed binary never proven)
  </read_first>
  <action>
    Add `spawned_serve_smoke` marked #[ignore] (doc comment: run by the tier3 target 04-10 wires):
    spawn env!("CARGO_BIN_EXE_apr") serve with the fixture APR on 127.0.0.1:0-style ephemeral or a
    fixed high port; poll GET /health/ready with bounded retries (e.g. 50 x 100ms) until 200 —
    assert the readiness body's classifier_artifact_sha256 equals the fixture file's hash; POST one
    /v1/classify round trip; parse INTO ClassifyResponse; assert labels match the golden; kill the
    child and reap it (no orphan on failure — use a guard struct whose Drop kills). Capture the
    child's exit/status directly, never through a pipe.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity -- --ignored spawned_serve_smoke</automated>
  </verify>
  <acceptance_criteria>
    - The smoke test passes when run with --ignored (recorded run in the SUMMARY with the readiness latency observed)
    - Exactly ONE spawned-serve test exists in the file (grep count of "spawn" against serve == 1 site)
    - Child cleanup is Drop-guarded (source assertion)
  </acceptance_criteria>
  <done>The installed binary demonstrably serves — once, deterministically gated, in tier3</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surfaces -> comparator | the gate itself must be falsifiable, or parity is a claim not a check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Repudiation | vacuous parity gate | mitigate | in-band skewed negative fails in every cargo test; ran-something guard on the Make target (04-10) |
| T-04-28 | Tampering | golden drift | mitigate | SHA-256 manifest over goldens; flipped-byte negative |
| T-04-29 | Spoofing | testing a stale/shadowed apr binary | mitigate | CARGO_BIN_EXE_apr resolution only — never PATH (CLAUDE.md rule 3/8) |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Both filters (default + --ignored smoke) recorded with counts; CARGO_INCREMENTAL=0 recommended
- If the dev-dep addition surfaces an unexpected cycle after 04-08's feature edits, fall back per RESEARCH A4: site the harness in a dedicated test crate — record the decision in the SUMMARY
</verification>

<success_criteria>
- D-13 delivered: live three-surface comparison + frozen goldens + in-band negative
- D-14 delivered: in-process HTTP leg in every test run; exactly one tier3 spawned smoke
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-09-SUMMARY.md` when done.
Record: pairwise test counts, golden hash values, the smoke run transcript summary.
</output>
