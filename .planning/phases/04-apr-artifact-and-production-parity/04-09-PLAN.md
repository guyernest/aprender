---
phase: 04-apr-artifact-and-production-parity
plan: 09
type: execute
wave: 8
depends_on: ["04-07", "04-08"]
files_modified:
  - crates/apr-cli/tests/setfit_parity.rs
  - crates/apr-cli/tests/fixtures/setfit_parity/goldens.json
  - crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256
  - crates/apr-cli/Cargo.toml
autonomous: true
requirements: [SAFE-01, OPS-04, OPS-01]
must_haves:
  truths:
    - "One harness feeds the SAME ordered input set — including a text containing a newline, a tab, an empty string and non-ASCII — through the library call, a spawned apr binary and an in-process HTTP request, and compares pairwise: exact labels, contract-tolerance probabilities/logits, identical artifact hash in all three"
    - "All three legs receive the input as ONE ClassifyRequestDocument, so no leg can silently re-split a multiline text (review M2)"
    - "Frozen goldens with a SHA-256 manifest pin the core surface — live compare proves the surfaces agree, goldens prove nobody moved"
    - "An in-band negative — a skewed response built through the PUBLIC validating constructor — FAILS the comparison in every cargo test run"
    - "ONE tier3 smoke test spawns a real apr serve on a reserved loopback port and completes startup -> readiness -> one classify round trip against the installed binary, with the port handed over explicitly and the child's output drained"
    - "The dev-dependency this harness adds is recorded as a KNOWN weakening of the apr-cli --no-default-features RUN leg, so SAFE-02's gating evidence is read off the check leg that dev-deps cannot reach"
  artifacts:
    - path: "crates/apr-cli/tests/setfit_parity.rs"
      provides: "three-surface parity harness + in-band negative + spawned smoke"
      contains: "CARGO_BIN_EXE_apr"
    - path: "crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256"
      provides: "fixture manifest (Ph1 D-13 discipline)"
      contains: "goldens.json"
  key_links:
    - from: "crates/apr-cli/tests/setfit_parity.rs"
      to: "aprender::setfit (library leg), env!(CARGO_BIN_EXE_apr) (CLI leg), realizar router oneshot (HTTP leg)"
      via: "pairwise comparison at contract tolerances over one shared request document"
      pattern: "oneshot"
---

<objective>
Build the D-13/D-14 parity gate: the composite harness no single analog covers — library vs
spawned CLI vs in-process HTTP, pairwise, on one committed input set, plus frozen goldens, the
in-band skewed negative, and the single tier3 spawned-serve smoke test.

**Review findings closed here.** M2 (HIGH in the review): the previous harness sent CLI input as
"one text per line" while including a whitespace/newline probe — a text containing `\n` would have
become two CLI texts, so the three surfaces were not receiving the same ordered input set and the
gate could have reported agreement it had not tested. All three legs now carry the identical
`ClassifyRequestDocument`. M1: the skewed in-band negative is built through the envelope's PUBLIC
validating constructor (a 10x-tolerance perturbation is a finite value, so no backdoor is needed
and none is shipped). The latency flake and the spawned-port handoff findings are closed in Tasks 1
and 3.

Purpose: SAFE-01's "core/CLI/HTTP parity failures" detector; the phase's final criterion-4 proof.
Sited in `crates/apr-cli/tests/` per RESEARCH A4 — verified: apr-cli already depends on realizar
(aprender-serve) optionally and serve never depends on apr-cli, so no cycle exists.

Output: setfit_parity.rs + goldens + manifest + Cargo.toml test target wiring.

Branch: `gsd/phase-4-apr-parity`. Wave 7, parallel with 04-15 (which owns exactly one other
integration-test file and touches no Cargo.toml).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-apr-artifact-and-production-parity/04-CONTEXT.md
@.planning/phases/04-apr-artifact-and-production-parity/04-PATTERNS.md
@.planning/phases/04-apr-artifact-and-production-parity/04-REVIEWS.md
@contracts/setfit-apr-v1.yaml

<interfaces>
- Library leg: `aprender::setfit::{artifact::load_setfit_apr, classify::{ClassifyRequestDocument, ClassifyResponse}}`; response values read through accessors
- CLI leg: spawn `env!("CARGO_BIN_EXE_apr")` — never PATH (shadowed-artifact rule; CLAUDE.md discipline 8); `predict <apr> --input <json-file> --json` prints the envelope
- HTTP leg: realizar (aprender-serve) `create_router_with_config` + `AppState::with_setfit_model` + `tower::util::ServiceExt::oneshot` (app_state_default.rs:42-52 pattern)
- Tolerances + input constants: contracts/setfit-apr-v1.yaml (exact labels; 1.0e-5 probabilities/logits; NaN-visible within idiom; latency finite and >= 0)
- `ClassifyResponse::new` / `ClassifyResult::new` are PUBLIC validating constructors (04-04) — the skewed negative uses them
- Cargo: dev-dependencies may add realizar with features ["setfit"], tower with util, axum body helpers; [[test]] name = "setfit_parity", required-features = ["setfit", "inference"]
- apr-cli Cargo.toml:126 ALREADY carries `realizar = { workspace = true, optional = true }` behind the `inference` feature. Cargo does not permit an optional dev-dependency, so the dev-dep added here is unconditional and its features UNIFY with the normal dependency on the same package for every test build — see Task 1 step 1 for the consequence and where SAFE-02's evidence must be read instead
- aprender-serve's `setfit` feature is `["dep:aprender", "aprender/setfit", "server"]`
- `apr serve` binds `config.bind_addr()` (apr-cli serve/handlers.rs:697, :1132) — the parent must supply a concrete port; the server prints a banner containing the bound address
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harness skeleton — one fixture artifact, one request document, three legs, pairwise compare</name>
  <files>crates/apr-cli/tests/setfit_parity.rs, crates/apr-cli/Cargo.toml</files>
  <read_first>
    - crates/aprender-serve/src/api/tests/app_state_default.rs (oneshot mechanics; do NOT copy its multi-status tolerance)
    - crates/aprender-core/src/setfit/artifact.rs tests (the `fixture_view_full_pin_shape()` builder — the harness builds its fixture artifact from the SAME default production-nullability recipe, at test start, into a tempdir; the ~90 MB real model is NOT used and nothing large is committed)
    - crates/aprender-core/src/setfit/classify.rs (ClassifyRequestDocument's serde shape and the response accessors)
    - crates/apr-cli/Cargo.toml lines 110-140 (the existing optional `realizar` dependency behind `inference`, the dev-dependencies block, and the existing [[test]] targets for the required-features declaration shape)
  </read_first>
  <action>
    1. Cargo.toml: `[[test]] name = "setfit_parity"` with `required-features = ["setfit", "inference"]`;
       dev-deps: realizar { workspace = true, features = ["setfit"] }, tower (util), plus body/JSON
       helpers already in the tree. Declare ONLY this test target: plan 04-15 runs in the same wave
       and deliberately owns no Cargo.toml entry (its file carries a module-level
       `#![cfg(feature = "setfit")]` guard and is auto-discovered), so a second [[test]] declared
       here would name a file this worktree may not have and break the build.
       RECORD, in a comment beside the dev-dep and again in the SUMMARY, the SAFE-02 consequence,
       because it makes one downstream gate weaker than it reads: Cargo does not allow an optional
       dev-dependency, so this entry is unconditional, and dev-dependency features UNIFY with the
       normal dependency on the same package for any build that includes test targets. Under
       `cargo test -p apr-cli --no-default-features`, aprender-serve is therefore still built with
       its `setfit` feature, which pulls `aprender/setfit` — so that RUN leg proves apr-cli's OWN
       gated code is absent, and does NOT prove the setfit surface is absent from the dependency
       graph. SAFE-02's gating evidence must be read off the BUILD leg instead:
       `cargo check -p apr-cli --no-default-features` does not build dev-dependencies and is
       unaffected (this is the leg 04-06's gating criterion already uses). 04-10 Task 2 carries the
       same note in the feature-matrix recipe so the matrix does not overclaim. Do NOT "fix" this by
       feature-gating the dev-dep — Cargo rejects that; the sanctioned alternative, if the weaker
       run leg is ever unacceptable, is RESEARCH A4's dedicated test crate, and that is a
       restructuring decision, not a silent one.
    2. setfit_parity.rs: build the tiny-fixture artifact bytes ONCE (core writer, full-pin shape),
       write to a tempdir file, load once for the library leg.
       The input set is ONE committed `ClassifyRequestDocument` constant containing 5 texts drawn
       from the contract's probe strings PLUS extras: the 64x truncation-boundary string, the
       unicode string, "ok", an emoji/URL string, and a string containing an embedded newline AND a
       tab. Add a second document with a single text, and a third that is the batch document with
       `include_logits = true`. Serialize the document to a temp JSON file for the CLI leg — the
       SAME serialized bytes the HTTP leg posts. A comment must state the review finding this
       closes: a line-delimited CLI format would have re-split the newline text and the gate would
       have compared different inputs while reporting agreement.
    3. Three legs producing core `ClassifyResponse` values:
       (a) library: `model.classify(&document)`;
       (b) CLI: `std::process::Command` on `env!("CARGO_BIN_EXE_apr")` with
       ["predict", apr_path, "--input", doc_path, "--json"]; capture output; assert success via
       `output.status.success()` (never through a pipe — Verification rule 1); parse stdout INTO
       `ClassifyResponse` (a CLI drift becomes a deserialization failure, and the validated
       Deserialize also proves the CLI emitted a well-formed envelope);
       (c) HTTP: router oneshot POST /v1/classify with the same serialized document; parse the body
       INTO `ClassifyResponse`.
    4. Pairwise comparator `assert_parity(a, b)` reading through accessors: labels EXACT;
       probabilities/logits within the contract tolerance using the NaN-visible partial_cmp idiom;
       margins within tolerance; artifact_sha256 EXACT-equal across all three;
       token_count/truncated EXACT. `backend` compared for EQUALITY across legs (same process
       family, same kernel) and asserted to contain no SIMD capability token. `latency_ms` is
       compared for neither equality nor positivity — only `is_finite() && >= 0.0` (review LOW
       finding: a fast op with coarse timer resolution legitimately reports 0).
    Tests: lib-vs-CLI, lib-vs-HTTP, CLI-vs-HTTP on the batch document; single-text on all three;
    logits-on variant; and one test asserting that the newline-bearing text produced exactly one
    result per document text on every leg (the input-fidelity witness).
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --features setfit,inference --test setfit_parity` exits 0 with >= 6 tests passed
    - Source assertion: the CLI leg parses stdout INTO `aprender::setfit::classify::ClassifyResponse` and uses CARGO_BIN_EXE_apr (never a PATH lookup)
    - Source assertion: the CLI leg passes `--input <json>` and the harness contains no `.lines()` splitting of input text (review M2)
    - Source assertion: no assertion of `latency_ms > 0` appears anywhere in the file
    - Source assertion: the Cargo.toml dev-dependency block carries the dev-dep unification comment naming `cargo check -p apr-cli --no-default-features` as the leg SAFE-02's gating evidence is read from (grep for `no-default-features` in Cargo.toml returns >= 1)
    - The SUMMARY records the unification consequence in one sentence, so 04-10 and 04-11 inherit it as a known limitation rather than rediscovering it
    - The harness builds its fixture through the core `fixture_view_full_pin_shape()` recipe (source assertion: the full-pin builder name or its documented equivalent appears in the harness), so the parity legs run on the production nullability shape
    - All three legs report the identical artifact_sha256 (asserted in every pairwise test)
    - The result-count test asserts `results().len() == document.texts.len()` on all three legs
  </acceptance_criteria>
  <done>The three surfaces demonstrably agree on values, on the same inputs, including inputs a line-oriented format would have mangled — and the one guarantee this harness weakens is written down where the weakening happens</done>
</task>

<task type="auto">
  <name>Task 2: Frozen goldens + SHA-256 manifest + the in-band skewed negative</name>
  <files>crates/apr-cli/tests/setfit_parity.rs, crates/apr-cli/tests/fixtures/setfit_parity/goldens.json, crates/apr-cli/tests/fixtures/setfit_parity/goldens.sha256</files>
  <read_first>
    - crates/apr-cli/tests/setfit_parity.rs (the harness you extend)
    - crates/aprender-core/src/setfit/classify.rs (ClassifyResponse::new / ClassifyResult::new — the public validating constructors the negative uses)
    - 04-CONTEXT.md specifics: "A gate that can go vacuous is not a gate" + the Ph1 D-24/Ph2 D-25/Ph3 D-08 in-band-negative house discipline
  </read_first>
  <action>
    1. Goldens: run the library leg on the committed request documents against the deterministic
       tiny fixture artifact; freeze the expected labels/probabilities/logits/margins/token facts
       into fixtures/setfit_parity/goldens.json with a goldens.sha256 manifest (Ph1 D-13). A test
       compares the live library leg against the goldens at contract tolerance — the "nobody moved"
       pin; blessing a new golden is a deliberate diff and the manifest makes a silent edit visible.
       The goldens record no latency value (a measurement cannot be frozen) and record backend as an
       exact string (an identity can).
    2. In-band negative (MUST run and reject the bad case in every cargo test): a
       `skewed_response(resp) -> ClassifyResponse` helper that rebuilds the response through the
       PUBLIC `ClassifyResponse::new` / `ClassifyResult::new` constructors with one probability
       perturbed by 10x the contract tolerance. Comment must state why this needs no backdoor: the
       perturbed value is finite, so it passes the envelope's validation, and the gate under test is
       the COMPARATOR, not the constructor. A test asserts `assert_parity(good, skewed)` REJECTS it
       (captured as a returned comparison error or a caught panic — never by log inspection).
       A second negative: a golden with one flipped byte fails the manifest check.
       A third negative: a response JSON with a `null` probability fails to deserialize into
       ClassifyResponse — the witness that the envelope's validated Deserialize is live at the
       surface boundary (review M1).
       The comment names the discipline chain (Ph1 D-24 / Ph2 D-25 / Ph3 D-08 / this plan).
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity golden</automated>
  </verify>
  <acceptance_criteria>
    - Golden + negative tests pass (>= 4 tests in this group; counted)
    - fixtures/setfit_parity/goldens.sha256 exists and the manifest-check test reads it
    - The skewed-negative test PASSES by observing rejection, asserted via matches!/panic capture, not log inspection
    - Source assertion: the skewed helper calls `ClassifyResponse::new` — there is no test-only constructor, no `pub(crate)` backdoor and no field mutation
  </acceptance_criteria>
  <done>Drift is loud, the gate is proven able to fail, and the negative needed no hole in the type</done>
</task>

<task type="auto">
  <name>Task 3: The ONE tier3 spawned-serve smoke test</name>
  <files>crates/apr-cli/tests/setfit_parity.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/serve/handlers.rs lines 690-710 and 1125-1145 (bind_addr and the startup banner — the parent must supply a concrete port; the banner names the bound address)
    - 04-CONTEXT.md D-14 (why exactly one spawned test: all-spawned = flaky-gate failure mode; in-process-only = installed binary never proven)
    - 04-REVIEWS.md (the spawned-port MEDIUM finding — 127.0.0.1:0 cannot be used by the parent unless the chosen port is communicated, and a fixed high port is collision-prone)
  </read_first>
  <action>
    Add `spawned_serve_smoke` marked `#[ignore]` (doc comment: run by the tier3 target 04-10 wires).
    Port handoff protocol, stated explicitly because the review found it underspecified:
    1. The PARENT reserves a port by binding `TcpListener::bind("127.0.0.1:0")`, reading
       `local_addr().port()`, then dropping the listener. Pass that port to the child via its
       address flag. Document the reserve-then-release race window and handle it: on a child that
       exits with an address-in-use error, retry the whole reserve-spawn sequence up to 3 times
       before failing. Do NOT use a fixed high port (collision-prone) and do NOT pass port 0 (the
       parent could not then know where to connect).
    2. Spawn `env!("CARGO_BIN_EXE_apr")` serve with the fixture APR. Set stdout and stderr to
       piped and DRAIN them on background threads into bounded buffers, so a chatty child cannot
       fill a pipe and deadlock; include the captured tail in any failure message.
    3. Poll GET /health/ready with bounded retries (50 x 100ms) until 200; assert the readiness
       body's `classifier_artifact_sha256` equals the fixture file's hash and
       `classifier_verified` is true.
    4. POST one /v1/classify round trip with the same committed request document; parse INTO
       ClassifyResponse; assert labels match the golden.
    5. Kill the child and reap it through a guard struct whose Drop kills and waits — no orphan on
       failure or panic. Read the child's status from the reaped `ExitStatus`, never through a pipe.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --features setfit,inference --test setfit_parity -- --ignored spawned_serve_smoke</automated>
  </verify>
  <acceptance_criteria>
    - The smoke test passes when run with --ignored (recorded run in the SUMMARY with the readiness latency observed and the port used)
    - Exactly ONE spawned-serve test exists in the file (grep count of serve-spawning sites == 1)
    - Source assertion: the port is obtained from a TcpListener bound to 127.0.0.1:0 and passed explicitly to the child; no fixed port literal appears
    - Source assertion: child stdout/stderr are drained on threads and the cleanup guard implements Drop
  </acceptance_criteria>
  <done>The installed binary demonstrably serves — once, deterministically gated, with a port the parent actually knows and a child it always reaps</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| surfaces -> comparator | the gate itself must be falsifiable, or parity is a claim not a check |
| harness -> input fidelity | a gate that reshapes its own inputs per surface proves nothing |
| dev-dependency graph -> feature-gating evidence | a dev-dep that unifies features silently widens the graph a gating leg claims to have narrowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Repudiation | vacuous parity gate | mitigate | in-band skewed negative fails in every cargo test; ran-something guard on the Make target (04-10) |
| T-04-28 | Tampering | golden drift | mitigate | SHA-256 manifest over goldens; flipped-byte negative |
| T-04-29 | Spoofing | testing a stale/shadowed apr binary | mitigate | CARGO_BIN_EXE_apr resolution only — never PATH (CLAUDE.md rule 3/8) |
| T-04-53 | Repudiation | surfaces compared on different inputs | mitigate | one serialized ClassifyRequestDocument feeds all three legs; a result-count test pins per-leg input fidelity |
| T-04-61 | Repudiation | a SAFE-02 run leg that proves less than it reads | mitigate | the dev-dep unification is recorded in Cargo.toml, in this SUMMARY and in 04-10's matrix comment; SAFE-02's gating evidence is read off the `cargo check --no-default-features` leg, which dev-deps do not reach |
| T-04-SC | Tampering | package installs | accept | zero new packages this phase |
</threat_model>

<verification>
- Both filters (default + --ignored smoke) recorded with counts; CARGO_INCREMENTAL=0 recommended
- The dev-dep unification note exists in Cargo.toml and the SUMMARY, and 04-10 Task 2 mirrors it in the feature-matrix recipe — a note in only one of the two places is the drift this pairing exists to prevent
- If the dev-dep addition surfaces an unexpected cycle after 04-08's feature edits, fall back per RESEARCH A4: site the harness in a dedicated test crate — record the decision in the SUMMARY
</verification>

<success_criteria>
- D-13 delivered: live three-surface comparison + frozen goldens + in-band negative
- D-14 delivered: in-process HTTP leg in every test run; exactly one tier3 spawned smoke with a specified port protocol
- The harness's cost to SAFE-02's run leg is stated, bounded, and pointed at the leg that still carries the guarantee
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-09-SUMMARY.md` when done.
Record: pairwise test counts, golden hash values, the smoke run transcript summary, the port
handoff behaviour observed (including any retry), and the dev-dependency feature-unification note in
the exact wording 04-10 and 04-11 will cite.
</output>
