---
phase: 04-apr-artifact-and-production-parity
plan: 19
type: execute
wave: 11
gap_closure: true
depends_on: []          # patches code delivered by 04-01..04-17; no NEW plan precedes it in wave 11
files_modified:
  - contracts/aprender/binding.yaml
  - crates/aprender-core/src/setfit/classify.rs
autonomous: true
requirements: [OPS-04, OPS-06, SAFE-01]
closes_findings: [SC4-backend_identity, SC5-misreporting-backend, BIND-004]

must_haves:
  truths:
    - "The `backend_identity` equation of `setfit-apr-v1.yaml` names a symbol that exists in the tree"
    - "`make contract-audit-phase4` reports 15/15 bound AND 15/15 implemented, with zero BIND-004 warnings"
    - "Flipping the status to `implemented` is EARNED: a test that the compiler itself witnesses proves the named symbol exists with the named signature, and the same test pins the binding row's strings"
    - "The ghost path `aprender::setfit::classify::backend_identity` appears nowhere in the registry"
    - "`contracts/setfit-apr-v1.yaml` is byte-unchanged — this is a registry edit, not a contract edit"
  artifacts:
    - path: "contracts/aprender/binding.yaml"
      provides: "the corrected backend_identity row: the shipped module_path, function and signature, status implemented"
      contains: "ExecutionBackend::identity"
    - path: "crates/aprender-core/src/setfit/classify.rs"
      provides: "the resolution guard — a compile-witnessed function item plus an include_str! pin of the registry row"
      contains: "binding.yaml"
  key_links:
    - from: "contracts/aprender/binding.yaml"
      to: "crates/aprender-core/src/setfit/encoder.rs"
      via: "module_path aprender::setfit::encoder + function ExecutionBackend::identity, pinned by a test that names the same two strings"
      pattern: "function: ExecutionBackend::identity"
    - from: "crates/aprender-core/src/setfit/classify.rs"
      to: "contracts/aprender/binding.yaml"
      via: "include_str! — a missing registry is a COMPILE error, not a silent skip"
      pattern: "include_str!\\(\"\\.\\./\\.\\./\\.\\./\\.\\./contracts/aprender/binding\\.yaml\"\\)"
  caveats:
    - "This plan closes ONE clause of SC4 and ONE clause of SC5. The OTHER half of each — running parity over a user-PRODUCED artifact — stays blocked behind F-10 and is Phase 5 work. OPS-04, OPS-06 and SAFE-01 all remain UNCHECKED after this plan; it removes their named blocker, it does not deliver the produced artifact they also require."
    - "This plan does NOT close F-10, the per-crate cargo-mutants gate (04-11 must-have 4), or the SAFE-02 'in CI' clause. OPS-01 and OPS-02 stay NOT MET."
    - "`pv verify-bindings` is measurably blind in this monorepo layout (0/124 verified, including known-good symbols like `softmax` and `gelu`). It is run and recorded, but it is NOT this plan's gate — the compile-witness test is."
---

<objective>
Correct the one Phase 4 binding row that names a symbol which does not exist, and earn the status
flip with a check rather than an assertion.

Purpose: `backend_identity` is named BY NAME in three requirements — OPS-04 ("backend identity"),
OPS-06 ("or misreporting the backend") and SAFE-01 ("the contract registry is not yet honest"). Its
row points at `aprender::setfit::classify::backend_identity`, which has never existed on this
branch. 04-10 deliberately left it `pending` rather than flip it, writing in the row's own notes
that "flipping this to `implemented` would record a claim nothing can check". This plan builds the
check first, then flips.

Output: a registry row naming `ExecutionBackend::identity`, a compile-witnessed resolution guard,
and `make contract-audit-phase4` at 15/15 implemented with zero BIND-004.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: work on the CURRENT branch `gsd/phase-2-contract-gate`. Do NOT create, rename or switch
branches. Do NOT open a PR.

**CLAUDE.md § "Contract Validation: DOGFOOD `pv`, NEVER bash" governs this entire plan.** Writing a
bash/yq/python script that re-implements what `pv` already does is muda and will be rejected. Note
the documented trap: `pv diff` takes TWO FILESYSTEM PATHS, never a git revision — materialise the
old revision with `git show <rev>:<path> > /tmp/<name>.yaml` first, then diff two real files.
Passing a revision where a path is expected reports `Failed to read contract file`, which reads
like a missing contract rather than a misused flag.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-apr-artifact-and-production-parity/04-VERIFICATION.md
@.planning/REQUIREMENTS.md

Measured at HEAD `81652bb50` during planning — re-confirm before editing, do not re-derive:

- `contracts/aprender/binding.yaml` — the `backend_identity` row (search for
  `equation: backend_identity`) currently reads `module_path: aprender::setfit::classify`,
  `function: backend_identity`, `status: pending`, with a long `notes:` block that already states
  the diagnosis and explicitly scopes the correction OUT of 04-10 as "a SEMANTIC edit to the
  registry rather than a binding-status flip".
- The shipped symbol: `crates/aprender-core/src/setfit/encoder.rs:227` declares
  `pub struct ExecutionBackend { device: &'static str, kernel: &'static str }` (both fields
  PRIVATE), `:232` opens `impl ExecutionBackend`, and `:235` is
  `pub fn identity(&self) -> String` returning `format!("{}:setfit-core:{}", self.device, self.kernel)`.
  Sibling accessors `device()` at :241 and `kernel()` at :247.
- `contracts/setfit-apr-v1.yaml:849-871` is the `backend_identity:` equation. Its `formula` is
  `backend = "<device>:<implementation>:<kernel>" ; v1 = "cpu:setfit-core:autograd-trueno-matmul"`,
  and its invariants forbid a capability-detection result (`Backend::AVX2`, `Backend::NEON`,
  `select_backend*`) from appearing in the field.
- Registry row conventions, read from the file: rows carry `contract` (the BARE filename — a `../`
  prefix parses and binds nothing), `equation`, `module_path`, `function`, optional `signature`,
  `status` (accepts only `implemented|partial|not_implemented|pending`), optional `notes`. Rows 1-2
  demonstrate the `signature:` field. The `selection_lock_lifecycle` row demonstrates that a
  cross-crate `module_path` (`entrenar::train::setfit::lock`) is accepted under
  `target_crate: aprender`.
- `Makefile:1468` — `PHASE4_CONTRACTS := contracts/setfit-apr-v1.yaml`;
  `Makefile:1652` — `contract-audit-phase4`;
  `Makefile:1379-1380` — `PV_BIN := cargo run --release -p aprender-contracts-cli --bin pv --`
  and `BINDING := contracts/aprender/binding.yaml`.
  A prebuilt `./target/release/pv` exists (5.3M) and can be used directly for exploratory runs.
- Measured at HEAD: `pv audit contracts/setfit-apr-v1.yaml --binding contracts/aprender/binding.yaml`
  → rc=0; `Total equations: 15 / Bound equations: 15 / Implemented: 14 / Not implemented: 1`;
  one line `[WARN] BIND-004: Equation 'backend_identity' in setfit-apr-v1.yaml is pending
  implementation`.
- Measured at HEAD: `pv verify-bindings contracts/aprender/binding.yaml --crate-name aprender`
  → rc=1, `aprender: 0/124 binding functions verified in source`, and the ghost list includes
  `softmax`, `gelu` and `within` — symbols that demonstrably DO exist. The tool does not resolve in
  this monorepo's `crates/*/src` layout. **This is a repo-wide finding, not a Phase 4 defect, and
  it is why this plan does not use `pv verify-bindings` as its gate.**
- `crates/aprender-core/src/setfit/classify.rs` already carries the exact guard pattern this plan
  extends: `mod backend` (a `#[cfg(test)] mod` opening around line 1432) with
  `const CONTRACT: &str = include_str!("../../../../contracts/setfit-apr-v1.yaml");` at :1440,
  `fn setfit_sources()` reading `src/setfit` via `read_dir`, and
  `fn execution_backend_has_no_public_constructor_or_setter` at :1531 which does
  `include_str!("encoder.rs")` and scans the struct and impl bodies.
- `Makefile:1969` — `setfit-classify-tests` runs
  `cargo test -p aprender-core --features setfit --lib setfit::classify::` with
  `assert_tests_ran ... 45`. Measured at verification: 50 passed. Adding two tests keeps it above
  the floor without a Makefile edit, which is why this plan does not touch the Makefile.

<interfaces>
From `crates/aprender-core/src/setfit/encoder.rs`:
  pub struct ExecutionBackend { /* device, kernel — both private */ }
  impl ExecutionBackend {
      pub fn identity(&self) -> String;      // "<device>:setfit-core:<kernel>"
      pub fn device(&self) -> &'static str;
      pub fn kernel(&self) -> &'static str;
  }
  const ENCODE_BACKEND: ExecutionBackend;    // module-private, handed out by the encode path only

From `crates/aprender-core/src/setfit/classify.rs` (test module `backend`, already present):
  const CONTRACT: &str = include_str!("../../../../contracts/setfit-apr-v1.yaml");
  fn setfit_sources() -> Vec<(String, String)>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build the check FIRST — a compile-witnessed resolution guard that pins the registry row to the shipped symbol</name>

  <read_first>
    - `crates/aprender-core/src/setfit/classify.rs` from the start of `mod backend` (around line
      1425) to the end of the file — the module being extended, its `CONTRACT` include_str! idiom,
      its `setfit_sources()` helper, and `execution_backend_has_no_public_constructor_or_setter`
      which is the closest structural analog for a source-scanning guard.
    - `crates/aprender-core/src/setfit/encoder.rs` lines 220-255 — the SOURCE OF TRUTH for the
      symbol being bound: the struct, the impl block, and the exact `identity` signature.
    - `contracts/aprender/binding.yaml` — the whole `backend_identity` row including its `notes:`
      block, plus two neighbouring rows so the field conventions (`signature:`, bare `contract:`
      filename, allowed `status:` values) are read rather than assumed.
    - `contracts/setfit-apr-v1.yaml` lines 845-875 — the `backend_identity` equation whose formula
      and invariants the guard must not contradict.
    - `CLAUDE.md` § "Verification Discipline" rule 7 (a guard regex ships a must-match /
      must-not-match case table) and rule 4 (extending a guard's SCOPE requires re-mutating in the
      new scope).
  </read_first>

  <behavior>
    - Test 1 (compile witness): coercing `ExecutionBackend::identity` into a typed function item —
      a binding of type `fn(&ExecutionBackend) -> String` — makes rustc itself the witness that the
      symbol exists at that path with that signature. If the symbol is renamed or its signature
      changes, this is a COMPILE error in the crate that owns it, not a silent registry drift.
    - Test 2 (registry pin, must-match arm): the `backend_identity` row in
      `contracts/aprender/binding.yaml`, read via `include_str!`, carries
      `module_path: aprender::setfit::encoder`, `function: ExecutionBackend::identity`, and
      `status: implemented`. The function column is TYPE-QUALIFIED because `identity` is an
      inherent method, not a free function: a bare `identity` would not resolve any better than
      the ghost it replaces. Precedent measured at HEAD — `binding.yaml:1390` carries
      `function: SetFitArtifactDoc` and `:1444` carries `function: ClassifyResponse`, so a type
      name in this column is the registry's own established form.
    - Test 3 (registry pin, must-not-match arm): the string
      `aprender::setfit::classify::backend_identity` — and the bare `function: backend_identity`
      form — appear NOWHERE in the registry. A correction that left the ghost behind in a second
      row would otherwise pass Test 2.
    - Test 4 (non-vacuity): the row parser used by Tests 2 and 3 finds a `backend_identity` row at
      all. A parser that silently matched nothing would make both pins vacuously true — this is the
      CR-02 zero-match class at the guard tier.
    - Test 5 (contract agreement): the identity produced by the live encode path still satisfies
      the contract's three-segment grammar and its middle segment is `setfit-core`, so the row now
      names a symbol whose OUTPUT is the thing the equation describes, not merely a symbol that
      compiles.
    - Test 6 (closes the `module_path` hole): Tests 1-3 witness the FUNCTION but compare
      `module_path` against a hard-coded literal inside `classify.rs` — a file that is not the
      module the literal names, so a moved `ExecutionBackend` would leave the literal true and the
      row wrong. Close it by reusing the sibling idiom already at `classify.rs:1533`:
      `include_str!("encoder.rs")` and assert that file declares `impl ExecutionBackend` and
      `pub fn identity`. That ties the `module_path` segment `encoder` to the actual file the
      symbol lives in, which is the strongest tie available without a proc-macro.
  </behavior>

  <files>crates/aprender-core/src/setfit/classify.rs</files>

  <action>
Extend the existing `#[cfg(test)] mod backend` in `crates/aprender-core/src/setfit/classify.rs`.
Do not create a new module and do not move the existing tests — the Make gate
`setfit-classify-tests` filters on `setfit::classify::`, so anything placed outside that path stops
being gated.

Add a second registry constant beside the existing `CONTRACT`, named `BINDING`, defined as
`include_str!("../../../../contracts/aprender/binding.yaml")`. Use the same four-`../` depth the
neighbouring `CONTRACT` constant already proves correct from this file. Give it the same doc
rationale the `CONTRACT` constant carries: `include_str!` and not a runtime read, because a missing
registry must be a COMPILE error here where a runtime read would be a silent skip.

Write the row parser as a small helper inside the module that locates the block beginning at the
line `  equation: backend_identity` within the `- contract: setfit-apr-v1.yaml` list and returns
the raw text of that block up to the next `- contract:` line. Have it return `Option<&str>` so
Test 4 can assert `is_some()` explicitly rather than relying on an `expect` inside the other tests.

Write Tests 1-5 per the behavior block. For Test 1 use a typed local binding of the function item
so the coercion is checked by rustc; add `let _ = f;` or call it against a value obtained from the
existing encode path so the binding is not dead. For Test 3 assemble the forbidden needles from
`concat!` fragments so the guard's own source text does not contain them — `classify.rs` is inside
the directory `setfit_sources()` walks, and the sibling guard
`the_setfit_surface_names_no_capability_detection_symbol` documents this exact F-05 self-scan
hazard in its own comment. For Test 5 reuse whatever the existing `mod backend` tests already use
to obtain a live `ExecutionBackend`; do not construct one, because `ExecutionBackend` has no public
constructor and the sibling guard exists to keep it that way.

Ship the must-match / must-not-match case table as executed assertions inside Tests 2, 3 and 4
rather than as a comment. Record in the SUMMARY that the table was re-run, not re-read.
  </action>

  <verify>
    <automated>cargo test -p aprender-core --features setfit --lib setfit::classify::backend > /tmp/04-19-t1.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "test result:" /tmp/04-19-t1.log</automated>
  </verify>

  <acceptance_criteria>
    - The SIX tests exist and RAN: the log for the filter `setfit::classify::backend` shows a
      `test result:` line with a passed count at least 6 higher than the same filter's count at
      HEAD. Measure the HEAD count with the identical command BEFORE editing and record both
      numbers in the SUMMARY. A zero-match filter exits 0 in libtest, so the count is the evidence,
      not the exit code.
    - Test 4 is not vacuous: temporarily rename the `equation: backend_identity` key in the
      registry to `equation: backend_identity_x`, re-run the filter, and confirm Test 4 turns RED
      naming the missing row. Revert the probe. Record the exact assertion message in the SUMMARY.
    - Test 1 is a real compile witness: temporarily rename `ExecutionBackend::identity` to
      `identity_x` in `encoder.rs`, confirm `cargo test -p aprender-core --features setfit --lib`
      fails with a COMPILE error naming the missing method (not a runtime assertion), and revert.
      Record the rustc error code and message in the SUMMARY.
    - Test 3's must-not-match arm is live: temporarily add a decoy row to the registry carrying
      `module_path: aprender::setfit::classify` and `function: backend_identity`, confirm Test 3
      turns RED, and revert.
    - Test 6 closes the `module_path` hole and is itself falsifiable: apply a COMPILE-NEUTRAL text
      mutation in `encoder.rs` — write `impl  ExecutionBackend {` with a double space, the form the
      sibling guard at `classify.rs:1532` also keys on — confirm Test 6 turns RED naming the missing
      declaration, and revert. Use the compile-neutral form deliberately: RENAMING the impl target
      yields E0412 and renaming the struct additionally breaks Test 1's
      `fn(&ExecutionBackend) -> String` binding, `ENCODE_BACKEND: ExecutionBackend`, and the sibling
      guard's own `src.find("impl ExecutionBackend {").expect(...)`. A build error means the probe
      tested Test 1, not Test 6 — the executor must observe Test 6's OWN assertion failing.
      Without this probe the test could pass on a file it never really
      inspected — the same vacuity class Test 4 exists to prevent one level over.
    - `rtk proxy grep -c 'include_str!("../../../../contracts/aprender/binding.yaml")' crates/aprender-core/src/setfit/classify.rs`
      returns exactly 1.
    - `make setfit-classify-tests` exits 0 with its `assert_tests_ran` floor of 45 satisfied.
    - No file outside `crates/aprender-core/src/setfit/classify.rs` is modified by this task:
      `rtk proxy git diff --name-only` lists that file and nothing else (the three probes above are
      reverted before this is checked).
  </acceptance_criteria>

  <done>A guard exists that rustc witnesses for the symbol and `include_str!` witnesses for the registry row, its must-match, must-not-match and non-vacuity arms were each observed RED on an induced mutation, and it runs inside the existing Make gate.</done>
</task>

<task type="auto">
  <name>Task 2: correct the binding row and earn the status flip, verified through `pv` and re-measured end to end</name>

  <files>contracts/aprender/binding.yaml</files>

  <read_first>
    - `contracts/aprender/binding.yaml` — the `backend_identity` row as it stands, in full,
      including the `notes:` prose that must be rewritten rather than appended to (it currently
      says the correction is out of scope, which this task makes false).
    - `crates/aprender-core/src/setfit/classify.rs` `mod backend` as modified by Task 1 — the two
      strings the row must match are pinned there; the row and the test must be edited to the SAME
      pair of values.
    - `CLAUDE.md` § "Contract Validation: DOGFOOD `pv`, NEVER bash" in full, including the `pv diff`
      two-filesystem-paths trap and the three sanctioned responses to a `pv validate` rejection.
    - `Makefile` lines 1460-1690 — `PHASE4_CONTRACTS`, `contract-audit-phase4` and the recorded
      BEFORE/AFTER induced-failure evidence for that gate, so the acceptance numbers are compared
      against the gate's own stated semantics rather than invented.
  </read_first>

  <action>
Edit one row and two header claims in `contracts/aprender/binding.yaml`, and nothing else in
`contracts/`.

THE ROW — the one whose `equation:` is `backend_identity` under `contract: setfit-apr-v1.yaml`.
Set `module_path` to `aprender::setfit::encoder`. Set `function` to
`ExecutionBackend::identity` — TYPE-QUALIFIED, because `identity` is an inherent method on
`ExecutionBackend` (`encoder.rs:235`) and a bare `identity` would be as unresolvable as the ghost
it replaces; the registry's own precedent for a type name in this column is `function:
SetFitArtifactDoc` at `:1390` and `function: ClassifyResponse` at `:1444`. Add a `signature` field
reading `fn identity(&self) -> String`, following the convention rows 1-2 demonstrate. Set `status`
to `implemented`.

THE TWO HEADER CLAIMS — measured at HEAD, the block above the setfit-apr-v1 rows states the very
thing this plan makes false, so leaving it would ship a stale attestation inside a plan about
registry honesty. `binding.yaml:1349-1350` reads *"FOURTEEN of fifteen are `implemented`;
`backend_identity` alone is still `pending`"*; update it to fifteen of fifteen. `:1366-1367` reads
*"out of 04-10's scope and the entry stays `pending` … `pv audit` reports it as BIND-004"*; update
it to record that plan 04-19 performed the semantic correction and that the audit now reports zero
BIND findings. Keep the surrounding paragraph's reasoning intact — it explains WHY 04-10 declined,
which remains true and is the reason the correction needed its own plan.

Rewrite the row's `notes:` block. Delete the sentences that are now false — that no
`aprender::setfit::classify::backend_identity` exists (still true, and worth keeping as the reason
for the correction), that flipping the status would record an uncheckable claim (no longer true),
and that correcting the column is out of 04-10's scope (discharged here). Keep and keep prominent
the D-12 / review-B6 substance: the value is produced BY the encode invocation that ran, and a
trueno capability-detection result must never reach the field. Add three recorded facts: the
shipped symbol is `ExecutionBackend::identity` at `crates/aprender-core/src/setfit/encoder.rs`; the
row is pinned by a test in `aprender::setfit::classify`'s `mod backend` that reads this file with
`include_str!` and that rustc witnesses for the symbol; and the measured limitation that
`pv verify-bindings` reports 0 of 124 functions verified in this monorepo layout — including
`softmax` and `gelu`, which exist — so its ghost list is not evidence about any single row. Put
that last fact here rather than in `deferred-items.md`: it is the registry documenting the blind
spot in its own audit tooling, and a wave-1 sibling plan owns `deferred-items.md`.

Do NOT touch `contracts/setfit-apr-v1.yaml`. It has exactly one commit in its entire history
(`488e307d5`, plan 04-01) and the phase's verification treats that immutability as evidence. The
equation is correct as written; only the registry pointed at the wrong symbol.

Run the `pv` gates in this order and capture each status with `cmd > log 2>&1; rc=$?`, never
through a pipe. First `pv validate contracts/setfit-apr-v1.yaml` to confirm the contract is
untouched and still schema-clean. Then `pv audit contracts/setfit-apr-v1.yaml --binding
contracts/aprender/binding.yaml` and read the four counters plus the absence of the BIND-004 line.
Then materialise the old registry with `git show HEAD:contracts/aprender/binding.yaml` into a real
file under the scratch directory and run `pv diff` against the working copy, giving it two
filesystem paths. If `pv diff` rejects a binding registry because it is not a contract kind,
RECORD that measured outcome verbatim and move on — do not work around it with a shell script, and
do not silently omit the attempt. Finally run `pv verify-bindings contracts/aprender/binding.yaml
--crate-name aprender` and record its measured before/after numbers even though it is blind here;
the point is that the number is reported honestly rather than the tool being quietly skipped.

Close with `make contract-audit-phase4`, which is the blocking tier3 gate and the number this
plan's must-have is stated in.
  </action>

  <verify>
    <automated>./target/release/pv audit contracts/setfit-apr-v1.yaml --binding contracts/aprender/binding.yaml > /tmp/04-19-audit.log 2>&1; rc=$?; echo "rc=$rc"; grep -E "Total equations|Bound equations|Implemented|Not implemented|BIND-" /tmp/04-19-audit.log</automated>
  </verify>

  <acceptance_criteria>
    - `pv audit contracts/setfit-apr-v1.yaml --binding contracts/aprender/binding.yaml` exits 0 and
      reports `Total equations: 15`, `Bound equations: 15`, `Implemented: 15`,
      `Not implemented: 0`, and the output contains ZERO lines matching `BIND-`. The HEAD baseline
      for comparison, measured during planning, is `Implemented: 14 / Not implemented: 1` with one
      `[WARN] BIND-004` line for this exact equation.
    - `make contract-audit-phase4` exits 0 and prints its
      `Phase 4 binding audit: 1 contract(s) audited, every equation is bound` line.
    - `pv validate contracts/setfit-apr-v1.yaml` exits 0 with `0 error(s), 0 warning(s)`.
    - `rtk proxy git diff --stat -- contracts/` lists `contracts/aprender/binding.yaml` and NOTHING
      else. `rtk proxy git diff --name-only -- contracts/setfit-apr-v1.yaml` is empty.
    - `rtk proxy git diff -- contracts/aprender/binding.yaml` touches exactly the ENUMERATED set
      and nothing else: (a) within the `backend_identity` block, the `module_path`, `function`,
      `signature`, `status` and `notes` lines; (b) the header claim at lines 1349-1350
      ("FOURTEEN of fifteen"); (c) the header claim at lines 1366-1367 ("out of 04-10's scope and
      the entry stays `pending` … BIND-004"). NO OTHER row's `equation`, `module_path`, `function`
      or `status` moves. Confirm by reading every hunk header and record the hunk count and the
      touched line ranges in the SUMMARY.
    - The header no longer contradicts the rows it introduces:
      `rtk proxy grep -c 'FOURTEEN of fifteen' contracts/aprender/binding.yaml` returns 0, and
      `rtk proxy grep -c 'the entry stays `pending`' contracts/aprender/binding.yaml` returns 0.
    - The ghost is gone repo-wide:
      `rtk proxy grep -c 'aprender::setfit::classify::backend_identity' contracts/aprender/binding.yaml`
      returns 0, and the same grep over `contracts/` returns 0. The bare form is also absent —
      `rtk proxy grep -c '^  function: identity$' contracts/aprender/binding.yaml` returns 0 —
      because an inherent method needs its type to resolve.
    - `rtk proxy grep -c 'function: ExecutionBackend::identity' contracts/aprender/binding.yaml`
      returns exactly 1.
    - Task 1's guard still passes AFTER the row edit —
      `cargo test -p aprender-core --features setfit --lib setfit::classify::backend` exits 0. This
      is the ordering proof: the check existed before the flip, and the flip satisfied it.
    - The `pv diff` attempt is recorded: the SUMMARY names the two materialised filesystem paths it
      was given and quotes its verbatim output, whether that output is a semver suggestion or a
      typed rejection of a non-contract kind.
    - The `pv verify-bindings` before/after numbers are recorded in the SUMMARY, with the explicit
      note that they are NOT this plan's evidence.
  </acceptance_criteria>

  <done>The registry names the shipped symbol, the audit reports 15/15 implemented with no BIND-004, `contracts/setfit-apr-v1.yaml` is byte-unchanged, and every `pv` invocation used is recorded with its measured status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract registry → auditor / verifier | `contracts/aprender/binding.yaml` is the artifact a human or a gate reads to decide whether a contracted equation is implemented. A row that names a nonexistent symbol while claiming `implemented` is a false attestation crossing this boundary. |
| execution → `ClassifyResponse.backend` | The identity string reaches every API, CLI and HTTP consumer as a claim about which kernel ran. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-74 | Repudiation | `contracts/aprender/binding.yaml` `backend_identity` row | mitigate | The live finding, one level up from code: an `implemented` status on a ghost symbol is an unfalsifiable claim, and `pv audit` cannot detect it because it reads the `status` field and does not resolve symbols (measured: the audit is rc=0 today with the ghost in place). Task 1 supplies the missing resolution check as a compile witness plus an `include_str!` pin BEFORE Task 2 flips the status. |
| T-04-75 | Spoofing | `ExecutionBackend` identity value | mitigate | Contract invariant: a capability-detection result (`Backend::AVX2`, `Backend::NEON`, `select_backend*`) must never appear in this field, because `trueno::Matrix::matmul` dispatches on SIZE and an AVX2 detection is fully consistent with scalar execution. Already enforced by `the_setfit_surface_names_no_capability_detection_symbol` and `execution_backend_has_no_public_constructor_or_setter`; this plan binds the row to the symbol those guards protect, so the registry and the guards now name the same thing. |
| T-04-76 | Tampering | the registry edit itself | mitigate | The edit is scoped to one row and proven so: `git diff --stat -- contracts/` must list only `binding.yaml`, and `contracts/setfit-apr-v1.yaml` must be byte-unchanged (it has one commit in its whole history and the phase's verification relies on that). `pv diff` is run on two materialised filesystem paths so a semver-relevant change cannot land unremarked. |
| T-04-77 | Information Disclosure | — | accept | No data path, no user input, no network. The plan touches a YAML registry and a test module. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable: no dependency is added. No `Cargo.toml` is in `files_modified`; if a task finds itself needing a new crate, stop and report. |
</threat_model>

<verification>
Run at the end of the plan, statuses captured with `cmd > log 2>&1; rc=$?` and never through a pipe:

1. `make contract-audit-phase4` — rc=0, 15/15 bound, zero `BIND-` lines in the `pv audit` output it
   drives.
2. `pv validate contracts/setfit-apr-v1.yaml` — rc=0, `0 error(s), 0 warning(s)`.
3. `cargo test -p aprender-core --features setfit --lib setfit::classify::` — rc=0, passed count
   at least 56 (measured HEAD baseline 50 via `make setfit-classify-tests`, floor 45; this plan
   adds six tests). Re-measure the baseline with that exact command before editing rather than
   inheriting the number.
4. `make setfit-classify-tests` — rc=0, floor 45 satisfied.
5. `cargo test -p aprender-core --features setfit --lib` — rc=0, passed count at least 14415.
   Measured at HEAD `81652bb50` during planning with exactly that command: **14423 passed, 0
   failed, 2 ignored** — unchanged from `0fb47958f` because the three post-verification commits do
   not touch `aprender-core`. Re-measure and record your own number; the floor is yours to set
   just below it.
6. `rtk proxy git diff --name-only` — exactly two files:
   `contracts/aprender/binding.yaml` and `crates/aprender-core/src/setfit/classify.rs`.
</verification>

<success_criteria>
- `backend_identity` names `aprender::setfit::encoder` / `ExecutionBackend::identity` — TYPE-
  QUALIFIED, because `identity` is an inherent method and a bare name would be as unresolvable as
  the ghost it replaces — with a recorded signature, and its status is `implemented`.
- The registry no longer contradicts itself: the header claims at `binding.yaml:1349-1350` and
  `:1366-1367` are updated in the same commit as the row, so nothing in the file still says
  `backend_identity` is pending or that correcting it is out of scope.
- The flip is earned: a guard that rustc witnesses for the symbol and `include_str!` witnesses for
  the row existed and was proven able to fail BEFORE the flip.
- `make contract-audit-phase4` reports 15/15 implemented with zero BIND-004.
- `contracts/setfit-apr-v1.yaml` is byte-unchanged.
- Every contract operation went through `pv`; no bash/yq/python re-implementation exists in the
  diff.
- The SUMMARY states plainly that this closes ONE clause each of SC4 and SC5; that OPS-04, OPS-06
  and SAFE-01 stay UNCHECKED because their "over a produced artifact" halves remain blocked by
  F-10; and that F-10, the per-crate mutation gate and the SAFE-02 "in CI" clause remain OPEN.
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-19-SUMMARY.md` when done.

The SUMMARY must record: the HEAD `pv audit` counters measured before editing and the counters
after; the FOUR induced-mutation RED messages from Task 1's acceptance criteria (renamed equation
key, renamed method, decoy row, renamed `impl ExecutionBackend`); the verbatim `pv diff` output with the two filesystem paths it was
given; the `pv verify-bindings` before/after numbers with the explicit note that they are not
evidence; and the explicit open-items statement.
</output>
