---
phase: 04-apr-artifact-and-production-parity
plan: 22
type: execute
wave: 12
gap_closure: true
depends_on: ["04-20", "04-21"]
files_modified:
  - Makefile
  - .planning/phases/04-apr-artifact-and-production-parity/deferred-items.md
autonomous: true
requirements: [SAFE-01, SAFE-02]
closes_findings: [F-07]

must_haves:
  truths:
    - "`bashrs` was actually RUN over the Makefile and over `scripts/`, and its per-leg results are recorded as measured numbers"
    - "No bashrs leg that did not run, or could not run, is reported as passing — a skipped check is recorded as an explicit NOT-RUN"
    - "`bashrs-lint-makefile` can FAIL — today it swallows bashrs's status with `|| echo` and exits 0 unconditionally"
    - "The exit-code semantics of BOTH `bashrs lint` and `bashrs make lint` are determined by control BEFORE any gate is written against them — they are different subcommands with different measured exit codes"
    - "No bashrs severity verdict is adopted as a gate threshold without first proving it corresponds to a real shell defect. Two error-severity findings are MEASURED false positives and the gate must not be satisfiable only by editing correct code around them"
    - "The repo-wide script backlog is triaged into `deferred-items.md` with an owner, not absorbed into this phase"
  artifacts:
    - path: "Makefile"
      provides: "bashrs-lint-makefile that reports its real status, plus one scoped bashrs gate that is green today and non-vacuous"
      contains: "bashrs"
    - path: ".planning/phases/04-apr-artifact-and-production-parity/deferred-items.md"
      provides: "D-04-22-A — the full measured inventory, the triage, and the named owner for the repo-wide backlog"
  key_links:
    - from: "Makefile bashrs-lint-makefile"
      to: "bashrs make lint"
      via: "a status read directly off the command, never through `|| echo` and never through a pipe"
      pattern: "rc=\\$\\$\\?"
    - from: "the new scoped gate"
      to: "scripts/apr_bin.sh and scripts/check_apr_bin_pinned.sh"
      via: "the guard scripts CLAUDE.md makes load-bearing for every `apr` invocation in this phase"
      pattern: "apr_bin"
  caveats:
    - "This plan does NOT fix the 51-of-59 script backlog. It MEASURES it, triages it and gives it an owner. Widening the phase to fix ~106 error-severity findings across 59 scripts is a repo-wide change with its own ticket."
    - "This plan does NOT close F-10. OPS-01, OPS-02, SC1, SC2, SC3 and the 'over a produced artifact' halves of SC4/SC5 remain OPEN — Phase 5 work per the ROADMAP's explicit blocking note."
    - "This plan does NOT close the per-crate cargo-mutants gate (04-11 must-have 4) or the SAFE-02 'in CI' clause. Both stay `human_verification` items. Do NOT push, do NOT open a PR, do NOT edit any file under `.github/`."
    - "SAFE-01 and SAFE-02 both stay UNCHECKED. This plan improves the honesty of the gate surface they are measured on; it does not deliver either requirement."
    - "TWO bashrs findings are MEASURED FALSE POSITIVES and MUST NOT be 'fixed' by editing the code they flag. (1) `scripts/check_apr_bin_pinned.sh:72` SC1078 'Did you forget to close this double-quoted string?' — the line is the standard `'\"'\"'` single-quote-escaping idiom inside the `ABS_APR` regex, and `bash -n scripts/check_apr_bin_pinned.sh` exits 0. Lines 70-71 of that same file say the regex 'has now been gotten wrong four times in this repo; if you change it, re-run the table rather than reading it' — editing it to appease a parser bug is the single most dangerous change available in this plan. (2) `Makefile:2327` SC2168 \"'local' is only valid in functions\" — the line is `dev-setup: ## Set up local dev environment with sibling repo overrides` and the flagged columns are the English words 'local d'. There is no shell `local` anywhere near it."
    - "Do NOT reword Makefile help text or shell comments to silence a linter. A blocking gate that trips on the word 'local', 'declare' or 'typeset' appearing in prose is a gate that will be disabled."
---

<objective>
Close **F-07**: CLAUDE.md mandates `bashrs` over shellcheck, and no bashrs check anywhere in Phase 4
has been reported as passing because the tool was absent from this host. It is now installed.

Purpose: the point of F-07 is not that lint findings exist — it is that **a check which does not run
must never be reported as passing**. The repo currently has three bashrs Make targets, none wired
into any tier, and one of them (`bashrs-lint-makefile`) swallows bashrs's exit status with
`|| echo` so it reports success unconditionally. That is the same class as the two defects
CLAUDE.md's Verification Discipline rule 1 records by ticket number: a gate that cannot fail.

Output: measured per-leg results for every bashrs surface, a `bashrs-lint-makefile` that can fail,
one scoped gate that is green today and proven non-vacuous, and a triaged backlog with an owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Branch: work on the CURRENT branch `gsd/phase-2-contract-gate`. Do NOT create, rename or switch
branches. Do NOT open a PR. Do NOT touch `.github/workflows/*.yml`.

**This plan runs AFTER 04-21** because 04-21 edits the `Makefile` (the `setfit-serve-tests` floor),
and the Makefile is the primary subject of the lint here. Linting the pre-edit Makefile would
produce an inventory that is stale the moment its sibling lands.

**The user's global preference is `justfile` over Makefile. This repo is Makefile-based today. Do
NOT convert it** — that is explicitly out of scope for this run and would be an enormous
uninstructed change.
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-apr-artifact-and-production-parity/04-VERIFICATION.md
@.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md

Measured at HEAD `81652bb50` during planning — re-confirm before editing, do not re-derive:

- **`bashrs` is now INSTALLED**, contradicting F-07's premise and D-04-11-A's closing note. Measured:
  `whence -a bashrs` returns exactly one path, `/Users/guy/.cargo/bin/bashrs`, and
  `bashrs --version` prints `bashrs 6.66.3` at rc=0. One path matters — CLAUDE.md rule 8 records two
  cases where a shadowed artifact made edits look effective while changing nothing.
- Subcommands confirmed present in 6.66.3: `lint`, `purify`, `make`, `gate`, `score`, `audit`,
  `classify`, `safety-check`, `format`, `coverage`, `comply`. There is a global `--strict` flag
  documented as "Enable strict mode (fail on warnings)", which implies the DEFAULT exit-code
  semantics are something other than fail-on-warning — **but that implication was not verified, and
  a measurement complicates the naive reading**: `bashrs lint scripts/apr_bin.sh` exits **1**. The
  semantics must be established by control before any gate depends on them.
- **`bashrs lint` over `scripts/*.sh`, run one file at a time: 51 of 59 scripts exit non-zero.**
  Severity tally across the whole corpus, ANSI stripped:
  error-severity — SC1078 (34), SC1100 (30), SC1007 (11), SC2296 (11), SC1035 (7), SC2122 (5),
  SC2188 (4), SC1028 (2), SC2104 (1), SC2105 (1) — **106 error-severity findings**;
  warning-severity — SC2086 (180), PERF002 (133), SC2047 (65), SC2154 (51), SC2164 (30),
  SC2161 (29), REL005 (26), SEC014 (24), SEC013 (24), SC2198 (21), SC2036 (20), and others;
  info-severity — SC1012 (216), SC2081 (133), SC2227 (129), PERF003 (104), SC2016 (94), and others.
  SEC013 is *"Insecure temporary file: hardcoded /tmp path is vulnerable to symlink attacks"* and
  SEC006 is *"Unsafe temp file - use mktemp for secure random names"* — the same finding class this
  phase closed in Rust as WR-02.
- **`bashrs make lint Makefile` exits 2.** 34 findings: MAKE012 (18, "Recursive make invocation"),
  MAKE010 (11, "missing error handling"), MAKE003 (2, "Unquoted variable in command"),
  MAKE018 (1), MAKE001 (1, "Non-deterministic `$(wildcard)`"), and **one error-severity SC2168**.
- **The three existing bashrs Make targets, none wired into tier1/tier2/tier3/tier4:**
  - `Makefile:1088` `lint-scripts` — loops `scripts/*.sh`, runs `bashrs lint "$$script" || exit 1`,
    and hard-fails with a non-zero exit when bashrs is absent. This one is HONEST but would be RED
    today (51 of 59 scripts).
  - `Makefile:1101` `bashrs-score` — loops `scripts/*.sh` running `bashrs score`, no status handling
    at all.
  - `Makefile:1109` `bashrs-lint-makefile` — the whole recipe is
    `@bashrs make lint Makefile || echo "⚠️  Makefile linting found issues"`. **`|| echo` swallows the
    status; the target exits 0 whatever bashrs says.** This is the F-07-class defect: a check that
    cannot fail, printing a warning nobody's gate reads.
- **Phase 4 changed exactly one file under `scripts/` + `Makefile`:** `git diff --name-only
  d66678e7a..HEAD -- scripts/ Makefile` returns `Makefile` alone. No script was added or modified by
  this phase. The scripts that are nonetheless load-bearing FOR this phase are the ones CLAUDE.md
  makes mandatory for every `apr` invocation: `scripts/apr_bin.sh` (sourced), its guard
  `scripts/check_apr_bin_pinned.sh`, and `scripts/check_sourced_libs_option_neutral.sh` which
  enforces that `apr_bin.sh` stays option-neutral.
- `scripts/*.sh` count: 59.
- Makefile conventions to follow, already used by every Phase 4 gate: statuses captured as
  `cmd > log 2>&1; rc=$$?` and never through a pipe; `assert_tests_ran` parses libtest's own
  passed-count with `awk` so a zero-match filter cannot report success; recipes carry inline
  rationale for anything excluded.
</context>

<tasks>

<task type="auto">
  <name>Task 1: establish bashrs's exit-code semantics by control, then run every leg and record the inventory</name>

  <files>.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md</files>

  <read_first>
    - `Makefile` lines 1085-1115 — the three existing bashrs targets in full, including
      `bashrs-lint-makefile`'s `|| echo`. This is the file being modified in Task 2 and a subject of
      the lint here.
    - `Makefile` lines 190-400 — `tier1`, `tier2`, `tier3`, `tier4` bodies, to confirm by reading
      (not by memory) that no bashrs target is wired into any of them.
    - `CLAUDE.md` § "Shell Scripts: Use bashrs (NOT shellcheck)" in full, including the
      sourced-library option-neutrality exception and the
      `check_sourced_libs_option_neutral.sh` enforcement.
    - `CLAUDE.md` § "Verification Discipline" rules 1 (never read `$?` through a pipe), 5 (a guard
      that does not scan the surface where the DECISION is made is theater), 7 (a guard regex ships
      a must-match / must-not-match case table) and 8 (a shadowed artifact is worse than a missing
      one).
    - `scripts/apr_bin.sh` lines 1-45 — the header documenting why it is option-neutral, so its
      bashrs findings are read against its stated design rather than "fixed" into a regression.
    - `.planning/.../deferred-items.md` § D-04-11-A (the CI-exclusion entry) — its closing note says
      bashrs is absent and that any new script would need linting elsewhere. That premise is now
      false and the successor entry must say so.
  </read_first>

  <action>
Prove the tool before trusting any number it prints. Record `whence -a bashrs` (or
`type -aP bashrs`) showing every path that claims the name, and `bashrs --version`. If more than one
path appears, STOP and report — CLAUDE.md rule 8 records two cases where a shadowed artifact made
edits look effective while changing nothing, and a lint inventory taken from the wrong binary is
worse than no inventory.

Establish the exit-code semantics by control rather than by reading the `--strict` help text, and
do it for BOTH subcommands the plan depends on — `bashrs lint` (shell scripts) and `bashrs make
lint` (Makefiles) are different code paths with different measured exit codes: `bashrs lint
scripts/apr_bin.sh` exits **1** while `bashrs make lint Makefile` exits **2**. A gate written for
one and applied to the other is unfounded.

Construct throwaway inputs under the scratch directory: a script with only info-severity findings,
a script with at least one error-severity finding, a Makefile with only warning-severity findings,
and a Makefile with an error-severity finding. Run the matching subcommand on each, capture `rc`
with `cmd > log 2>&1; rc=$?`, and record every code. Repeat with the global `--strict` flag. The
resulting table — at minimum {info-only, error-present} x {default, --strict} for `lint`, and the
same for `make lint` — is what Task 2's gate is written against. Delete the throwaway inputs
afterwards.

**Then establish the semantics' TRUSTWORTHINESS, which is the part that decides Task 2's design.**
For every error-severity finding on the four files Task 2 will gate, run an independent
shell-semantic control — `bash -n` for scripts, `make -n <target>` or a targeted read for the
Makefile — and record whether the finding corresponds to a real shell defect. Measured at HEAD,
two do not: `scripts/check_apr_bin_pinned.sh:72` SC1078 on the `ABS_APR` regex's single-quote
escaping (`bash -n` exits 0), and `Makefile:2327` SC2168 on the English words "local d" in a
`dev-setup:` help string. Confirm both yourself rather than inheriting them, and record any
further ones you find. This is the measurement that stops the plan from adopting a linter's
verdict as ground truth.

Run every leg and record each one's status and finding counts. At minimum: `bashrs make lint
Makefile`; `bashrs lint` over each of `scripts/*.sh` individually, so one script's failure does not
mask another's (the corpus-level `bashrs lint scripts/*.sh` form hides which file failed); and
`bashrs gate --strict .` plus `bashrs score` on the three guard scripts, both as RECORDED runs
rather than as gates. For each leg record: the exact command, the exit code read directly off the
command, and the finding counts broken down by severity. Strip ANSI before tallying — the output is
coloured and a naive grep over the raw bytes miscounts.

Record NOT-RUN explicitly wherever it applies. If a leg cannot run — an unsupported subcommand, a
crash, a timeout — the recorded value is `NOT RUN: <reason>`, never an omission and never a green
tick. F-07 says this in as many words and the phase's whole verification discipline turns on it.

Write the inventory into `.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md`
as a new section `## D-04-22-A`, appended at the end. Do not edit or delete the existing sections.
Note that a wave-1 sibling (04-21) appends `## D-04-21-A` to the same file; append below it and
resolve any conflict by keeping both.
  </action>

  <verify>
    <automated>bashrs make lint Makefile > /tmp/04-22-makefile.log 2>&1; rc=$?; echo "makefile_lint_rc=$rc"; sed 's/\x1b\[[0-9;]*m//g' /tmp/04-22-makefile.log | grep -oE "\[(error|warning|info)\] [A-Z]+[0-9]+" | sort | uniq -c | sort -rn</automated>
  </verify>

  <acceptance_criteria>
    - `whence -a bashrs` (or `type -aP bashrs`) output is recorded and shows exactly ONE path. If it
      shows more, the task STOPPED and reported rather than proceeding.
    - `bashrs --version` is recorded verbatim. The planner measured `bashrs 6.66.3`; if the executor
      measures a different version, the executor's measurement wins and every count below is
      re-derived rather than inherited.
    - The **eight-cell** exit-code control table exists in the SUMMARY: {info-only, error-present}
      × {default, `--strict`} for `bashrs lint`, AND the same four for `bashrs make lint`. Eight
      cells, each holding a measured `rc`, every one a number and not a prediction. Four cells
      would cover only the script subcommand and would let Task 2's gate — which is written
      against `bashrs make lint`, a different code path measured exiting 2 rather than 1 — depend
      on semantics nobody established.
    - Every one of the 59 scripts has a recorded per-file exit code. The count of non-zero files is
      recorded. The planner measured 51; a different number is fine and is the executor's to report.
    - The Makefile leg's exit code and per-code tally are recorded. The planner measured rc=2 with
      MAKE012 (18), MAKE010 (11), MAKE003 (2), MAKE018 (1), MAKE001 (1) and one SC2168 error — note
      this was measured BEFORE 04-21's floor edit, so a small delta is expected and is itself worth
      recording.
    - Statuses were read directly off each command, never through a pipe. Verify by inspecting the
      commands recorded in the SUMMARY: any `cmd | ...; echo $?` form is a defect and must be
      re-run. This is the exact shape of the two defects CLAUDE.md rule 1 records.
    - `deferred-items.md` gains a `## D-04-22-A` section; `rtk proxy git diff --
      .planning/phases/04-apr-artifact-and-production-parity/deferred-items.md` shows additions
      only, no deletions, and both `## D-04-21-A` and `## D-04-22-A` are present.
    - No `scripts/*.sh` file is modified by this task: `rtk proxy git diff --name-only -- scripts/`
      is empty.
  </acceptance_criteria>

  <done>bashrs is proven unshadowed; the exit-code semantics of BOTH `bashrs lint` and `bashrs make lint` are a measured eight-cell table rather than an assumption; every error-severity finding on the four files Task 2 will gate has been checked against an independent shell-semantic control and classified as real or false-positive; every leg has a recorded status with a severity breakdown; and anything that could not run is recorded as an explicit NOT-RUN.</done>
</task>

<task type="auto">
  <name>Task 2: make `bashrs-lint-makefile` capable of failing, and add one scoped gate that is green today and proven non-vacuous</name>

  <files>Makefile</files>

  <read_first>
    - Task 1's measured exit-code control table and per-leg inventory — the gate written here must
      be written against the MEASURED semantics, not against the help text.
    - `Makefile` lines 1085-1115 — the three targets being changed or left alone.
    - `Makefile` lines 1640-1690 — `contract-audit-phase4`, the phase's model for a gate that
      refuses to report success when it audited nothing (`if [ "$$audited" -eq 0 ]` → FAIL). The new
      gate copies that shape.
    - `Makefile` lines 2160-2180 — `setfit-serve-tests` as modified by 04-21, and the
      `assert_tests_ran` idiom, so the new gate follows the same non-vacuity discipline.
    - `Makefile` lines 285-400 — the `tier3` body, the wiring target for anything blocking.
    - The findings list from Task 1 for the Makefile specifically. Confirm SC2168's ACTUAL reported
      location before triaging it — it must be read, not remembered, because what it lands on is
      the whole point: measured at HEAD it flags cols 22-28 (`local d`) of `Makefile:2327`,
      `dev-setup: ## Set up local dev environment with sibling repo overrides`, which is English
      prose in a help string and not a shell `local` at all. Triage it as a false positive; do NOT
      edit the line.
  </read_first>

  <action>
Fix the honesty defect first. Rewrite `bashrs-lint-makefile` at `Makefile:1109` so it reads bashrs's
status directly — `bashrs make lint Makefile > target/bashrs-makefile.log 2>&1; rc=$$?` — echoes the
log, and then decides. Delete the `|| echo "⚠️  Makefile linting found issues"` form entirely: it is
the reason the target has never been able to fail. Have the target exit non-zero when bashrs reports
error-severity findings, using the threshold the Task 1 control table established. Add an inline
comment recording what it used to do and why that was a defect, in the same style the Phase 4 gates
use for their exclusions — the next reader should not have to re-derive that `|| echo` swallows a
status.

**Fix NO source to satisfy a linter.** Task 1 measured exactly one error-severity Makefile finding
— SC2168 at `Makefile:2327` — and measured it to be a FALSE POSITIVE: the line is
`dev-setup: ## Set up local dev environment with sibling repo overrides` and the flagged columns
are the English words "local d" in a help string. Do NOT reword it. A gate satisfied by deleting
the word "local" from prose would trip on the next help string containing "local", "declare" or
"typeset", and rewording correct code to appease a parser bug is how a gate becomes a liability.
Triage it into `D-04-22-A` as a bashrs defect instead. The same ruling applies to
`scripts/check_apr_bin_pinned.sh:72` SC1078: that file's own lines 70-71 say the regex "has now
been gotten wrong four times in this repo; if you change it, re-run the table rather than reading
it", and `bash -n` on it exits 0.

Do NOT attempt the 18 MAKE012 "recursive make" findings — an architectural observation about the
whole Makefile, not a defect this plan can close. Do NOT attempt the 11 MAKE010 findings wholesale;
if any fall inside the Phase 4 setfit gate block, fix those and record which, noting that the Phase
4 gates already read their statuses correctly and MAKE010 is largely flagging the advisory
`|| echo "... not found"` pattern used elsewhere in the file.

Add ONE new gate that is green today, is worth wiring, and is founded on shell semantics rather
than on a linter's severity verdict. Scope it to the surface this phase's verification depends on:
the `Makefile` plus the three guard scripts CLAUDE.md makes load-bearing for every `apr`
invocation — `scripts/apr_bin.sh`, `scripts/check_apr_bin_pinned.sh`,
`scripts/check_sourced_libs_option_neutral.sh`.

Build it as a **baseline-non-increase gate with justified entries**, not a zero-errors gate. A
zero-errors gate over those four files is unsatisfiable today without editing correct code —
measured per file at HEAD: `apr_bin.sh` rc=1 / 0 errors, `check_apr_bin_pinned.sh` **rc=2 /
1 error**, `check_sourced_libs_option_neutral.sh` rc=1 / 0 errors, `Makefile` rc=2 / 1 error. So:
record a committed baseline of the error-severity count PER FILE; have the gate re-measure and FAIL
when any file's count EXCEEDS its baseline; and require every baselined entry to carry a written
justification naming its independent shell-semantic control (`bash -n` rc, or the read that showed
the flag lands on prose). A baseline entry without a justification is a defect the gate must
reject, so the baseline cannot decay into a silent suppression list. Site the baseline where the
gate reads it, and state in the recipe that a finding fixed upstream by bashrs should be REMOVED
from the baseline rather than left as permanent slack.

Copy `contract-audit-phase4`'s non-vacuity shape in spirit: count the files actually examined and
FAIL with an explicit message if that count is zero, so a glob that stops matching cannot report
success. Carry inline comments giving the scope rationale — 51 of 59 scripts are red today, wiring
the whole corpus would make the gate permanently red, and a permanently red gate stops being run
(the Ph1 D-26 failure mode this phase has cited twice) — and giving the baseline rationale, so the
next reader knows the gate detects REGRESSION rather than asserting cleanliness.

Ship the must-match / must-not-match case table for THIS gate, per CLAUDE.md rule 7, as executed
probes rather than as a comment. Must-match (the gate FAILS): a genuinely broken script added to
the scoped set; an unterminated quote introduced into one of the three guards. Must-not-match (the
gate PASSES): the four files as they stand today; a Makefile help string containing the word
"local"; the existing `ABS_APR` single-quote idiom. Run every row and record the results — the
`apr`-invocation patterns in this repo were wrong five times and every one was caught by such a
table, none by review.

Wire ONLY the new scoped gate and the fixed `bashrs-lint-makefile` into `tier3`, beside the existing
Phase 4 gates. Do NOT wire `lint-scripts` — it is honest but red, and wiring a red gate into a
blocking tier converts it into a thing developers disable. Record that decision inline as an
exclusion rationale, the way 04-10's targets record theirs.

**State the tool-absent path explicitly, and make it fail.** Both new/changed targets are wired
into blocking tier3, so what they do when `bashrs` is missing or unrunnable is part of their
contract. The repo's ambient idiom is the `|| echo "... not found"` swallowing form this plan is
deleting, and `pmat-score` at `Makefile:1125` still uses it — so the default a future editor copies
is the wrong one. Both targets must, when `bashrs` is absent or its invocation fails to produce a
parseable report, exit NON-ZERO with a message naming the reason and naming the install command.
`lint-scripts` at `Makefile:1090-1099` already does exactly this and is the in-file precedent to
copy. F-07's rule is literal: a check that did not run is never reported as passing.

**Close the `setfit-cli-eval-tests` bookkeeping, which only this plan can do correctly.** This is
not bashrs work; it is here because wave 12 is the first point at which the number is stable.
Measured at HEAD: the gate-table row `Makefile:1918` reads
`#   setfit-cli-eval-tests      cli   eval::setfit            15 / 0        13` while the actual
count is 16, and the `assert_tests_ran` floor at `Makefile:2148` is 13 — seven below what sibling
plan 04-20 leaves behind. 04-21 could not fix it (04-20 is its same-wave sibling, so any value 04-21
wrote would be stale by four on contact) and 04-20 does not own the Makefile. So: re-measure
`cargo test -p apr-cli --features setfit --lib eval::setfit` AFTER 04-20 has landed, write the
measured count into the `:1918` row, and raise the floor at `:2148` to just below it — expected 19
against a measured 20, but the measurement decides, not this plan. `Makefile:1927-1929` is the
standing instruction being honoured: *"Re-measure and RAISE the floors whenever a phase adds tests
here"*, followed by the record of what happened when nobody did.

Leave `bashrs-score` alone. It is advisory, it handles no status, and changing it is not in this
plan's scope; note it in the deferred entry instead.
  </action>

  <verify>
    <automated>make bashrs-lint-makefile > /tmp/04-22-t2a.log 2>&1; rc=$?; echo "bashrs_lint_makefile_rc=$rc"; tail -5 /tmp/04-22-t2a.log</automated>
  </verify>

  <acceptance_criteria>
    - `rtk proxy grep -c '|| echo "⚠️  Makefile linting found issues"' Makefile` returns 0. The
      swallowing form is gone, not merely supplemented.
    - `bashrs-lint-makefile` CAN fail, proven by induction: temporarily introduce an error-severity
      shell defect into a scratch Makefile recipe, run the target, confirm it exits NON-ZERO naming
      the finding, and revert. Record the exact output. A target that has only ever been seen
      passing is not evidence — this is the whole point of the plan and it must be demonstrated,
      not asserted.
    - `make bashrs-lint-makefile` exits 0 against the Makefile's recorded baseline. **SC2168 is
      still present in its log and that is CORRECT** — it is a measured false positive on the
      English words "local d" at `Makefile:2327`, it is carried as a justified baseline entry, and
      `rtk proxy git diff -- Makefile` must show NO change to line 2327. A run that reports zero
      SC2168 would mean the help text was reworded, which this plan forbids.
    - No source was edited to satisfy a linter. Assert directly:
      `rtk proxy git diff -- scripts/` is empty, and the `Makefile` diff does not touch line 2327
      or any `##` help string. Record the diffed line numbers in the SUMMARY.
    - The new scoped gate exists, **exits 0 today**, and names the four files it examined in its own
      output. This is only achievable because it is a baseline-non-increase gate: a zero-errors gate
      cannot pass, since `check_apr_bin_pinned.sh` carries a measured error-severity finding that
      must not be "fixed".
    - Every baseline entry carries a justification naming its independent control. Assert
      mechanically: the count of baseline entries equals the count of justification strings, and
      the gate FAILS when an entry is added without one. Prove the latter by adding an
      unjustified entry, observing the failure, and reverting. Record the message.
    - The gate detects REGRESSION, proven by induction on a REAL defect (not a false-positive
      class): introduce an unterminated quote into one of the three guard scripts, confirm the gate
      exits NON-ZERO naming that file and reporting a count above its baseline, and revert. This is
      the probe that proves the gate can fail and it must not be skipped.
    - The must-match / must-not-match case table was RUN, not written. Record each row's observed
      outcome: the two must-match rows FAIL the gate; the three must-not-match rows PASS it —
      specifically including a Makefile help string containing the word "local" and the existing
      `ABS_APR` single-quote idiom, the two forms whose false positives shaped this design.
    - Its non-vacuity guard is proven: temporarily point its file list at a glob that matches
      nothing, confirm it FAILS with the explicit "examined nothing" message rather than reporting
      success, and revert. Record the message.
    - The `setfit-cli-eval-tests` bookkeeping is closed against a POST-04-20 measurement, not an
      inherited number: `make setfit-cli-eval-tests` exits 0; the floor at `Makefile:2148` is
      strictly greater than 13 and strictly less than the measured passed count; and the `:1918`
      row's count matches that measurement. Record the command, the count and both old/new floor
      values. Prove the raised floor is non-vacuous by filtering the suite to a zero-match name and
      confirming `assert_tests_ran` FAILS; revert.
    - Both targets are wired into `tier3`: `rtk proxy grep -n 'tier3:' -A 40 Makefile` shows the two
      target names. `lint-scripts` is NOT wired, and the exclusion rationale is present as an inline
      comment naming the measured 51-of-59 red count.
    - `make tier3` is not made worse. Because `tier3` is long, it is acceptable to verify by running
      the two newly wired targets directly plus a `make -n tier3` dry run confirming they appear in
      the recipe; record which form was used and its status. If a full `make tier3` is run, record
      its rc and note the known standing reds (D-04-04-B's 24 `aprender-train` names,
      D-04-08-A's 51 `aprender-serve` failures) so a red is not misattributed to this plan.
    - `rtk proxy git diff --name-only` lists exactly `Makefile` and the phase's `deferred-items.md`.
      No file under `scripts/` and no file under `.github/` is modified.
  </acceptance_criteria>

  <done>The Makefile lint target reads its own status and was shown to fail on an induced defect; the one error-severity Makefile finding (SC2168) is triaged as a MEASURED FALSE POSITIVE and carried as a justified baseline entry, with `Makefile:2327` byte-unchanged; and a scoped baseline-non-increase gate covering the Makefile plus the three `apr`-pinning guard scripts is wired into tier3 with its non-vacuity guard, its real-defect regression probe and its must-match/must-not-match table all demonstrated.</done>
</task>

<task type="auto">
  <name>Task 3: triage the repo-wide backlog into `deferred-items.md` with an owner, and state plainly what remains NOT RUN</name>

  <files>.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md</files>

  <read_first>
    - Task 1's full inventory as written into `## D-04-22-A` — the raw material for the triage.
    - `.planning/.../deferred-items.md` in full, especially `## D-04-11-A` (the CI-exclusion entry
      whose "bashrs is NOT installed" premise is now false) and `## D-04-13-A` (the cleanup-pass
      entry, as the house style for a prioritised deferral list with a stated reason per item).
    - `.planning/STATE.md` § "Pending Todos" — the repo-wide entries already recorded there
      (D-ITEM-01's vacuous macOS guards, the `check_apr_bin_pinned.sh` docs-scope note), which are
      the same class as this backlog and show how the project words an owner-less repo-wide finding.
    - `04-VERIFICATION.md` frontmatter `human_verification:` block — the last entry is the bashrs
      one, and this task's output is what a human reading it next will act on.
  </read_first>

  <action>
Extend `## D-04-22-A` in `deferred-items.md` from an inventory into a triage. Group the findings by
what a fixer would actually do, not by lint code: the security class first (SEC013 hardcoded `/tmp`
symlink exposure, SEC006 unsafe temp names, SEC014) because it is the same defect class this phase
closed in Rust as WR-02 and it is the group most likely to matter; then the correctness class
(SC1078, SC1100, SC1007, SC2296 and the other error-severity codes, with their counts); then the
quoting/word-splitting class (SC2086 at 180 occurrences); then performance and style. For each
group state the count, name two or three representative files, and state whether a mechanical fix is
plausible or whether it needs per-site judgement.

Name an owner and a shape for the work rather than leaving it unassigned. State explicitly that it
is a repo-wide shell-portability and safety pass with its own ticket, that it is NOT Phase 4 scope
and NOT Phase 5 scope, and that it must ship its own must-match / must-not-match case table per
CLAUDE.md rule 7 because a mechanical `SC2086` fix over 180 sites is exactly the kind of change that
silently alters behaviour. Cross-reference D-ITEM-01 from Phase 2 (the vacuous macOS `grep -oP`
guards) as an adjacent finding in the same file family, so whoever picks this up finds both.

**Record the two measured bashrs FALSE POSITIVES as tool defects, in their own sub-heading.** They
are the most reusable output of this plan: each is a case where adopting the linter's severity
verdict would have caused a real regression. For each, record the file and line, the rule id, the
exact flagged text, the independent control that refuted it, and the consequence that was avoided.
(1) `scripts/check_apr_bin_pinned.sh:72` SC1078 — the `ABS_APR` regex's standard `'"'"'`
single-quote escaping; `bash -n` exits 0; the avoided consequence is editing a regex whose own
lines 70-71 record that it has been gotten wrong four times and must be re-validated against a
12-case table rather than read. (2) `Makefile:2327` SC2168 — the English words "local d" inside
`dev-setup: ## Set up local dev environment with sibling repo overrides`; the avoided consequence is
a blocking tier3 gate that trips on any future help string containing "local", "declare" or
"typeset". State that both are carried as justified baseline entries rather than suppressed, and
that they should be removed from the baseline if a later bashrs release fixes them. Consider whether
they are worth reporting upstream and record the decision either way.

Correct the stale premise in the record. `## D-04-11-A`'s closing note says bashrs is not installed
and that any new script would need linting elsewhere. Do not edit that section — add a dated line in
`## D-04-22-A` stating that the premise is now false, that bashrs 6.66.3 is installed at a single
path, and therefore that the clean fix D-04-11-A proposed for `setfit-api-boundary` (extracting the
`cargo tree` loop into `scripts/setfit_api_boundary.sh` so a CI step and the Make target invoke one
script) is now unblocked on the tooling side. Note in the same breath that it is NOT unblocked on
the process side: it needs a `.github/workflows/ci.yml` edit, which CLAUDE.md places outside
autonomous scope, so it stays a human decision.

State the NOT-RUN set as its own short list, unambiguously. At minimum it contains: the 51-of-59
script corpus (measured, triaged, NOT fixed); anything Task 1 recorded as NOT RUN; the per-crate
cargo-mutants gate (04-11 must-have 4, ≥10 h projected per D-04-11-B); and the SAFE-02 "in CI"
clause (16 legs applied at `.github/workflows/ci.yml:378-397`, never executed). Write it so a
verifier reading only this section cannot mistake any of them for closed.
  </action>

  <verify>
    <automated>DI=.planning/phases/04-apr-artifact-and-production-parity/deferred-items.md; grep -c "NOT RUN" "$DI"; grep -c "^## D-04-22-A" "$DI"</automated>
  </verify>

  <acceptance_criteria>
    - `## D-04-22-A` contains a triage grouped by fix-shape, and every group carries a COUNT taken
      from Task 1's measurement rather than a qualitative word like "many".
    - The security group is present and names SEC013, SEC006 and SEC014 with their counts, and
      states the connection to WR-02 (the same temp-file defect class this phase closed in Rust).
    - The false-positive sub-heading exists and documents BOTH measured cases with all five fields
      (file:line, rule id, flagged text, refuting control, avoided consequence). Verify the
      controls were actually run by checking that the recorded `bash -n` exit code is present as a
      number. A false positive asserted without its control is the same defect one level up.
    - An owner and a shape are named for the repo-wide pass, and the entry states explicitly that it
      is neither Phase 4 nor Phase 5 scope and that it must ship a must-match / must-not-match case
      table per CLAUDE.md rule 7.
    - The stale premise is corrected additively: `## D-04-11-A` is byte-unchanged
      (`rtk proxy git diff` shows no deletions in the file), and `## D-04-22-A` carries the dated
      line stating bashrs is now installed and that `setfit_api_boundary.sh` extraction is unblocked
      on tooling but still blocked on the `.github/` process rule.
    - A `NOT RUN` list exists and contains at least these four items: the 51-of-59 script corpus;
      any Task 1 NOT-RUN leg; the per-crate cargo-mutants gate; and the SAFE-02 "in CI" clause.
      Verify with `grep -c "NOT RUN"` returning at least 4.
    - Nothing in the entry reports a skipped or unavailable check as passing. Re-read the whole
      section against that single question before finishing — it is F-07's literal requirement and
      the reason this plan exists.
    - `rtk proxy git diff --name-only` for the whole plan lists exactly `Makefile` and the phase's
      `deferred-items.md`.
  </acceptance_criteria>

  <done>The repo-wide backlog is triaged by fix-shape with measured counts and a named owner, the stale "bashrs is absent" premise is corrected additively, and the NOT-RUN set is stated so no verifier can mistake any of it for closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer / CI → the gate surface (`Makefile`, `scripts/`) | Every claim this phase makes about SAFE-01 and SAFE-02 is mediated by these files. A gate that cannot fail is an authenticity defect in the evidence itself, one level above the code. |
| shell script → filesystem via hardcoded `/tmp` paths | 24 measured SEC013 findings: a predictable temp path in a world-writable directory is a symlink-attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-87 | Repudiation | `Makefile:1109` `bashrs-lint-makefile` | mitigate | The live finding. `@bashrs make lint Makefile || echo "…"` exits 0 unconditionally, so the target has never been able to report the 34 findings bashrs actually produces — including one error-severity SC2168. This is the same defect class CLAUDE.md rule 1 records twice by ticket (`qwen-story-daily` capturing `tee`'s status; `make publish`'s unreachable POST-PUBLISH VERIFICATION). Task 2 reads the status directly and proves the target can fail by inducing a defect. |
| T-04-88 | Repudiation | reporting an unavailable check as passing | mitigate | F-07's literal requirement. Task 1 records NOT-RUN explicitly per leg; Task 3 collects the NOT-RUN set into one list a verifier cannot miss. The failure mode being prevented is the phase reporting a green bashrs line for a check that never executed. |
| T-04-89 | Tampering | hardcoded `/tmp` paths in `scripts/` (SEC013 ×24, SEC006, SEC014) | transfer | Real and measured, and the same class this phase closed in Rust as WR-02 (`.{name}.tmp` opened with `create(true)` following symlinks). It is NOT closed here: 59 scripts are a repo-wide surface, and fixing them inside a gap-closure plan would be an uninstructed widening. Transferred to the named owner in `## D-04-22-A` with counts and representative files, so it is actionable rather than merely noted. |
| T-04-90 | Denial of Service | wiring a permanently-red gate into a blocking tier | mitigate | 51 of 59 scripts are red today. Wiring `lint-scripts` into `tier3` would make the blocking tier permanently red, and a permanently red gate stops being run — the Ph1 D-26 failure mode this phase has cited twice. Task 2 wires only a scoped gate that is green today, with the exclusion rationale recorded inline. |
| T-04-91 | Tampering | a mechanical `SC2086` fix over 180 sites | accept, deferred | Adding quotes changes word-splitting behaviour and can silently alter what a script does. Explicitly deferred with the requirement that the fixer ship a must-match / must-not-match case table (CLAUDE.md rule 7), because the `apr`-invocation patterns were wrong five times and every one was caught by such a table, none by review. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | `bashrs` was installed OUTSIDE this plan and is verified rather than installed by it: `whence -a` must show exactly one path and `--version` is recorded. If bashrs is absent or shadowed at execution time, STOP and report — do not `cargo install` a linter as a side effect of a gap-closure plan, and do not record the leg as passing. |
</threat_model>

<verification>
Run at the end of the plan, statuses captured with `cmd > log 2>&1; rc=$?` and never through a pipe:

1. `make bashrs-lint-makefile` — rc=0 on the fixed Makefile, and the induced-defect run recorded as
   NON-ZERO.
2. The new scoped gate — rc=0, its examined-file count non-zero, and its zero-match probe recorded
   as FAILING.
3. `make -n tier3` — both new targets present in the recipe.
4. `make contract-audit-phase4` — rc=0. The Makefile was edited; this re-proves the phase's blocking
   contract gate still runs.
5. `make setfit-serve-tests` — rc=0 with 04-21's floor intact. The Makefile was edited by two plans;
   this re-proves the sibling's edit survived.
5b. `make setfit-cli-eval-tests` — rc=0 with the NEWLY RAISED floor satisfied, re-proving both that
   04-20's tests landed and that the floor now tracks them.
6. `bashrs make lint Makefile` — recorded rc and per-code tally, compared against the pre-edit
   numbers from Task 1.
</verification>

<success_criteria>
- bashrs ran, on a binary proven unshadowed, and every leg has a recorded status with a severity
  breakdown.
- `bashrs-lint-makefile` reads its own status and was demonstrated to fail on an induced defect.
- One scoped gate covering the Makefile plus the three `apr`-pinning guard scripts is green, wired
  into tier3, and proven non-vacuous by a zero-match probe.
- The one error-severity Makefile finding (SC2168) is RECORDED AS A BASHRS FALSE POSITIVE with its
  refuting control, NOT fixed: `Makefile:2327` is byte-unchanged and SC2168 still appears in the
  lint log, which is the correct outcome. MAKE012 and the bulk of MAKE010 are deferred with stated
  reasons rather than rewritten. No source anywhere was edited to satisfy a linter.
- The 51-of-59 script backlog is triaged by fix-shape with measured counts and a named owner.
- The `setfit-cli-eval-tests` row and floor are corrected against a measurement taken AFTER 04-20
  landed, closing the one piece of gate bookkeeping no wave-11 plan could do without it decaying.
- Nothing skipped or unavailable is reported as passing, and the NOT-RUN list makes that checkable.
- The SUMMARY states plainly that F-10, the per-crate mutation gate and the SAFE-02 "in CI" clause
  remain OPEN; that OPS-01 and OPS-02 stay NOT MET; and that SAFE-01 and SAFE-02 stay UNCHECKED.
</success_criteria>

<output>
Create `.planning/phases/04-apr-artifact-and-production-parity/04-22-SUMMARY.md` when done.

The SUMMARY must record: the `whence -a bashrs` output and version; the EIGHT-cell exit-code control
table (both subcommands x {info-only, error-present} x {default, --strict}); the shell-semantic
control result for every error-severity finding on the four gated files, with each classified real
or false-positive; the per-leg inventory with the non-zero script count and the Makefile tally; the verbatim
output of the induced-defect run that proved `bashrs-lint-makefile` can fail; the verbatim output of
the zero-match probe that proved the new gate non-vacuous; the NOT-RUN list; and the explicit
open-items statement.
</output>
