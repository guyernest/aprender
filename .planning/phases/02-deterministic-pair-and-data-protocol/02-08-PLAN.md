---
phase: 02-deterministic-pair-and-data-protocol
plan: 08
type: execute
wave: 6
depends_on: [02-07]
files_modified:
  - crates/aprender-contrastive-data/tests/negative_leaky.rs
  - crates/aprender-contrastive-data/tests/negative_materializing.rs
  - crates/aprender-contrastive-data/tests/ui.rs
  - crates/aprender-contrastive-data/tests/ui/
autonomous: true
requirements: [DATA-05, DATA-06]

must_haves:
  truths:
    - "A deliberately leaky sampler (pairs reaching for validation IDs) is REJECTED by the same endpoint gate that accepts the honest sampler, with the offending ID named — and the mirror test proves the gate is not rejecting everything"
    - "A deliberately materializing sampler (full Cartesian product) FAILS the capacity-invariant gate at a size where the honest sampler passes, measured structurally, never self-reported"
    - "A compatibility-profile selection run does not COMPILE — trybuild pins the missing Split<Validation> witness with committed .stderr snapshots"
    - "The 10×-examples-under-fixed-budget scaling property holds: honest retained state grows linearly in examples, never with the pair space"
    - "cargo-mutants scoped to the new crate runs and surviving mutants are killed or individually justified (D-26)"
  artifacts:
    - path: "crates/aprender-contrastive-data/tests/negative_leaky.rs"
      provides: "In-band leaky negative + mirror (D-25)"
      contains: "EndpointNotInSelection"
    - path: "crates/aprender-contrastive-data/tests/negative_materializing.rs"
      provides: "In-band materializing negative + capacity invariant + 10x scaling property"
      contains: "retained_state"
    - path: "crates/aprender-contrastive-data/tests/ui.rs"
      provides: "trybuild harness for DATA-06 non-constructibility"
      contains: "compile_fail"
  key_links:
    - from: "crates/aprender-contrastive-data/tests/negative_leaky.rs"
      to: "crates/aprender-contrastive-data/src/pairs.rs"
      via: "the SAME validate_endpoints call rejects the leaky variant and accepts the honest one"
      pattern: "validate_endpoints"
    - from: "crates/aprender-contrastive-data/tests/ui.rs"
      to: "crates/aprender-contrastive-data/tests/ui/"
      via: "trybuild::TestCases::compile_fail over ui/*.rs with committed .stderr"
      pattern: "compile_fail"
---

<objective>
Build the phase's honesty gates (D-25, Phase 1 D-24 discipline): the two in-band negative
variants that must fail in every `cargo test`, the trybuild compile-fail proofs for DATA-06's
"cannot be constructed" claims, the scaling property, and the scoped mutation run (D-26).

Purpose: "no leakage" and "bounded memory" are the phase's two fakeable claims — a passing test
suite can assert both without them being true. A gate that has only ever been observed passing is
not evidence. These negatives are the phase's highest-value artifact (CONTEXT specifics).
Output: negative tests RED-under-gate/GREEN-mirror, committed .stderr snapshots, mutation report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-band negatives — leaky sampler and materializing sampler, each with its mirror</name>
  <files>crates/aprender-contrastive-data/tests/negative_leaky.rs, crates/aprender-contrastive-data/tests/negative_materializing.rs</files>
  <read_first>
    - crates/aprender-core/tests/setfit_conformance/detach_negative.rs IN FULL (the exact three-element discipline: header doc explaining WHY the negative exists; failure for the RIGHT reason with offenders NAMED; the mirror test proving the same call accepts the honest implementation)
    - crates/aprender-contrastive-data/src/pairs.rs (validate_endpoints, retained_state_size, PairSampler API from 02-07)
    - contracts/contrastive-pair-protocol-v1.yaml (OBLIG-CPP-LEAKY-RED, OBLIG-CPP-CAPACITY-INVARIANT — the obligations these tests discharge)
  </read_first>
  <action>
    negative_leaky.rs (obligation OBLIG-CPP-LEAKY-RED): build an honest Selection from a synthetic
    canonical corpus, then construct a test-only LEAKY pair list — the honest sampler's output
    with one pair's endpoint replaced by a VALIDATION row's ID. Assert:
    (a) `validate_endpoints(&leaky, &selection)` returns Err(EndpointNotInSelection) and the error
    message NAMES the offending ID (that is what makes a real failure diagnosable rather than
    merely red);
    (b) before that, prove the leak is the ONLY defect: the same list minus the poisoned pair
    passes (without this, the rejection assertion would also pass against a gate that rejects
    everything malformed for unrelated reasons);
    (c) the MIRROR: the identical `validate_endpoints` call over the honest sampler's untouched
    output returns Ok. Header doc: copy the detach_negative.rs voice — state that a leakage gate
    observed only passing is not evidence, and that this file is the D-25 discipline applied to
    PF-002.

    negative_materializing.rs (obligation OBLIG-CPP-CAPACITY-INVARIANT): implement a test-only
    MaterializingSampler that enumerates the full Cartesian pair set (the reference
    implementation's own trap, Finding F2) exposing the same structural state measurement. Define
    the gate as a shared helper in the test: `retained_state ≤ C_LINEAR * (examples + budget) +
    C_CONST` with named constants derived from the honest sampler's structure (state the
    derivation in comments — bucket entries + prefix sums + cursor). Assert:
    (a) at a layout where pair space >> examples (e.g. 3 classes × 64 → 24,576-pair space), the
    materializer's structural state VIOLATES the bound (RED under the gate);
    (b) the MIRROR: the honest PairSampler at the same layout satisfies it;
    (c) the 10× scaling property: honest retained_state_size at N and 10N under the SAME budget
    grows by ≤ ~10× + constant (linear in examples), while the materializer's grows
    quadratically. Measurement is STRUCTURAL via the #[cfg(test)] introspection accessors — a
    self-reported size is exactly as trustworthy as a self-reported decreasing loss (D-25).
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test negative_leaky && cargo test -p aprender-contrastive-data --test negative_materializing</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass in a plain `cargo test -p aprender-contrastive-data` (in-band — no feature gate, no #[ignore])
    - negative_leaky.rs asserts the error message contains the poisoned ID string
    - Both files contain a mirror test whose name contains "mirror" or states the honest implementation passes the SAME gate call
    - negative_materializing.rs computes the bound from named constants and structural accessors — grep shows no test asserting against a sampler-reported "memory used" string
    - The header doc of each file explains why the negative exists (non-empty //! block referencing the obligation ID)
  </acceptance_criteria>
  <done>Both fakeable claims now have an in-band wrong implementation that the real gates demonstrably catch, plus mirrors proving the gates accept honesty.</done>
</task>

<task type="auto">
  <name>Task 2: trybuild compile-fail proofs — leakage is not constructible</name>
  <files>crates/aprender-contrastive-data/tests/ui.rs, crates/aprender-contrastive-data/tests/ui/</files>
  <read_first>
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md "No Analog Found" trybuild row (no TestCases harness exists in-repo; use the standard upstream shape)
    - crates/aprender-contrastive-data/src/{split.rs,select.rs,pairs.rs} public APIs (what must NOT compile)
    - contracts/contrastive-pair-protocol-v1.yaml OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE
  </read_first>
  <action>
    Create tests/ui.rs with the standard harness: `let t = trybuild::TestCases::new();
    t.compile_fail("tests/ui/*.rs");`. Author compile-fail cases (each a minimal program using the
    crate's public API), with COMMITTED .stderr snapshots pinning the diagnostic:
    1. `select_on_test_split.rs` — calling FewShotSelector::select with a Split<Test> where
       Split<Train> is demanded: type error.
    2. `select_on_compat_split.rs` — same with Split<CompatibilityTest>.
    3. `compat_profile_no_validation_witness.rs` — a compatibility-profile flow attempting to
       produce the Split<Validation> witness selection requires: no API exists; the program
       cannot typecheck (this is the D-19 core: not rejected — unrepresentable).
    4. `pairs_from_raw_ids.rs` — constructing a PairSampler from anything other than a Selection
       (e.g. a Vec<String> of IDs): no such constructor exists.
    Each case file carries a comment naming the obligation and the requirement (DATA-06). Run
    once with TRYBUILD=overwrite to generate the .stderr snapshots, review them for the expected
    diagnostic (the error must be about the missing type/API, not an unrelated syntax error),
    then commit the snapshots.
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test ui</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-contrastive-data --test ui` passes with all four cases
    - tests/ui/ contains four .rs cases and four committed .stderr snapshots
    - Each .stderr references the crate's actual types (Split, Selection, or the missing method/constructor) — not a syntax error
    - Each case file's comment names OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE and DATA-06
  </acceptance_criteria>
  <done>DATA-06's "cannot be constructed" claims are pinned by compiler-verified, snapshot-reviewed evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped mutation run + phase gate sweep</name>
  <files>crates/aprender-contrastive-data/tests/negative_leaky.rs, crates/aprender-contrastive-data/tests/negative_materializing.rs</files>
  <read_first>
    - CLAUDE.md mutation invocation (`cargo mutants --no-times --timeout 300 --in-place`)
    - The 02-02..02-07 SUMMARYs (what the crate now contains — for triaging survivors)
  </read_first>
  <action>
    1. Run `cargo mutants -p aprender-contrastive-data --no-times --timeout 300 --in-place`
       (D-26 scope). Capture the outcome file; for each SURVIVING mutant either (a) add/extend a
       test in the most appropriate existing test module to kill it, or (b) record it in the
       SUMMARY with a one-line justification (e.g. equivalent mutant). Do not weaken any gate to
       improve the score. Target: caught rate consistent with the repo's ≥85% mutation threshold
       for the new crate.
    2. Phase gate sweep, capture-then-check each status (never $? through a pipe):
       `make tier2` (includes the new crate line), `make contrastive-data-boundary`, the
       tier3 contract-validation target over the full $(CONTRACTS) list (both phase contracts),
       and the CPU feature-matrix sanity: `cargo check -p aprender-contrastive-data`,
       `cargo check -p apr-cli`, `cargo check --workspace`.
    3. Record measured runtimes for the new tier2 line in the SUMMARY (Phase 1 measured-comment
       discipline) and reconcile the Makefile comment from 02-02 Task 3 if the measurement moved.
  </action>
  <verify>
    <automated>make tier2 && make contrastive-data-boundary</automated>
  </verify>
  <acceptance_criteria>
    - cargo-mutants completes; the SUMMARY lists total/caught/survived with per-survivor dispositions
    - `make tier2` exits 0; `make contrastive-data-boundary` exits 0
    - The tier3 contract-validation target exits 0 over a $(CONTRACTS) list containing both phase contracts
    - `cargo check --workspace` exits 0
  </acceptance_criteria>
  <done>The crate's test suite is mutation-hardened and every phase gate runs green inside its tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → claims | The gates themselves are the product; a vacuous gate is worse than none |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Repudiation | vacuous leakage gate | mitigate | In-band leaky negative RED + mirror GREEN under the SAME call (Task 1) |
| T-02-26 | Denial of Service | unbounded-memory sampler passing tests | mitigate | Structural capacity invariant + materializing negative RED + 10× scaling property (Task 1) |
| T-02-27 | Elevation (data) | leakage expressible through the public API | mitigate | Four trybuild compile-fail proofs with committed .stderr (Task 2) |
| T-02-28 | Tampering | tests that pass by construction (weak assertions) | mitigate | Scoped cargo-mutants with per-survivor disposition (Task 3) |
</threat_model>

<verification>
- Plain `cargo test -p aprender-contrastive-data` runs both negatives and the trybuild harness — no opt-in flags
- All phase gates green inside their tiers
- Mutation report recorded
</verification>

<success_criteria>
- DATA-05's boundedness and DATA-06's fail-closed isolation are evidenced by negatives, not just positives
- D-25 and D-26 discharged; OBLIG-CPP-LEAKY-RED, OBLIG-CPP-CAPACITY-INVARIANT, OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE all bound to running tests
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-08-SUMMARY.md` when done
</output>
