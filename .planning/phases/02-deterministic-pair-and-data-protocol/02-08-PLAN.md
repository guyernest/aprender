---
phase: 02-deterministic-pair-and-data-protocol
plan: 08
type: execute
wave: 6
depends_on: [02-07]
files_modified:
  - crates/aprender-contrastive-data/tests/negative_leaky.rs
  - crates/aprender-contrastive-data/tests/negative_materializing.rs
  - crates/aprender-contrastive-data/tests/ui.rs
  - crates/aprender-contrastive-data/tests/ui/
  - crates/aprender-contrastive-data/tests/common/mod.rs
  - crates/aprender-contrastive-data/src/
  - contracts/aprender/binding.yaml
  - Makefile
autonomous: true
requirements: [DATA-05, DATA-06]

must_haves:
  truths:
    - "A deliberately leaky pair dump (an endpoint replaced by a validation row's ID) is REJECTED by the same validate_pair_records call that accepts the honest dump, with the offending ID named — and the mirror test proves the gate is not rejecting everything"
    - "A deliberately materializing sampler FAILS the capacity-invariant gate at a layout where the honest sampler passes, measured through the SAME public RetainedState::state_report call, never self-reported"
    - "The capacity gate is exercised at K = N (all-singleton) under a fixed small budget — the adversarial layout that an O(K^2) class-pair design would fail and a three-class test could never expose"
    - "A compatibility-profile selection run does not COMPILE: trybuild pins both the type mismatch on PreparedDataset<Compatibility> and the absent validation_witness method, with committed .stderr snapshots that name the crate's real types"
    - "Every equation in contracts/contrastive-pair-protocol-v1.yaml is bound in contracts/aprender/binding.yaml and the binding audit runs as a BLOCKING tier3 gate — pv validate checks shape, not coverage"
    - "cargo-mutants runs scoped to the new crate under a bounded wall clock, and surviving mutants are killed or individually justified (D-26)"
  artifacts:
    - path: "crates/aprender-contrastive-data/tests/negative_leaky.rs"
      provides: "In-band leaky negative built from UntrustedPairRecord + mirror (D-25)"
      contains: "EndpointNotInSelection"
    - path: "crates/aprender-contrastive-data/tests/negative_materializing.rs"
      provides: "In-band materializing negative implementing RetainedState + capacity invariant + K=N and 10x scaling properties"
      contains: "RetainedState"
    - path: "crates/aprender-contrastive-data/tests/ui.rs"
      provides: "trybuild harness for DATA-06 non-constructibility"
      contains: "compile_fail"
    - path: "contracts/aprender/binding.yaml"
      provides: "Binding entries for every equation of both phase contracts (single owner for this phase)"
      contains: "contrastive-pair-protocol-v1.yaml"
  key_links:
    - from: "crates/aprender-contrastive-data/tests/negative_leaky.rs"
      to: "crates/aprender-contrastive-data/src/pairs.rs"
      via: "the SAME validate_pair_records call rejects the poisoned dump and accepts the honest one"
      pattern: "validate_pair_records"
    - from: "crates/aprender-contrastive-data/tests/negative_materializing.rs"
      to: "crates/aprender-contrastive-data/src/pairs.rs"
      via: "both samplers implement the public RetainedState trait, so the gate is one call"
      pattern: "state_report"
    - from: "crates/aprender-contrastive-data/tests/ui.rs"
      to: "crates/aprender-contrastive-data/tests/ui/"
      via: "trybuild::TestCases::compile_fail over ui/*.rs with committed .stderr"
      pattern: "compile_fail"
    - from: "Makefile"
      to: "contracts/aprender/binding.yaml"
      via: "contract-audit-phase2 target wired into tier3 as a blocking gate"
      pattern: "contract-audit-phase2"
---

<objective>
Build the phase's honesty gates (D-25, Phase 1 D-24 discipline): the two in-band negative variants
that must fail in every `cargo test`, the trybuild compile-fail proofs for DATA-06's "cannot be
constructed" claims, the adversarial K = N capacity property, the binding-coverage gate, and the
scoped mutation run (D-26).

Purpose: "no leakage" and "bounded memory" are the phase's two fakeable claims — a passing test
suite can assert both without them being true. A gate that has only ever been observed passing is
not evidence. These negatives are the phase's highest-value artifact (CONTEXT specifics).
Output: negative tests RED-under-gate/GREEN-mirror, committed .stderr snapshots, binding coverage,
mutation report, full gate sweep.

## Review corrections incorporated (02-REVIEWS.md)

Both reviewers praised this plan's negative-testing philosophy; every correction below is about
whether the tests can actually be written against the API.

- **F14 / Codex 02-08 HIGH — `negative_materializing.rs` could not call the proposed
  `#[cfg(test)] fn retained_state_size`.** A library method under `#[cfg(test)]` is invisible to
  external integration tests, which are separate crates. Plan 02-07 now exposes a stable public
  `SamplerStateReport` + `RetainedState` trait; this plan's gate calls that, and the in-band
  MaterializingSampler implements the SAME trait so the gate is literally one call for both.
- **F13 / Codex 02-08 HIGH — "replace a pair endpoint with a validation ID" conflicts with the
  structural `SelectedId` private-constructor design.** A trusted `LabeledPair` cannot represent
  poisoned input by construction. The poisoning test now builds `UntrustedPairRecord`s (plan
  02-07) and runs them through `validate_pair_records`, which is the real untrusted boundary.
- **V1 / Codex 02-08 HIGH — the compatibility trybuild case could not prove a runtime profile
  property.** Plan 02-03 made profile a type parameter, so the two compatibility cases are now
  genuinely non-compiling: `PreparedDataset<Compatibility>` cannot be passed where
  `&PreparedDataset<Canonical>` is expected, and it has no `validation_witness` method at all.
- **F1 / Codex 02-08 HIGH — testing only three classes could never expose the O(K²) class-pair
  state.** An adversarial K = N all-singleton layout under a fixed small budget is now a required
  case, cross-checked against plan 02-04's `[1]*32` contracted fixture.
- **F18 / Codex 02-08 MEDIUM — mutation fixes can modify any source file but the frontmatter
  listed only two negative tests.** `files_modified` now includes `crates/aprender-contrastive-data/src/`,
  the shared test module, the binding registry, and the Makefile.
- **F18 second half / Codex 02-08 MEDIUM — a 300-second timeout per mutant can run for hours.**
  The run is bounded: `--timeout 60` per mutant (this crate's full test suite runs in seconds) and
  an overall `timeout 2700` wrapper whose expiry is reported as a partial result rather than
  silently accepted.
- **F9 / Codex 02-02 HIGH (cross-plan) — `pv validate` checks contract SHAPE, not binding
  coverage, and no plan updated `contracts/aprender/binding.yaml`.** This plan is the phase's
  single owner of that file (two plans appending to one YAML list would collide) and adds the
  `contract-audit-phase2` target as a BLOCKING tier3 gate.
- **F20 / Codex 02-02 MEDIUM + Gemini §4.2 — the crates.io cascade.** The gate sweep runs
  `cargo package --no-verify` for both crates and records the required publish order; the
  verifying `cargo package -p apr-cli` is deliberately not gated, for the reason stated in plan
  02-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-band negatives — leaky untrusted dump and materializing sampler, each with its mirror</name>
  <files>crates/aprender-contrastive-data/tests/negative_leaky.rs, crates/aprender-contrastive-data/tests/negative_materializing.rs, crates/aprender-contrastive-data/tests/common/mod.rs</files>
  <read_first>
    - crates/aprender-core/tests/setfit_conformance/detach_negative.rs IN FULL (the exact three-element discipline: header doc explaining WHY the negative exists; failure for the RIGHT reason with offenders NAMED; the mirror test proving the same call accepts the honest implementation)
    - crates/aprender-contrastive-data/src/pairs.rs (`UntrustedPairRecord`, `parse_pair_dump`, `validate_pair_records`, `SamplerStateReport`, `RetainedState`, `PairSampler` — plan 02-07's API)
    - crates/aprender-contrastive-data/tests/common/mod.rs (plan 02-04's shared fixture models and loaders; `load_contracted()` supplies the `[1]*32` row)
    - contracts/contrastive-pair-protocol-v1.yaml OBLIG-CPP-LEAKY-RED, OBLIG-CPP-CAPACITY-INVARIANT, OBLIG-CPP-KN-ADVERSARIAL (the obligations these tests discharge)
  </read_first>
  <action>
    **negative_leaky.rs** (discharges OBLIG-CPP-LEAKY-RED). Build an honest `PreparedDataset<Canonical>`
    from a synthetic corpus, select, and dump the honest pair stream with `dump_pairs`. Then
    construct the LEAKY input the way the API actually allows (review finding F13): parse the dump
    with `parse_pair_dump` into `Vec<UntrustedPairRecord>` and replace ONE record's `lo` with a
    VALIDATION row's ID. A trusted `LabeledPair` cannot represent this — its endpoints are
    `SelectedId` ordinals with a private constructor — which is exactly why the untrusted DTO is
    the correct poisoning surface. Assert:
    (a) `validate_pair_records(&leaky, &selection)` returns `Err(EndpointNotInSelection)` and the
        error message NAMES the offending validation ID (that is what makes a real failure
        diagnosable rather than merely red);
    (b) before that, prove the leak is the ONLY defect: the same record list with the poisoned
        record REMOVED validates Ok. Without this, the rejection assertion would also pass against
        a gate that rejects everything malformed for unrelated reasons;
    (c) the MIRROR: the identical `validate_pair_records` call over the untouched parsed dump
        returns Ok and yields pairs equal to the sampler's own output.
    Add a second poisoning case for the target channel: flip one record's `target` from 0.0 to 1.0
    and assert `Err(PairTargetMismatch)` naming both the declared and derived targets — a leakage
    gate that only checks membership would miss a mislabeled pair.
    Header doc: copy the detach_negative.rs voice — state that a leakage gate observed only passing
    is not evidence, and that this file is the D-25 discipline applied to PF-002.

    **negative_materializing.rs** (discharges OBLIG-CPP-CAPACITY-INVARIANT and
    OBLIG-CPP-KN-ADVERSARIAL). Implement a test-only `MaterializingSampler` that enumerates the
    full Cartesian pair set (the reference implementation's own trap, RESEARCH Finding F2) and
    implements the crate's public `RetainedState` trait, reporting its enumerated pairs in
    `materialized_pairs`. Define the gate ONCE as a shared helper taking `&dyn RetainedState`:
    `total_retained_entries() <= C_LINEAR * (examples + budget) + C_CONST`, with the constants
    derived from the honest sampler's structure and the derivation stated in comments (bucket
    entries + positive weights + negative weights + class offsets + cursor). Assert:
    (a) at a layout where the pair space dwarfs the examples (3 classes × 64 → a 24,576-pair
        space), the materializer VIOLATES the bound (RED under the gate);
    (b) the MIRROR: the honest `PairSampler` at the same layout satisfies it;
    (c) the 10× scaling property: honest `total_retained_entries()` at N and 10N under the SAME
        budget grows by ≤ ~10× + constant, while the materializer's grows quadratically;
    (d) **the adversarial K = N case (review finding F1):** an all-singleton layout with 32
        classes of one member each, under a FIXED SMALL budget (16). Assert
        `negative_weight_entries == 32` and `class_offset_entries == 32` — a class-pair design
        would need ~496 entries here, and the expected 496 is read from plan 02-04's `[1]*32`
        contracted fixture (`negative_capacity`) rather than hand-typed, so the two artifacts
        cross-check. Also assert the honest sampler satisfies the linear bound at K = 8, 32, and
        128 all-singleton layouts, and that the growth is linear in K.
    Measurement is STRUCTURAL through the public `state_report()` — a self-reported size is exactly
    as trustworthy as a self-reported decreasing loss (D-25).
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test negative_leaky > /tmp/t-leaky.log 2>&1; a=$?; cargo test -p aprender-contrastive-data --test negative_materializing > /tmp/t-mat.log 2>&1; b=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass in a plain `cargo test -p aprender-contrastive-data` (in-band — no feature gate, no `#[ignore]`)
    - negative_leaky.rs asserts the error message contains the poisoned validation ID string, and contains the "leak is the only defect" control assertion
    - negative_leaky.rs contains the target-mismatch poisoning case asserting `PairTargetMismatch`
    - Both files contain a mirror test whose name contains "mirror" and which makes the SAME gate call against the honest implementation
    - negative_materializing.rs defines the bound from named constants and calls `state_report()` through the shared `&dyn RetainedState` helper; grep shows no assertion against a sampler-reported "memory used" string
    - The K = N case exists with 32 singleton classes and a fixed budget of 16, asserts `negative_weight_entries == 32`, and reads 496 from plan 02-04's contracted fixture rather than hard-coding it
    - The header doc of each file explains why the negative exists (non-empty `//!` block referencing the obligation ID)
    - **Falsification of the gates themselves:** temporarily "fix" the materializer to report honest structural counts and confirm the capacity test goes GREEN (proving the gate reads the report rather than the type); temporarily un-poison the leaky record and confirm the rejection test goes RED. Record both observations in the SUMMARY, then revert.
  </acceptance_criteria>
  <done>Both fakeable claims now have an in-band wrong implementation that the real gates demonstrably catch — including at the adversarial K = N layout — plus mirrors proving the gates accept honesty.</done>
</task>

<task type="auto">
  <name>Task 2: trybuild compile-fail proofs — leakage is not constructible</name>
  <files>crates/aprender-contrastive-data/tests/ui.rs, crates/aprender-contrastive-data/tests/ui/</files>
  <read_first>
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md "No Analog Found" trybuild row (no TestCases harness exists in-repo; use the standard upstream shape)
    - crates/aprender-contrastive-data/src/{prepared.rs,select.rs,pairs.rs} public APIs (what must NOT compile) — especially that `validation_witness` is implemented only in `impl PreparedDataset<Canonical>` and that `FewShotSelector::select` takes `&PreparedDataset<Canonical>`
    - crates/aprender-contrastive-data/src/split.rs (both constructors are `pub(crate)` — case 5 depends on that)
    - contracts/contrastive-pair-protocol-v1.yaml OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE
  </read_first>
  <action>
    Create `tests/ui.rs` with the standard harness:
    `let t = trybuild::TestCases::new(); t.compile_fail("tests/ui/*.rs");`
    Author compile-fail cases (each a minimal program using the crate's public API), with COMMITTED
    `.stderr` snapshots pinning the diagnostic:
    1. `select_on_compat_dataset.rs` — calling `FewShotSelector::select` with a
       `&PreparedDataset<Compatibility>` where `&PreparedDataset<Canonical>` is demanded. Expected
       diagnostic: a type mismatch naming both `PreparedDataset<Compatibility>` and
       `PreparedDataset<Canonical>`. **This is the case the pre-review design could not produce**,
       because profile was a runtime field and the equivalent call compiled and returned an error
       (review V1 / adjudicated claim #1).
    2. `compat_dataset_has_no_validation_witness.rs` — calling `.validation_witness()` on a
       `PreparedDataset<Compatibility>`. Expected: `no method named 'validation_witness' found`.
       This is the D-19 core: not rejected — unrepresentable.
    3. `replay_on_compat_dataset.rs` — calling `Selection::replay` with a compatibility dataset:
       same type mismatch, proving the REPLAY path is closed too (review finding F6 — a forged
       compatibility manifest has no dataset it can be replayed against).
    4. `pairs_from_raw_ids.rs` — constructing a `PairSampler` from anything other than a
       `&Selection` (e.g. a `Vec<String>` of IDs): no such constructor exists.
    5. `split_constructed_directly.rs` — calling `Split::<Train>::from_jsonl_bytes` from outside
       the crate: the constructor is `pub(crate)`, so a downstream caller cannot mint a typed split
       and therefore cannot assemble a train/validation pair from unrelated datasets.
    Each case file carries a comment naming the obligation and the requirement (DATA-06). Run once
    with `TRYBUILD=overwrite` to generate the `.stderr` snapshots, REVIEW each snapshot for the
    expected diagnostic (the error must be about the missing type/method/visibility, not an
    unrelated syntax error or an unresolved import), then commit them. If any snapshot shows a
    syntax or import error instead of the intended diagnostic, the case is not proving what it
    claims — fix the case, do not accept the snapshot.
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test ui > /tmp/t-ui.log 2>&1; rc=$?; n=$(ls crates/aprender-contrastive-data/tests/ui/*.stderr 2>/dev/null | wc -l | tr -d ' '); [ "$rc" -eq 0 ] && [ "$n" -eq 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p aprender-contrastive-data --test ui` passes with all five cases
    - tests/ui/ contains five `.rs` cases and five committed `.stderr` snapshots
    - Each `.stderr` references the crate's actual types or methods (`PreparedDataset<Compatibility>`, `PreparedDataset<Canonical>`, `validation_witness`, `Selection`, `from_jsonl_bytes`) — not a syntax error, not an unresolved-import error. Quote each snapshot's first diagnostic line in the SUMMARY
    - Each case file's comment names OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE and DATA-06
    - **Falsification of the harness:** temporarily edit one case so it DOES compile and confirm trybuild reports the unexpected success as a failure. Record, then revert
  </acceptance_criteria>
  <done>DATA-06's "cannot be constructed" claims are pinned by compiler-verified, snapshot-reviewed evidence that the corrected typestate actually supports.</done>
</task>

<task type="auto">
  <name>Task 3: Binding coverage gate, bounded mutation run, and the full phase gate sweep</name>
  <files>contracts/aprender/binding.yaml, Makefile, crates/aprender-contrastive-data/src/, crates/aprender-contrastive-data/tests/common/mod.rs</files>
  <read_first>
    - contracts/aprender/binding.yaml (head — the entry shape: contract, equation, module_path, function, signature, status, notes) and Makefile lines 866-950 (`BINDING`, `contract-audit`, `contract-validate`, and tier3's invocation at ~253)
    - The 02-02..02-07 SUMMARYs (what the crate now contains, and every `#[contract]` annotation added — for building the binding entries and triaging survivors)
    - CLAUDE.md mutation invocation and Verification Discipline rule 1
    - Makefile `setfit-feature-matrix` target (~275-294, the carried-forward Phase 1 D-06 CPU feature matrix)
    - /tmp/cpp-audit.log or the SUMMARY of plan 02-02 Task 3 (the recorded baseline `pv audit` output showing the equations as unbound)
  </read_first>
  <action>
    1. **Binding coverage (review finding F9).** Add one entry to `contracts/aprender/binding.yaml`
       for every equation in `contracts/contrastive-pair-protocol-v1.yaml` and every obligation in
       `contracts/tweet-eval-stance-benchmark-v1.yaml` that has a Rust implementation, using the
       existing entry shape (contract, equation, module_path, function, signature, status, notes).
       Source the module paths and signatures from the `#[contract]` annotations plans 02-03,
       02-05, 02-06, and 02-07 added — do not invent paths; if an equation has no implementing
       function, record it with an explicit status rather than omitting it, and name the reason.
       This plan is the phase's SINGLE owner of `binding.yaml`; earlier plans deliberately did not
       touch it so two wave-mates could not collide on one YAML list.

       Then add a Makefile target `contract-audit-phase2` that runs
       `$(PV_BIN) audit <contract> --binding $(BINDING)` for both phase contracts, capturing each
       status directly (never `$?` through a pipe), and FAILS if either audit reports an unbound
       equation. Wire it into tier3 next to `$(MAKE) contract-validate`, following the D-26
       evidence discipline in the Makefile comment block at ~238-254: run it STANDALONE first with
       its status captured, record the wall time and result in the comment, and only then wire it.
       **Do not wire the repo-wide `contract-audit` target** — it iterates all 40+ contracts and
       its current state is unknown; scoping to this phase's two contracts is the change this
       phase can honestly stand behind. Record the standalone repo-wide `contract-audit` result in
       the SUMMARY as an observation if it is cheap to obtain.

    2. **Bounded mutation run (D-26, review finding F18).** Run
       `timeout 2700 cargo mutants -p aprender-contrastive-data --no-times --timeout 60 --in-place
        > /tmp/mutants.log 2>&1; rc=$?`
       — `--timeout 60` per mutant is generous for a crate whose full suite runs in seconds, and
       the outer `timeout 2700` bounds the whole run. If the outer timeout fires (rc 124), report
       it as a PARTIAL result in the SUMMARY with the counts obtained so far and do not claim a
       completed run. Capture the outcome file; for each SURVIVING mutant either (a) add or extend
       a test in the most appropriate existing module to kill it, or (b) record it in the SUMMARY
       with a one-line justification (e.g. equivalent mutant). Do not weaken any gate to improve
       the score. Target: caught rate consistent with the repo's ≥85% mutation threshold for the
       new crate. Mutation-driven test additions may touch any file under
       `crates/aprender-contrastive-data/` — that is why the frontmatter lists the src tree.

    3. **Phase gate sweep**, capturing each status directly (never `$?` through a pipe):
       `make tier2` (includes the new crate line); `make contrastive-data-boundary`;
       `make contract-audit-phase2`; the tier3 contract-validation path over the full
       `$(CONTRACTS)` list (both phase contracts); `make setfit-feature-matrix` (the carried-forward
       Phase 1 D-06 CPU feature matrix — `--no-default-features`, `--features setfit`,
       all-features must all stay green with the new crate in the workspace);
       `cargo check -p aprender-contrastive-data`, `cargo check -p apr-cli`,
       `cargo check --workspace`; and the publishability pair
       `cargo package --no-verify -p aprender-contrastive-data` and
       `cargo package --no-verify -p apr-cli` (review finding F20 / Gemini §4.2). Record in the
       SUMMARY that `aprender-contrastive-data` MUST publish before `apr-cli` in the cascade, and
       repeat plan 02-02's honest note that the verifying `cargo package -p apr-cli` cannot pass
       until that publish happens. Never self-serve the publish (CLAUDE.md).

    4. Record measured runtimes for the new tier2 line and reconcile the Makefile comment from plan
       02-02 Task 3 if the measurement moved (Phase 1 measured-comment discipline).
  </action>
  <verify>
    <automated>make tier2 > /tmp/g-tier2.log 2>&1; a=$?; make contrastive-data-boundary > /tmp/g-cdb.log 2>&1; b=$?; make contract-audit-phase2 > /tmp/g-audit.log 2>&1; c=$?; make setfit-feature-matrix > /tmp/g-sfm.log 2>&1; d=$?; cargo check --workspace > /tmp/g-ws.log 2>&1; e=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "$c" -eq 0 ] && [ "$d" -eq 0 ] && [ "$e" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `contracts/aprender/binding.yaml` contains entries referencing `contrastive-pair-protocol-v1.yaml` for every equation, with module paths matching the actual `#[contract]` annotations (spot-check three against the source)
    - `make contract-audit-phase2` exits 0 and its log shows zero unbound equations for both phase contracts; the SUMMARY quotes the before (plan 02-02's baseline, all unbound) and after outputs so the delta is visible
    - **Falsification of the audit gate:** temporarily delete one binding entry and confirm `make contract-audit-phase2` exits nonzero naming that equation. Record, then revert
    - tier3 invokes `contract-audit-phase2`, with a D-26-style Makefile comment recording the standalone run's measured wall time and result
    - cargo-mutants completes (or reports a bounded partial result); the SUMMARY lists total/caught/survived with per-survivor dispositions and states the exact invocation used
    - `make tier2`, `make contrastive-data-boundary`, `make setfit-feature-matrix`, and `cargo check --workspace` all exit 0 (statuses captured directly)
    - The tier3 contract-validation path exits 0 over a `$(CONTRACTS)` list containing both phase contracts
    - Both `cargo package --no-verify` runs exit 0; the SUMMARY records the publish order and why the verifying apr-cli package is not gated
  </acceptance_criteria>
  <done>Every contract equation is bound and audited by a blocking tier gate, the crate's test suite is mutation-hardened under a bounded run, and every phase gate — including the carried-forward CPU feature matrix and the packaging checks — runs green inside its tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → claims | The gates themselves are the product; a vacuous gate is worse than none |
| contract text → implementation | A schema-valid contract with no binding is a claim nothing checks |
| untrusted pair dump → validator | This is the surface the leaky negative attacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Repudiation | vacuous leakage gate | mitigate | In-band leaky negative built from `UntrustedPairRecord` RED + mirror GREEN under the SAME `validate_pair_records` call, plus a control proving the leak is the only defect (Task 1) |
| T-02-26 | Denial of Service | unbounded-memory sampler passing tests | mitigate | Structural capacity invariant read through the public `RetainedState::state_report`, materializing negative RED, 10× scaling property, and the K = N adversarial layout (Task 1, review finding F1) |
| T-02-27 | Elevation (data) | leakage expressible through the public API | mitigate | Five trybuild compile-fail proofs with reviewed, committed `.stderr` snapshots against the corrected typestate (Task 2) |
| T-02-28 | Tampering | tests that pass by construction (weak assertions) | mitigate | Scoped, wall-clock-bounded cargo-mutants with per-survivor disposition (Task 3) |
| T-02-46 | Repudiation | contract obligations bound to nothing | mitigate | binding.yaml entries for every equation + blocking `contract-audit-phase2` in tier3, with the gate's failure mode observed by deleting an entry (Task 3, review finding F9) |
| T-02-47 | Tampering (supply chain) | publish cascade breaking on an unpublished dependency | mitigate | `cargo package --no-verify` for both crates + recorded publish order; the verifying apr-cli package is honestly declared un-gateable until the new crate publishes (Task 3, review finding F20) |
</threat_model>

<verification>
- Plain `cargo test -p aprender-contrastive-data` runs both negatives and the trybuild harness — no opt-in flags
- All phase gates green inside their tiers, including `make setfit-feature-matrix` and `make contract-audit-phase2`
- Mutation report recorded with the exact invocation and per-survivor dispositions
- Every gate in this plan has had its FAILURE observed once (leaky un-poisoned, materializer made honest, trybuild case made to compile, binding entry deleted) and the observations recorded
</verification>

<success_criteria>
- DATA-05's boundedness and DATA-06's fail-closed isolation are evidenced by negatives that compile against the real API, not by positives alone
- The O(K) claim is defended at K = N, the layout that would have exposed the rejected design
- D-25 and D-26 discharged; OBLIG-CPP-LEAKY-RED, OBLIG-CPP-CAPACITY-INVARIANT, OBLIG-CPP-KN-ADVERSARIAL, OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE all bound to running tests AND to binding-registry entries
- Review findings F1, F9, F13, F14, F18, F20 and the V1 consequence are closed
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-08-SUMMARY.md` when done
</output>
</content>
