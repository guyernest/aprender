---
phase: 02-deterministic-pair-and-data-protocol
plan: 09
type: execute
wave: 6
depends_on: [02-05, 02-06, 02-07]
files_modified:
  - crates/apr-cli/src/data_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/commands/mod.rs
  - crates/apr-cli/src/commands/data_contrastive.rs
  - docs/examples/tweet-eval-stance.md
autonomous: true
requirements: [DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "A user can run `apr data select --data <dir> --shots <8|16|32|64> --seed <contracted seed>` and get an atomically written selection-manifest.json plus machine-readable JSON with ordered IDs, labels, the semantic hash, the exclusion record, and the persisted access ledger"
    - "A user can run `apr data pairs --selection <manifest> --data <dir> [--budget <n>] [--hard-cap <n>] [--dump <path>]` and get the pair-manifest hash and counts; two runs with the same inputs print the same hash"
    - "Canonical splits are reached ONLY through PreparedDataset::from_attested_bytes, so a mixed, compatibility-profile, stale-schema, or forged directory is a typed error before any row is selected"
    - "--seed is REQUIRED and validated against the ten contracted benchmark seeds [13,17,23,29,31,37,41,43,47,53]; there is no default 42, because 42 is not a contracted seed"
    - "Every file this command writes is written atomically (temp file in the destination directory, fsync, rename) with no-clobber by default and an explicit --force, and an interrupted write leaves no partial artifact"
    - "The CLI is a pure filesystem adapter: it reads bytes, calls the crate, writes bytes — no parsing, hashing, selection, or sampling logic lives in apr-cli"
  artifacts:
    - path: "crates/apr-cli/src/commands/data_contrastive.rs"
      provides: "select + pairs command implementations over the crate API, with attested ingest and atomic writes"
      contains: "from_attested_bytes"
    - path: "crates/apr-cli/src/data_commands.rs"
      provides: "Select and Pairs clap variants with a required, contracted-seed-validated --seed"
      contains: "Select"
    - path: "crates/apr-cli/src/dispatch_analysis.rs"
      provides: "dispatch arms for the new variants"
      contains: "data_contrastive"
    - path: "docs/examples/tweet-eval-stance.md"
      provides: "prepare → select → pairs workflow incl. schema-version policy, seed policy, and budget/hard-cap semantics"
      contains: "apr data select"
  key_links:
    - from: "crates/apr-cli/src/dispatch_analysis.rs"
      to: "crates/apr-cli/src/commands/data_contrastive.rs"
      via: "dispatch_data_command match arms passing cli.json/cli.offline through"
      pattern: "data_contrastive"
    - from: "crates/apr-cli/src/commands/data_contrastive.rs"
      to: "crates/aprender-contrastive-data"
      via: "from_attested_bytes → FewShotSelector::select → SelectionManifest::to_file_bytes / from_bytes → Selection::replay → PairSampler → pair_manifest_hash, all via the crate's public API"
      pattern: "aprender_contrastive_data"
---

<objective>
Expose the phase's user surface (ROADMAP phrases every Phase 2 criterion as "A user can…"): the
`apr data select` and `apr data pairs` subcommands as thin, hardened filesystem adapters over
`aprender-contrastive-data`, plus the documented workflow.

Purpose: DATA-03's selection manifest and DATA-04/05's replayable bounded pairs become user
operations. CLI shape follows the research recommendation (Open Question 5 resolution, a
discretion item): two subcommands under the existing `apr data` namespace, dump as a flag per
D-09's "explicit dump path", names matching the existing verb style (Audit, Split, Balance).
Output: two subcommands + tests + updated docs example.

## Review corrections incorporated (02-REVIEWS.md)

Codex rated this plan HIGH: "the user surface can bypass provenance and compatibility isolation
even if the lower-level happy-path tests pass." Every finding is addressed.

- **F16 / Codex 02-09 HIGH — the command read JSONL with hardcoded declarations and validated
  neither `benchmark-manifest.json`, nor its profile, source hashes, schema version, nor split
  hashes, so a mixed or forged directory could pass row-level checks.** Both commands now ingest
  through the crate-owned `PreparedDataset::<Canonical>::from_attested_bytes` boundary that plan
  02-06 Task 2 delivers. A compatibility directory fails `ProfileMismatch`; a mixed directory
  fails `SplitHashMismatch`; a stale directory fails `UnsupportedSchemaVersion`.
- **F6 / Codex 02-09 HIGH — `Selection::from_manifest` was insufficiently strict, so delegating to
  it did not close the replay hole.** Plan 02-05 replaced it with `Selection::replay`, which
  consumes a canonical prepared dataset and revalidates every invariant including a deterministic
  recomputation. `run_pairs` calls only that.
- **F7 / Codex 02-09 HIGH — the access ledger was never persisted.** It is now part of the
  selection-manifest payload (plan 02-05), so `apr data select` writing the manifest IS the
  persistence, and `--json` surfaces it.
- **F16 / Codex 02-09 HIGH — pair dumps lacked no-clobber, atomic write, and rollback.** Every
  write in this module is temp-file + `write_all` + `sync_all` + `rename` within the destination
  directory, with `create_new` no-clobber semantics, `--force` to replace, and temp cleanup on any
  error path. Tested with an induced mid-write failure.
- **F3 / Codex 02-09 MEDIUM — explicit budget versus hard cap remained ambiguous at the CLI.**
  Resolved to match plan 02-07's `budget_resolution`: `--budget` above `--hard-cap` is an error
  naming both numbers and suggesting `--hard-cap`, never a silent clamp. The help text says so.
- **F16 / Codex 02-09 MEDIUM — selection defaulted to seed 42, which is not one of the ten
  contracted benchmark seeds.** Verified: `crates/apr-cli/src/commands/data_tweeteval.rs:45`
  defines `BENCHMARK_SEEDS: [u64; 10] = [13, 17, 23, 29, 31, 37, 41, 43, 47, 53]`. `--seed` is now
  REQUIRED with no default and is validated against that list; `--any-seed` is the explicit,
  documented escape hatch for experimentation, and the manifest records which mode was used.
- **Codex 02-09 LOW — generic command names are misleading if the implementation is hardcoded to
  TweetEval declarations.** Addressed honestly: the declarations come from the attested benchmark
  manifest in the data directory rather than from hardcoded TweetEval constants, so the commands
  are as generic as their name; the help text states which manifest field supplies the label map
  and counts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-05-SUMMARY.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-06-SUMMARY.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clap variants + dispatch arms — required contracted seed, explicit cap semantics</name>
  <files>crates/apr-cli/src/data_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/commands/mod.rs</files>
  <read_first>
    - crates/apr-cli/src/data_commands.rs `Split` variant (~lines 43-66: doc comment per variant AND per field, `value_name` on positionals, `#[arg(long, default_value = ...)]`) and the `TweetEvalStance` const-backed default pattern (~lines 7-24)
    - crates/apr-cli/src/dispatch_analysis.rs `dispatch_data_command` (~lines 726-791: destructure, deref copies, `as_deref` options, `cli.offline`/`cli.json` passed explicitly)
    - crates/apr-cli/src/commands/mod.rs (module registration list)
    - crates/apr-cli/src/commands/data_tweeteval.rs line ~45 — `BENCHMARK_SEEDS: [u64; 10] = [13, 17, 23, 29, 31, 37, 41, 43, 47, 53]`. **42 is not in this list** (review finding F16)
    - crates/aprender-contrastive-data/src/pairs.rs `budget_resolution` behavior (plan 02-07) — the CLI must mirror it, not reinvent it
  </read_first>
  <action>
    Add two variants to `DataCommands` (doc comments on every variant and field — clap renders
    them):

    - `Select { data: PathBuf (long, value_name "DIR" — the directory containing canonical
      train/validation/test JSONL plus benchmark-manifest.json from `apr data tweet-eval-stance`;
      the label map, per-class counts, and profile come from that manifest's dataset_attestation
      section, NOT from hardcoded constants), shots: u32 (long, REQUIRED, no default — validated
      against {8,16,32,64} in the command), seed: u64 (long, **REQUIRED, no default** — must be one
      of the ten contracted benchmark seeds unless --any-seed is passed; the doc comment lists them
      and states that 42 is deliberately not among them), any_seed: bool (long "any-seed" — opt out
      of contracted-seed validation for experimentation; recorded in the manifest and the JSON
      output), output: Option<PathBuf> (short+long — defaults to the data dir), force: bool
      (long — replace an existing selection-manifest.json) }`
    - `Pairs { selection: PathBuf (long, value_name "FILE" — the selection-manifest.json), data:
      PathBuf (long, value_name "DIR" — the same attested canonical directory, used to revalidate
      the selection), budget: Option<u64> (long — None means the D-14 default: the closed-form
      oversampling count CLAMPED by the hard cap. **A value above --hard-cap is an ERROR, not a
      silent clamp** — say so in the doc comment), hard_cap: Option<u64> (long "hard-cap" — None
      means the crate's contracted DEFAULT_HARD_CAP; the D-14 configurable clamp, passed through to
      PairConfig verbatim), dump: Option<PathBuf> (long — D-09's explicit audit dump; a FLAG, not a
      subcommand), force: bool (long — replace an existing dump file) }`

    Note that `Pairs` deliberately has NO `--seed`: the root seed is part of the selection manifest
    and re-supplying it at the pair stage would create two sources of truth for one replay tuple.
    State that in the doc comment.

    Register `pub mod data_contrastive;` in `commands/mod.rs`. Add two dispatch arms in
    `dispatch_data_command` calling `commands::data_contrastive::run_select(...)` /
    `run_pairs(...)`, passing `cli.json` and `cli.offline` explicitly per the existing pattern.
  </action>
  <verify>
    <automated>cargo check -p apr-cli > /tmp/c9.log 2>&1; a=$?; cargo run -p apr-cli --bin apr -- data select --help > /tmp/h-sel.log 2>&1; b=$?; cargo run -p apr-cli --bin apr -- data pairs --help > /tmp/h-pairs.log 2>&1; c=$?; s=$(grep -c "shots" /tmp/h-sel.log); h=$(grep -c "hard-cap" /tmp/h-pairs.log); [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "$c" -eq 0 ] && [ "$s" -ge 1 ] && [ "$h" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `apr data select --help` shows data/shots/seed/any-seed/output/force with doc text; `apr data pairs --help` shows selection/data/budget/hard-cap/dump/force
    - `apr data select --help` output does NOT contain `default_value` text for `--seed` (it is required), and the help text lists the ten contracted seeds
    - `apr data pairs --help` states that `--budget` above `--hard-cap` is an error, not a clamp
    - `grep -v '^[[:space:]]*//' crates/apr-cli/src/data_commands.rs | grep -c '"42"'` returns 0 (no seed-42 default anywhere — review finding F16)
    - dispatch_analysis.rs contains both arms and passes `cli.json` through
    - Every variant field has a doc comment (clap-rendered)
    - `cargo check -p apr-cli` exits 0
  </acceptance_criteria>
  <done>The CLI surface exists, routes to the new command module, requires a contracted seed, and states the budget/hard-cap contract in its own help text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Command implementations — attested ingest, atomic writes, strict replay; plus the workflow docs</name>
  <files>crates/apr-cli/src/commands/data_contrastive.rs, docs/examples/tweet-eval-stance.md</files>
  <read_first>
    - crates/apr-cli/src/commands/data_tweeteval.rs (as refactored by plan 02-06): read-once bytes discipline, `create_new` no-clobber + `--force`, rollback-on-partial-write (`written: Vec<PathBuf>` + remove_all), JSON output shape, `write_canonical_fixture` test infra to reuse
    - crates/aprender-contrastive-data public API: `PreparedDataset::<Canonical>::from_attested_bytes` (02-06), `FewShotSelector::select`, `Selection::replay`, `SelectionManifest::{from_selection, to_file_bytes, from_bytes}`, `AccessLedger`, `PairConfig`, `PairSampler`, `PairReplayRecord::from_sampler`, `pair_manifest_hash`, `dump_pairs`, `parse_pair_dump`, `validate_pair_records`
    - docs/examples/tweet-eval-stance.md (the workflow doc to extend)
    - crates/apr-cli/src/commands/data_tweeteval.rs line ~45 (`BENCHMARK_SEEDS`) — the contracted-seed list to validate against; cite the source in a comment rather than duplicating a literal without provenance
  </read_first>
  <behavior>
    Happy paths:
    - run_select on a fixture canonical dir with shots=8, seed=13: writes selection-manifest.json (no-clobber; `--force` to replace) and prints JSON with ordered ids AND labels, semantic_hash, shots, seed, seed_mode, excluded_ids, reduced pools, and the access ledger when `--json`
    - Determinism: two runs → identical semantic_hash in the output and byte-identical manifest payloads (volatile fields differ, hashed payload does not)
    - run_pairs on that manifest: prints pair_manifest_hash (hex), positive/negative counts, resolved budget, whether the default was clamped, emitted kinds, singleton policy + version, degenerate policy version, and the three-clause deviation summary; two runs → identical hash
    - `--dump` writes one JSON line per pair in stream order; re-reading it with `parse_pair_dump` + `validate_pair_records` against the replayed Selection yields exactly the sampler's pairs

    Rejections — every one a named test (review findings F16, F3, F6):
    - shots=9 → structured CliError naming the allowed set {8,16,32,64}, BEFORE touching the filesystem
    - seed=42 without `--any-seed` → structured CliError naming the ten contracted seeds and the `--any-seed` flag (42 is not contracted)
    - seed=42 WITH `--any-seed` → succeeds, and both the manifest and the JSON output record the uncontracted seed mode
    - a data dir prepared with the setfit COMPATIBILITY profile → `ProfileMismatch` surfaced as a structured CliError (the crate call cannot even be typed to accept it — the CLI only ever calls the canonical constructor)
    - a MIXED data dir (train.jsonl from one preparation, validation.jsonl from another) → `SplitHashMismatch` naming the split
    - a data dir whose benchmark-manifest.json is `schema_version: 1` → `UnsupportedSchemaVersion` naming the supported set and the remediation
    - a data dir whose benchmark-manifest.json has an edited attestation (fingerprint or exclusion hash altered) → `FingerprintMismatch` / `ExclusionRecordMismatch`
    - a selection manifest referencing an ID absent from the train split → `EndpointNotInSelection` naming the id
    - a selection manifest whose recorded row hash disagrees with the split content → `RowHashMismatch` naming the id
    - a selection manifest with an internally consistent but WRONG ordered list → `SelectionReplayMismatch` (the case membership-plus-hash checking alone would accept)
    - `--budget 0` → `ZeroBudget`; `--hard-cap 0` → `ZeroHardCap`; `--budget` above `--hard-cap` → `BudgetExceedsHardCap` naming both numbers and suggesting `--hard-cap` (NOT a silent clamp)
    - an ODD `--budget` with both pair kinds available → succeeds, and the reported positive/negative counts differ by exactly 1

    Write safety (review finding F16):
    - Writing a manifest when one already exists and `--force` is absent → structured CliError naming the path; the existing file is byte-unchanged
    - With `--force`, the existing file is replaced and the replacement is complete (never truncated)
    - An induced failure mid-write (inject an error after the temp file is written but before rename — e.g. via a test-only seam or by writing to a path whose rename target is a directory) leaves NO partial artifact and NO leftover temp file, and the pre-existing file is byte-unchanged
    - The same three properties hold for `--dump`
  </behavior>
  <action>
    Implement `run_select` and `run_pairs` as PURE adapters (the D-05 division): every `fs::read`
    happens once per file in this module; all bytes go to crate calls; all outputs come back as
    typed values or bytes.

    **Ingest (review finding F16).** Both commands read `benchmark-manifest.json` plus the canonical
    JSONL files and call
    `PreparedDataset::<Canonical>::from_attested_bytes(attestation_bytes, splits, &mut ledger)`
    (plan 02-06 Task 2). Do NOT construct splits from hardcoded declarations: the label map, class
    counts, split hashes, exclusion record, and fingerprint come from the attested manifest, and
    the crate re-derives and compares all of them before any split is exposed. This is what makes a
    mixed, compatibility, stale, or forged directory a typed error rather than a row-level pass.
    Because the CLI only ever calls the CANONICAL constructor, a compatibility directory is
    rejected by profile check — and the type system independently forbids a compatibility dataset
    from reaching selection.

    **run_select.** Validate `shots ∈ {8,16,32,64}` and the seed policy BEFORE touching the
    filesystem: `--seed` must be one of `BENCHMARK_SEEDS` (cite `data_tweeteval.rs:45` in a
    comment) unless `--any-seed` is passed; record the seed mode. Then attested ingest →
    `FewShotSelector::select(&dataset, &cfg, &mut ledger)` →
    `SelectionManifest::from_selection(&selection, &ledger)` → write
    `selection-manifest.json` as `to_file_bytes()` VERBATIM (the crate owns the on-disk envelope —
    the CLI composes NO JSON around it).

    **run_pairs.** Read the manifest bytes → `SelectionManifest::from_bytes` (which verifies the
    envelope digest) → attested ingest of the data dir → `Selection::replay(&manifest, &dataset,
    &mut ledger)` — the crate's strict replay constructor; the CLI never reconstitutes a Selection
    itself → build `PairConfig` from `--budget`/`--hard-cap` and the manifest's root seed →
    `PairSampler::new` (which applies `resolve_budget`, so `BudgetExceedsHardCap` and the zero
    cases surface from ONE place) → `PairReplayRecord::from_sampler` + `pair_manifest_hash` for
    output; `--dump` streams `dump_pairs` to the file.

    **Atomic writes (review finding F16).** Write EVERY artifact through one shared helper:
    create a temp file in the DESTINATION directory (same filesystem, so rename is atomic), write
    all bytes, `sync_all()`, then `rename()` over the target. Honor no-clobber by checking the
    target's existence up front unless `--force`. On any error, remove the temp file — extend the
    baseline's `written: Vec<PathBuf>` rollback discipline to cover temp paths. Provide a test-only
    seam so the mid-write failure case can be induced deterministically.

    Both commands: human-readable default output, machine JSON under `--json`, and a structured
    `CliError` for every failure, mapping `ContrastiveDataError` while preserving message detail
    (the crate's messages already name split, index, id, expected, and got).

    Tests: reuse the `write_canonical_fixture` infra to build tiny synthetic canonical dirs
    (canonical, compatibility, mixed, stale-schema, tampered-attestation); cover every behavior
    bullet; name the test module so `cargo test -p apr-cli --lib data_contrastive` selects it.

    **Docs.** Extend `docs/examples/tweet-eval-stance.md` with the prepare → select → pairs
    workflow: the three command lines with a contracted seed; the manifest fields (ordered
    examples with labels, exclusions, singleton policy, deviation, persisted access ledger); the
    seed policy (ten contracted seeds; `--any-seed` and what it costs you); the default budget
    story (closed form clamped by `--hard-cap` per D-14) AND the explicit rule that a `--budget`
    above the cap is an error; the replay story (pairs are regenerated from the tuple, only the
    hash persists — D-09); and the schema-version policy (`schema_version: 2` only; a version-1
    directory must be re-prepared, there is no migration — review finding F22).
  </action>
  <verify>
    <automated>cargo test -p apr-cli --lib data_contrastive > /tmp/t-dc.log 2>&1; a=$?; cargo test -p apr-cli --lib > /tmp/t-cli-all.log 2>&1; b=$?; s=$(grep -v '^[[:space:]]*//' crates/apr-cli/src/commands/data_contrastive.rs | grep -c "Sha256"); [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "$s" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - All behavior tests pass, including every named rejection test and all six write-safety tests; `cargo test -p apr-cli --lib` green overall
    - data_contrastive.rs contains no SHA-256 computation, no Fisher-Yates, no pair drawing, and no manifest JSON composition — `grep -v '^[[:space:]]*//' … | grep -c` returns 0 for each of `Sha256`, `swap(`, `unrank`, and `json!` (logic and format live in the crate)
    - `grep -c "from_attested_bytes" crates/apr-cli/src/commands/data_contrastive.rs` ≥ 2 (both commands ingest through the attested boundary — review finding F16)
    - `grep -c "Selection::replay" crates/apr-cli/src/commands/data_contrastive.rs` ≥ 1 and there is no manual `Selection` construction in this file
    - The written selection-manifest.json is the verbatim output of `to_file_bytes` and parses back via `SelectionManifest::from_bytes` (round-trip test)
    - Every write goes through the shared atomic helper (grep: `rename` appears in the helper and `File::create` does not appear outside it); the induced mid-write failure test proves no partial artifact and no leftover temp file
    - The invalid-shots error names {8, 16, 32, 64}; the uncontracted-seed error names the ten seeds and `--any-seed`; the over-cap error names both numbers and suggests `--hard-cap`
    - docs/examples/tweet-eval-stance.md contains `apr data select` and `apr data pairs` sections covering the seed policy, the budget/hard-cap rule, the replay story, and the schema-version policy
  </acceptance_criteria>
  <done>Users can select and generate pairs deterministically from the CLI, with attested ingest, strict replay, contracted seeds, atomically written manifests and dumps, and replay hashes proven stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → CLI → crate | Manifest and JSONL bytes from disk are untrusted until the crate's attested boundary validates them |
| CLI output → later phases | The written selection manifest is the artifact Phase 5 hands to both methods, and it carries the access-ledger evidence |
| process interruption → on-disk artifacts | A partially written manifest would be indistinguishable from a valid one to a later reader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Tampering | edited or forged selection manifest replayed into pairs | mitigate | `run_pairs` rebuilds the Selection ONLY via `Selection::replay`, which revalidates profile, fingerprints, exclusions, membership, both row hashes, balance, ordering, versions, digest, and recomputes the selection deterministically (Task 2, review finding F6) |
| T-02-30 | Elevation (data) | selection over a compatibility-profile directory | mitigate | The CLI calls only `PreparedDataset::<Canonical>::from_attested_bytes`; a compatibility attestation is `ProfileMismatch`, and the type system independently forbids a compatibility dataset from reaching selection (Task 2) |
| T-02-48 | Tampering | mixed / stale / tampered prepared directory | mitigate | Attested ingest checks profile, schema version, per-split hashes, class counts, exclusion hash, and fingerprint before exposing any split — four named rejection tests (Task 2, review finding F16) |
| T-02-31 | Tampering | partial or clobbered writes corrupting a manifest or dump | mitigate | Temp file in the destination dir + `sync_all` + atomic `rename`, no-clobber by default with `--force`, temp cleanup on every error path, and an induced mid-write failure test (Task 2, review finding F16) |
| T-02-49 | Denial of Service | unbounded pair budget from the command line | mitigate | `--budget` above `--hard-cap` is `BudgetExceedsHardCap` naming both numbers; zero budget and zero cap are typed errors; resolution happens in ONE place, the crate's `resolve_budget` (Task 2, review finding F3) |
| T-02-50 | Repudiation | selection performed on an uncontracted seed without a record | mitigate | `--seed` is required and contracted-seed-validated; `--any-seed` is explicit and the mode is recorded in the manifest and JSON output (Tasks 1-2, review finding F16) |
| T-02-32 | Repudiation | ambiguous human-only output | mitigate | `--json` machine-readable output with semantic_hash, labels, exclusions, ledger, and the full replay tuple in both commands |
</threat_model>

<verification>
- `cargo test -p apr-cli --lib data_contrastive` green; `cargo test -p apr-cli --lib` green
- `make tier2` green
- End-to-end smoke on a synthetic fixture dir: prepare → select → pairs, hash stable across two runs, dump round-trips through `parse_pair_dump` + `validate_pair_records`
- Negative smoke: the compatibility, mixed, stale-schema, and tampered-attestation fixture dirs each fail with the expected typed error
</verification>

<success_criteria>
- DATA-03/04/05 delivered at the USER tier: replayable selection manifests and bounded pair replay through `apr data`
- CLI remains a filesystem adapter; the crate owns all semantics including the on-disk manifest envelope, the attested ingest boundary, budget resolution, and the manifest→Selection replay path (D-04/D-05 hold end to end)
- Review findings F3, F6, F7, F16 and the seed-42 defect are closed at the user surface
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-09-SUMMARY.md` when done
</output>
</content>
