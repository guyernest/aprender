---
phase: 02-deterministic-pair-and-data-protocol
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - scripts/setfit_fixtures/generate_fixtures.py
  - crates/aprender-contrastive-data/tests/setfit_reference/
  - crates/aprender-contrastive-data/tests/reference_fixtures.rs
autonomous: true
requirements: [DATA-04, DATA-05]

must_haves:
  truths:
    - "Committed fixtures record the pinned setfit 1.1.3 sampler's MEASURED behavior (self-pairs included, hardcoded seed 42, max_pairs halving) — never its documentation (which the measurement contradicts, Finding F2)"
    - "A separate fixture family records Aprender's CONTRACTED counts (self-pairs excluded, closed-form capacities) so the D-15 deviation is visible as data, not prose"
    - "The degenerate-layout divergence (layout [4,1]: reference epoch length 22 vs doc-formula 12) is present in both families so it can never be silently papered over"
    - "Every fixture file is covered by a manifest.sha256 so re-baselining is a reviewable diff, never a silent overwrite"
  artifacts:
    - path: "scripts/setfit_fixtures/generate_fixtures.py"
      provides: "Extended generator: pair-count fixture families emitted from the hash-locked venv"
      contains: "setfit_reference"
    - path: "crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256"
      provides: "Integrity manifest over all pair-count fixtures"
    - path: "crates/aprender-contrastive-data/tests/reference_fixtures.rs"
      provides: "Rust-side manifest verification + fixture deserialization used by plan 02-07's count tests"
      contains: "manifest.sha256"
  key_links:
    - from: "crates/aprender-contrastive-data/tests/reference_fixtures.rs"
      to: "crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256"
      via: "in-test digest verification (Phase 1 D-13 pattern)"
      pattern: "sha256"
---

<objective>
Extend the Phase 1 fixture generator to emit RNG-independent SetFit pair-count reference fixtures
from the hash-locked `setfit==1.1.3` environment, recording MEASURED semantics (D-23, Pitfall 1),
alongside a parallel family of Aprender-contracted counts — plus the Rust-side integrity verifier.

Purpose: D-22 accepts that pair identities cannot match Python's RNG, so the evidence strategy is
invariant parity: counts, balance, class-correctness, self-pair/orientation facts, and singleton
behavior on hand-enumerable layouts. Plan 02-07's pair tests consume these fixtures; without this
plan its "matches the reference" claims would be assertions about documentation the pinned
implementation contradicts.
Output: two committed fixture families + manifest.sha256 + a Rust verifier test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generate_fixtures.py with measured + contracted pair-count families</name>
  <files>scripts/setfit_fixtures/generate_fixtures.py, crates/aprender-contrastive-data/tests/setfit_reference/</files>
  <read_first>
    - scripts/setfit_fixtures/generate_fixtures.py (main() sectioning at ~line 352, manifest writer at ~lines 913–928, FATAL-on-inconsistency style)
    - scripts/setfit_fixtures/README + pyproject (the exact uv invocation convention for the hash-locked venv)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md "Measured reference behavior" code example (the exact numbers below come from it) and Pitfalls 1–2
  </read_first>
  <action>
    Add a new section to generate_fixtures.py emitting JSON fixtures into
    `crates/aprender-contrastive-data/tests/setfit_reference/` (a NEW output dir alongside the
    Phase 1 one — do not touch the Phase 1 fixtures or their manifest). Two families, clearly
    separated by filename prefix:

    Family 1 — `setfit_measured_*.json`: run the pinned `setfit.sampler.ContrastiveDataset`
    (sampling_strategy="oversampling", multilabel=False) and RECORD what it does, per layout:
    - layout [8,4,8] (the docs' own worked example): expect stored pos 82 (62 + 20 self-pairs),
      neg 128, len_pos = len_neg = 128, total 256; orientation-duplicate count 0
    - singleton layout (labels [0,0,0,0,1] — four of class 0, one of class 1): stored pos 11
      including 5 self-pairs (the singleton class SELF-PAIRS in the reference), total len 22
    - layout [4,1]: total epoch length 22 (the Pitfall 2 divergence case)
    - balanced [8,8,8] and [64,64,64]: epoch lengths 384 and 24,576
    - max_pairs=100 on [8,4,8]: stored exactly 50 pos + 50 neg (max_pairs // 2 each)
    Each fixture also records: setfit version "1.1.3", the note that shuffle uses a HARDCODED
    internal RandomState(42) ignoring the trainer seed, and that enumeration materializes the full
    O(N²) triangle regardless of max_pairs. If any measured number differs from the expected
    values above, FAIL FATALLY with both numbers (the generator style) — that would mean the venv
    is not the pinned one.

    Family 2 — `aprender_contracted_*.json`: for the SAME layouts, the closed-form Aprender
    counts (self-pairs EXCLUDED, D-14): positive_capacity Σ C(n_k,2), negative_capacity
    Σ_{j<k} n_j·n_k, default_epoch_budget 2·max(...). Worked values that MUST appear:
    [8,4,8] → pos 62 / neg 128 / budget 256; [8,8,8] → budget 384; [64,64,64] → budget 24,576;
    [4,1] → budget 12 (vs measured 22 — the divergence pair); singleton class → 0 positive
    capacity (NegativesOnly falls out of C(1,2)=0). Each file names the three-clause deviation:
    identities sampled not enumerated; count capped above N; self-pairs excluded whereas pinned
    setfit 1.1.3 includes the diagonal contradicting its own docs. Never attribute the exclusion
    to SetFit (PF-008).

    Write `crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256` covering every
    emitted file (sorted, the Phase 1 writer pattern) and self-verify with `shasum -a 256 -c`.
    Run the generator via the venv's documented invocation and commit the emitted fixtures.
  </action>
  <verify>
    <automated>shasum -a 256 -c crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256 --quiet && ls crates/aprender-contrastive-data/tests/setfit_reference/ | grep -c "setfit_measured"</automated>
  </verify>
  <acceptance_criteria>
    - tests/setfit_reference/ contains ≥1 `setfit_measured_*.json` and ≥1 `aprender_contracted_*.json` per layout listed above
    - The [8,4,8] measured fixture contains stored-pos 82 and total 256; the contracted fixture contains pos 62 / budget 256
    - The [4,1] pair of fixtures shows 22 (measured) vs 12 (contracted) — the divergence is data
    - manifest.sha256 exists, covers all fixture files, and `shasum -a 256 -c` passes from the fixture dir
    - `git diff scripts/setfit_fixtures/generate_fixtures.py` shows only additive changes (Phase 1 sections untouched)
  </acceptance_criteria>
  <done>Both fixture families are committed with integrity manifests, recording measured reality and contracted policy side by side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rust-side fixture integrity verifier + typed fixture model</name>
  <files>crates/aprender-contrastive-data/tests/reference_fixtures.rs</files>
  <read_first>
    - crates/aprender-core/tests/setfit_conformance.rs lines ~1199–1210 (the manifest verification pattern to copy)
    - The fixture JSONs emitted by Task 1 (field names are the deserialization contract)
  </read_first>
  <behavior>
    - Test 1: every file listed in manifest.sha256 exists and its SHA-256 matches (fail naming the drifted file)
    - Test 2: every fixture file in the dir is listed in the manifest (no orphan fixtures)
    - Test 3: each measured fixture deserializes into a typed struct (serde, deny_unknown_fields) exposing layout, stored_pos, stored_neg, total, self_pair_count
    - Test 4: each contracted fixture deserializes exposing layout, positive_capacity, negative_capacity, default_epoch_budget, deviation clauses (length 3)
    - Test 5: the [4,1] divergence is asserted: measured total 22 != contracted budget 12
  </behavior>
  <action>
    Create tests/reference_fixtures.rs (integration test — fs use is fine here, tests are outside
    the D-04 library boundary). Define the two serde structs matching Task 1's JSON schema; they
    are the SAME structs plan 02-07's count tests will import (make them `pub` in the test file or
    a shared `tests/common/` module so 02-07 reuses rather than redefines). Implement the five
    behavior tests. Use sha2 for digests. Selectable via
    `cargo test -p aprender-contrastive-data --test reference_fixtures`.
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test reference_fixtures</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior tests pass
    - The test fails (verified once by temporarily corrupting a byte in one fixture, then reverting) when a fixture drifts from the manifest — record the observed failure in the SUMMARY
    - The typed fixture structs use `deny_unknown_fields`
  </acceptance_criteria>
  <done>Fixture integrity is machine-checked in every cargo test run, and 02-07 has a typed model to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python venv → committed fixtures | Reference numbers cross from the pinned environment into repo truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | committed fixture drift | mitigate | manifest.sha256 + in-test digest verification (Task 2); re-baselining is a reviewable manifest diff |
| T-02-12 | Repudiation | fixtures encoding docs instead of behavior | mitigate | Generator FAILS FATALLY if measured numbers differ from the recorded expectations; families are prefix-separated so a doc-derived number cannot masquerade as measured |
| T-02-SC | Tampering | package installs | accept | Generator runs in the existing hash-locked uv venv (Phase 1 D-12); no new installs |
</threat_model>

<verification>
- `shasum -a 256 -c` passes in tests/setfit_reference/
- `cargo test -p aprender-contrastive-data --test reference_fixtures` green
- Phase 1 fixture manifest untouched (`git diff --stat crates/aprender-core/tests/fixtures/` empty)
</verification>

<success_criteria>
- Measured-vs-contracted evidence base exists for every layout plan 02-07 tests against
- The D-15/F2 deviation is recorded as committed data with integrity protection
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-04-SUMMARY.md` when done
</output>
