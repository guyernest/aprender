---
phase: 02-deterministic-pair-and-data-protocol
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - scripts/setfit_fixtures/generate_fixtures.py
  - crates/aprender-contrastive-data/tests/setfit_reference/
  - crates/aprender-contrastive-data/tests/common/mod.rs
  - crates/aprender-contrastive-data/tests/reference_fixtures.rs
autonomous: true
requirements: [DATA-04, DATA-05]

must_haves:
  truths:
    - "Committed fixtures record the pinned setfit 1.1.3 sampler's MEASURED behavior (self-pairs included, hardcoded internal seed 42, max_pairs halving, O(N^2) enumeration) — never its documentation, which the measurement contradicts (RESEARCH Finding F2)"
    - "A separate fixture family records Aprender's CONTRACTED counts (self-pairs excluded, closed-form capacities) so the D-15 deviation is visible as data, not prose"
    - "The degenerate-layout divergence (layout [4,1]: measured epoch length 22 vs contracted budget 12) is present in both families so it can never be silently papered over"
    - "Every fixture is covered by manifest.sha256 AND the canonical integrity check is the Rust verifier, which resolves manifest paths relative to the manifest file — not a shell command whose result depends on the caller's working directory"
    - "Each fixture records the reproducibility triple: setfit version, the uv lockfile SHA-256, and the uv version — a version string alone does not identify the environment that produced the number"
    - "Shared fixture models live in tests/common/mod.rs, because each Rust integration-test file is its own crate and cannot import public items from another one"
  artifacts:
    - path: "scripts/setfit_fixtures/generate_fixtures.py"
      provides: "Extended generator: pair-count fixture families emitted from the hash-locked venv into a temp dir, then compared before replacing committed files"
      contains: "setfit_reference"
    - path: "crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256"
      provides: "Integrity manifest over all pair-count fixtures, with paths relative to this file"
    - path: "crates/aprender-contrastive-data/tests/common/mod.rs"
      provides: "Shared typed fixture models importable by every integration test in this crate"
      contains: "deny_unknown_fields"
    - path: "crates/aprender-contrastive-data/tests/reference_fixtures.rs"
      provides: "Canonical Rust-side manifest verification consumed by plans 02-07 and 02-08"
      contains: "manifest.sha256"
  key_links:
    - from: "crates/aprender-contrastive-data/tests/reference_fixtures.rs"
      to: "crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256"
      via: "in-test digest verification with manifest-relative path resolution (Phase 1 D-13 pattern)"
      pattern: "sha256"
    - from: "crates/aprender-contrastive-data/tests/common/mod.rs"
      to: "crates/aprender-contrastive-data/tests/setfit_reference/"
      via: "typed serde models deserializing the emitted fixture JSON; reused by 02-07 and 02-08 via `mod common;`"
      pattern: "MeasuredFixture|ContractedFixture"
---

<objective>
Extend the Phase 1 fixture generator to emit RNG-independent SetFit pair-count reference fixtures
from the hash-locked `setfit==1.1.3` environment, recording MEASURED semantics (D-23, Pitfall 1),
alongside a parallel family of Aprender-contracted counts — plus shared typed fixture models and
the Rust-side integrity verifier that is the canonical check.

Purpose: D-22 accepts that pair identities cannot match Python's RNG, so the evidence strategy is
invariant parity: counts, balance, class-correctness, self-pair/orientation facts, and singleton
behavior on hand-enumerable layouts. Plans 02-07 and 02-08 consume these fixtures; without this
plan their "matches the reference" claims would be assertions about documentation the pinned
implementation contradicts.
Output: two committed fixture families + manifest.sha256 + shared fixture models + a Rust verifier.

## Review corrections incorporated (02-REVIEWS.md)

Both reviewers called this the best-isolated plan; the corrections are test-layout repairs, not
design changes.

- **F15 / Codex 02-04 MEDIUM — public structs in one integration test cannot be imported by
  another.** Each file under `tests/` is a separate crate. The typed fixture models move to
  `tests/common/mod.rs`, included with `mod common;` from `reference_fixtures.rs` and later from
  plan 02-07's and 02-08's integration tests. (The repo's alternative precedent is the
  `#[path = "..."]` include used by `crates/aprender-core/tests/setfit_conformance.rs`; the
  `tests/common/mod.rs` idiom is preferred because cargo already treats that path as a module
  rather than a test target.)
- **F15 / Codex 02-04 MEDIUM — `shasum -c` resolves listed paths relative to the CWD, and the
  automated command and the acceptance text assumed different directories.** The Rust verifier
  becomes the canonical, cross-platform check; it resolves every manifest entry relative to the
  manifest file's own directory. `shasum -a 256 -c` is retained only as a documented convenience
  with its required `cd` stated explicitly, and it is NOT the `<automated>` command.
- **F15 / Codex 02-04 LOW — "singleton class → zero positive capacity" was imprecise.** Corrected
  everywhere to "a singleton class contributes zero positive capacity"; `[4,1]` still yields six
  positives from the four-member class.
- **F15 / Codex 02-04 LOW — "additive only" was too restrictive.** Relaxed to an outcome
  condition: the Phase 1 emitted fixtures and their manifest must be byte-identical after the run;
  shared generator helpers may be refactored as long as that holds.
- **F15 / Codex 02-04 suggestion — record the Python environment, not just the package version.**
  Each fixture now records `setfit_version`, `uv_lock_sha256`, and `uv_version`.
- **F15 / Codex 02-04 suggestion — generate into a temp directory and compare before replacing.**
  The generator writes to a temp dir, diffs against the committed tree, and reports the diff;
  replacement is deliberate, so a re-baseline is always a reviewable change.
- **F1 (consequence) — the K≈N adversarial layout gets a contracted fixture here** so plan 02-08's
  capacity gate has a reference row to assert against. A three-class fixture set could never
  expose the O(K²) sampler the review rejected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generate_fixtures.py with measured + contracted pair-count families</name>
  <files>scripts/setfit_fixtures/generate_fixtures.py, crates/aprender-contrastive-data/tests/setfit_reference/</files>
  <read_first>
    - scripts/setfit_fixtures/generate_fixtures.py (main() sectioning at ~line 352, manifest writer at ~lines 913-928, FATAL-on-inconsistency style)
    - scripts/setfit_fixtures/README and pyproject and uv.lock (the exact uv invocation convention for the hash-locked venv, and the lockfile whose SHA-256 each fixture must record)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md "Measured reference behavior" code example (the exact numbers below come from it) and Pitfalls 1-2
    - contracts/contrastive-pair-protocol-v1.yaml equations `positive_capacity`, `negative_capacity`, `default_epoch_budget`, `pair_stream_degenerate_policy` (the contracted numbers must agree with the contract authored in 02-02)
  </read_first>
  <action>
    Add a new section to generate_fixtures.py emitting JSON fixtures for
    `crates/aprender-contrastive-data/tests/setfit_reference/` (a NEW output dir alongside the
    Phase 1 one — do not touch the Phase 1 fixtures or their manifest). **Write first into a temp
    directory**, then diff against the committed tree and print the diff; only replace the
    committed files when the run is a deliberate re-baseline (review finding F15). Two families,
    separated by filename prefix.

    Family 1 — `setfit_measured_*.json`: run the pinned `setfit.sampler.ContrastiveDataset`
    (sampling_strategy="oversampling", multilabel=False) and RECORD what it does, per layout:
    - layout [8,4,8] (the docs' own worked example): stored pos 82 (62 + 20 self-pairs), neg 128,
      len_pos = len_neg = 128, total 256; orientation-duplicate count 0
    - singleton layout (labels [0,0,0,0,1] — four of class 0, one of class 1): stored pos 11
      including 5 self-pairs (the singleton class SELF-PAIRS in the reference), total len 22
    - layout [4,1]: total epoch length 22 (the Pitfall 2 divergence case)
    - balanced [8,8,8] and [64,64,64]: epoch lengths 384 and 24,576
    - max_pairs=100 on [8,4,8]: stored exactly 50 pos + 50 neg (max_pairs // 2 each)
    - **adversarial all-singleton layout [1]*32 (K = N = 32)** — record the reference's epoch
      length for it. This is the K≈N layout plan 02-08 uses to expose an O(K²) sampler; a
      three-class fixture set cannot (review finding F1).
    Each fixture also records the reproducibility triple `setfit_version: "1.1.3"`,
    `uv_lock_sha256` (SHA-256 of `scripts/setfit_fixtures/uv.lock`), `uv_version`; plus the notes
    that shuffle uses a HARDCODED internal `RandomState(42)` ignoring the trainer seed, and that
    enumeration materializes the full O(N²) index triangle regardless of `max_pairs`. If any
    measured number differs from the expected values above, FAIL FATALLY printing both numbers
    (the generator's existing style) — that would mean the venv is not the pinned one.

    Family 2 — `aprender_contracted_*.json`: for the SAME layouts, the closed-form Aprender counts
    (self-pairs EXCLUDED, D-14): `positive_capacity` Σ C(n_k,2), `negative_capacity` Σ_{j<k} n_j·n_k,
    `default_epoch_budget` 2·max(...). Worked values that MUST appear: [8,4,8] → pos 62 / neg 128 /
    budget 256; [8,8,8] → budget 384; [64,64,64] → budget 24,576; [4,1] → pos 6 / neg 4 /
    budget 12 (vs measured 22 — the divergence pair); [1]*32 → **pos 0 / neg 496 / budget 992**,
    with `degenerate_case: "negatives_only"` recorded (a singleton class CONTRIBUTES ZERO POSITIVE
    CAPACITY — the [4,1] layout still yields six positives from its four-member class; review
    finding F15 wording correction). Each file names the three-clause deviation: identities
    sampled not enumerated; count capped above N; self-pairs excluded whereas pinned setfit 1.1.3
    includes the diagonal contradicting its own docs. Never attribute the exclusion to SetFit
    (PF-008). Copy the clause wording from `contracts/contrastive-pair-protocol-v1.yaml`
    (OBLIG-CPP-DEVIATION-DECLARED) so contract and fixtures cannot drift.

    Write `crates/aprender-contrastive-data/tests/setfit_reference/manifest.sha256` covering every
    emitted file, sorted, using the Phase 1 writer pattern with **paths recorded relative to the
    manifest file's own directory** (this is what makes the Rust verifier's resolution rule
    well-defined — review finding F15). Run the generator via the venv's documented invocation and
    commit the emitted fixtures.
  </action>
  <verify>
    <automated>cd crates/aprender-contrastive-data/tests/setfit_reference && shasum -a 256 -c manifest.sha256 --quiet > /tmp/fixman.log 2>&1; rc=$?; m=$(ls | grep -c '^setfit_measured'); c=$(ls | grep -c '^aprender_contracted'); [ "$rc" -eq 0 ] && [ "$m" -ge 6 ] && [ "$c" -ge 6 ]</automated>
  </verify>
  <acceptance_criteria>
    - tests/setfit_reference/ contains one `setfit_measured_*.json` and one `aprender_contracted_*.json` per layout listed above, including the `[1]*32` adversarial layout
    - The [8,4,8] measured fixture contains stored-pos 82 and total 256; its contracted counterpart contains pos 62 / neg 128 / budget 256
    - The [4,1] pair of fixtures shows 22 (measured) vs 12 (contracted) — the divergence is data, not prose
    - The [1]*32 contracted fixture contains positive_capacity 0, negative_capacity 496, budget 992, and `degenerate_case: "negatives_only"`
    - Every fixture contains `setfit_version`, `uv_lock_sha256`, and `uv_version`
    - manifest.sha256 exists, covers every fixture file, records paths relative to itself, and `shasum -a 256 -c` passes when run FROM the fixture directory (the `cd` is part of the command, stated explicitly)
    - `git status --porcelain crates/aprender-core/tests/fixtures/` is empty — the Phase 1 fixtures and their manifest are byte-identical after the run (the outcome condition that replaced "additive only")
    - The generator's temp-dir-then-diff path is exercised at least once and the diff output is recorded in the SUMMARY
  </acceptance_criteria>
  <done>Both fixture families are committed with integrity manifests and environment attestation, recording measured reality and contracted policy side by side, including the K≈N adversarial layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared typed fixture models in tests/common + the canonical Rust integrity verifier</name>
  <files>crates/aprender-contrastive-data/tests/common/mod.rs, crates/aprender-contrastive-data/tests/reference_fixtures.rs</files>
  <read_first>
    - crates/aprender-core/tests/setfit_conformance.rs lines ~1199-1210 (the manifest verification pattern to copy) and its `#[path = ...]` module-include style near the top
    - The fixture JSONs emitted by Task 1 (field names are the deserialization contract)
    - Cargo's integration-test layout rules: every `tests/*.rs` is its own crate; `tests/common/mod.rs` is a module, not a test target (this is the whole reason for this task's structure — review finding F15)
  </read_first>
  <behavior>
    - Test 1: every file listed in manifest.sha256 exists and its SHA-256 matches; paths are resolved relative to the MANIFEST FILE's directory (computed from `env!("CARGO_MANIFEST_DIR")`), so the test result does not depend on the process working directory. Failure names the drifted file.
    - Test 2: every fixture file in the directory is listed in the manifest (no orphan fixtures)
    - Test 3: each measured fixture deserializes into `common::MeasuredFixture` (serde, deny_unknown_fields) exposing layout, stored_pos, stored_neg, total, self_pair_count, setfit_version, uv_lock_sha256, uv_version
    - Test 4: each contracted fixture deserializes into `common::ContractedFixture` exposing layout, positive_capacity, negative_capacity, default_epoch_budget, degenerate_case (Option), and deviation clauses (exactly 3)
    - Test 5: the [4,1] divergence is asserted — measured total 22 != contracted budget 12
    - Test 6: the [1]*32 adversarial contracted fixture asserts positive_capacity == 0 and negative_capacity == 496, so plan 02-08's K≈N gate has a reference row rather than a hand-typed constant
    - Test 7: `common::load_measured()` / `common::load_contracted()` return every fixture keyed by layout, and the loaders are the surface plans 02-07/02-08 consume (proven by the loaders being used, not merely defined)
  </behavior>
  <action>
    Create `tests/common/mod.rs` holding the two `#[derive(Deserialize)] #[serde(deny_unknown_fields)]`
    fixture models, the manifest-relative path resolution helper (`fixture_dir()` built from
    `env!("CARGO_MANIFEST_DIR")`), the digest verifier, and the `load_measured` / `load_contracted`
    loaders. These are the SAME items plans 02-07 and 02-08 will use via `mod common;` — do not
    define them inside `reference_fixtures.rs`, because a public item in one integration-test
    crate is not importable from another (review finding F15).

    Create `tests/reference_fixtures.rs` with `mod common;` and the seven behavior tests. Filesystem
    use is fine here — integration tests are outside the D-04 library boundary, and
    `make contrastive-data-boundary` scans only `src/`. Use `sha2` for digests (add it as a
    dev-dependency if it is not already inherited). Selectable via
    `cargo test -p aprender-contrastive-data --test reference_fixtures`.
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data --test reference_fixtures > /tmp/t-fix.log 2>&1; rc=$?; [ "$rc" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - All seven behavior tests pass, run from the repository root AND from `crates/aprender-contrastive-data/` (run both; the results must be identical — this is the proof that path resolution is manifest-relative, review finding F15)
    - The test fails when a fixture drifts: temporarily corrupt one byte in a fixture, observe the failure naming that file, record the observed output in the SUMMARY, then revert
    - `tests/common/mod.rs` exists, contains both fixture structs with `deny_unknown_fields`, and `tests/reference_fixtures.rs` contains `mod common;`
    - No fixture struct is defined inside `reference_fixtures.rs` (grep: the struct definitions appear only in `tests/common/mod.rs`)
    - `make contrastive-data-boundary` still exits 0 (the boundary gate scans src/ only; test-tree fs use is correct and intended)
  </acceptance_criteria>
  <done>Fixture integrity is machine-checked in every cargo test run by a working-directory-independent verifier, and 02-07/02-08 have importable typed models rather than duplicated definitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python venv → committed fixtures | Reference numbers cross from the pinned environment into repo truth |
| committed fixtures → downstream assertions | A drifted fixture would silently re-baseline plans 02-07/02-08's claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | committed fixture drift | mitigate | manifest.sha256 + in-test digest verification with manifest-relative resolution (Task 2); re-baselining is a reviewable manifest diff produced by the generator's temp-dir compare |
| T-02-12 | Repudiation | fixtures encoding docs instead of behavior | mitigate | Generator FAILS FATALLY if measured numbers differ from the recorded expectations; families are prefix-separated so a doc-derived number cannot masquerade as measured |
| T-02-39 | Repudiation | unreproducible reference environment | mitigate | Each fixture records setfit version + uv lockfile SHA-256 + uv version, so "1.1.3" alone cannot stand in for an environment (review finding F15) |
| T-02-SC | Tampering | package installs | accept | Generator runs in the existing hash-locked uv venv (Phase 1 D-12); no new installs |
</threat_model>

<verification>
- `cargo test -p aprender-contrastive-data --test reference_fixtures` green from BOTH the repo root and the crate directory
- `shasum -a 256 -c manifest.sha256` passes when run from `crates/aprender-contrastive-data/tests/setfit_reference/`
- Phase 1 fixture tree byte-identical: `git status --porcelain crates/aprender-core/tests/fixtures/` empty
</verification>

<success_criteria>
- Measured-vs-contracted evidence base exists for every layout plans 02-07 and 02-08 test against, including the K≈N adversarial layout
- The D-15 / RESEARCH-F2 deviation is recorded as committed data with integrity protection and environment attestation
- Shared fixture models are importable across integration tests instead of being redefined
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-04-SUMMARY.md` when done
</output>
</content>
