---
phase: 02-deterministic-pair-and-data-protocol
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-03]
files_modified:
  - crates/apr-cli/src/commands/data_tweeteval.rs
  - contracts/tweet-eval-stance-benchmark-v1.yaml
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "data_tweeteval.rs is a thin TweetEval adapter over aprender-contrastive-data: URLs, filenames, expected counts, label names, clap glue, and the ureq fetch stay; parsing, hashing, validation, and dedup move to crate calls"
    - "Every pre-existing baseline test — including FALSIFY-TWEET-EVAL-006 (revision honesty) — passes UNCHANGED against the relocated code"
    - "The JSONL row output is byte-identical to the D-06 baseline; the manifest gains the exclusion record and bumps schema_version 1 → 2"
    - "Preparing the real pinned dataset excludes exactly one duplicate group (train:70 ≡ validation:3, both `none`) and the reduced pools (none 158 / against 319 / favor 109) still supply 64 shots"
  artifacts:
    - path: "crates/apr-cli/src/commands/data_tweeteval.rs"
      provides: "Thin adapter: fetch/fs/clap in CLI, model/validation/hashing/dedup in crate"
      contains: "aprender_contrastive_data"
    - path: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      provides: "Dataset half of DATA-02: duplicate/conflicting-role obligations + real-duplicate falsification test"
      contains: "e3af840b"
  key_links:
    - from: "crates/apr-cli/src/commands/data_tweeteval.rs"
      to: "crates/aprender-contrastive-data/src/split.rs"
      via: "read file bytes once → Split::from_jsonl_bytes / crate ingest (hash-from-same-buffer moves INTO the crate call)"
      pattern: "from_jsonl_bytes|ingest"
    - from: "crates/apr-cli/src/commands/data_tweeteval.rs"
      to: "crates/aprender-contrastive-data/src/dedup.rs"
      via: "prepare-time cross_split_exclusions recorded into the benchmark manifest"
      pattern: "cross_split_exclusions"
---

<objective>
Refactor the committed D-06 baseline `data_tweeteval.rs` into a thin filesystem/network adapter
over `aprender-contrastive-data` (the D-05 ETL seam), integrate prepare-time cross-split
duplicate exclusion (D-18), add the real-duplicate golden, and grow the tweet-eval contract's
dataset half of DATA-02.

Purpose: Phase 3 and Phase 5 must consume typed splits without depending on CLI modules (OPS-01
applied early). The relocation is a reviewable diff against the tracked baseline — the exact
reason D-06 landed first.
Output: relocated adapter with byte-parity proof, live dedup on real data, grown contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate the generic core — data_tweeteval.rs becomes a thin adapter</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/data_tweeteval.rs IN FULL (the committed baseline — every behavior listed below is guarded by an existing test in its ~lines 562–813)
    - crates/aprender-contrastive-data/src/{schema.rs,split.rs,hash.rs,dedup.rs} (the crate APIs delivered by 02-03 — use them, do not duplicate)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md "data_tweeteval.rs thin-adapter refactor" section (the survival checklist)
  </read_first>
  <behavior>
    - ALL pre-existing data_tweeteval tests pass with test code unmodified (additions allowed, edits not)
    - A byte-parity test proves the refactored path emits train/validation/test JSONL rows byte-identical to the baseline fixture output (reuse write_fixture_split/write_canonical_fixture infra)
    - The benchmark manifest now contains an `exclusions` section (excluded IDs, groups, reduced per-class pools, normalization version) and `schema_version: 2`
    - A synthetic fixture with a cross-split duplicate produces a non-empty exclusion record in the manifest; a clean fixture produces an empty one
  </behavior>
  <action>
    Refactor to the D-05 seam. KEEPS (CLI-side): CANONICAL_REVISION const, URLs/filenames, ureq
    fetch, `--source` local-dir path, validate_revision 40-hex check, expected counts and
    LABEL_NAMES (passed into the crate as SplitDeclaration values), clap wiring,
    rollback-on-partial-write (written: Vec<PathBuf> + remove_all), create_new no-clobber without
    --force, stale validation.jsonl removal on --force setfit re-prepare, JSON output. MOVES to
    crate calls: row parsing/validation ladder, per-split hashing, JSONL encoding
    (crate encode_jsonl for byte-identical rows), dataset fingerprint, and NEW: prepare-time
    `cross_split_exclusions` whose ExclusionRecord lands in the manifest.

    Critical disciplines that must survive the seam (Pitfall 7):
    - Read each source file exactly ONCE; pass those bytes to the crate boundary which hashes and
      parses THE SAME buffer. Never two fs::read calls per source file. Keep the load-bearing
      comment with the code.
    - `revision_verified = downloaded.is_some()` honesty is computed in the CLI (the crate cannot
      know provenance) — no constructor may take it as an unprovenance'd caller bool beyond the
      existing derivation. FALSIFY-TWEET-EVAL-006 must pass unchanged.
    - The setfit compatibility profile maps to the crate's Compatibility profile declarations
      (merged test ingests as CompatibilityTest role; NO validation declaration exists for it —
      D-19).
    Manifest: add the exclusions section; bump schema_version 1 → 2 (manifest bytes change;
    JSONL row bytes must NOT — that is the Runtime State Inventory contract). Map crate
    ContrastiveDataError variants into CliError::ValidationFailed preserving the message detail.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --lib data_tweeteval</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --lib data_tweeteval` exits 0 with every pre-existing test name still present (compare test list before/after: `cargo test -p apr-cli --lib data_tweeteval -- --list`)
    - `git diff <baseline>..HEAD -- crates/apr-cli/src/commands/data_tweeteval.rs` shows net deletion of parsing/hash logic (the file shrinks; state the before/after line counts in the SUMMARY)
    - data_tweeteval.rs imports `aprender_contrastive_data` and contains NO local sha256 helper and NO local per-row validation ladder
    - The byte-parity test exists and passes; the manifest test asserts `"schema_version": 2` and the exclusions section
    - `grep -c "fs::read" crates/apr-cli/src/commands/data_tweeteval.rs` shows each source file read once (no doubled reads; verify against the read-once loop)
  </acceptance_criteria>
  <done>The TweetEval command is a thin adapter; generic model/validation/hashing/dedup live in the crate; every baseline guarantee still holds and is proven by the unchanged tests.</done>
</task>

<task type="auto">
  <name>Task 2: Real-duplicate golden + tweet-eval contract growth (dataset half of DATA-02)</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs, contracts/tweet-eval-stance-benchmark-v1.yaml</files>
  <read_first>
    - crates/apr-cli/src/commands/data_tweeteval.rs opt-in network test pattern (~lines 803–812: `#[ignore = "opt-in network test..."]`)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md Finding F1 / Pitfall 3 (the measured duplicate facts)
    - contracts/tweet-eval-stance-benchmark-v1.yaml (as restructured by plan 02-01)
  </read_first>
  <action>
    1. Add an opt-in network test (same `#[ignore]` convention as
       pinned_upstream_satisfies_the_dataset_contract) that prepares the REAL pinned dataset and
       asserts the exclusion record contains EXACTLY one duplicate group: train:70 ≡ validation:3,
       exact SHA-256 e3af840b5398272c89e5e1e3e1730c26b0f5bc856d3d46531a2a64dd8844a2c3, both
       labeled `none`; reduced pools none=158, against=319, favor=109, each ≥ 64. This is the
       live golden that makes D-18's design exercised by real data on every opt-in run — an empty
       excluded_ids on real data is the Pitfall 3 warning sign and must FAIL this test.
    2. Grow the contract's dataset half of DATA-02: add obligations
       OBLIG-TWEET-EVAL-DUP-EXCLUSION (canonical preparation records the known real duplicate
       group and the reduced pools above) and OBLIG-TWEET-EVAL-CONFLICTING-ROLES (a source file
       claiming two roles is a typed error), plus falsification tests binding them — the
       real-duplicate one referencing the opt-in test command
       (`cargo test -p apr-cli --lib data_tweeteval -- --ignored`), the conflicting-role and
       label-conflict-duplicate ones referencing the synthetic-fixture tests from Task 1 / plan
       02-03. Record duplicates by HASH, never by tweet text (licensing — the contract must not
       contain tweet content).
    3. Run `pv diff contracts/tweet-eval-stance-benchmark-v1.yaml HEAD` and apply the suggested
       semver bump; `pv validate` must stay green (pv only — never yq/bash).
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml && cargo test -p apr-cli --lib data_tweeteval</automated>
  </verify>
  <acceptance_criteria>
    - The opt-in test exists with the exact expected duplicate facts (grep the file for "e3af840b" and "158")
    - Contract contains OBLIG-TWEET-EVAL-DUP-EXCLUSION and OBLIG-TWEET-EVAL-CONFLICTING-ROLES and no tweet text
    - `pv validate` exits 0; metadata version bumped per pv diff suggestion
    - Offline test suite still green (the network test is #[ignore]-gated; CI stays offline per SAFE-02)
  </acceptance_criteria>
  <done>The one real duplicate is a permanent executable golden, and the dataset-specific DATA-02 obligations are contract-bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw.githubusercontent.com → CLI | Pinned-revision fetch (HTTPS, 40-hex revision validated) |
| local --source dir → CLI | User bytes claiming to be the pinned dataset |
| CLI → crate | Bytes cross into the validated boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | dataset substitution across the refactor | mitigate | Count contracts + hash-from-same-buffer preserved through the seam; byte-parity golden vs baseline (Task 1) |
| T-02-18 | Spoofing/Repudiation | local dir attested as pinned revision | mitigate | revision_verified derivation stays CLI-side and FALSIFY-TWEET-EVAL-006 passes unchanged |
| T-02-19 | Information Disclosure | tweet text entering repo/contract | mitigate | Duplicates recorded by SHA-256 only (Task 2); no vendored text |
| T-02-20 | Elevation (data) | compat-profile leakage into selection | mitigate | Compat profile maps to CompatibilityTest role with no validation declaration (Task 1, D-19) |
</threat_model>

<verification>
- `cargo test -p apr-cli --lib data_tweeteval` green offline
- Opt-in network test green when run with network (record its output in the SUMMARY if run)
- `pv validate` green on the grown contract
- `make tier2` green
</verification>

<success_criteria>
- DATA-01 fully delivered through the seam: acquisition, canonical JSONL, hashes, provenance, no committed text
- DATA-02 fully delivered for this dataset: all typed failures incl. duplicates/conflicting roles, with the real duplicate as a live golden
- The relocation is a clean reviewable diff against the D-06 baseline
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-06-SUMMARY.md` when done
</output>
