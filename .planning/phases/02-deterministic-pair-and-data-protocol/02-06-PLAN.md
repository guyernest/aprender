---
phase: 02-deterministic-pair-and-data-protocol
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-03]
files_modified:
  - crates/apr-cli/src/commands/data_tweeteval.rs
  - crates/aprender-contrastive-data/src/attestation.rs
  - contracts/tweet-eval-stance-benchmark-v1.yaml
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "The D-05 seam is honored EXACTLY as locked: paired *_text.txt / *_labels.txt decoding, label integer parsing, LABEL_NAMES mapping, ID minting, URLs, filenames, expected counts, clap args and the ureq fetch stay CLI-side; the generic model — typed rows, split roles, per-row hashes, dataset fingerprint, cross-split duplicate detection, JSONL read/write — moves into the crate"
    - "Every pre-existing baseline test — including FALSIFY-TWEET-EVAL-006 (revision honesty) — passes UNCHANGED against the relocated code"
    - "The JSONL row output is byte-identical to the D-06 baseline; the benchmark manifest gains a crate-owned attestation section plus the exclusion record and bumps schema_version 1 → 2, with an explicit rejection policy for version 1 (no silent migration)"
    - "PreparedDataset::<P>::from_attested_bytes is the crate-owned boundary that validates profile, schema version, per-split hashes, fingerprint, and exclusion record BEFORE any canonical split is exposed — a mixed or forged directory cannot pass row-level checks alone"
    - "Preparing the real pinned dataset excludes exactly ONE coalesced duplicate group (train:70 ≡ validation:3, both `none`) and the reduced pools (none 158 / against 319 / favor 109) still supply 64 shots — D-27's exclude-and-record half, exercised by real data"
  artifacts:
    - path: "crates/apr-cli/src/commands/data_tweeteval.rs"
      provides: "Thin adapter: fetch/fs/clap/paired-file decoding in CLI, typed model/validation/hashing/dedup/JSONL in the crate"
      contains: "aprender_contrastive_data"
    - path: "crates/aprender-contrastive-data/src/attestation.rs"
      provides: "DatasetAttestation + PreparedDataset::from_attested_bytes — the crate-owned identity boundary"
      contains: "from_attested_bytes"
    - path: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      provides: "Dataset half of DATA-02: duplicate/conflicting-role obligations + real-duplicate falsification test + schema-version policy"
      contains: "e3af840b"
  key_links:
    - from: "crates/apr-cli/src/commands/data_tweeteval.rs"
      to: "crates/aprender-contrastive-data/src/prepared.rs"
      via: "decode paired files CLI-side into Vec<LabeledExample>, then PreparedDataset::<Canonical>::from_labeled_rows (D-05 seam)"
      pattern: "from_labeled_rows"
    - from: "crates/aprender-contrastive-data/src/attestation.rs"
      to: "crates/aprender-contrastive-data/src/prepared.rs"
      via: "from_attested_bytes re-derives every attested field before exposing splits"
      pattern: "PreparedDataset"
---

<objective>
Refactor the committed D-06 baseline `data_tweeteval.rs` into a thin filesystem/network adapter
over `aprender-contrastive-data` along the seam D-05 actually specifies, add the crate-owned
dataset attestation boundary that later commands must pass before touching canonical splits,
integrate prepare-time cross-split duplicate exclusion (D-18/D-27), add the real-duplicate golden,
and grow the tweet-eval contract's dataset half of DATA-02.

Purpose: Phase 3 and Phase 5 must consume typed splits without depending on CLI modules (OPS-01
applied early). The relocation is a reviewable diff against the tracked baseline — the exact
reason D-06 landed first.
Output: relocated adapter with byte-parity proof, a validating attestation boundary, live dedup on
real data, grown contract.

## Review corrections incorporated (02-REVIEWS.md)

- **Codex blocking #3 / F-seam — "the TweetEval refactor has no viable ETL seam".** Codex was
  right that the previous plan text could not be implemented: it claimed to relocate "the raw
  parsing ladder, text/label alignment, label parsing, and count validation" into a crate that
  only ingests JSONL, while upstream TweetEval ships paired `*_text.txt` / `*_labels.txt`. The
  orchestrator resolved this against locked **D-05**, which already picks the seam: the crate owns
  "labeled-example schema, typed split roles, JSONL read/write, per-row content hashes, dataset
  fingerprint, cross-split duplicate detection", and `apr-cli` keeps "URLs, filenames, expected
  counts, label names, clap args, and the `ureq` network fetch". **Paired text/label decoding is
  TweetEval-specific and stays CLI-side.** This plan's Task 1 text is corrected to match D-05
  rather than inventing a new seam: `decode_utf8`, line splitting, the text/label length check,
  label integer parsing, `LABEL_NAMES` lookup, and `{split}:{index}` ID minting remain in
  `data_tweeteval.rs`; what moves is everything downstream of `Vec<LabeledExample>`.
- **F16 / Codex 02-09 HIGH — nothing validated `benchmark-manifest.json`, its profile, source
  hashes, schema version, or split hashes, so a mixed or forged directory could pass row-level
  checks.** Task 2 adds the crate-owned `DatasetAttestation` + `from_attested_bytes` boundary that
  plan 02-09's commands must go through. It lives here because this is the plan that owns the
  benchmark-manifest format.
- **F8 (consequence) / Codex 02-06 HIGH — "exactly one group" could become two.** Now guaranteed
  by plan 02-03's union-find coalescing; the real-data golden asserts `groups().len() == 1` and a
  pool decrement of exactly one, which is the assertion that would have failed under the old
  independent-grouping design.
- **V2 / Codex 02-06 MEDIUM — `pv diff ... HEAD` is invalid.** Task 3 materializes the old
  contract with `git show <rev>:<path> > /tmp/…` and diffs two real files.
- **F22 / Codex 02-06 MEDIUM — schema version 2 was introduced with no old-version policy.**
  Task 1 defines it explicitly: `SUPPORTED_BENCHMARK_SCHEMA_VERSIONS = [2]`; a version-1 manifest
  is `UnsupportedSchemaVersion` with a message telling the user to re-prepare. There is
  deliberately NO migration shim, because a version-1 manifest predates the exclusion record and
  any synthesized value would be an unattested guess.
- **Codex 02-06 MEDIUM — "every prior test unmodified" could block necessary assertions about the
  intentional schema change.** Relaxed precisely: no pre-existing test may be DELETED or have its
  assertions weakened, and the test-name set must not shrink; a pre-existing test that pins
  `schema_version: 1` may be updated to 2 with the change called out explicitly in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate the generic model — data_tweeteval.rs becomes a thin adapter on the D-05 seam</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/data_tweeteval.rs IN FULL (the committed baseline). Pay particular attention to `load_canonical_dataset` (~line 236, the read-once loop and `source_sha256` map), `decode_utf8` (~270), `load_split` (~280-335, the paired-file ladder that STAYS), the manifest family (~64-113), and the test block (~562-813)
    - crates/aprender-contrastive-data/src/{schema.rs,split.rs,hash.rs,dedup.rs,prepared.rs} (the crate APIs delivered by 02-03 — use them, do not duplicate)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md **D-05 verbatim** (it decides the seam) and D-27
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md "data_tweeteval.rs thin-adapter refactor" section (the survival checklist)
  </read_first>
  <behavior>
    - ALL pre-existing data_tweeteval tests pass; the test-name set does not shrink (`cargo test -p apr-cli --lib data_tweeteval -- --list` before/after)
    - A byte-parity test proves the refactored path emits train/validation/test JSONL rows byte-identical to the baseline fixture output (reuse write_fixture_split / write_canonical_fixture infra)
    - The benchmark manifest now contains a crate-owned `dataset_attestation` section and an `exclusions` section (excluded IDs, coalesced groups, reduced per-class pools, normalization version), and `schema_version: 2`
    - Reading a `schema_version: 1` manifest fails with a message naming the supported set and telling the user to re-run the prepare command with `--force` — no silent migration (review finding F22)
    - A synthetic fixture with a cross-split duplicate produces a non-empty exclusion record with exactly ONE group; a clean fixture produces an empty one
    - The setfit compatibility profile builds a `PreparedDataset<Compatibility>`; there is no code path in this file that constructs a `PreparedDataset<Canonical>` from compatibility inputs (D-19)
  </behavior>
  <action>
    Refactor to the D-05 seam — and to that seam only.

    **STAYS CLI-side (TweetEval-specific, per D-05):** `CANONICAL_REVISION`, `UPSTREAM_*`,
    `SOURCE_FILES`, `TRAIN_COUNTS`/`VALIDATION_COUNTS`/`TEST_COUNTS`, `LABEL_NAMES`,
    `FEW_SHOT_SIZES`, `BENCHMARK_SEEDS`, the `ureq` fetch, the `--source` local-dir path,
    `validate_revision`'s 40-hex check, clap wiring, rollback-on-partial-write
    (`written: Vec<PathBuf>` + remove_all), `create_new` no-clobber without `--force`, stale
    `validation.jsonl` removal on `--force` setfit re-prepare, and JSON output. **Also staying:
    the entire paired-file decoding ladder** — `decode_utf8`, `text.lines()` / `labels.lines()`,
    the text/label length mismatch check, `raw_label.trim().parse::<usize>()`, the `LABEL_NAMES`
    bounds lookup, and `{canonical_name}:{index}` ID minting. Upstream TweetEval ships
    `*_text.txt` + `*_labels.txt`; decoding that format is dataset-specific and D-05 assigns it
    here. Do not attempt to push it into the crate — the crate has no such API and adding one
    would contradict the locked seam (review: Codex blocking #3, resolved against D-05).

    **MOVES to crate calls (everything downstream of typed rows):** replace the per-row
    `StanceSample` construction result with `Vec<aprender_contrastive_data::schema::LabeledExample>`
    and hand the three vectors to
    `PreparedDataset::<Canonical>::from_labeled_rows(train, validation, test, &decls, &mut ledger)`.
    The crate now owns: duplicate-ID detection, `label_text == LABEL_NAMES[label]` agreement,
    empty-input rejection, per-class count validation against the declaration, per-row exact and
    normalized hashes, the dataset fingerprint, coalesced cross-split duplicate exclusion, and
    JSONL encoding (`PreparedDataset::encode_jsonl`, which is what keeps row bytes identical).
    Delete the local `sha256` helper for row/split content and the local per-row validation
    branches that the crate now performs; keep `sha256` only if it is still needed for the raw
    SOURCE-file hashes, which remain a CLI provenance concern.

    Critical disciplines that must survive the seam (Pitfall 7):
    - Read each source file exactly ONCE into the `raw: BTreeMap<String, Vec<u8>>` map and hash
      THOSE bytes for `source_sha256`, exactly as the baseline does. Never two `fs::read` calls
      per source file. Keep the load-bearing comment with the code.
    - `revision_verified = downloaded.is_some()` honesty is computed in the CLI (the crate cannot
      know provenance). No constructor may take it as an unprovenanced caller bool.
      FALSIFY-TWEET-EVAL-006 must pass unchanged.
    - The setfit compatibility profile builds `PreparedDataset<Compatibility>` (merged test ingests
      as the CompatibilityTest role; there is no validation declaration for it — D-19). Because
      profile is a TYPE parameter after plan 02-03, the two profiles are separate code paths that
      the compiler keeps apart.

    Manifest: add the `dataset_attestation` section (Task 2's serialized form, embedded verbatim —
    the CLI composes no JSON around it) and the `exclusions` section; bump `schema_version` 1 → 2
    and define `SUPPORTED_BENCHMARK_SCHEMA_VERSIONS = [2]` with a version-1 read returning a
    structured error naming the supported set and the remediation command (review finding F22).
    Manifest bytes change; **JSONL row bytes must NOT** — that is the Runtime State Inventory
    contract, proven by the byte-parity test. Map crate `ContrastiveDataError` variants into
    `CliError::ValidationFailed` preserving message detail.
  </action>
  <verify>
    <automated>cargo test -p apr-cli --lib data_tweeteval > /tmp/t-te.log 2>&1; a=$?; cargo check -p apr-cli > /tmp/c-cli.log 2>&1; b=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p apr-cli --lib data_tweeteval` exits 0 and the `-- --list` test-name set is a superset of the baseline's (record both lists in the SUMMARY); no pre-existing test was deleted or weakened, and any test whose `schema_version` assertion moved 1 → 2 is named explicitly in the SUMMARY
    - `git diff <baseline-commit>..HEAD -- crates/apr-cli/src/commands/data_tweeteval.rs` shows net deletion of hashing and per-row validation logic (state before/after line counts in the SUMMARY)
    - data_tweeteval.rs imports `aprender_contrastive_data`, contains NO local per-row validation ladder, and contains NO local SHA-256 of row/split CONTENT (a `sha256` over raw source-file bytes for provenance is permitted and expected)
    - data_tweeteval.rs STILL contains `decode_utf8`, the text/label length-mismatch check, `parse::<usize>()` label parsing, the `LABEL_NAMES` lookup, and `{canonical_name}:{index}` ID minting — the D-05 seam is honored, not exceeded
    - `grep -c "fs::read" crates/apr-cli/src/commands/data_tweeteval.rs` is consistent with one read per source file inside the existing read-once loop (verify against the loop, not against a bare count)
    - The byte-parity test exists and passes; the manifest test asserts `"schema_version": 2`, the `dataset_attestation` section, and the exclusions section
    - A test proves a `schema_version: 1` manifest is rejected with a message naming `[2]` and the remediation command
    - `cargo check -p apr-cli` exits 0
  </acceptance_criteria>
  <done>The TweetEval command is a thin adapter on exactly the D-05 seam; the generic model lives in the crate; every baseline guarantee still holds and is proven by the unchanged tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: attestation.rs — the crate-owned identity boundary consumers must pass before splits are exposed</name>
  <files>crates/aprender-contrastive-data/src/attestation.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/prepared.rs and src/dedup.rs and src/hash.rs (plan 02-03 outputs — `PreparedDataset<P>`, `ExclusionRecord::hash`, `DatasetFingerprint`)
    - crates/apr-cli/src/commands/data_tweeteval.rs manifest family (~lines 64-113) — the section this attestation is embedded into
    - contracts/contrastive-pair-protocol-v1.yaml equation `dataset_attestation`
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-REVIEWS.md plan 02-09 section (why a row-level check alone is insufficient)
  </read_first>
  <behavior>
    - `DatasetAttestation::from_prepared(&ds)` produces a section carrying: profile, schema_version, label_names, per-split JSONL SHA-256, per-split per-class counts, normalization version, exclusion-record hash, dataset fingerprint
    - `PreparedDataset::<Canonical>::from_attested_bytes(attestation_bytes, splits, &mut ledger)` succeeds on a self-consistent set and returns a dataset equal (same fingerprint, same exclusions) to the one that produced the attestation
    - Rejection tests, one each (the point of the boundary — review finding F16):
      * attestation profile "compatibility" fed to the CANONICAL constructor → ProfileMismatch
      * attestation profile "canonical" fed to the COMPATIBILITY constructor → ProfileMismatch
      * unsupported `schema_version` → UnsupportedSchemaVersion naming the supported set
      * a missing required split buffer → MissingSplit naming the role
      * one split's bytes swapped for another valid dataset's bytes (the "mixed directory" case) → SplitHashMismatch naming the split
      * a byte-level edit to one split that preserves nothing → SplitHashMismatch
      * an attestation whose recorded fingerprint disagrees with the re-derived one → FingerprintMismatch
      * an attestation whose recorded exclusion hash disagrees with the re-derived one → ExclusionRecordMismatch
      * an attestation whose per-class counts disagree with the split contents → InvalidClassCounts
    - No filesystem or path type appears in this module (bytes in, typed values out — D-04)
  </behavior>
  <action>
    Add `attestation.rs` (the stub was declared by plan 02-02's skeleton; do not touch lib.rs).
    Define `DatasetAttestation` as a `#[serde(deny_unknown_fields)]` struct with a deterministic
    canonical serialization (BTreeMap, fixed field order), `from_prepared`, `to_bytes`, and
    `from_bytes`. Then add `impl PreparedDataset<Canonical>` and `impl PreparedDataset<Compatibility>`
    blocks IN THIS MODULE (Rust permits inherent impls in any module of the defining crate, so
    plan 02-05's `prepared.rs` is untouched and waves stay parallel) providing
    `from_attested_bytes(attestation_bytes: &[u8], splits: &BTreeMap<String, Vec<u8>>, ledger: &mut AccessLedger)`.

    The constructor must, in this order: parse the attestation; check `schema_version` against the
    supported set; check `attestation.profile == P::PROFILE` (else `ProfileMismatch`); check that
    every role this profile requires has a buffer (else `MissingSplit`); check each buffer's
    SHA-256 against the attested split hash (else `SplitHashMismatch` naming the split) BEFORE
    parsing it; then build the `PreparedDataset` through the ordinary plan-02-03 path; then
    re-derive and compare the per-class counts, the exclusion-record hash, and the dataset
    fingerprint (`InvalidClassCounts` / `ExclusionRecordMismatch` / `FingerprintMismatch`). Only
    after every check passes may the value — and therefore any `Split<R>` accessor — exist. Record
    the ingest in the ledger.

    Document the threat this closes in the module doc: a caller pointing at a directory whose
    `train.jsonl` came from one preparation and `validation.jsonl` from another would pass every
    row-level check while silently breaking split identity; the attested split hashes and
    fingerprint are what make that a typed error. Annotate `from_attested_bytes` with
    `#[provable_contracts_macros::contract("contrastive-pair-protocol-v1", equation = "dataset_attestation")]`.
    Tests selectable via `cargo test -p aprender-contrastive-data attestation`.
  </action>
  <verify>
    <automated>cargo test -p aprender-contrastive-data attestation > /tmp/t-att.log 2>&1; a=$?; make contrastive-data-boundary > /tmp/cdb6.log 2>&1; b=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - All nine rejection tests plus the success test pass
    - `attestation.rs` contains `from_attested_bytes` for BOTH profile types and the split-hash check precedes parsing (verify by reading the function order and by a test that a corrupted buffer fails with SplitHashMismatch rather than MalformedRow)
    - `git diff --stat crates/aprender-contrastive-data/src/prepared.rs` is empty (this task adds impls in its own module so wave 4 stays parallel with plan 02-05)
    - attestation.rs contains the `dataset_attestation` #[contract] annotation
    - `make contrastive-data-boundary` exits 0
  </acceptance_criteria>
  <done>A consumer cannot obtain canonical splits from a directory whose profile, schema version, split hashes, class counts, exclusions, or fingerprint disagree with its attestation.</done>
</task>

<task type="auto">
  <name>Task 3: Real-duplicate golden + tweet-eval contract growth (dataset half of DATA-02)</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs, contracts/tweet-eval-stance-benchmark-v1.yaml</files>
  <read_first>
    - crates/apr-cli/src/commands/data_tweeteval.rs opt-in network test pattern (~lines 803-812: `#[ignore = "opt-in network test..."]`)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md Finding F1 / Pitfall 3 (the measured duplicate facts)
    - contracts/tweet-eval-stance-benchmark-v1.yaml (as restructured by plan 02-01)
    - crates/aprender-contracts-cli/src/cli.rs lines 60-90 (the REAL `pv diff` signature — two filesystem paths)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md D-27
  </read_first>
  <action>
    1. Add an opt-in network test (same `#[ignore]` convention as
       `pinned_upstream_satisfies_the_dataset_contract`) that prepares the REAL pinned dataset and
       asserts the exclusion record contains EXACTLY ONE coalesced duplicate group:
       train:70 ≡ validation:3, exact SHA-256
       `e3af840b5398272c89e5e1e3e1730c26b0f5bc856d3d46531a2a64dd8844a2c3`, both labeled `none`,
       `detected_by` recording BOTH exact and normalized (they co-fire), `label_conflict == false`;
       reduced pools none=158, against=319, favor=109, each ≥ 64. Assert `groups().len() == 1`
       explicitly — under the pre-review independent-grouping design this would have been 2, so
       this assertion is the executable form of review finding F8. Also assert that the prepare
       call SUCCEEDED (D-27's exclude-and-record half: real-data duplicate content is not fatal).
       An empty `excluded_ids` on real data is the Pitfall 3 warning sign and must FAIL this test.
    2. Grow the contract's dataset half of DATA-02:
       - OBLIG-TWEET-EVAL-DUP-EXCLUSION — canonical preparation SUCCEEDS and records exactly one
         coalesced duplicate group and the reduced pools above (cite D-18 and D-27).
       - OBLIG-TWEET-EVAL-CONFLICTING-ROLES — a source claiming two roles is a typed error.
       - OBLIG-TWEET-EVAL-SCHEMA-VERSION — the benchmark manifest is `schema_version: 2`;
         supported set is `[2]`; a version-1 manifest is rejected with a typed error naming the
         remediation, and no migration is performed (review finding F22).
       - OBLIG-TWEET-EVAL-ATTESTATION — canonical splits are exposed to `apr data select`/`pairs`
         only through `PreparedDataset::from_attested_bytes`, so a mixed or forged directory is a
         typed error rather than a row-level pass (review finding F16).
       Add falsification tests binding each: the real-duplicate one referencing
       `cargo test -p apr-cli --lib data_tweeteval -- --ignored`; the conflicting-role,
       label-conflict, schema-version, and attestation ones referencing the synthetic-fixture
       tests from Task 1, Task 2, and plan 02-03. Record duplicates by HASH, never by tweet text
       (licensing — the contract must not contain tweet content).
    3. Semver bump via the REAL `pv diff` interface (review finding V2 — `pv diff <file> HEAD` is
       invalid; `crates/aprender-contracts-cli/src/cli.rs:73-78` takes two paths):
         `git show <plan-02-01-contract-commit>:contracts/tweet-eval-stance-benchmark-v1.yaml > /tmp/tweet-eval-prev.yaml`
         `cargo run --release -p aprender-contracts-cli --bin pv -- diff /tmp/tweet-eval-prev.yaml contracts/tweet-eval-stance-benchmark-v1.yaml`
       Apply the suggested bump. `pv validate` must stay green (pv only — never yq/bash).
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml > /tmp/pv-te2.log 2>&1; a=$?; cargo test -p apr-cli --lib data_tweeteval > /tmp/t-te2.log 2>&1; b=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - The opt-in test exists with the exact expected duplicate facts (grep the file for `e3af840b` and `158`) and asserts `groups().len() == 1` plus a successful prepare
    - Contract contains OBLIG-TWEET-EVAL-DUP-EXCLUSION, OBLIG-TWEET-EVAL-CONFLICTING-ROLES, OBLIG-TWEET-EVAL-SCHEMA-VERSION, OBLIG-TWEET-EVAL-ATTESTATION, and no tweet text
    - `pv validate` exits 0 (status captured directly); the metadata version is bumped per the `pv diff` suggestion, and the SUMMARY quotes the two-path `pv diff` command actually run
    - Offline test suite still green (the network test is `#[ignore]`-gated; CI stays offline per SAFE-02)
    - If the opt-in network test is run, its output is recorded verbatim in the SUMMARY; if it is not run, the SUMMARY says so explicitly rather than implying it passed
  </acceptance_criteria>
  <done>The one real duplicate is a permanent executable golden proving D-27's exclude-and-record half on real data, and the dataset-specific DATA-02 obligations — including schema-version and attestation policy — are contract-bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw.githubusercontent.com → CLI | Pinned-revision fetch (HTTPS, 40-hex revision validated) |
| local `--source` dir → CLI | User bytes claiming to be the pinned dataset |
| prepared output directory → later commands | A directory whose splits came from different preparations would pass every row-level check |
| CLI → crate | Bytes and typed rows cross into the validated boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | dataset substitution across the refactor | mitigate | Count contracts + hash-from-same-buffer preserved through the seam; byte-parity golden vs baseline (Task 1) |
| T-02-18 | Spoofing/Repudiation | local dir attested as pinned revision | mitigate | `revision_verified` derivation stays CLI-side and FALSIFY-TWEET-EVAL-006 passes unchanged |
| T-02-19 | Information Disclosure | tweet text entering repo/contract | mitigate | Duplicates recorded by SHA-256 only (Task 3); no vendored text |
| T-02-20 | Elevation (data) | compat-profile leakage into selection | mitigate | The compatibility profile builds `PreparedDataset<Compatibility>`, a different TYPE with no validation witness (Task 1, D-19 + review V1) |
| T-02-42 | Tampering | mixed or forged prepared directory | mitigate | `from_attested_bytes` verifies profile, schema version, per-split hashes, class counts, exclusion hash, and fingerprint before any split is exposed (Task 2, review finding F16) |
| T-02-43 | Repudiation | silent migration of a stale manifest | mitigate | `SUPPORTED_BENCHMARK_SCHEMA_VERSIONS = [2]`; version 1 is a typed error naming the remediation, never an inferred upgrade (Task 1, review finding F22) |
</threat_model>

<verification>
- `cargo test -p apr-cli --lib data_tweeteval` green offline; test-name set not shrunk
- `cargo test -p aprender-contrastive-data attestation` green
- Opt-in network test green when run with network (output recorded in the SUMMARY, or its absence stated)
- `pv validate` green on the grown contract; the `pv diff` command used is the two-path form
- `make tier2` green
</verification>

<success_criteria>
- DATA-01 fully delivered through the seam D-05 actually specifies: acquisition, canonical JSONL, hashes, provenance, no committed text
- DATA-02 fully delivered for this dataset: all typed failures incl. duplicates/conflicting roles, with the real duplicate as a live golden and D-27's split semantics implemented
- Canonical splits are reachable only through an attested boundary
- The relocation is a clean reviewable diff against the D-06 baseline
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-06-SUMMARY.md` when done
</output>
</content>
