---
phase: 02-deterministic-pair-and-data-protocol
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Cargo.toml
  - crates/aprender-contrastive-data/Cargo.toml
  - crates/aprender-contrastive-data/allowed-deps.txt
  - crates/aprender-contrastive-data/src/lib.rs
  - crates/aprender-contrastive-data/src/error.rs
  - crates/aprender-contrastive-data/src/schema.rs
  - crates/aprender-contrastive-data/src/hash.rs
  - crates/aprender-contrastive-data/src/split.rs
  - crates/aprender-contrastive-data/src/prepared.rs
  - crates/aprender-contrastive-data/src/attestation.rs
  - crates/aprender-contrastive-data/src/dedup.rs
  - crates/aprender-contrastive-data/src/ledger.rs
  - crates/aprender-contrastive-data/src/rng.rs
  - crates/aprender-contrastive-data/src/buckets.rs
  - crates/aprender-contrastive-data/src/select.rs
  - crates/aprender-contrastive-data/src/pairs.rs
  - crates/aprender-contrastive-data/src/manifest.rs
  - crates/apr-cli/Cargo.toml
  - contracts/contrastive-pair-protocol-v1.yaml
  - Makefile
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DATA-05, DATA-06]

must_haves:
  truths:
    - "cargo check -p aprender-contrastive-data succeeds as a publishable workspace member; the three MISSING workspace dependency entries (aprender-rand, unicode-normalization, trybuild) exist so every downstream `{ workspace = true }` resolves"
    - "The full module skeleton is declared ONCE here as `pub mod` stubs, so plans 02-03..02-07 each edit only their own module file and never contend on lib.rs — this is what keeps waves 3-5 parallelizable"
    - "pv validate accepts contracts/contrastive-pair-protocol-v1.yaml on day one (never a known-red contract in tree)"
    - "The D-04 boundary gate is a POSITIVE dependency allowlist derived from the resolved tree plus a src/-wide ban on fs/net/path symbols with NO cfg(test) exemption — a deny-list plus a cfg(test)-blind grep can pass vacuously"
    - "Every DATA-02 failure class plus the degenerate-case, version-mismatch, arithmetic, and untrusted-input classes that plans 02-05/02-07/02-09 need has a typed error variant (exhaustive initial design, #[non_exhaustive], extensible with a recorded rationale)"
    - "Publishability is verified, not asserted: `cargo package --no-verify` runs for the new crate and for apr-cli, and the publish-order constraint (new crate BEFORE published apr-cli) is recorded"
  artifacts:
    - path: "crates/aprender-contrastive-data/Cargo.toml"
      provides: "Publishable crate manifest, workspace lints, version.workspace"
      contains: "aprender-rand"
    - path: "crates/aprender-contrastive-data/allowed-deps.txt"
      provides: "Positive dependency allowlist compared against the resolved normal-dependency closure"
      contains: "aprender-rand"
    - path: "crates/aprender-contrastive-data/src/error.rs"
      provides: "ContrastiveDataError — full typed error surface for the phase"
      contains: "BudgetExceedsHardCap"
    - path: "crates/aprender-contrastive-data/src/lib.rs"
      provides: "Complete `pub mod` skeleton so no later plan edits lib.rs"
      contains: "pub mod prepared"
    - path: "contracts/contrastive-pair-protocol-v1.yaml"
      provides: "Dataset-agnostic pair-protocol contract (D-24): equations, obligations, falsification tests, kani harnesses, qa_gate"
      contains: "proof_obligations"
    - path: "Makefile"
      provides: "contrastive-data-boundary target + tier2 test line + $(CONTRACTS) entry"
      contains: "contrastive-data-boundary"
  key_links:
    - from: "Cargo.toml"
      to: "crates/aprender-contrastive-data/Cargo.toml"
      via: "[workspace] members entry + [workspace.dependencies] path+version entry"
      pattern: "aprender-contrastive-data"
    - from: "Cargo.toml"
      to: "crates/aprender-rand"
      via: "NEW [workspace.dependencies] entry — aprender-rand is a workspace MEMBER but had no workspace-dependency entry (verified: root Cargo.toml line 27 members, absent from the deps table)"
      pattern: "aprender-rand = \\{ path"
    - from: "crates/apr-cli/Cargo.toml"
      to: "crates/aprender-contrastive-data"
      via: "workspace dependency"
      pattern: "aprender-contrastive-data = \\{ workspace = true \\}"
    - from: "Makefile"
      to: "contracts/contrastive-pair-protocol-v1.yaml"
      via: "$(CONTRACTS) explicit list reached by tier3's contract-validate"
      pattern: "contrastive-pair-protocol-v1"
---

<objective>
Create the `aprender-contrastive-data` workspace crate (D-01) with its complete module skeleton
and typed error surface, author the pv-valid `contrastive-pair-protocol-v1.yaml` contract (D-24),
and install non-vacuous D-04 bytes-boundary enforcement plus tier wiring.

Purpose: this plan is the interface-first foundation — the error enum and the contract's equations
ARE the contracts plans 02-03/02-05/02-07 implement against, and the boundary check makes D-04 an
enforced property instead of an aspiration. Note for reviewers: D-01/D-02 is a RECORDED deviation
from .planning/research/STACK.md's ownership table, and the corrected evidence is Finding F3
(apr-cli DOES depend on aprender-data via the `alimentar` alias; the surviving rationale is the
arrow/parquet/zstd/s3 weight of aprender-data flowing into Phase 3 and Lambda consumers). Do not
"fix" the crate home back toward STACK.md.
Output: compiling publishable crate + pv-valid phase contract + enforced boundary gates.

## Review corrections incorporated (02-REVIEWS.md)

- **V3 / Codex 02-02 HIGH — the three workspace dependency entries do not exist.** Verified this
  session: root `Cargo.toml` `[workspace.dependencies]` contains `rand`, `rand_chacha`, `proptest`,
  `tempfile`, `sha2`, `serde`, `thiserror`, `provable-contracts-macros` — but NOT `aprender-rand`
  (a member at line 27 with no dependency entry), NOT `unicode-normalization` (declared per-crate
  at `crates/aprender-train/Cargo.toml:127` and `crates/apr-cli/Cargo.toml:238,271` with literal
  `"0.1"`), and NOT `trybuild` (declared per-crate at
  `crates/aprender-contracts-macros/Cargo.toml:23` and `crates/aprender-test-derive/Cargo.toml:23`).
  Task 1 adds all three explicitly; without them every `{ workspace = true }` in waves 3-6 fails
  to resolve.
- **F10 / Codex 02-02 HIGH — the frozen error enum omitted variants later plans need.** The enum
  is widened (serialization/IO, unsupported schema/policy/algorithm version, semantic-hash
  mismatch, arithmetic overflow, zero budget/hard cap, invalid shots, ordinal out of range, target
  mismatch, profile/split-hash/fingerprint/exclusion mismatches) AND the framing changes: this is
  an exhaustive INITIAL design review, not a permanently closed enum. Downstream plans MAY add
  variants; the rule is that any addition is recorded in that plan's SUMMARY and reflected in the
  contract's error-taxonomy obligation.
- **F9 / Codex 02-02 HIGH — the boundary gate did not enforce its contract.** Replaced: (a) the
  dependency deny-list becomes a POSITIVE allowlist file compared against the resolved
  `cargo tree -e normal` closure, so a newly introduced dependency fails by default rather than
  passing because nobody thought to deny it; (b) the `#[cfg(test)]`-blind grep is replaced by a
  src/-wide ban on `std::fs`/`std::net`/`std::path`/`Path`/`PathBuf` with NO cfg(test) exemption,
  which makes "the public API contains no path types" a grep-checkable consequence rather than an
  unprovable claim.
- **F9 (second half) / Codex 02-02 HIGH — `pv validate` checks shape, not binding coverage.**
  Verified: `Makefile:875` defines `BINDING := contracts/aprender/binding.yaml` and
  `Makefile:932` defines `contract-audit`, which NO tier invokes. Task 3 proves the audit
  mechanism works against the new contract and records the output; the binding entries and the
  blocking tier wiring are owned by plan 02-08 Task 3 (single owner — two plans appending to one
  YAML list in the same wave would conflict).
- **F20 / Codex 02-02 MEDIUM + Gemini §4.2 — publishability asserted but never verified.**
  Task 1 runs `cargo package --no-verify` for the new crate and for `apr-cli`, and records the
  cascade order. The full verifying `cargo package -p apr-cli` is NOT used as a gate and the
  reason is stated honestly: it resolves `aprender-contrastive-data` from the registry, where the
  crate does not yet exist, so it cannot pass until the human-approved publish lands the new crate
  first (RESEARCH Pitfall 8 / Finding F5).
- **F19 / Codex 02-02 MEDIUM — Kani harness names without runnable harnesses.** Same disposition
  as plan 02-01: `cargo-kani` is not installed and no `#[kani::proof]` exists in this repository.
  Harness entries are required by pv's PROVABILITY-001 rule, so each one's `property` prose states
  it is declared and not executed here and names the runnable, identically bounded proptest that
  is the actual evidence.
- **New (this replan) — the module skeleton lands here.** Declaring every module as a `pub mod`
  stub in one place removes lib.rs from every downstream plan's `files_modified`. Without it,
  02-05 and 02-06 (wave 4) and 02-06 and 02-07 would collide on `lib.rs` and the reviewed
  six-wave DAG would have to serialize. Consumers use module paths
  (`aprender_contrastive_data::split::Split`) rather than crate-root re-exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold aprender-contrastive-data, wire the workspace (3 missing dep entries), declare the full module skeleton, author the error surface</name>
  <files>crates/aprender-contrastive-data/Cargo.toml, crates/aprender-contrastive-data/allowed-deps.txt, crates/aprender-contrastive-data/src/lib.rs, crates/aprender-contrastive-data/src/error.rs, crates/aprender-contrastive-data/src/schema.rs, crates/aprender-contrastive-data/src/hash.rs, crates/aprender-contrastive-data/src/split.rs, crates/aprender-contrastive-data/src/prepared.rs, crates/aprender-contrastive-data/src/attestation.rs, crates/aprender-contrastive-data/src/dedup.rs, crates/aprender-contrastive-data/src/ledger.rs, crates/aprender-contrastive-data/src/rng.rs, crates/aprender-contrastive-data/src/buckets.rs, crates/aprender-contrastive-data/src/select.rs, crates/aprender-contrastive-data/src/pairs.rs, crates/aprender-contrastive-data/src/manifest.rs, Cargo.toml, crates/apr-cli/Cargo.toml</files>
  <read_first>
    - crates/aprender-rand/Cargo.toml (small-publishable-crate precedent — note it carries its OWN `[lints.rust]` block because it predates workspace lints; do NOT copy that, and note its `[lib] name = "trueno_rand"`)
    - crates/aprender-train-common/Cargo.toml (verify the `[lints] workspace = true` form recent members use)
    - Root Cargo.toml: `[workspace] members` block (line ~27, containing `"crates/aprender-rand"`), `[workspace.package] version = "0.63.0"` / `rust-version = "1.91"` (~line 117), and `[workspace.dependencies]` (starts line 128) — confirm for yourself that `aprender-rand`, `unicode-normalization`, and `trybuild` are ABSENT before adding them, and copy the exact shape of `aprender-data = { path = "crates/aprender-data", version = "0.63.0" }`
    - crates/apr-cli/Cargo.toml lines ~228-235 (the `alimentar = { workspace = true }` consumption style with its explanatory comment)
    - crates/aprender-rand/src/lib.rs and src/error.rs (crate-docs + thiserror templates)
    - crates/apr-cli/src/commands/data_tweeteval.rs error-message style (~lines 331-335: split name + index + expected + got)
    - crates/aprender-core/src/autograd/ops/pooling.rs lines 41-53 (the `# Errors` doc ladder and the `#[contract]` annotation form)
  </read_first>
  <action>
    (A) Add the THREE MISSING workspace dependency entries to root `Cargo.toml`
    `[workspace.dependencies]` (review finding V3 — verified absent this session):
      `aprender-rand = { path = "crates/aprender-rand", version = "0.63.0" }`  (place with the
        other workspace-internal path crates, near the `aprender-data` entry)
      `unicode-normalization = "0.1"`  (matches the literal version the two existing consumers
        already pin; adding a workspace entry does not change their resolution)
      `trybuild = "1"`  (matches `crates/aprender-contracts-macros/Cargo.toml:23`)
    Then add `"crates/aprender-contrastive-data",` to `[workspace] members` and
    `aprender-contrastive-data = { path = "crates/aprender-contrastive-data", version = "0.63.0" }`
    to `[workspace.dependencies]` — read the CURRENT `[workspace.package] version` and use it; do
    not assume 0.63.0 is still current.

    (B) Create `crates/aprender-contrastive-data/Cargo.toml` (D-01): package name
    `aprender-contrastive-data`, `version.workspace = true`, `edition = "2021"`,
    `rust-version.workspace = true` if that is the member convention (verify against
    aprender-train-common), description "Deterministic, leakage-safe contrastive data
    construction: class buckets, balanced few-shot selection, bounded pair sampling",
    `license = "MIT OR Apache-2.0"`, repository matching workspace members,
    `[lints] workspace = true`. NO `publish = false` — publishing is mandatory because the
    published `apr-cli` will depend on it (RESEARCH Finding F5), and it must enter the publish
    cascade BEFORE apr-cli.
    Dependencies (all workspace, zero new third-party per the Package Legitimacy Audit):
    `aprender-rand = { workspace = true }` (library name is `trueno_rand` — use that in imports),
    `sha2`, `serde` (derive), `serde_json`, `thiserror`, `unicode-normalization`,
    `provable-contracts-macros`. Dev-dependencies: `proptest = { workspace = true }`,
    `trybuild = { workspace = true }`.
    FORBIDDEN deps (D-04): anything filesystem/network/async-shaped — no ureq, tokio, reqwest,
    hyper, arrow, memmap2, tempfile, aws-*.

    (C) Create `crates/aprender-contrastive-data/allowed-deps.txt` — the POSITIVE allowlist
    (review finding F9). One package name per line, sorted, comments with `#`. Populate it by
    running `cargo tree -p aprender-contrastive-data -e normal --prefix none --no-dedupe`,
    extracting the first whitespace field of each line, `sort -u`, and reviewing every entry
    before committing it. The file is the gate's source of truth in Task 3: any package in the
    resolved closure that is not in this file fails the gate. A new transitive dependency
    therefore fails by DEFAULT rather than passing because nobody thought to deny it.

    (D) Create `src/error.rs` — `ContrastiveDataError`, `#[non_exhaustive]`, thiserror-derived.
    Messages carry split/index/expected/got in the data_tweeteval style. Variants:
      DATA-02 ingest classes:
        MalformedRow { split, index, reason }
        InvalidUtf8 { split, index }
        EmptyInput { split, index }
        UnknownLabel { split, index, label }
        LabelTextMismatch { split, index, label, expected_text, got_text }
        InvalidClassCounts { split, expected, got }
        DuplicateId { split, id }
        ConflictingSourceRole { declared, embedded }
        SplitRoleMismatch { expected_role, embedded_role }
        RowHashMismatch { id, expected, got }
        CrossSplitDuplicateUnderflow { class_label, pool, shots }
      Selection / manifest classes:
        InvalidShots { got, allowed }
        SemanticHashMismatch { expected, got }
        SelectionReplayMismatch { field }
        EndpointNotInSelection { id, found_in }
      Attestation / profile classes (review finding F16, plan 02-06 boundary):
        ProfileMismatch { expected, got }
        MissingSplit { role }
        SplitHashMismatch { split, expected, got }
        FingerprintMismatch { expected, got }
        ExclusionRecordMismatch { expected, got }
      Pair classes:
        SelfPair { id }
        NoPairCapacity { positive_capacity, negative_capacity }
        ZeroBudget
        ZeroHardCap
        BudgetExceedsHardCap { budget, hard_cap }
        BudgetExceedsCapacity { budget, capacity }
        OrdinalOutOfRange { ordinal, budget }
        PairTargetMismatch { lo, hi, declared_target, derived_target }
      Version / arithmetic / plumbing classes (review finding F10):
        UnsupportedSchemaVersion { field, got, supported }
        UnsupportedPolicyVersion { policy, got, supported }
        UnsupportedAlgorithmVersion { got, supported }
        ArithmeticOverflow { operation }
        Serialization { context, detail }
        Io { context, detail }        // `dump_pairs<W: Write>` maps Write failures here
    Document each variant with the `# Errors` ladder style from
    `crates/aprender-core/src/autograd/ops/pooling.rs` lines 41-49. Add a module-level doc note:
    **this enum is an exhaustive INITIAL design review, not permanently closed** — a downstream
    plan MAY add a variant, and when it does it must record the addition and the reason in its
    SUMMARY and extend the contract's OBLIG-CPP-ERROR-TAXONOMY list.

    (E) Create `src/lib.rs` with the COMPLETE module skeleton so no later plan edits this file:
    crate docs headed `# Contract: contrastive-pair-protocol-v1.yaml` (do NOT cite
    rand-philox-v1.yaml — it does not exist in this repo, RESEARCH Open Question 4), a short
    determinism doctest, then
      `pub mod error; pub mod schema; pub mod hash; pub mod split; pub mod dedup;
       pub mod prepared; pub mod attestation; pub mod ledger; pub mod rng; pub mod buckets;
       pub mod select; pub mod pairs; pub mod manifest;`
    plus one crate-root convenience re-export, `pub use error::ContrastiveDataError;`. Create
    every listed module file as a stub containing only its `//!` module doc naming the plan that
    fills it (e.g. `//! Cross-split duplicate coalescing. Implemented by plan 02-03 Task 3.`).
    Document in lib.rs WHY the crate exposes modules rather than a flat re-export surface: it
    keeps each plan's edits confined to one file so waves 3-5 stay parallel.

    (F) Consume it: add `aprender-contrastive-data = { workspace = true }` to
    `crates/apr-cli/Cargo.toml` with a one-line comment ("Phase 2: contrastive data protocol —
    apr data select/pairs + TweetEval adapter core").

    (G) Packaging guards and publishability (review finding F20). Re-run both CB-510 scripts
    (`bash scripts/check_include_files.sh`, `bash scripts/check_package_includes.sh`), then
    `cargo package --no-verify -p aprender-contrastive-data` and
    `cargo package --no-verify -p apr-cli`, each with its status captured directly. Record in the
    SUMMARY: (1) both packaged file lists, (2) that `aprender-contrastive-data` MUST publish
    before `apr-cli` in the cascade, and (3) that the full verifying `cargo package -p apr-cli`
    is deliberately NOT a gate here because it resolves the new crate from the registry where it
    does not yet exist — asserting otherwise would be a gate that cannot pass until the
    human-approved publish happens (CLAUDE.md: never self-serve the publish cascade).
  </action>
  <verify>
    <automated>cargo check -p aprender-contrastive-data > /tmp/cd-check.log 2>&1; a=$?; cargo check -p apr-cli > /tmp/cli-check.log 2>&1; b=$?; bash scripts/check_include_files.sh > /tmp/inc.log 2>&1; c=$?; bash scripts/check_package_includes.sh > /tmp/pkg.log 2>&1; d=$?; cargo package --no-verify -p aprender-contrastive-data > /tmp/cd-package.log 2>&1; e=$?; [ "$a" -eq 0 ] && [ "$b" -eq 0 ] && [ "$c" -eq 0 ] && [ "$d" -eq 0 ] && [ "$e" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p aprender-contrastive-data` and `cargo check -p apr-cli` both exit 0 (statuses captured directly)
    - Root Cargo.toml `[workspace.dependencies]` now contains all four of `aprender-rand = { path`, `unicode-normalization`, `trybuild`, `aprender-contrastive-data = { path` — verify with `grep -v '^#' Cargo.toml | grep -c` for each, each ≥ 1
    - `crates/aprender-contrastive-data/Cargo.toml` contains `[lints]` with `workspace = true` and does NOT contain `publish = false`
    - `cargo metadata --format-version 1 > /tmp/meta.json 2>&1; rc=$?` gives rc=0 and `grep -c '"name":"aprender-contrastive-data"' /tmp/meta.json` ≥ 1
    - `src/error.rs` contains all of: `SelfPair`, `BudgetExceedsCapacity`, `BudgetExceedsHardCap`, `ZeroBudget`, `ZeroHardCap`, `NoPairCapacity`, `OrdinalOutOfRange`, `PairTargetMismatch`, `InvalidShots`, `UnsupportedSchemaVersion`, `UnsupportedPolicyVersion`, `UnsupportedAlgorithmVersion`, `SemanticHashMismatch`, `SelectionReplayMismatch`, `ArithmeticOverflow`, `Serialization`, `Io`, `ProfileMismatch`, `MissingSplit`, `SplitHashMismatch`, `FingerprintMismatch`, `ExclusionRecordMismatch`, `LabelTextMismatch`, `EmptyInput`, `SplitRoleMismatch`, `ConflictingSourceRole`, `DuplicateId`, `RowHashMismatch`, `CrossSplitDuplicateUnderflow`, `EndpointNotInSelection`
    - `src/error.rs` module doc states the enum is an initial exhaustive design and that additions must be recorded in a SUMMARY and in the contract's error taxonomy
    - `src/lib.rs` declares all thirteen `pub mod` lines and every named stub file exists; `grep -c "rand-philox-v1" crates/aprender-contrastive-data/src/lib.rs` returns 0
    - `crates/aprender-contrastive-data/allowed-deps.txt` exists, is sorted, and every line matches a package present in `cargo tree -p aprender-contrastive-data -e normal --prefix none --no-dedupe`
    - Both CB-510 scripts exit 0; both `cargo package --no-verify` runs exit 0; the SUMMARY records the publish-order constraint and why the verifying apr-cli package is not gated
  </acceptance_criteria>
  <done>The crate compiles as a publishable workspace member with the complete phase error surface, a full module skeleton that keeps later waves parallel, resolvable workspace dependencies, and verified packaging.</done>
</task>

<task type="auto">
  <name>Task 2: Author contracts/contrastive-pair-protocol-v1.yaml — pv-valid from the start, with the review-corrected equations</name>
  <files>contracts/contrastive-pair-protocol-v1.yaml</files>
  <read_first>
    - contracts/setfit-encoder-conformance-v1.yaml (section shapes: contract/metadata at top, `equations:` ~line 97, `proof_obligations:` ~360, `falsification_tests:` ~613, `kani_harnesses:` ~812 with fields id/obligation/property/bound/strategy/solver/harness, `qa_gate:` ~849)
    - crates/aprender-rand/src/philox.rs (`generate_at` ~lines 97-100, `with_key_counter` ~42-44, the `next_f32` precision caveat ~59-62 — the contract forbids next_f32 for index draws)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md — "Code Examples" (closed-form capacity, CanonicalPair, normalized hash, measured setfit 1.1.3 behavior), Patterns 2/4/7, and Pitfalls 1-2
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md decisions D-10 through D-27 (**D-27 first** — it is the newest and it splits the DATA-02 semantics)
    - crates/apr-cli/src/commands/data_tweeteval.rs line ~45 (`BENCHMARK_SEEDS` — the contracted seed list, never invented)
  </read_first>
  <action>
    Author the dataset-agnostic phase contract (D-24) covering DATA-03/04/05/06 and the generic
    half of DATA-02. It must pass `pv validate` on first commit. Every equation below reflects a
    review correction; the code in later plans must match the contract, not the reverse.

    `equations:` (formula + domain/codomain + invariants + pre/postconditions each):

    - `rng_key_derivation` — **byte encoding is FROZEN here** (review finding F21; both reviewers
      raised it). `key = trunc64(SHA-256(DOMAIN_TAG ‖ root_seed.to_le_bytes() ‖ domain.as_bytes()))`
      with `DOMAIN_TAG = b"apr-contrastive-v1\x00"`; the 64-bit truncation takes digest bytes
      0..8 and forms `[u32::from_le_bytes(d[0..4]), u32::from_le_bytes(d[4..8])]` — LITTLE-ENDIAN,
      stated explicitly. `counter = [ordinal_lo_u32, ordinal_hi_u32, stream_id, 0]`. A 64-bit
      value is assembled from a Philox output block as
      `((lanes[1] as u64) << 32) | (lanes[0] as u64)` — lane 0 is the LOW half, stated explicitly.
      Draw i is a pure function of (key, counter) via `Philox4x32::generate_at` — worker-count
      independence is structural (D-20). State the RNG obligations INLINE; never reference
      rand-philox-v1.yaml (dangling, RESEARCH Open Question 4).
    - `rng_domain_strings` — the FROZEN domain-string table (review finding F21 / Gemini §4.3), so
      cross-platform formatting cannot drift: `select/{label}` where `{label}` is the class label
      rendered as a base-10 `usize` with no padding; `pairs/pos/class`; `pairs/pos/rank`;
      `pairs/neg/class`; `pairs/neg/first`; `pairs/neg/second`. No other domain strings exist in
      v1; adding one is a versioned contract change.
    - `bounded_draw` — `bounded(key, stream, ordinal, n: NonZeroU64) -> u64` using 64-bit
      multiply-shift `((x as u128 * n as u128) >> 64) as u64` from an assembled 64-bit lane.
      Contracted as THE derivation (Assumption A3). Modulo and `next_f32` draws are FORBIDDEN.
      **`n` is `NonZeroU64` so a zero bound is unrepresentable** rather than silently misbehaving
      (review finding F21 / Codex 02-05 MEDIUM).
    - `normalized_content_hash` — SHA-256 over NFC-normalized, trimmed, internal-whitespace-collapsed
      input; NO casefolding (deliberate — D-17); version tag `nfc-trim-ws-v1`.
    - `split_ingest_boundary` — the crate-internal row validation gate ladder: parse and hash the
      SAME buffer; embedded `source_split` must equal the declared role else `SplitRoleMismatch`;
      duplicate IDs, unknown labels, `label_text != label_names[label]`, and per-class count
      mismatches are typed errors (D-16). The public entry points are the `PreparedDataset`
      constructors, not a bare per-split constructor.
    - `prepared_dataset_typestate` — **the review's blocking finding #2, corrected (V1).**
      Profile is a TYPE PARAMETER, not a runtime field: `PreparedDataset<Canonical>` and
      `PreparedDataset<Compatibility>` are distinct types with distinct constructors. Only
      `PreparedDataset<Canonical>` exposes `validation_witness()`; the method does not exist on
      the compatibility type. Selection consumes `&PreparedDataset<Canonical>`, so train,
      validation, exclusions, and fingerprint are bound to ONE dataset value and a compatibility
      dataset cannot be passed at all. The previous design made `profile` a runtime field on
      `SplitDeclaration`, which meant `Split::<Validation>::from_jsonl_bytes(bytes, &compat_decl)`
      COMPILED and merely returned `Err(ConflictingSourceRole)` — so the promised trybuild
      compile-fail proof was unobtainable and DATA-06's "cannot be constructed" was unproven.
    - `dataset_attestation` — the crate-owned, hash-attested identity section a consumer must
      present before canonical splits are exposed (review finding F16): profile, schema_version,
      label map, per-split JSONL SHA-256, per-class counts, normalization version, exclusion
      record, dataset fingerprint. `from_attested_bytes` re-derives every field from the supplied
      buffers and fails typed on any disagreement (`ProfileMismatch`, `SplitHashMismatch`,
      `FingerprintMismatch`, `ExclusionRecordMismatch`, `UnsupportedSchemaVersion`, `MissingSplit`).
    - `cross_split_exclusion` — **duplicate groups are COALESCED** (review finding F8): an exact
      duplicate is necessarily also a normalized duplicate, so grouping independently by both keys
      double-counts and can double-decrement the pool. Grouping is by connected components
      (union-find) over the union of exact-hash and normalized-hash edges; each component is ONE
      group recording which detection kinds fired. Affected TRAIN rows are deterministically
      removed from the selection pool and recorded; **never fatal at prepare time (D-18, upheld
      verbatim by D-27)**; the typed error fires only when the reduced pool < `shots_per_class`
      (`CrossSplitDuplicateUnderflow`).
    - `split_span_fail_closed` — **D-27's other half.** Any pair or selection whose endpoints
      would cross split roles is fail-closed: structurally (pair endpoints are `SelectedId`
      ordinals into one `Selection`; selection consumes one `PreparedDataset<Canonical>`) and, for
      untrusted replayed input, by typed error (`EndpointNotInSelection`). This — not prepare-time
      duplicate content — is what ROADMAP criterion 5's "rejected fail-closed" refers to.
    - `few_shot_selection` — partial Fisher-Yates over sorted per-class buckets; swap index for
      step i from domain `select/{label}` ordinal i; output order is draw order, classes
      concatenated in ascending label order (DATA-03 ordered manifest). `shots_per_class` must be
      in {8,16,32,64} else `InvalidShots` BEFORE any draw.
    - `selection_canonical_payload` — **non-circular by construction** (review finding F4). The
      hashed canonical payload EXCLUDES its own digest; `semantic_hash = SHA-256(payload_bytes)`
      and the digest lives in an OUTER file envelope alongside unhashed volatile metadata. The
      payload contains: schema_version, algorithm_version, profile, dataset_fingerprint,
      validation_fingerprint, label map, normalization version, root_seed, shots_per_class, the
      ordered `SelectedExample` list (id, label, exact_hash, normalized_hash), the exclusion
      record (excluded ids + reduced per-class pools), and the access-ledger hash.
    - `selection_replay` — replay consumes a canonical prepared dataset, never bare bytes (review
      finding F6). It revalidates: profile (structurally, by type), dataset and validation
      fingerprints, exclusion record equality, membership, per-row exact AND normalized hashes,
      ID uniqueness, per-class balance equal to `shots_per_class`, ordering, supported
      schema/algorithm versions, recomputed `semantic_hash` equality, and — because selection is
      deterministic — that recomputing the selection from (dataset, seed, shots) reproduces the
      recorded ordered list (`SelectionReplayMismatch` otherwise).
    - `access_ledger_persistence` — the ledger has a deterministic canonical byte form and a
      `ledger_hash`; the ledger records AND its hash are embedded in the selection manifest
      payload so Phase 5's selection-lock has an artifact to read (review finding F7). A ledger
      that exists only in memory is not evidence.
    - `canonical_pair` — unordered `(min, max)` over `SelectedId`; self-pairs structurally
      impossible (D-12); the 1.0/0.0 target is DERIVED from endpoint classes at emission, never
      accepted from caller input.
    - `untrusted_pair_ingest` — replayed/dumped pair bytes parse into a SEPARATE DTO
      (`UntrustedPairRecord { lo: String, hi: String, target: f32 }`, `deny_unknown_fields`) and
      are validated into trusted `LabeledPair` values at the boundary (review finding F13):
      membership (`EndpointNotInSelection`), canonical ordering, distinctness (`SelfPair`), and
      target agreement with the endpoints' classes (`PairTargetMismatch`). A trusted pair type
      with a private constructor cannot represent poisoned input, so the poisoning gate needs its
      own untrusted representation.
    - `positive_capacity` = Σ C(n_k,2); `negative_capacity` = Σ_{j<k} n_j·n_k. **Both return a
      typed result, not a bare u64** — every step uses checked arithmetic and overflow is
      `ArithmeticOverflow` (review finding F11).
    - `default_epoch_budget` = `2·max(pos, neg)`, and the contracted default is
      `effective_default_budget = min(closed_form, hard_cap)` (D-14). `hard_cap` is a `PairConfig`
      field with contract-resident default `DEFAULT_HARD_CAP = 1_048_576` (2^20; chosen so the
      clamp never engages for any contracted layout — the largest contracted closed form is
      24,576). Worked values under the default cap: balanced 3-class 8-shot → 384, 64-shot →
      24,576 (clamp inactive); clamp-engages example: [64,64,64] with hard_cap 10,000 → effective
      default 10,000.
    - `budget_resolution` — **the hard cap is not bypassable** (review finding F3). Resolution:
      `hard_cap == 0` → `ZeroHardCap`; `budget == Some(0)` → `ZeroBudget`;
      `budget == Some(b) && b > hard_cap` → `BudgetExceedsHardCap { budget, hard_cap }` (an
      explicit over-cap request FAILS LOUDLY rather than being silently truncated — the user
      raises `--hard-cap` deliberately); `budget == None` → `min(closed_form, hard_cap)`, with
      the manifest recording whether the clamp engaged. This preserves the cap's DoS role for
      explicit budgets while never silently discarding a number the user typed.
    - `pair_stream` — alternate P,N,P,N (even ordinals positive, odd negative). Positive draw:
      class k weighted C(n_k,2) via an O(K) prefix-sum array, then triangular unranking within
      that class. **Negative draw is O(K), NOT O(K²)** (review finding F1 — the phase's DATA-05
      blocker): do NOT enumerate unordered class pairs. Instead keep per-class weights
      `w_c = n_c · (S − n_c)` where `S = Σ n_i` (an O(K) array whose sum is `2·negative_capacity`),
      draw the first class j against those weights, draw the first endpoint uniformly inside class
      j, then draw `t` uniformly in `[0, S − n_j)` and map it to a global bucket index that skips
      class j's contiguous block using the O(K) class-offset array (`t < offset_j ? t : t + n_j`),
      resolving the second class by binary search over the offsets. Sampling ordered cross-class
      endpoint pairs uniformly this way induces exactly the `n_j·n_k` weight on each unordered
      class pair, so the D-14 semantics are unchanged while retained state stays O(examples + K)
      with K ≤ examples. Retained state: sorted buckets O(N) + class weights O(K) + class offsets
      O(K) + a cursor. One stream per (selection_hash, seed, strategy_version,
      singleton_policy_version, budget); epoch-independent and offset-resumable (Open Question 3).
    - `pair_stream_degenerate_policy` — **every degenerate case is defined** (review finding F2),
      version-tagged `degenerate_policy_version: 1`:
      `hard_cap == 0` → `ZeroHardCap`; effective budget 0 → `ZeroBudget`;
      `positive_capacity == 0 && negative_capacity == 0` (single class of size ≤ 1, or no
      examples) → `NoPairCapacity`;
      `positive_capacity == 0 && negative_capacity > 0` (e.g. every class a singleton — the K≈N
      adversarial layout) → the stream emits NEGATIVES ONLY and the replay record states
      `emitted_kinds: negatives_only`; `negative_capacity == 0 && positive_capacity > 0` (one
      class, ≥ 2 members) → POSITIVES ONLY, recorded the same way;
      odd effective budget with both kinds available → the alternation yields ⌈B/2⌉ positives and
      ⌊B/2⌋ negatives, so the balance invariant is `|#pos − #neg| ≤ 1` at every prefix and `== 0`
      at every EVEN prefix. Oversampling draws with replacement, so `budget > capacity` is NOT an
      error on this strategy; `BudgetExceedsCapacity` is reserved for the unique-capacity check.
    - `singleton_policy` — version-tagged enum, v1 sole variant `NegativesOnly`: a singleton class
      produces no positives (its C(1,2)=0 weight), still appears in negatives, and the manifest
      records the policy plus affected-class count (D-13). Closes the STATE.md Phase 2 blocker.
    - `pair_manifest_replay` and `pair_manifest_hash` — pairs regenerate from the replay tuple;
      only the hash is persisted (D-09). **The hash commits the tuple, not just the pairs**
      (review finding F12): `SHA-256( PairReplayRecord::to_canonical_bytes() ‖ 0x1E ‖ pair_0_bytes
      ‖ pair_1_bytes ‖ … )`, streamed, never collected. Hashing pair bytes alone would let two
      different seeds or policies produce an identical stream hash.

    `proof_obligations:` (precedent shape) — OBLIG-CPP-BYTES-BOUNDARY (public API has no
    Path/PathBuf; the crate's `src/` tree contains no fs/net/path symbols at all, including under
    cfg(test); the resolved normal-dependency closure is a subset of `allowed-deps.txt` — names
    the Makefile target from Task 3 as its check); OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE (selection
    consumes `&PreparedDataset<Canonical>`; `PreparedDataset<Compatibility>` has no
    `validation_witness` method and cannot be passed to selection — trybuild evidence, D-19,
    corrected per review V1); OBLIG-CPP-BOUNDARY-VALIDATION (mislabeled `source_split` in
    honest-looking bytes is a typed error, D-16); OBLIG-CPP-ATTESTATION-BEFORE-EXPOSURE (canonical
    splits are exposed only after the attestation's profile, schema version, split hashes,
    fingerprint, and exclusion record all re-derive — review finding F16);
    OBLIG-CPP-DUP-GROUP-COALESCED (an exact+normalized duplicate is ONE group and decrements the
    pool ONCE — review finding F8); OBLIG-CPP-CAPACITY-INVARIANT (retained sampler state ≤
    c·(examples + budget) measured structurally through the public `state_report()` diagnostics,
    with the in-band materializing negative RED — D-25); OBLIG-CPP-KN-ADVERSARIAL (the invariant
    holds at K ≈ N under a fixed small budget, the layout that the O(K²) design would have
    violated and that a 3-class test can never expose — review finding F1);
    OBLIG-CPP-LEAKY-RED (untrusted pair records naming a non-selected ID are rejected with
    `EndpointNotInSelection` naming the offending ID); OBLIG-CPP-BUDGET-FAIL-CLOSED (`ZeroBudget`,
    `ZeroHardCap`, `BudgetExceedsHardCap`, `NoPairCapacity`, `BudgetExceedsCapacity` — the hard
    cap binds explicit budgets too); OBLIG-CPP-REPLAY-DETERMINISM (identical replay tuple →
    identical pair sequence and manifest hash, independent of worker count; the hash commits the
    tuple); OBLIG-CPP-SELECTION-REPLAY-STRICT (a manifest that disagrees with a recomputed
    selection, or whose fingerprints/exclusions/balance disagree, is rejected typed);
    OBLIG-CPP-LEDGER-PERSISTED (the ledger's canonical bytes and hash are inside the selection
    manifest payload — review finding F7); OBLIG-CPP-SINGLETON-EXPLICIT (policy + version +
    affected count in the manifest, never silent); OBLIG-CPP-ERROR-TAXONOMY (the enumerated
    failure classes; extending it requires a contract change);
    OBLIG-CPP-DEVIATION-DECLARED — the three-clause Aprender deviation statement (widened per
    RESEARCH Finding F2): (1) pair identities are sampled, not enumerate-then-shuffled; (2) the
    count is capped above N; (3) self-pairs are excluded, whereas pinned setfit 1.1.3 includes the
    diagonal, contradicting its own documentation. The deviation is an explicit Aprender policy and
    is NEVER attributed to SetFit (PF-008).

    `falsification_tests:` — one per obligation minimum, with runnable commands matching the test
    layout later plans implement: `cargo test -p aprender-contrastive-data rng`, `... select`,
    `... pairs`, `... dedup`, `... prepared`, `... attestation`,
    `cargo test -p aprender-contrastive-data --test negative_leaky`,
    `--test negative_materializing`, `--test ui`, `--test reference_fixtures`, plus
    `make contrastive-data-boundary`. Include prediction / expected_output / if_fails diagnosis
    per the precedent shape. Required predictions: the degenerate-layout divergence (layout [4,1]:
    measured reference epoch length 22 vs contracted budget 12 — both numbers visible in the
    fixture families, Pitfall 2); a clamp-engages prediction (explicit `hard_cap` below the closed
    form yields the cap as the effective default); an explicit-over-cap prediction
    (`budget > hard_cap` yields `BudgetExceedsHardCap`, NOT a silent clamp); and the K≈N
    adversarial capacity prediction.

    `kani_harnesses:` — KANI-CPP-001 (`CanonicalPair::new` never yields `lo == hi` nor `lo > hi`;
    bound 4, bounded_int, cadical, harness `verify_canonical_pair_ordering`) and KANI-CPP-002
    (capacity closed forms never wrap for class sizes bounded by the harness; checked arithmetic;
    harness `verify_capacity_no_overflow`). **Honesty requirement (review finding F19):**
    `cargo-kani` is not installed in this repository and no `#[kani::proof]` exists anywhere in
    `crates/` — verified this session. Each entry's `property` prose MUST state that the harness
    is DECLARED and NOT EXECUTED here and MUST name the runnable, identically bounded proptest
    that is the actual evidence (`cargo test -p aprender-contrastive-data pairs canonical_pair_ordering`
    and `... capacity_no_overflow`, both bounded to the same 4). Write no text that could be read
    as claiming a machine-checked proof.

    `qa_gate:` — id F-CONTRASTIVE-001, checks [validation, falsification], pass_criteria explicitly
    requiring: `pv validate` exits 0, every falsification test green, AND both in-band negatives
    (leaky, materializing) RED under the same gates that accept the honest implementations (copy
    the setfit qa_gate's negative-must-be-RED phrasing).

    Validate with `cargo run --release -p aprender-contracts-cli --bin pv -- validate` — pv only,
    never bash/yq. If pv rejects for schema-content reasons, stop and surface (A2 fallback).
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/contrastive-pair-protocol-v1.yaml > /tmp/pv-cpp.log 2>&1; rc=$?; n=$(grep -v '^ *#' contracts/contrastive-pair-protocol-v1.yaml | grep -c "rand-philox-v1"); [ "$rc" -eq 0 ] && [ "$n" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/contrastive-pair-protocol-v1.yaml` exits 0 (status captured directly)
    - Contract contains equations: rng_key_derivation, rng_domain_strings, bounded_draw, normalized_content_hash, split_ingest_boundary, prepared_dataset_typestate, dataset_attestation, cross_split_exclusion, split_span_fail_closed, few_shot_selection, selection_canonical_payload, selection_replay, access_ledger_persistence, canonical_pair, untrusted_pair_ingest, positive_capacity, negative_capacity, default_epoch_budget, budget_resolution, pair_stream, pair_stream_degenerate_policy, singleton_policy, pair_manifest_replay, pair_manifest_hash
    - `rng_key_derivation` states little-endian explicitly for BOTH the seed bytes and the 64-bit key truncation, and `rng_domain_strings` lists exactly the six frozen domain strings
    - `bounded_draw` specifies `NonZeroU64` for the bound
    - `pair_stream` contains the per-class weight form `n_c * (S - n_c)` and explicitly forbids enumerating unordered class pairs; the words "O(K^2)" or "class-pair prefix sums" appear only as the rejected alternative
    - `budget_resolution` states that an explicit budget above the hard cap is `BudgetExceedsHardCap`, not a silent clamp
    - `selection_canonical_payload` states that the hashed payload EXCLUDES its own digest and that the digest lives in the outer envelope
    - `pair_manifest_hash` states the header-then-stream construction
    - `cross_split_exclusion` states connected-component (union-find) coalescing and cites D-18 and D-27
    - `split_span_fail_closed` cites D-27 and distinguishes it from prepare-time duplicate content
    - Contract contains OBLIG-CPP-DEVIATION-DECLARED with all three clauses and the text stating the deviation is never attributed to SetFit
    - Contract contains OBLIG-CPP-BYTES-BOUNDARY, OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE, OBLIG-CPP-ATTESTATION-BEFORE-EXPOSURE, OBLIG-CPP-DUP-GROUP-COALESCED, OBLIG-CPP-CAPACITY-INVARIANT, OBLIG-CPP-KN-ADVERSARIAL, OBLIG-CPP-SELECTION-REPLAY-STRICT, OBLIG-CPP-LEDGER-PERSISTED, OBLIG-CPP-ERROR-TAXONOMY, OBLIG-CPP-REPLAY-DETERMINISM
    - `grep -v '^ *#' contracts/contrastive-pair-protocol-v1.yaml | grep -c "rand-philox-v1"` returns 0
    - Each kani_harnesses entry's `property` prose states declared-not-executed and names its runnable proptest
    - qa_gate pass_criteria text requires the two in-band negatives to be RED
  </acceptance_criteria>
  <done>The phase contract exists, is pv-valid, and pins every equation and obligation that plans 02-03/02-05/02-06/02-07/02-08 bind to — including the O(K) negative sampler, the non-bypassable hard cap, the non-circular selection payload, and the frozen RNG byte encoding.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile gates — $(CONTRACTS) entry, tier2 line, non-vacuous contrastive-data-boundary target, binding-audit mechanism probe</name>
  <files>Makefile</files>
  <read_first>
    - Makefile tier2 block (~lines 185-215) including the Phase 1 measured-runtime comment discipline
    - Makefile `setfit-feature-matrix` target (~lines 275-294) — the capture-then-check status discipline to copy verbatim in shape (`cmd > target/x.txt 2>&1 || { …; exit 1; }` then grep the captured file)
    - Makefile tier3 (~lines 226-296) including the D-26 evidence comment block at ~238-254 explaining WHY `contract-validate` is invoked broadly — mirror that evidence discipline for anything new you wire in
    - Makefile `CONTRACTS :=` list (line ~876, now containing the tweet-eval entry from plan 02-01), `BINDING := contracts/aprender/binding.yaml` (~875), `contract-audit` (~932)
    - crates/aprender-contrastive-data/allowed-deps.txt (Task 1's positive allowlist)
  </read_first>
  <action>
    Three Makefile changes plus one evidence probe.

    1. Append `contracts/contrastive-pair-protocol-v1.yaml` as a continuation line to
       `CONTRACTS :=`. Tier3 already reaches the list via `$(MAKE) contract-validate` (line ~253),
       so no tier3 edit is needed for validation.

    2. Add a tier2 line: `@cargo test -p aprender-contrastive-data` with an echo label
       ("Phase 2 contrastive-data: protocol unit gates...") and a measured-runtime comment in the
       Phase 1 style — actually MEASURE the wall time of the invocation on a warm tree and record
       the number in the comment (do not guess). Place it after the Phase 1 SetFit lines.

    3. Add target `contrastive-data-boundary` (D-04 enforcement, Pattern 5), copying the
       `setfit-feature-matrix` capture-then-check discipline (CLAUDE.md Verification Discipline
       rule 1 — never read `$?` through a pipe). **Both halves are POSITIVE checks** (review
       finding F9 — a deny-list plus a cfg(test)-blind grep can pass vacuously):

       a. **Dependency allowlist.** Run
          `cargo tree -p aprender-contrastive-data -e normal --prefix none --no-dedupe
           > target/contrastive-data-tree.txt 2>&1` and check `cargo tree`'s OWN status FIRST,
          failing with a "would pass vacuously" message if it failed. Then extract the first
          whitespace field of each non-empty line, `sort -u` into
          `target/contrastive-data-deps.txt`, and compare against
          `crates/aprender-contrastive-data/allowed-deps.txt` (also normalized by stripping `#`
          comments and blank lines, and sorted). Any dependency present in the resolved closure
          and ABSENT from the allowlist fails the gate, printing every offender. `-e normal`
          deliberately excludes dev-dependencies, so `proptest`/`trybuild` are correctly out of
          the runtime closure.

       b. **Source-surface ban.** Scan every file under `crates/aprender-contrastive-data/src/`
          for the symbols `std::fs`, `std::net`, `std::path`, `PathBuf`, and `Path`, filtering
          comment lines first (drop lines matching `^[[:space:]]*//`) per grep-gate hygiene so a
          doc comment can neither trip nor satisfy the gate. **There is deliberately NO
          `#[cfg(test)]` exemption**: a cfg-aware grep is not reliably possible, and forbidding
          the symbols throughout `src/` makes "the crate's public API contains no path types" a
          mechanical consequence rather than an unverifiable claim. Tests that genuinely need a
          filesystem belong in `tests/` (outside the library boundary) or in `apr-cli`. Any hit
          fails the gate, printing file and line.

       Wire `contrastive-data-boundary` into tier3 immediately after `$(MAKE) setfit-feature-matrix`
       (line ~254) so the gate lives INSIDE a tier — a target outside the tiers is a target that
       stops being run (Phase 1 D-26). Mirror the D-26 evidence comment: run the target STANDALONE
       first with its status captured directly, record the wall time and result in the comment, and
       only then wire it.
       Lint any shell fragments with bashrs conventions (quoted variables, explicit status checks).

    4. **Binding-audit mechanism probe (no file edit).** Run
       `cargo run --release -p aprender-contracts-cli --bin pv -- audit contracts/contrastive-pair-protocol-v1.yaml --binding contracts/aprender/binding.yaml
        > /tmp/cpp-audit.log 2>&1; rc=$?` and record the captured output in the SUMMARY. The
       expected result at this point is that the contract's equations report as UNBOUND — that is
       the correct baseline, and capturing it proves the audit mechanism works against this
       contract before plan 02-08 bets its gate on it. **Do not edit
       `contracts/aprender/binding.yaml` here**: plan 02-08 Task 3 is its single owner for this
       phase, because two plans appending to one YAML list would collide.
  </action>
  <verify>
    <automated>make contrastive-data-boundary > /tmp/cdb.log 2>&1; a=$?; n=$(grep -v '^#' Makefile | grep -c "contracts/contrastive-pair-protocol-v1.yaml"); m=$(grep -v '^#' Makefile | grep -c "contracts/tweet-eval-stance-benchmark-v1.yaml"); [ "$a" -eq 0 ] && [ "$n" -ge 1 ] && [ "$m" -ge 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `make contrastive-data-boundary` exits 0 against the current crate (status captured directly)
    - **Mutation proof of the dependency half** (run, observe, revert): temporarily add `tempfile = { workspace = true }` to the crate's `[dependencies]` — `make contrastive-data-boundary` must exit nonzero and PRINT `tempfile` as an offender not present in `allowed-deps.txt`. Record the observed output in the SUMMARY, then revert.
    - **Mutation proof of the source half** (run, observe, revert): temporarily add `use std::path::PathBuf;` to `src/schema.rs` — the target must exit nonzero naming `schema.rs` and the line. Separately confirm that adding the same text inside a `#[cfg(test)] mod tests { … }` block ALSO fails (there is no cfg(test) exemption, by design). Record both observations, then revert.
    - **Vacuity proof**: temporarily rename `crates/aprender-contrastive-data/allowed-deps.txt` — the target must FAIL with a clear message rather than passing on an empty allowlist. Record, then revert.
    - `grep -v '^#' Makefile | grep -c "contracts/contrastive-pair-protocol-v1.yaml"` ≥ 1 and `... tweet-eval-stance-benchmark-v1.yaml` ≥ 1, both inside the `CONTRACTS :=` block
    - tier3 invokes `contrastive-data-boundary`, and the Makefile carries a D-26-style comment recording the standalone run's measured wall time and result
    - tier2 contains a `cargo test -p aprender-contrastive-data` line with a measured-runtime comment containing an actual measured number
    - The target checks `cargo tree`'s own exit status before consuming its output (no `$?`-through-a-pipe anywhere in the target)
    - `/tmp/cpp-audit.log` from the `pv audit` probe is quoted in the SUMMARY, and `git diff --stat contracts/aprender/binding.yaml` is empty (02-08 owns that file)
  </acceptance_criteria>
  <done>D-04 is mechanically enforced by two positive checks whose failure modes have been observed rather than assumed, the crate's tests run in tier2, both phase contracts are tier3-reachable, and the binding-audit mechanism is proven to work before 02-08 depends on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace dependency graph → published crate | The Lambda-bound crate must not transitively pull network/fs/async deps |
| contracts/ → tier3 gate | A contract outside $(CONTRACTS) is unenforced |
| gate implementation → the claim it makes | A deny-list or a cfg-blind grep can report PASS while the property is false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (supply chain) | aprender-contrastive-data dependency closure | mitigate | POSITIVE allowlist (`allowed-deps.txt`) compared against the resolved `cargo tree -e normal` closure — a new transitive dep fails by default (Task 3); zero new third-party deps per the Package Legitimacy Audit |
| T-02-05 | Elevation | bytes-boundary bypass via fs/path APIs | mitigate | src/-wide symbol ban with NO cfg(test) exemption + OBLIG-CPP-BYTES-BOUNDARY names the check; failure modes proven by mutation (Task 3 acceptance) |
| T-02-06 | Repudiation | unreachable or vacuous contract gates | mitigate | Both contracts appended to explicit $(CONTRACTS) reached by tier3; boundary target wired into tier3 with D-26 evidence discipline; vacuity proof required before the gate is trusted |
| T-02-35 | Repudiation | binding audit assumed to work | mitigate | `pv audit --binding` probed against the new contract and its output recorded (Task 3) before plan 02-08 makes it blocking |
| T-02-36 | Denial of Service | unbounded pair budget accepted at the API | mitigate | `budget_resolution` equation makes the hard cap bind explicit budgets (`BudgetExceedsHardCap`), and the typed variants ship in the error surface here (Task 1) |
| T-02-SC | Tampering | cargo installs | accept | Zero package installs this phase (audit table in 02-RESEARCH.md); publishing remains a human-approved release action |
</threat_model>

<verification>
- `cargo check --workspace` green after the member addition
- `cargo test -p aprender-contrastive-data` green
- `pv validate` green on both phase contracts
- `make contrastive-data-boundary` green, and its three mutation/vacuity proofs observed and recorded
- Both CB-510 scripts green; both `cargo package --no-verify` runs green
</verification>

<success_criteria>
- New crate compiles, is publishable (verified, not asserted), and carries the full phase error surface
- The three missing workspace dependency entries exist, so waves 3-6 can resolve `{ workspace = true }`
- The module skeleton is declared once, keeping waves 3-5 free of lib.rs contention
- Phase contract pv-valid on first commit with the review-corrected equations later plans bind to
- D-04 mechanically enforced inside a tier by gates whose failure has been observed
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-02-SUMMARY.md` when done
</output>
</content>
