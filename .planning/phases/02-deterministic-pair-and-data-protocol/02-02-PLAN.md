---
phase: 02-deterministic-pair-and-data-protocol
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - Cargo.toml
  - crates/aprender-contrastive-data/Cargo.toml
  - crates/aprender-contrastive-data/src/lib.rs
  - crates/aprender-contrastive-data/src/error.rs
  - crates/apr-cli/Cargo.toml
  - contracts/contrastive-pair-protocol-v1.yaml
  - Makefile
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, DATA-05, DATA-06]

must_haves:
  truths:
    - "cargo check -p aprender-contrastive-data succeeds as a publishable workspace member with the D-04 dependency profile (no ureq/tokio/arrow/fs-shaped deps)"
    - "pv validate accepts contracts/contrastive-pair-protocol-v1.yaml on day one (never a known-red contract in tree)"
    - "make tier3's contract validation reaches BOTH phase contracts, and a dedicated Makefile target fails if a forbidden dependency or std::fs/std::net usage enters the crate"
    - "Every DATA-02 failure class plus SelfPair and BudgetExceedsCapacity has a typed error variant downstream plans implement against (interface-first)"
  artifacts:
    - path: "crates/aprender-contrastive-data/Cargo.toml"
      provides: "Publishable crate manifest, workspace lints, version.workspace"
      contains: "aprender-rand"
    - path: "crates/aprender-contrastive-data/src/error.rs"
      provides: "ContrastiveDataError — full typed error surface for the phase"
      contains: "BudgetExceedsCapacity"
    - path: "contracts/contrastive-pair-protocol-v1.yaml"
      provides: "Dataset-agnostic pair-protocol contract (D-24): equations, obligations, falsification tests, kani harnesses, qa_gate"
      contains: "proof_obligations"
    - path: "Makefile"
      provides: "contrastive-data-boundary target + tier2 test line + $(CONTRACTS) entry"
      contains: "contrastive-data-boundary"
  key_links:
    - from: "Cargo.toml"
      to: "crates/aprender-contrastive-data/Cargo.toml"
      via: "[workspace] members entry + [workspace.dependencies] path+version entry"
      pattern: "aprender-contrastive-data"
    - from: "crates/apr-cli/Cargo.toml"
      to: "crates/aprender-contrastive-data"
      via: "workspace dependency"
      pattern: "aprender-contrastive-data = \\{ workspace = true \\}"
    - from: "Makefile"
      to: "contracts/contrastive-pair-protocol-v1.yaml"
      via: "$(CONTRACTS) explicit list"
      pattern: "contrastive-pair-protocol-v1"
---

<objective>
Create the `aprender-contrastive-data` workspace crate (D-01) with its complete typed error
surface, author the pv-valid `contrastive-pair-protocol-v1.yaml` contract (D-24), and install the
mechanical D-04 bytes-boundary enforcement (dependency-closure + source-surface checks) plus tier
wiring.

Purpose: this plan is the interface-first foundation — the error enum and the contract's equations
ARE the contracts plans 02-03/02-05/02-07 implement against, and the boundary check makes D-04 an
enforced property instead of an aspiration. Note for reviewers: D-01/D-02 is a RECORDED deviation
from .planning/research/STACK.md's ownership table, and the corrected evidence is Finding F3
(apr-cli DOES depend on aprender-data via the `alimentar` alias; the surviving rationale is the
arrow/parquet/zstd/s3 weight of aprender-data flowing into Phase 3 and Lambda consumers). Do not
"fix" the crate home back toward STACK.md.
Output: compiling publishable crate + pv-valid phase contract + enforced boundary gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold aprender-contrastive-data and wire the workspace</name>
  <files>crates/aprender-contrastive-data/Cargo.toml, crates/aprender-contrastive-data/src/lib.rs, crates/aprender-contrastive-data/src/error.rs, Cargo.toml, crates/apr-cli/Cargo.toml</files>
  <read_first>
    - crates/aprender-rand/Cargo.toml (small-publishable-crate precedent — but note it carries its own [lints]; do NOT copy that)
    - crates/aprender-train-common/Cargo.toml (verify the `[lints] workspace = true` form recent members use)
    - Root Cargo.toml: [workspace] members block (~line 27), the `alimentar` alias (line ~208), [workspace.dependencies] entries like `aprender-data = { path = "crates/aprender-data", version = "0.63.0" }` (~line 239)
    - crates/apr-cli/Cargo.toml lines ~228–235 (the commented alimentar consumption style)
    - crates/aprender-rand/src/lib.rs and src/error.rs (crate-docs + thiserror templates)
    - crates/apr-cli/src/commands/data_tweeteval.rs error-message style (~lines 331–335: split name + index + expected + got)
  </read_first>
  <action>
    Create `crates/aprender-contrastive-data/` (D-01):

    `Cargo.toml`: package name `aprender-contrastive-data`, `version.workspace = true`,
    edition 2021, `rust-version` via workspace if that is the member convention (verify against
    aprender-train-common), description "Deterministic, leakage-safe contrastive data
    construction: class buckets, balanced few-shot selection, bounded pair sampling",
    license "MIT OR Apache-2.0", repository matching workspace members, `[lints] workspace = true`.
    NO `publish = false` — publishing is mandatory because published apr-cli will depend on it
    (Finding F5); it must enter the publish cascade BEFORE apr-cli (flag this in the SUMMARY; the
    publish itself stays a human-approved release action).
    Dependencies (all workspace/in-tree, zero new third-party per the Package Legitimacy Audit):
    `aprender-rand` (workspace — library name is `trueno_rand`, use that in imports), `sha2`,
    `serde` (with derive), `serde_json`, `thiserror`, `unicode-normalization`,
    `provable-contracts-macros` (same pin apr-cli uses). Dev-dependencies: `proptest`, `trybuild`.
    FORBIDDEN deps (D-04): anything filesystem/network/async-shaped — no ureq, tokio, arrow,
    memmap2, tempfile in the library deps.

    `src/error.rs`: the COMPLETE `ContrastiveDataError` enum for the phase, `#[non_exhaustive]`,
    thiserror-derived, one variant per failure class — downstream plans add NO variants, they only
    consume these (interface-first). Variants (names indicative, messages must carry split/index/
    expected/got per the data_tweeteval style):
    - MalformedRow { split, index, reason } (parse/JSON errors)
    - InvalidUtf8 { split, index }
    - UnknownLabel { split, index, label }
    - InvalidClassCounts { split, expected, got } (also covers pool < shots_per_class at selection)
    - DuplicateId { split, id }
    - ConflictingSourceRole { declared, embedded } (DATA-02 conflicting source roles)
    - SplitRoleMismatch { expected_role, embedded_role } (D-16 untrusted-bytes boundary)
    - RowHashMismatch { id, expected, got }
    - CrossSplitDuplicateUnderflow { class_label, pool, shots } (D-18: reduced pool cannot supply shots)
    - SelfPair { id } (D-12)
    - BudgetExceedsCapacity { budget, capacity } (D-11)
    - EndpointNotInSelection { id, found_in } (leakage gate: pair endpoint outside the Selection)
    Document each variant with the `# Errors`-ladder style from
    crates/aprender-core/src/autograd/ops/pooling.rs lines 41–49.

    `src/lib.rs`: crate docs headed `# Contract: contrastive-pair-protocol-v1.yaml` (do NOT cite
    rand-philox-v1.yaml — it does not exist in this repo, RESEARCH Open Question 4), a short
    determinism doctest (same inputs → same error/value), `mod error;` +
    `pub use error::ContrastiveDataError;`. Later plans add their own `mod` lines.

    Workspace wiring: add `"crates/aprender-contrastive-data",` to [workspace] members; add
    `aprender-contrastive-data = { path = "crates/aprender-contrastive-data", version = "0.63.0" }`
    to [workspace.dependencies] (match the CURRENT workspace version in root Cargo.toml — read it,
    do not assume 0.63.0 is still current); add `aprender-contrastive-data = { workspace = true }`
    to crates/apr-cli/Cargo.toml with a one-line comment ("Phase 2: contrastive data protocol —
    apr data select/pairs + TweetEval adapter core").

    After the workspace edit, re-run BOTH packaging guards (CB-510 discipline):
    `bash scripts/check_include_files.sh` and `bash scripts/check_package_includes.sh`.
  </action>
  <verify>
    <automated>cargo check -p aprender-contrastive-data && cargo test -p aprender-contrastive-data && bash scripts/check_include_files.sh && bash scripts/check_package_includes.sh</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p aprender-contrastive-data` exits 0
    - crates/aprender-contrastive-data/Cargo.toml contains `[lints]` with `workspace = true` and does NOT contain `publish = false`
    - `cargo metadata --format-version 1 | grep -c aprender-contrastive-data` ≥ 1 (member registered)
    - src/error.rs contains variants: `SelfPair`, `BudgetExceedsCapacity`, `SplitRoleMismatch`, `ConflictingSourceRole`, `DuplicateId`, `EndpointNotInSelection`, `CrossSplitDuplicateUnderflow`
    - src/lib.rs contains `# Contract: contrastive-pair-protocol-v1.yaml` and does not mention rand-philox-v1
    - crates/apr-cli/Cargo.toml contains `aprender-contrastive-data = { workspace = true }` and `cargo check -p apr-cli` exits 0
    - Both CB-510 scripts exit 0
  </acceptance_criteria>
  <done>The crate compiles as a publishable workspace member with the complete phase error surface and no forbidden dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Author contracts/contrastive-pair-protocol-v1.yaml — pv-valid from the start</name>
  <files>contracts/contrastive-pair-protocol-v1.yaml</files>
  <read_first>
    - contracts/setfit-encoder-conformance-v1.yaml (section shapes: contract/metadata at top, `equations:` ~line 97, `proof_obligations:` ~360, `falsification_tests:` ~613, `kani_harnesses:` ~812, `qa_gate:` ~849)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-RESEARCH.md — "Code Examples" (closed-form capacity, CanonicalPair, normalized hash, measured setfit 1.1.3 behavior) and Pitfalls 1–2
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md decisions D-10 through D-25
  </read_first>
  <action>
    Author the dataset-agnostic phase contract (D-24) covering DATA-03/04/05/06 and the generic
    half of DATA-02. It must pass `pv validate` on first commit. Sections and REQUIRED content:

    `equations:` (formula + domain/codomain + invariants + pre/postconditions each):
    - `rng_key_derivation`: key = trunc64(SHA-256(domain_tag ‖ root_seed_le ‖ domain_string)) →
      [u32;2]; counter = [ordinal_lo, ordinal_hi, stream_id, 0]; draw i is a pure function of
      (key, counter) via Philox4x32::generate_at — worker-count independence is structural (D-20).
      State the RNG obligations INLINE here; never reference rand-philox-v1.yaml (dangling).
    - `bounded_draw`: 64-bit multiply-shift ((x as u128 * n as u128) >> 64) from a 64-bit lane;
      contracted as THE derivation (Assumption A3); modulo and next_f32 draws are forbidden.
    - `normalized_content_hash`: SHA-256 over NFC-normalized, trimmed, internal-whitespace-collapsed
      input; NO casefolding (deliberate — D-17); version tag `nfc-trim-ws-v1`.
    - `split_ingest_boundary`: from_jsonl_bytes(bytes, decl) hashes and parses THE SAME buffer;
      embedded source_split must equal the declared role or SplitRoleMismatch (D-16).
    - `cross_split_exclusion`: duplicate groups (exact OR normalized hash) across split pairs are
      deterministically removed from the training pool and recorded, never fatal at prepare time;
      typed error only when reduced pool < shots_per_class (D-18).
    - `few_shot_selection`: partial Fisher–Yates over sorted per-class buckets; swap index for step
      i from domain `select/{class_label}` ordinal i; output order = draw order (DATA-03 ordered
      manifest).
    - `canonical_pair`: unordered (min,max), self-pairs structurally impossible (D-12); pair label
      derived from endpoint classes at emission (1.0 same / 0.0 different), never caller input.
    - `positive_capacity` = Σ C(n_k,2); `negative_capacity` = Σ_{j<k} n_j·n_k;
      `default_epoch_budget` = min(2·max(pos, neg), hard_cap) (D-14: SetFit's O(K) closed-form
      oversampling count CLAMPED by a configurable hard cap — the min(closed_form, hard_cap)
      clamp term is part of the contracted equation, not an implementation nicety). `hard_cap` is
      a PairConfig field (02-07) with contract-resident default `DEFAULT_HARD_CAP = 1_048_576`
      (2^20; discretion value chosen so the clamp never engages for any contracted layout — the
      largest contracted closed form is 24,576). Worked values under the default cap: balanced
      3-class 8-shot → 384, 64-shot → 24,576 (clamp inactive); clamp-engages example:
      [64,64,64] with hard_cap 10,000 → effective default 10,000.
    - `pair_stream`: alternate P,N,P,N; positive: class k weighted C(n_k,2) via prefix sums, then
      triangular unranking; negative: class pair (j,k) weighted n_j·n_k, one member each; state
      O(examples) buckets + O(1) per draw; one stream per (selection_hash, seed, policy_version,
      budget), epoch-independent, offset-resumable (Open Question 3 resolution).
    - `singleton_policy`: version-tagged enum, v1 sole variant NegativesOnly — a singleton class
      produces no positives (its C(1,2)=0 weight), still appears in negatives, and the manifest
      records the policy plus affected-class count (D-13). Closes the STATE.md Phase 2 blocker.
    - `pair_manifest_replay`: pairs regenerate from (selection_hash, root_seed, strategy_version,
      singleton_policy_version, budget); only the pair-manifest hash is persisted (D-09).

    `proof_obligations:` (typed, precedent shape) — OBLIG-CPP-BYTES-BOUNDARY (public API has no
    Path/PathBuf, crate links no std::fs/std::net outside cfg(test), dependency closure matches
    allowlist — names the Makefile target from Task 3 as its check); OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE
    (selection demands &Split<Train> AND a &Split<Validation> witness; the compatibility profile
    yields no Split<Validation>, so a compat selection cannot compile — trybuild evidence, D-19);
    OBLIG-CPP-BOUNDARY-VALIDATION (mislabeled source_split in honest-looking bytes is a typed
    error, D-16); OBLIG-CPP-CAPACITY-INVARIANT (retained sampler state ≤ c·(examples + budget),
    measured structurally, with the in-band materializing negative RED — D-25);
    OBLIG-CPP-LEAKY-RED (in-band leaky sampler rejected with EndpointNotInSelection naming the
    offending ID); OBLIG-CPP-BUDGET-FAIL-CLOSED (BudgetExceedsCapacity, D-11);
    OBLIG-CPP-REPLAY-DETERMINISM (identical replay tuple → identical pair sequence and manifest
    hash, independent of worker count); OBLIG-CPP-SINGLETON-EXPLICIT (policy + version + affected
    count in the manifest, never silent); OBLIG-CPP-DEVIATION-DECLARED — the three-clause Aprender
    deviation statement (widened per Finding F2): (1) pair identities are sampled, not
    enumerate-then-shuffled; (2) the count is capped above N; (3) self-pairs are excluded, whereas
    pinned setfit 1.1.3 includes the diagonal, contradicting its own documentation. The deviation
    is an explicit Aprender policy and is NEVER attributed to SetFit (PF-008).

    `falsification_tests:` — one per obligation minimum, with runnable commands matching the test
    layout later plans implement: `cargo test -p aprender-contrastive-data rng`, `... select`,
    `... pairs`, `... dedup`, `cargo test -p aprender-contrastive-data --test negative_leaky`,
    `--test negative_materializing`, `--test ui`, plus `make contrastive-data-boundary`. Include
    prediction / expected_output / if_fails diagnosis per the precedent shape. For the budget
    formula include the degenerate-layout divergence prediction (layout [4,1]: measured reference
    epoch length 22 vs doc-formula 12 — both numbers visible in the fixture families, Pitfall 2)
    AND a clamp-engages prediction (an explicit hard_cap below the closed form yields the cap as
    the effective default — proves the D-14 clamp is live, not decorative).

    `kani_harnesses:` — KANI-CPP-001 (CanonicalPair::new never yields lo==hi nor lo>hi; bound 4,
    bounded_int, cadical) and KANI-CPP-002 (capacity closed forms never overflow for class sizes
    bounded by the harness; checked arithmetic).

    `qa_gate:` — id F-CONTRASTIVE-001, checks [validation, falsification], pass_criteria
    explicitly requiring: pv validate exits 0, every falsification test green, AND both in-band
    negatives (leaky, materializing) RED under the same gates that accept the honest
    implementations (copy the setfit qa_gate's negative-must-be-RED phrasing).

    Validate with `cargo run --release -p aprender-contracts-cli --bin pv -- validate` — pv only,
    never bash/yq. If pv rejects for schema-content reasons, stop and surface (A2 fallback).
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/contrastive-pair-protocol-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/contrastive-pair-protocol-v1.yaml` exits 0
    - Contract contains equations: rng_key_derivation, bounded_draw, normalized_content_hash, split_ingest_boundary, cross_split_exclusion, few_shot_selection, canonical_pair, positive_capacity, negative_capacity, default_epoch_budget, pair_stream, singleton_policy, pair_manifest_replay
    - The default_epoch_budget equation contains the min(closed_form, hard_cap) clamp term AND the contract-resident DEFAULT_HARD_CAP = 1_048_576 constant (D-14's "clamped by a configurable hard cap" is contract text)
    - Contract contains OBLIG-CPP-DEVIATION-DECLARED with all three clauses and the text stating the deviation is never attributed to SetFit
    - Contract contains OBLIG-CPP-BYTES-BOUNDARY, OBLIG-CPP-LEAKAGE-NOT-CONSTRUCTIBLE, OBLIG-CPP-CAPACITY-INVARIANT, OBLIG-CPP-REPLAY-DETERMINISM
    - `grep -c "rand-philox-v1" contracts/contrastive-pair-protocol-v1.yaml` returns 0
    - qa_gate pass_criteria text requires the two in-band negatives to be RED
  </acceptance_criteria>
  <done>The phase contract exists, is pv-valid, and pins every equation and obligation that plans 02-03/02-05/02-07/02-08 bind to — including the D-14 hard-cap clamp on the default budget.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile gates — $(CONTRACTS) entry, tier2 line, contrastive-data-boundary target</name>
  <files>Makefile</files>
  <read_first>
    - Makefile tier2 block (~lines 185–215) including the Phase 1 measured-runtime comment discipline
    - Makefile `setfit-feature-matrix` target (~lines 266–291) — the capture-then-check status discipline to copy verbatim in shape
    - Makefile `CONTRACTS :=` list (line ~876, now containing the tweet-eval entry from plan 02-01)
  </read_first>
  <action>
    Three Makefile changes:

    1. Append `contracts/contrastive-pair-protocol-v1.yaml` as a continuation line to
       `CONTRACTS :=`.

    2. Add a tier2 line: `@cargo test -p aprender-contrastive-data` with an echo label
       ("Phase 2 contrastive-data: protocol unit gates...") and a measured-runtime comment in the
       Phase 1 style — actually MEASURE the wall time of the invocation on a warm tree and record
       the number in the comment (do not guess). Place it after the Phase 1 SetFit lines.

    3. Add target `contrastive-data-boundary` (D-04 enforcement, Pattern 5), copying the
       setfit-feature-matrix capture-then-check discipline (CLAUDE.md Verification Discipline
       rule 1 — never read $? through a pipe):
       a. `cargo tree -p aprender-contrastive-data -e normal > target/contrastive-data-tree.txt`,
          check cargo tree's OWN status first (fail with a "would pass vacuously" message if it
          failed), THEN grep the captured file for the deny-list: `ureq|tokio|arrow|memmap2|reqwest|hyper|aws-`
          — any hit fails with the offending line printed.
       b. Source-surface check: grep the crate's src/ for `std::fs`, `std::net`, and
          `std::path::` occurrences, FILTERING comment lines first (grep -v '^\s*//' per grep-gate
          hygiene — a doc comment must not be able to trip or satisfy the gate), and fail if the
          count outside `#[cfg(test)]` modules is nonzero. Keep the implementation simple and
          honest: capture the grep output to target/, check status, print offenders on failure.
       Wire `contrastive-data-boundary` into the same tier that runs setfit-feature-matrix (read
       the Makefile to see which tier invokes it, and mirror that placement) so the gate lives
       INSIDE a tier — a target outside the tiers is a target that stops being run (Phase 1 D-26).
    Lint any shell fragments with bashrs conventions (quoted variables, explicit status checks).
  </action>
  <verify>
    <automated>make contrastive-data-boundary && grep -c "contrastive-pair-protocol-v1.yaml" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `make contrastive-data-boundary` exits 0 against the current crate
    - Deliberately temporary check (run then revert): adding `use std::fs;` to src/lib.rs makes `make contrastive-data-boundary` exit nonzero naming lib.rs — record the observed failure output in the SUMMARY, then revert
    - `grep -A45 "^CONTRACTS :=" Makefile` contains both `contrastive-pair-protocol-v1.yaml` and `tweet-eval-stance-benchmark-v1.yaml`
    - tier2 contains a `cargo test -p aprender-contrastive-data` line with a measured-runtime comment containing an actual measured number
    - The target checks `cargo tree`'s own exit status before grepping its output (no $?-through-a-pipe)
  </acceptance_criteria>
  <done>D-04 is mechanically enforced, the crate's tests run in tier2, and both phase contracts are tier3-reachable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace dependency graph → published crate | The Lambda-bound crate must not transitively pull network/fs/async deps |
| contracts/ → tier3 gate | A contract outside $(CONTRACTS) is unenforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (supply chain) | aprender-contrastive-data dependency closure | mitigate | `contrastive-data-boundary` Makefile deny-list gate (Task 3) doubles as the V10 supply-chain check; zero new third-party deps per the Package Legitimacy Audit |
| T-02-05 | Elevation | bytes-boundary bypass via fs/path APIs | mitigate | Source-surface check (no std::fs/std::net/std::path outside cfg(test)) + OBLIG-CPP-BYTES-BOUNDARY names the check |
| T-02-06 | Repudiation | unreachable contract gates | mitigate | Both contracts appended to explicit $(CONTRACTS); qa_gate requires in-band negatives RED |
| T-02-SC | Tampering | cargo installs | accept | Zero package installs this phase (audit table in 02-RESEARCH.md) |
</threat_model>

<verification>
- `cargo check --workspace` green after the member addition
- `cargo test -p aprender-contrastive-data` green
- `pv validate` green on both phase contracts
- `make contrastive-data-boundary` green; both CB-510 scripts green
</verification>

<success_criteria>
- New crate compiles, publishable, with the full phase error surface (interface-first)
- Phase contract pv-valid on first commit with all equations/obligations later plans bind to
- D-04 mechanically enforced inside a tier
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-02-SUMMARY.md` when done
</output>
