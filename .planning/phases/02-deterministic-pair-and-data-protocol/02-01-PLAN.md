---
phase: 02-deterministic-pair-and-data-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/apr-cli/src/commands/data_tweeteval.rs
  - crates/apr-cli/src/commands/mod.rs
  - crates/apr-cli/src/commands/eval/mod.rs
  - crates/apr-cli/src/commands/eval/eval_mod_tests.rs
  - crates/apr-cli/src/data_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/extended_commands.rs
  - crates/aprender-train/src/eval/classification/mod.rs
  - crates/aprender-train/src/eval/classification/metrics.rs
  - crates/aprender-train/src/eval/classification/basic_tests.rs
  - contracts/tweet-eval-stance-benchmark-v1.yaml
  - docs/examples/tweet-eval-stance.md
  - Makefile
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "The 813-line D-06 baseline (TweetEval loader, contract, docs example, eval F_avg) exists as its own tracked commit with ZERO content edits, so the Phase 2 relocation diffs against a reviewable baseline"
    - "pv validate accepts contracts/tweet-eval-stance-benchmark-v1.yaml (currently rejected with PROVABILITY-001 x2)"
    - "make tier3's contract validation actually reaches the tweet-eval contract (it is in the explicit $(CONTRACTS) list, which is not a glob)"
  artifacts:
    - path: "crates/apr-cli/src/commands/data_tweeteval.rs"
      provides: "Committed baseline: canonical + setfit profiles, count contracts, hash-from-parsed-bytes, revision_verified honesty"
      contains: "revision_verified"
    - path: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      provides: "pv-valid contract: existing falsification tests + new proof_obligations + kani_harnesses"
      contains: "proof_obligations"
    - path: "Makefile"
      provides: "$(CONTRACTS) list containing the tweet-eval contract"
      contains: "contracts/tweet-eval-stance-benchmark-v1.yaml"
  key_links:
    - from: "Makefile"
      to: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      via: "$(CONTRACTS) explicit list consumed by the tier3 contract validation target"
      pattern: "tweet-eval-stance-benchmark-v1"
---

<objective>
Land the uncommitted D-06 baseline exactly as-is in its own commit, then make the tweet-eval
contract a real gate: restructure it to the pv-valid shape and wire it into the Makefile
$(CONTRACTS) list so tier3 exercises it.

Purpose: D-06 (locked) requires the 813 uncommitted lines to land as-is FIRST so the Phase 2
relocation into `aprender-contrastive-data` is a reviewable diff against a tracked baseline.
RESEARCH Finding F4 proved `pv validate` rejects the contract today (PROVABILITY-001 x2: no
proof_obligations, no kani_harnesses) and that it escapes tier3 only because $(CONTRACTS) is an
explicit hardcoded list — without this plan the D-24 contract gate is theater.
Output: baseline commit + pv-valid contract + $(CONTRACTS) wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the D-06 baseline exactly as-is (zero content edits)</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs, crates/apr-cli/src/commands/mod.rs, crates/apr-cli/src/commands/eval/mod.rs, crates/apr-cli/src/commands/eval/eval_mod_tests.rs, crates/apr-cli/src/data_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/extended_commands.rs, crates/aprender-train/src/eval/classification/mod.rs, crates/aprender-train/src/eval/classification/metrics.rs, crates/aprender-train/src/eval/classification/basic_tests.rs, contracts/tweet-eval-stance-benchmark-v1.yaml, docs/examples/tweet-eval-stance.md</files>
  <read_first>
    - Output of `git status --short` (confirm the exact uncommitted set before staging)
    - crates/apr-cli/src/commands/data_tweeteval.rs (the baseline being landed — confirm it is the 813-line loader with canonical + setfit profiles)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md (D-06 verbatim)
  </read_first>
  <action>
    Land the D-06 baseline per the locked decision: as-is, in its own commit, before ANY Phase 2
    code change. Stage EXACTLY the twelve files listed in <files> — nothing else. Explicitly do
    NOT stage: anything under `.planning/`, anything under `.serena/`, or
    `.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md` (untracked planning
    doc committed separately by the planning workflow).

    Before committing, prove the baseline is green WITHOUT editing it:
    1. `cargo fmt --check` on the touched crates
    2. `cargo clippy -p apr-cli -- -D warnings` and `cargo clippy -p aprender-train -- -D warnings`
    3. `cargo test -p apr-cli --lib data_tweeteval` (the seven FALSIFY-TWEET-EVAL tests must pass)
    4. `cargo test -p apr-cli --lib eval` and `cargo test -p aprender-train --lib classification`
       (the F_avg additions)
    If any check is red, STOP and surface the failure — do not silently edit the baseline
    (that would violate D-06 "as-is"). A pure `cargo fmt` mechanical fix is the only permitted
    touch, and it must be called out in the commit body if applied.

    Confirm `docs/examples/` is not gitignored: `git check-ignore -v docs/examples/tweet-eval-stance.md`
    must return exit 1 (CB-510 discipline).

    Commit on the current branch with message:
    `feat(data): land TweetEval abortion-stance baseline as-is (D-06, pre-Phase-2)` and a body
    noting: 813-line baseline; carries contract tweet-eval-stance-benchmark-v1.yaml with seven
    passing falsification tests; landed unmodified so the Phase 2 relocation into
    aprender-contrastive-data diffs against a tracked baseline. Per D-06 this work is intended to
    ride as its own PR; the invariant that MUST hold is: one standalone commit containing exactly
    these files, created before any other Phase 2 change. If repository branch state permits a
    dedicated PR (per CLAUDE.md autonomous-mode PR flow), open one; otherwise the standalone
    commit satisfies the bisectability rationale of D-06.
  </action>
  <verify>
    <automated>git log -1 --stat | grep -c "data_tweeteval" && cargo test -p apr-cli --lib data_tweeteval 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --stat` (for the baseline commit) lists crates/apr-cli/src/commands/data_tweeteval.rs, contracts/tweet-eval-stance-benchmark-v1.yaml, and docs/examples/tweet-eval-stance.md
    - `git status --short` no longer shows any of the twelve files as modified/untracked
    - `git status --short` still shows `.serena/` as untracked (proof it was not swept into the commit)
    - `cargo test -p apr-cli --lib data_tweeteval` exits 0 with all falsification tests passing
    - `git diff <baseline-commit>~1..<baseline-commit> -- crates/apr-cli/src/commands/data_tweeteval.rs` shows the file as ADDED (new), not modified — proof of zero pre-commit edits beyond optional cargo fmt
    - `git check-ignore -v docs/examples/tweet-eval-stance.md` exits 1
  </acceptance_criteria>
  <done>The D-06 baseline exists as one standalone tracked commit with all its tests green and zero content edits.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the tweet-eval contract to pv-valid shape and wire it into $(CONTRACTS)</name>
  <files>contracts/tweet-eval-stance-benchmark-v1.yaml, Makefile</files>
  <read_first>
    - contracts/tweet-eval-stance-benchmark-v1.yaml (current shape — committed as-is by Task 1)
    - contracts/setfit-encoder-conformance-v1.yaml (the ONLY pv-passing precedent: `proof_obligations:` near line 360, `kani_harnesses:` near line 812, `qa_gate:` near line 849 — copy the section shapes exactly)
    - Makefile lines 870–920 (PV_BIN definition at line ~874, `CONTRACTS :=` explicit list starting line ~876, and the target that iterates $(CONTRACTS))
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md (Contracts section — the five section-shape excerpts)
  </read_first>
  <action>
    Restructure `contracts/tweet-eval-stance-benchmark-v1.yaml` so `pv validate` passes, WITHOUT
    weakening anything it already asserts. Preserve unchanged: the split sizes (587/66/280), label
    map, official F_avg invariant, few-shot protocol (shots and contracted seeds), provenance
    requirements, all seven existing falsification tests, and gate F-TWEET-EVAL-001.

    Add the two missing pv-required sections, following the setfit-encoder-conformance-v1.yaml
    shapes verbatim:

    `proof_obligations:` — at minimum:
    - OBLIG-TWEET-EVAL-COUNTS: canonical splits contain exactly 587/66/280 rows with the exact
      per-class counts the contract already declares; any deviation is a typed error before write.
    - OBLIG-TWEET-EVAL-REVISION-HONESTY: `revision_verified` is true ONLY when this run fetched
      files from the pinned revision 4fbd22cd78421f05b1ecdb4fc5725bc7a7bd8f66; a local `--source`
      run must record false. Binds FALSIFY-TWEET-EVAL-006.
    - OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES: every recorded SHA-256 is computed from the SAME
      byte buffer that was parsed and validated (read-once discipline; two reads of one source is
      a contract violation).

    `kani_harnesses:` — at least one, in the precedent shape (id, obligation, property, bound,
    strategy: bounded_int, solver: cadical, harness name). A suitable one:
    KANI-TWEET-EVAL-001 — the label index → LABEL_NAMES mapping is always in-bounds for accepted
    rows (bound 4).

    Then run `pv diff contracts/tweet-eval-stance-benchmark-v1.yaml HEAD` (pv is the ONLY
    sanctioned contract tool — never a bash/yq workaround) and apply the semver bump it suggests
    to the metadata version.

    Validate with the canonical invocation: `cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml`.
    Per Assumption A2: if pv rejects the restructured contract for CONTENT reasons the precedent
    shape cannot fix, STOP and surface — extending the pv schema is its own engineering ticket
    (CLAUDE.md fix options 2/3), not something to work around here.

    Finally, append `contracts/tweet-eval-stance-benchmark-v1.yaml` as a new continuation line to
    the `CONTRACTS :=` explicit list in the Makefile (line ~876; it is a hardcoded list, NOT a
    glob — the YAML file existing in contracts/ does nothing by itself). Do not add
    contrastive-pair-protocol-v1.yaml yet — that file does not exist until plan 02-02.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml && grep -c "tweet-eval-stance-benchmark-v1.yaml" Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/tweet-eval-stance-benchmark-v1.yaml` exits 0 (no PROVABILITY-001)
    - The contract contains `proof_obligations:` with OBLIG-TWEET-EVAL-COUNTS, OBLIG-TWEET-EVAL-REVISION-HONESTY, OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES
    - The contract contains `kani_harnesses:` with at least KANI-TWEET-EVAL-001
    - All seven pre-existing FALSIFY-TWEET-EVAL-00x entries are still present verbatim in `falsification_tests:`
    - `grep -A40 "^CONTRACTS :=" Makefile` output contains `contracts/tweet-eval-stance-benchmark-v1.yaml`
    - The Makefile target that iterates $(CONTRACTS) completes with the new entry included (run it once)
    - `cargo test -p apr-cli --lib data_tweeteval` still exits 0 (the restructure changed no behavior)
  </acceptance_criteria>
  <done>The tweet-eval contract passes pv validate, is reachable from tier3 via $(CONTRACTS), and lost none of its existing falsification power.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream raw.githubusercontent.com → CLI | Pinned-revision dataset bytes cross into the loader (baseline code) |
| local `--source` dir → CLI | User-supplied bytes claim to be the pinned dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data_tweeteval.rs dataset ingestion | mitigate | Exact per-split/per-class count contracts + SHA-256 from the parsed bytes (baseline behavior, landed unchanged; OBLIG-TWEET-EVAL-COUNTS + OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES make it a pv-checked obligation) |
| T-02-02 | Spoofing/Repudiation | provenance manifest (`revision_verified`) | mitigate | FALSIFY-TWEET-EVAL-006 preserved verbatim; OBLIG-TWEET-EVAL-REVISION-HONESTY added in Task 2 |
| T-02-03 | Information Disclosure | tweet text licensing | mitigate | Baseline never commits tweet text (JSONL is user-generated output, not repo content); docs example documents offline/mirrored prep |
| T-02-SC | Tampering | cargo installs | accept | This plan installs zero packages; RESEARCH Package Legitimacy Audit records zero new third-party deps for the whole phase |
</threat_model>

<verification>
- `git log` shows the standalone D-06 baseline commit followed by the contract-restructure commit
- `cargo test -p apr-cli --lib data_tweeteval` green (7 falsification tests)
- `cargo test -p apr-cli --lib eval` and `cargo test -p aprender-train --lib classification` green
- `pv validate contracts/tweet-eval-stance-benchmark-v1.yaml` exits 0
- The tier3 contract-validation target iterates a $(CONTRACTS) list containing the tweet-eval contract
</verification>

<success_criteria>
- D-06 satisfied: baseline tracked as-is in its own commit, zero content edits
- Finding F4 closed for the tweet-eval contract: pv-valid and tier3-reachable
- No behavior change anywhere: all baseline tests pass unmodified
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-01-SUMMARY.md` when done
</output>
