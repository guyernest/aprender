---
phase: 02-deterministic-pair-and-data-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/apr-cli/src/commands/data_tweeteval.rs
  - crates/apr-cli/src/commands/mod.rs
  - crates/apr-cli/src/commands/eval/mod.rs
  - crates/apr-cli/src/commands/eval/eval_mod_tests.rs
  - crates/apr-cli/src/data_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/extended_commands.rs
  - crates/aprender-train/src/eval/classification/mod.rs
  - crates/aprender-train/src/eval/classification/metrics.rs
  - crates/aprender-train/src/eval/classification/basic_tests.rs
  - contracts/tweet-eval-stance-benchmark-v1.yaml
  - docs/examples/tweet-eval-stance.md
  - Makefile
  - CLAUDE.md
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "The 813-line D-06 baseline exists as ONE standalone tracked commit whose diff SINCE THE BRANCH POINT is exactly the twelve baseline files, with every committed blob byte-identical to a SHA-256 recorded BEFORE staging (no cargo fmt, no content edits)"
    - "The baseline is independently reviewable: a stacked PR based on `gsd/phase-1-differentiable-minilm-conformance` (NOT the repository default branch — Phase 1 is not on main, so a default-base PR would carry a 75+-commit diff). If the environment cannot host a PR against a non-default base, the standalone hash-attested commit satisfies D-06 on its own and the executor continues — this is never a STOP"
    - "pv validate accepts contracts/tweet-eval-stance-benchmark-v1.yaml (currently rejected with PROVABILITY-001 x2)"
    - "make tier3's contract-validate actually reaches the tweet-eval contract (it is in the explicit $(CONTRACTS) list, which is not a glob)"
    - "Every pv diff invocation in this repo — plans AND CLAUDE.md — uses the real two-filesystem-path form; `pv diff <contract> HEAD` is impossible against crates/aprender-contracts-cli/src/cli.rs:73-78"
    - "Declared kani_harnesses are never presented as machine-checked: Kani is not installed in this repo and no #[kani::proof] exists, so each declared harness is backed by an equally-bounded runnable proptest named in its falsification test"
  artifacts:
    - path: "crates/apr-cli/src/commands/data_tweeteval.rs"
      provides: "Committed baseline: canonical + setfit profiles, count contracts, hash-from-parsed-bytes, revision_verified honesty"
      contains: "revision_verified"
    - path: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      provides: "pv-valid contract: existing falsification tests + new proof_obligations + kani_harnesses with honest verification status"
      contains: "proof_obligations"
    - path: "Makefile"
      provides: "$(CONTRACTS) list containing the tweet-eval contract"
      contains: "contracts/tweet-eval-stance-benchmark-v1.yaml"
    - path: "CLAUDE.md"
      provides: "Corrected pv diff usage example (two filesystem paths, git show to materialize the old revision)"
      contains: "git show"
  key_links:
    - from: "Makefile"
      to: "contracts/tweet-eval-stance-benchmark-v1.yaml"
      via: "$(CONTRACTS) explicit list consumed by tier3's contract-validate target (Makefile line ~253)"
      pattern: "tweet-eval-stance-benchmark-v1"
---

<objective>
Land the uncommitted D-06 baseline exactly as-is in its own commit and its own PR, then make the
tweet-eval contract a real gate: restructure it to the pv-valid shape and wire it into the
Makefile $(CONTRACTS) list so tier3 exercises it.

Purpose: D-06 (locked) requires the 813 uncommitted lines to land as-is FIRST so the Phase 2
relocation into `aprender-contrastive-data` is a reviewable diff against a tracked baseline.
RESEARCH Finding F4 proved `pv validate` rejects the contract today (PROVABILITY-001 x2: no
proof_obligations, no kani_harnesses) and that it escapes tier3 only because $(CONTRACTS) is an
explicit hardcoded list — without this plan the D-24 contract gate is theater.
Output: baseline commit + its own PR + pv-valid contract + $(CONTRACTS) wiring + a corrected
repo-wide `pv diff` usage example.

## Review corrections incorporated (02-REVIEWS.md)

- **V2 / Codex 02-01 HIGH — `pv diff <contract> HEAD` is invalid.** Verified against
  `crates/aprender-contracts-cli/src/cli.rs:73-78` (`Diff { old: PathBuf, new: PathBuf }`).
  Task 2 materializes the old contract from the baseline commit with `git show` into a temp path
  and diffs two real files. `CLAUDE.md:431` documents the same wrong form and is corrected here —
  the plans inherited the defect from the repo doc, so fixing only the plans would leave the trap
  armed for the next reader.
- **F17 / Codex 02-01 HIGH — `cargo test ... | tail -1` reads `tail`'s status.** Every
  `<automated>` block in this plan captures status directly
  (`cmd > /tmp/log 2>&1; rc=$?; [ "$rc" -eq 0 ]`) and inspects saved output separately, per
  CLAUDE.md Verification Discipline rule 1.
- **F17 / Codex 02-01 MEDIUM — "zero content edits" conflicted with permitting `cargo fmt`, and
  an untracked file appearing as ADDED does not prove it was unchanged.** Task 1 now records
  SHA-256 of every baseline file BEFORE staging and re-verifies after committing; `cargo fmt` is
  run in `--check` mode only and is never permitted to write in this task.
- **F23 / Codex 02-01 MEDIUM — PR sequencing was ambiguous.** Task 1 now creates a dedicated
  branch and opens the baseline PR; Task 2 branches FROM the baseline commit so the baseline PR's
  diff can never grow. The D-06 invariant being protected is stated literally.
- **Checker blocker 1 (this revision) — the baseline PR gate was unachievable against real repo
  state.** Verified this session: the current branch is
  `gsd/phase-1-differentiable-minilm-conformance`, `git rev-list --count main..HEAD` is **>= 1**
  (75 at authoring time, 76 one session later, and growing with every further planning commit — the
  load-bearing fact is that it is not zero, never the exact number),
  `gh pr list --state open` is **empty** (Phase 1 has NOT landed on `main`), and
  `.planning/config.json` sets `git.branching_strategy: "none"`. A PR targeting the repository
  default branch would therefore render a 75+-commit diff, so this task's "exactly twelve files"
  acceptance check — and `must_haves.truths[0]`, and Task 2's "baseline PR file list unchanged"
  check — would all fail, and the STOP escape hatch would halt Wave 1 and with it the phase.
  **Corrected:** the baseline PR is STACKED on `gsd/phase-1-differentiable-minilm-conformance`,
  which yields the clean twelve-file diff and fully honors D-06's intent (a bisectable,
  independently reviewable baseline diff), and every assertion evaluates the diff SINCE THE BRANCH
  POINT rather than against the default branch. A documented, explicitly non-STOP fallback covers
  environments that cannot host a non-default-base PR. Task 1 also now states the branch/PR policy
  for waves 2-6 ONCE, so the rest of the phase is not left with an implied working branch and no
  policy (checker warning 8).
- **F19 / Codex 02-01 LOW — Kani entries are metadata only.** Verified: `cargo-kani` is not on
  PATH and the repository contains zero `#[kani::proof]` harnesses, including for Phase 1's
  `setfit-encoder-conformance-v1.yaml`. **Partially rejected as scoped:** installing and wiring
  Kani is a toolchain addition outside this phase (and outside its Environment Availability
  table). Accepted in substance: each declared harness must carry an explicit
  not-yet-executed statement in its `property` prose and must be backed by a runnable,
  identically-bounded proptest named by its falsification test, so nothing in this repo can be
  read as claiming machine-checked proof it does not have. The repo-wide gap is surfaced in the
  SUMMARY, not silently inherited.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md
@.planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the D-06 baseline hash-attested, in its own branch and PR</name>
  <files>crates/apr-cli/src/commands/data_tweeteval.rs, crates/apr-cli/src/commands/mod.rs, crates/apr-cli/src/commands/eval/mod.rs, crates/apr-cli/src/commands/eval/eval_mod_tests.rs, crates/apr-cli/src/data_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/extended_commands.rs, crates/aprender-train/src/eval/classification/mod.rs, crates/aprender-train/src/eval/classification/metrics.rs, crates/aprender-train/src/eval/classification/basic_tests.rs, contracts/tweet-eval-stance-benchmark-v1.yaml, docs/examples/tweet-eval-stance.md</files>
  <read_first>
    - Output of `git status --short` (confirm the exact uncommitted set before staging)
    - Output of `git branch --show-current` and `git rev-list --count main..HEAD` (expected: `gsd/phase-1-differentiable-minilm-conformance`, and a count **>= 1** — Phase 1 has NOT landed on `main`, which is why the baseline PR is stacked on the Phase 1 branch rather than targeting the default branch. **Do not pin the exact number and do not treat a different one as a discrepancy to surface or stop on:** it was 75 when this plan was written and 76 one session later, and it grows with every further planning commit on this branch. The fact being established is "not on `main`", not the count. Every load-bearing gate in this task is branch-point-relative and unaffected)
    - `.planning/config.json` `git.branching_strategy` (expected `"none"` — GSD is not managing branches for this phase, so STEP 6's policy is this plan's to set)
    - crates/apr-cli/src/commands/data_tweeteval.rs (the baseline being landed — confirm it is the 813-line loader with canonical + setfit profiles, `BENCHMARK_SEEDS: [u64; 10] = [13, 17, 23, 29, 31, 37, 41, 43, 47, 53]` at line ~45, and `revision_verified`)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-CONTEXT.md (D-06 verbatim)
    - CLAUDE.md "Git Workflow (Branch Protection)" and "Autonomous Operation Mode" sections (branch + PR + auto-merge flow)
  </read_first>
  <action>
    Land the D-06 baseline per the locked decision: as-is, in its own commit, in its own PR,
    before ANY Phase 2 code change.

    STEP 1 — record the pre-staging bytes (this is the proof, not `git status`). Write
    `/tmp/d06-baseline.sha256` containing `shasum -a 256` of each of the twelve files listed in
    `<files>`, in that order. An untracked file appearing as ADDED in `git show --stat` proves
    only that it is new; it does NOT prove the committed bytes equal the bytes that were tested.
    The recorded digest does.

    STEP 2 — prove the baseline is green WITHOUT editing it. Run each check separately,
    capturing its status directly and never through a pipe (CLAUDE.md Verification Discipline
    rule 1 — `cmd > /tmp/x.log 2>&1; rc=$?`):
      a. `cargo fmt -p apr-cli --check` and `cargo fmt -p aprender-train --check`, both captured
         into `/tmp/d06-fmt.log` — that exact path is what this task's acceptance criteria read
         (**--check only; this task never runs a formatting write**. If either fails, STOP and
         surface the diff — a `cargo fmt` write here would contradict D-06 "as-is" and would
         invalidate the STEP 1 digests.)
      b. `cargo clippy -p apr-cli -- -D warnings`; `cargo clippy -p aprender-train -- -D warnings`
      c. `cargo test -p apr-cli --lib data_tweeteval` (the seven FALSIFY-TWEET-EVAL tests)
      d. `cargo test -p apr-cli --lib eval`; `cargo test -p aprender-train --lib classification`
    If any check is red, STOP and surface the failure. Do not edit the baseline.

    STEP 3 — CB-510 check: `git check-ignore -v docs/examples/tweet-eval-stance.md` must exit 1.

    STEP 4 — branch and commit. Create a dedicated branch `gsd/phase-2-d06-baseline` from the
    current HEAD (uncommitted work carries over). Stage EXACTLY the twelve files in `<files>` —
    nothing else. Explicitly do NOT stage: anything under `.planning/`, anything under `.serena/`,
    or `.planning/phases/02-deterministic-pair-and-data-protocol/*.md`.
    Commit message: `feat(data): land TweetEval abortion-stance baseline as-is (D-06, pre-Phase-2)`
    with a body noting: 813-line baseline; carries contract tweet-eval-stance-benchmark-v1.yaml
    with seven passing falsification tests; landed unmodified so the Phase 2 relocation into
    aprender-contrastive-data diffs against a tracked baseline.

    STEP 5 — re-verify the committed bytes. Confirm `git status --porcelain -- <the twelve files>`
    is EMPTY (working tree equals HEAD for those paths) and then
    `shasum -a 256 -c /tmp/d06-baseline.sha256` passes. Together these prove the committed blobs
    are byte-identical to the bytes recorded in STEP 1 and tested in STEP 2.

    STEP 6 — open the baseline PR (D-06's "own PR", made explicit per review finding F23).
    **Base branch: `gsd/phase-1-differentiable-minilm-conformance` — a STACKED PR, not one
    targeting the repository default branch.** Verified this session: the current branch is
    `gsd/phase-1-differentiable-minilm-conformance`, `git rev-list --count main..HEAD` is >= 1
    (75+ and growing — never assert an exact count), and
    `gh pr list --state open` is empty, so Phase 1 has not landed on `main`. A default-base PR
    would render a 75+-commit diff rather than these twelve files, and its own acceptance check
    would fail. Stacking on the Phase 1 branch yields exactly the twelve-file diff and fully
    honors D-06's intent: a bisectable, independently reviewable baseline diff.

    Push `gsd/phase-2-d06-baseline` and run
      `gh pr create --base gsd/phase-1-differentiable-minilm-conformance --head gsd/phase-2-d06-baseline`
    with title `feat(data): TweetEval abortion-stance baseline (D-06)` and a body explaining that
    this PR is deliberately a pure code-motion-free baseline landing whose entire purpose is to
    make the Phase 2 relocation a reviewable diff, and that it is stacked on the Phase 1 branch
    because Phase 1 is not yet on `main`. **Do NOT arm auto-merge** — auto-merging into an
    unlanded Phase 1 branch would reorder the stack; leave the PR open for review and record its
    number in the SUMMARY.

    Assert the diff SINCE THE BRANCH POINT, never against the default branch:
      `git rev-list --count gsd/phase-1-differentiable-minilm-conformance..HEAD` is 1
      `git diff --name-only gsd/phase-1-differentiable-minilm-conformance...HEAD` lists exactly
        the twelve files (three-dot = merge-base diff, which is what the PR renders)
      `gh pr view <N> --json baseRefName,files` shows the Phase 1 branch as base and those twelve
        files and no others

    **Documented fallback — the executor must NEVER stop here.** If the environment cannot host a
    PR against a non-default base (no `gh` auth, fork restrictions, protected-base rules), SKIP
    the PR and proceed to Task 2. The standalone hash-attested commit satisfies D-06 on its own:
    the SHA-256 digest recorded in STEP 1 and re-verified in STEP 5 is STRONGER evidence of
    byte-identity than a PR is — a PR proves review availability, a digest proves the bytes. In
    that case record in the SUMMARY (a) which mechanism failed and its exact error, (b) the
    baseline commit SHA, (c) the digest verification result, and (d) that D-06's "own PR" clause
    was discharged by the standalone hash-attested commit. The only thing that is never
    permissible is folding the contract restructure into the baseline commit.

    **The invariant this task protects, stated literally:** the baseline exists as ONE standalone
    commit whose diff since the branch point is exactly these twelve files, created before any
    other Phase 2 change, and bisectable. Task 2 branches FROM this commit precisely so this diff
    can never grow.

    **Branch and PR policy for the REST of Phase 2 — stated once, here; plans 02-02..02-09 inherit
    it and do not restate it (checker warning 8).** `.planning/config.json` sets
    `git.branching_strategy: "none"`, so GSD is not managing branches for this phase and the
    policy is this plan's to set. All Phase 2 work after this commit rides on ONE working branch,
    `gsd/phase-2-contract-gate`, created by Task 2 FROM the baseline commit. Every plan in waves
    2-6 commits to that branch: they do NOT create their own branches and do NOT open their own
    PRs. The phase ships as exactly two PRs — (1) this baseline PR, and (2) one Phase 2 PR opened
    from `gsd/phase-2-contract-gate` after wave 6, stacked on the same base while Phase 1 remains
    unlanded. Rebasing the Phase 2 branch onto `main` after Phase 1 lands is a post-phase action,
    not a plan step. If the fallback above was taken (no PRs possible), the same single-branch
    rule still holds and the phase ships as commits on `gsd/phase-2-contract-gate`.
  </action>
  <verify>
    <automated>shasum -a 256 -c /tmp/d06-baseline.sha256 > /tmp/d06-verify.log 2>&1; rc=$?; git status --porcelain -- crates/apr-cli/src/commands/data_tweeteval.rs contracts/tweet-eval-stance-benchmark-v1.yaml docs/examples/tweet-eval-stance.md > /tmp/d06-status.log 2>&1; s=$?; cargo test -p apr-cli --lib data_tweeteval > /tmp/d06-test.log 2>&1; t=$?; [ "$rc" -eq 0 ] && [ "$s" -eq 0 ] && [ ! -s /tmp/d06-status.log ] && [ "$t" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --stat` for the baseline commit lists exactly the twelve files in `<files>` and no others
    - `shasum -a 256 -c /tmp/d06-baseline.sha256` exits 0 AND `git status --porcelain -- <the twelve files>` produces empty output — together proving the committed blobs equal the pre-staging bytes
    - `/tmp/d06-fmt.log` records `cargo fmt -p apr-cli --check` exiting 0; the repository contains no `cargo fmt` write in this task's history (the SUMMARY states this explicitly)
    - `cargo test -p apr-cli --lib data_tweeteval` exits 0 (status captured directly, not read through a pipe) with all seven FALSIFY-TWEET-EVAL tests passing
    - `git status --short` still shows `.serena/` as untracked (proof it was not swept into the commit)
    - `git check-ignore -v docs/examples/tweet-eval-stance.md` exits 1
    - `git rev-list --count gsd/phase-1-differentiable-minilm-conformance..HEAD` is 1 and `git diff --name-only gsd/phase-1-differentiable-minilm-conformance...HEAD` lists exactly the twelve files — this is the diff-since-branch-point assertion, and it holds whether or not a PR was opened
    - If the PR was opened: `gh pr view <N> --json baseRefName,files` shows base `gsd/phase-1-differentiable-minilm-conformance` and exactly those twelve files, and the PR number is recorded in the SUMMARY. If the fallback was taken: the SUMMARY records the failure mechanism, the baseline commit SHA, the digest result, and that D-06's own-PR clause was discharged by the standalone hash-attested commit
    - The SUMMARY states the Phase 2 branch/PR policy verbatim (one working branch `gsd/phase-2-contract-gate` for waves 2-6; no per-plan branches or PRs), so plans 02-02..02-09 have a single place to read it
  </acceptance_criteria>
  <done>The D-06 baseline exists as one standalone hash-attested commit on its own branch whose diff since the branch point is exactly the twelve files, reviewable as a stacked PR (or, on the documented fallback, as the digest-attested commit itself), all tests green, zero content edits proven by digest rather than by git status alone; and the branch/PR policy for waves 2-6 is written down once.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the tweet-eval contract to pv-valid shape, wire $(CONTRACTS), fix the repo's pv diff example</name>
  <files>contracts/tweet-eval-stance-benchmark-v1.yaml, Makefile, CLAUDE.md</files>
  <read_first>
    - contracts/tweet-eval-stance-benchmark-v1.yaml (current shape — committed as-is by Task 1)
    - contracts/setfit-encoder-conformance-v1.yaml (the ONLY pv-passing precedent: `proof_obligations:` near line 360, `falsification_tests:` near 613, `kani_harnesses:` near 812 with fields id/obligation/property/bound/strategy/solver/harness, `qa_gate:` near 849 — copy the section shapes exactly)
    - crates/aprender-contracts-cli/src/cli.rs lines 60-90 (the REAL `Diff { old: PathBuf, new: PathBuf }` signature — this is why `pv diff <contract> HEAD` cannot work)
    - Makefile lines 866-940 (PV_BIN at ~874, BINDING at ~875, `CONTRACTS :=` explicit list starting ~876, `contract-validate` at ~920, `contract-audit` at ~932, and tier3's `$(MAKE) contract-validate` at ~253 with its D-26 evidence comment block)
    - CLAUDE.md line ~431 (the incorrect `pv diff contracts/apr-mcp-server-v1.yaml HEAD~3` example)
    - .planning/phases/02-deterministic-pair-and-data-protocol/02-PATTERNS.md (Contracts section — the five section-shape excerpts)
  </read_first>
  <action>
    Create branch `gsd/phase-2-contract-gate` FROM the Task 1 baseline commit (so the baseline PR's
    diff cannot grow — review finding F23), then do the following on it.

    (A) Restructure `contracts/tweet-eval-stance-benchmark-v1.yaml` so `pv validate` passes,
    WITHOUT weakening anything it already asserts. Preserve unchanged: the split sizes
    (587/66/280), label map, official F_avg invariant, the few-shot protocol including the ten
    contracted seeds `[13, 17, 23, 29, 31, 37, 41, 43, 47, 53]` (these are the values in
    `data_tweeteval.rs:45` — cite the source, never invent a seed list), provenance requirements,
    all seven existing falsification tests, and gate F-TWEET-EVAL-001.

    Add the two missing pv-required sections, following the setfit-encoder-conformance-v1.yaml
    shapes verbatim.

    `proof_obligations:` — at minimum:
    - OBLIG-TWEET-EVAL-COUNTS: canonical splits contain exactly 587/66/280 rows with the exact
      per-class counts the contract already declares (train [159,319,109], validation [18,36,12],
      test [45,189,46]); any deviation is a typed error before write.
    - OBLIG-TWEET-EVAL-REVISION-HONESTY: `revision_verified` is true ONLY when this run fetched
      files from the pinned revision 4fbd22cd78421f05b1ecdb4fc5725bc7a7bd8f66; a local `--source`
      run must record false. Binds FALSIFY-TWEET-EVAL-006.
    - OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES: every recorded SHA-256 is computed from the SAME
      byte buffer that was parsed and validated (read-once discipline; two reads of one source is
      a contract violation).
    - OBLIG-TWEET-EVAL-SEED-SET: the ten contracted benchmark seeds are exactly
      [13, 17, 23, 29, 31, 37, 41, 43, 47, 53]; 42 is NOT a contracted seed and any tool
      defaulting to it is defaulting outside the contract. (This obligation is what plan 02-09's
      required-`--seed` CLI design discharges — review finding F16.)

    `kani_harnesses:` — pv's PROVABILITY-001 rule requires the section. Add at least
    KANI-TWEET-EVAL-001: the label index → LABEL_NAMES mapping is always in-bounds for accepted
    rows (bound 4, strategy bounded_int, solver cadical, harness `verify_label_index_in_bounds`).
    **Honesty requirement (review finding F19):** `cargo-kani` is not installed in this repository
    and no `#[kani::proof]` harness exists anywhere in `crates/` — verified this session. The
    `property:` prose of every harness entry MUST therefore state, in words, that the harness is
    DECLARED and NOT YET EXECUTED in this repository, and MUST name the runnable, identically
    bounded proptest that stands in as executable evidence
    (`cargo test -p apr-cli --lib data_tweeteval label_index`). Add that proptest if it does not
    already exist. Do not write any text that could be read as claiming a machine-checked proof.
    Record in the SUMMARY that Phase 1's `setfit-encoder-conformance-v1.yaml` has the same
    declared-not-executed gap — a repo-wide observation this plan surfaces but does not fix.

    (B) Semver bump via the REAL `pv diff` interface. `pv diff` takes TWO FILESYSTEM PATHS
    (`crates/aprender-contracts-cli/src/cli.rs:73-78`); `pv diff <contract> HEAD` resolves "HEAD"
    as a path and fails. Materialize the old contract first:
      `git show <task-1-baseline-commit>:contracts/tweet-eval-stance-benchmark-v1.yaml > /tmp/tweet-eval-old.yaml`
    then run
      `cargo run --release -p aprender-contracts-cli --bin pv -- diff /tmp/tweet-eval-old.yaml contracts/tweet-eval-stance-benchmark-v1.yaml`
    and apply the semver bump it suggests to the metadata version. `pv` remains the only
    sanctioned contract tool — never a bash/yq/python workaround.

    (C) Validate: `cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml`.
    Per Assumption A2: if pv rejects the restructured contract for CONTENT reasons the precedent
    shape cannot fix, STOP and surface — extending the pv schema is its own engineering ticket
    (CLAUDE.md fix options 2/3), not something to work around here.

    (D) Append `contracts/tweet-eval-stance-benchmark-v1.yaml` as a new continuation line to the
    `CONTRACTS :=` explicit list in the Makefile (line ~876; it is a hardcoded list, NOT a glob —
    the YAML file existing in `contracts/` does nothing by itself; tier3 reaches it only via
    `$(MAKE) contract-validate` at line ~253). Do not add contrastive-pair-protocol-v1.yaml yet —
    that file does not exist until plan 02-02.

    (E) Fix `CLAUDE.md` line ~431. Replace `pv diff contracts/apr-mcp-server-v1.yaml HEAD~3` with
    the two-path form plus the `git show` materialization step, and add a one-line note that
    `pv diff` takes two filesystem paths (citing `crates/aprender-contracts-cli/src/cli.rs`). This
    doc defect is what the plans inherited (review adjudicated claim #2); leaving it would re-arm
    the trap for the next reader.
  </action>
  <verify>
    <automated>cargo run --release -p aprender-contracts-cli --bin pv -- validate contracts/tweet-eval-stance-benchmark-v1.yaml > /tmp/pv-tweeteval.log 2>&1; rc=$?; grep -v '^#' Makefile | grep -c "contracts/tweet-eval-stance-benchmark-v1.yaml" > /tmp/mk-count.txt 2>&1; n=$(cat /tmp/mk-count.txt); cargo test -p apr-cli --lib data_tweeteval > /tmp/te-test.log 2>&1; t=$?; [ "$rc" -eq 0 ] && [ "$n" -ge 1 ] && [ "$t" -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/tweet-eval-stance-benchmark-v1.yaml` exits 0 (no PROVABILITY-001); status captured directly, not through a pipe
    - The contract contains `proof_obligations:` with OBLIG-TWEET-EVAL-COUNTS, OBLIG-TWEET-EVAL-REVISION-HONESTY, OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES, OBLIG-TWEET-EVAL-SEED-SET
    - The contract contains `kani_harnesses:` with KANI-TWEET-EVAL-001 whose `property` prose states it is declared and not executed in this repository and names its runnable proptest
    - `grep -v '^#' contracts/tweet-eval-stance-benchmark-v1.yaml | grep -c "13, 17, 23, 29, 31, 37, 41, 43, 47, 53"` ≥ 1 (the contracted seed list is the one in data_tweeteval.rs, not an invented one)
    - All seven pre-existing FALSIFY-TWEET-EVAL-00x entries are still present verbatim in `falsification_tests:`
    - `grep -v '^#' Makefile | grep -c "contracts/tweet-eval-stance-benchmark-v1.yaml"` ≥ 1 and the line sits inside the `CONTRACTS :=` continuation block
    - `make contract-validate > /tmp/cv.log 2>&1; rc=$?` gives rc=0 with the new entry present in the log
    - `grep -c "pv diff contracts/apr-mcp-server-v1.yaml HEAD~3" CLAUDE.md` returns 0, and CLAUDE.md now contains a `git show` materialization step before a two-path `pv diff`
    - `cargo test -p apr-cli --lib data_tweeteval` still exits 0 (the restructure changed no behavior)
    - The baseline diff did not grow: `git diff --name-only gsd/phase-1-differentiable-minilm-conformance...<task-1-baseline-commit>` still lists exactly the twelve files. If a PR was opened, `gh pr view <N> --json files` still shows exactly those twelve files; if the Task 1 fallback was taken, the git assertion alone is the check (this is why Task 2 branches FROM the baseline commit)
  </acceptance_criteria>
  <done>The tweet-eval contract passes pv validate, is reachable from tier3 via $(CONTRACTS), lost none of its existing falsification power, declares no unearned proof, and the repo's own pv diff documentation no longer teaches an invalid invocation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream raw.githubusercontent.com → CLI | Pinned-revision dataset bytes cross into the loader (baseline code) |
| local `--source` dir → CLI | User-supplied bytes claim to be the pinned dataset |
| repository documentation → future engineers | An incorrect documented command propagates into plans and scripts (adjudicated claim #2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data_tweeteval.rs dataset ingestion | mitigate | Exact per-split/per-class count contracts + SHA-256 from the parsed bytes (baseline behavior, landed unchanged; OBLIG-TWEET-EVAL-COUNTS + OBLIG-TWEET-EVAL-HASH-FROM-PARSED-BYTES make it a pv-checked obligation) |
| T-02-02 | Spoofing/Repudiation | provenance manifest (`revision_verified`) | mitigate | FALSIFY-TWEET-EVAL-006 preserved verbatim; OBLIG-TWEET-EVAL-REVISION-HONESTY added in Task 2 |
| T-02-03 | Information Disclosure | tweet text licensing | mitigate | Baseline never commits tweet text (JSONL is user-generated output, not repo content); docs example documents offline/mirrored prep |
| T-02-33 | Tampering | baseline commit not byte-identical to what was tested | mitigate | Pre-staging SHA-256 record + post-commit `shasum -c` + empty `git status --porcelain` (Task 1 STEP 1/5); `cargo fmt` write forbidden in this task |
| T-02-34 | Repudiation | unearned verification claims (declared-but-unexecuted Kani harnesses) | mitigate | Every harness entry states declared-not-executed and names a runnable equally-bounded proptest (Task 2, review finding F19) |
| T-02-SC | Tampering | cargo installs | accept | This plan installs zero packages; RESEARCH Package Legitimacy Audit records zero new third-party deps for the whole phase |
</threat_model>

<verification>
- `git log` shows the standalone D-06 baseline commit; `git diff --name-only gsd/phase-1-differentiable-minilm-conformance...<baseline-commit>` lists exactly twelve files (and the stacked baseline PR, if opened, shows the same twelve against base `gsd/phase-1-differentiable-minilm-conformance`)
- `shasum -a 256 -c /tmp/d06-baseline.sha256` passes after the commit
- `cargo test -p apr-cli --lib data_tweeteval` green (7 falsification tests), status captured directly
- `cargo test -p apr-cli --lib eval` and `cargo test -p aprender-train --lib classification` green
- `pv validate contracts/tweet-eval-stance-benchmark-v1.yaml` exits 0
- `make contract-validate` exits 0 over a $(CONTRACTS) list containing the tweet-eval contract
- CLAUDE.md no longer documents an invalid `pv diff` invocation
</verification>

<success_criteria>
- D-06 satisfied: baseline tracked as-is in its own commit, independently reviewable as a stacked PR (or, on the documented fallback, as the digest-attested commit), zero content edits proven by digest
- The Phase 2 branch/PR policy for waves 2-6 is recorded once and inherited by every later plan
- Finding F4 closed for the tweet-eval contract: pv-valid and tier3-reachable
- Review findings V2, F17, F19, F23 closed at their source (including the CLAUDE.md defect)
- No behavior change anywhere: all baseline tests pass unmodified
</success_criteria>

<output>
Create `.planning/phases/02-deterministic-pair-and-data-protocol/02-01-SUMMARY.md` when done
</output>
