---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 07
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - crates/apr-cli/tests/setfit_cli_lifecycle.rs
  - crates/aprender-train/tests/setfit_apr_lifecycle.rs
  - crates/aprender-train/src/train/setfit/verify.rs
  - crates/aprender-train/src/train/setfit/apr_evaluate_tests.rs
  - crates/apr-cli/tests/setfit_parity.rs
  - crates/apr-cli/src/commands/setfit_train.rs
  - crates/apr-cli/src/commands/predict_tests.rs
  - crates/apr-cli/src/commands/eval/setfit_tests.rs
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "One user-produced setfit-apr-v1 exists: the spawned `apr setfit train -> inspect -> eval(validation --lock-out) -> eval(test --selection-lock) -> predict` chain completes with exit 0 at every rung on the production encoder (D-01's proof obligation; 04-15's rung 4 flips from exit 6 to 0)"
    - "The same artifact SHA-256 appears in inspect, eval, and predict outputs (one artifact through every surface)"
    - "Every stale 'no user-reachable path produces a setfit-apr-v1' prose claim in the F-10 blast radius is re-worded to the post-unblock truth; the slice-encoder REFUSAL tests stay green (the slice still cannot compute probe_unicode)"
  artifacts:
    - path: "crates/apr-cli/tests/setfit_cli_lifecycle.rs"
      provides: "Positive production-chain rungs, env-gated on the production checkout"
      contains: "APRENDER_MINILM_DIR"
  key_links:
    - from: "crates/apr-cli/tests/setfit_cli_lifecycle.rs"
      to: "env!(\"CARGO_BIN_EXE_apr\")"
      via: "spawned fork/exec with reaped ExitStatus — never a status through a pipe"
      pattern: "CARGO_BIN_EXE_apr"
    - from: "the production chain"
      to: "contracts/setfit-apr-v1.yaml"
      via: "the produced artifact loads through load_setfit_apr's 8-rung ladder (accept path now user-reachable)"
---

<objective>
Prove the F-10 unblock end-to-end at the binary tier: extend the 04-15 spawned ladder with
POSITIVE production rungs that produce, inspect, evaluate (lock-disciplined), and predict from a
user-produced `setfit-apr-v1` — then re-audit every file whose prose still claims the refusal.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: D-01 demands proof that one user-produced artifact exists BEFORE any benchmark-cell plan
depends on it; this evidence is also what the verifier needs for the OPS-01/OPS-02 positive tiers
(checking those boxes is the verifier's call, not this plan's).
Output: green production ladder + honest prose across the blast radius.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (F-R6; "F-10 flip blast radius"; Pattern 5 note on rung 4)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (section 14: spawned-tier rules)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Positive production ladder in setfit_cli_lifecycle.rs</name>
  <files>crates/apr-cli/tests/setfit_cli_lifecycle.rs, crates/apr-cli/src/commands/setfit_train.rs</files>
  <read_first>
    - crates/apr-cli/tests/setfit_cli_lifecycle.rs (FULL header :1-90 — the four rules; the rung table :64-71; the blocked-rung convention :56-60 with embedded restore instructions; how rungs 1-3 build the data/selection inputs)
    - crates/aprender-train/tests/full_weight_parity.rs (:54 — the APRENDER_MINILM_DIR env-gate + skip pattern)
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (frozen E/B — the train config must use them so the run's regime id is calibrated)
    - crates/apr-cli/src/commands/setfit_train.rs (:601-621 check ordering; the config file format the test writes)
  </read_first>
  <action>
    Add a new spawned test (e.g. `setfit_cli_production_chain_completes_after_the_calibration_edit`)
    to setfit_cli_lifecycle.rs, env-gated: skip with a printed message naming
    `APRENDER_MINILM_DIR` when the production checkout (default
    `~/.cache/aprender/minilm-l6-v2-1110a243`) is absent. Reuse the existing rungs-1-3 machinery to
    produce the attested dataset directory and a selection manifest (s8, seed 13 — the cheapest
    calibrated cell; timing known from 05-01's probe, pre-authorized at the 05-03 checkpoint).
    Chain, every invocation a real fork/exec of `env!("CARGO_BIN_EXE_apr")` with a reaped
    ExitStatus: (r0) `--version` exit 0 non-empty (mechanism proof); (r1) `setfit train` with a
    config at the frozen E/B and `--model-dir $APRENDER_MINILM_DIR` — exit 0 and `model.apr`
    EXISTS (the exact rung 04-15 measured at exit 6); (r2) `inspect model.apr --json` exit 0, JSON
    contains the SetFit section with a 64-hex artifact sha; (r3) `eval --split validation
    --lock-out L` exit 0, lock file exists; (r4) `eval --split test --selection-lock L` exit 0;
    (r5) `predict` with a JSON request document against model.apr exit 0 with ordered labels +
    probabilities. Assert the SAME artifact sha256 appears in r2's inspect JSON, r3/r4's eval rows,
    and r5's predict response (parse, compare — the one-artifact-through-every-surface claim).
    Leave every existing blocked-rung/refusal test untouched: their refusals are slice-dir-shaped
    and must survive (the slice still cannot compute probe_unicode). If any existing test panics
    with its embedded restore instructions, follow THOSE instructions rather than improvising.
    Run with `CARGO_INCREMENTAL=0`; pin nothing by absolute path (CARGO_BIN_EXE_apr is the pin).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --test setfit_cli_lifecycle --features setfit > /tmp/ladder.log 2>&1; rc=$?; tail -10 /tmp/ladder.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The new test exists, env-gated, and passes on this host (checkout present) — the log shows it RAN, not skipped (assert non-skip locally; the skip path is for CI)
    - r1 exit 0 with model.apr present — quoted in the SUMMARY beside 04-15's historical exit-6 record (the flip, measured)
    - One 64-hex sha asserted equal across inspect/eval/predict outputs
    - All pre-existing lifecycle tests green (no refusal weakened)
  </acceptance_criteria>
  <done>The production chain completes at the spawned tier; the artifact is user-produced and cross-surface consistent</done>
</task>

<task type="auto">
  <name>Task 2: Blast-radius prose re-audit</name>
  <files>crates/aprender-train/tests/setfit_apr_lifecycle.rs, crates/aprender-train/src/train/setfit/verify.rs, crates/aprender-train/src/train/setfit/apr_evaluate_tests.rs, crates/apr-cli/tests/setfit_parity.rs, crates/apr-cli/src/commands/predict_tests.rs, crates/apr-cli/src/commands/eval/setfit_tests.rs</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md ("F-10 flip blast radius" — the enumerated file list and which claims survive vs go stale)
    - each file in the files list (search for "F-10", "no user-reachable", "setfit-apr-v1", "cannot produce" prose)
  </read_first>
  <action>
    Audit every file in the enumerated blast radius (plus a repo-wide search:
    `pmat query "F-10 user-reachable setfit-apr-v1" --limit 10` and a targeted
    `grep -rn "no user-reachable\|F-10" crates/ --include=*.rs`) for prose claims of the form "no
    user-reachable path produces a setfit-apr-v1". Re-word each to the post-unblock truth, e.g.
    "before Phase 5's 05-03 calibration edit (commit <SHA from 05-03's SUMMARY>), no user-reachable
    path produced a setfit-apr-v1; the production regime is now calibrated and the slice fixture
    remains refused". Do NOT alter any assertion, any refusal test, or any slice-encoder behavior —
    this task edits comments/doc prose and test NAMES only where a name states a falsehood (rename
    only if the name itself claims the refusal is universal; record every rename because Make
    filters and CR-02 vacuity floors may reference names — re-run the touched suites and the
    scoped Make targets afterwards to prove no filter went vacuous). Finish by running the full
    setfit-scoped suites to prove nothing flipped.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit setfit:: > /tmp/pa1.log 2>&1; rc=$?; tail -3 /tmp/pa1.log; test $rc -eq 0 && CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit > /tmp/pa2.log 2>&1; rc2=$?; tail -3 /tmp/pa2.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "no user-reachable" crates/ --include=*.rs` returns only lines that are historically-scoped (mention the unblock commit or "before Phase 5") — zero present-tense refusal claims remain
    - Zero assertion/expect changes in the diff (the diff for these files touches comments, doc strings, and at most deliberately-renamed test fns)
    - If any test was renamed: the affected Make target was re-run and its assert_tests_ran floor still passes (log excerpt in SUMMARY)
    - Both scoped suites exit 0
  </acceptance_criteria>
  <done>Prose is honest; refusals intact; no vacuous filter introduced</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned apr binary -> verdicts | every verdict from a reaped ExitStatus of the cargo-built binary; no bare `apr`, no pipe status |
| prose edits -> gate meaning | a re-word must never weaken an assertion or rename a test out of its Make filter unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Spoofing (wrong binary) | spawned ladder | mitigate | env!("CARGO_BIN_EXE_apr") pin (rule 3); r0 --version mechanism proof before interpreting later exits |
| T-05-07-02 | Tampering (vacuous gate) | test renames | mitigate | renames recorded; affected Make floors re-run (CR-02) |
| T-05-07-03 | Repudiation | artifact identity across surfaces | mitigate | one sha asserted equal across inspect/eval/predict outputs |
| T-05-07-04 | Elevation | slice refusal weakening | mitigate | zero assertion changes; slice probe_unicode refusal suites re-run green |
| T-05-07-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- Production ladder green (ran, not skipped, on this host); all pre-existing suites green
- Present-tense F-10 refusal prose eliminated; renames floor-checked
</verification>

<success_criteria>
- A user-produced setfit-apr-v1 demonstrated at the spawned tier with cross-surface hash identity; blast radius honest
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-07-SUMMARY.md` when done
</output>
