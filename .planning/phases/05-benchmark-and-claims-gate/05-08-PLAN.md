---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 08
type: execute
wave: 3
depends_on: ["05-04", "05-05"]
files_modified:
  - crates/aprender-train/src/train/setfit/apr_evaluate.rs
  - crates/aprender-train/src/train/setfit/apr_evaluate_row_tests.rs
  - crates/aprender-train/src/train/setfit/bench_metrics.rs
  - crates/aprender-train/src/train/setfit/mod.rs
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "A per-row-predictions evaluator door exists BESIDE evaluate_validation_from_artifact, takes the SAME ReloadedSetFitCredential (unreachable without the reload door), and re-checks artifact-vs-dataset identity the same way — the ONE evaluation door bench cells call (OPS-03)"
    - "EVAL-01 assembly produces F_avg via the contract-bound f1_average_for_classes(&[1,2]), macro-F1, per-class metrics, MCC, confusion matrix, and validation-only top-label ECE + multiclass Brier, all bound to the explicit ordered labels [none, against, favor]"
    - "Calibration diagnostics come from the VALIDATION split only (D-07); the QualityBlock records calibration_split = validation structurally"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/apr_evaluate.rs"
      provides: "evaluate_rows_from_artifact per-row predictions door"
      contains: "evaluate_rows_from_artifact"
    - path: "crates/aprender-train/src/train/setfit/bench_metrics.rs"
      provides: "QualityBlock assembly from prediction vectors via the shipped metric surfaces"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/bench_metrics.rs"
      to: "f1_average_for_classes"
      via: "contract-bound official_f_avg over class indices [1, 2]"
      pattern: "f1_average_for_classes"
    - from: "crates/aprender-train/src/train/setfit/bench_metrics.rs"
      to: "aprender_core::calibration::expected_calibration_error_top_label"
      via: "the 05-04 contract-bound functions — never ClassifyEvalReport (RNG/bootstrap exclusion)"
---

<objective>
Open the one evaluation door benchmark cells need: a per-row-predictions sibling of
`evaluate_validation_from_artifact`, plus the EVAL-01 metric assembly that turns prediction
vectors into the row's QualityBlock using only shipped, fixture-verified surfaces.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EvalRow's existing evaluator returns a SCALAR; F_avg/MCC/confusion/calibration need
per-row vectors, and OPS-03 forbids classify-in-the-adapter — the door must live in the library.
Output: the door + assembly, unit-proven over the in-crate fixture artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern "Anti-Patterns": a second evaluator; Don't Hand-Roll table)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (section 13: the door's analog with its corpus-identity re-check comment)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: evaluate_rows_from_artifact — the per-row predictions door</name>
  <files>crates/aprender-train/src/train/setfit/apr_evaluate.rs, crates/aprender-train/src/train/setfit/apr_evaluate_row_tests.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/apr_evaluate.rs (evaluate_validation_from_artifact :185-195 and its full body — the credential type, the corpus-from-the-artifact's-own-record re-check comment, the metric plumbing; how apr_evaluate_tests.rs builds the in-crate fixture credential)
    - crates/aprender-train/src/train/setfit/apr_evaluate_tests.rs (the fixture construction to reuse — do NOT edit this file; new tests go in the new sibling file)
    - crates/aprender-train/src/train/setfit/evaluate.rs (the trusted evaluator + ValidationMetricKind — what "one evaluation door" means here)
  </read_first>
  <behavior>
    - evaluate_rows_from_artifact(credential, dataset, split) returns per-row predicted class index + full K-vector probabilities + the ordered labels + n_rows, for validation and (token-gated where the existing API demands) test splits
    - It takes ReloadedSetFitCredential — the same type as the scalar door — so it is unreachable without the reload door (compile-level: no other constructor path)
    - It performs the same artifact-vs-dataset identity re-check; a mismatched dataset is the same typed error the scalar door returns
    - Scalar consistency: accuracy recomputed from the returned rows equals the scalar door's value on the fixture (the two doors cannot disagree)
  </behavior>
  <action>
    Add `evaluate_rows_from_artifact` beside the scalar door in apr_evaluate.rs, returning a
    `RowPredictions` struct (predicted indices, probability vectors, ordered labels, n_rows, and
    the artifact sha it evaluated — so callers can stamp rows without re-deriving). Reuse the
    scalar door's internals (extract a shared private helper rather than duplicating the eval
    loop — one implementation, two return shapes). Keep the credential parameter type and the
    corpus-identity re-check byte-for-byte in spirit (same error variants). Register the new test
    sibling file `apr_evaluate_row_tests.rs` following the crate's sibling-include convention (via
    mod.rs or the include! style the module already uses — mirror whichever apr_evaluate_tests.rs
    uses). Tests per the behavior block, over the existing in-crate fixture credential (the F-10
    workaround fixture is still the correct unit-tier substrate; production runs come later via
    bench).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit apr_evaluate > /tmp/door.log 2>&1; rc=$?; tail -5 /tmp/door.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - apr_evaluate.rs contains `pub fn evaluate_rows_from_artifact` taking the same credential type as the scalar door (grep both signatures)
    - The eval loop is shared (the scalar door delegates to the same private helper — no duplicated classify loop; grep shows one loop site)
    - The scalar-consistency test passes (rows-derived accuracy == scalar door value on the fixture)
    - apr_evaluate_tests.rs is byte-untouched (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>The door exists, credential-gated, identity-checked, consistent with the scalar door</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QualityBlock assembly (bench_metrics.rs)</name>
  <files>crates/aprender-train/src/train/setfit/bench_metrics.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/eval/classification/metrics.rs (MultiClassMetrics::from_predictions :61; f1_average_for_classes :10 — contract-bound official_f_avg; the None-on-bad-class-index behavior)
    - crates/aprender-core/src/metrics/agreement.rs (matthews_corrcoef :63 signature)
    - crates/aprender-core/src/calibration.rs (the 05-04 multiclass functions' signatures)
    - crates/aprender-train/src/train/setfit/bench_row.rs (QualityBlock field list from 05-05 — the assembly's output type)
  </read_first>
  <behavior>
    - assemble_quality_block(test_rows: &RowPredictions, validation_rows: &RowPredictions, ordered_labels) fills every QualityBlock field: f_avg (classes [1,2] = against, favor), macro_f1 over all 3, per-class precision/recall/f1, mcc, confusion matrix, n_test_rows, ece_top_label_validation (n_bins 10), brier_multiclass_validation, calibration_split "validation"
    - A hand-computed 6-row toy case reproduces every field exactly (kept small enough to verify by hand in the test comment)
    - Label ordering is explicit: passing labels in a different order changes per-class attribution and the test proves it (two-sided ordering test)
    - A class index outside K is a typed error (propagating f1_average_for_classes's None as an error, never a silent 0)
  </behavior>
  <action>
    Create `bench_metrics.rs` (feature setfit, registered in mod.rs): the assembly function that
    maps RowPredictions -> QualityBlock using ONLY the shipped surfaces named in read_first —
    MultiClassMetrics::from_predictions for per-class/macro/confusion,
    f1_average_for_classes(&f1, &[1, 2]) for the official F_avg (TweetEval classes: index 1 =
    against, 2 = favor per the ordered labels ["none","against","favor"]), matthews_corrcoef for
    MCC, and the 05-04 aprender-core calibration functions for validation-split ECE (n_bins 10) +
    Brier. Calibration inputs come from the VALIDATION RowPredictions only (D-07) — the function
    signature makes the two splits separate parameters so a caller cannot accidentally feed test
    probabilities into calibration. Fill the value_bits siblings for headline f64s. NEVER import
    ClassifyEvalReport (add a test-side grep-style guard is unnecessary — but the SUMMARY must
    state the import audit). Tests per the behavior block.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit bench_metrics > /tmp/bm.log 2>&1; rc=$?; tail -5 /tmp/bm.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - bench_metrics.rs uses f1_average_for_classes, MultiClassMetrics::from_predictions, matthews_corrcoef, expected_calibration_error_top_label, brier_score_multiclass (grep all five symbols)
    - `grep -c ClassifyEvalReport crates/aprender-train/src/train/setfit/bench_metrics.rs` returns 0
    - The hand-computed toy case and the label-ordering two-sided test pass
    - Suite exits 0 with non-zero matched tests
  </acceptance_criteria>
  <done>QualityBlock assembly complete on shipped, fixture-verified metric surfaces only</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact/credential -> evaluation | the door is only reachable through the reload credential; corpus identity re-checked |
| metric assembly -> claims rows | a swapped label order or test-split calibration would corrupt every row silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Elevation (bypass) | evaluator door | mitigate | credential parameter type = ReloadedSetFitCredential; no alternate constructor; shared loop with the scalar door |
| T-05-08-02 | Tampering | label attribution | mitigate | explicit ordered labels; two-sided ordering test; F_avg pinned to classes [1,2] |
| T-05-08-03 | Tampering (RNG) | calibration inputs | mitigate | 05-04 contract-bound functions only; ClassifyEvalReport import forbidden and grep-asserted |
| T-05-08-04 | Information disclosure (split leakage) | calibration split | mitigate | separate validation/test parameters; calibration_split field structurally "validation" |
| T-05-08-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- apr_evaluate + bench_metrics suites green; scalar-consistency and ordering tests green
- Import audit: no ClassifyEvalReport
</verification>

<success_criteria>
- One evaluation door, per-row capable; EVAL-01 metric set assembled and hand-verified; validation-only calibration structural
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-08-SUMMARY.md` when done
</output>
