---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - contracts/setfit-train-lifecycle-v1.yaml
  - crates/aprender-train/src/train/setfit/thresholds.rs
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "The three-place edit (contract + CALIBRATED_REGIMES + test) lands as ONE commit, only AFTER the human approved the measured thresholds and the pv diff (D-04)"
    - "The fixture regime entry and the fixture frozen_thresholds block are byte-untouched — git diff shows purely additive hunks plus only the deliberately amended EXACTLY-ONE invariant prose (D-03)"
    - "All 40 production (seed, cell) regime ids satisfy is_calibrated and resolve the PRODUCTION table, so no benchmark cell can hit UncalibratedRegime mid-matrix (D-02)"
    - "The production entry's architecture component is byte-equal to the measured id recorded by 05-01, and the full-list equality assertion (CR-04) survives over the 2-entry list"
  artifacts:
    - path: "contracts/setfit-train-lifecycle-v1.yaml"
      provides: "Second calibrated_regimes entry + additive per-regime thresholds block + MEASURED/COVERED record + amended invariant prose"
      contains: "seeds=13,17,23,29,31,37,41,43,47,53"
    - path: "crates/aprender-train/src/train/setfit/thresholds.rs"
      provides: "CALIBRATED_REGIMES with 2 entries; production RegimeThresholds from measured ε; test updated len 1->2 with equality preserved; 40-cell envelope coverage test"
      contains: "production_envelope_is_calibrated"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/thresholds.rs"
      to: "contracts/setfit-train-lifecycle-v1.yaml"
      via: "thresholds_match_the_contract include_str! parse — string-equal full-list comparison over BOTH entries"
      pattern: "thresholds_match_the_contract"
    - from: "contracts/setfit-train-lifecycle-v1.yaml"
      to: ".planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md"
      via: "every frozen production ε traceable to a measured window row"
---

<objective>
Land the F-10 unblock: the deliberate, `pv diff`-flagged, human-approved edit that adds the
production `all-MiniLM-L6-v2` regime (all 10 contracted seeds, all 4 cell labels at the frozen
E/B) with freshly measured per-class ε as a SECOND regime entry (D-01..D-04, Ph3 D-10(c)).
This is the single highest-risk edit of the phase; the checkpoint is the ceremony three Phase 4
executors correctly refused to bypass.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: until this lands, no user-reachable path produces a `setfit-apr-v1`; every downstream
benchmark plan is blocked on it.
Output: one commit touching contract + constant + test together, with pv validate/diff evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (F-R1..F-R4; Pattern 1; Pitfall 1)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 1 and 10)
@.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the three-place edit in the working tree (NO commit)</name>
  <files>contracts/setfit-train-lifecycle-v1.yaml, crates/aprender-train/src/train/setfit/thresholds.rs</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (the "Checkpoint input" section: proposed entry string, per-class ε table, MEASURED/COVERED table)
    - contracts/setfit-train-lifecycle-v1.yaml (calibrated_regimes at ~:281 with the EXACTLY-ONE invariant two lines below; frozen_thresholds block ~:102-133; DERIVATION invariant ~:148-154; the Phase-5 consequence clause ~:328-334; metadata header for the pv-diff house style)
    - crates/aprender-train/src/train/setfit/thresholds.rs (post-05-02 state: per-regime structure, table_for, the test's len==1 + equality assertions, the optional per_regime_thresholds serde field)
    - contracts/tweet-eval-stance-benchmark-v1.yaml (lines 1-36: the in-header pv-diff record house style to replicate)
  </read_first>
  <action>
    Prepare — in the working tree only, DO NOT commit — the full synchronized edit:
    (1) CONTRACT: append the production entry to `calibrated_regimes` (the exact string from the
    measurements file — architecture component byte-copied from the measured id, seeds all 10,
    cells all 4 at frozen E/B); add the additive per-regime thresholds block (production regime id
    -> per-class ε/scale/gating + embedding_delta_floor, mirroring the frozen_thresholds column
    shape) in the location the 05-02 serde field expects; amend the EXACTLY-ONE invariant prose to
    state the set now holds exactly TWO fingerprints, each measured on its own architecture, and
    that cross-application of one regime's numbers to the other's architecture remains forbidden;
    add the D-02 MEASURED-vs-COVERED record (which cells were measured, which are covered by the
    documented margin) beside the DERIVATION invariant; update the metadata header with the exact
    `pv diff` command and its suggested bump per the tweet-eval house style. The fixture entry line
    and the entire fixture frozen_thresholds block stay byte-identical.
    (2) RUST CONSTANT: add the production entry as the second element of `CALIBRATED_REGIMES` (or
    the per-regime source 05-02 derived it from) and build the production `RegimeThresholds` from
    the measured frozen values.
    (3) TEST: in `thresholds_match_the_contract`, change the len assertion literal 1 -> 2 (update
    its message to explain both fingerprints are independently measured), keep the full-list
    string-equality assertion over the COMPLETE 2-entry list (CR-04 protection — never a subset
    check), and add per-regime association assertions: the contract's per-regime block parses to
    exactly the Rust production table values AND the fixture block still parses to the fixture
    values. Add `production_envelope_is_calibrated`: iterate all 40 production regime ids (10 seeds
    x 4 cell labels, rendered through the same grammar `<arch@rev>|seeds=<s>|cells=<cell>` the run
    renders) asserting `table_for` returns the PRODUCTION table for each (and the fixture table for
    a fixture id — two-sided).
    Then produce the checkpoint evidence: run `target/release/pv validate contracts/setfit-train-lifecycle-v1.yaml`
    (rebuild pv via `cargo run --release -p aprender-contracts-cli --bin pv --` if stale); if pv
    rejects the per-regime block SHAPE, restructure the block to fit the existing schema (CLAUDE.md
    option 1) — never work around pv. Materialize the old revision:
    `git show HEAD:contracts/setfit-train-lifecycle-v1.yaml > /tmp/setfit-train-lifecycle-old.yaml`
    then `target/release/pv diff /tmp/setfit-train-lifecycle-old.yaml contracts/setfit-train-lifecycle-v1.yaml`
    and record the suggested bump (the bump is pv's call). Run the byte-identity control:
    `git diff -U0 contracts/setfit-train-lifecycle-v1.yaml` — every deletion hunk must touch ONLY
    the amended invariant prose and metadata header lines; the fixture entry string and
    frozen_thresholds lines must appear in no deletion. Run the full thresholds/tune/evidence
    suites against the working tree.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit thresholds > /tmp/thr2.log 2>&1; rc=$?; tail -5 /tmp/thr2.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/setfit-train-lifecycle-v1.yaml` reports 0 errors
    - `pv diff` output captured with its suggested version bump
    - `git diff -U0` deletion hunks confined to the amended invariant prose + metadata header (fixture entry + frozen_thresholds lines in no deletion hunk)
    - `thresholds_match_the_contract` green with len==2 and full-list equality; `production_envelope_is_calibrated` green over all 40 ids
    - NOTHING committed yet (`git log -1` predates the edit; changes only in working tree)
  </acceptance_criteria>
  <done>Complete edit staged in the working tree with pv + byte-identity + envelope evidence assembled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-04 checkpoint — present measured thresholds + pv diff, wait for approval</name>
  <files>none (presentation only)</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md
    - the Task 1 evidence bundle (pv validate/diff output, byte-identity control, envelope test result)
  </read_first>
  <action>
    Present the Task 1 evidence bundle to the human and WAIT. Do not commit, do not proceed, do
    not re-litigate the measured numbers — revisions happen only in response to the human's
    feedback, and every revision is re-presented before any commit (D-04).
  </action>
  <what-built>
    The prepared (uncommitted) three-place F-10 unblock edit: production regime entry
    (10 seeds x 4 cells at frozen E/B), measured per-class ε table as a second regime block,
    fixture entry byte-untouched, test updated to len==2 with CR-04 equality preserved,
    40-cell envelope coverage proven.
  </what-built>
  <how-to-verify>
    1. Review the measured ε table: per class — window bounds, frozen value (rounded DOWN, 2 sig figs), noise-floor clearance multiplier, binding parameter.
    2. Review the MEASURED-vs-COVERED table (6 measured boundary cells, 34 covered by the stated margin) — approve or challenge the margin rationale.
    3. Review the `pv diff` output and its suggested version bump.
    4. Review the byte-identity control: deletions confined to the deliberately amended invariant prose.
    5. Note the projected wall-clock for the full 40-cell SetFit run (from 05-01's probe timing) — approving here PRE-AUTHORIZES that compute for plan 05-12 (it will otherwise re-ask at >1hr per CLAUDE.md).
    6. Note the gradient_free (attention_key_bias) production re-derivation verdict.
  </how-to-verify>
  <acceptance_criteria>
    - Human response recorded verbatim in the SUMMARY (approval, or the change requests that were executed before re-presenting)
    - If approved: explicit note whether the 05-12 SetFit compute projection was pre-authorized
  </acceptance_criteria>
  <verify>
    <human-check>Human reviewed the measured ε table, MEASURED-vs-COVERED margins, pv diff + bump, byte-identity control, and the 05-12 compute projection</human-check>
  </verify>
  <resume-signal>Type "approved" to land the edit, or describe required changes (the executor revises and re-presents; it never commits without approval)</resume-signal>
  <done>Explicit human approval (or revision instructions executed and re-approved) recorded verbatim</done>
</task>

<task type="auto">
  <name>Task 3: Commit the approved edit as one commit and re-verify the gate both ways</name>
  <files>contracts/setfit-train-lifecycle-v1.yaml, crates/aprender-train/src/train/setfit/thresholds.rs</files>
  <read_first>
    - the approved working-tree diff (git diff)
  </read_first>
  <action>
    Commit contract + constant + test together as ONE commit (Pitfall 1: never a YAML append alone)
    with a message naming D-01..D-04 and the measured basis, e.g.
    `feat(05-03): calibrate production all-MiniLM-L6-v2 regime — deliberate D-10(c) contract edit (human-approved)`.
    Then re-verify both directions: (a) POSITIVE — `production_envelope_is_calibrated` green;
    (b) NEGATIVE — the existing UncalibratedRegime negatives still refuse foreign ids (run the tune
    + evidence + thresholds filters, plus `regime_membership_is_component_wise`), and the fixture
    slice path still behaves identically (run the setfit:: lib suite for aprender-train). Run
    `make contract-audit-phase4` (the lifecycle contract's bindings must stay 15/15 with zero BIND
    lines) and `target/release/pv validate contracts/setfit-train-lifecycle-v1.yaml` one final time
    at the committed state. Record the commit SHA in the SUMMARY — downstream plans (05-07 onward)
    reference it as the unblock point.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit setfit:: > /tmp/post-edit.log 2>&1; rc=$?; tail -5 /tmp/post-edit.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --stat` shows ONE commit touching exactly contracts/setfit-train-lifecycle-v1.yaml + thresholds.rs
    - `make contract-audit-phase4` exits 0 with zero BIND- lines (rc captured on its own line, not through a pipe)
    - Full aprender-train setfit:: lib suite exits 0 at the committed state
    - SUMMARY records the unblock commit SHA
  </acceptance_criteria>
  <done>The F-10 unblock is committed, human-approved, and verified two-sided</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract edit -> Phase 1-3 gate meaning | the edit must be purely additive so every earlier phase's gate keeps its exact meaning |
| executor -> contract | an inline relaxation by an executor is the exact failure D-10(c) forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering (gate loosening) | calibrated_regimes list | mitigate | full-list string equality over the 2-entry list preserved (CR-04); len literal exactly 2; human checkpoint before commit (D-04) |
| T-05-03-02 | Tampering | fixture regime semantics | mitigate | byte-identity control: deletion hunks confined to amended prose; fixture ε values asserted by the per-regime association test |
| T-05-03-03 | Spoofing | production regime id | mitigate | architecture component byte-copied from the measured run id (F-R2); envelope test renders all 40 ids through the run's own grammar |
| T-05-03-04 | Repudiation | edit provenance | mitigate | pv diff + suggested bump recorded in the contract header; one commit; approval recorded verbatim in SUMMARY |
| T-05-03-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- pv validate 0 errors at committed state; contract-audit-phase4 zero BIND lines
- 40-cell envelope test green; UncalibratedRegime negatives green
- One commit, three surfaces, human approval recorded
</verification>

<success_criteria>
- Production regime calibrated and committed after explicit approval; fixture semantics untouched; both gate directions verified
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-03-SUMMARY.md` when done
</output>
