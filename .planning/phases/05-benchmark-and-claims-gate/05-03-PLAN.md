---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
#
# REVISED 2026-08-17 against plan 05-01's execution evidence (05-CONTEXT amendment D-16..D-18).
# The previous version of this plan freezes "fresh per-class epsilon per the 10x/10x rule" as a
# second regime entry. That premise is REFUTED: five of six parameter classes have no legal epsilon
# at production step count. The plan now DECIDES which lower bound binds, on the measured evidence,
# at a blocking human checkpoint — and lands nothing under a rule that does not survive.
phase: 05-benchmark-and-claims-gate
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-14"]
files_modified:
  - contracts/setfit-train-lifecycle-v1.yaml
  - crates/aprender-train/src/train/setfit/thresholds.rs
  - crates/aprender-train/src/train/setfit/evidence.rs
  - .planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "D-03's premise is recorded as REFUTED with its measured numbers and NO epsilon is frozen under the 10x/10x rule (D-16): five of six classes have no legal window at the production envelope, and the collapse holds within s64 alone (real/near-null = 39.7 against a rule needing > 100), not only across mixed cells"
    - "WHICH lower bound BINDS is an explicit, human-selected decision recorded in the contract, chosen from a table of candidates each carrying its measured per-class window, its width, and the claim it gives up. Both candidate BOUNDS are already contracted — the 1e-8 near-null bound and the f32 rounding-noise clearance condition — and the noise floor was ALREADY the operative lower bound for layer_norm_weight, whose near-null edge the contract records as 0.000e0. But the noise-floor bound's contracted form is a CLEARANCE REQUIREMENT (no parameter may satisfy its threshold by rounding alone; the 49x-315x figures are recorded as what was MEASURED, not as a required `lower = k x floor` formula), so ANY multiplier on it is CHOSEN by this plan and must be recorded as chosen, never cited. The memo therefore prints L2 at BOTH `lower = noise_floor` (the bare contracted condition) and `lower = 10 x noise_floor` (a chosen, methodology-preserving factor), and no factor anywhere is picked to make the table close (05-01's explicit instruction)"
    - "No contract, threshold-table, constant or test change is committed before the D-04 checkpoint. The ONE exception is deliberate and stated: the decision memo is committed in Task 1 as its own commit, because it is rule-independent evidence that must survive an option-F halt and because the checkpoint should review a hash-referenceable artifact rather than a working-tree file a revision can silently rewrite. D-04 governs the gate edit, and a measurement memo is not a gate change. The executor then prepares the edit, runs pv validate and pv diff (two filesystem paths, old revision materialized with git show), presents the measured basis and the diff, and waits. The human's response is recorded verbatim; every revision is re-presented before any commit — the ceremony three Phase 4 executors correctly refused to bypass"
    - "attention_key_bias is disposed of explicitly with measured numbers (D-17). The refuted sentences go: `deltas are f32 cancellation residue` and the unqualified `makes dL/db_k exactly zero` are contradicted by grad_norm_max 8.084e-10 (s8) / 7.298e-10 (s64) and by the delta scaling with the learning rate. The sentence that survives is `dL/db_k = 0 in EXACT arithmetic`, which the contract already states correctly at gradient_free_parameters"
    - "The fixture regime entry and the fixture frozen_thresholds block are byte-untouched: git diff -U0 shows deletion hunks ONLY in the deliberately amended invariant prose and the metadata header, so pv diff reads as additive plus a named correction (D-03's surviving half)"
    - "The 05-02 tripwire is MIGRATED, not silenced: every regime-less threshold read routes through table_for naming the regime it means — 5 sites in evidence.rs (frozen.of(class)) and 6 in thresholds.rs's test module — and the panicking sole() accessor is deleted rather than left as a landmine for the next reader"
    - { statement: "Every epsilon frozen (if any) is PROVEN legal by 05-14's fail-closed derivation rather than read off a report: epsilon_basis returns the success value for the production regime with the new table in place, so the four-cell combine that is RED before this plan is GREEN after it for a reason the contract records", verification: backstop }
    - "If no basis is approved at the checkpoint, the plan HALTS having landed only what is true regardless of the rule, and SAYS SO in the SUMMARY — it never freezes a second rule to make itself look complete. The halt branch names the downstream plans that stay blocked (05-07, 05-12, and 05-09/05-11/05-13 transitively)"
    - "Contract MEMBERSHIP covers all 40 production (seed, cell) regime ids: each satisfies is_calibrated and resolves the PRODUCTION table, so no benchmark cell can hit UncalibratedRegime mid-matrix (D-02). This is SYNTACTIC coverage — empirical threshold success on the unmeasured cells remains a per-run invariant enforced at judgement time; the driver halts on the first evidence failure rather than continuing the matrix (review consensus item 7)"
    - "The production entry's architecture component is byte-equal to the measured id recorded by 05-01, the full-list equality assertion (CR-04) survives over the 2-entry list, and the contract records WHY that component reads `minilm-slice-` for a full 6-layer/30522-vocab encoder — plus that any change to architecture_fingerprint() is a contract-breaking change requiring a new separately measured regime entry, never a silent alias"
  artifacts:
    - path: ".planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md"
      provides: "The candidate lower-bound tables derived by running the shipped combine, each candidate's per-class window and width, the claim each gives up, the arithmetic refutation of factor relaxation, the coverage arithmetic for the two unmeasured cells, and the checkpoint options table"
      contains: "CANDIDATE LOWER BOUNDS"
    - path: "contracts/setfit-train-lifecycle-v1.yaml"
      provides: "Second calibrated_regimes entry + additive per_regime_thresholds block + the amended DERIVATION / lower-edge invariants naming which bound binds and why + MEASURED/COVERED record + corrected attention_key_bias invariants + ARCHITECTURE FINGERPRINT PROVENANCE note + pv diff header record"
      contains: "seeds=13,17,23,29,31,37,41,43,47,53"
    - path: "crates/aprender-train/src/train/setfit/thresholds.rs"
      provides: "CALIBRATED_REGIMES with 2 entries; production RegimeThresholds from the approved basis; len 1->2 with full-list equality preserved; 40-cell envelope coverage test; sole() deleted and its 6 test call sites migrated"
      contains: "production_envelope_is_calibrated"
    - path: "crates/aprender-train/src/train/setfit/evidence.rs"
      provides: "The 5 regime-less frozen.of(class) call sites migrated to table_for naming the fixture regime"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/thresholds.rs"
      to: "contracts/setfit-train-lifecycle-v1.yaml"
      via: "thresholds_match_the_contract include_str! parse — string-equal full-list comparison over BOTH entries, plus the per-regime association loop 05-02 left vacuous and this plan makes live"
      pattern: "thresholds_match_the_contract"
    - from: "contracts/setfit-train-lifecycle-v1.yaml"
      to: ".planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md"
      via: "every frozen production epsilon traceable to a measured window row under the NAMED lower bound the human selected"
    - from: "crates/aprender-train/src/train/setfit/evidence.rs"
      to: "the production regime's frozen table"
      via: "05-14's epsilon_basis reads the gated set from table_for, so landing the table is what turns the RED derivation GREEN"
      pattern: "epsilon_basis"
---

<objective>
Decide, on measured evidence, what replaces the 10x/10x epsilon window rule at production step
count — and land the F-10 unblock only under a rule that survives.

The previous version of this plan froze "fresh per-class epsilon per the 10x/10x rule" as a second
regime entry. Plan 05-01 was front-loaded precisely so that premise would fail early if it was
going to (D-01). It did: five of six parameter classes have NO legal epsilon at the production
envelope. So this plan can no longer freeze epsilon under that rule, and the replacement is a
decision with a blocking human gate, not a number an executor picks.

What did NOT fail: separation. `ctrl_max = 0.000e0 < real_min` for all six classes in every
measured cell — the 1e-30 control writes back bit-identical weights even at 1536 steps. Real
training remains cleanly distinguishable from no training. This plan must not be executed as
though calibration were impossible; what failed is epsilon-freezing under one specific rule.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: until an approved production regime lands, no user-reachable path produces a
`setfit-apr-v1` and every downstream benchmark plan stays blocked. This is still the phase's
keystone; it is now a keystone with a decision inside it.
Output: the decision memo, and — if and only if the human approves a basis — one commit touching
contract + constants + tests together, with pv validate/diff evidence and 05-14's fail-closed
derivation turning GREEN for a recorded reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md  (READ THE D-16..D-18 AMENDMENT FIRST, then D-01..D-04 and D-15)
@.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md  (the measured tables; the PROVISIONAL banner; PLAN CLOSEOUT findings 1-6)
@.planning/phases/05-benchmark-and-claims-gate/05-01-SUMMARY.md  (what 05-01 delivered and deliberately did not; "Next Steps — all for 05-03")
@.planning/phases/05-benchmark-and-claims-gate/05-02-SUMMARY.md  (the per-regime `Thresholds` restructure and the deliberate `sole()` tripwire; "Handoff to 05-03")
@.planning/phases/05-benchmark-and-claims-gate/05-14-PLAN.md  (the fail-closed derivation this plan is verified by)
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (F-R1..F-R4; Pattern 1; Pitfall 1)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 1 and 10)
</context>

<evidence_that_forced_this_revision>
Read `05-CONTEXT.md` § "Amendment — Execution Evidence (2026-08-17, from plan 05-01)" for the
authoritative statement. The load-bearing facts, so this plan can be executed without
re-deriving them:

**The rule.** The contract's DERIVATION invariant requires
`10 * worst_control_delta <= eps <= best_real_delta / 10` across ALL measured cells, with the upper
edge rounded DOWN to two significant figures. `worst_control_delta` is instantiated by two
conditions: the 1e-30 null control and the 1e-8 near-null control.

**What collapsed, and why it is the near-null leg and not the control.** The 1e-30 control writes
back bit-identical weights at 1536 steps (101 rows, max `relative_delta` 0.0, zero rows moved),
so `worst_ctrl` is pinned at exactly `0.000e0` and the near-null leg alone sets every lower bound.
Near-null grew 56–72x from 24 to 1536 steps for five classes and 299x for `attention_key_bias`.
The four-cell basis (`s8:13, s8:31, s8:53, s64:13`, 12 passes):

| class | lower `10 x worst_nnull` | upper `best_real / 10` | window | lower exceeds upper by |
|---|---|---|---|---|
| `embedding` | 1.553e-3 | 1.813e-4 | **EMPTY** | 8.56x |
| `layer_norm_weight` | 4.802e-6 | 1.891e-5 | EXISTS | — |
| `layer_norm_bias` | 9.844e-4 | 7.112e-5 | **EMPTY** | 13.84x |
| `projection_weight` | 1.051e-3 | 1.231e-4 | **EMPTY** | 8.54x |
| `projection_bias` | 1.101e-3 | 3.447e-5 | **EMPTY** | 31.94x |
| `attention_key_bias` | 9.278e-8 | 1.714e-8 | **EMPTY** | 5.41x |

Cross-cell mixing makes it worse, but the collapse is real within s64 alone
(`3.683e-7 / 9.278e-9 = 39.7`, against a rule needing `> 100`).

**The basis is PROVISIONAL.** 3 of 6 boundary cells measured for the s64 half — `s64:31` and
`s64:53` were never run. Unmeasured cells can only NARROW windows further: within a fixed rule the
lower bound is monotone non-decreasing and the upper monotone non-increasing in measured cells.

**What is NOT in question.** Separation. Also: debug and release measure bit-identically (the build
profile is a price, not a degree of freedom); persisted evidence is bit-identical across processes;
and the architecture component `minilm-slice-h384-l6-a12-i1536-v30522@1110a243` is byte-identical
across `cells=s8e1b16` and `cells=s64e1b16` over a genuinely cross-half 12-pass combine — which is
what makes the collapse a result about STEP COUNT rather than about two different models.

**Explicitly untested.** The superlinear-growth *hypothesis* (299x over a 64x step increase
suggesting the s8 near-null is underflow-dominated). It must not be stated as a mechanism, and
nothing in this plan depends on it.
</evidence_that_forced_this_revision>

<tasks>

<!-- planner-discipline-allow: frozen.of( -->
<!-- planner-discipline-allow: fn sole -->
<!-- Both literals are zero-count criteria on RUST SOURCE files (thresholds.rs / evidence.rs). They
     appear in this plan's prose only to name the eleven call sites being migrated; plan text is not
     on the grep path, and naming the sites imprecisely would make the migration ambiguous. -->

<task type="auto">
  <name>Task 1: Derive the candidate lower bounds, write and commit the decision memo, prepare the recommended edit</name>
  <files>.planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md, contracts/setfit-train-lifecycle-v1.yaml, crates/aprender-train/src/train/setfit/thresholds.rs, crates/aprender-train/src/train/setfit/evidence.rs</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md — the s8 cross-cell basis table (worst_ctrl / worst_nnull / best_real / noise_floor / eps/noise per class), the four-cell FINDING 1 table, and the PROVISIONAL banner naming the missing cells
    - contracts/setfit-train-lifecycle-v1.yaml — `calibrated_regimes` (~:281) with the EXACTLY-ONE invariant two lines below; the fixture `frozen_thresholds` block (~:102-133); the DERIVATION invariant (~:148-164); "THE LOWER EDGE IS A REAL MEASUREMENT" (~:165-171); "EVERY FROZEN EPSILON CLEARS f32 ROUNDING NOISE" (~:179-185); `gradient_free_parameters` (~:219-250) including THE MECHANISM and WHY NO EPSILON; the metadata header prose at ~:45-58; the Phase-5 consequence clause (~:328-334)
    - crates/aprender-train/src/train/setfit/thresholds.rs — post-05-02 state: `RegimeThresholds`, `table_for`, `frozen()`, `CALIBRATED_REGIMES`/`FIXTURE_REGIME`, the optional `per_regime_thresholds` serde field, `sole()` and its `of`/`embedding_delta_floor` wrappers, and the six test call sites that read through them
    - crates/aprender-train/src/train/setfit/evidence.rs — the five `frozen.of(class)` regime-less reads, and 05-14's `epsilon_basis` (the function whose gated set this plan's table feeds)
    - contracts/tweet-eval-stance-benchmark-v1.yaml lines 1-36 — the in-header `pv diff` record house style to replicate
    - CLAUDE.md — "Contract Validation: DOGFOOD pv, NEVER bash"; Verification Discipline rules 1 and 2
  </read_first>
  <action>
    Four things, in order.

    **What this task commits, stated once and unambiguously.** It commits the decision memo, on its
    own, in step (2) — nothing else. No contract byte, no threshold constant, no test literal is
    committed here; those wait for the D-04 checkpoint and land together in Task 3. The memo is
    committed deliberately rather than left in the working tree for three reasons: it is
    rule-independent (the L1 refutation and the candidate tables hold whichever option is selected,
    including option F), the checkpoint should review a hash-referenceable artifact instead of a file
    a later revision can silently rewrite, and 05-01's house precedent is to commit evidence
    artifacts as they are produced. D-04 governs the gate edit; a measurement memo is not a gate
    change. Both halves of this are machine-checked in `<verify>`.

    **(1) Derive the candidate lower bounds by RUNNING the shipped combine, never by hand
    arithmetic.** Point `APRENDER_CALIBRATION_STORE` at the committed
    `calibration-store/` and `APRENDER_CALIBRATION_COMBINE` at all four banked cells. It exits
    non-zero after 05-14 — that is the expected state, and the report is still written, so read the
    per-class aggregates from it (status captured on its own line, never through a pipe). Then add a
    REPORT-ONLY `CANDIDATE LOWER BOUNDS` section to the basis renderer — roughly thirty lines, no
    new knob, and NOT a second frozen-rule site: the frozen rule stays the single named site 05-14
    created. For each class, print the window and its width under each candidate, all sharing the
    contracted `upper = best_real / 10` (nothing measured challenges the upper factor):

    - **L1 — `10 x worst_nnull` (the D-03 rule).** Expect five EMPTY. Printed as the baseline being
      replaced, not as a live option.
    - **L2 — the rounding-noise floor as the lower bound. PRINT IT AT TWO FACTORS.** The contract's
      OTHER recorded lower bound is the clearance condition "EVERY FROZEN EPSILON CLEARS f32
      ROUNDING NOISE". Be precise about what that citation does and does not license: the contract
      states the CONDITION (no parameter may satisfy its threshold by rounding alone) and records
      49x-315x as the clearance the fixture epsilons HAPPENED TO HAVE. It does NOT state a required
      `lower = k x floor` formula. So print two columns, and label them differently:
        - **L2-bare — `lower = noise_floor`.** The contracted condition at its literal strength.
        - **L2-10x — `lower = 10 x noise_floor`.** A CHOSEN safety factor, not a contract citation.
          The reasons to choose it are that it mirrors the near-null leg's own factor and is
          strictly stricter than bare clearance — good reasons, and still a choice this plan makes
          and must own.
      Note and verify the identity that makes L2-10x checkable: its window width is exactly the
      already-reported `eps/noise` column divided by ten, because that column IS
      `upper / noise_floor`. On the s8 half the L2-10x widths are therefore 10.2x (embedding), 31.7x
      (layer_norm_weight), 119x (layer_norm_bias), 207x (projection_weight), 57.8x
      (projection_bias), 1.51x (attention_key_bias), and the L2-bare widths are ten times those.
      Those figures are s8-half UPPER BOUNDS on the four-cell widths: `best_real` is set entirely by
      the s8 half, so `upper` is unchanged, while folding in `s64:13`'s noise floors can only raise
      `lower`. Read the ACTUAL four-cell noise floors out of the run; do not carry the s8 numbers
      forward as if they were the four-cell ones.
    - **L3 — `k x worst_nnull` for a relaxed `k`.** Print the largest admissible `k` per class,
      `k <= best_real / (10 x worst_nnull)`, and the largest admissible PRODUCT `k_low x k_up <=
      best_real / worst_nnull`. Print them so the numbers are on the record and the candidate can
      be rejected by arithmetic rather than by taste.

    **(2) Write `05-03-epsilon-basis-decision.md` and COMMIT it as its own commit** (e.g.
    `docs(05-03): candidate epsilon lower bounds derived from the 12 banked passes`). Structure it as
    05-01's options tables are structured — measured numbers, what each option gives up, a
    recommendation labelled NON-BINDING. It must contain, at minimum:

    - The L1 refutation with the four-cell table and the within-s64 ratio 39.7.
    - The L2 tables — BOTH factors, per-class window and width — and the exact claim L2 GIVES UP,
      stated at full strength: under L2 the gate no longer refuses a genuinely-executed but
      2000x-underpowered contrastive run. State also why that claim is not the contracted one: the
      contract's central claim is "the encoder demonstrably moved" and its three NAMED adversaries
      are a frozen encoder, a centroid baseline and a 1e-30-LR run — all three produce exactly
      `0.000e0` at 1536 steps (measured), so all three remain refused with the widest possible
      margin. The 1e-8 near-null run is not a named adversary; it was introduced by 03-05 to obtain
      a non-degenerate lower bound because the 1e-30 control bounds epsilon from above only. L2
      supplies a different non-degenerate lower bound whose CONDITION the contract already requires.
    - **The precedent claim, narrowed to what is true, in these terms and no stronger:** the noise
      floor is already a contracted lower-bound CONDITION, and it was already the OPERATIVE one for
      `layer_norm_weight` — the contract's own fixture derivation table records that class's
      `worst 1e-8` and `lower edge` as `0.000e0`, so the near-null leg was degenerate there and the
      clearance condition was doing the work. Do NOT write that the whole rule pre-exists, and do
      NOT present the multiplier as inherited: the bound is cited, the FACTOR is chosen. Say
      explicitly which factor the recommendation uses, that it is a choice, and why that choice
      (mirrors the near-null leg; stricter than bare clearance). And say plainly that L2 is a
      WEAKENING of what the gate claims however the factor is set, so the contract must record it
      as such.
    - The L3 refutation BY ARITHMETIC, with the numbers from (1). On the four-cell basis the
      binding class is `projection_bias`: the largest admissible near-null factor is ~0.31 and the
      largest admissible factor PRODUCT is ~3.13 against the contracted 100. So L3 is not a
      reduced margin, it is an inverted one — epsilon would sit BELOW the near-null delta it is
      supposed to exceed, which would make a near-null run PASS. Recompute both figures from the
      run rather than quoting these; they are stated here so a divergence is visible.
    - `attention_key_bias`'s disposition, both options, with numbers. Under L2 its window is the
      narrowest of six by two orders of magnitude (width ~1.51x; clearance over its own noise floor
      151x against 1019x–20654x for the rest). (i) GATE it at the window's upper edge rounded down
      — legal but razor thin, and a 1.51x window CANNOT be frozen from a provisional basis. (ii)
      UNGATE it with a MEASURED justification of the margin kind, mirroring the methodology of the
      contract's own `WHY NO EPSILON` invariant, and note that the class is not load-bearing
      because the five gated classes already refuse all three named adversaries. Say explicitly
      that D-17 closed both options *under L1* and that reopening (ii) under a different lower
      bound is what "05-03 must choose a replacement rule on this evidence" means — do not present
      this as overriding D-17.
    - **One sentence disarming an apparent D-17 collision, because a reader who knows D-17 will stop
      on it.** D-17 forbids carrying forward `eps/noise = 15.1`, and the L2-10x width for
      `attention_key_bias` is derived as `15.1 / 10 = 1.51`. State why that is a different use and a
      legal one: the column is `upper / noise_floor`, so under L2 the window's WIDTH is
      `upper / lower = (upper / noise_floor) / k` as pure arithmetic on two measured quantities.
      What D-17 prohibits is reading that column as a MARGIN for an epsilon that has no legal
      window — dividing an illegal `upper` by the noise floor. Under L2 the window is non-empty, so
      `upper` is a legal epsilon and the quantity is a width rather than a laundered margin. Add
      that the executor must still recompute it from the four-cell noise floors read out of the run,
      which is what makes the number this plan's own rather than inherited.
    - The COVERAGE arithmetic for the two unmeasured cells: per class, the factor by which its
      noise floor would have to RISE, or its `best_real` FALL, to close its L2 window. Only
      `attention_key_bias` (1.51x) is plausibly within reach of two more cells; embedding is next
      at ~10.2x and the rest are 31.7x–207x. Note that 05-01 stopped those six passes because they
      could not change the verdict under L1 — under L2 they CAN, because they set the worst noise
      floor. That is the non-revisionist reason it is legitimate to re-ask for the compute.
    - The options table for the checkpoint, exactly as reproduced in Task 2, with the measured cost
      of option B (6 passes at ~55 min, ~5.5 h, per-pass retryable under D', disk preflight
      >= 10 GiB, `cargo test` invocation overhead ~2 min per pass) and the non-binding
      recommendation.

    **(3) Prepare the three-place edit for the RECOMMENDED option in the working tree, uncommitted.**
    If the human selects differently at Task 2, re-prepare and re-present — the D-04 ceremony
    already requires that, and it never commits without approval.

    - CONTRACT: append the production entry to `calibrated_regimes` (architecture component
      byte-copied from a measured id, all 10 contracted seeds, all 4 cell labels at the frozen
      E/B = `s8e1b16,s16e1b16,s32e1b16,s64e1b16`); add the additive `per_regime_thresholds` block in
      the location 05-02's serde field expects; amend the EXACTLY-ONE invariant to state the set
      now holds exactly TWO fingerprints, each measured on its own architecture, cross-application
      still forbidden. Amend the DERIVATION and lower-edge invariants to record, for the PRODUCTION
      regime only: that the near-null lower bound is UNSATISFIABLE at this envelope with the
      four-cell numbers inline; which bound BINDS and the human's selection; that the MULTIPLIER on
      the binding bound is CHOSEN BY THIS EDIT and not inherited from the fixture derivation — the
      contract's clearance invariant states a condition and records 49x-315x as measured, never a
      required formula — with the chosen factor named, its rationale (mirrors the near-null leg's
      factor; strictly stricter than bare clearance) and the bare-condition window printed beside
      it so a reader can see what the factor bought; and that this is a weakening of the claim, with
      the weakened claim spelled out. Add the D-02
      MEASURED-vs-COVERED record beside it, at exactly the strength the evidence supports —
      "contract membership covers all 40 cells; empirical threshold success on the unmeasured cells
      remains a per-run invariant enforced at judgement time" — followed by 05-01's
      prospective-validation result (both cells with their numbers, or the literal
      `NOT RUN — compute budget`). Do NOT write a sentence asserting that unmeasured cells will
      clear epsilon. Correct the `attention_key_bias` prose per D-17: the metadata-header phrase
      that makes the mechanism claim without the exact-arithmetic qualifier, and the
      `gradient_free_parameters` invariant calling the deltas cancellation residue, are amended and
      a PRODUCTION-scoped invariant is ADDED recording `grad_norm_max` 8.084e-10 (s8) / 7.298e-10
      (s64), the learning-rate scaling, and the chosen disposition with its margin. Keep the
      exact-arithmetic sentence. Add the ARCHITECTURE FINGERPRINT PROVENANCE note beside the
      production entry: `minilm-slice-` is emitted unconditionally by
      `BertSentenceEncoder::architecture_fingerprint()` and renders for the FULL production checkout
      as well as the fixture slice; the entry string is a rendered-verbatim identity token, not a
      semantic claim about slicing; the dimensional components `h384-l6-a12-i1536-v30522` are what
      distinguish the architectures; and any change to that rendering is a CONTRACT-BREAKING change
      requiring a new separately measured regime entry through this same `pv diff`-flagged,
      human-approved procedure — never a prefix alias or normalization rule in
      `RegimeCoordinates::covers`. Update the metadata header with the exact `pv diff` command and
      its suggested bump per the tweet-eval house style. The fixture entry line and the entire
      fixture `frozen_thresholds` block stay byte-identical.
    - RUST: add the production entry as the second element of `CALIBRATED_REGIMES` and build its
      `RegimeThresholds` from the approved basis. MIGRATE the 05-02 tripwire rather than working
      around it: route the six `thresholds.rs` test reads and the five `evidence.rs`
      `frozen.of(class)` reads through `table_for` naming the regime they mean (the fixture regime
      for the fixture-scale assertions), then DELETE `sole()` and its two regime-less wrappers so
      the landmine is removed instead of re-armed. This is real work the 05-02 SUMMARY handed over
      by name; the panic is the signal, not the obstacle.
    - TESTS: in `thresholds_match_the_contract`, change the len literal 1 -> 2 (message explaining
      both fingerprints are independently measured), KEEP the full-list string-equality assertion
      over the COMPLETE 2-entry list (CR-04 — never a subset check), and make live the per-regime
      association loop 05-02 left vacuous: the contract's per-regime block must parse to exactly
      the Rust production table AND the fixture block still to the fixture values. Add
      `production_envelope_is_calibrated`: iterate all 40 production regime ids (10 seeds x 4 cell
      labels, rendered through the same `<arch@rev>|seeds=<s>|cells=<cell>` grammar the run renders)
      asserting `table_for` resolves the PRODUCTION table for each, and the FIXTURE table for a
      fixture id — two-sided, so it cannot pass by resolving everything.

    **(4) Assemble the checkpoint evidence bundle.** `pv validate contracts/setfit-train-lifecycle-v1.yaml`
    (rebuild `pv` with `cargo run --release -p aprender-contracts-cli --bin pv --` if stale). If
    `pv` rejects the per-regime block SHAPE, restructure the block to fit the existing schema
    (CLAUDE.md option 1) — never a bash/yq/python workaround. Materialize the old revision and diff
    TWO FILESYSTEM PATHS:
    `git show HEAD:contracts/setfit-train-lifecycle-v1.yaml > /tmp/setfit-train-lifecycle-old.yaml`
    then `pv diff /tmp/setfit-train-lifecycle-old.yaml contracts/setfit-train-lifecycle-v1.yaml`,
    recording the suggested bump (the bump is pv's call). Run the byte-identity control
    `git diff -U0 contracts/setfit-train-lifecycle-v1.yaml` and confirm every deletion hunk touches
    only the amended invariant prose and metadata header. Run 05-14's derivation against the
    prepared table and confirm it now returns the success value for the production regime — that is
    the proof the frozen basis is legal, and it must be a RUN, not a reading. Run the
    thresholds / tune / evidence suites against the working tree.
  </action>
  <verify>
    <automated>: "every cheap deterministic criterion below is MACHINE-run here, none is left to prose — above all the no-commit boundary, which is this task's whole point"; MEMO=.planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md; CONTRACT=contracts/setfit-train-lifecycle-v1.yaml; THR=crates/aprender-train/src/train/setfit/thresholds.rs; EV=crates/aprender-train/src/train/setfit/evidence.rs; test -f "$MEMO" || exit 1; grep -q 'CANDIDATE LOWER BOUNDS' "$MEMO" || exit 1; git log -1 --name-only --format= > /tmp/last-commit-files.txt; grep -q 'epsilon-basis-decision' /tmp/last-commit-files.txt || exit 1; grep -qE 'setfit-train-lifecycle-v1\.yaml|thresholds\.rs' /tmp/last-commit-files.txt && exit 1; test -n "$(git status --porcelain "$CONTRACT" "$THR")" || exit 1; test "$(grep -c 'fn sole' "$THR")" -eq 0 || exit 1; test "$(grep -c 'frozen\.of(' "$EV")" -eq 0 || exit 1; test "$(grep -c 'fn epsilon_basis' "$EV")" -eq 1 || exit 1; target/release/pv validate "$CONTRACT" > /tmp/pv-validate.log 2>&1; rc=$?; tail -3 /tmp/pv-validate.log; test $rc -eq 0 || exit 1; CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit thresholds > /tmp/thr2.log 2>&1; rc=$?; tail -5 /tmp/thr2.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    Machine-checked in `<verify>` above (listed so a reader can see they are not prose): the memo
    exists and carries `CANDIDATE LOWER BOUNDS`; the memo IS the last commit's content; that same
    commit touches NEITHER the contract NOR `thresholds.rs`; both of those files nonetheless have
    uncommitted working-tree edits (so the edit was prepared, not skipped); `grep -c 'fn sole'`
    returns 0; no regime-less class read remains in `evidence.rs`; `grep -c 'fn epsilon_basis'`
    still returns 1 (the candidate columns are report-only and created no second frozen-rule site);
    `pv validate` reports 0 errors; the `thresholds` suite is green.

    Judgement-checked, recorded in the SUMMARY:
    - The memo's `CANDIDATE LOWER BOUNDS` tables were produced BY RUNNING the combine (invocation and rc quoted), and contain the L1 refutation, BOTH L2 factor variants with per-class window and width, the explicit statement that the multiplier is chosen rather than cited, the narrowed `layer_norm_weight` precedent claim, the L3 arithmetic refutation with both admissible factors, the D-17 width-versus-margin clarification, the `attention_key_bias` options with numbers, the coverage arithmetic for the two unmeasured cells, and the options table with a NON-BINDING recommendation
    - `pv diff` output captured with its suggested version bump
    - `git diff -U0 contracts/setfit-train-lifecycle-v1.yaml` deletion hunks confined to the amended invariant prose + metadata header; the fixture entry string and every fixture `frozen_thresholds` line appear in NO deletion hunk
    - `thresholds_match_the_contract` green with len==2 and full-list equality; the per-regime association loop is live (it fails if a single production epsilon literal is perturbed — prove it by perturbing one and reverting); `production_envelope_is_calibrated` green over all 40 ids and two-sided on a fixture id
    - 05-14's derivation returns the success value for the production regime against the prepared table, run and quoted
  </acceptance_criteria>
  <done>The decision memo is COMMITTED with tables derived by running the shipped code, and the recommended edit is staged UNCOMMITTED in the working tree with pv + byte-identity + fail-closed-derivation evidence</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-04 checkpoint, extended — the human SELECTS the epsilon basis and approves the diff</name>
  <files>none (presentation only)</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md
    - the Task 1 evidence bundle (pv validate/diff output, byte-identity control, envelope test result, the fail-closed derivation run)
  </read_first>
  <decision>
    Which lower bound binds the production regime's epsilon, what happens to `attention_key_bias`,
    and whether the two unmeasured boundary cells are measured before anything is frozen.
  </decision>
  <context>
    D-03's premise is refuted: at the production envelope five of six classes have no legal epsilon
    under `10 x worst_nnull <= eps <= best_real / 10`. Separation is intact, so this is not
    "calibration is impossible" — it is that one specific rule does not survive 1536 optimizer
    steps. Both candidate lower bounds are already written into this contract; the question is which
    one binds, and what claim is being given up by answering.

    TWO SEPARATE KNOBS, not one. (i) WHICH bound binds — contracted either way. (ii) WHAT MULTIPLIER
    sits on it — NOT contracted for the noise floor, which the contract states as a clearance
    condition with 49x-315x recorded as measured rather than as a formula. Options A/B/C below are
    written at the recommended 10x factor; any of them may be selected with `lower = noise_floor`
    (bare contracted clearance, ten times wider windows) instead, and the memo prints both columns so
    that substitution needs no re-derivation. Say which factor you are selecting.

    No gate change has been committed. The decision memo is already committed on its own (Task 1),
    deliberately, because it is rule-independent evidence.
  </context>
  <options>
    <option id="option-a">
      <name>L2 with the chosen 10x factor (`lower = 10 x noise_floor`), attention_key_bias UNGATED with a measured-margin justification, frozen from the four-cell PROVISIONAL basis</name>
      <pros>No new compute. Five gated classes with windows 10.2x–207x wide AT THE 10x FACTOR (ten times wider at bare contracted clearance, which is the fallback if you want more headroom). All three of the contract's named adversaries stay refused with the widest possible margin. The contract records both the provisionality and the chosen factor at exactly their strength.</pros>
      <cons>Gives up the near-null claim: a genuinely-executed but 2000x-underpowered run would pass. Commits to a CHOSEN multiplier that the contract does not supply. Freezes with 2 of 6 boundary cells unmeasured — and unmeasured cells can only narrow windows, embedding being the thinnest survivor at ~10.2x.</cons>
    </option>
    <option id="option-b">
      <name>Option A, but measure s64:31 and s64:53 first</name>
      <pros>Removes the PROVISIONAL asterisk from the number every downstream benchmark cell is judged against. Resumable at one-pass granularity thanks to 05-01's D' work, so a lost pass costs one pass. Legitimate to ask now for a reason that did not exist before: 05-01 stopped these six passes because they could not change the verdict under L1 — under L2 they set the worst noise floor and can.</pros>
      <cons>~5.5 h of compute (6 passes at ~55 min each, plus ~2 min invocation overhead per pass), which exceeds the CLAUDE.md 60-minute check-in and is what this option authorizes. Disk preflight >= 10 GiB before each pass.</cons>
    </option>
    <option id="option-c">
      <name>L2 (10x factor) with attention_key_bias GATED at its thin window</name>
      <pros>Keeps all six classes gated, so the gate's coverage is not narrowed at all. Note that this class is the one where the factor choice bites hardest: at bare clearance its window is ~15.1x wide instead of ~1.51x, so selecting the bare factor is the coherent way to gate it without a razor-thin margin — at the cost of a lower bound with no safety factor at all.</pros>
      <cons>Requires option B's compute first — a ~1.51x window cannot honestly be frozen from a provisional basis, since two more cells could close it. Accepts the narrowest margin of the six by two orders of magnitude.</cons>
    </option>
    <option id="option-d">
      <name>Relax the safety factors on the near-null bound</name>
      <pros>None available on this evidence.</pros>
      <cons>REFUTED BY ARITHMETIC, and presented so the numbers are on the record rather than as an equal option: the largest admissible near-null factor is ~0.31 and the largest admissible factor product ~3.13 against the contracted 100 (projection_bias binding). That is not a reduced margin but an inverted one — epsilon would sit below the near-null delta it must exceed, so a near-null run would PASS. Also forbidden outright by 05-01's closeout: do not pick a number to make the table close.</cons>
    </option>
    <option id="option-e">
      <name>Narrow the regime envelope to the measured cells only</name>
      <pros>The collapse is a step-count effect, so an s8-only entry would have windows under L1.</pros>
      <cons>The benchmark's s16/s32/s64 cells would then hit UncalibratedRegime mid-matrix, which is exactly what D-02 exists to prevent. Not viable for this milestone.</cons>
    </option>
    <option id="option-f">
      <name>HALT — land only what is true regardless of the rule, and stop</name>
      <pros>A plan that halts honestly beats one that freezes a second rule that also does not survive. What lands is real: the sole() migration, the D-17 correction, and the L1 refutation recorded in the contract.</pros>
      <cons>F-10 stays blocked; 05-07 and 05-12 cannot run, and 05-09/05-11/05-13 stay blocked transitively; the milestone's claims layer does not ship this cycle.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Review the CANDIDATE LOWER BOUNDS tables: per class and per candidate — window bounds, width, noise-floor clearance, binding parameter. Confirm the numbers were produced by running the shipped combine (the invocation and its rc are quoted) and not by hand.
    2. **Review the MULTIPLIER as a decision of its own.** The noise-floor BOUND is contracted; the factor on it is NOT — the contract states a clearance condition and records 49x-315x as measured, never a formula. So compare the two printed variants: `lower = noise_floor` (bare contracted condition) and `lower = 10 x noise_floor` (chosen). Confirm the memo and the prepared contract text both call the factor chosen rather than citing it, and decide whether 10x is the factor you want. A factor is a real knob and this is the moment to set it deliberately — 05-01's closeout forbids setting it to make the table close.
    3. Review the L2 claim-given-up statement. This is the substantive question: are you content that the gate proves "the encoder moved beyond f32 rounding noise, and a run that does not train produces exactly zero" rather than "the encoder moved more than a 2000x-underpowered run would"? If not, options C/F.
    4. Review the L3 refutation arithmetic (largest admissible factor ~0.31; largest admissible product ~3.13 against 100). If those numbers do not hold in the run's output, that is a stop-and-decide, not a formality.
    5. Decide `attention_key_bias`: gated at a ~1.51x window (needs option B first) or ungated with the measured-margin justification. Confirm the refuted sentences are going and the exact-arithmetic sentence is staying.
    6. Review the MEASURED-vs-COVERED record and the coverage arithmetic for `s64:31`/`s64:53` — approve or challenge freezing from a provisional basis, or order option B.
    7. Review the `pv diff` output and its suggested version bump.
    8. Review the byte-identity control: deletions confined to the deliberately amended invariant prose and the metadata header; the fixture entry and its frozen_thresholds block untouched.
    9. Review 05-14's fail-closed derivation result against the prepared table: it must RETURN THE SUCCESS VALUE for the production regime, so the same four-cell combine that is RED before this plan is GREEN after it. A prepared table that does not turn it green must not be committed.
    10. Note the projected wall-clock for the full 40-cell SetFit run (from 05-01's probe timing) — approving here PRE-AUTHORIZES that compute for plan 05-12, which will otherwise re-ask at >1 hr per CLAUDE.md. Note also that 05-12 runs cells SEQUENTIALLY: parallel cell processes would corrupt EVAL-05's resource measurements, so a wall-clock overrun is a checkpoint, never a reason to parallelize.
    11. Review the ARCHITECTURE FINGERPRINT PROVENANCE note: `minilm-slice-` is a rendering artifact, kept verbatim on purpose; no aliasing rule was added to the membership check.
    12. Review the PROSPECTIVE VALIDATION status from 05-01 (cells s16/seed 41 and s32/seed 29): numbers, or `NOT RUN — compute budget`. If NOT RUN, confirm you accept the weakened coverage claim.
  </how-to-verify>
  <acceptance_criteria>
    - The selected option is recorded verbatim in the SUMMARY, with the human's rationale as given
    - If a different option than the prepared one is selected, the executor re-prepares the edit and RE-PRESENTS the full bundle before any GATE change is committed; the re-presentation is recorded too. (Task 1's memo commit is already in history and is not re-litigated; if the selection changes the recommendation, the memo is AMENDED by a follow-up commit that records the selection, never rewritten in place)
    - If option B or C is selected, the compute authorization is recorded explicitly with its measured cost before any pass runs, and the six passes are banked to the committed store as 05-01 banked the first twelve
    - If option F is selected, the SUMMARY says so plainly, records what landed, and names 05-07, 05-12, 05-09, 05-11 and 05-13 as blocked — the halt is reported, never disguised as a partial success
    - If approved: an explicit note whether the 05-12 SetFit compute projection was pre-authorized (05-12's own must_haves depend on this)
  </acceptance_criteria>
  <verify>
    <human-check>Human reviewed the candidate lower-bound tables, the claim L2 gives up, the L3 arithmetic refutation, the attention_key_bias disposition, the coverage arithmetic, pv diff + bump, the byte-identity control, 05-14's derivation result, and the 05-12 compute projection — and SELECTED an option</human-check>
  </verify>
  <resume-signal>Select: option-a, option-b, option-c, option-d, option-e, or option-f — or describe required changes (the executor revises and re-presents; it never commits a gate change without approval)</resume-signal>
  <done>An explicit option selection recorded verbatim, with any ordered revisions executed and re-approved before any gate change is committed</done>
</task>

<task type="auto">
  <name>Task 3: Commit the approved edit as ONE commit and re-verify the gate both ways</name>
  <files>contracts/setfit-train-lifecycle-v1.yaml, crates/aprender-train/src/train/setfit/thresholds.rs, crates/aprender-train/src/train/setfit/evidence.rs</files>
  <read_first>
    - the approved working-tree diff (`git diff`)
    - the Task 2 selection as recorded
  </read_first>
  <action>
    If option F was selected, land ONLY the rule-independent work — the `sole()` migration, the
    D-17 correction, and the contract's L1-refutation record — as one commit, write the SUMMARY
    stating plainly that no production epsilon was frozen and which downstream plans are blocked,
    and stop. Do not fabricate a regime entry to make the plan read as complete.

    Otherwise commit contract + constants + tests together as ONE commit (Pitfall 1: never a YAML
    append alone), with a message naming D-01..D-04, D-16..D-18, the selected lower bound and the
    `attention_key_bias` disposition, e.g.
    `feat(05-03): calibrate production all-MiniLM-L6-v2 regime on the <selected> lower bound — deliberate D-10(c) contract edit (human-approved)`.

    Then re-verify BOTH directions:
    (a) POSITIVE — `production_envelope_is_calibrated` green over all 40 ids; and 05-14's
    derivation returns the success value for the production regime at the COMMITTED state, so the
    four-cell combine that was RED is now GREEN. Quote both rc values.
    (b) NEGATIVE — the existing `UncalibratedRegime` negatives still refuse foreign ids (run the
    tune + evidence + thresholds filters plus `regime_membership_is_component_wise`), the fixture
    slice path still behaves identically (full `setfit::` lib suite for aprender-train), and the
    fixture regime's own derivation is unaffected (05-14's fixture green control still green).
    (c) Run `make contract-audit-phase4` — the lifecycle contract's bindings must stay 15/15 with
    zero BIND lines, rc captured on its own line and never through a pipe — and
    `pv validate contracts/setfit-train-lifecycle-v1.yaml` one final time at the committed state.

    Record the commit SHA in the SUMMARY: downstream plans (05-07 onward) reference it as the
    unblock point. Also record, for 05-07's benefit, whether `apr setfit train` is now expected to
    reach exit 0 on the production encoder — that is 05-07's proof obligation, not this plan's
    claim, and it must be stated as an expectation rather than a result.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit setfit:: > /tmp/post-edit.log 2>&1; rc=$?; tail -5 /tmp/post-edit.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --stat` shows ONE commit touching exactly the three source files this plan owns — contract + `thresholds.rs` + `evidence.rs` — and NOT the decision memo, which landed as its own commit in Task 1 (`git log --oneline -2` shows the two commits in that order)
    - `make contract-audit-phase4` exits 0 with zero BIND- lines (rc captured on its own line)
    - Full aprender-train `setfit::` lib suite exits 0 at the committed state, counts stated
    - The four-cell combine's rc is quoted at three points — 05-01's `rc=0`, RED after 05-14, GREEN after this commit — so the arc is legible without re-deriving it
    - The `UncalibratedRegime` negatives and 05-14's fixture green control are both still green
    - SUMMARY records the unblock commit SHA, the selected option verbatim, and the 05-12 compute pre-authorization note
    - If option F was selected: the SUMMARY states no epsilon was frozen and names every blocked downstream plan
  </acceptance_criteria>
  <done>Either the F-10 unblock is committed, human-approved and verified two-sided — or the plan has halted honestly with only rule-independent work landed and the blockage named</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and content CREATED by this plan (newly-created — exclude from drift verification):

- `.planning/phases/05-benchmark-and-claims-gate/05-03-epsilon-basis-decision.md` — the candidate
  lower-bound derivation, the claim-given-up statement, the L3 arithmetic refutation, the coverage
  arithmetic, and the checkpoint options table
- The `CANDIDATE LOWER BOUNDS` report-only section in the basis renderer (`evidence.rs`)
- Second entry in `CALIBRATED_REGIMES` (the production regime id string) + its `RegimeThresholds`
  value (`thresholds.rs`)
- Test fn `production_envelope_is_calibrated` (`thresholds.rs`)
- Contract additions in `contracts/setfit-train-lifecycle-v1.yaml`: the production
  `calibrated_regimes` entry, the additive `per_regime_thresholds` block, the amended
  EXACTLY-ONE -> EXACTLY-TWO invariant prose, the production-scoped DERIVATION / lower-edge
  invariants naming which bound binds and the claim given up, the D-02 MEASURED-vs-COVERED record,
  the ARCHITECTURE FINGERPRINT PROVENANCE note, the corrected + added `attention_key_bias`
  invariants, and the `pv diff` metadata-header record

REMOVED by this plan (so its absence is not read as drift): `Thresholds::sole()` and its two
regime-less wrappers, deleted after their eleven call sites were migrated to `table_for`.
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses — both still stand after this revision:

- INCORPORATED (consensus item 7, Codex MEDIUM): the coverage claim in both the contract and this
  plan's `must_haves` is weakened to contract-membership + per-run judgement-time invariant. The
  40-cell `table_for` test is explicitly labelled as proving SYNTACTIC coverage only.
- INCORPORATED (Gemini Finding 5, documentation half): the ARCHITECTURE FINGERPRINT PROVENANCE
  note. The remediation Gemini actually asked for (prefix normalization in
  `RegimeCoordinates::covers`) is REJECTED — see 05-02's rejection note.

Revision notes (2026-08-17, post-05-01 execution):

- The review's sharpest concern about this plan was "six cells are an engineering margin, not
  evidence". Execution went further than the review did: the margin does not merely thin, it ceases
  to exist for five of six classes. The revision therefore does not answer that concern with better
  prose — it moves the choice of lower bound into a blocking human decision with the measured
  tables attached.
- `depends_on` gains `05-14`. That plan makes an empty window a hard failure of the derivation, so
  this plan's central claim is machine-checked rather than read off a report. See 05-14's
  `<wave_placement>` for why gate-first.
- `files_modified` gains `evidence.rs` (the eleven-site `sole()` migration includes five reads
  there, plus the report-only candidate columns) and the decision memo.
</review_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract edit -> Phase 1-3 gate meaning | the edit must be purely additive apart from named corrections, so every earlier phase's gate keeps its exact meaning |
| executor -> contract | an inline relaxation by an executor is the exact failure D-10(c) forbids; the checkpoint is the structural form of the ceremony three Phase 4 executors refused to bypass |
| measured basis -> frozen epsilon | the only evidence any production epsilon is legal; a rule chosen to make the table close would launder a failed calibration into a green gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering (gate loosening) | calibrated_regimes list | critical | mitigate | full-list string equality over the 2-entry list preserved (CR-04); len literal exactly 2; human checkpoint before commit (D-04) |
| T-05-03-02 | Tampering | fixture regime semantics | high | mitigate | byte-identity control: deletion hunks confined to amended prose + header; fixture epsilon values asserted by the now-live per-regime association test; 05-14's fixture green control re-run after the commit |
| T-05-03-03 | Spoofing | production regime id | high | mitigate | architecture component byte-copied from the measured run id (F-R2); envelope test renders all 40 ids through the run's own grammar; provenance note forbids prefix aliasing |
| T-05-03-04 | Repudiation | edit provenance | medium | mitigate | pv diff + suggested bump recorded in the contract header; one commit; the option selection recorded verbatim in the SUMMARY |
| T-05-03-05 | Tampering (a rule chosen to close the table) | the replacement lower bound | critical | mitigate | candidates derived by RUNNING the shipped combine, not by hand; L3's admissible factors printed so relaxation is refuted by arithmetic on the record; the selection is the human's at a blocking checkpoint; every frozen epsilon must make 05-14's fail-closed derivation return the success value |
| T-05-03-06 | Repudiation (a weakened claim recorded as an unchanged one) | the contract's lower-edge invariants | high | mitigate | the contract must state the weakened claim explicitly — which adversaries remain refused and which run would now pass — rather than silently substituting one bound for the other |
| T-05-03-07 | Elevation (exemption by omission) | attention_key_bias disposition | medium | mitigate | D-17 requires an explicit decision with measured numbers; 05-14 requires a window for every class the regime gates and requires ALL classes where no table exists, so an unrecorded exemption cannot pass |
| T-05-03-SC | Tampering | npm/pip/cargo installs | low | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- Candidate tables derived by running the shipped combine, with the invocation and rc quoted
- pv validate 0 errors at committed state; pv diff bump recorded; contract-audit-phase4 zero BIND lines
- Byte-identity control: fixture entry + frozen_thresholds block in no deletion hunk
- `sole()` deleted and all eleven regime-less reads migrated; per-regime association test live and
  proven to bite on a perturbed literal
- 40-cell envelope test green and two-sided; UncalibratedRegime negatives green
- 05-14's derivation: RED before, GREEN after, with both rc values quoted
- One commit, or an honest halt naming the blocked downstream plans
</verification>

<success_criteria>
- Either the production regime is calibrated under a human-selected, measured, explicitly-recorded
  lower bound — fixture semantics untouched, both gate directions verified, the frozen basis proven
  legal by running the fail-closed derivation — or the plan halted at the checkpoint with only
  rule-independent work landed and the blockage named
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-03-SUMMARY.md` when done
</output>
