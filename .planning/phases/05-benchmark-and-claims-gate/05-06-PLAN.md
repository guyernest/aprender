---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - crates/apr-cli/src/model_ops_commands.rs
  - crates/apr-cli/src/dispatch.rs
  - crates/apr-cli/src/commands/finetune.rs
  - crates/aprender-train/src/finetune/classify_trainer.rs
  - crates/aprender-train/src/finetune/classify_pipeline/mod.rs
  - crates/aprender-train/src/finetune/classify_reload_tests.rs
autonomous: true
requirements: [EVAL-02, EVAL-05]

must_haves:
  truths:
    - "apr finetune --task classify accepts --selection-manifest and resolves rows through the SAME Phase 2 manifest->Selection replay path SetFit uses, refusing to start on hash mismatch (D-10)"
    - "The three hardcodes are controllable per cell: seed (was 42 — contract-excluded), val_split (was 0.2 — uncontracted random split), early_stopping_patience (was 10 — unlocked model selection); defaults unchanged when the new flags are absent"
    - "ClassifyTrainer tolerates val_split 0.0 with early stopping disabled (A6 probed, not assumed), and the run result exposes epochs_completed so a LoRA row can attest no early stop fired"
    - "One shared implementation serves the CLI flag path and the future bench caller (OPS-03 — no second training path)"
    - "The LoRA RELOAD -> ORDERED-PROBABILITY-VECTOR route is DEMONSTRATED, not assumed: a tiny adapter is trained, written, reloaded in a FRESH PROCESS from the written base+adapter artifacts, and evaluated into a full K-length probability vector in the contracted label order — proven on a toy config BEFORE any 9B cell is scheduled (review consensus item 1: this is the route EVAL-05 and every LoRA QualityBlock depend on, and no plan previously named a concrete API for it)"
  artifacts:
    - path: "crates/apr-cli/src/commands/finetune.rs"
      provides: "run_classify_core shared entry + selection-manifest ingest + explicit TrainingConfig control"
      contains: "selection_manifest"
    - path: "crates/apr-cli/src/model_ops_commands.rs"
      provides: "--selection-manifest, --seed, --val-split, --early-stopping-patience args on the Finetune variant"
    - path: "crates/aprender-train/src/finetune/classify_pipeline/mod.rs"
      provides: "The named reload+probability route the LoRA bench path calls: adapter load onto a from_apr base, plus a full-probability-vector prediction entry"
      contains: "predict_proba_tokenized"
    - path: "crates/aprender-train/src/finetune/classify_reload_tests.rs"
      provides: "The fresh-process save->reload->probability-vector preflight test"
      contains: "lora_adapter_reload_yields_ordered_probability_vector"
  key_links:
    - from: "crates/apr-cli/src/commands/finetune.rs"
      to: "aprender_contrastive_data::Selection::replay"
      via: "read_attested_canonical + read_selection_manifest + Selection::replay (the exact three-call door eval/setfit.rs:227-237 uses)"
      pattern: "Selection::replay"
    - from: "crates/apr-cli/src/commands/finetune.rs"
      to: "TrainingConfig"
      via: "explicit per-cell fields replacing the 1675-1685 hardcode block"
    - from: "crates/aprender-train/src/finetune/classify_reload_tests.rs"
      to: "ClassifyPipeline::from_apr + the adapter load route"
      via: "spawned fresh-process reload of the written base+adapter, evaluated into a K-length probability vector"
      pattern: "predict_proba_tokenized"
---

<objective>
Give the 9B LoRA baseline the same structural selection guarantee SetFit has (D-10): a
`--selection-manifest` input on `apr finetune --task classify` resolved through the Phase 2
manifest->Selection door, plus explicit control of the three hardcoded internals that would
otherwise poison EVAL-02/EVAL-04 (seed 42, val_split 0.2, early stopping = unlocked model
selection — Pitfall 3).

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-02's identical-sampled-ID guarantee must be one shared code path, not an exporter's
promise; frozen-defaults policy (research discretion, adopted: frozen published defaults for both
methods, early stopping disabled) requires these knobs to exist.
Output: wired CLI flags, shared core entry, A6 probe evidence, refusal tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (F-R12 fields; Pitfall 3; Assumption A6; "The LoRA fields D-10 must control" code example)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (section 8)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI args + manifest->Selection ingest + shared core entry</name>
  <files>crates/apr-cli/src/model_ops_commands.rs, crates/apr-cli/src/dispatch.rs, crates/apr-cli/src/commands/finetune.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/finetune.rs (run_classify :1563-1689 — especially the TrainingConfig hardcode block :1675-1685 {val_split: 0.2, save_every: 5, early_stopping_patience: 10, seed: 42} and how ClassifyConfig is built; the dispatch entry)
    - crates/apr-cli/src/model_ops_commands.rs (the Finetune variant at :6 and --task at :46-48 — clap arg style)
    - crates/apr-cli/src/dispatch.rs (:686-753 — the ModelOps Finetune pass-through)
    - crates/apr-cli/src/commands/eval/setfit.rs (:227-237 — the exact three-call ingest door to copy: AccessLedger::new, data_contrastive::read_attested_canonical, data_contrastive::read_selection_manifest, Selection::replay with the refusal-with-remedy message style)
    - crates/aprender-train/src/finetune/classify_trainer.rs (TrainingConfig :26 — every field pub; what the train result type exposes)
  </read_first>
  <action>
    Add to the Finetune variant in model_ops_commands.rs: `--selection-manifest <FILE>` (Option),
    `--seed <u64>` (Option), `--val-split <f32>` (Option), `--early-stopping-patience <usize>`
    (Option), each with value_name and doc comments stating the refusal semantics (house style from
    SetfitCommands::Train). Thread them through dispatch.rs into finetune::run. In finetune.rs,
    extract the classify path into a shared `pub(crate) fn run_classify_core(...)` taking an
    explicit resolved `TrainingConfig` + optional selection inputs, so the flag path and any future
    caller (plan 05-09's bench run) drive ONE implementation. Behavior when --selection-manifest is
    given: require --data (the attested canonical directory), run the three-call ingest door
    exactly as eval/setfit.rs does (read_attested_canonical + read_selection_manifest +
    Selection::replay), REFUSE with a typed CliError::ValidationFailed naming both flags and the
    remedy if replay fails, and restrict the training rows to the replayed selection's IDs before
    any batch is built. Record the manifest's semantic_hash in the run's output/report so a row can
    carry it. TrainingConfig construction: seed = --seed when given else the existing 42 (but when
    --selection-manifest is present, --seed is REQUIRED — refuse "selection-manifest runs must pin
    a seed"); val_split = --val-split when given else 0.2; early_stopping_patience = the flag when
    given else 10; save_every set so that with early stopping disabled only the FINAL epoch
    checkpoint is the model (no best-epoch selection under the frozen-defaults policy). Absent all
    new flags, behavior is byte-compatible with today (defaults preserved; existing tests
    untouched).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo check -p apr-cli --features setfit > /tmp/ft-check.log 2>&1; rc=$?; tail -3 /tmp/ft-check.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - model_ops_commands.rs contains `selection_manifest`, and finetune.rs contains `run_classify_core`
    - finetune.rs contains the three-call door (`read_attested_canonical`, `read_selection_manifest`, `Selection::replay`) — grep shows all three symbols
    - The hardcode block at the old :1675-1685 site no longer contains bare literals 42 / 0.2 / 10 without the flag-else fallback
    - `apr finetune --task classify --selection-manifest X` without `--seed` refuses with a message naming both flags (unit-testable via the arg-validation path)
  </acceptance_criteria>
  <done>Flags wired, ingest door shared, one core implementation, defaults preserved</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A6 probe (val_split 0.0), epochs_completed exposure, refusal tests</name>
  <files>crates/aprender-train/src/finetune/classify_trainer.rs, crates/apr-cli/src/commands/finetune.rs</files>
  <read_first>
    - crates/aprender-train/src/finetune/classify_trainer.rs (FULL: how val_split partitions batches, where early stopping reads patience, what the result/metrics carry — epoch_time_ms/samples_per_sec/total_time_ms per apr-finetune-metrics-v1)
    - the Task 1 diff
  </read_first>
  <behavior>
    - ClassifyTrainer with val_split 0.0 and early_stopping_patience 0 trains to exactly epochs_requested with zero validation batches and no division-by-zero / empty-slice panic (A6 probe as a small unit test)
    - The train result exposes epochs_completed; with early stopping disabled, epochs_completed == epochs_requested
    - Selection-manifest hash mismatch refuses BEFORE any training work (typed error; test with a doctored manifest byte)
    - A selection manifest whose IDs are not present in --data refuses with a typed error naming the missing count
  </behavior>
  <action>
    Probe A6 with a minimal unit test on ClassifyTrainer (tiny synthetic dataset, val_split 0.0,
    patience 0): if the trainer divides by zero, requires val batches, or early-stops implicitly,
    patch classify_trainer.rs minimally so val_split 0.0 is a first-class supported value (skip
    validation pass entirely, treat patience 0 as "disabled") — a trainer-side patch is in scope
    per A6's risk note; keep the patch surgical and covered by the new test. Expose
    `epochs_completed` in the train result if not already present (needed by the LoRA row's
    no-selection attestation — the claims contract's epochs_completed == epochs_requested rule).
    Add the two refusal tests for the manifest path (hash mismatch; IDs absent from data) at the
    finetune.rs level using the smallest constructible attested directory + manifest (reuse the
    fixtures/helpers the data_contrastive tests use). All tests run in every `cargo test` (no
    ignore, no feature beyond what the surfaces already require).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib classify_trainer > /tmp/ct.log 2>&1; rc=$?; tail -5 /tmp/ct.log; test $rc -eq 0 && CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit finetune > /tmp/ft.log 2>&1; rc2=$?; tail -5 /tmp/ft.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - A test named for the A6 probe exists and passes (val_split 0.0 + patience 0 -> epochs_completed == epochs_requested, no validation batches)
    - The train result type exposes epochs_completed (grep in classify_trainer.rs)
    - Both refusal tests assert typed errors and run in the default test invocation (non-zero matched tests in the log)
    - No behavior change for default invocations: pre-existing finetune tests all green
  </acceptance_criteria>
  <done>A6 resolved by measurement; attestation data exposed; refusals proven</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 3: LoRA reload preflight — prove save -> fresh-process reload -> ordered probability vector</name>
  <files>crates/aprender-train/src/finetune/classify_pipeline/mod.rs, crates/aprender-train/src/finetune/classify_trainer.rs, crates/aprender-train/src/finetune/classify_reload_tests.rs</files>
  <read_first>
    - crates/aprender-train/src/finetune/classify_pipeline/mod.rs (`ClassifyPipeline::new` :506, `from_pretrained` :643, `from_apr` :798 — note it builds FRESH LoRA layers via `build_lora_layers` and does NOT load trained adapter weights; `forward_only_tokenized` :1147 returns `(f32, usize)` = loss + predicted class, NOT a probability vector; `pre_tokenize` :973)
    - crates/aprender-train/src/finetune/classify_trainer.rs (:1250 — the `model.adapter.apr` write site; what the writer serializes and under which tensor names)
    - crates/aprender-train/src/finetune/multi_adapter_pipeline.rs (:372 `save_adapter_checkpoint` — the nearest existing adapter-checkpoint analog to follow)
    - crates/aprender-train/src/finetune/instruct_pipeline/constructors.rs (:91-92 — the `adapter_model.safetensors` sibling-detection pattern already used at load time)
    - crates/aprender-train/src/train/setfit/apr_evaluate.rs (the SetFit side's per-row probability-vector shape, so both methods hand `assemble_quality_block` the same thing)
    - contracts/setfit-benchmark-claims-v1.yaml (the QualityBlock fields a LoRA row must fill — this is what the route must be able to produce)
  </read_first>
  <behavior>
    - `predict_proba_tokenized(&mut self, token_ids: &[u32]) -> Vec<f32>` (name adapted to the module's conventions) returns a K-length vector that sums to 1.0 within 1e-5, in the SAME class index order the trainer was given — no argmax, no scalar
    - Round-trip: train a toy classifier (tiny `TransformerConfig`, 3 classes, 2 synthetic examples per class, 1 epoch), write base+adapter, then in a FRESH PROCESS load exactly those written files and produce probability vectors for the same inputs
    - Reload fidelity: the fresh-process probability vectors match the in-process post-training vectors within 1e-4 per element (a reload that silently drops the adapter would return near-uniform/base-model values and this assertion turns red)
    - Two-sided control: loading the base WITHOUT the adapter yields probability vectors that DIFFER from the with-adapter vectors by more than 1e-3 on at least one element — proving the assertion above is measuring adapter application, not tautological reload
    - The written artifact set is enumerable: the test asserts the exact file names it reloads from and their byte sizes (these become `base_model_bytes` / `artifact_bytes` / `deployable_total_bytes` in 05-09)
  </behavior>
  <action>
    This is the phase's LoRA tracer: the thinnest end-to-end path through train -> write -> fresh-process
    reload -> ordered probability vector, wired for keeps. Codex's review named it the highest-value
    change in either review, because EVAL-05 and every LoRA `QualityBlock` depend on a route that no
    plan had identified and that the code does not obviously provide: `ClassifyPipeline::from_apr`
    builds FRESH LoRA layers rather than loading trained ones, and `forward_only_tokenized` returns
    `(loss, predicted_class)` rather than a probability vector.
    (1) PROBE FIRST, then build. Determine by reading and by a compiling probe whether a load path for
    the written `model.adapter.apr` exists (`multi_adapter_pipeline::save_adapter_checkpoint`'s
    counterpart, or the `adapter_model.safetensors` sibling detection in instruct_pipeline's
    constructors). Record the finding literally in the SUMMARY: "route X exists / no route exists".
    (2) If no route exists, add the minimal one: a `ClassifyPipeline::load_adapter(&mut self, path: &Path)`
    that reads the adapter written by classify_trainer.rs:1250 and installs those weights into the
    layers `build_lora_layers` created, refusing with a typed error on rank/shape/layer-count mismatch
    (never a silent partial load — a partially applied adapter is the failure mode that would produce
    plausible-but-wrong numbers in all 40 cells).
    (3) Add `predict_proba_tokenized` beside `forward_only_tokenized`, reusing the same forward
    (extract the shared head-logits helper rather than duplicating the forward pass — OPS-03), and
    returning the softmax over ordered class indices.
    (4) Write the preflight test in the new sibling `classify_reload_tests.rs`, registered the way
    `classification_tests.rs` / `classify_trainer_tests.rs` are. The FRESH PROCESS requirement is
    load-bearing (the whole point is that an in-process reload benefits from warm state and can mask a
    non-loading adapter): implement it by having the test spawn itself with a marker env var
    (`APRENDER_RELOAD_CHILD=1`) via `std::process::Command` on `std::env::current_exe()`, running the
    child leg with `--exact --nocapture`, reaping the ExitStatus, and comparing the child's printed
    probability vectors against the parent's in-process values. Never read a status through a pipe.
    Keep the toy config small enough that the whole test runs in under 60 s on CPU.
    (5) HARD GATE: if this task cannot produce an ordered probability vector from a fresh-process
    reload, STOP and report it as a finding. Do NOT proceed to 05-09's LoRA path or 05-11's 40 remote
    9B cells on an assumed route — that is exactly the "40 expensive cells against an unproven API"
    failure the review flagged. A stop here costs one plan; a stop at 05-11 costs the GPU matrix.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib classify_reload > /tmp/reload.log 2>&1; rc=$?; tail -20 /tmp/reload.log; test $rc -eq 0 && grep -E 'test result.*[1-9][0-9]* passed' /tmp/reload.log > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `classify_pipeline/mod.rs` contains a public probability-vector entry (`predict_proba_tokenized` or the module-conventional name) whose return type is a `Vec<f32>` of length `num_classes`, and the SUMMARY quotes its exact signature
    - A test named `lora_adapter_reload_yields_ordered_probability_vector` exists in classify_reload_tests.rs, spawns a fresh child process (grep shows `current_exe` and a reaped `ExitStatus`, and zero `$?`-through-a-pipe patterns), and passes
    - The with-adapter vs without-adapter two-sided control is asserted and passes (max elementwise difference > 1e-3 recorded in the SUMMARY as a number, not a claim)
    - Fresh-process vs in-process vectors agree within 1e-4 per element; the SUMMARY records the measured max difference
    - The SUMMARY states literally whether an adapter load route pre-existed or was added, and enumerates the exact written file names + byte sizes the reload consumed
    - `cargo test -p aprender-train --lib classify_reload` exits 0 with a non-zero matched-test count (CR-02 vacuity floor)
  </acceptance_criteria>
  <precondition>The A6 patch from Task 2 is in the working tree, so a toy run can complete with val_split 0.0 and early stopping disabled.</precondition>
  <reversibility rating="reversible">Adding a probability-vector accessor and an adapter load route is additive to an existing pipeline; nothing in Phases 1-4 reads either symbol.</reversibility>
  <done>The LoRA reload -> ordered probability vector route is demonstrated end-to-end in a fresh process, with a two-sided control proving the adapter is actually applied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection manifest file -> training rows | untrusted bytes; digest verified inside from_bytes before replay; replay verified against the attested dataset |
| CLI defaults -> claims validity | hardcoded seed/split/early-stop would silently invalidate EVAL-02 pairing and inject unlocked model selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | selection manifest | mitigate | SelectionManifest::from_bytes digest-verifies before return; Selection::replay refuses mismatched data; typed refusal before training starts |
| T-05-06-02 | Elevation (unlocked model selection) | early stopping / best-epoch checkpoint | mitigate | patience 0 = disabled; final-epoch-only checkpoint under frozen defaults; epochs_completed attested in rows and enforced by the 05-10 gate |
| T-05-06-03 | Spoofing (wrong seed) | TrainingConfig.seed | mitigate | --seed REQUIRED with --selection-manifest; contracted-seed enforcement happens at the bench layer (05-09), CLI stays generic |
| T-05-06-04 | Repudiation | which rows trained | mitigate | manifest semantic_hash recorded in the run output for the row |
| T-05-06-05 | Spoofing (silently-unapplied adapter, severity HIGH) | adapter reload route | mitigate | `load_adapter` refuses rank/shape/layer-count mismatch with a typed error (never a partial load); the Task 3 two-sided control proves with-adapter and without-adapter vectors differ, so a no-op reload cannot pass |
| T-05-06-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files CREATED by this plan (newly-created — exclude from drift verification):

- CLI flags on the `Finetune` variant: `--selection-manifest`, `--seed`, `--val-split`, `--early-stopping-patience`
- `apr_cli::commands::finetune::run_classify_core`
- `epochs_completed` on the classify train result (classify_trainer.rs)
- `ClassifyPipeline::load_adapter` (only if Task 3's probe finds no pre-existing route — the SUMMARY records which)
- `ClassifyPipeline::predict_proba_tokenized`
- `crates/aprender-train/src/finetune/classify_reload_tests.rs` with `lora_adapter_reload_yields_ordered_probability_vector`
- Env var `APRENDER_RELOAD_CHILD` (fresh-child leg marker)
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- INCORPORATED (consensus item 1, VERIFIED HIGH — the review's highest-value change): Task 3 added. Codex found that no plan identified a concrete API that reloads the written base+adapter artifact in a fresh process and yields ordered full probability vectors, yet EVAL-05 and every LoRA QualityBlock depend on it; the code confirms the gap (`from_apr` builds fresh LoRA layers; `forward_only_tokenized` returns `(loss, class)`). The preflight now runs in wave 2, three waves before any 9B cell is scheduled, and is a hard stop-and-report gate.
- ALREADY ADDRESSED (Gemini Finding 2, "Uncontrolled Model Selection and Contamination"): Tasks 1 and 2 already control seed/val_split/early stopping explicitly and probe A6 by measurement rather than assumption; 05-05 contracts `val_split: 0.0`, `early_stopping_disabled: true`, `epochs_completed == epochs_requested`. Gemini's additional ask — decoupling test evaluation from `apr finetune` into an isolated evaluation entrypoint — is satisfied by Task 3's `predict_proba_tokenized` on a RELOADED pipeline plus 05-09 Task 3's rule that quality is computed from the reloaded artifacts, never from in-memory final-epoch state.
</review_notes>

<verification>
- apr-cli + aprender-train suites green; default-path behavior unchanged
- Grep evidence of the three-call door and the removed bare hardcodes
</verification>

<success_criteria>
- D-10 structural: one shared path, hash-refusing, per-cell controllable; A6 measured; attestation fields available for rows
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-06-SUMMARY.md` when done
</output>
