---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 12
type: execute
wave: 7
# 05-10 added per review consensus item 4 (VERIFIED HIGH): the declared graph previously omitted the
# claims gate even though strict wave-order execution happened to mask it. Wave 6 -> 7 follows
# 05-11's move to wave 6 (05-11 and 05-12 write the same run-manifest, so they must stay in
# different waves).
depends_on: ["05-03", "05-09", "05-10", "05-11"]
files_modified:
  - benchmarks/tweeteval-stance/rows/
  - benchmarks/tweeteval-stance/run-manifest.json
  - benchmarks/tweeteval-stance/selections/
autonomous: false
requirements: [EVAL-02, EVAL-03, EVAL-05]

must_haves:
  truths:
    - "All 40 SetFit cells ran CPU from the RELOADED production artifact per cell (train -> APR -> reload -> measure -> evaluate under lock discipline), each emitting one digest-committed row"
    - "Every SetFit row pairs with its LoRA counterpart on an identical selection_manifest_hash (EVAL-02's guarantee, spot-verified here, gate-enforced in 05-13)"
    - "The compute was pre-authorized (05-03 checkpoint) or re-authorized here before running (CLAUDE.md >1hr rule) — never silently burned"
    - "Cells run SEQUENTIALLY, one process at a time against one bench dir. A wall-clock overrun is escalated as a checkpoint, never resolved by running cells concurrently — concurrent cells would contend for CPU and memory and invalidate every EVAL-05 resource number in the run (review item 9)"
    - "The claims gate (05-10) exists and is green before these cells are generated, and the pilot-cell evidence from 05-11 already proved a real row passes every per-row gate check"
  prohibitions:
    - "No committed benchmark artifact — row, selection manifest, lock record, candidate ledger, or manifest — may contain dataset text. The evidence set carries IDs, hashes and metrics only; the attested dataset directory stays outside the repository."
    - "A wall-clock or budget pressure must never be relieved by degrading the measurement protocol (fewer warmups, skipped cold-probe children, sampled instead of child-measured peaks). If the run does not fit, the run is escalated, not thinned."
  artifacts:
    - path: "benchmarks/tweeteval-stance/run-manifest.json"
      provides: "All 80 cells complete with content hashes"
    - path: "benchmarks/tweeteval-stance/rows/"
      provides: "40 setfit-*.json rows beside the 40 lora rows"
  key_links:
    - from: "rows/setfit-*.json"
      to: "rows/lora-*.json"
      via: "identical selection_manifest_hash per (shots, seed) — both methods consumed the same selections/ artifact"
    - from: "each setfit row"
      to: "its cell's model.apr"
      via: "apr_artifact_sha256 + lock reference (Ph4 D-16 workflow, consumed per cell)"
---

<objective>
Execute the 40 SetFit cells (4 shots x 10 contracted seeds) on the CPU host decided at the 05-11
checkpoint (default: this macOS host), through `apr setfit bench run` per cell with per-cell
resume, completing the 80-cell matrix.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: the SetFit half of EVAL-02/EVAL-03/EVAL-05 — benchmark predictions from the RELOADED
production artifact (success criterion 5), resource metrics under the contracted protocol.
Output: 40 verified SetFit rows; run manifest complete at 80/80.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 5: resume; Pattern 6: resource mechanisms; Pitfall 7 host sharp edges)
@.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md  (the per-cell timing basis for the projection)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute authorization gate + full 40-cell SetFit run</name>
  <files>benchmarks/tweeteval-stance/rows/, benchmarks/tweeteval-stance/run-manifest.json, benchmarks/tweeteval-stance/selections/</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-03-SUMMARY.md (whether the SetFit 40-cell compute was pre-authorized at the D-04 checkpoint, and the unblock commit SHA)
    - .planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md (the Q6 host decision; the selections/ set both methods share)
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (probe timing — the projection basis; s64 ~8x s8)
    - scripts/run_bench_cells.sh (driver semantics: resume, pin via apr_bin.sh, rc discipline)
  </read_first>
  <action>
    First the authorization gate: project the 40-cell wall-clock from 05-01's measured per-cell
    timing (10x s8-class + 10x s16 + 10x s32 + 10x s64, scaling by selected-row count) and WRITE
    THE PROJECTION DOWN as a number before running anything — the review raised (unverified) that
    40 sequential SetFit passes at s=64 could take 3-4 h, and an unrecorded projection cannot be
    compared against the outcome afterwards. Add the per-cell cold-probe child (one extra process
    load per cell) to the projection; it is small but it is real.
    PARALLELISM IS FORBIDDEN, and this is the deliberate answer to the review's suggestion of
    "concurrent single-threaded cell processes to keep execution under 45 minutes": running cells
    concurrently would make every cell's `train_peak_rss_bytes`, `cold_latency_ms`,
    `warm_latency_ms_median` and `throughput_rows_per_sec` a measurement of a contended machine,
    which is not the quantity EVAL-05 contracts, and it would race the shared run-manifest.
    `scripts/run_bench_cells.sh` has no `--jobs` flag by design (05-09). If the projection is
    unacceptable, the options are: run on a different host (re-open Open Q6 at a checkpoint),
    reduce nothing and accept the wall-clock, or stop — never parallelize, and never thin the
    measurement protocol.
    If the 05-03 SUMMARY records pre-authorization, proceed; otherwise, if the projection exceeds 60
    minutes on a non-lambda-vector host, STOP and present the projection as a checkpoint
    (CLAUDE.md compute rule) and wait. Then run:
    `CARGO_INCREMENTAL=0 bash scripts/run_bench_cells.sh setfit benchmarks/tweeteval-stance <attested-data-dir> "$APRENDER_MINILM_DIR"`
    on the decided host, with the attested TweetEval dataset dir at
    `~/.cache/aprender/tweeteval-stance-bench/` (generate once via the `apr data` doors if absent —
    the 04-15 rung-1 machinery; the dataset dir stays OUTSIDE the repo so no tweet text can be
    committed and no .gitignore change is needed). The driver consumes the SAME selections/ set
    05-11 used (byte-identical manifests = the pairing key). Watch for the two known host hazards:
    disk (the ENOSPC history — CARGO_INCREMENTAL=0 already exported; if ENOSPC hits, stop and
    report with numbers, delete nothing) and wall-clock drift beyond projection (report, don't
    push through silently). Per-cell resume means an interrupted run restarts cheaply — rerun the
    driver, verified cells skip.
  </action>
  <verify>
    <automated>ls benchmarks/tweeteval-stance/rows/setfit-*.json | wc -l | grep -q '^ *40$'</automated>
  </verify>
  <acceptance_criteria>
    - The projection + authorization basis is recorded BEFORE the run in the SUMMARY as a NUMBER (pre-auth reference or checkpoint transcript), and the actual wall-clock is recorded after, with the ratio stated
    - 40 files rows/setfit-s{shots}-seed{seed}.json exist covering the full contracted grid
    - The driver log shows per-cell PASS lines with rc=0 (rc never read through a pipe), and the run used ONE cell process at a time (the driver's pid lock plus the absence of any `--jobs`/`&`/`xargs -P` construct)
    - If any cell failed in the evidence class, the driver HALTED there and the SUMMARY reports the failing cell and refusal text — no tally-and-continue
    - One quoted setfit row excerpt: backend_identity `cpu:setfit-core:...` (executed, not config), apr_artifact_sha256 present, lock reference + committed locks/*.lock.json path present, `train_peak_rss_mechanism` matching the decided host's mechanism, `inference_peak_rss_mechanism` = the child-measured mechanism, `cold_measured_in_child_process: true`, and `artifact_bytes == deployable_total_bytes`
  </acceptance_criteria>
  <done>40 SetFit rows emitted from reloaded production artifacts under the contracted protocol</done>
</task>

<task type="auto">
  <name>Task 2: 80/80 manifest closure + pairing spot-check + commit</name>
  <files>benchmarks/tweeteval-stance/run-manifest.json, benchmarks/tweeteval-stance/rows/</files>
  <read_first>
    - benchmarks/tweeteval-stance/run-manifest.json (post-run state)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (what verify_run will demand in 05-13 — this task pre-checks, it does not replace the gate)
  </read_first>
  <action>
    Confirm the manifest records 80/80 complete cells with hashes. Spot-check pairing BEFORE the
    report plan: for at least 4 (shots, seed) pairs spanning all four shot levels, extract
    selection_manifest_hash from both method rows and assert equality (a mismatch here means the
    selections diverged — stop and diagnose against 05-11's rsync verification rather than
    proceeding to a guaranteed 05-13 refusal). Confirm no committed file contains raw tweet text
    (grep control over rows/ and selections/ — selection manifests carry IDs and hashes only).
    Commit rows + manifest + selections as the claims evidence set with a message naming the cell
    counts. Do NOT run the full bench report here — that is 05-13's headline act over the complete,
    committed set.
  </action>
  <verify>
    <automated>grep -c '"status"[: ]*"complete"' benchmarks/tweeteval-stance/run-manifest.json | grep -q '^80$' || python3 -c "import json,sys; m=json.load(open('benchmarks/tweeteval-stance/run-manifest.json')); cells=[e for e in (m.get('payload',m).get('expectation') or []) if (e.get('status')=='complete')]; sys.exit(0 if len(cells)==80 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Manifest shows exactly 80 complete cells (both the grep or the JSON count route succeeds)
    - Four pairing equalities quoted in the SUMMARY (hash values, per shot level)
    - The text-free grep control result recorded; rows + manifest + selections committed on the branch
  </acceptance_criteria>
  <done>The 80-cell evidence set is complete, pair-consistent, text-free, and committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long-running local compute -> host stability | ENOSPC history on this host; incremental cache disabled; stop-and-report discipline |
| rows -> repository | committed evidence must never contain dataset text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12-01 | DoS | 40-cell compute | mitigate | projection + pre-authorization gate before running; per-cell resume on interruption |
| T-05-12-02 | Tampering | pairing key | mitigate | same selections/ artifact as 05-11; 4-pair spot-check before the report wave |
| T-05-12-03 | Information disclosure | tweet text | mitigate | dataset dir outside the repo; grep control before commit |
| T-05-12-04 | Spoofing | backend identity | mitigate | cpu:setfit-core identity read from execution; quoted row excerpt in SUMMARY |
| T-05-12-05 | Repudiation (contended measurements, severity MEDIUM) | wall-clock pressure -> parallel cells | mitigate | parallelism structurally impossible (no `--jobs` in the driver, pid lock on the bench dir); an overrun is escalated as a checkpoint and the protocol is never thinned |
| T-05-12-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<artifacts_this_phase_produces>
Files CREATED by this plan (newly-created — exclude from drift verification):

- `benchmarks/tweeteval-stance/rows/setfit-s{8|16|32|64}-seed{13|17|23|29|31|37|41|43|47|53}.json` (40 rows)
- `benchmarks/tweeteval-stance/locks/setfit-s{shots}-seed{seed}.lock.json` (40 committed lock records)
- `benchmarks/tweeteval-stance/run-manifest.json` completed to 80/80
- The attested TweetEval dataset directory at `~/.cache/aprender/tweeteval-stance-bench/` (OUTSIDE the repo, deliberately — no dataset text is ever committed)
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- INCORPORATED (consensus item 4, VERIFIED HIGH): `depends_on` gains 05-10 (the declared graph previously omitted the gate); wave 6 -> 7 to stay behind 05-11, with which it shares the run-manifest file.
- INCORPORATED (item 9, UNVERIFIED — judged and answered rather than adopted): the wall-clock projection must now be recorded as a number before the run and compared afterwards. Gemini's proposed remedy — concurrent single-threaded cell processes — is REJECTED on measurement grounds: concurrency would make every EVAL-05 resource number a measurement of a contended machine and would race the shared run-manifest. The honest options are a different host, accepting the wall-clock, or stopping.
</review_notes>

<verification>
- 40 setfit rows + 80/80 manifest; pairing spot-checks green; evidence committed
</verification>

<success_criteria>
- The complete matrix exists locally, pair-consistent and committed, ready for the fail-closed report
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-12-SUMMARY.md` when done
</output>
