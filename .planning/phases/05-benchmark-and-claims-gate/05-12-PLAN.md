---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 12
type: execute
# Wave and depends_on DELIBERATELY UNCHANGED across the D-19 replan. 05-11 was repurposed rather
# than vacated (it now retargets the declared matrix from 80 cells to 40), and this plan needs
# that retarget: the first `apr setfit bench run` writes a run manifest derived from
# `RunManifest::expectation()`, so a cell generated before the retarget would declare 80 expected
# cells, 40 of which can never exist. The dependency is therefore MORE load-bearing than it was,
# not less.
wave: 7
depends_on: ["05-03", "05-09", "05-10", "05-11"]
files_modified:
  - benchmarks/tweeteval-stance/rows/
  - benchmarks/tweeteval-stance/selections/
  - benchmarks/tweeteval-stance/locks/
  - benchmarks/tweeteval-stance/run-manifest.json
autonomous: false
requirements: [EVAL-02, EVAL-03, EVAL-05]

must_haves:
  truths:
    - "All 40 SetFit cells ran on CPU from the RELOADED production artifact per cell (train -> APR -> reload -> measure -> evaluate under lock discipline), each emitting one digest-committed row"
    - "The compute was authorized at a blocking checkpoint BEFORE any cell ran. 05-03's checkpoint explicitly did NOT pre-authorize it — the human deferred the question to wave 7 — so this plan asks, with the wall-clock projection written down as a number first"
    - "ONE pilot cell was driven through the REAL gate before the remaining 39 were authorized, in two halves that together cover what a single half does not: (a) the single-cell verification door run against the pilot row on disk exits 0, exercising row digest, schema, manifest-hash agreement, slot agreement, the recomputed lock digest, lock role/rule and the resource and size fields; (b) `bench report` over a COPY of the bench directory refuses, and the refusal names a MISSING cell and nothing about the pilot row's own content"
    - "The pilot row is the artifact the driver itself accepts: the later sweep SKIPS that cell by digest rather than re-running it, which is a behavioural proof that the pilot's invocation and the driver's agree — a stronger check than comparing two spellings of a command"
    - "Every row records the selection-manifest hash for its (shots, seed) cell — the retained EVAL-02 pairing key that lets the deferred second arm be added later without re-running this half (D-ITEM-05-15)"
    - "Cells run SEQUENTIALLY, one process at a time against one bench directory. A wall-clock overrun is escalated as a checkpoint, never resolved by running cells concurrently — concurrent cells would contend for CPU and memory and invalidate every EVAL-05 resource number in the run"
    - "The within-row peak-RSS mechanism asymmetry is DECLARED for all 40 rows, not sampled from one: the TRAIN peak on this host is a sysinfo-sampled lower bound carrying its sample-interval field, while the INFERENCE peak is a true kernel high-water mark measured in a dedicated fresh child. The tally is over 40 rows because one quoted row is an anecdote"
    - "The claims gate exists, is green, and was re-proven at the 40-cell scope by 05-11 before these cells were generated"
  prohibitions:
    - "No committed benchmark artifact — row, selection manifest, lock record or run manifest — may contain dataset text. The evidence set carries IDs, hashes and metrics only; the attested dataset directory stays outside the repository."
    - "A wall-clock or budget pressure must never be relieved by degrading the measurement protocol: not fewer warmups, not a skipped cold-probe child, not a sampled peak where a child-measured one is contracted, not a reduced seed or shot set. If the run does not fit, the run is escalated, not thinned."
    - "Cells must never be run concurrently. The driver has no parallel-dispatch flag by design and holds a single-writer lock; neither may be worked around."
    - "A failing cell is never papered over. An evidence-class refusal HALTS the sweep and is reported with its refusal text; it is never tallied-and-continued, and no row is hand-edited to make the gate pass."
    - "No artifact produced here may carry a second method's row, ledger or comparison. Under the 40-cell active scope such a row is a typed gate refusal; producing one to see what happens would put an unpublishable artifact in the evidence directory."
  artifacts:
    - path: "benchmarks/tweeteval-stance/rows/"
      provides: "40 setfit-s{shots}-seed{seed}.json rows, each digest-committed"
    - path: "benchmarks/tweeteval-stance/run-manifest.json"
      provides: "All 40 contracted cells complete with content hashes"
    - path: "benchmarks/tweeteval-stance/selections/"
      provides: "The 40 selection manifests — the retained pairing key, one file per cell"
  key_links:
    - from: "benchmarks/tweeteval-stance/rows/setfit-*.json"
      to: "benchmarks/tweeteval-stance/selections/s{shots}-seed{seed}/selection-manifest.json"
      via: "selection_manifest_hash recorded per row — EVAL-02's retained pairing key"
    - from: "each row"
      to: "its cell's reloaded model.apr and lock record"
      via: "apr_artifact_sha256 + the committed locks/*.lock.json whose bytes the gate re-hashes"
    - from: "the pilot cell"
      to: "the 39-cell sweep"
      via: "hash-based resume — the driver SKIPs the pilot, proving the pilot's row is what the driver would have produced"
---

<objective>
Generate the phase's evidence set: the 40 SetFit cells (4 shot levels x the ten contracted seeds)
on this CPU host, one process per cell, from reloaded production artifacts, fronted by a compute
authorization checkpoint and by a pilot cell driven through the real gate.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: after D-19 this is the phase's ONLY cell-generation plan. It delivers the amended EVAL-02
matrix, EVAL-03's machine-readable rows, and EVAL-05's resource measurements.
Output: 40 verified rows, 40 selection manifests, 40 lock records, and a run manifest closed at
40/40, committed as the claims evidence set.
</objective>

<amended_requirements>
## What this plan delivers under the AMENDED requirement text

| Id | Delivered here | NOT delivered, and why |
|----|----------------|------------------------|
| EVAL-02 | All 40 SetFit shot/seed cells, each bound to a recorded selection manifest whose hash is written into the row | The second method's 40 cells. Deferred as `D-ITEM-05-15`; the pairing key is recorded precisely so the SetFit half never needs re-running |
| EVAL-03 | One machine-readable row per cell with dataset/model revisions, selection lock, artifact hash, encoder-update evidence, executed backend identity, quality metrics and resource metrics | Nothing withheld. EVAL-03 was not amended |
| EVAL-05 | Training time, cold/warm latency, throughput with batch and warmup boundaries, the two peak-RSS figures with their mechanisms, artifact size, calibration and quality — all from the same reloaded production artifacts | Cross-METHOD cost comparison. Within-SetFit comparison across shot levels and seeds is delivered |

EVAL-02 remains Pending and amended in scope; it is not satisfied by this plan's narrower matrix.
</amended_requirements>

<selection_manifest_ownership>
## Where the selection manifests come from

The retired LoRA plan claimed to own generating the 40 selection manifests. That claim was already
stale when it was written. `scripts/run_bench_cells.sh` — shipped and lint-clean under 05-09 —
generates them itself in `generate_selections()`, one per (shots, seed), into
`$BENCH_DIR/selections/s{shots}-seed{seed}/selection-manifest.json`, via `apr data select` against
the attested dataset directory, and SKIPS any that already exist. The skip is deliberate and
documented in the script: regenerating a manifest is a NEW draw even at the same seed, so reuse is
what makes "one file" true rather than "two generators agreed".

So the capability survives the retirement untouched and this plan inherits it by invoking the
driver. Verify it rather than trusting this paragraph: read `generate_selections` and the
`selection=` path expression in `run_one_cell`, and confirm the two spellings agree — the 05-09
driver-gate test asserts exactly that, and it is in the suite this plan's verify runs.

The one manifest generated outside the driver is the pilot cell's, in Task 2, through the same
`apr data select` invocation copied verbatim from `generate_selections`. The sweep in Task 3 then
finds it present and skips it, which is the check that the two routes agree.
</selection_manifest_ownership>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (read the reconciliation banner FIRST; Pattern 5 resume and Pattern 6 resource mechanisms still hold, every lambda-vector claim is superseded)
@.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md  (the measured per-cell timing basis for the projection)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Compute authorization — the projection, written down as a number, then approved</name>
  <files>none (projection + presentation; no cell runs in this task)</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-03-SUMMARY.md (the checkpoint transcript: the SetFit compute was explicitly NOT pre-authorized and the human deferred the question to wave 7 — read the recorded words, do not infer approval from the phase having continued)
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (measured per-pass wall-clock at the production envelope; s64 is the expensive end)
    - .planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md (the retarget landed and the gate is green at the 40-cell scope — the precondition for spending anything)
    - scripts/run_bench_cells.sh (the sweep this authorizes: sequential, single-writer, halt-on-first-evidence-failure, hash-based resume)
    - .planning/STATE.md (the host's standing hazards: the recurring incremental-cache ENOSPC, the arm64 tier2 reds, the apr-cli package KNOWN-RED — none of which is a Phase 5 regression)
  </read_first>
  <precondition>05-11's retarget is committed and `make setfit-bench-tests` is green at the 40-cell scope. If the expectation set is still 80, this plan does not start — the first cell would write a manifest declaring 40 cells that can never exist.</precondition>
  <decision>
    Whether to spend the projected wall-clock on the 40-cell SetFit sweep on this host, now.
  </decision>
  <context>
    05-03's checkpoint recorded, verbatim, that this compute is NOT pre-authorized and that the
    human deferred the question to wave 7. This is wave 7. CLAUDE.md reserves compute decisions
    beyond an hour on non-pre-authorized hosts to the human, and the pre-authorized host named in
    that rule is the one D-19 proved unreachable — so there is no host on which this is
    pre-authorized.

    Automation first, then ask. Before presenting, compute and WRITE DOWN the projection as a
    number: ten cells at each shot level, scaled from 05-01's measured per-pass timing by selected
    row count, plus the per-cell cold-probe child (one extra process load per cell — small, real,
    and easy to leave out of a projection). An unrecorded projection cannot be compared against the
    outcome afterwards, which is the only way a wall-clock surprise becomes a finding rather than a
    feeling. Also record the free-disk figure and the projected peak, because the recurring failure
    mode on this host is the incremental cache filling the volume mid-sweep.

    Note what is NOT on the table. Parallelism is not an option: concurrent cell processes would
    make every `train_peak_rss_bytes`, `cold_latency_ms`, `warm_latency_ms_median` and
    `throughput_rows_per_sec` a measurement of a contended machine, which is not the quantity
    EVAL-05 contracts, and they would race the shared run manifest. The driver has no such flag by
    design. Thinning the protocol is not an option either. The honest choices are to accept the
    wall-clock, to move to a different host, or to stop.
  </context>
  <options>
    <option id="option-a">
      <name>Approve the sweep on this host at the projected wall-clock, sequential, protocol intact</name>
      <pros>The evidence set exists and 05-13 can run. Resume is hash-based at cell granularity, so an interruption costs one cell rather than the sweep. The measurement protocol is exactly the one the contract describes.</pros>
      <cons>The projected wall-clock is spent on one machine that cannot do anything else meaningful while it runs, and the TRAIN-peak column will be a sampled lower bound on this platform (labelled as such, per row).</cons>
    </option>
    <option id="option-b">
      <name>Approve, but run the pilot cell first and re-present the projection with the pilot's measured time before authorizing the remaining 39</name>
      <pros>Replaces the projection's weakest input — a per-cell time extrapolated from calibration passes — with one measured cell of exactly the shape being run. Costs one cheap s8 cell. The pilot is needed anyway for the gate proof, so nothing is wasted.</pros>
      <cons>Two checkpoints instead of one. Recommended default nonetheless: Task 2 exists regardless, so this option only changes where the authorization boundary sits.</cons>
    </option>
    <option id="option-c">
      <name>Move to a different CPU host</name>
      <pros>Frees this machine.</pros>
      <cons>No second host is available — D-19 enumerated the candidates and found none reachable, and the one AWS instance that exists is a t3.small. Listed so the option is refuted on the record rather than omitted.</cons>
    </option>
    <option id="option-d">
      <name>HALT — do not spend the compute this cycle</name>
      <pros>The claims gate, the retargeted contract and the 40-cell expectation all remain as real, shipped, tested work. The record says plainly that the evidence set was not generated and why.</pros>
      <cons>EVAL-02, EVAL-03 and EVAL-05 have no evidence, 05-13 does not run, and the milestone's claims layer does not ship this cycle.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Read the projection as a number, with its inputs shown: the measured per-pass basis, the
       scaling to each shot level, and the cold-probe child overhead. A projection with no stated
       basis is a guess.
    2. Confirm the free-disk figure against the projected peak. The incremental cache has filled
       this volume before; `CARGO_INCREMENTAL=0` is exported by the driver, and that is a
       mitigation rather than a guarantee.
    3. Confirm you accept that the TRAIN-peak column on this platform is a sampled lower bound
       carrying its sample-interval field, while the INFERENCE peak is a true kernel high-water
       mark from a dedicated child. The original host question is moot — there is no second host —
       but the consequence is not, and it will be declared per row and surfaced in the report
       rather than averaged into one peak figure.
    4. Confirm the sweep is sequential and that a wall-clock overrun will come back to you as a
       checkpoint rather than being resolved by parallelism or by a thinner protocol.
    5. State whether the authorization covers the whole sweep (option-a) or only the pilot, with
       the remainder re-presented against the pilot's measured time (option-b).
  </how-to-verify>
  <acceptance_criteria>
    - The projection is recorded in the SUMMARY as a NUMBER with its inputs, BEFORE any cell ran
    - The selected option is recorded verbatim with the human's rationale as given
    - The free-disk figure and the projected peak are recorded
    - If option-d is selected, the SUMMARY says so plainly and names 05-13 as blocked; the halt is reported, never dressed as partial success
  </acceptance_criteria>
  <verify>
    <human-check>Human reviewed the written projection and its basis, the disk headroom, the declared train-vs-inference peak mechanism asymmetry, and the sequential-only constraint — and SELECTED an option</human-check>
  </verify>
  <resume-signal>Select: option-a, option-b, option-c or option-d — or ask for a revised projection (the executor re-computes and re-presents; no cell runs before a selection is recorded)</resume-signal>
  <done>An explicit authorization recorded verbatim, with the projection on the record before any compute was spent</done>
</task>

<task type="auto">
  <name>Task 2: The pilot cell, driven through the real gate before the remaining 39 are authorized</name>
  <files>benchmarks/tweeteval-stance/selections/, benchmarks/tweeteval-stance/rows/, benchmarks/tweeteval-stance/locks/, benchmarks/tweeteval-stance/run-manifest.json</files>
  <read_first>
    - scripts/run_bench_cells.sh (`generate_selections` and `run_one_cell` — the two invocations this task copies VERBATIM, including the flag order and the fact that the select output flag takes a DIRECTORY)
    - scripts/apr_bin.sh (the pin; sourced, option-neutral, reports by return status)
    - .planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md (the single-cell verification door this task consumes, and what it does and does not check)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (`verify_run`'s numbered refusal ORDER — this is why the two halves below are both needed)
    - CLAUDE.md (Verification Discipline rules 1, 2, 3 and 6: never read a status through a pipe; prove the mechanism engaged; pin the binary; one failing input is an anecdote)
  </read_first>
  <precondition>Task 1 recorded an authorization covering at least the pilot cell. The attested TweetEval benchmark dataset directory exists outside the repository (generate it once through the `apr data` doors if absent — it stays outside the repo so no dataset text can ever be committed and no ignore-rule change is needed).</precondition>
  <action>
    This replaces the pilot proof the retired plan carried, and it is deliberately two halves,
    because one half alone proves less than the retired plan claimed. `verify_run` checks manifest
    digest, then the expectation set, then completeness, and only THEN reads any row's bytes. A
    report run over a directory holding one row therefore refuses at the completeness step and
    never reaches the pilot row at all. Running only that half would establish that the gate
    refuses an incomplete set — worth having — while appearing to establish that the pilot row
    passed every per-row check, which it would not.

    Pin the binary first, by sourcing the pin script and failing on its return status. Then:

    Generate the pilot cell's selection manifest with the same `apr data select` invocation
    `generate_selections` issues — same flags, same order, and the output flag naming the cell's
    DIRECTORY, since it writes a fixed filename inside whatever directory it is given. Then run the
    single pilot cell with the same `apr setfit bench run` invocation `run_one_cell` issues for the
    SetFit method, capturing rc with the `||` form the driver uses rather than a bare status read
    on the next line.

    Half (a) — the per-row half. Run the single-cell verification door from 05-11 against the pilot
    cell in the real bench directory and require it to exit 0. This is the door that runs the same
    per-row checks `verify_run`'s loop runs: row file present, row digest, schema, manifest-hash
    agreement, slot agreement, the recomputed lock digest against the committed lock bytes, lock
    role and rule, and the resource and size fields. Quote its output.

    Half (b) — the whole-set half. Copy the bench directory to a temporary path and run the real
    `bench report` against the COPY, never against the real directory. Require a non-zero status,
    and require the refusal to be about a MISSING cell and nothing about the pilot row's content.
    Quote the refusal verbatim with rc on its own line.

    If either half fails, STOP and report. A per-row defect found after 39 more cells have been
    spent would invalidate the whole set, which is the ordering discipline this phase already
    adopted when it put the gate ahead of the cells.

    Do not delete the pilot's row. Task 3's sweep must find it and skip it.
  </action>
  <verify>
    <automated>test -f benchmarks/tweeteval-stance/rows/setfit-s8-seed13.json && test -s /tmp/p12-pilot-cell-verify.log && test -s /tmp/p12-pilot-report-refusal.log && grep -q '^rc=0$' /tmp/p12-pilot-cell-verify.log && grep -qv '^rc=0$' /tmp/p12-pilot-report-refusal.log</automated>
  </verify>
  <acceptance_criteria>
    - `benchmarks/tweeteval-stance/selections/s8-seed13/selection-manifest.json` and `benchmarks/tweeteval-stance/rows/setfit-s8-seed13.json` exist, and the row's lock record is committed alongside
    - Half (a): the single-cell door exited 0 against the pilot cell; its output is quoted and the SUMMARY names each per-row check it covered
    - Half (b): `bench report` on the COPY exited non-zero, rc captured on its own line, refusal quoted verbatim, and the refusal names a missing cell — nothing about the pilot row's digest, schema, slot, lock or fields
    - The real bench directory was not the target of the report run (the copy's path is quoted and differs)
    - One quoted row excerpt showing: the executed backend identity (read from execution, not echoed from config), the selection-manifest hash, `apr_artifact_sha256`, the committed lock path, both peak-RSS mechanism strings, the cold-probe-in-child marker, and the artifact size fields
    - The pilot's measured wall-clock is recorded, and — if Task 1 selected option-b — the revised projection was re-presented and the remainder authorized before Task 3 ran
  </acceptance_criteria>
  <done>One real cell exists and has passed both the per-row gate checks and the whole-set refusal check, before any further compute is spent</done>
</task>

<task type="auto">
  <name>Task 3: The remaining 39 cells, 40/40 closure, and the committed evidence set</name>
  <files>benchmarks/tweeteval-stance/rows/, benchmarks/tweeteval-stance/selections/, benchmarks/tweeteval-stance/locks/, benchmarks/tweeteval-stance/run-manifest.json</files>
  <read_first>
    - scripts/run_bench_cells.sh (resume semantics, the single-writer lock, the evidence-vs-transient exit codes, and the vacuity floor that asserts the sweep covered the contracted count)
    - benchmarks/tweeteval-stance/run-manifest.json (post-pilot state)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (what `verify_run` will demand in 05-13 — this task pre-checks; it does not replace the gate)
  </read_first>
  <action>
    Run the driver for the SetFit method against the bench directory, the attested dataset directory
    and the pinned encoder checkout. It generates the 39 missing selection manifests, skips the
    pilot's existing one, skips the pilot CELL by digest, and runs the remaining 39 one process at
    a time.

    The pilot skip is a load-bearing observation, not a convenience: if the driver RE-RUNS the pilot
    cell instead of skipping it, the pilot's invocation and the driver's disagree about something —
    a flag, a path, a digest — and that is a finding to diagnose before the sweep continues, not a
    duplicate to shrug at. Record which of SKIP or a re-run happened.

    Watch the two known host hazards and report rather than push through: free disk against the
    projection, and wall-clock against Task 1's recorded number. If the sweep halts on an
    evidence-class refusal, report the cell and the refusal text and stop — the driver's exit code
    distinguishes an evidence failure from a transient one precisely so the operator knows whether
    resuming is safe, and re-running never fixes the former. If wall-clock exceeds the projection
    materially, that is a checkpoint back to the human, never a reason to parallelize or to thin
    the protocol.

    Then close the set. Confirm the run manifest records the full contracted count as complete with
    content hashes. Tally the peak-RSS mechanisms across ALL 40 rows rather than quoting one — the
    expected shape on this platform is a sampled train peak with its sample-interval field present
    in every row, and a child-measured inference peak in every row — and record both tallies with
    the row count each covers. A single quoted row would be an anecdote; a 40-row tally is a
    measurement, and it is what 05-13 has to render honestly.

    Run the dataset-text control over the committed evidence directories before committing: the
    rows, selection manifests and lock records carry identifiers, hashes and metrics only.
    Then commit the rows, selections, locks and manifest as the claims evidence set, with the cell
    count in the message. Do NOT run the full report here — that is 05-13's act over the complete,
    committed set.
  </action>
  <verify>
    <automated>ls benchmarks/tweeteval-stance/rows/setfit-*.json | wc -l | grep -q '^ *40$' && python3 -c "import json,sys; m=json.load(open('benchmarks/tweeteval-stance/run-manifest.json')); p=m.get('payload',m); cells=[e for e in (p.get('expectation') or p.get('cells') or []) if e.get('status')=='complete']; sys.exit(0 if len(cells)==40 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - 40 files `rows/setfit-s{8|16|32|64}-seed{13|17|23|29|31|37|41|43|47|53}.json` exist, covering the full contracted grid
    - The run manifest records exactly 40 complete cells with content hashes, and the driver's own end-of-sweep count assertion passed
    - The driver log shows per-cell PASS lines with their statuses captured on the command rather than through a pipe, a SKIP line for the pilot cell, and no parallel-dispatch construct anywhere in the invocation
    - The actual wall-clock is recorded beside Task 1's projection, with the ratio stated
    - The mechanism tally is recorded over all 40 rows: the count carrying a sampled train-peak mechanism with its sample-interval field present, and the count carrying a child-measured inference-peak mechanism. Both counts and their denominator are stated
    - The dataset-text control was run over `rows/`, `selections/` and `locks/` and returned no match; the exact command and its result are recorded
    - If any cell failed in the evidence class, the driver HALTED there and the SUMMARY reports the failing cell and its refusal text — no tally-and-continue, no hand-edited row
    - The evidence set is committed on the branch with the cell count in the message
  </acceptance_criteria>
  <done>The 40-cell SetFit evidence set is complete, mechanism-declared, text-free and committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| long-running local compute -> host stability | the recurring incremental-cache ENOSPC history on this host; stop-and-report discipline, never silent deletion |
| generated rows -> repository | committed evidence must never contain dataset text |
| wall-clock pressure -> measurement protocol | the one boundary where a schedule problem can silently become a correctness problem |
| a single cell -> the whole matrix | a per-row defect found late invalidates every cell already spent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12-01 | Denial of service | the 40-cell sweep | medium | mitigate | projection written down as a number and authorized at a blocking checkpoint before any cell; hash-based per-cell resume on interruption; disk headroom recorded |
| T-05-12-02 | Repudiation | contended measurements | high | mitigate | parallelism structurally impossible — no dispatch flag in the driver, single-writer pid lock on the bench directory; an overrun escalates as a checkpoint and the protocol is never thinned |
| T-05-12-03 | Information disclosure | dataset text in committed evidence | high | mitigate | the attested dataset directory lives outside the repository; a text control is run over rows, selections and locks before the commit |
| T-05-12-04 | Spoofing | backend identity | medium | mitigate | identity read from execution rather than echoed from config; quoted in the row excerpt |
| T-05-12-05 | Tampering | a late-discovered per-row gate defect | high | mitigate | the two-half pilot proof runs the real per-row door AND the real whole-set refusal before the remaining 39 are authorized |
| T-05-12-06 | Repudiation | a mechanism asymmetry averaged away | medium | mitigate | the train-vs-inference peak mechanisms are tallied over all 40 rows and declared per row, so 05-13 cannot render them as one figure without contradicting the evidence |
| T-05-12-07 | Tampering | a divergent pilot invocation | low | mitigate | the driver must SKIP the pilot cell by digest; a re-run is a finding to diagnose, recorded either way |
| T-05-12-SC | Tampering | package installs | low | accept (n/a) | no installs in this plan |
</threat_model>

<artifacts_this_phase_produces>
Files CREATED by this plan (newly-created — exclude from drift verification):

- `benchmarks/tweeteval-stance/rows/setfit-s{8|16|32|64}-seed{13|17|23|29|31|37|41|43|47|53}.json` (40 rows)
- `benchmarks/tweeteval-stance/selections/s{shots}-seed{seed}/selection-manifest.json` (40 selection manifests — the retained pairing key)
- `benchmarks/tweeteval-stance/locks/` (40 committed lock records whose bytes the gate re-hashes)
- `benchmarks/tweeteval-stance/run-manifest.json` closed at 40/40
- The attested TweetEval benchmark dataset directory, OUTSIDE the repository by design — no dataset text is ever committed

Deliberately NOT created: any second-method row, ledger or comparison artifact. Under the 40-cell
active scope such a row is a typed gate refusal.
</artifacts_this_phase_produces>

<review_notes>
Replan notes (2026-09-08, D-19 scoped replan):

- This plan absorbs the phase's cell generation entirely; it is now the only cell-generation plan.
  The `depends_on: 05-11` edge is RETAINED rather than dropped, because 05-11 was repurposed into
  the matrix retarget and this plan cannot correctly write a run manifest before that lands.
- The retired plan's pilot-cell proof is replaced by a stronger two-half proof, for a measured
  reason: `verify_run` refuses at the completeness step before it reads any row, so the
  copy-and-refuse procedure alone never touches the pilot row's bytes and cannot establish the
  per-row properties the retired plan's acceptance criteria claimed for it.
- Open question Q6 (macOS local versus a Linux CPU host) is moot in its original form — D-19
  established there is no second host. Its CONSEQUENCE is preserved and made explicit instead: the
  train-peak mechanism on this platform is a sampled lower bound while the inference peak is a
  kernel high-water mark, and that asymmetry is declared per row and tallied over all 40.
- The sequential-only constraint and the no-protocol-thinning constraint are carried forward
  verbatim from the previous revision; both were review-derived and neither is affected by D-19.
- The compute is NOT pre-authorized: 05-03's checkpoint recorded that the human deferred the
  question to wave 7. Task 1 asks.
</review_notes>

<verification>
- 40 rows; run manifest closed at 40/40 with content hashes; driver's own count assertion passed
- Both halves of the pilot proof recorded with their statuses and quoted output
- Mechanism tally over 40 rows, dataset-text control clean, evidence set committed
</verification>

<success_criteria>
- The phase's complete evidence set exists on this branch, generated sequentially under the contracted protocol from reloaded production artifacts, with its measurement mechanisms declared rather than averaged, and ready for the fail-closed report
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-12-SUMMARY.md` when done
</output>
