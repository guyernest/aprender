---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 11
type: execute
wave: 5
depends_on: ["05-06", "05-09"]
files_modified:
  - scripts/dispatch-bench-lora.sh
  - benchmarks/tweeteval-stance/rows/
  - benchmarks/tweeteval-stance/run-manifest.json
autonomous: false
requirements: [EVAL-02, EVAL-03, EVAL-05]

must_haves:
  truths:
    - "Host access and the qwen3.5-9b base-weight location + SHA-256 were confirmed at a human checkpoint BEFORE any cell ran (research open question A1 — the LoRA wave's hard precondition)"
    - "All 40 LoRA rows exist locally, each ingested via bench run --record with digest verification — transport never rewrites a row"
    - "Every LoRA row records its true executed backend/hardware identity (pipeline-read GPU name), the contracted seed, val_split 0.0, early stopping disabled, epochs_completed == epochs_requested, and the selection manifest hash it consumed"
  artifacts:
    - path: "scripts/dispatch-bench-lora.sh"
      provides: "Remote dispatch: same-SHA checkout, remote build, per-cell bench run, artifact return (dispatch-*.sh house pattern)"
    - path: "benchmarks/tweeteval-stance/run-manifest.json"
      provides: "40 lora cells recorded complete with content hashes"
  key_links:
    - from: "benchmarks/tweeteval-stance/rows/lora-*.json"
      to: "selections/*.json"
      via: "selection_manifest_hash recorded per row — the EVAL-02 pairing key both methods share"
    - from: "remote rows"
      to: "local manifest"
      via: "bench run --record (digest re-verified on ingest; bench report re-verifies again)"
---

<objective>
Execute the 40 LoRA cells (4 shots x 10 contracted seeds) on the lambda-vector GPU host (D-09,
pre-authorized compute), fronted by the environment checkpoint the orchestrator mandates: confirm
host access and record the base-weight hash BEFORE any cell runs.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call). Rows land on this branch
as committed evidence files.

Purpose: half of EVAL-02's 80-cell matrix, with as-deployed GPU resource metrics (EVAL-05) and
method-tagged rows (EVAL-03).
Output: 40 verified LoRA rows in the local benchmark directory + the dispatch script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 5; Open Questions 1 and 6; Environment Availability — lambda-vector UNVERIFIED)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (No Analog Found: GPU-host orchestration; Shared Patterns: digest-verify)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: lambda-vector environment checkpoint (A1) — access, base weights, host decisions</name>
  <files>none (probe + presentation)</files>
  <read_first>
    - scripts/dispatch-distill-phase-3-gx10.sh (header — the ssh/build/run/return house pattern; the RTX 4090 and /home/noah references)
    - .planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md (Open Q1: no ~/.ssh/config entry on this host; Open Q6: SetFit host choice rides this checkpoint)
  </read_first>
  <action>
    Automation FIRST, then ask: run every probe listed under what-built (ssh candidates, GPU/disk
    inventory, base-weight search + hash) before presenting; only the facts automation cannot
    obtain (credentials, weight locations unknown to the filesystem, the Q6 host choice, the
    compute go-ahead) go to the human. WAIT for the resume signal before Task 2 — no cell may run
    first (orchestrator constraint; research open question A1).
  </action>
  <what-built>
    Automation-first probe results, gathered BEFORE asking: (a) ssh reachability attempts against
    the host names/aliases found in scripts/dispatch-*.sh (run `grep -h 'ssh\|HOST' scripts/dispatch-*.sh`
    to enumerate candidates, then try `ssh -o BatchMode=yes -o ConnectTimeout=5 <candidate> true`,
    rc captured per attempt); (b) if reachable: `nvidia-smi -L`, disk free, whether a repo checkout
    exists and at what SHA, and a search for qwen3.5-9b base weights (locate + SHA-256 of the
    weights file if found — this hash goes into every LoRA row's base_model_sha256); (c) the
    projected 40-cell LoRA wall-clock (per-cell estimate from one dry-run if reachable).
  </what-built>
  <how-to-verify>
    1. If ssh failed on all candidates: provide the correct host alias/credentials (this is the A1 blocker — the plan cannot proceed without it).
    2. Confirm the base-weight file + hash (or state where to pull them and approve the pull).
    3. Decide Open Q6: run the SetFit 40 cells (plan 05-12) on this macOS host (sysinfo peak-RSS) or on lambda-vector CPU (uniform VmHWM)? Default recommendation: macOS local, mechanism declared per row.
    4. Approve the LoRA compute (pre-authorized per CLAUDE.md, but the projection is presented for the record).
  </how-to-verify>
  <acceptance_criteria>
    - Probe attempts with per-candidate rc values recorded in the SUMMARY (never "assumed reachable")
    - On approval: host alias, base-weight path + SHA-256, and the Q6 decision recorded verbatim
  </acceptance_criteria>
  <verify>
    <human-check>Human confirmed host access, base-weight identity + SHA-256, the Q6 SetFit-host decision, and the LoRA compute projection</human-check>
  </verify>
  <resume-signal>Provide host access details / confirm weights + hash / state the Q6 host choice, then "approved"</resume-signal>
  <done>Reachability + base-weight hash + Q6 decision recorded verbatim; approval received</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch the 40 LoRA cells and ingest the rows</name>
  <files>scripts/dispatch-bench-lora.sh, benchmarks/tweeteval-stance/rows/, benchmarks/tweeteval-stance/run-manifest.json</files>
  <read_first>
    - scripts/dispatch-distill-phase-3-gx10.sh (FULL — the remote build/run/return mechanics to copy)
    - scripts/run_bench_cells.sh (the 05-09 driver the remote side reuses with METHOD=lora)
    - crates/apr-cli/src/commands/setfit_bench.rs (--record semantics)
    - CLAUDE.md (Verification Discipline rules 1-3: rc discipline, prove-the-mechanism, pin the binary)
  </read_first>
  <action>
    Write `scripts/dispatch-bench-lora.sh` (bashrs-clean, `set -euo pipefail`): (1) push the
    current branch and record the SHA; (2) on lambda-vector via ssh: checkout the SAME SHA, build
    apr with the finetune features (CARGO_INCREMENTAL=0), verify `--version` non-empty (mechanism
    proof) — never a bare `apr`, always the freshly built path; (3) generate/reuse the attested
    dataset dir + the 40 selection manifests remotely via `apr data` (or rsync the local
    selections/ so both methods consume byte-identical manifests — PREFERRED: rsync local
    selections up, since the pairing key must be the same artifact; verify each manifest's
    semantic hash after transfer); (4) run the 40 lora cells via run_bench_cells.sh METHOD=lora
    (per-cell resume; every row records the pipeline-read GPU identity — spot-check one remote row
    JSON for a real gpu name string before continuing, Verification Discipline rule 2); (5) rsync
    `rows/lora-*.json` back; (6) locally ingest EVERY row via `"$APR" setfit bench run --record
    <row> --bench-dir benchmarks/tweeteval-stance` (digest re-verified per row; rc per ingest on
    its own line); (7) final tally: 40/40 recorded. Commit the rows + manifest (they are the
    claims evidence set; text-free — rows contain metrics and hashes, never tweet text; verify
    with a grep for any raw-text field before committing). If any cell fails: report the cell,
    keep the completed rows (resume handles the rest), do not fabricate.
  </action>
  <verify>
    <automated>ls benchmarks/tweeteval-stance/rows/lora-*.json | wc -l | grep -q '^ *40$' && bashrs lint scripts/dispatch-bench-lora.sh > /tmp/dl.log 2>&1; rc=$?; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 40 files matching rows/lora-s{8|16|32|64}-seed{13|17|23|29|31|37|41|43|47|53}.json exist locally, each ingested via --record (ingest log with 40 rc=0 lines)
    - run-manifest.json shows all 40 lora cells complete with hashes
    - One quoted row excerpt in the SUMMARY showing: a real pipeline-read GPU identity string, seed from the contracted set, val_split 0.0, early stopping disabled, epochs_completed == epochs_requested, base_model_sha256 equal to the checkpoint-recorded hash
    - `grep -l 'text"' benchmarks/tweeteval-stance/rows/` returns nothing (no tweet text committed)
  </acceptance_criteria>
  <done>40 LoRA rows locally verified and recorded; dispatch script committed and lint-clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote host -> local benchmark dir | rows cross hosts as files; self-verifying envelopes re-checked on ingest and again at report time |
| ssh/remote build -> measurements | a wrong-SHA or wrong-binary remote run produces confident numbers about code we are not running |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Spoofing (wrong code) | remote build | mitigate | same-SHA checkout asserted remotely; --version mechanism proof; SHA recorded in the SUMMARY |
| T-05-11-02 | Tampering | rows in transport | mitigate | digest verified by --record on ingest and by verify_run at report time; rows never rewritten in transit |
| T-05-11-03 | Spoofing | GPU identity | mitigate | pipeline-read gpu name spot-checked in a real row before the batch continues (rule 2) |
| T-05-11-04 | Information disclosure | tweet text in committed rows | mitigate | rows carry metrics/hashes only; pre-commit grep control |
| T-05-11-05 | Tampering | pairing key divergence | mitigate | local selections rsynced up and hash-verified so both methods consume byte-identical manifests |
| T-05-11-SC | Tampering | package installs | accept (n/a) | remote build uses the repo's locked workspace; no new packages |
</threat_model>

<verification>
- 40/40 lora rows ingested with rc=0 each; manifest complete for the lora half; bashrs lint clean
</verification>

<success_criteria>
- A1 resolved at a checkpoint with recorded evidence; 40 as-deployed GPU rows, honestly identified, locally verified
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md` when done
</output>
