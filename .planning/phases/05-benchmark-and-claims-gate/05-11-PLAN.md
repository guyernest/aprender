---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 11
type: execute
# WAVE AND DEPENDENCIES DELIBERATELY UNCHANGED. This plan number was the 40 LoRA GPU cells;
# D-19 (2026-09-07) proved that arm unbuildable and it is retired below. The number is
# REPURPOSED rather than tombstoned-and-vacated, because the descope creates real work that
# must land in exactly this slot: the declared matrix has to shrink from 80 cells to 40 BEFORE
# 05-12 generates a single cell, since the first `bench run` writes a run-manifest derived from
# `RunManifest::expectation()`. Repurposing keeps 05-12 -> 05-11 and 05-13 -> 05-11 pointing at
# a plan that produces something they genuinely need, so the phase's dependency graph is not
# renumbered and no plan is left depending on an artifact nothing produces.
wave: 6
depends_on: ["05-06", "05-09", "05-10"]
files_modified:
  - contracts/setfit-benchmark-claims-v1.yaml
  - crates/aprender-train/src/train/setfit/bench_row.rs
  - crates/aprender-train/src/train/setfit/bench_row_tests.rs
  - crates/aprender-train/src/train/setfit/bench_gate.rs
  - crates/aprender-train/src/train/setfit/bench_gate_tests.rs
  - crates/aprender-core/src/stats/hypothesis.rs
  - crates/apr-cli/src/commands/setfit_bench.rs
  - crates/apr-cli/src/commands/setfit_bench_tests.rs
  - crates/apr-cli/src/setfit_commands.rs
  - Makefile
autonomous: false
# EVAL-02: the declared matrix and the retained pairing mechanism (amended scope — see
# <amended_requirements> for exactly which clause each id is and is not satisfied by here).
# EVAL-04: the expectation set, the fail-closed rule, and single-method uncertainty.
# EVAL-05: the resource presentation, whose cross-method comparison section is removed and
# whose within-row mechanism asymmetry must now be surfaced instead.
requirements: [EVAL-02, EVAL-04, EVAL-05]

must_haves:
  truths:
    - "The declared expectation set is exactly 40 SetFit cells, derived CLOSED-FORM from the claims contract alone (methods x shots x seeds with methods = [setfit]) and still cross-pinned to the Rust derivation by the include_str! test, so the 40 is a consequence of the contract rather than a literal typed into code"
    - "The narrowing was APPROVED at a blocking human checkpoint against a pv diff of two filesystem paths, with the suggested semver bump recorded. The claims contract itself names this exact threat — a producer declaring a smaller expectation set, satisfying it completely and publishing a complete run — so an executor narrowing it autonomously would be that threat executed by us"
    - "All SIX of 05-10's doctored negatives still run in a default `cargo test -p aprender-train --lib --features setfit`, with their doctored shapes and asserted variant tags unchanged; the ONLY permitted change to them is the expectation scope they are constructed under"
    - "The FOUR doctored shapes that also exist in the 40-cell active scope (missing cell, trimmed row, bit-flipped row bytes, post-test-selection) are RE-MUTATED under the active scope and refuse there too, with the same variant tags. CLAUDE.md Verification Discipline rule 4: extending a guard's SCOPE requires re-mutating in the new scope — the 80-cell proof does not transfer to a 40-cell expectation"
    - "The two doctored shapes that exist only in the two-method design (unpaired selection hash; forged provenance — a ledger carrying two candidate lines against a row claiming one, plus an edited lock file) run under the RETAINED deferred scope, which production code cannot construct"
    - "A row whose cell lies outside the active expectation set is a typed refusal that names the out-of-scope cell BEFORE any arithmetic. This is the structural form of the D-19 prohibition: a report cannot imply a second method's result if a second method's row cannot enter a verified set at all"
    - "The rendered report contains no cross-method section and no comparative claim language, proven by a must-match / must-not-match case table whose must-not-match rows are the ACTUAL strings the two-method renderer emitted, not invented near-misses"
    - "Uncertainty survives the descope: per shot level a closed-form 95% interval over the ten contracted seeds using the frozen t_{0.975,9}, computed through ONE definition of the moments (OPS-03 — no second mean/std), pinned against a scipy reference case in the pinned uv env, and labelled as a seed-dispersion interval rather than as a comparison"
    - "The within-row peak-RSS mechanism asymmetry is surfaced at the point of presentation: the TRAIN peak and the INFERENCE peak never share a column, and a sysinfo_sampled figure carries its lower-bound label wherever it is rendered"
    - "A single-cell verification door exists that runs the gate's REAL per-row checks (row digest, schema, manifest-hash agreement, slot agreement, recomputed lock digest, lock role/rule, resource and size fields) against one cell on disk, sharing one implementation with verify_run's row loop, and emitting no statistic of any kind"
    - "The deferred two-method design is RETAINED in the contract — pairing rule, paired-delta statistics, cross-method size and mechanism comparability rules — marked deferred and unexercised with a pointer to D-ITEM-05-15, so the arm can be restored rather than re-derived"
    - "verify_run keeps its public two-argument signature and its refusal ORDER; the CLI cannot construct the deferred scope"
    - "Every Makefile assert_tests_ran floor this plan touches is RE-MEASURED against the shipped suite and RAISED to the new count"
  prohibitions:
    - "No assert_tests_ran floor may be lowered. A floor that drops to match a shrunken suite is how a weakened gate reports green, and this phase's own CR-02 lesson is that a gate which ran nothing exits 0 while printing success."
    - "The narrowing must not DELETE a single equation, proof obligation or falsification test describing the two-method design. Deletion would destroy the record that makes D-ITEM-05-15 restorable, and it would turn a scope amendment into a quiet loss of contracted guarantees."
    - "No executor may narrow the expectation set without the Task 1 approval, and no fallback, retry or convenience path may reduce it further. The contract's own words are that a producer could declare a twelve-cell expectation, satisfy it completely, and publish a `complete` run."
    - "No script named for remote LoRA dispatch is created, and no plan artifact may reference one. The retired arm leaves no half-built machinery behind that a later reader could mistake for a capability."
    - "The single-cell verification door must never emit an aggregate, a mean, a dispersion or an interval. The report is the only door that emits numbers; a per-cell door that printed statistics would be a partial-data report under another name, which the contract forbids."
  artifacts:
    - path: "contracts/setfit-benchmark-claims-v1.yaml"
      provides: "The 40-cell active expectation set + the retained, deferred two-method design + the single-method statistics rules"
    - path: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      provides: "verify_run over the 40-cell expectation; the factored per-row verification shared with the single-cell door"
      contains: "verify_run"
    - path: "crates/apr-cli/src/commands/setfit_bench.rs"
      provides: "Single-method report rendering (no comparison sections) + the single-cell verification subcommand"
    - path: ".planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md"
      provides: "The tombstone of record for the retired LoRA arm, and the retarget evidence 05-12/05-13 read"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/bench_row.rs"
      to: "contracts/setfit-benchmark-claims-v1.yaml"
      via: "include_str! cross-pin — the Rust expectation set equals the contract's as a SET and in CARDINALITY (40), and a mutated contract copy turns it red"
      pattern: "EXPECTED_CELLS"
    - from: "crates/apr-cli/src/commands/setfit_bench.rs"
      to: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      via: "the single-cell door calls the SAME per-row verification verify_run's loop calls (OPS-03: one implementation per operation)"
---

<objective>
Retarget the phase's declared matrix from 80 comparison cells to 40 SetFit cells, under D-19, and
retire the LoRA dispatch arm this plan number used to carry.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: 05-12 cannot generate a single cell until the expectation set is 40, because the first
`apr setfit bench run` writes a run manifest derived from `RunManifest::expectation()`. And the
gate that judges those cells must be re-proven AT the new scope before they are spent — the phase
already ruled (review consensus item 4) that a defect discovered in the gate after an expensive
matrix has run would invalidate the whole matrix.

Output: the amended claims contract, the retargeted gate and renderer, the six preserved negatives
plus two new ones, the single-cell verification door, and a written tombstone for the retired arm.
</objective>

<retired_work>
## What this plan number used to be, and why it is not that any more

This plan was "lambda-vector checkpoint (A1) then the 40 LoRA GPU cells". It is retired by
`05-CONTEXT.md` D-19, recorded 2026-09-07 at this plan's own Task 1 checkpoint, before any compute
was spent. No LoRA cell was ever run. Two independent causes, either fatal alone:

- **(a) No GPU host.** `lambda-vector`/`gx10`, hardcoded by every `scripts/dispatch-*.sh`, is
  unreachable: 12 enumerated candidates, 12 failures, rc captured per attempt. The AWS fallback was
  enumerated and refuted rather than assumed — five instances, all stopped, none a GPU instance.
- **(b) The architecture is unimplemented — the decisive cause.** Qwen3.5-9B declares hybrid
  `layer_types` (24 linear + 8 full attention), `attn_output_gate`, and a multimodal
  `text_config`-nested checkpoint. `TransformerConfig` has no field for any of the three. The only
  hybrid-forward artifact sits in `aprender-contracts-staging`, which has no `Cargo.toml` and never
  compiles. Cause (b) is an inference from STRUCTURAL ABSENCE, not an observed loader failure
  (Verification Discipline rule 6); the cheap falsifier — point the loader at the real `config.json`
  — was named and NOT run.

**What is NOT retired.** The pairing MECHANISM is the whole point of keeping it: every SetFit cell
still records the selection-manifest hash that would bind a second method to an identical
sampled-ID set, so the arm can be added later without re-running the SetFit half
(`D-ITEM-05-15`). 05-06's `--selection-manifest` door, `run_classify_core`, `load_adapter`,
`predict_proba_tokenized` and the candidate-ledger single-candidate guarantee all remain shipped
code with shipped tests; what they lack is a real 9B checkpoint to be exercised against. 05-04's
paired-t machinery is likewise retained and contract-bound, unexercised by this phase's report.

**What does NOT get built.** `scripts/dispatch-bench-lora.sh` is not written. Nothing in this phase
may reference it.

**The selection manifests are not orphaned.** The retired plan claimed ownership of generating the
40 selection manifests. That claim was already stale: `scripts/run_bench_cells.sh` — shipped by
05-09 — generates them itself in `generate_selections()`, into
`$BENCH_DIR/selections/s{shots}-seed{seed}/selection-manifest.json`, skipping any that already
exist so that one file feeds every consumer. Measured, not assumed: read the function and the
`run_one_cell` path expression that consumes it. The capability therefore survives the retirement
untouched, and 05-12 inherits it by invoking the driver.
</retired_work>

<amended_requirements>
## What this plan delivers under the AMENDED requirement text — and what it does not

Recorded here rather than left implicit, because a narrower deliverable silently filed against an
unamended requirement is exactly the false-completion defect this project has hit six times.

| Id | Amended text this plan serves | Delivered here | NOT delivered, and why |
|----|-------------------------------|----------------|------------------------|
| EVAL-02 | "...for SetFit, each cell bound to a recorded selection manifest so that any future second method can be paired against it" | The declared matrix becomes the 40 SetFit cells; the pairing key and the pairing RULE are retained in contract and code | The second arm. Deferred as `D-ITEM-05-15`, not cancelled |
| EVAL-04 | "...recompute headline means, dispersion, and uncertainty exactly from all 40 stored SetFit cells, and any missing, selectively omitted, unmatched, or post-test-selected cell invalidates the report" | The 40-cell expectation set; the fail-closed rule UNWEAKENED; single-method uncertainty as a closed-form seed-dispersion interval on the frozen t constant | The paired SetFit-versus-LoRA delta clause. The machinery stays; this phase does not exercise it |
| EVAL-05 | "...compare training time, latency, throughput, peak memory, artifact size, calibration and quality from the reloaded production artifacts" | Comparison across shot levels and seeds within SetFit, with per-figure mechanism labels and the train-vs-inference peak asymmetry surfaced | Cross-METHOD resource comparison. The renderer's comparison section is removed, not repurposed |

Neither EVAL-02 nor EVAL-04 is satisfied by this plan. Both remain Pending, amended in scope.
</amended_requirements>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/deferred-items.md  (D-ITEM-05-15 — the restoration ticket this narrowing must stay compatible with)
@.planning/REQUIREMENTS.md  (the "Phase 5 in-flight amendment" table — items 1, 5 and 6 are this plan's brief)
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (read the reconciliation banner FIRST; every LoRA/lambda-vector claim below it is superseded)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the prepared narrowing of the claims contract</name>
  <files>none (preparation + presentation; nothing is committed in this task)</files>
  <read_first>
    - contracts/setfit-benchmark-claims-v1.yaml (FULL — `equations.expectation_set`, `completeness_rule`, `pairing_rule`, `claims_statistics`, `model_size_comparability`, `resource_protocol`, and every proof obligation and falsification test that names a cell count)
    - .planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md (D-19 in full, including its stated confidence limits and the falsifier that was not run)
    - .planning/phases/05-benchmark-and-claims-gate/deferred-items.md (D-ITEM-05-15 — the restoration path the retained half must serve)
    - .planning/phases/05-benchmark-and-claims-gate/05-03-PLAN.md (Task 2 — the house form of a blocking contract-edit checkpoint: prepare, pv diff two filesystem paths, present, wait)
  </read_first>
  <decision>
    Whether to commit the prepared narrowing of `setfit-benchmark-claims-v1` from an 80-cell
    two-method expectation set to a 40-cell SetFit-only active set, with the two-method design
    retained as an explicitly deferred scope; and at which semver bump.
  </decision>
  <context>
    D-19 has already ruled the LoRA arm out of this phase, and ROADMAP.md and REQUIREMENTS.md carry
    that amendment. This checkpoint is NOT re-litigating the descope. It exists because narrowing
    the CONTRACT is a different act from narrowing the requirement, and it is the one edit the
    claims contract explicitly warns about: its own `completeness_rule` says a producer could
    declare a smaller expectation set, satisfy it completely, and publish a "complete" run. An
    executor performing that edit unsupervised is that adversary. The ceremony is the same one D-04
    established for the lifecycle contract and 05-03 executed: prepare everything, show the diff of
    two real files, wait.

    The substantive question for the human is not "may we descope" but "does this diff NARROW the
    declared matrix while RETAINING the two-method design, or does it delete it". Deleting the
    pairing rule, the paired-delta statistics or the cross-method comparability rules would make
    D-ITEM-05-15 a re-derivation instead of a restoration, and would quietly drop contracted
    guarantees under cover of a scope amendment.

    Prepare BEFORE asking. Materialize the current revision with `git show` into a temp path, write
    the amended file, run `pv validate` on the amended file and `pv diff` on the two filesystem
    paths (never a git revision — `pv diff` opens both arguments as files), and present the
    suggested version bump. Also present a RETAINED-vs-NARROWED inventory: every equation,
    obligation and falsification test that mentions a cell count or a second method, each marked
    narrowed, retained-as-deferred, or unchanged, with nothing unaccounted for.
  </context>
  <options>
    <option id="option-a">
      <name>Commit the prepared narrowing: active scope = 40 SetFit cells; the two-method design retained as an explicitly deferred scope pointing at D-ITEM-05-15</name>
      <pros>05-12 can write a run manifest that means what it says. The fail-closed rule, the six doctored negatives and the invalidate-on-omission behaviour are untouched; only the declared matrix shrinks. The restoration ticket inherits an intact two-method specification. The phase's thesis — refuse incomplete or unequal claims — is applied to ourselves rather than waived.</pros>
      <cons>The contract, which is the anti-omission mechanism, is edited by the party it constrains. That is why this checkpoint exists and why the RETAINED-vs-NARROWED inventory is presented rather than summarized.</cons>
    </option>
    <option id="option-b">
      <name>Commit the narrowing, but DELETE the two-method equations rather than deferring them</name>
      <pros>A smaller contract with no unexercised clauses; nothing in the file describes something the code does not run.</pros>
      <cons>Rejected as the recommendation. D-ITEM-05-15 would become a re-derivation of the pairing rule, the paired-delta statistics and the cross-method comparability rules from scratch, and the deletion of contracted guarantees would be indistinguishable in the history from a scope amendment. The retained clauses are also what keeps two of the six doctored negatives meaningful.</cons>
    </option>
    <option id="option-c">
      <name>Run the D-19(b) falsifier first: point aprender's loader at the real Qwen3.5-9B config.json and record whether it accepts or rejects</name>
      <pros>D-19(b) rests on structural absence, not on an observed failure — Verification Discipline rule 6. The falsifier needs no GPU and costs minutes. If the loader accepts the checkpoint, (b) is refuted and the arm reduces to a host-access question.</pros>
      <cons>Does not change this phase's outcome: cause (a) is measured and stands alone, so even a refuted (b) leaves no host to run 40 GPU cells on. Worth doing for the ticket's sake; it is not a reason to hold the contract edit.</cons>
    </option>
    <option id="option-d">
      <name>HALT — leave the contract at 80 and stop the phase here</name>
      <pros>Nothing is narrowed. The phase ends with a claims gate that is shipped and green but with no evidence set, and the record says exactly why.</pros>
      <cons>The 40 SetFit cells are buildable today and EVAL-01/03/05 are demonstrable with them; halting forfeits real, honest evidence to avoid an edit whose sole purpose is to stop the manifest from claiming cells that cannot exist. 05-12 and 05-13 do not run.</cons>
    </option>
  </options>
  <how-to-verify>
    1. Read the RETAINED-vs-NARROWED inventory line by line. Every equation, obligation and
       falsification test naming a cell count or a second method must appear exactly once, marked
       narrowed / retained-as-deferred / unchanged. An unaccounted item is a stop.
    2. Confirm the ACTIVE expectation set is still derivable CLOSED-FORM from the file alone —
       `methods x shots x seeds` with an enumerated one-element method list — and that 40 is a
       product, not a literal. A hand-written 40 beside an unchanged product would be the same
       defect in the other direction.
    3. Confirm the pairing rule, the paired-delta statistics, and the cross-method size and
       mechanism comparability rules are PRESENT and marked deferred, with the D-ITEM-05-15
       pointer. Absence here selects option-b by accident.
    4. Confirm the prohibition is contracted, not merely planned: no report produced under the
       active scope may state or imply a SetFit-versus-LoRA result, and a row outside the active
       expectation set is a refusal.
    5. Read the `pv diff` output and its suggested version bump. A narrowing of a declared
       guarantee should not land as a patch bump; if the tool suggests one, say so rather than
       accepting it.
    6. Confirm `pv validate` passes on the amended file and that the byte-level control holds:
       the diff touches the expectation/scope/statistics regions and the metadata header, and
       nothing else.
    7. Note that approving here authorizes NO compute. 05-12's 40-cell SetFit run is separately
       NOT pre-authorized — 05-03's checkpoint deferred that question to wave 7 by explicit human
       instruction, and 05-12 asks for it on its own.
  </how-to-verify>
  <acceptance_criteria>
    - The selected option is recorded verbatim in the SUMMARY with the human's rationale as given
    - The RETAINED-vs-NARROWED inventory is reproduced in the SUMMARY in full, not summarized
    - The `pv diff` invocation, its output and its suggested bump are quoted, with rc captured on its own line
    - If a different option is selected, the executor re-prepares and RE-PRESENTS the full bundle before anything is committed; the re-presentation is recorded too
    - If option-c is selected, the falsifier's actual result is recorded — accepted or rejected, with the invocation — and D-ITEM-05-15 is amended by a follow-up commit rather than rewritten in place
    - If option-d is selected the SUMMARY says so plainly and names 05-12 and 05-13 as blocked; a halt is reported, never dressed as partial success
  </acceptance_criteria>
  <verify>
    <human-check>Human reviewed the RETAINED-vs-NARROWED inventory, the closed-form derivation of 40, the retention of the deferred two-method clauses, the pv diff and its suggested bump, and SELECTED an option</human-check>
  </verify>
  <resume-signal>Select: option-a, option-b, option-c or option-d — or describe required changes (the executor revises and re-presents; it never commits a contract change without approval)</resume-signal>
  <done>An explicit option selection recorded verbatim, with any ordered revisions executed and re-approved before the contract is committed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget the contract and the gate to the 40-cell active scope, preserving every negative</name>
  <files>contracts/setfit-benchmark-claims-v1.yaml, crates/aprender-train/src/train/setfit/bench_row.rs, crates/aprender-train/src/train/setfit/bench_row_tests.rs, crates/aprender-train/src/train/setfit/bench_gate.rs, crates/aprender-train/src/train/setfit/bench_gate_tests.rs, crates/aprender-core/src/stats/hypothesis.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/bench_row.rs (the `BENCH_METHODS` / `BENCH_SHOTS` / `BENCH_SEEDS` / `EXPECTED_CELLS` block and `RunManifest::expectation()` — `EXPECTED_CELLS` is already a PRODUCT of the three list lengths, so the arithmetic follows the method list automatically)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (`verify_run`'s numbered refusal order; the row loop; `verify_pairing`; `verify_lora_attestation`; `aggregate`; `shot_delta`; the `BenchGateError` variant-tag table)
    - crates/aprender-train/src/train/setfit/bench_gate_tests.rs (the six doctored negatives and the synthetic run builder they mutate; `TARGET_LORA` and the two-method-only shapes)
    - crates/aprender-core/src/stats/hypothesis.rs (`moments_or_zero_variance` — the ONE private moments helper; `paired_ci`, `paired_ci95_df9`, `ttest_1samp_f64`, `T_CRIT_975_DF9`, `PAIRED_DESIGN_N`)
    - scripts/setfit_fixtures/gen_claims_fixtures.py (the pinned uv fixture generator and its SHA-256 manifest — the house route for a scipy reference case)
  </read_first>
  <precondition>Task 1 recorded an explicit approval. If the recorded selection is option-d, this task does not run.</precondition>
  <behavior>
    - The contract's active expectation set enumerates ONE method and yields 40 cells as a product; the deferred two-method scope is present, marked deferred, and yields 80.
    - `RunManifest::expectation()` returns exactly 40 unique cell keys in the contract order (shots ascending, then seed ascending), and the include_str! cross-pin asserts SET equality AND cardinality equality against the contract's active list. Two in-test MUTATED copies of the contract string — one adding a seed, one adding a method — each turn the pin red, matching the existing mutation-control style.
    - `verify_run(manifest, rows_dir)` keeps its two-argument signature, its refusal ORDER, and every variant tag. It verifies against the ACTIVE scope.
    - A row whose cell is not in the active expectation set is refused by a typed variant that NAMES the offending cell, before any arithmetic. A verified set therefore cannot contain a second method's row.
    - The per-row verification inside `verify_run`'s loop is factored into one function that takes the manifest, the rows directory and one cell key, and performs exactly the checks the loop performed: row file present, row digest, schema, manifest-hash agreement, slot agreement, recomputed lock digest against the committed lock bytes, lock role/rule, and the resource and size field checks. `verify_run` calls it; nothing duplicates it (OPS-03).
    - `aggregate` over an active-scope verified set emits per-shot single-method statistics — mean, (n-1) std, min, max over exactly ten seeds — plus a 95% interval over those ten seeds computed with `T_CRIT_975_DF9`, and emits NO delta section.
    - The seed-dispersion interval reuses the existing moments path: either `paired_ci95_df9` against a zero comparator, documented as the one-sample specialisation, or a thin `ci95_one_sample_df9` that delegates through `paired_ci`/`moments_or_zero_variance`. Either way there is no second definition of the mean or the (n-1) std, and a zero-variance seed set produces the typed zero-variance shape with an explicit no-interval reason — never a NaN, never a serde null that reads as a missing measurement.
    - A scipy reference case in the pinned uv env pins the interval's endpoints; the fixture manifest's SHA-256 covers it.
    - The SIX doctored negatives keep their shapes and variant tags. Four of them — missing cell, trimmed row, bit-flipped row bytes, post-test-selection — are ALSO built and asserted under the active 40-cell scope. The two two-method-only shapes — unpaired selection hash, forged provenance — run under the retained deferred scope, which production code cannot construct.
    - Aggregation over an active-scope set is bit-identical across two invocations (f64 bits compared), as before.
  </behavior>
  <action>
    Commit the approved contract text first, as its own commit, so the diff the human approved is
    in history unmixed with code. Then retarget the code.

    Represent the two expectation scopes explicitly rather than by deleting the second method from
    the shared method list: the `Method` enum keeps both variants (05-09's `--record` transport and
    its shipped tests depend on the second variant being representable), and the ACTIVE method list
    that `EXPECTED_CELLS` and `expectation()` derive from names one method. Give the deferred
    two-method scope a constructor that production code cannot reach — the recommended siting is a
    test-only constructor beside the active one — so a future caller cannot widen the expectation
    set by passing an argument. The public `verify_run` delegates to the scoped form with the
    active scope, so the CLI is untouched.

    Preserve the negatives by re-siting them, never by rewriting them: the synthetic run builder
    takes the scope it is building for, the four active-scope shapes are added as new cases beside
    the existing ones rather than replacing them, and every case keeps asserting its variant tag.
    Re-mutating the four in the new scope is the point — the existing proof was taken against an
    80-cell expectation and does not transfer.

    Where `bench_gate.rs` module prose, error text or doc comments state a cell count or describe a
    two-method comparison, update them to describe what the code now does, and state the residual
    honestly in the module doc: the gate proves detection of INCONSISTENT evidence, not of truthful
    provenance, and the active scope now also means the gate cannot be handed a second method's row
    at all. Do not overstate what the narrowing buys.

    Leave the deferred-scope code paths in place and reachable from the deferred-scope tests:
    pairing verification, the LoRA attestation checks and the paired-delta computation are retained
    machinery, contract-bound and unexercised, exactly as 05-04's paired-t is.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit bench_gate > /tmp/p11t2-gate.log 2>&1; rc=$?; tail -5 /tmp/p11t2-gate.log; test $rc -eq 0 && CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib stats:: > /tmp/p11t2-stats.log 2>&1; rc2=$?; tail -5 /tmp/p11t2-stats.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `target/release/pv validate contracts/setfit-benchmark-claims-v1.yaml` exits 0 with rc captured on its own line, and the contract commit is separate from the code commit
    - The contract's active expectation set yields 40 as a PRODUCT of an enumerated one-method list; the deferred scope yields 80 and is marked deferred with the D-ITEM-05-15 pointer
    - Ten negative/mutation assertions are green and each names its variant tag: the six original doctored shapes, the four active-scope re-mutations (which reuse four of those six tags), and the out-of-scope-row refusal with its own tag. The count of DISTINCT tags asserted is recorded in the SUMMARY
    - The two contract-mutation controls (an added seed; an added method) each turn the include_str! pin red, and both are reverted — the RED transcript is quoted
    - `grep -n 'fn verify_run' crates/aprender-train/src/train/setfit/bench_gate.rs` still shows a two-argument public signature
    - The per-row verification exists as ONE function called by both `verify_run`'s loop and (in Task 3) the single-cell door; the SUMMARY names it and shows the two call sites
    - The seed-dispersion interval's scipy reference case is listed in the fixture manifest and `shasum -c` on the manifest exits 0
    - No equation, obligation or falsification test describing the two-method design was deleted (`pv diff` of the pre- and post-edit files quoted, with additions and retentions distinguished from removals)
  </acceptance_criteria>
  <done>The gate verifies a 40-cell SetFit expectation, refuses an out-of-scope row, still refuses all six doctored shapes, and carries single-method uncertainty on the frozen t constant</done>
</task>

<task type="auto">
  <name>Task 3: Single-method presentation, the single-cell verification door, and re-measured gate floors</name>
  <files>crates/apr-cli/src/commands/setfit_bench.rs, crates/apr-cli/src/commands/setfit_bench_tests.rs, crates/apr-cli/src/setfit_commands.rs, Makefile</files>
  <read_first>
    - crates/apr-cli/src/commands/setfit_bench.rs (`render_header`, `render_quality`, `render_deltas`, `render_resource_detail`, `render_resource_comparison`, `comparison_row`, `render_sizes`, `render_human` — and the constants they emit, which are the source of the must-not-match rows)
    - crates/apr-cli/src/setfit_commands.rs (the `Bench` subcommand shape 05-09 established, and the clap conflict style)
    - Makefile (the `setfit-bench-tests` target and its four `assert_tests_ran` floors — the row, gate, metrics and CLI logs)
    - crates/apr-cli/src/commands/setfit_bench_tests.rs (the guard-regex case-table style: must-match AND must-not-match, where the must-not-match rows are neighbouring real output rather than invented text)
  </read_first>
  <action>
    Make the renderer scope-aware rather than defensive. Under the active scope the human report
    presents, per shot level: the official F_avg and the per-class metrics as mean, sample (n-1)
    std, min and max over the ten contracted seeds, plus the seed-dispersion 95% interval from
    Task 2 — labelled at the point of presentation as an interval over the ten contracted seeds at
    fixed data and protocol, not a population interval and not a comparison. The cross-method
    sections are REMOVED under this scope, not emitted empty: a section header with no rows still
    tells a reader a comparison was attempted, which is the implication D-19 forbids.

    Add one short rendered note stating plainly that a single method was measured, that no second
    method was run, and where the reason lives (D-19 and D-ITEM-05-15), so a reader cannot mistake
    absence for parity. It is one paragraph of prose, not a results claim.

    Surface the peak-RSS asymmetry at the point of presentation. Under the contract the TRAIN peak
    and the INFERENCE peak are two fields with two mechanism strings, and a sampled figure is a
    lower bound that is not comparable to a kernel high-water mark. On the host this phase runs on,
    those two fields will carry different mechanism CLASSES within the SAME row. So the two peaks
    must never share a column or a single "peak memory" figure, each must print its mechanism
    string beside its number, and a sampled figure must carry its lower-bound label wherever it
    appears — including in the per-shot summary, not only in a methods paragraph.

    Add the single-cell verification door as a `bench` subcommand taking a bench directory and one
    cell key. It calls the per-row verification function Task 2 factored out — the same one
    `verify_run`'s loop calls — and prints the refusal or a pass line. It emits NO statistic: no
    mean, no dispersion, no interval, no aggregate. It is a diagnostic door, and 05-12's pilot-cell
    proof is its first consumer. Give it a clap conflict with anything report-shaped so it cannot
    be mistaken for a partial report.

    Then re-measure every `assert_tests_ran` floor this plan's suites touch, by reading the actual
    counts out of the fresh logs, and RAISE each to the measured value. Do not lower any floor. If
    a floor would have to fall for the gate to pass, that is a finding about the suite, not a
    number to edit.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit setfit_bench > /tmp/p11t3-cli.log 2>&1; rc=$?; tail -5 /tmp/p11t3-cli.log; test $rc -eq 0 && make setfit-bench-tests > /tmp/p11t3-make.log 2>&1; rc2=$?; tail -8 /tmp/p11t3-make.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - A must-match / must-not-match case table pins the rendered active-scope report. The MUST-NOT-MATCH rows are the ACTUAL literals the two-method renderer emitted — the delta-table header, the comparison-section header, and the per-row comparison format — copied out of the pre-change source, not invented near-misses. The table's provenance (the source lines the literals came from) is recorded in the SUMMARY
    - The rendered active-scope report contains no delta section and no cross-method section, and still contains the estimation-first note and the seed-dispersion interval label
    - A test asserts the two peak-RSS figures are rendered as separate labelled figures with their mechanism strings, and that a sampled mechanism renders its lower-bound label
    - The single-cell door exists, calls the shared per-row verification, and a test asserts its output matches none of the statistic literals the report emits (mean, std, interval, the delta and comparison headers)
    - Every `assert_tests_ran` floor touched is quoted BEFORE and AFTER with the measured count that justifies the new value; no floor decreased (`git diff Makefile` inspected for decreases and the result stated)
    - `make setfit-bench-tests` and `make contract-audit-phase5` each exit 0 with rc captured on its own line, and the audit shows zero BIND- lines
    - An induced-red control: one floor is temporarily raised above the measured count, `make setfit-bench-tests` is shown to FAIL, and the change is reverted. A floor that cannot fail is not a floor
  </acceptance_criteria>
  <done>The report presents single-method statistics without implying a comparison, the mechanism asymmetry is visible, the per-cell door exists, and the suite floors are re-measured and provably able to fail</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this plan -> the anti-omission contract | the party the contract constrains is the party editing it; the checkpoint is the control |
| gate scope -> published claims | a narrowed expectation set is indistinguishable from a complete one to a reader who does not read the contract |
| retained deferred machinery -> production callers | code that describes a two-method comparison must be unreachable from any shipped door |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11-01 | Elevation of privilege | the expectation-set edit | critical | mitigate | blocking human checkpoint with a RETAINED-vs-NARROWED inventory and a `pv diff` of two filesystem paths; the contract's own text names this adversary |
| T-05-11-02 | Tampering | gate strength during the retarget | high | mitigate | all six doctored shapes preserved with their variant tags; the four that exist in the new scope RE-MUTATED there (Verification Discipline rule 4) |
| T-05-11-03 | Information disclosure | a report implying a comparison never run | high | mitigate | an out-of-scope row is a typed refusal; the comparison sections are removed rather than emitted empty; a rendered case table whose must-not-match rows are the real prior literals |
| T-05-11-04 | Repudiation | deleted two-method guarantees passed off as a scope amendment | medium | mitigate | deletion prohibited; the inventory accounts for every clause; `pv diff` distinguishes removals from retentions |
| T-05-11-05 | Spoofing | a green gate over a shrunken suite | high | mitigate | floors re-measured and raised, never lowered; an induced-red control proves a floor can still fail |
| T-05-11-06 | Information disclosure | the single-cell door read as a partial report | medium | mitigate | the door emits no statistic; clap conflicts with report-shaped flags; a test asserts its output matches none of the report's statistic literals |
| T-05-11-07 | Tampering | a future caller widening the expectation set | medium | mitigate | the deferred scope has no production constructor; `verify_run` keeps a two-argument signature that admits no scope argument |
| T-05-11-SC | Tampering | package installs | low | accept (n/a) | no new registry package; the scipy reference case uses the existing pinned uv environment and its locked resolution |
</threat_model>

<artifacts_this_phase_produces>
Files and symbols CREATED or newly-scoped by this plan (newly-created — exclude from drift verification):

- The amended `contracts/setfit-benchmark-claims-v1.yaml`: a 40-cell ACTIVE expectation set, a retained DEFERRED two-method scope, and the single-method statistics rules
- The factored per-row verification function in `crates/aprender-train/src/train/setfit/bench_gate.rs`, shared by `verify_run`'s loop and the single-cell door
- The out-of-scope-cell refusal variant and its negative
- The active-scope re-mutations of four of the six doctored shapes
- The seed-dispersion 95% interval and its scipy reference case in the pinned uv fixture manifest
- The single-cell verification subcommand on `apr setfit bench`
- The active-scope rendering case table (must-match / must-not-match) in `crates/apr-cli/src/commands/setfit_bench_tests.rs`
- `.planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md` — the tombstone of record for the retired LoRA arm

Deliberately NOT created: any remote LoRA dispatch script, and any LoRA row, ledger or selection artifact.
</artifacts_this_phase_produces>

<review_notes>
Replan notes (2026-09-08, D-19 scoped replan):

- The plan number is REPURPOSED, not vacated. The alternative — a pure tombstone plus folding the
  retarget into 05-12 — was rejected on the phase's own established ordering discipline: the gate
  that judges the cells must exist AND be proven at the scope it will judge them in, in an earlier
  wave, before compute is spent. That was review consensus item 4's ruling and it applies with more
  force now, because the scope itself is what changed.
- The retired plan's claim to own selection-manifest generation was already stale when it was
  written: `scripts/run_bench_cells.sh::generate_selections` (05-09) generates them. Verified by
  reading the function and the consuming path expression, not inferred from the plan text.
- The retired plan's pilot-cell proof claimed more than its procedure established. Running
  `bench report` over a copy holding one row refuses at the completeness step, which sits BEFORE
  the row loop, so the pilot row's own bytes are never read. That is why this plan adds a
  single-cell verification door rather than only re-specifying the copy-and-refuse procedure — see
  05-12 Task 1, which does both and says what each half proves.
- <!-- REVIEW REJECTED: deleting the two-method equations from the contract as "dead specification". Rejected: the two-method clauses are what keep two of the six doctored negatives meaningful, and D-ITEM-05-15's restoration path depends on them existing. A deletion would also be indistinguishable in history from a quiet loss of contracted guarantees. Retained-as-deferred instead, with the pointer. -->
</review_notes>

<verification>
- Contract committed separately, `pv validate` rc=0, `pv diff` and its suggested bump recorded
- Ten negative/mutation assertions green with their variant tags recorded; the two contract-mutation controls shown RED then reverted
- `make setfit-bench-tests` and `make contract-audit-phase5` rc=0 with zero BIND- lines; one induced floor-red demonstrated and reverted
- No cross-method section and no comparative literal in the rendered active-scope report
</verification>

<success_criteria>
- The declared matrix is 40 SetFit cells, derived closed-form from an approved contract, with the fail-closed gate not weakened in any respect and re-proven at the new scope
- The two-method design is retained, deferred and restorable; the LoRA arm is retired with its evidence written down rather than silently dropped
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md` when done
</output>
