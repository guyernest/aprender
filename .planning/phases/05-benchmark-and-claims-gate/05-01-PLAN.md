---
# branch: gsd/phase-2-contract-gate (Phase 5 continues on it per the 02-01 policy; no PR is open — opening one is the human's call)
phase: 05-benchmark-and-claims-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-train/src/train/setfit/evidence.rs
  - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "A timed single-cell probe ran BEFORE the boundary matrix, and the matrix projection was derived from it (CLAUDE.md >1hr compute rule honored)"
    - "The production epochs/batch were frozen from the pinned setfit 1.1.3 env BEFORE any calibration pass (they are baked into every cell label)"
    - "Every gated parameter class has a measured ε window (10x worst-control <= eps <= best-real/10) on the production all-MiniLM-L6-v2 encoder"
    - "The proposed regime entry's architecture@revision component is byte-copied from a measured run's own printed CALIBRATION REGIME id, never hand-composed"
    - "TWO contracted-but-unmeasured cells are RESERVED as prospective validation, run AFTER ε is frozen and BEFORE the 05-03 approval — or, if compute forbids, their absence is recorded explicitly and 05-03's coverage claim is weakened to contract-membership-only (review finding: six measured cells are an engineering margin, not evidence for the other 34)"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/evidence.rs"
      provides: "production_calibration_matrix #[ignore]d env-gated harness beside calibration_matrix_epsilon_basis"
      contains: "production_calibration_matrix"
    - path: ".planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md"
      provides: "Frozen E/B, probe timing + projection, 18-pass boundary tables, per-class ε derivation, proposed contract entry, MEASURED-vs-COVERED table, prospective-validation result"
      contains: "Prospective validation"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/evidence.rs"
      to: "UpdateEvidence::from_tune_output"
      via: "harness calls run_tuning + from_tune_output with NO gate widening (the UncalibratedRegime refusal lives in validate_evidence, judgement time only)"
      pattern: "from_tune_output"
    - from: "crates/aprender-train/src/train/setfit/evidence.rs"
      to: "~/.cache/aprender/minilm-l6-v2-1110a243"
      via: "APRENDER_MINILM_DIR env gate (full_weight_parity.rs precedent)"
      pattern: "APRENDER_MINILM_DIR"
---

<objective>
Measure the production calibration envelope for the F-10 unblock (D-01/D-02/D-03): freeze the
production epochs/batch from the pinned setfit 1.1.3 defaults, run a TIMED single-cell probe, then
the boundary matrix ({s8, s64} x 3 seeds x 3 conditions) on the production
`sentence-transformers/all-MiniLM-L6-v2` checkout, and derive per-class ε windows plus the exact
proposed regime entry string. This plan MEASURES; it edits no contract and no gate — the deliberate
three-place edit is plan 05-03 at the D-04 human checkpoint.

Branch: work lands on `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: everything in Phase 5 is blocked on F-10; front-loading the measurement surfaces failure earliest (D-01).
Output: the harness test + `05-01-calibration-measurements.md` (the D-04 checkpoint's evidence input).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (sections: "F-10 Unblock: Measured Mechanics", Pattern 2, Pitfall 2, Assumptions A2)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (section 2: Production calibration harness)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze production epochs/batch (A2) and run the timed s8 probe</name>
  <files>crates/aprender-train/src/train/setfit/evidence.rs, .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/evidence.rs (lines 1550-1720: calibration_matrix_epsilon_basis — the exact harness shape to replicate; CONTROL_LR=1e-30, NEAR_NULL_LR=1e-8 constants; the printed "CALIBRATION REGIME:" line)
    - crates/aprender-train/tests/full_weight_parity.rs (lines 1-80: the APRENDER_MINILM_DIR env-gate + skip-with-message pattern)
    - crates/aprender-train/src/train/setfit/mod.rs (calibration_regime_id + cell_label + shots_component: cell label grammar `s{shots}e{epochs}b{batch}`)
    - crates/aprender-train/src/train/setfit/config.rs (SetFitTrainConfig knobs the harness config must satisfy; max_length accepts only 256)
    - scripts/setfit_fixtures/pyproject.toml and README.md (the pinned uv env: setfit 1.1.3)
    - .planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md (F-R3, F-R5, F-R6, Pitfall 2)
  </read_first>
  <action>
    (1) Verify A2 from the PINNED env, not the web: run
    `cd scripts/setfit_fixtures && uv run python -c "from setfit import TrainingArguments; a = TrainingArguments(); print(a.num_epochs, a.batch_size, a.body_learning_rate)"`
    and record the raw output. Freeze the production epochs E and batch B for this milestone from
    those defaults (expected shape: 1 epoch, batch 16, body lr 2e-5 — but the env output is the
    authority; if batch_size is a tuple, the contrastive-body member is the one that maps to
    SetFitTrainConfig's batch knob). These two values are baked into every cell label
    (`s{shots}e{E}b{B}`) and into the D-02 contract entry — record them FIRST in a "Frozen
    production hyperparameters" section of the measurements file, with the exact command and its
    stdout.
    (2) Add `production_calibration_matrix` to evidence.rs beside `calibration_matrix_epsilon_basis`:
    `#[test] #[ignore = "..."]`, env-gated on `APRENDER_MINILM_DIR` (default
    `~/.cache/aprender/minilm-l6-v2-1110a243`, resolved via home dir; print a SKIP message naming the
    env var and return early when the checkout is absent — full_weight_parity.rs pattern). It imports
    the production encoder via `SetFitMiniLm::from_pretrained_dir`, builds selections at the frozen
    E/B, and reuses the fixture harness mechanics verbatim: real condition encoder_lr 2e-5, control
    1e-30, near-null 1e-8, per-class aggregation (real min/median/max, ctrl max, near-null max +
    moved flags, rounding_noise_floor, support fraction, binding parameter name), and it PRINTS the
    regime id via the same "CALIBRATION REGIME:" line. Support a probe mode via env var
    `APRENDER_CALIBRATION_PROBE=1` that restricts the run to ONE cell (s8, seed 13, REAL condition
    only) so timing can be measured before committing to the matrix. Do NOT touch run_tuning,
    validate_evidence, thresholds.rs, or any gate — measurement is legal today because the
    UncalibratedRegime refusal lives at judgement time (tune.rs:1139), not in run_tuning.
    (3) Run the probe timed:
    `CARGO_INCREMENTAL=0 APRENDER_CALIBRATION_PROBE=1 cargo test -p aprender-train --lib --features setfit production_calibration -- --ignored --nocapture`
    capturing wall-clock (use `date +%s` before/after or `/usr/bin/time`; never read a status
    through a pipe — redirect to a log file and read rc on the next line). Record: probe wall-clock,
    the printed regime id (must begin `minilm-slice-h384-l6-a12-i1536-v30522@1110a243` — the
    hardcoded prefix renders even for the full model, F-R2), and the projection for the full
    boundary matrix: 18 passes total, s64 cells weighted ~8x s8 (rows scale). Write the projection
    arithmetic into the measurements file.
    (4) COMPUTE GATE (CLAUDE.md: check in BEFORE >1hr compute on non-lambda-vector hosts): if the
    projected boundary-matrix wall-clock exceeds 60 minutes, STOP after this task, report the
    projection table as a checkpoint, and wait for the human's go/no-go (options: run local anyway,
    move the matrix to lambda-vector CPU, or trim the seed subset). Only proceed to Task 2 if the
    projection is under 60 minutes or the human approves.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 APRENDER_CALIBRATION_PROBE=1 cargo test -p aprender-train --lib --features setfit production_calibration -- --ignored --nocapture > /tmp/probe.log 2>&1; rc=$?; tail -20 /tmp/probe.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - 05-01-calibration-measurements.md contains a "Frozen production hyperparameters" section with the literal uv command and its stdout, and explicit `epochs = E` / `batch = B` lines
    - /tmp/probe.log (and the measurements file) contains a line beginning `CALIBRATION REGIME: minilm-slice-h384-l6-a12-i1536-v30522@1110a243`
    - The measurements file records probe wall-clock seconds and the 18-pass projection arithmetic (s64 weighted ~8x s8)
    - evidence.rs contains `fn production_calibration_matrix` with `#[ignore` and a string reference to `APRENDER_MINILM_DIR`
    - No diff in thresholds.rs, tune.rs, or any contracts/ file (`git diff --name-only` shows only evidence.rs and the measurements file)
  </acceptance_criteria>
  <done>Frozen E/B recorded from the pinned env; probe timed and green; projection recorded; compute gate decision documented</done>
</task>

<task type="auto">
  <name>Task 2: Run the boundary matrix (2 cells x 3 seeds x 3 conditions)</name>
  <files>.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (Task 1 output: frozen E/B, probe timing)
    - contracts/setfit-train-lifecycle-v1.yaml (lines ~98-160: the evidence_gate DERIVATION invariant — window rule, per-class table columns; lines ~276-334: calibration_regime + the Phase-5 consequence clause)
    - .planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md (F-R4: gradient_free re-derivation clause; F-R5: methodology)
  </read_first>
  <action>
    Run the full boundary matrix through `production_calibration_matrix` (probe env var unset):
    cells {s8, s64} x seeds {13, 31, 53} x conditions {real 2e-5, control 1e-30, near-null 1e-8}
    = 18 tuning passes, at the frozen E/B from Task 1. Seeds 13/31/53 are the min/median/max of the
    contracted set {13,17,23,29,31,37,41,43,47,53}. Capture the full report output to a log and
    transcribe into the measurements file, per cell and per parameter class: real min/median/max,
    ctrl max, near-null max + moved flag, rounding_noise_floor, support fraction, binding parameter
    name, and the printed regime id for EVERY cell (all six must share the architecture component;
    quote at least one full id verbatim). Assert (and record) `control_max < real_min` for every
    gated class in every cell — if any cell violates it, STOP and report: the ε window does not
    exist and the D-04 checkpoint needs the raw data, not a forced number.
    Separately re-derive the gradient_free class on the production encoder per the contract's
    RE-DERIVATION-NOT-RELAXATION clause (FALSIFY-STL-002 pattern): confirm from the measured deltas
    that attention key bias remains gradient-free (dL/db_k = 0 by softmax shift-invariance) at
    production scale, and record the measured basis for keeping it ungated (or flag if it MOVED —
    that would be a finding for the checkpoint, not something to paper over).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit production_calibration -- --ignored --nocapture > /tmp/matrix.log 2>&1; rc=$?; tail -30 /tmp/matrix.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The measurements file contains 6 per-cell tables (s8/s64 x seeds 13/31/53), each covering every ParameterClass with the columns named in the contract's DERIVATION invariant
    - Every gated class row records ctrl_max and real_min with ctrl_max < real_min (or the plan stopped and reported)
    - At least one full regime id is quoted verbatim from the log, architecture component `minilm-slice-h384-l6-a12-i1536-v30522@1110a243`
    - A "gradient_free re-derivation" section records the production-scale verdict for attention_key_bias with its measured basis
  </acceptance_criteria>
  <done>18 passes complete, all tables transcribed, separation holds (or stop-and-report executed)</done>
</task>

<task type="auto">
  <name>Task 3: Derive frozen ε values and compose the proposed regime entry</name>
  <files>.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md (Task 2 tables)
    - crates/aprender-train/src/train/setfit/thresholds.rs (lines 273-300: the fixture Thresholds::frozen() values — the shape the production table mirrors; lines 107-159: RegimeCoordinates grammar)
    - contracts/setfit-train-lifecycle-v1.yaml (lines ~102-133: frozen_thresholds block shape; line ~281: the fixture entry string whose grammar the new entry follows)
  </read_first>
  <action>
    From the Task 2 tables, derive the production regime's frozen values per Phase 3's exact
    methodology: for each gated class, window = [10 x worst control delta across all measured
    cells, best real delta / 10 across all measured cells]; frozen ε = the window's upper edge
    rounded DOWN to two significant figures; record the noise-floor clearance ratio per class
    (must clear rounding_noise_floor by a wide margin — record the multiplier, fixture precedent
    49x-315x); derive the production embedding_delta_floor the same way; record support fractions.
    Every frozen value must name its binding parameter (the row that set the window edge).
    Then compose the PROPOSED contract entry, byte-for-byte: architecture@revision component COPIED
    from a Task 2 printed id (never hand-composed — F-R2), then
    `|seeds=13,17,23,29,31,37,41,43,47,53|cells=s8e{E}b{B},s16e{E}b{B},s32e{E}b{B},s64e{E}b{B}`
    with E/B the Task 1 frozen values (sets, order irrelevant — membership is component-wise per
    RegimeCoordinates::covers). Write the D-02 MEASURED-vs-COVERED table: MEASURED = {s8,s64} x
    {13,31,53} (6 cells); COVERED-BY-MARGIN = the remaining 34 cells, with the documented margin
    argument (s16/s32 lie inside the measured shot envelope; the 7 unmeasured seeds vary only the
    Philox stream, and the window already spans 3 seeds — state this as the margin rationale the
    contract will record). Close the file with a "Checkpoint input" section: the complete proposed
    per-regime thresholds table + entry string + open questions for the human (if any).
    THEN run PROSPECTIVE VALIDATION (addresses review concern: six measured cells are an engineering
    margin, not evidence that the other 34 clear the frozen threshold). Reserve exactly TWO
    contracted-but-unmeasured cells that were NOT part of the Task 2 boundary matrix — `s16` seed 41
    and `s32` seed 29 — and, AFTER the ε values above are frozen (do NOT adjust ε in response to
    these results; that would turn validation back into derivation), run each cell's REAL condition
    once through `production_calibration_matrix` in a prospective mode (env var
    `APRENDER_CALIBRATION_PROSPECTIVE="s16:41,s32:29"` restricting the run to those two cells, REAL
    condition only — same env-gate mechanics as `APRENDER_CALIBRATION_PROBE`). For each cell record,
    per gated class: measured real min delta, the frozen ε, and PASS (real_min > ε) or FAIL. Write a
    "Prospective validation" section holding the two per-cell tables and one verdict line. If the
    projected wall-clock for these two cells exceeds the compute budget agreed at Task 1's gate, DO
    NOT run them: write the section anyway stating `NOT RUN — compute budget`, and record the
    consequence explicitly — 05-03's coverage claim must then read "contract membership covers all
    40 cells; empirical threshold success remains a per-run invariant enforced at judgement time".
    If either cell FAILS, that is a FINDING for the D-04 checkpoint (the ε window does not
    generalize): report it with the raw numbers; never re-derive ε to make it pass.
  </action>
  <verify>
    <automated>grep -c 'seeds=13,17,23,29,31,37,41,43,47,53' .planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Per-class table with columns: class, window lower, window upper, frozen ε (2 sig figs, rounded DOWN), noise-floor clearance multiplier, support fraction, binding parameter
    - The proposed entry string appears in full and its architecture component is byte-equal to a Task 2 quoted id (both strings quoted adjacently so equality is visible)
    - The MEASURED-vs-COVERED table enumerates all 40 (shot, seed) combinations with each marked MEASURED or COVERED and a stated margin rationale
    - A "Checkpoint input" section exists (the D-04 evidence package for plan 05-03)
    - A "Prospective validation" section exists naming cells `s16` seed 41 and `s32` seed 29, and either (a) records per-class real_min vs frozen ε with an explicit PASS/FAIL verdict for both cells, or (b) records the literal string `NOT RUN — compute budget` together with the weakened-coverage consequence sentence for 05-03
    - The frozen ε values in the "Checkpoint input" table are byte-identical before and after the prospective run (quote both states) — validation never fed back into derivation
  </acceptance_criteria>
  <done>Complete ε derivation + proposed entry + MEASURED/COVERED table ready for the 05-03 checkpoint</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files CREATED by this plan (newly-created — exclude from drift verification):

- Test fn `production_calibration_matrix` (crates/aprender-train/src/train/setfit/evidence.rs)
- Env vars read by that harness: `APRENDER_MINILM_DIR`, `APRENDER_CALIBRATION_PROBE`, `APRENDER_CALIBRATION_PROSPECTIVE`
- File `.planning/phases/05-benchmark-and-claims-gate/05-01-calibration-measurements.md` with sections: "Frozen production hyperparameters", "Probe timing and projection", "Boundary matrix", "gradient_free re-derivation", "MEASURED-vs-COVERED", "Prospective validation", "Checkpoint input"
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses recorded in-plan so a future reviewer does not re-raise them:

- INCORPORATED (Codex MEDIUM, consensus item 7): prospective validation of two contracted-but-unmeasured cells added to Task 3, with an explicit compute-budget fallback that weakens 05-03's claim rather than overstating it.
- <!-- REVIEW REJECTED: Gemini's 05-01 row ("document that production regime prefix `minilm-slice-` is accepted under explicit contract aliasing rules") is answered in 05-03 Task 1 as DOCUMENTATION of the rendering rule, not as an aliasing rule. No aliasing/normalization is added anywhere — see 05-02's rejection note. -->
</review_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement -> contract gate | numbers measured here become the calibrated-regime gate's thresholds in 05-03; a wrong number here weakens a Phase 3 safety gate |
| host cache dir -> harness | `~/.cache/aprender/minilm-l6-v2-1110a243` weights are loaded; MiniLmImport::open verifies tokenizer SHA-256 against the pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | proposed regime entry string | mitigate | architecture component byte-copied from the harness's own printed id and quoted beside it; never hand-composed |
| T-05-01-02 | Elevation (gate loosening) | ε derivation | mitigate | window rule + round-DOWN + noise-floor clearance recorded per class with binding parameter names; separation assertion ctrl_max < real_min enforced in-harness |
| T-05-01-03 | Spoofing (wrong model measured) | production checkout | mitigate | `SetFitMiniLm::from_pretrained_dir` runs the pinned import ladder (tokenizer SHA-256 vs pin); regime id printed from the run proves h384-l6-a12-i1536-v30522 dims |
| T-05-01-04 | DoS (compute overrun) | boundary matrix | mitigate | timed probe + projection + hard 60-minute check-in gate before the matrix |
| T-05-01-SC | Tampering | package installs | accept (n/a) | this plan installs nothing; the uv env is the existing hash-locked scripts/setfit_fixtures lockfile |
</threat_model>

<verification>
- `CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit thresholds` still green (no gate files touched)
- `git diff --name-only` limited to evidence.rs + the measurements file
- Probe ran before matrix (timestamps in the measurements file)
</verification>

<success_criteria>
- Harness exists, env-gated, probe-mode capable; measurements file complete through the "Checkpoint input" section
- No contract, thresholds, or tune edits in this plan
- Compute gate honored (probe -> projection -> conditional check-in)
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-01-SUMMARY.md` when done
</output>
