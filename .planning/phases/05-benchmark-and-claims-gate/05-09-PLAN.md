---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 09
type: execute
wave: 4
depends_on: ["05-05", "05-06", "05-07", "05-08"]
files_modified:
  - crates/apr-cli/src/setfit_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/commands/setfit_bench.rs
  - crates/apr-cli/Cargo.toml
  - scripts/run_bench_cells.sh
autonomous: true
requirements: [EVAL-03, EVAL-05]

must_haves:
  truths:
    - "apr setfit bench run executes ONE method/shot/seed cell and emits one digest-committed row file + a run-manifest update (D-13/D-14); a --record mode ingests a transported row file with full verification and NO re-execution"
    - "Resource metrics follow the rewritten contracted protocol exactly: COLD latency and inference peak RSS are measured in a DEDICATED FRESH CHILD PROCESS that only loads the artifact and predicts once (never in the process that just trained); TRAIN peak RSS is a separate field with its own mechanism string; warm = median of 10 after 3 warmup; throughput over the full test split at a declared batch — consistently bounded per EVAL-05 (review consensus item 2)"
    - "Peak-RSS mechanisms are comparable by construction: the parent reads the child's true kernel high-water mark (`child_max_rss_time_l` on macOS, `child_max_rss_vm_hwm` on Linux); the `sysinfo_sampled_<hz>` fallback records its interval and is emitted only where no child measurement is possible"
    - "Model size is recorded as artifact_bytes + deployable_total_bytes on every row (+ base_model_bytes on LoRA rows), so an adapter-only figure can never stand in for a deployable size (review consensus item 8)"
    - "Every LoRA training invocation appends one line to the cell's append-only candidate ledger BEFORE test evaluation, and the row records the ledger digest and a line count of exactly 1 (review consensus item 5)"
    - "Re-running a completed cell is idempotent-or-refused, never silently duplicated: bench run refuses an existing row file without --force, --record is idempotent on an identical digest and a typed error on a differing one, and an interrupted cell leaves either NO row or one complete digest-valid row (atomic rename) — never a partial"
    - { statement: "The 40-cell driver writes one bench-dir from one process at a time (single-writer); concurrent cell processes against the same bench-dir are refused, because parallel execution would both race the run-manifest and corrupt the EVAL-05 resource measurements", verification: backstop }
    - "The SetFit cell path drives ONLY existing library doors (train lifecycle, load_setfit_apr, lock workflow, evaluate_rows_from_artifact, assemble_quality_block); the LoRA cell path drives run_classify_core — no second implementation of anything (OPS-03)"
    - "backend_identity is read from execution (ExecutionBackend::identity for SetFit; pipeline GPU identity for LoRA), never echoed from config (Ph4 D-12)"
  artifacts:
    - path: "crates/apr-cli/src/commands/setfit_bench.rs"
      provides: "The bench run filesystem adapter (data_contrastive.rs house style)"
      contains: "pub(crate) fn run"
    - path: "crates/apr-cli/src/setfit_commands.rs"
      provides: "Bench { Run } nested subcommand"
      contains: "BenchCommands"
    - path: "scripts/run_bench_cells.sh"
      provides: "Resume-capable 40-cell driver (bashrs-clean, set -euo pipefail)"
  key_links:
    - from: "crates/apr-cli/src/commands/setfit_bench.rs"
      to: "aprender_train::train::setfit::bench_row"
      via: "row construction + RunManifest::record — all semantics in the library, adapter writes files"
    - from: "crates/apr-cli/src/commands/setfit_bench.rs"
      to: "Selection::replay"
      via: "the same three-call ingest door as eval/setfit.rs (the EVAL-02 pairing key consumed, never re-derived)"
      pattern: "Selection::replay"
---

<objective>
Ship `apr setfit bench run` (D-13): one cell in, one verified row out, for both methods, with the
contracted resource measurements — plus the record-only ingest mode for GPU-host transport and the
resume-capable 40-cell driver script.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-03's row emission and EVAL-05's bounded resource measurement happen here; `bench
report` (05-10) and both cell-execution plans (05-11/05-12) consume this surface.
Output: CLI wiring + adapter + resource module + driver script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 5, Pattern 6, Open Q5 hybrid recommendation — ADOPTED: library calls inside bench run, spawned bench run per cell in the driver)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 6, 7; Shared Patterns: atomic write, refusal-with-remedy, backend identity)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI wiring + resource measurement module</name>
  <files>crates/apr-cli/src/setfit_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/commands/setfit_bench.rs, crates/apr-cli/Cargo.toml</files>
  <read_first>
    - crates/apr-cli/src/setfit_commands.rs (:16-101 — SetfitCommands::Train's doc/arg style; value_name, --force, --dry-run conventions)
    - crates/apr-cli/src/extended_commands.rs (:799-802 — the nested-subcommand shape for Bench { command: BenchCommands })
    - crates/apr-cli/src/dispatch_analysis.rs (:578, :832-860 — dispatch_setfit_command; the cli.offline-not-threaded comment style to preserve)
    - crates/apr-cli/src/commands/bench.rs (:102-117 — the existing warmup/iterations precedent, defaults warmup 3)
    - contracts/setfit-benchmark-claims-v1.yaml (the resource protocol section — the boundaries this module implements verbatim)
    - Cargo.toml at repo root (:266 — sysinfo 0.32 workspace dep declaration to inherit)
  </read_first>
  <action>
    Extend `SetfitCommands` with `Bench { #[command(subcommand)] command: BenchCommands }` and
    `BenchCommands::Run` carrying: --method (setfit|lora), --shots, --seed, --data <DIR>,
    --selection <FILE>, --bench-dir <DIR>, --model-dir <DIR> (setfit encoder checkout),
    --config <FILE> (optional, file-first per house rule), --force, --json; plus the record mode as
    `--record <ROW_FILE>` (mutually exclusive with the execution args — clap conflicts_with). Add
    the dispatch arm in dispatch_analysis.rs under #[cfg(feature = "setfit")]; keep the
    offline-not-threaded discipline and its comment style (bench opens no socket). Create
    `commands/setfit_bench.rs` with the setfit_train.rs check ordering: full request validation
    first, then refuse-existing-output (row file, unless --force), then inputs. Implement the
    resource module inside setfit_bench.rs (or a small `mod resource` within it), following the
    REWRITTEN contract protocol in setfit-benchmark-claims-v1 (05-05 section 6 — the review found
    the previous single-peak, same-process design labelled its numbers more strongly than the
    protocol could support). THREE distinct measurement surfaces:
    (a) TRAIN PEAK — `train_peak_rss() -> (u64, String)` measured inside the training process:
    cfg(target_os = "linux") reads /proc/self/status VmHWM (text parse, mechanism "vm_hwm"); other
    platforms use sysinfo (add `sysinfo = { workspace = true }` to apr-cli Cargo.toml) sampling on a
    thread with a sampled max, emitting mechanism `sysinfo_sampled_<hz>` with the ACTUAL measured
    interval plus `peak_rss_sample_interval_hz` on the row. This value is labelled TRAIN peak and
    nothing else: VmHWM is process-CUMULATIVE, so in a process that trains then reloads then infers
    it reports the training peak, not the inference peak.
    (b) COLD LATENCY + INFERENCE PEAK — measured in a DEDICATED FRESH CHILD PROCESS. Add a hidden
    measurement mode (`--cold-probe <ARTIFACT> --probe-text <FILE>`, conflicts_with the execution
    args) whose entire job is: load the artifact, run exactly ONE single-text classify, print one
    machine-readable `COLD_LATENCY_MS=<f64>` line, exit. The parent resolves its own executable path
    (`std::env::current_exe`, never a bare `apr`), spawns that child, reaps the ExitStatus on its own
    line (never a status through a pipe), parses the cold latency, and reads the child's TRUE kernel
    high-water mark: `child_max_rss_vm_hwm` on Linux, `child_max_rss_time_l` on macOS — both exact
    kernel HWMs of an equivalently-scoped child, hence comparable across the two hosts. Implement by
    wrapping the child in `/usr/bin/time -l` (macOS) / `/usr/bin/time -v` (Linux) and parsing the
    reported maximum resident set size. Do NOT reach for `mach_task_basic_info`: this workspace sets
    `unsafe_code = "forbid"`, so Gemini's suggestion is rejected on that ground, and the child route
    gives the same true-HWM semantics with no unsafe and the same shape on both platforms.
    (c) WARM + THROUGHPUT — warm = median of 10 single-text classifies after 3 warmups against the
    RELOADED model in the measuring process; throughput = full test split at the declared batch size.
    Unit-test: the VmHWM parser against a canned /proc/self/status text; the max-RSS parser against
    BOTH a canned macOS `/usr/bin/time -l` block and a canned Linux `/usr/bin/time -v` block, driven
    by an explicit must-match / must-not-match case table (CLAUDE.md rule 7 — these parsers are
    exactly the "guard regexes ship a case table" shape that has been wrong five times in this repo,
    and the units differ: macOS reports BYTES, Linux reports KILOBYTES, so the table must pin the
    unit conversion in both directions); the median helper against an even/odd case; and that a
    sampled mechanism string always carries a non-zero interval.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit setfit_bench > /tmp/sb1.log 2>&1; rc=$?; tail -5 /tmp/sb1.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `apr setfit bench run --help` (via the built binary or clap unit test) lists method/shots/seed/data/selection/bench-dir, `--record`, and `--cold-probe`, each with its conflict rule
    - setfit_bench.rs contains the VmHWM parser, the `/usr/bin/time` max-RSS parser, and the four mechanism string constants (`vm_hwm`, `sysinfo_sampled_<hz>`, `child_max_rss_vm_hwm`, `child_max_rss_time_l`)
    - apr-cli Cargo.toml gains sysinfo via workspace inheritance (no version literal drift)
    - The max-RSS parser's must-match / must-not-match table passes for both canned blocks, including the macOS-bytes vs Linux-kilobytes unit conversion in both directions
    - The cold-probe child path resolves its executable via `current_exe` (grep) and reaps an ExitStatus with rc captured on its own line; comment-filtered, `grep -v '^[[:space:]]*//' crates/apr-cli/src/commands/setfit_bench.rs | grep -cE '\|[[:space:]]*grep.*\$\?'` returns 0
    - Unit tests for both parsers and the median pass; the dispatch arm compiles under --features setfit
  </acceptance_criteria>
  <done>Surface + resource mechanics in place, unit-proven</done>
</task>

<task type="auto">
  <name>Task 2: SetFit cell execution path + row emission + --record mode</name>
  <files>crates/apr-cli/src/commands/setfit_bench.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/eval/setfit.rs (:197-262 — the check ordering + the three-call ingest door + how lock write/consume flows through the CLI; EvalRow/LockRow field sourcing)
    - crates/apr-cli/src/commands/setfit_train.rs (:601-621 ordering; :176-209 atomic_write + refuse_existing_output)
    - crates/aprender-train/src/train/setfit/bench_row.rs (BenchRow/RunManifest constructors + record())
    - crates/aprender-train/src/train/setfit/bench_metrics.rs (assemble_quality_block signature)
    - crates/aprender-train/src/train/setfit/apr_evaluate.rs (evaluate_rows_from_artifact signature)
    - crates/aprender-core/src/setfit/encoder.rs (:227-238 — ExecutionBackend::identity)
  </read_first>
  <action>
    Implement the setfit method path as pure library orchestration (no semantics in the adapter):
    (1) ingest data + selection via the three-call door; refuse contracted-seed violations (the
    bench layer enforces seed ∈ {13,17,23,29,31,37,41,43,47,53} and shots ∈ {8,16,32,64} — a
    non-contracted cell is a typed refusal naming the contract); (2) train via the same library
    lifecycle `apr setfit train` uses (config file-first at the frozen E/B defaults; wall-clock the
    training for train_wall_ms); (3) write the APR atomically (artifact_bytes = bytes written);
    (4) reload through load_setfit_apr (the production loader — only a verified artifact proceeds);
    (5) measure warm/throughput per the resource module against the RELOADED model, and measure
    COLD latency + inference peak RSS by spawning the Task-1 `--cold-probe` child against the written
    artifact (never the first classify in this process — it has just trained and is operationally
    warm); record `train_peak_rss_bytes` from this process and `inference_peak_rss_bytes` from the
    child, each with its own mechanism string;
    (6) evaluate: validation rows via evaluate_rows_from_artifact, create the selection lock, mint
    the token, grant, then test rows (the exact Ph4 D-16 workflow the eval command uses — consumed,
    never reimplemented); COMMIT THE LOCK BYTES: atomically write the cell's canonical lock record to
    `{bench-dir}/locks/setfit-s{shots}-seed{seed}.lock.json` and set the row's `lock.lock_hash` to
    the SHA-256 of exactly those bytes and `lock.lock_record_path` to that relative path, so 05-10's
    gate can RECOMPUTE the digest from the committed file instead of trusting a row field (review
    consensus item 5); (7) assemble QualityBlock (validation rows -> calibration, test rows ->
    quality); (8) build the BenchRow: backend_identity from ExecutionBackend::identity, host
    identity from the environment, MethodEvidence::Setfit {evidence_table_hash from the run's
    recorded evidence, apr_artifact_sha256, lock ref}, and the size fields `artifact_bytes` =
    `deployable_total_bytes` = bytes of the written standalone APR (SetFit ships one file — state
    that equality in a comment so a reader does not mistake it for a copy-paste);
    (9) atomic-write the row file
    `rows/{method}-s{shots}-seed{seed}.json` + RunManifest::record into
    `{bench-dir}/run-manifest.json` (initialize the manifest from the contract-derived expectation
    on first touch). Implement `--record <ROW_FILE>`: from_bytes (digest+schema verified), verify
    the cell key matches the filename and is expected, copy into rows/ atomically, record the hash
    — NO execution; refusal messages name the remedy. Tests: a unit test for the record mode over
    a synthetic valid row + each refusal (bad digest, unexpected cell, re-record with different
    hash); the full setfit execution path is exercised for real in plan 05-12 (its cost is a
    training run — do not fake it here with a stub row labeled real).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit setfit_bench > /tmp/sb2.log 2>&1; rc=$?; tail -5 /tmp/sb2.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - setfit_bench.rs greps show: Selection::replay, load_setfit_apr (or the loader door's public name), evaluate_rows_from_artifact, assemble_quality_block, ExecutionBackend::identity — and NO direct classify loop of its own
    - Non-contracted seed (e.g. 42) refuses with a message naming setfit-benchmark-claims-v1; a non-contracted shot count (e.g. 0 or 12) refuses the same way — proving the empty/degenerate cell key is a typed refusal, not a run
    - --record round-trip test green; its refusal tests assert distinct typed errors, and a test proves --record is IDEMPOTENT when re-recording the identical digest and a typed error when the digest differs
    - A test proves `bench run` refuses an existing row file without --force (the re-run-twice case), and that a row file is written by atomic rename so an interrupted cell leaves either no file or a complete digest-valid one
    - The SetFit row's `lock.lock_hash` equals the SHA-256 recomputed from the committed `locks/…lock.json` bytes (unit test over a synthetic lock file), and `deployable_total_bytes == artifact_bytes` for SetFit rows
    - Row filename grammar `{method}-s{shots}-seed{seed}.json` produced by one function with a unit test
  </acceptance_criteria>
  <done>SetFit cells executable end-to-end via library doors; transport ingest verified</done>
</task>

<task type="auto">
  <name>Task 3: LoRA cell path + the 40-cell driver script</name>
  <files>crates/apr-cli/src/commands/setfit_bench.rs, scripts/run_bench_cells.sh</files>
  <read_first>
    - crates/apr-cli/src/commands/finetune.rs (run_classify_core from 05-06 — the shared entry; where epochs_completed + manifest hash surface; pipeline.gpu_name()/gpu_total_memory() at ~:1629)
    - .planning/phases/05-benchmark-and-claims-gate/05-06-SUMMARY.md (Task 3's preflight result: the EXACT reload route it proved — whether `load_adapter` pre-existed or was added, the exact `predict_proba_tokenized` signature, and the written file names the reload consumed. This task calls THAT route by name; it does not invent one)
    - crates/aprender-train/src/finetune/classify_pipeline/mod.rs (the reload + probability entries as they exist after 05-06 Task 3)
    - contracts/setfit-benchmark-claims-v1.yaml (the LoRA no-selection attestation fields, the candidate-ledger rule, and the size-field rule)
    - scripts/apr_bin.sh (the pin the driver sources); CLAUDE.md bashrs section (option-neutral sourced libs; set -euo pipefail in executables)
    - Makefile (:2101-2106 — the rc-never-through-a-pipe comment block the script must honor)
  </read_first>
  <action>
    LoRA path in setfit_bench.rs: call `run_classify_core` with the explicit per-cell
    TrainingConfig (contracted seed, val_split 0.0, early_stopping_patience 0, published
    ClassifyConfig defaults otherwise — rank 16, alpha 16.0, lr 1e-4, epochs 3, max_seq_len 512,
    batch 32; frozen-defaults policy) and the selection manifest.
    PRECONDITION — the reload route is the one 05-06 Task 3 PROVED, called by name. Read
    05-06-SUMMARY.md first and use the exact `load_adapter` / `predict_proba_tokenized` entries it
    recorded. If that summary reports the preflight did not succeed, STOP and report: do not
    improvise a reload path here (review consensus item 1 — 40 remote 9B cells against an unproven
    API is the failure mode this ordering exists to prevent).
    CANDIDATE LEDGER (review consensus item 5): BEFORE training starts and BEFORE any test access,
    append exactly one JSONL line to `{bench-dir}/ledger/lora-s{shots}-seed{seed}.jsonl`
    ({timestamp, selection_manifest_hash, epochs_requested, seed, config_hash}) using an
    append-only open (never truncate, never rewrite); if the file already has a line and --force was
    not given, REFUSE with a typed error naming the ledger path — a second candidate for the same
    cell is exactly the uncontracted model selection the attestation claims did not happen.
    After the adapter is written, LOAD the written base+adapter artifacts through that named reload
    route and take warm/throughput against the RELOADED model, and COLD latency + inference peak RSS
    via the Task-1 `--cold-probe` fresh child — never against the in-memory final-epoch training
    state (EVAL-05: metrics come from the reloaded production artifacts, mirroring the SetFit path's
    steps 4-5 reload discipline). Evaluate the reloaded model on the test split through
    `predict_proba_tokenized`, producing the same K-length ordered probability vectors the SetFit
    door returns; assemble quality via assemble_quality_block (same assembly, method-agnostic).
    MethodEvidence::Lora {base_model_sha256 (SHA-256 of the base weights file — computed once and
    cached beside the weights), base_model_bytes (that file's byte length), adapter_sha256 (the
    written adapter), epochs_requested/completed from the result, early_stopping_disabled: true,
    val_split: 0.0, no_selection_attestation: true, candidate_ledger_sha256 (over the ledger file's
    bytes AFTER the run), candidates_trained (its line count — must be 1), candidate_ledger_path}.
    Size fields: `artifact_bytes` = adapter bytes ONLY, `deployable_total_bytes` = base_model_bytes
    + adapter bytes. Never emit adapter-only bytes as the method's deployable size (review consensus
    item 8). Backend/hardware identity from the pipeline's executed GPU identity (gpu_name +
    gpu_total_memory) or cpu identity when no GPU engaged — never from config (Verification
    Discipline rule 2: if the row says GPU, a pipeline-read GPU name must exist). Same row/manifest
    emission as Task 2.
    Driver script `scripts/run_bench_cells.sh`: `#!/usr/bin/env bash`, `set -euo pipefail`,
    sources `. scripts/apr_bin.sh || exit 1` (the pin; sourced lib stays option-neutral), args:
    METHOD BENCH_DIR DATA_DIR [MODEL_DIR]; generates the 40 selection manifests via
    `"$APR" data select` (one per shot x seed, skipping ones that already exist and verify), then
    loops the 40 cells invoking `"$APR" setfit bench run ...` one process per cell (isolation +
    per-cell resume: skip a cell whose row file exists and whose hash the manifest confirms —
    query via a `--record`-style verify or a manifest read with jq-free grep of the recorded
    hash; keep the skip logic simple and hash-based, never filename-based); every rc captured on
    its own line (`rc=$?` immediately after the command, never through a pipe); CARGO_INCREMENTAL=0
    exported; terminal states only (PASS/FAIL per cell + a final tally).
    HALT ON FIRST EVIDENCE FAILURE (the mitigation the review's consensus item 7 asks for): if a
    cell exits non-zero for a reason in the EVIDENCE class — `UncalibratedRegime`, an
    evidence-threshold refusal, a lock or attestation refusal, or a digest failure — the driver
    STOPS IMMEDIATELY, prints the failing cell and the refusal text, and exits non-zero. It does NOT
    continue to the remaining cells. 05-03's coverage claim is deliberately only "membership covers
    all 40 cells; empirical threshold success remains a per-run invariant", and the only thing that
    keeps that claim honest is the first empirical failure ending the run rather than being buried
    in an end-of-run tally. Distinguish this from a transient/infrastructure failure (ENOSPC, ssh
    drop), which also stops but with a different exit code and message so the operator knows resume
    is safe.
    SINGLE WRITER: the driver refuses to start if another driver holds the bench dir (a pid lock
    file removed on exit; a stale lock is reported, never silently stolen). Cells run SEQUENTIALLY
    and there is deliberately NO `--jobs` flag: concurrent cell processes would race the
    run-manifest AND invalidate every EVAL-05 resource number by contending for CPU and memory. If
    wall-clock is the problem, that is a checkpoint (05-12), never a parallelization.
    Lint it: `bashrs lint scripts/run_bench_cells.sh` must be clean (bashrs 6.66.3 is on PATH — the
    REQUIREMENTS.md "not installed" note is stale per research).
  </action>
  <verify>
    <automated>bashrs lint scripts/run_bench_cells.sh > /tmp/brs.log 2>&1; rc=$?; tail -5 /tmp/brs.log; test $rc -eq 0 && CARGO_INCREMENTAL=0 cargo check -p apr-cli --features setfit > /tmp/sb3.log 2>&1; rc2=$?; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - setfit_bench.rs LoRA path greps show run_classify_core and the attestation field construction; no TrainingConfig literal 42/0.2/10 anywhere in the bench path
    - The LoRA path calls the reload + probability entries by the exact names 05-06-SUMMARY.md records (grep both symbols); it contains no second adapter-load or softmax implementation of its own
    - The ledger append happens BEFORE the training call in source order (grep line numbers quoted in the SUMMARY), uses an append-only open, and a unit test proves a second candidate for an existing cell is refused without --force
    - LoRA rows carry `base_model_bytes`, `artifact_bytes` (adapter only), `deployable_total_bytes == base_model_bytes + adapter bytes`, `candidate_ledger_sha256`, and `candidates_trained == 1` — asserted by a unit test over a synthetic cell
    - The GPU identity is read from pipeline accessors (grep gpu_name) with a cpu fallback that never fabricates a GPU string
    - run_bench_cells.sh: `set -euo pipefail` present; `. scripts/apr_bin.sh` sourced; zero `$?`-through-pipe patterns; bashrs lint rc=0
    - run_bench_cells.sh contains no parallel-dispatch construct, checked comment-filtered so the script's own explanatory comments cannot invalidate the gate: `grep -v '^[[:space:]]*#' scripts/run_bench_cells.sh | grep -cE -- '--jobs|xargs +-P|&[[:space:]]*$'` returns 0. The script holds a pid lock over the bench dir, and halts on the first evidence-class failure with a distinct exit code from the transient class
    - Resume logic is hash-based (grep shows the manifest/row verification step, not a bare -f existence check alone)
  </acceptance_criteria>
  <done>Both method paths complete; driver script lint-clean and resume-capable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transported row files -> --record | untrusted bytes from the GPU host; digest + schema + cell-identity verified before recording |
| config -> row provenance | backend/hardware identity must come from execution, not config echo |
| driver shell -> verdicts | rc discipline; pinned binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Spoofing | backend identity | mitigate | ExecutionBackend::identity / pipeline gpu accessors only; cpu fallback cannot fabricate GPU |
| T-05-09-02 | Tampering | transported rows | mitigate | --record verifies digest, schema, expected cell, and filename-key agreement before recording |
| T-05-09-03 | Tampering (uncontracted cell) | seed/shots inputs | mitigate | bench layer refuses non-contracted seed/shots with a contract-naming error (42 explicitly excluded) |
| T-05-09-04 | Repudiation | resource numbers | mitigate | mechanism string mandatory per row; boundaries implemented verbatim from the contract protocol |
| T-05-09-05 | DoS | oversized row/manifest reads | mitigate | bounded read before parse (16 MiB house cap pattern from 04-18) in the adapter's file readers |
| T-05-09-06 | Repudiation (mislabelled resource numbers, severity HIGH) | cold latency / peak RSS | mitigate | cold + inference peak measured in a dedicated fresh child; train peak is a separate field; mechanism strings mandatory and their comparability declared in the contract; parsers pinned by a two-platform case table |
| T-05-09-07 | Elevation (undeclared second LoRA candidate, severity MEDIUM) | candidate ledger | mitigate | append-only ledger written before test access; a second candidate refuses without --force; digest + line count recorded in the row and recomputed by 05-10 |
| T-05-09-08 | Tampering (concurrent writers) | run-manifest / bench dir | mitigate | driver holds a pid lock; no parallel-cell construct exists in the script; row writes are atomic renames so an interrupted cell leaves no partial file |
| T-05-09-SC | Tampering | package installs | accept (n/a) | sysinfo inherited from the existing workspace dep — no new registry package |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files CREATED by this plan (newly-created — exclude from drift verification):

- `SetfitCommands::Bench { command: BenchCommands }`, `BenchCommands::Run` with flags `--method --shots --seed --data --selection --bench-dir --model-dir --config --force --json --record --cold-probe --probe-text`
- Module `crates/apr-cli/src/commands/setfit_bench.rs` with `pub(crate) fn run`, the resource submodule (`train_peak_rss`, the VmHWM parser, the `/usr/bin/time` max-RSS parser, the median helper, the cold-probe child spawn), and the row filename function
- Mechanism constants `vm_hwm`, `sysinfo_sampled_<hz>`, `child_max_rss_vm_hwm`, `child_max_rss_time_l`
- Output line format `COLD_LATENCY_MS=<f64>` (the cold-probe child's machine-readable contract with its parent)
- Directory layout under the benchmark dir: `rows/{method}-s{shots}-seed{seed}.json`, `locks/setfit-s{shots}-seed{seed}.lock.json`, `ledger/lora-s{shots}-seed{seed}.jsonl`, `run-manifest.json`
- `scripts/run_bench_cells.sh` (with its pid lock file and evidence-vs-transient exit codes)
- `sysinfo` dependency line in crates/apr-cli/Cargo.toml (workspace inheritance)
</artifacts_this_phase_produces>

<flagged_assumptions>
- **EVAL-05 edge probe returned `unclassified` and is NOT auto-dismissed.** The deterministic probe
  could not categorize EVAL-05 ("compare training time, warm/cold latency, throughput, peak memory,
  artifact size, calibration, and quality from the reloaded production artifacts"). Surfaced here
  rather than dropped: the unresolved question is what a comparison MEANS when the two sides ran on
  deliberately different hardware (D-09). This plan's answer is that every resource number carries
  its host identity, mechanism string, and measurement scope, and that the report (05-10 Task 2)
  presents them per host with an explicit non-pooling framing line — but no plan in this phase
  claims the two hosts' latency or throughput figures are equivalent, and none should. If a
  reviewer wants a same-hardware comparison, that is a new phase, not a relabelling of these rows.
</flagged_assumptions>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- INCORPORATED (consensus item 2, VERIFIED HIGH): cold latency and inference peak RSS move to a dedicated fresh child process; train peak becomes its own field; both platforms use a true kernel high-water mark of an equivalently-scoped child so the two hosts' numbers are comparable by construction; the sampled fallback records its interval and is labelled a lower bound.
- INCORPORATED (consensus item 1): the LoRA path now calls the reload/probability route by the exact names 05-06 Task 3 proved, and stops if that preflight did not succeed.
- INCORPORATED (consensus item 5): the append-only candidate ledger; SetFit lock bytes committed for digest recomputation.
- INCORPORATED (consensus item 8): `base_model_bytes` / `artifact_bytes` / `deployable_total_bytes` split at row-construction time for both methods.
- INCORPORATED (consensus item 7 mitigation): the driver halts on the first evidence-class failure instead of tallying.
- <!-- REVIEW REJECTED: Gemini's plan-matrix row 05-09 asks for `mach_task_basic_info` on macOS to read `resident_size_max`. REJECTED: this workspace sets `unsafe_code = "forbid"` at the workspace lint level, and that API requires either unsafe or a new mach-bindings dependency. The `/usr/bin/time -l` child max-RSS route adopted above gives the same true-kernel-HWM semantics, on both platforms, with no unsafe and no new package. -->
- <!-- REVIEW REJECTED: Gemini's same row asks to "support cell resume via matrix.json". Resume is already hash-based against the run-manifest; see 05-05's rejection of the matrix.json/run_manifest.json split. -->
</review_notes>

<verification>
- apr-cli setfit_bench suites green with non-zero matched tests; bashrs lint clean; cargo check green
</verification>

<success_criteria>
- One-cell execution for both methods through single library doors; contracted resource protocol implemented; transport ingest and resume proven
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-09-SUMMARY.md` when done
</output>
