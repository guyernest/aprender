---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 05
type: execute
wave: 2
depends_on: ["05-04"]
files_modified:
  - contracts/setfit-benchmark-claims-v1.yaml
  - crates/aprender-train/src/train/setfit/bench_row.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - Makefile
autonomous: true
requirements: [EVAL-03, EVAL-04]

must_haves:
  truths:
    - "One versioned, deny_unknown_fields, method-tagged row type exists with the shared mandatory core plus SetFit/LoRA evidence blocks — nothing null-padded, nothing SetFit-shaped faked (D-12)"
    - "The run manifest declares the 80-cell expectation set (2 methods x 4 shots x 10 contracted seeds) derived from the claims contract, and rows/manifest are digest-verified BEFORE returning (D-14)"
    - "The claims contract owns row schema, completeness rule, pairing rule, LoRA no-selection attestation rule, stats equations with the frozen t literal, and the resource-measurement protocol — and it is validated by pv and by the Makefile from the day it exists (D-15, Pitfall 5)"
    - "Bootstrap/CI-resampling fields are structurally impossible in rows (deny_unknown_fields) and textually excluded by the contract (D-06)"
    - "Selection safety is RECOMPUTABLE evidence, not a self-asserted boolean: a SetFit row's lock_hash must equal the SHA-256 of a committed canonical lock-record file, and a LoRA row must carry an append-only candidate-ledger digest with candidates_trained == 1 — the residual (a determined producer can still forge a consistent ledger) is named in the contract, not hidden (review consensus item 5)"
    - "Resource fields are named exactly as strongly as the protocol supports: train_peak_rss_bytes and inference_peak_rss_bytes are SEPARATE fields with SEPARATE mechanism strings, cold latency is measured in a dedicated fresh child process, and a sampled mechanism records its interval and is labelled a lower bound (review consensus item 2)"
    - "Model size is comparable across methods: artifact_bytes (own written artifact) and deployable_total_bytes (what a user must ship) are both mandatory, and a LoRA row additionally carries base_model_bytes — adapter-only bytes are never presented as a deployable size (review consensus item 8)"
    - "The contract-parity test parses the contract into a TYPED expectation set and compares sets and cardinality, rejecting extras and duplicates — substring presence in a comment cannot satisfy it (review consensus item 6)"
    - "Cell identity collides rather than merges: recording an already-recorded (method, shots, seed) with a DIFFERENT hash is a typed error; re-recording the identical hash is idempotent (resume). Manifest and report ordering is the deterministic contract order (method, then shots ascending, then seed ascending) — cell keys are unique so no tie can arise"
    - { statement: "An empty or zero-cell benchmark directory, and a manifest whose expectation set is not exactly the 80 contract-derived cells, are both typed refusals before any row byte is read", verification: backstop }
  artifacts:
    - path: "contracts/setfit-benchmark-claims-v1.yaml"
      provides: "The Phase 5 claims contract"
      contains: "PF-007"
    - path: "crates/aprender-train/src/train/setfit/bench_row.rs"
      provides: "BenchRow + MethodEvidence + RunManifest with digest-committing envelopes"
      contains: "deny_unknown_fields"
    - path: "Makefile"
      provides: "$(CONTRACTS) append + PHASE5_CONTRACTS + contract-audit-phase5 target"
      contains: "contract-audit-phase5"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/bench_row.rs"
      to: "contracts/setfit-benchmark-claims-v1.yaml"
      via: "include_str! parity test pinning the Rust expectation-set derivation to the contract's enumerated methods/shots/seeds"
      pattern: "include_str!"
    - from: "crates/aprender-train/src/train/setfit/bench_row.rs"
      to: "SelectionManifest"
      via: "same envelope discipline: semantic_hash over canonical payload bytes, verify-before-return from_bytes"
---

<objective>
Define the claims layer's data plane: the `setfit-benchmark-claims-v1.yaml` contract (D-15), the
method-tagged BenchRow (D-12), and the hashed RunManifest with the contract-derived 80-cell
expectation set (D-14) — all in the library (`aprender-train`, feature `setfit`), never in the CLI
(house rule: adapters are filesystem shims).

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-03's machine-readable row and EVAL-04's manifest-defined completeness are the
structures every later plan writes into or refuses against.
Output: contract + row/manifest types + Make wiring, each proven able to fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 4; Open Q3 option (b) ADOPTED and Q4 resolved to the library; Pattern 6 resource protocol inputs)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 3, 9; Shared Patterns: digest-verify, deny_unknown_fields envelope)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contracts/setfit-benchmark-claims-v1.yaml and wire it into the Makefile</name>
  <files>contracts/setfit-benchmark-claims-v1.yaml, Makefile</files>
  <read_first>
    - contracts/tweet-eval-stance-benchmark-v1.yaml (FULL: metadata header house style with in-file pv diff record; equations shape incl. official_f_avg; the contracted seed set 13,17,23,29,31,37,41,43,47,53 and the "42 is NOT a contracted seed" clause; dataset identity this contract must REFERENCE, not restate)
    - contracts/setfit-apr-v1.yaml (skim: the referenced artifact contract; how references between contracts are phrased)
    - Makefile (lines ~1739-1793: the $(CONTRACTS) explicit list ending at contracts/setfit-apr-v1.yaml, and the PHASE2_CONTRACTS narrowing precedent at :1788-1793)
    - .planning/research/PITFALLS.md (PF-007, PF-008 — quote both by id in the contract)
    - scripts/setfit_fixtures/claims_stats/t_critical.json (05-04 Task 1 output — the env-computed value the contract's frozen t literal is copied from; 05-04 runs in wave 1 so this file exists before this task starts)
  </read_first>
  <action>
    Create `contracts/setfit-benchmark-claims-v1.yaml` (one-contract-per-phase, Ph1 D-23),
    following the tweet-eval contract's kind/structure so `pv validate` accepts it (if pv rejects
    the shape, restructure to the KernelContract shape with equations/proof_obligations/
    falsification_tests — never bypass pv). Content, machine-readable-beside-prose:
    (1) ROW SCHEMA: schema_version 1; shared mandatory core — method ("setfit"|"lora"), shots
    (8|16|32|64), seed (the ten contracted values), dataset revision + fingerprint, model
    revision(s), selection_manifest_hash (the sampled-ID pairing key), backend_identity (grammar
    `<device>:<implementation>:<kernel>`, read from execution per Ph4 D-12), host identity
    (hostname/os/arch), quality block (f_avg, macro_f1, per-class precision/recall/f1, mcc,
    confusion matrix, n_test_rows, ordered_labels ["none","against","favor"],
    ece_top_label_validation, brier_multiclass_validation, calibration_split fixed "validation"),
    resource block (train_wall_ms, cold_latency_ms + cold_measured_in_child_process: true,
    warm_latency_ms_median, throughput_rows_per_sec + throughput_batch_size + warmup_count,
    train_peak_rss_bytes + train_peak_rss_mechanism, inference_peak_rss_bytes +
    inference_peak_rss_mechanism, peak_rss_sample_interval_hz (present only when the mechanism is
    a sampled one), artifact_bytes, deployable_total_bytes);
    method evidence blocks — setfit: {evidence_table_hash, apr_artifact_sha256, lock {lock_hash,
    role, rule, lock_record_path}}; lora: {base_model_sha256, base_model_bytes, adapter_sha256,
    epochs_requested, epochs_completed, early_stopping_disabled: true, val_split: 0.0,
    no_selection_attestation: true, candidate_ledger_sha256, candidates_trained: 1,
    candidate_ledger_path}. State the D-06 EXCLUSION: no bootstrap or resampled field may exist in
    a row; rows are deny_unknown_fields so one cannot ride along.
    (1b) MODEL SIZE COMPARABILITY (review consensus item 8): `artifact_bytes` is the bytes of the
    artifact THIS method wrote (SetFit: the standalone APR; LoRA: the adapter alone).
    `deployable_total_bytes` is what a user must ship to serve the model — for SetFit it equals
    artifact_bytes (the APR is standalone); for LoRA it is base_model_bytes + adapter_bytes. Both
    fields are MANDATORY on every row. The contract states in prose that presenting LoRA's
    adapter-only `artifact_bytes` beside SetFit's standalone APR is a forbidden comparison, and
    that any cross-method size claim must use `deployable_total_bytes`.
    (1c) SELECTION-SAFETY EVIDENCE (review consensus item 5 — "a gate could pass while the claim is
    false" is this phase's own stated bar). A SetFit row's `lock.lock_hash` must equal the SHA-256
    of the bytes of a committed canonical lock-record file at `lock.lock_record_path` (relative to
    the benchmark dir); the report RECOMPUTES that digest from the file rather than trusting the
    row field. A LoRA row must reference an APPEND-ONLY candidate ledger at
    `candidate_ledger_path` — one JSONL line per training invocation for that (method, shots, seed)
    written BEFORE test evaluation, each line carrying {timestamp, selection_manifest_hash,
    epochs_requested, seed, config_hash} — with `candidate_ledger_sha256` over the ledger file's
    bytes and `candidates_trained` equal to its line count, which the contract requires to be
    exactly 1. State the RESIDUAL honestly in-contract: this raises selection safety from a
    self-asserted boolean to recomputable append-only evidence, but a producer that controls both
    the ledger and the rows can still emit a mutually consistent forgery; a cryptographic
    train-then-seal credential for the LoRA side is named as future work, not claimed here.
    (2) COMPLETENESS RULE: the expectation set is the contract-derived 80 cells (methods x shots x
    seeds enumerated HERE); the manifest must list all 80; report refuses on any missing,
    hash-failing, or unmatched cell; completeness is defined by manifest + contract, never a
    directory listing.
    (3) PAIRING RULE: per (shots, seed), the setfit and lora rows must carry an identical
    selection_manifest_hash.
    (4) LoRA NO-SELECTION ATTESTATION (Open Q3 option (b), adopted, STRENGTHENED per review): LoRA
    rows attest epochs_completed == epochs_requested, early stopping disabled, val_split 0.0, and
    exactly one trained candidate per cell — where "exactly one" is now checked against the
    recomputed candidate-ledger digest and line count of (1c), not against a boolean alone. The
    SetFitCredential seal is still NOT widened.
    (5) STATS EQUATIONS: per (method, shots) mean, (n-1) sample std, min/max over the 10 seeds; per
    shots the per-seed paired deltas (SetFit - LoRA on the same hash), mean delta, and the 95% CI
    `d_bar +/- t_crit * s_d / sqrt(10)` with the frozen literal t_crit copied from
    scripts/setfit_fixtures/claims_stats/t_critical.json (contract-resident constant, Ph1 D-14;
    coordinate with plan 05-04's frozen T_CRIT_975_DF9 — same number, two pinned copies compared by
    a test in 05-10).
    (6) RESOURCE PROTOCOL (success criterion 3, contracted boundaries — REWRITTEN per review
    consensus item 2, which found the previous labels stronger than the protocol supported):
    - COLD LATENCY is measured in a DEDICATED FRESH CHILD PROCESS that does nothing but load the
      written artifact and perform exactly ONE single-text classify. It is NOT the first classify
      inside the process that just finished training — that process's filesystem cache, allocator
      arenas and warm pages make it operationally warm. The row carries
      `cold_measured_in_child_process: true`; the contract forbids emitting a cold latency measured
      in a process that also trained.
    - WARM LATENCY = median of 10 single-text classifies after 3 warmup classifies (the apr bench
      warmup default), measured against the reloaded model.
    - THROUGHPUT = n_test_rows / wall over one full test-split batch pass at the declared batch size.
    - PEAK RSS is TWO distinct metrics, never one. `train_peak_rss_bytes` is the peak of the
      TRAINING process and is labelled as such; `inference_peak_rss_bytes` is the peak of the
      dedicated cold-measurement child. Reporting a single "peak RSS" is forbidden, because
      `/proc/self/status` VmHWM is process-CUMULATIVE — in a process that trains then reloads then
      infers it reports the training peak, not the inference peak.
    - MECHANISM STRINGS are mandatory per metric, and the contract declares which mechanisms are
      MUTUALLY COMPARABLE. Preferred for BOTH platforms, because it makes SetFit-vs-LoRA memory
      comparable: measure the child's true kernel high-water mark from the PARENT after the child
      exits — Linux reads the child's `/proc/<pid>/status` VmHWM before reap, or `getrusage`
      `ru_maxrss` for children; macOS reads the same `ru_maxrss` (both are true kernel HWMs, both
      reachable without `unsafe` via `/usr/bin/time -l` on macOS and `/usr/bin/time -v` on Linux
      around the spawned child). Mechanism strings: `child_max_rss_time_l` (macOS),
      `child_max_rss_vm_hwm` (Linux) — the contract declares these two MUTUALLY COMPARABLE, since
      both are exact kernel high-water marks of an equivalently-scoped child process.
      `sysinfo_sampled_<hz>` remains permitted ONLY as a fallback, MUST record the actual sampling
      interval in `peak_rss_sample_interval_hz`, and is declared a SAMPLED LOWER BOUND that is NOT
      comparable to a `child_max_rss_*` figure. A cross-method memory comparison between two
      different mechanism classes must be labelled at the point of comparison (enforced by the
      05-10 renderer).
    - Resource metrics are NEVER pooled across hosts (D-09 framing); training time = wall-clock of
      the training invocation; artifact/deployable sizes per (1b).
    NOTE for the executor: Gemini's review proposed `mach_task_basic_info` on macOS. That is
    rejected — this workspace sets `unsafe_code = "forbid"`, and the child-process `ru_maxrss`
    route above gives the same true-HWM semantics with no unsafe and the same shape on both
    platforms.
    (7) Quote PF-007 and PF-008 by id; state D-08 (estimation-first, no significance verdicts in
    claim language; p-values machine-readable detail only); REFERENCE
    tweet-eval-stance-benchmark-v1 (dataset identity, F_avg) and setfit-apr-v1 (artifact) rather
    than restating them.
    Then wire the Makefile: append the new file to the explicit $(CONTRACTS) list (after
    contracts/setfit-apr-v1.yaml), add `PHASE5_CONTRACTS := contracts/setfit-benchmark-claims-v1.yaml`
    and a `contract-audit-phase5` target scoped to it following the PHASE2_CONTRACTS precedent
    (rc captured on its own line, never through a pipe). Prove both gates can fail (Pitfall 5):
    induce one schema mutation in the contract (e.g. temporarily rename a required key), observe
    `make validate-contracts` (or the direct pv validate) go red, revert, observe green; record
    both runs.
  </action>
  <verify>
    <automated>target/release/pv validate contracts/setfit-benchmark-claims-v1.yaml && grep -v '^#' Makefile | grep -c 'setfit-benchmark-claims-v1' | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `pv validate contracts/setfit-benchmark-claims-v1.yaml` exits 0 with 0 errors
    - The contract enumerates the ten seeds 13,17,23,29,31,37,41,43,47,53 and both methods and all four shot counts under machine-readable keys (NOT in comments — the 05-05 T2 typed-set parity test parses these keys), so the 80-cell derivation is closed-form from this file
    - The contract contains the strings "PF-007" and "PF-008" and the frozen t literal matching claims_stats/t_critical.json
    - The row schema declares `train_peak_rss_bytes`, `inference_peak_rss_bytes`, both mechanism fields, `cold_measured_in_child_process`, `artifact_bytes`, `deployable_total_bytes`, `candidate_ledger_sha256`, `candidates_trained`, and `lock_record_path`
    - The resource section declares which mechanism strings are mutually comparable and states that `sysinfo_sampled_*` is a sampled lower bound not comparable to `child_max_rss_*`
    - The selection-safety section states the recomputation rule (lock digest from committed bytes; ledger digest + line count) AND names the residual forgery risk explicitly
    - Makefile $(CONTRACTS) includes the new file AND `make contract-audit-phase5` exists; the induced-red/reverted-green control is recorded in the SUMMARY with both rc values
  </acceptance_criteria>
  <done>Claims contract exists, pv-valid, Makefile-wired, falsification-proven</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BenchRow + MethodEvidence + RunManifest in aprender-train</name>
  <files>crates/aprender-train/src/train/setfit/bench_row.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-contrastive-data/src/manifest.rs (SelectionManifest envelope :169-178; from_bytes verify-before-return :248-272; schema_version refusal :313-316, 401-404; to_file_bytes pretty/canonical split :228-237)
    - crates/apr-cli/src/commands/eval/setfit.rs (EvalRow :111-143 — the field vocabulary; LockRow :158-171; value_bits convention: IEEE-754 bits beside every headline f64)
    - crates/aprender-train/src/train/setfit/mod.rs (module registration style, feature gating)
    - contracts/setfit-benchmark-claims-v1.yaml (Task 1 output — the schema this type implements)
  </read_first>
  <behavior>
    - A BenchRow round-trips bytes -> from_bytes -> to_file_bytes -> from_bytes with identical semantic_hash
    - from_bytes REFUSES: an unknown field (deny_unknown_fields), a wrong schema_version, a bit-flipped payload (digest mismatch), a setfit row missing its lock block, a lora row missing its attestation block — each with a DISTINCT typed error variant
    - RunManifest::expectation() derives exactly 80 unique cell keys and equals the contract-enumerated set by TYPED SET COMPARISON: the test deserializes the `include_str!`-ed contract with serde_yaml into typed `Vec<String>`/`Vec<u32>` fields (the `#[cfg(test)] ContractFile` struct convention thresholds.rs already uses), builds the Cartesian product, and asserts set equality AND cardinality equality against the Rust-derived set. Substring/`contains` checks are forbidden — a value sitting in a YAML comment must NOT satisfy this test
    - Two-sided parity negatives: an in-test MUTATED COPY of the contract string with (a) one EXTRA seed and (b) one DUPLICATED seed each make the parity assertion fail, proving it rejects extras and duplicates rather than only detecting absence
    - Recording the same cell twice with different hashes is a typed error (no silent overwrite); re-recording the IDENTICAL hash is idempotent and returns Ok (the resume path)
    - `expectation()` and every serialized listing iterate in the deterministic contract order (method, then shots ascending, then seed ascending); a test asserts the exact key sequence of the first and last five entries
    - A LoRA row missing `candidate_ledger_sha256`/`candidates_trained`, or a SetFit row missing `lock_record_path`, is unrepresentable (the field is non-Option in its MethodEvidence variant), so `from_bytes` rejects it with the missing-block error variant
  </behavior>
  <action>
    Create `bench_row.rs` under `crates/aprender-train/src/train/setfit/` (feature `setfit`,
    registered in mod.rs) implementing the Task 1 schema: `BenchRow` with the envelope discipline
    of SelectionManifest — `{ semantic_hash, volatile, payload }`, `#[serde(deny_unknown_fields)]`
    on every struct, `schema_version: u32` checked on parse, canonical compact bytes for the digest
    vs pretty bytes on disk, `from_bytes` verifying the SHA-256 BEFORE returning. Method tagging
    via a serde-tagged enum `MethodEvidence { Setfit { evidence_table_hash, apr_artifact_sha256,
    lock: BenchLockRef { lock_hash, role, rule, lock_record_path } }, Lora { base_model_sha256,
    base_model_bytes, adapter_sha256, epochs_requested, epochs_completed, early_stopping_disabled,
    val_split, no_selection_attestation, candidate_ledger_sha256, candidates_trained,
    candidate_ledger_path } }` — a missing block is unrepresentable, not null-padded (D-12), and
    every provenance field above is non-Option so a row cannot omit it. Headline f64 metrics carry a
    `_bits: u64` sibling per the EvalRow value_bits convention. `QualityBlock` and `ResourceBlock`
    structs match the contract field-for-field (05-08 and 05-09 will populate them). `RunManifest`:
    envelope + payload holding schema_version, the contract id string, the 80-cell expectation
    table (each entry: method, shots, seed, status pending|complete, row_sha256 when complete), a
    `record(cell, hash)` transition (typed error on re-record with a different hash; idempotent on
    identical re-record for resume), and `expectation()` derived in code from the same
    methods/shots/seeds constants and emitted in the deterministic contract order (method, then
    shots ascending, then seed ascending). Typed errors extend the crate's existing error taxonomy
    per its own conventions (no unwrap, no panic paths). Unit tests: the behaviors above, plus the
    TYPED-SET contract-parity test — deserialize the `include_str!`-ed
    contracts/setfit-benchmark-claims-v1.yaml with serde_yaml into `#[cfg(test)]` structs holding
    typed `methods: Vec<String>`, `shots: Vec<u32>`, `seeds: Vec<u32>` (the convention
    thresholds.rs's `#[cfg(test)] ContractFile` already establishes), build the Cartesian product,
    and assert SET equality AND cardinality equality against the Rust-derived expectation. Do NOT
    implement this as `contract_str.contains(&seed.to_string())`: the review found that form passes
    when the value sits in a comment, when the contract holds extras, and when Rust builds a
    differently-shaped product. Add the two mutated-copy negatives (an extra seed; a duplicated
    seed) proving the comparison rejects extras and duplicates, not merely absence.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit bench_row > /tmp/row.log 2>&1; rc=$?; tail -5 /tmp/row.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - bench_row.rs exists with `deny_unknown_fields` on every serde struct (grep count of deny_unknown_fields >= 4)
    - Five distinct refusal tests pass (unknown field, schema_version, digest, missing setfit lock, missing lora attestation), each asserting a different error variant
    - `RunManifest::expectation()` test asserts exactly 80 unique cells and TYPED-SET parity with the contract (serde_yaml parse, set + cardinality equality). Non-vacuity check, comment-filtered so plan prose copied into a comment cannot invalidate it: `grep -v '^[[:space:]]*//' <parity test file> | grep -c 'contains('` returns 0
    - Both mutated-contract negatives (extra seed, duplicate seed) fail the parity assertion in-test
    - An ordering test pins the deterministic key sequence; a re-record with the identical hash returns Ok and with a differing hash returns the typed error
    - `cargo test -p aprender-train --lib --features setfit bench_row` exits 0 and the filter matched > 0 tests (assert non-zero from the log line, CR-02)
  </acceptance_criteria>
  <done>Row + manifest types shipped with digest-verify-before-return and structural method tagging</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and files CREATED by this plan (newly-created — exclude from drift verification):

- Contract `contracts/setfit-benchmark-claims-v1.yaml` (all sections: row schema, completeness rule, pairing rule, LoRA no-selection attestation + candidate ledger, stats equations, resource protocol)
- Module `crates/aprender-train/src/train/setfit/bench_row.rs` with types `BenchRow`, `MethodEvidence` (`Setfit` / `Lora` variants), `BenchLockRef`, `QualityBlock`, `ResourceBlock`, `RunManifest`, and fns `RunManifest::expectation`, `RunManifest::record`, `BenchRow::from_bytes`, `BenchRow::to_file_bytes`
- New row/contract field names: `train_peak_rss_bytes`, `train_peak_rss_mechanism`, `inference_peak_rss_bytes`, `inference_peak_rss_mechanism`, `peak_rss_sample_interval_hz`, `cold_measured_in_child_process`, `deployable_total_bytes`, `base_model_bytes`, `candidate_ledger_sha256`, `candidates_trained`, `candidate_ledger_path`, `lock_record_path`
- Mechanism string constants: `child_max_rss_time_l`, `child_max_rss_vm_hwm`, `sysinfo_sampled_<hz>`
- Makefile: `PHASE5_CONTRACTS`, target `contract-audit-phase5`, `$(CONTRACTS)` append
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- INCORPORATED (consensus item 2): the resource protocol is rewritten — cold latency in a dedicated fresh child, train vs inference peak RSS as separate fields, comparable `child_max_rss_*` mechanisms preferred on both platforms, `sysinfo_sampled_*` demoted to a labelled lower bound with a recorded interval.
- INCORPORATED (consensus item 5): SetFit lock bytes are committed and the digest is recomputed; LoRA gains an append-only candidate ledger with a digest and a line count. The residual forgery risk is stated in-contract rather than papered over.
- INCORPORATED (consensus item 6): contract parity becomes a typed set+cardinality comparison with extra/duplicate negatives; substring checking is explicitly forbidden.
- INCORPORATED (consensus item 8): `artifact_bytes` / `deployable_total_bytes` / `base_model_bytes` split, with a contract prose rule forbidding adapter-only-vs-standalone-APR size comparisons.
- <!-- REVIEW REJECTED: Gemini Finding 3 ("Manifest Generation Deadlock and Content Hash Circularity") claims the run manifest cannot be pre-declared and proposes splitting `matrix.json` from `run_manifest.json`. REFUTED against this plan's own design: `expectation()` is derived IN CODE from the methods/shots/seeds constants (never from row hashes), each entry carries `status pending|complete` with `row_sha256` present only when complete, and `record(cell, hash)` is idempotent on identical re-record precisely to support resume — so there is no ordering cycle and a crash at cell 35 leaves a valid manifest with 35 complete and 45 pending entries. The concurrent-write half is handled by the wave structure: 05-11 and 05-12 write the same run-manifest file and are in different waves for exactly that reason. Splitting the type would add a second artifact and a second digest for no gained property. No change. -->
</review_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk row/manifest files -> library types | untrusted bytes; digest + schema verified before any value is readable |
| contract enumeration -> completeness gate | the 80-cell expectation is the anti-omission mechanism; it must be derivable from the contract, not a directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | row files | mitigate | semantic_hash over canonical bytes verified in from_bytes before return (SelectionManifest pattern) |
| T-05-05-02 | Repudiation (selective omission) | expectation set | mitigate | 80 cells enumerated in the contract and derived in code; include_str! parity test pins the two together |
| T-05-05-03 | Tampering (RNG smuggling) | row schema | mitigate | deny_unknown_fields everywhere; contract textually excludes bootstrap fields (D-06) |
| T-05-05-04 | Spoofing | method identity | mitigate | serde-tagged MethodEvidence — a lora row cannot present setfit evidence or vice versa (SAFE-03 spirit) |
| T-05-05-05 | Tampering | duplicate cell recording | mitigate | record() refuses a differing hash for an existing cell with a typed error |
| T-05-05-06 | Repudiation (unprovable selection safety, severity MEDIUM) | LoRA no-selection attestation | mitigate + accept residual | attestation is backed by an append-only candidate ledger whose digest and line count are recomputed by the 05-10 gate; the residual (a producer controlling both ledger and rows can forge consistently) is named in-contract rather than hidden |
| T-05-05-07 | Spoofing (incomparable size claim, severity MEDIUM) | artifact_bytes | mitigate | `deployable_total_bytes` mandatory on every row; contract forbids cross-method comparison on adapter-only bytes; 05-10 renderer enforces |
| T-05-05-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- pv validate 0 errors; make contract-audit-phase5 present and scoped
- bench_row suite green with non-zero matched tests
- Induced contract mutation observed red then reverted green
</verification>

<success_criteria>
- Contract + types agree via include_str! parity; five refusals distinct and green; Makefile wiring proven non-vacuous
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-05-SUMMARY.md` when done
</output>
