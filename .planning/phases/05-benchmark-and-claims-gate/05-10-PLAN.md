---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 10
type: execute
wave: 5
depends_on: ["05-09"]
files_modified:
  - crates/aprender-train/src/train/setfit/bench_gate.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/apr-cli/src/setfit_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/commands/setfit_bench.rs
  - contracts/aprender/binding.yaml
  - Makefile
autonomous: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "bench report refuses BEFORE any arithmetic on: a missing cell, a trimmed row, a mismatched sampled-ID pair, a hash-failing row, and a post-test-selected/attestation-violating cell — and each of the five doctored negatives is REFUSED in every cargo test (D-15)"
    - "All statistics are recomputed from stored rows with closed-form arithmetic only: per (method, shots) mean/(n-1) std/min/max; per shots the per-seed paired deltas, mean delta, and 95% CI via the frozen t constant — bit-exactly reproducible (EVAL-04, D-05/D-06)"
    - "The report never prints significance verdicts; claim language is estimation-first; p-values live only in the machine-readable detail (D-08)"
    - "Every new Make filter is provably non-vacuous (asserts matched-test floors; one induced red observed and reverted) — CR-02"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      provides: "Fail-closed verification pipeline + closed-form aggregation (library-side; the CLI is a shim)"
      contains: "verify_run"
    - path: "crates/apr-cli/src/commands/setfit_bench.rs"
      provides: "bench report subcommand: human + --json output"
    - path: "Makefile"
      provides: "bench suite targets with assert_tests_ran floors; contract-audit-phase5 extended with binding rows"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      to: "aprender_core::stats::hypothesis"
      via: "ttest_rel_f64 + T_CRIT_975_DF9 + paired CI helper — the only uncertainty machinery (no RNG)"
      pattern: "T_CRIT_975_DF9"
    - from: "contracts/aprender/binding.yaml"
      to: "setfit-benchmark-claims-v1 equations"
      via: "binding rows pointing at the shipped gate/stats functions — status implemented, zero BIND lines in the scoped audit"
---

<objective>
Ship the claims gate and `apr setfit bench report` (D-13/D-15): the library-side fail-closed
verification pipeline over manifest + rows, closed-form statistics recomputation, estimation-first
output, the five in-band doctored negatives, and the non-vacuous Make/binding wiring.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-04's "missing, selectively omitted, unmatched, or post-test-selected cells
invalidate the report" is THE phase-defining property; it must be structural and falsified, not
asserted.
Output: bench_gate library + report CLI + negatives + gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 3, Pattern 4; Pitfall 4, Pitfall 6; Security Domain threat table)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 3, 12; Shared Patterns: non-vacuous gates, status-never-through-a-pipe)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bench_gate — fail-closed verification + closed-form aggregation + five negatives</name>
  <files>crates/aprender-train/src/train/setfit/bench_gate.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/bench_row.rs (BenchRow/RunManifest/MethodEvidence — the input types; the expectation() derivation)
    - crates/aprender-core/src/stats/hypothesis.rs (ttest_rel_f64, T_CRIT_975_DF9, the paired-CI helper from 05-04)
    - contracts/setfit-benchmark-claims-v1.yaml (completeness/pairing/attestation rules + stats equations + the frozen t literal — the gate implements THIS, field for field)
    - crates/apr-cli/src/commands/eval/setfit.rs (LockRow role/rule semantics the setfit lock check reads)
  </read_first>
  <behavior>
    - verify_run(manifest, rows_dir) walks in order: manifest digest -> expectation equals the contract-derived 80 -> every expected cell recorded -> every row file present, digest-green, schema-green, manifest-hash-matching -> per (shots, seed) pairing on identical selection_manifest_hash -> setfit lock discipline (lock role/rule valid) and lora attestation (epochs_completed == requested, early_stopping_disabled, val_split 0.0) -> ONLY then returns a VerifiedRunSet; each failure is a DISTINCT typed error naming the cell
    - aggregate(verified) computes per (method, shots): mean, (n-1) std, min, max over exactly 10 seeds; per shots: 10 per-seed deltas (setfit - lora), mean delta, s_d, CI95 via T_CRIT_975_DF9; plus the p-value (detail only) via ttest_rel_f64 — and two invocations over the same rows produce bit-identical output (determinism test compares serialized f64 bits)
    - FIVE doctored negatives, each built in-test by mutating a programmatically-generated valid 80-row set (synthetic small values): (1) missing cell, (2) trimmed row (a required block removed), (3) mismatched sampled-ID hash in one pair, (4) bit-flipped row bytes, (5) post-test-selection (setfit lock rule violated / lora epochs_completed < requested) — each asserts ITS error variant, all running in every cargo test (no ignore, no feature beyond setfit)
    - aggregate is unreachable on an unverified set (type-state: aggregate takes VerifiedRunSet, constructible only by verify_run)
  </behavior>
  <action>
    Create `bench_gate.rs` (feature setfit, registered in mod.rs) implementing the behavior block.
    The verification order is the contract's rule order — refuse early, never aggregate partial
    data. `VerifiedRunSet` has no public constructor (the 03/04 typestate house pattern) so the
    stats cannot be run on an unverified directory even by a future caller. Aggregation uses ONLY
    the 05-04 f64 helpers (no local mean/std reimplementation if hypothesis.rs shipped them; if
    aggregation helpers were deliberately left to this module by 05-04, implement them here once,
    f64, and note the siting). Include the cross-pin test: the code's t constant equals the
    contract's frozen literal (parse the claims contract via include_str! — a drifted copy turns
    red). Build the synthetic valid row-set builder as a test helper (also reused by 05-13's
    determinism check if convenient). No RNG imports anywhere in this module.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit bench_gate > /tmp/bg.log 2>&1; rc=$?; tail -5 /tmp/bg.log; test $rc -eq 0 && grep -E 'test result.*[1-9][0-9]* passed' /tmp/bg.log > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - bench_gate.rs contains verify_run + aggregate with VerifiedRunSet lacking a public constructor (grep confirms no `pub fn new` on it)
    - Five negative tests exist with five DISTINCT asserted error variants; the suite runs them in a default `cargo test -p aprender-train --lib --features setfit` invocation
    - The bit-identical determinism test passes (two aggregations compared by f64 bits)
    - The t-constant cross-pin test (code vs contract literal) passes
  </acceptance_criteria>
  <done>The gate refuses all five dishonesty shapes and aggregates deterministically</done>
</task>

<task type="auto">
  <name>Task 2: bench report CLI — human + --json, estimation-first</name>
  <files>crates/apr-cli/src/setfit_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/commands/setfit_bench.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/setfit_bench.rs (the 05-09 run/record structure this extends)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (verify_run/aggregate signatures)
    - contracts/setfit-benchmark-claims-v1.yaml (D-08 language rule; D-09 framing rule: per-host resource presentation, never pooled)
  </read_first>
  <action>
    Add `BenchCommands::Report { bench_dir, json, out (optional file for the JSON payload) }` +
    dispatch. The adapter reads manifest + rows (bounded reads), calls verify_run then aggregate,
    and renders: (a) human tables — per (method, shots) quality mean ± std [min, max]; per shots
    the paired delta with CI95; resource metrics grouped BY HOST/backend identity with an explicit
    "as-deployed method costs; hosts differ by design and are never averaged together" framing
    line (D-09); (b) --json — the full machine-readable detail including per-seed deltas and
    p-values. Claim language is estimation-first: the renderer contains no
    "significant/significantly better/worse" strings; add a unit test rendering a full synthetic
    report and asserting the output contains no "significant" substring (case-insensitive) while
    containing "95% CI". On ANY verify_run failure, print the typed refusal (cell-naming) and exit
    non-zero — no partial tables ever. Refusal messages name the remedy (which cell, what to
    re-run).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit setfit_bench > /tmp/br.log 2>&1; rc=$?; tail -5 /tmp/br.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The no-"significant" rendering test passes; the human output test shows the D-09 framing line
    - A refused run set produces exit-nonzero with a cell-naming message and ZERO table output (test asserts absence of the table header in refusal output)
    - --json output round-trips through serde and contains per_seed_deltas and p_value fields under a detail key
  </acceptance_criteria>
  <done>Report renders verified data only, estimation-first, both formats</done>
</task>

<task type="auto">
  <name>Task 3: Make gates + binding rows, all non-vacuous</name>
  <files>Makefile, contracts/aprender/binding.yaml</files>
  <read_first>
    - Makefile (:2122-2168 setfit-tests recipe — rc-on-its-own-line + assert_tests_ran; :2259-2264 measured-floor comment table; the contract-audit-phase5 target from 05-05)
    - contracts/aprender/binding.yaml (:1349-1371 — the 04-19 backend_identity resolution and the row format; binding rows are BARE contract filenames, status implemented|partial|not_implemented|pending)
  </read_first>
  <action>
    Add Make targets: `setfit-bench-tests` running the three new suites (aprender-train bench_row +
    bench_gate + bench_metrics filters; apr-cli setfit_bench filter) each with rc captured on its
    own line, CARGO_INCREMENTAL=0, log-to-file, tail, and an assert_tests_ran floor MEASURED from
    the actual run (record the measured counts in the comment-table style). Wire into tier3 (heavy)
    and add the fast subset to tier2 only if its wall-clock is < 5 s measured — otherwise tier3
    only, noted. Add binding rows to contracts/aprender/binding.yaml for the claims contract's
    equations (stats equations -> aprender-core hypothesis/calibration fns; completeness/pairing ->
    bench_gate::verify_run; row schema -> bench_row types), status `implemented`, bare contract
    filename in the contract: field (the Phase 2 trap: a ../ prefix parses and binds nothing).
    Extend `contract-audit-phase5` to run the scoped binding audit over the claims contract and
    require zero BIND- lines. Prove non-vacuity twice (CR-02): (1) temporarily rename one bench
    test and observe the floor fail; revert. (2) temporarily point one binding row at a nonexistent
    symbol and observe the audit refuse; revert. Record all four runs (red, green, red, green) with
    rc values in the SUMMARY.
  </action>
  <verify>
    <automated>make setfit-bench-tests > /tmp/mbt.log 2>&1; rc=$?; tail -5 /tmp/mbt.log; test $rc -eq 0 && make contract-audit-phase5 > /tmp/cap5.log 2>&1; rc2=$?; tail -3 /tmp/cap5.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `make setfit-bench-tests` exits 0 with per-suite assert_tests_ran floors whose numbers were measured (comment table updated)
    - `make contract-audit-phase5` exits 0 with zero BIND- lines and covers setfit-benchmark-claims-v1
    - Both induced-red controls recorded with rc values (floor red on rename; audit red on dead symbol), both reverted green
    - `grep -v '^#' Makefile | grep -c 'setfit-bench-tests'` returns non-zero
  </acceptance_criteria>
  <done>Gates wired, floors measured, falsification proven both ways</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark directory -> report | fully untrusted: rows/manifest may be doctored, omitted, transported; verify_run is the boundary guard |
| aggregation -> published claims | arithmetic must be closed-form and deterministic or "exactly recompute" is false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | doctored rows | mitigate | five in-band negatives in every cargo test; digest + schema + manifest-hash triple check per row |
| T-05-10-02 | Repudiation (selective omission) | expectation set | mitigate | contract-derived 80 cells compared to manifest before any read of rows |
| T-05-10-03 | Elevation (post-test selection) | lock/attestation rules | mitigate | setfit lock rule + lora epochs/early-stop attestation checked per pair; negative (5) pins it |
| T-05-10-04 | Tampering (RNG) | aggregation | mitigate | VerifiedRunSet typestate; only 05-04 closed-form helpers; bit-identical determinism test |
| T-05-10-05 | Information disclosure (verdict laundering) | report language | mitigate | no-"significant" rendering test; p-values detail-only (D-08) |
| T-05-10-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- bench_gate + setfit_bench suites green; make setfit-bench-tests + contract-audit-phase5 green with zero BIND lines
- All four induced-control runs recorded
</verification>

<success_criteria>
- The claims gate is fail-closed, falsified five ways, deterministic, estimation-first, and wired into non-vacuous gates
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-10-SUMMARY.md` when done
</output>
