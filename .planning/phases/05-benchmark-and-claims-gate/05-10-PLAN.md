---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 10
type: execute
wave: 5
depends_on: ["05-09"]
files_modified:
  - crates/aprender-train/src/train/setfit/bench_gate.rs
  - crates/aprender-train/src/train/setfit/mod.rs
  - crates/apr-cli/src/setfit_commands.rs
  - crates/apr-cli/src/dispatch_analysis.rs
  - crates/apr-cli/src/commands/setfit_bench.rs
  - contracts/aprender/binding.yaml
  - Makefile
autonomous: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "bench report refuses BEFORE any arithmetic on: a missing cell, a trimmed row, a mismatched sampled-ID pair, a hash-failing row, a post-test-selected/attestation-violating cell, and a forged/multi-entry candidate ledger — and each of the SIX doctored negatives is REFUSED in every cargo test (D-15)"
    - "Provenance is RECOMPUTED, not trusted: verify_run re-derives each SetFit row's lock_hash from the committed locks/*.lock.json bytes and each LoRA row's candidate_ledger_sha256 + line count from the committed ledger/*.jsonl bytes; a row field that disagrees with the file it names is a refusal (review consensus item 5)"
    - "All statistics are recomputed from stored rows with closed-form arithmetic only: per (method, shots) mean/(n-1) std/min/max; per shots the per-seed paired deltas, mean delta, and 95% CI via the frozen t constant — bit-exactly reproducible (EVAL-04, D-05/D-06)"
    - "A zero-variance paired delta set (all ten deltas identical, including all-zero) surfaces the typed ZeroVarianceDifferences case with a point estimate and an explicit no-CI marker — never a NaN, never an infinite CI, never a serde-null in the JSON payload"
    - "The report never prints significance verdicts; claim language is estimation-first; p-values live only in the machine-readable detail (D-08)"
    - "Resource comparisons are mechanism-labelled at the point of comparison: any table putting a SetFit figure beside a LoRA figure prints both mechanism strings, and any pairing of a sampled lower bound with an exact kernel high-water mark carries an explicit incomparability note (review consensus item 2)"
    - "Model-size presentation uses deployable_total_bytes for any cross-method claim; adapter-only artifact_bytes is shown only in the per-method detail, labelled as the adapter (review consensus item 8)"
    - "Report row ordering is deterministic (method, then shots ascending, then seed ascending); cell keys are unique so no equal-comparing tie can arise, and two runs emit the same order"
    - { statement: "An empty rows directory, a manifest with zero complete cells, or a manifest whose expectation set is not exactly the contract-derived 80 are each a typed refusal before any arithmetic — the report has no partial-data mode", verification: backstop }
  prohibitions:
    - "The report must never present a cross-method comparison whose two sides were measured by different mechanisms or on different hosts without labelling that difference at the point of comparison — a technically-true table that reads as a like-for-like benchmark is the failure mode this phase exists to prevent."
    - "The report must never present LoRA's adapter-only artifact_bytes as the method's deployable size beside SetFit's standalone APR."
    - "No number may appear in any report output that cannot be recomputed from the committed rows, locks, and ledgers alone."
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      provides: "Fail-closed verification pipeline + closed-form aggregation (library-side; the CLI is a shim)"
      contains: "verify_run"
    - path: "crates/apr-cli/src/commands/setfit_bench.rs"
      provides: "bench report subcommand: human + --json output"
    - path: "Makefile"
      provides: "bench suite targets with assert_tests_ran floors; contract-audit-phase5 extended with binding rows"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/bench_gate.rs"
      to: "aprender_core::stats::hypothesis"
      via: "ttest_rel_f64 + T_CRIT_975_DF9 + paired CI helper — the only uncertainty machinery (no RNG)"
      pattern: "T_CRIT_975_DF9"
    - from: "contracts/aprender/binding.yaml"
      to: "setfit-benchmark-claims-v1 equations"
      via: "binding rows pointing at the shipped gate/stats functions — status implemented, zero BIND lines in the scoped audit"
---

<objective>
Ship the claims gate and `apr setfit bench report` (D-13/D-15): the library-side fail-closed
verification pipeline over manifest + rows, closed-form statistics recomputation, estimation-first
output, the five in-band doctored negatives, and the non-vacuous Make/binding wiring.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-04's "missing, selectively omitted, unmatched, or post-test-selected cells
invalidate the report" is THE phase-defining property; it must be structural and falsified, not
asserted.
Output: bench_gate library + report CLI + negatives + gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pattern 3, Pattern 4; Pitfall 4, Pitfall 6; Security Domain threat table)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 3, 12; Shared Patterns: non-vacuous gates, status-never-through-a-pipe)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bench_gate — fail-closed verification + closed-form aggregation + five negatives</name>
  <files>crates/aprender-train/src/train/setfit/bench_gate.rs, crates/aprender-train/src/train/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/bench_row.rs (BenchRow/RunManifest/MethodEvidence — the input types; the expectation() derivation)
    - crates/aprender-core/src/stats/hypothesis.rs (ttest_rel_f64, T_CRIT_975_DF9, the paired-CI helper from 05-04)
    - contracts/setfit-benchmark-claims-v1.yaml (completeness/pairing/attestation rules + stats equations + the frozen t literal — the gate implements THIS, field for field)
    - crates/apr-cli/src/commands/eval/setfit.rs (LockRow role/rule semantics the setfit lock check reads)
  </read_first>
  <behavior>
    - verify_run(manifest, rows_dir) walks in order: manifest digest -> expectation equals the contract-derived 80 -> every expected cell recorded -> every row file present, digest-green, schema-green, manifest-hash-matching -> per (shots, seed) pairing on identical selection_manifest_hash -> PROVENANCE RECOMPUTATION: for each setfit row, read the bytes at `lock.lock_record_path` and assert SHA-256(bytes) == `lock.lock_hash` (plus lock role/rule valid); for each lora row, read the bytes at `candidate_ledger_path`, assert SHA-256(bytes) == `candidate_ledger_sha256`, assert its JSONL line count == `candidates_trained` == 1, and assert the ledger's recorded selection_manifest_hash equals the row's -> lora attestation (epochs_completed == requested, early_stopping_disabled, val_split 0.0) -> ONLY then returns a VerifiedRunSet; each failure is a DISTINCT typed error naming the cell AND the file whose bytes disagreed
    - aggregate(verified) computes per (method, shots): mean, (n-1) std, min, max over exactly 10 seeds; per shots: 10 per-seed deltas (setfit - lora), mean delta, s_d, CI95 via T_CRIT_975_DF9; plus the p-value (detail only) via ttest_rel_f64 — and two invocations over the same rows produce bit-identical output (determinism test compares serialized f64 bits)
    - A zero-variance delta set (s_d == 0.0) propagates 05-04's typed `ZeroVarianceDifferences` into an explicit `ci95: null_reason = "zero_variance"` shape in the aggregate — the point estimate is still reported, the CI is structurally absent with a stated reason, and NO non-finite f64 is ever serialized (Ph3 CR-03: serde_json renders non-finite f64 as `null`, which would read as a missing measurement rather than a degenerate one)
    - Aggregation iterates in the deterministic contract order (method, then shots ascending, then seed ascending), asserted by a test on the emitted key sequence
    - SIX doctored negatives, each built in-test by mutating a programmatically-generated valid 80-row set (synthetic small values): (1) missing cell, (2) trimmed row (a required block removed), (3) mismatched sampled-ID hash in one pair, (4) bit-flipped row bytes, (5) post-test-selection (setfit lock rule violated / lora epochs_completed < requested), (6) FORGED PROVENANCE — a lora ledger file carrying TWO candidate lines while the row still claims `candidates_trained: 1`, AND (as a sub-case in the same negative) a setfit lock file whose bytes were edited so the recomputed digest no longer matches the row's `lock_hash` — each asserts ITS error variant, all running in every cargo test (no ignore, no feature beyond setfit)
    - aggregate is unreachable on an unverified set (type-state: aggregate takes VerifiedRunSet, constructible only by verify_run)
  </behavior>
  <action>
    Create `bench_gate.rs` (feature setfit, registered in mod.rs) implementing the behavior block.
    The verification order is the contract's rule order — refuse early, never aggregate partial
    data. State the RESIDUAL honestly in the module's `//!` doc, matching the claims contract's own
    wording: recomputing lock and ledger digests raises selection safety from a self-asserted
    boolean to append-only, recomputable evidence, but a producer that controls the rows AND the
    lock/ledger files can still emit a mutually consistent forgery; the six negatives prove
    detection of INCONSISTENT evidence, not of truthful provenance. Do not overstate what the gate
    proves — this is the review's sharpest finding and the phase's own stated bar ("a gate could
    pass while the claim is false"). `VerifiedRunSet` has no public constructor (the 03/04 typestate house pattern) so the
    stats cannot be run on an unverified directory even by a future caller. Aggregation uses ONLY
    the 05-04 f64 helpers (no local mean/std reimplementation if hypothesis.rs shipped them; if
    aggregation helpers were deliberately left to this module by 05-04, implement them here once,
    f64, and note the siting). Include the cross-pin test: the code's t constant equals the
    contract's frozen literal (parse the claims contract via include_str! — a drifted copy turns
    red). Build the synthetic valid row-set builder as a test helper (also reused by 05-13's
    determinism check if convenient). No RNG imports anywhere in this module.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit bench_gate > /tmp/bg.log 2>&1; rc=$?; tail -5 /tmp/bg.log; test $rc -eq 0 && grep -E 'test result.*[1-9][0-9]* passed' /tmp/bg.log > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - bench_gate.rs contains verify_run + aggregate with VerifiedRunSet lacking a public constructor (grep confirms no `pub fn new` on it)
    - SIX negative tests exist with SIX DISTINCT asserted error variants; the suite runs them in a default `cargo test -p aprender-train --lib --features setfit` invocation
    - The provenance-recomputation step reads bytes from disk (grep shows the lock and ledger file reads inside verify_run) and its two forged-provenance sub-cases both refuse
    - The bit-identical determinism test passes (two aggregations compared by f64 bits)
    - The zero-variance aggregate test passes: the serialized zero-variance payload contains the key `null_reason` with value `zero_variance`, contains no `ci95` numeric bounds, and a JSON parse walks every f64 in the payload asserting `is_finite()` — assert this by parsing, not by grepping for the strings NaN/Infinity (a substring grep here would collide with the `null_reason` key itself)
    - The deterministic-ordering test pins the emitted key sequence
    - The t-constant cross-pin test (code vs contract literal) passes
  </acceptance_criteria>
  <done>The gate refuses all five dishonesty shapes and aggregates deterministically</done>
</task>

<task type="auto">
  <name>Task 2: bench report CLI — human + --json, estimation-first</name>
  <files>crates/apr-cli/src/setfit_commands.rs, crates/apr-cli/src/dispatch_analysis.rs, crates/apr-cli/src/commands/setfit_bench.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/setfit_bench.rs (the 05-09 run/record structure this extends)
    - crates/aprender-train/src/train/setfit/bench_gate.rs (verify_run/aggregate signatures)
    - contracts/setfit-benchmark-claims-v1.yaml (D-08 language rule; D-09 framing rule: per-host resource presentation, never pooled)
  </read_first>
  <action>
    Add `BenchCommands::Report { bench_dir, json, out (optional file for the JSON payload) }` +
    dispatch. The adapter reads manifest + rows (bounded reads), calls verify_run then aggregate,
    and renders: (a) human tables — per (method, shots) quality mean ± std [min, max]; per shots
    the paired delta with CI95 (and, for a zero-variance delta set, the point estimate plus
    `CI unavailable (zero variance)` rather than a number); resource metrics grouped BY
    HOST/backend identity with an explicit "as-deployed method costs; hosts differ by design and are
    never averaged together" framing line (D-09).
    RESOURCE LABELLING (review consensus item 2 — the numbers must not read stronger than the
    protocol): every resource table prints, per column, the mechanism string and the measurement
    scope (`train peak` vs `inference peak`, `cold (fresh child)` vs `warm (median of 10 after 3)`).
    When two figures being shown side by side come from DIFFERENT mechanism classes — e.g. a
    `sysinfo_sampled_*` lower bound beside a `child_max_rss_*` exact high-water mark — the renderer
    emits an explicit incomparability note on that row (`mechanisms differ: sampled lower bound vs
    exact kernel high-water mark — not a like-for-like comparison`). Peak RSS is NEVER rendered as a
    single unlabelled "peak memory" number.
    SIZE LABELLING (review consensus item 8): any cross-method size row uses
    `deployable_total_bytes`; LoRA's adapter-only `artifact_bytes` appears only in the per-method
    detail, labelled "adapter only". A test asserts the cross-method size table's header names
    `deployable` and that the adapter-only figure never appears in it.
    (b) --json — the full machine-readable detail including per-seed deltas, p-values, every
    mechanism string, and both size fields. Claim language is estimation-first: the renderer contains no
    "significant/significantly better/worse" strings; add a unit test rendering a full synthetic
    report and asserting the output contains no "significant" substring (case-insensitive) while
    containing "95% CI". On ANY verify_run failure, print the typed refusal (cell-naming) and exit
    non-zero — no partial tables ever. Refusal messages name the remedy (which cell, what to
    re-run).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib --features setfit setfit_bench > /tmp/br.log 2>&1; rc=$?; tail -5 /tmp/br.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The no-"significant" rendering test passes; the human output test shows the D-09 framing line
    - A refused run set produces exit-nonzero with a cell-naming message and ZERO table output (test asserts absence of the table header in refusal output)
    - Every resource column in the rendered output carries its mechanism string and measurement scope; a synthetic report mixing a `sysinfo_sampled_10hz` figure with a `child_max_rss_vm_hwm` figure renders the incomparability note (test asserts the note's presence, and a same-mechanism control asserts its ABSENCE — two-sided, so the note is not unconditional boilerplate)
    - The cross-method size table uses `deployable_total_bytes`; a test asserts LoRA's adapter-only `artifact_bytes` value does not appear in that table's rows
    - A zero-variance synthetic delta set renders `CI unavailable (zero variance)` and its JSON carries `null_reason`
    - --json output round-trips through serde and contains per_seed_deltas and p_value fields under a detail key
  </acceptance_criteria>
  <done>Report renders verified data only, estimation-first, both formats</done>
</task>

<task type="auto">
  <name>Task 3: Make gates + binding rows, all non-vacuous</name>
  <files>Makefile, contracts/aprender/binding.yaml</files>
  <read_first>
    - Makefile (:2122-2168 setfit-tests recipe — rc-on-its-own-line + assert_tests_ran; :2259-2264 measured-floor comment table; the contract-audit-phase5 target from 05-05)
    - contracts/aprender/binding.yaml (:1349-1371 — the 04-19 backend_identity resolution and the row format; binding rows are BARE contract filenames, status implemented|partial|not_implemented|pending)
  </read_first>
  <action>
    Add Make targets: `setfit-bench-tests` running the three new suites (aprender-train bench_row +
    bench_gate + bench_metrics filters; apr-cli setfit_bench filter) each with rc captured on its
    own line, CARGO_INCREMENTAL=0, log-to-file, tail, and an assert_tests_ran floor MEASURED from
    the actual run (record the measured counts in the comment-table style). Wire into tier3 (heavy)
    and add the fast subset to tier2 only if its wall-clock is < 5 s measured — otherwise tier3
    only, noted. Add binding rows to contracts/aprender/binding.yaml for the claims contract's
    equations (stats equations -> aprender-core hypothesis/calibration fns; completeness/pairing ->
    bench_gate::verify_run; row schema -> bench_row types), status `implemented`, bare contract
    filename in the contract: field (the Phase 2 trap: a ../ prefix parses and binds nothing).
    Extend `contract-audit-phase5` to run the scoped binding audit over the claims contract and
    require zero BIND- lines. Prove non-vacuity twice (CR-02): (1) temporarily rename one bench
    test and observe the floor fail; revert. (2) temporarily point one binding row at a nonexistent
    symbol and observe the audit refuse; revert. Record all four runs (red, green, red, green) with
    rc values in the SUMMARY.
  </action>
  <verify>
    <automated>make setfit-bench-tests > /tmp/mbt.log 2>&1; rc=$?; tail -5 /tmp/mbt.log; test $rc -eq 0 && make contract-audit-phase5 > /tmp/cap5.log 2>&1; rc2=$?; tail -3 /tmp/cap5.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `make setfit-bench-tests` exits 0 with per-suite assert_tests_ran floors whose numbers were measured (comment table updated)
    - `make contract-audit-phase5` exits 0 with zero BIND- lines and covers setfit-benchmark-claims-v1
    - Both induced-red controls recorded with rc values (floor red on rename; audit red on dead symbol), both reverted green
    - `grep -v '^#' Makefile | grep -c 'setfit-bench-tests'` returns non-zero
  </acceptance_criteria>
  <done>Gates wired, floors measured, falsification proven both ways</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark directory -> report | fully untrusted: rows/manifest may be doctored, omitted, transported; verify_run is the boundary guard |
| aggregation -> published claims | arithmetic must be closed-form and deterministic or "exactly recompute" is false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | doctored rows | mitigate | five in-band negatives in every cargo test; digest + schema + manifest-hash triple check per row |
| T-05-10-02 | Repudiation (selective omission) | expectation set | mitigate | contract-derived 80 cells compared to manifest before any read of rows |
| T-05-10-03 | Elevation (post-test selection) | lock/attestation rules | mitigate | setfit lock rule + lora epochs/early-stop attestation checked per pair; negative (5) pins it |
| T-05-10-04 | Tampering (RNG) | aggregation | mitigate | VerifiedRunSet typestate; only 05-04 closed-form helpers; bit-identical determinism test |
| T-05-10-05 | Information disclosure (verdict laundering) | report language | mitigate | no-"significant" rendering test; p-values detail-only (D-08) |
| T-05-10-06 | Repudiation (forged provenance, severity MEDIUM) | lock/ledger evidence | mitigate + accept residual | digests recomputed from the committed lock and ledger bytes; sixth doctored negative covers a multi-entry ledger and an edited lock file; the residual (consistent forgery by a producer controlling both) is stated in the module doc and the contract |
| T-05-10-07 | Information disclosure (misleading like-for-like comparison, severity MEDIUM) | resource + size rendering | mitigate | mechanism string + measurement scope printed per column; incomparability note on mixed-mechanism rows, proven two-sided; cross-method size claims use deployable_total_bytes only |
| T-05-10-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files CREATED by this plan (newly-created — exclude from drift verification):

- Module `crates/aprender-train/src/train/setfit/bench_gate.rs` with `verify_run`, `aggregate`, the opaque `VerifiedRunSet`, the six doctored-negative error variants, and the synthetic valid-row-set test builder
- Aggregate field `ci95.null_reason` (value `"zero_variance"`)
- `BenchCommands::Report { bench_dir, json, out }` + its dispatch arm + the renderer in crates/apr-cli/src/commands/setfit_bench.rs
- Renderer strings: the D-09 per-host framing line, and the mixed-mechanism incomparability note
- Makefile target `setfit-bench-tests` (+ its measured assert_tests_ran floors and comment table); `contract-audit-phase5` extension
- Binding rows in contracts/aprender/binding.yaml for the claims contract's equations
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- INCORPORATED (consensus item 5, VERIFIED MEDIUM): verify_run recomputes lock and ledger digests from committed bytes instead of reading row fields, and a sixth doctored negative covers forged provenance. The residual is stated rather than hidden.
- INCORPORATED (consensus item 2): the renderer labels mechanism and scope per column and emits a two-sided-proven incomparability note on mixed-mechanism rows.
- INCORPORATED (consensus item 8): cross-method size claims use `deployable_total_bytes`; adapter-only bytes are confined to per-method detail.
- INCORPORATED (consensus item 3, downstream half): a zero-variance delta set renders an explicit no-CI marker rather than a non-finite number.
- <!-- REVIEW REJECTED: Gemini Finding 1 / plan-matrix row 05-10 asks to change the F_avg class indices from `&[1,2]` to `&[0,2]`. REFUTED — the ordered labels are ["none","against","favor"] (matching HuggingFace tweet_eval stance ClassLabel), so `[1,2]` IS (F1_against + F1_favor)/2. Applying the "fix" would invert the phase's headline metric. See 05-08's fuller rejection note; the indices are UNCHANGED here and in 05-08. -->
- <!-- REVIEW REJECTED: Gemini's row asks to "verify 5 doctored negative fixtures against the decoupled matrix specification". The matrix.json/run_manifest.json split it presupposes is refuted in 05-05; the negatives run against the single run-manifest design and there are now six of them. -->
</review_notes>

<verification>
- bench_gate + setfit_bench suites green; make setfit-bench-tests + contract-audit-phase5 green with zero BIND lines
- All four induced-control runs recorded
</verification>

<success_criteria>
- The claims gate is fail-closed, falsified five ways, deterministic, estimation-first, and wired into non-vacuous gates
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-10-SUMMARY.md` when done
</output>
