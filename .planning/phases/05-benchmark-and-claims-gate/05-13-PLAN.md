---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 13
type: execute
# Wave and depends_on DELIBERATELY UNCHANGED across the D-19 replan. 05-11 was repurposed into
# the matrix retarget rather than vacated, so all three named dependencies still produce something
# this plan consumes: 05-10 the gate, 05-11 the 40-cell scope and the single-method renderer,
# 05-12 the evidence set.
wave: 8
depends_on: ["05-10", "05-11", "05-12"]
files_modified:
  - benchmarks/tweeteval-stance/report.md
  - benchmarks/tweeteval-stance/report.json
  - crates/apr-cli/src/commands/output_verification.rs
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]

must_haves:
  truths:
    - "apr setfit bench report ran over the real 40-cell SetFit set: gate green, statistics recomputed, human and JSON outputs committed — and a second run produced a bit-identical JSON payload, so EVAL-04's 'exactly recompute' is demonstrated rather than asserted"
    - "The report presents SINGLE-METHOD statistics: per shot level the official F_avg and per-class metrics as mean, sample (n-1) std, min and max over the ten contracted seeds, plus the seed-dispersion 95% interval on the frozen t constant, labelled as an interval over the ten contracted seeds at fixed data and protocol"
    - "The report is estimation-first — point estimates and dispersion, never a significance verdict; the test statistic and its p-value live only in the machine-readable detail"
    - "NO committed report artifact states or implies a second-method result. This is enforced three ways rather than asserted: structurally, because a second method's cell is refused by the gate's existing expectation-set and slot checks under the 40-cell active scope; at render time, by 05-11's must-not-match case table, which the mandated single-method note is ALSO pinned against so the note cannot trip the gate; and here, by a negative control over the committed bytes with a positive half so it cannot pass vacuously"
    - "The two peak-RSS figures are presented separately with their mechanism strings, and the sampled train-peak figure carries its lower-bound label in the committed report — the asymmetry 05-12 tallied over all 40 rows is visible to a reader, not averaged into one peak number"
    - "apr qa's encoder-only refusal names apr validate --quality and apr eval at the refusal site (D-11's in-scope half); the capability extension remains a deferred ticket"
    - "The phase's gates all pass at the closing SHA: setfit-bench-tests, contract-audit-phase5, contract-audit-phase4, pv validate over the touched contracts, setfit-all-tests, and the gate's doctored negatives in a bare cargo test"
    - "The closing evidence map states, per requirement id, whether the id is met in full or met by a NARROWER deliverable under the 2026-09-07 amendment — an amended requirement is never filed as satisfied without saying which clause is deferred"
  prohibitions:
    - "No number may appear in report.md or report.json that cannot be recomputed from the committed rows and lock records alone. A claim a future auditor cannot reproduce from this repository is not a claim this phase is allowed to publish."
    - "No report artifact may state or imply a SetFit-versus-a-second-method result, in prose, in a table, in a section header, or in an empty section that merely looks like one was attempted."
    - "The closing audit must never convert an evidence pointer into a checked requirement box, and must never mark an amended requirement complete. Flipping requirement or phase-completion state is the verifier's act, not this plan's — six recorded occurrences of the false-completion defect."
    - "A destructive negative control never touches the real benchmark directory. Controls run on a copy, and the real directory is re-verified afterwards."
  artifacts:
    - path: "benchmarks/tweeteval-stance/report.md"
      provides: "The human-readable single-method claims report"
    - path: "benchmarks/tweeteval-stance/report.json"
      provides: "The machine-readable detail — per-cell values, the seed-dispersion interval, mechanism strings, the test statistic and p-value as detail only"
  key_links:
    - from: "benchmarks/tweeteval-stance/report.json"
      to: "benchmarks/tweeteval-stance/rows/"
      via: "verify_run — every number recomputable from the committed rows and lock records alone"
    - from: "the committed report"
      to: "contracts/setfit-benchmark-claims-v1.yaml"
      via: "the 40-cell active scope: a report can only exist over a complete, hash-verified, in-scope set"
---

<objective>
Close the phase: run the fail-closed report over the real 40-cell SetFit evidence set, prove exact
recomputability against the committed bytes, prove the no-comparison prohibition holds in the
published artifact, land the D-11 refusal-message improvement, and run the closing audit.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: this is the milestone's claims deliverable — the auditable, recomputable SetFit evidence
set EVAL-01..EVAL-05 describe under their 2026-09-07 amended scope.
Output: committed report pair, the qa message fix, and a closing evidence map for the verifier that
states plainly which requirements are met narrowly and which clause each defers.
</objective>

<amended_requirements>
## What "closing the phase" means after D-19

The phase still claims all five EVAL ids. Two of them were amended in SCOPE on 2026-09-07, not
dropped, and this plan's closing map must say so rather than filing a narrower deliverable against
the original text.

| Id | Status this plan can evidence | The clause that is NOT evidenced |
|----|-------------------------------|----------------------------------|
| EVAL-01 | Met in full — official F_avg, per-class metrics, three-class macro-F1, MCC, confusion matrix, validation-only calibration diagnostics bound to explicit ordered labels | — |
| EVAL-02 | Met NARROWLY — all 40 SetFit shot/seed cells, each bound to a recorded selection manifest | The second arm. Deferred as `D-ITEM-05-15` |
| EVAL-03 | Met in full — one machine-readable row per cell with the contracted core and evidence block | — |
| EVAL-04 | Met NARROWLY — exact recomputation of means, dispersion and uncertainty from all 40 stored cells, with the invalidate-on-omission behaviour UNWEAKENED | The paired second-method delta clause. The machinery is retained and contract-bound, unexercised here |
| EVAL-05 | Met NARROWLY — the full cost picture within SetFit, from the same reloaded production artifacts | Cross-method cost comparison |

EVAL-02 and EVAL-04 stay Pending. Recording them as complete would file a narrower deliverable
against an unamended promise, which is the defect this project has hit six times.
</amended_requirements>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/REQUIREMENTS.md  (EVAL-01..05 verbatim AND the "Phase 5 in-flight amendment" table — the closing map maps to the amended text, not to the original)
@.planning/phases/05-benchmark-and-claims-gate/deferred-items.md  (D-ITEM-05-15 — the pointer the report's single-method note carries)
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (read the reconciliation banner FIRST; Pitfall 7 host sharp edges and Pitfall 8 PF-007/PF-008 still hold)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 40-row report, its exact-recompute demonstration, and the no-comparison control</name>
  <files>benchmarks/tweeteval-stance/report.md, benchmarks/tweeteval-stance/report.json</files>
  <read_first>
    - benchmarks/tweeteval-stance/run-manifest.json (the complete 40-cell set)
    - crates/apr-cli/src/commands/setfit_bench.rs (the report flags from 05-10 and the single-method rendering from 05-11)
    - .planning/phases/05-benchmark-and-claims-gate/05-11-SUMMARY.md (the must-not-match literal table and its provenance — the control below reuses those literals rather than inventing a list)
    - .planning/phases/05-benchmark-and-claims-gate/05-12-SUMMARY.md (the 40-row mechanism tally the report must render honestly)
    - scripts/apr_bin.sh (the pin, for the spawned report runs)
  </read_first>
  <precondition>05-12 committed 40 rows and a run manifest closed at 40/40, and 05-11's retarget is in history. If the manifest still declares 80 expected cells, this plan does not start — the gate would refuse for the right reason on the wrong set.</precondition>
  <action>
    Pin the binary by sourcing the pin script and failing on its return status. Run the report over
    the real bench directory, emitting the machine-readable payload to the committed JSON path and
    capturing the human output to the committed markdown path, with the status captured on the
    command rather than through a pipe.

    The gate must pass on the first honest attempt. If it refuses, the refusal is a FINDING about
    the evidence set: diagnose via the cell it names and fix the evidence by re-running that cell
    through the normal doors. Never edit a row, a lock record or the manifest to make the gate pass.

    Then the exact-recompute demonstration. Run the report a SECOND time into a temporary path and
    compare the two machine-readable payloads byte for byte, excluding only whatever envelope
    section the schema itself declares volatile. Compare with a tool whose exit status is
    meaningful for this purpose — the diff utility's status is unreliable through this environment's
    tooling, so use a byte comparator or a digest of each payload and compare the digests.

    Then two controls, both on a COPY of the benchmark directory and never on the real one:

    First, the omission control. Remove one row file from the copy and show the report refuses,
    naming that cell. The in-band negatives already prove this in-test on synthetic data; this
    proves it against the real data shape.

    Second, the no-second-method control over the COMMITTED bytes. Take the must-not-match literals
    from 05-11's rendering case table — the ones copied out of the pre-change two-method renderer,
    not a list invented here — and assert none of them appears in either committed report artifact,
    case-insensitively. Assert positively too, so the control cannot pass vacuously on an empty
    file: the report must contain the seed-dispersion interval label, the single-method note with
    its deferred-ticket pointer, and both peak-RSS mechanism strings.

    The mandated note and this gate are designed to hold TOGETHER, and 05-11 is where that was
    resolved: the note refers to the deferred arm only by the decision id and the ticket id and by
    the phrase "a second method", and 05-11 pins the note's rendered text against this same
    must-not-match table in its own suite. So if this control fires on the note, that is a
    regression in the note, not a reason to weaken the gate or to remove the note. Diagnose it as a
    finding against 05-11's pin; do not edit either side to make the other pass.

    Re-verify the real directory after the controls and commit the report pair.
  </action>
  <verify>
    <automated>test -s benchmarks/tweeteval-stance/report.json && test -s benchmarks/tweeteval-stance/report.md && ! grep -qiE 'lora|versus|paired delta|significant' benchmarks/tweeteval-stance/report.md && ! grep -qiE 'lora|paired delta' benchmarks/tweeteval-stance/report.json</automated>
  </verify>
  <acceptance_criteria>
    - `report.json` and `report.md` exist, are committed, and the gate's pass is recorded with its status on its own line
    - The two-run byte-identity result is recorded with the comparator used and its status; a status read through a pipe is not acceptable evidence
    - The omission control on the COPY refused with a cell-naming error; its status and message are quoted, and the real directory is shown untouched and re-verified afterwards
    - The no-second-method control is recorded: the must-not-match literals used, their provenance in 05-11's case table, and the result over both committed artifacts
    - The positive half of that control is recorded too: the seed-dispersion interval label, the single-method note with its deferred-ticket pointer, and both peak-RSS mechanism strings are present in the committed report
    - The committed report renders the two peak figures separately, each with its mechanism string, and the sampled figure carries its lower-bound label — the rendered lines are quoted
    - The headline table is quoted in the SUMMARY: per shot level, F_avg mean with its (n-1) std, min and max, and the seed-dispersion interval
  </acceptance_criteria>
  <done>The claims report exists over the real 40-cell set, is exactly recomputable, its gate bites on real-shaped data, and it publishes no comparison it cannot substantiate</done>
</task>

<task type="auto">
  <name>Task 2: D-11 — the apr qa refusal message names the right tools</name>
  <files>crates/apr-cli/src/commands/output_verification.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/output_verification.rs (the refusal site for an APR with no embedded tokenizer; this file is include!-d into the qa command module, so the message surfaces through `apr qa`)
    - .planning/REQUIREMENTS.md (Phase 4 closing audit item 7 — the plain-BERT control proving the refusal is generative-gate behaviour and not SetFit-specific)
    - crates/apr-cli/src/commands/data_contrastive.rs (the refusal-with-remedy message house style)
  </read_first>
  <action>
    At the refusal site only, extend the error message for encoder-only APRs to state the scope and
    the remedy: that this gate is a generative-model gate, and that encoder and classifier APRs are
    covered by the quality-validation door, the evaluation door and this phase's benchmark door.
    Do NOT change the exit code, do NOT change the detection logic, and do NOT add any capability
    path — D-11 defers the capability to its own ticket.

    Add or extend a unit test with a must-match / must-not-match table over the message. The
    must-not-match rows must include any wording that would suggest the artifact itself is invalid,
    because the refusal is about the gate's scope rather than about the model. Keep the existing
    exit-code behaviour pinned by whatever test already asserts it.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib qa > /tmp/p13-qa.log 2>&1; rc=$?; tail -5 /tmp/p13-qa.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The refusal message names the quality-validation door and the evaluation door; the exit code is unchanged and the pre-existing exit-code test is still green
    - The must-match / must-not-match message test passes, and its must-not-match rows include invalid-artifact wording
    - No new capability path in the diff: the change touches message construction and tests only, and the qa command module itself is untouched
  </acceptance_criteria>
  <done>The refusal is correctly documented at the refusal site; the capability stays deferred</done>
</task>

<task type="auto">
  <name>Task 3: Phase closing audit and the amended-requirements evidence map</name>
  <files>none (evidence gathering; the SUMMARY carries the results)</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-VALIDATION.md (the full-suite command set; note the retired lambda-vector row)
    - .planning/REQUIREMENTS.md (the amended EVAL-02 and EVAL-04 text and the in-flight amendment table — the map is written against these, not against the original wording)
    - .planning/STATE.md (the standing host caveats: the Darwin workspace-check exclusion, the arm64 tier2 reds, the apr-cli package KNOWN-RED — read them correctly, do not report them as Phase 5 regressions)
    - .claude/skills/pre-release/SKILL.md (the standing KNOWN-RED any pre-release check here must read correctly)
  </read_first>
  <action>
    Run and record, with each status captured on the command and reported on its own line: the
    benchmark suite target; the Phase 5 contract audit, which must show zero unbound rows; the
    Phase 4 contract audit, since the lifecycle edit must not have disturbed Phase 4's coverage;
    contract validation over the touched and new contracts, which after this replan are the
    lifecycle contract, the claims contract and the calibration contract; the full scoped SetFit
    suite; and the gate's doctored negatives in a bare cargo test invocation, where the count of
    distinct asserted refusal variants is read from the log rather than quoted from a plan.

    Read the standing KNOWN-REDs correctly. The apr-cli packaging check stays red until the
    human-run publish cascade; the arm64 tier2 clippy reds are pre-existing and untouched by this
    phase; the workspace type-check needs its documented exclusion on this platform. None of these
    is a Phase 5 regression and none may be reported as one.

    Then write the closing evidence map. For each of EVAL-01..EVAL-05 give the artifact, test or
    command that demonstrates it, AND state whether the id is met in full or met by a narrower
    deliverable under the 2026-09-07 amendment, naming the deferred clause and its ticket. Add the
    verifier note listing the Phase 4 items whose user-facing tiers now have production evidence
    via 05-07. These are evidence pointers only. Checking any box, flipping any requirement state,
    or marking the phase complete is the verifier's act, never this plan's.

    Finally, diff the planning state files after every handler interaction and revert any completion
    claim the verifier has not earned. That discipline exists because the false-completion defect
    has occurred six times in this project.
  </action>
  <verify>
    <automated>make setfit-bench-tests > /tmp/p13-cl1.log 2>&1; rc=$?; tail -3 /tmp/p13-cl1.log; test $rc -eq 0 && make contract-audit-phase5 > /tmp/p13-cl2.log 2>&1; rc2=$?; tail -3 /tmp/p13-cl2.log; test $rc2 -eq 0 && make contract-audit-phase4 > /tmp/p13-cl3.log 2>&1; rc3=$?; tail -3 /tmp/p13-cl3.log; test $rc3 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - All six audit commands are recorded with individual statuses; both scoped contract audits show zero unbound rows
    - The count of distinct refusal variants asserted by the gate's negatives is READ FROM THE LOG and recorded, not quoted from a plan document
    - The evidence map covers all five EVAL ids with concrete pointers, and marks EVAL-02, EVAL-04 and EVAL-05 as met by a narrower deliverable with the deferred clause and `D-ITEM-05-15` named
    - No requirement checkbox is edited anywhere: the requirements file is unchanged across this plan's OWN commit range (`<base>^..<head>`, both SHAs quoted), not across an unpinned working-tree diff, which for an already-committed change would report nothing and pass vacuously
    - The standing KNOWN-REDs are each named and classified as pre-existing, with the reason, so a reader cannot mistake them for phase regressions
  </acceptance_criteria>
  <done>Phase gates green at the closing SHA; the verifier receives an evidence map that is honest about which requirements are met narrowly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed evidence -> future auditors | the report must be recomputable from committed rows and lock records alone, forever |
| the published report -> a reader | absence of a second method must not read as parity with one |
| closing audit -> completion claims | evidence pointers must not become checkbox flips |
| destructive controls -> the real evidence set | a control that mutates the published directory destroys what it was proving |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-13-01 | Repudiation | recomputability | high | mitigate | two-run byte identity demonstrated on the real set with a comparator whose status is meaningful; rows and report committed together |
| T-05-13-02 | Tampering | negative controls | medium | mitigate | destructive controls run on a copy only; the real directory re-verified afterwards |
| T-05-13-03 | Elevation of privilege | the requirements table | high | mitigate | evidence map only; explicit no-checkbox criterion; amended ids marked narrow; the verifier owns every flip |
| T-05-13-04 | Information disclosure | a comparison implied by an artifact | high | mitigate | three independent enforcements — structural (an out-of-scope row cannot enter a verified set), render-time (the must-not-match case table) and here (a control over the committed bytes with a positive half so it cannot pass vacuously) |
| T-05-13-05 | Information disclosure | a misleading resource figure | medium | mitigate | the two peak figures rendered separately with their mechanism strings and the sampled figure's lower-bound label, verified in the COMMITTED report rather than only in the renderer's unit tests |
| T-05-13-06 | Spoofing | a KNOWN-RED misread as a regression | low | mitigate | each standing red named and classified with its reason in the closing SUMMARY |
| T-05-13-SC | Tampering | package installs | low | accept (n/a) | no new registry package; the pinned fixture environment and its locked resolution are unchanged by this plan |
</threat_model>

<artifacts_this_phase_produces>
Files and symbols CREATED by this plan (newly-created — exclude from drift verification):

- `benchmarks/tweeteval-stance/report.md` — the human single-method claims report
- `benchmarks/tweeteval-stance/report.json` — the machine-readable detail: per-cell values, the seed-dispersion interval, mechanism strings, and the test statistic and p-value as detail only
- The extended encoder-only refusal message in `crates/apr-cli/src/commands/output_verification.rs`, with its must-match / must-not-match test — no new capability path
- The amended-requirements evidence map in `05-13-SUMMARY.md` — pointers only; no checkbox is flipped here
</artifacts_this_phase_produces>

<review_notes>
Replan notes (2026-09-08, D-19 scoped replan):

- Retargeted from 80 cells to 40. The fail-closed gate is NOT weakened: `verify_run`, its refusal
  order, the invalidate-on-omission rule and the doctored negatives are unchanged in substance;
  05-11 re-proved four of the six doctored shapes at the new scope because the old proof was taken
  against a different expectation set and does not transfer.
- The paired second-method delta section is removed from the report. 05-04's paired-t machinery is
  retained and contract-bound, unexercised by this phase rather than deleted, and 05-11 supplies
  the single-method replacement for EVAL-04's "uncertainty": a closed-form 95% interval over the
  ten contracted seeds on the frozen t constant, computed through one definition of the moments and
  labelled as a seed-dispersion interval rather than as a comparison.
- The prohibition on stating or implying a second-method result DID earn an in-band control, and in
  fact three layered ones. The structural one is the strongest and is the reason the others are
  cheap: under the 40-cell active scope a second method's cell is refused by the gate's EXISTING
  expectation-set check (before any row is read) and by its slot check, so the report has no path
  to such a claim even if a renderer regressed. No new error variant was minted for it — a variant
  reachable only from a test-only constructor would be a guard over a path production cannot take.
- The closing map now distinguishes met-in-full from met-narrowly per requirement id. Filing a
  narrower deliverable against an unamended promise is the specific failure this phase's own
  history warns about.

Canon security breadcrumb: supply-chain package legitimacy is carried by the `T-05-NN-SC` row in
every plan's register. This phase installs no new registry package, so the package-legitimacy
protocol and its blocking checkpoint do not fire here.
</review_notes>

<!-- planner-discipline-allow: lora -->
<!-- planner-discipline-allow: versus -->
<!-- planner-discipline-allow: paired delta -->
<!-- planner-discipline-allow: significant -->

<verification>
- Report pair committed over 40 rows, byte-identical on re-run, gate proven on real-shaped data
- Both negative controls recorded, run on copies, real directory re-verified
- All closing audits green with recorded statuses; standing KNOWN-REDs read correctly
</verification>

<success_criteria>
- The milestone's claims deliverable exists, recomputable and estimation-first, publishing exactly what was measured and no comparison that was not
- D-11's in-scope half landed; the verifier receives a complete evidence map that names every narrowly-met requirement and its deferred clause
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-13-SUMMARY.md` when done
</output>
