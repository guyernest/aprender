---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 13
type: execute
wave: 7
depends_on: ["05-10", "05-11", "05-12"]
files_modified:
  - benchmarks/tweeteval-stance/report.md
  - benchmarks/tweeteval-stance/report.json
  - crates/apr-cli/src/commands/output_verification.rs
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]

must_haves:
  truths:
    - "apr setfit bench report ran over the real 80-cell set: gate green, statistics recomputed, human + JSON outputs committed — and a second run produced a bit-identical JSON payload (EVAL-04's 'exactly recompute', demonstrated not asserted)"
    - "The report presents estimation-first headline numbers (mean ± std, min/max, paired CIs) with resource costs grouped per host and never pooled (D-08, D-09)"
    - "apr qa's encoder-only refusal names apr validate --quality and apr eval at the refusal site (D-11's in-scope half); the capability extension remains a deferred ticket"
    - "The phase's gates all pass at the closing SHA: setfit-bench-tests, contract-audit-phase5, pv validate over new/edited contracts, and the five in-band negatives"
  artifacts:
    - path: "benchmarks/tweeteval-stance/report.md"
      provides: "The human-readable claims report"
    - path: "benchmarks/tweeteval-stance/report.json"
      provides: "The machine-readable detail (per-seed deltas, p-values)"
  key_links:
    - from: "benchmarks/tweeteval-stance/report.json"
      to: "benchmarks/tweeteval-stance/rows/"
      via: "verify_run gate — every number recomputable from the committed rows alone"
---

<objective>
Close the phase: run the fail-closed report over the real 80-cell evidence set, prove exact
recomputability, land the D-11 refusal-message improvement, and run the closing audit.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: this is the milestone's claims deliverable — the auditable, recomputable SetFit-vs-9B-LoRA
comparison EVAL-01..EVAL-05 describe.
Output: committed report pair + qa message fix + closing-audit evidence for the verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Pitfall 7: host sharp edges the closing audit must read correctly; Pitfall 8: PF-007/PF-008)
@.planning/REQUIREMENTS.md  (EVAL-01..05 — the closing evidence maps to these verbatim)
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 80-row report + exact-recompute demonstration</name>
  <files>benchmarks/tweeteval-stance/report.md, benchmarks/tweeteval-stance/report.json</files>
  <read_first>
    - benchmarks/tweeteval-stance/run-manifest.json (the complete set)
    - crates/apr-cli/src/commands/setfit_bench.rs (report flags from 05-10)
    - scripts/apr_bin.sh (pin the binary for the spawned report runs)
  </read_first>
  <action>
    Pin the binary (`. scripts/apr_bin.sh || exit 1`), then run
    `"$APR" setfit bench report --bench-dir benchmarks/tweeteval-stance --json --out benchmarks/tweeteval-stance/report.json`
    capturing the human output to report.md (rc on its own line). The gate must pass on the first
    honest attempt — if it refuses, the refusal is a FINDING about the evidence set (diagnose via
    the named cell; fix the evidence by re-running the affected cell through the normal doors,
    never by editing a row). Then the exact-recompute demonstration: run the report a SECOND time
    into a temp path and compare the two JSON payloads byte-for-byte (excluding any volatile
    envelope section the schema declares — the payload must be identical; `cmp` the canonical
    payload bytes). Additionally run one NEGATIVE control against a COPY of the benchmark dir
    (never the real one): remove one row file from the copy and show the report refuses naming
    that cell (the in-band negatives already prove this in-test; this control proves it against
    the real data shape). Commit report.md + report.json. Quote the headline table (per-shot
    F_avg mean ± std for both methods + paired CI) in the SUMMARY.
  </action>
  <verify>
    <automated>test -s benchmarks/tweeteval-stance/report.json && test -s benchmarks/tweeteval-stance/report.md && ! grep -qi 'significant' benchmarks/tweeteval-stance/report.md</automated>
  </verify>
  <acceptance_criteria>
    - report.json + report.md exist and are committed; report.md contains "95% CI" and the D-09 per-host framing line, and no case-insensitive "significant"
    - The two-run byte-identity result is recorded (cmp rc=0 on the payload)
    - The removed-row negative control on the COPY refused with a cell-naming error (rc + message quoted); the real directory untouched
  </acceptance_criteria>
  <done>The claims report exists, is exactly recomputable, and its gate bites on real-shaped data</done>
</task>

<task type="auto">
  <name>Task 2: D-11 — apr qa refusal message names the right tools</name>
  <files>crates/apr-cli/src/commands/output_verification.rs</files>
  <read_first>
    - crates/apr-cli/src/commands/output_verification.rs (line 517: the exact refusal site — `CliError::ValidationFailed("APR missing embedded tokenizer")`; this file is include!-d into qa.rs at qa.rs:537, so the message surfaces through `apr qa`)
    - .planning/REQUIREMENTS.md (Phase 4 closing audit item 7 — the plain-BERT control proving the refusal is generative-gate behavior, not SetFit-specific)
    - crates/apr-cli/src/commands/data_contrastive.rs (:614-626 — the refusal-with-remedy message house style)
  </read_first>
  <action>
    At the refusal site only, extend the error message for encoder-only APRs to state the scope
    and the remedy: that `apr qa` is a generative-model gate, and encoder/classifier APRs are
    covered by `apr validate --quality`, `apr eval`, and `apr setfit bench` (message text follows
    the refusal-with-remedy house style). Do NOT change the exit code, the detection logic, or add
    any capability path (D-11: the capability extension is a deferred ticket). Add/extend a unit
    test with a must-match/must-not-match table on the message (must match: "validate --quality",
    "generative"; must not match: any suggestion that the artifact is invalid). Keep the existing
    exit-5 behavior pinned by whatever test already asserts it.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p apr-cli --lib qa > /tmp/qa.log 2>&1; rc=$?; tail -5 /tmp/qa.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - The refusal message contains "validate --quality" and names apr eval; exit code unchanged (existing exit-code test still green)
    - The must-match/must-not-match message test passes
    - No new capability path in the diff (output_verification.rs diff touches message construction + tests only; qa.rs untouched)
  </acceptance_criteria>
  <done>The refusal is correctly documented at the refusal site; capability stays deferred</done>
</task>

<task type="auto">
  <name>Task 3: Phase closing audit</name>
  <files>none (evidence gathering; SUMMARY carries the results)</files>
  <read_first>
    - .planning/phases/05-benchmark-and-claims-gate/05-VALIDATION.md (the full-suite command set)
    - .planning/STATE.md (the standing host caveats: Darwin cargo-check exclusion, arm64 tier2 reds, apr-cli package KNOWN-RED — read them correctly, do not report them as Phase 5 regressions)
  </read_first>
  <action>
    Run and record, rc on its own line for each: (1) `make setfit-bench-tests`; (2)
    `make contract-audit-phase5` (zero BIND- lines); (3) `make contract-audit-phase4` (the
    lifecycle edit must not have disturbed Phase 4's 15/15); (4)
    `target/release/pv validate` over the three touched/new contracts
    (setfit-train-lifecycle-v1, setfit-benchmark-claims-v1, calibration-v1); (5)
    `make setfit-all-tests`; (6) the five in-band negatives run in a bare
    `cargo test -p aprender-train --lib --features setfit bench_gate` (non-zero matched). Read the
    standing KNOWN-REDs correctly: `cargo package -p apr-cli` stays red until the human publish
    cascade (pre-release skill), tier2 arm64 clippy reds are pre-existing, `cargo check
    --workspace` needs `--exclude aprender-profile` on Darwin. Close the SUMMARY with a
    requirements-evidence map: for each of EVAL-01..EVAL-05, the artifact/test/command that
    demonstrates it, plus a note for the verifier listing the Phase 4 items whose "a user can"
    tiers now have production evidence via 05-07 (OPS-01, OPS-02, positive tiers of TRN-07,
    APR-01..05, OPS-03..05) — evidence pointers only; checking any box is the verifier's call,
    never this plan's (the sixth false-completion lesson).
  </action>
  <verify>
    <automated>make setfit-bench-tests > /tmp/cl1.log 2>&1; rc=$?; tail -3 /tmp/cl1.log; test $rc -eq 0 && make contract-audit-phase5 > /tmp/cl2.log 2>&1; rc2=$?; tail -3 /tmp/cl2.log; test $rc2 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - All six audit commands recorded with individual rc values; zero BIND- lines in both scoped audits
    - The requirements-evidence map covers all five EVAL ids with concrete pointers (file/test/command per id)
    - No requirement checkbox edited anywhere (`git diff .planning/REQUIREMENTS.md` empty from this plan)
  </acceptance_criteria>
  <done>Phase gates green at the closing SHA; verifier handoff evidence assembled</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed evidence -> future auditors | the report must be recomputable from committed rows alone, forever |
| closing audit -> completion claims | evidence pointers must not become checkbox flips (the recurring false-completion defect) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Repudiation | recomputability | mitigate | two-run byte-identity demonstrated on the real set; rows + report committed together |
| T-05-13-02 | Tampering | negative control | mitigate | destructive control runs on a COPY only; real dir untouched and re-verified after |
| T-05-13-03 | Elevation (false completion) | requirements table | mitigate | evidence map only; explicit no-checkbox acceptance criterion; verifier owns the flips |
| T-05-13-04 | Information disclosure | report language | mitigate | no-"significant" grep in the committed report; p-values detail-only |
| T-05-13-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- Report pair committed, byte-identical on re-run, gate proven on real-shaped data
- All closing audits green with recorded rc values; KNOWN-REDs read correctly
</verification>

<success_criteria>
- The milestone's claims deliverable exists, recomputable and estimation-first; D-11's in-scope half landed; the phase hands the verifier a complete evidence map
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-13-SUMMARY.md` when done
</output>
