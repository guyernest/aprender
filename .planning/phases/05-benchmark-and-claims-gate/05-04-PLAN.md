---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - scripts/setfit_fixtures/gen_claims_fixtures.py
  - scripts/setfit_fixtures/claims_stats/
  - crates/aprender-core/src/calibration.rs
  - crates/aprender-core/src/calibration_tests.rs
  - crates/aprender-core/src/stats/hypothesis.rs
  - crates/aprender-core/src/stats/tests_claims_stats.rs
  - contracts/calibration-v1.yaml
autonomous: true
requirements: [EVAL-01, EVAL-04]

must_haves:
  truths:
    - "Top-label ECE and multiclass Brier exist in aprender-core::calibration beside the binary pair, bound to explicit ordered labels, with sklearn/scipy reference fixtures from the pinned uv env (D-07)"
    - "f64 paired-t helpers (mean, (n-1) std, min/max, paired deltas, 95% CI) exist with the t critical value frozen as a constant VERIFIED against scipy.stats.t.ppf(0.975, 9) — no RNG, no inverse CDF, no bootstrap anywhere in the claims math (D-05/D-06)"
    - "The new fixtures live in their OWN directory with their OWN manifest.sha256 — the existing Phase 1 fixture corpus and its manifest are byte-untouched"
  artifacts:
    - path: "scripts/setfit_fixtures/gen_claims_fixtures.py"
      provides: "scipy/sklearn reference fixture generator with three-way cross-checks + SHA-256 manifest + self-verification"
    - path: "crates/aprender-core/src/calibration.rs"
      provides: "expected_calibration_error_top_label + brier_score_multiclass beside the binary pair"
      contains: "brier_score_multiclass"
    - path: "crates/aprender-core/src/stats/hypothesis.rs"
      provides: "f64 paired stats + frozen T_CRIT_975_DF9 + CI half-width helper"
      contains: "T_CRIT_975_DF9"
  key_links:
    - from: "crates/aprender-core/src/calibration_tests.rs"
      to: "scripts/setfit_fixtures/claims_stats/"
      via: "include_str! fixture parity with recorded RED values (glm_tests.rs:280 precedent)"
    - from: "crates/aprender-core/src/stats/hypothesis.rs"
      to: "ttest_1samp"
      via: "f64 mirror reuses the existing closed-form core; extension of the owner module, no duplicate t machinery"
---

<objective>
Build the numerics substrate for the claims layer: multiclass calibration metrics (D-07) and
closed-form paired statistics (D-05/D-06), each falsified against reference fixtures generated in
the pinned uv environment (scipy 1.18.0 / scikit-learn 1.9.0, already in uv.lock — no new pins).

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: EVAL-01's calibration diagnostics and EVAL-04's exactly-recomputable uncertainty both
depend on these ~70 lines of contracted arithmetic; getting them fixture-verified now unblocks the
row/report plans.
Output: two contract-bound calibration functions, f64 paired-stat helpers, a frozen t constant,
and a hash-manifested fixture set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (Patterns 2 and 3; Assumptions A1, A3, A4; "Don't Hand-Roll")
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (sections 4, 5, 11)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reference fixture generator in the pinned uv env (verifies A1/A3/A4)</name>
  <files>scripts/setfit_fixtures/gen_claims_fixtures.py, scripts/setfit_fixtures/claims_stats/</files>
  <read_first>
    - scripts/setfit_fixtures/generate_fixtures.py (write_manifest at :922-949 — SHA-256 manifest + shasum self-verification; the upstream-pin refusal pattern at :870-880)
    - scripts/setfit_fixtures/uv.lock (confirm scipy 1.18.0 + scikit-learn 1.9.0 resolve — no pyproject edit is needed or permitted)
    - .planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md (Pattern 2 formulas, Pattern 3 shapes, A1/A3/A4)
  </read_first>
  <action>
    Write `gen_claims_fixtures.py` (run via `cd scripts/setfit_fixtures && uv run python gen_claims_fixtures.py`)
    emitting JSON fixtures into a NEW `scripts/setfit_fixtures/claims_stats/` directory with its own
    `manifest.sha256` (write_manifest pattern scoped to that directory + shasum -c self-verification
    before exit). The existing fixture corpus and its manifest must be byte-untouched. Fixtures:
    (a) `t_critical.json`: `scipy.stats.t.ppf(0.975, 9)` at full f64 repr — this VERIFIES A1
    (expected ~2.2621571628, but the env value is the authority) — plus ppf(0.975, df) for df in
    {5, 9, 19} as context rows.
    (b) `paired_t_cases.json`: >= 4 cases of paired samples (n=10, mirroring the 10-seed design;
    include a near-tie case and a constant-difference case) each recording scipy's
    `ttest_rel` statistic + two-sided pvalue AND the closed-form CI bounds
    d_bar +/- t_crit * s_d/sqrt(n) computed with numpy in f64.
    (c) `ece_top_label_cases.json`: >= 4 cases of K=3 probability matrices + integer labels +
    n_bins=10, ECE computed by a small numpy reference implementation (conf = max_k p, pred =
    argmax_k p, equal-width bins over [0,1], ECE = sum_b (n_b/N)|acc_b - conf_b|); record A3's
    verification (whether sklearn exposes a multiclass ECE — expected NO; note the probe result in
    the file header).
    (d) `brier_multiclass_cases.json`: >= 4 cases with BS = (1/N) sum_i sum_k (p_ik - y_ik)^2
    one-hot, each cross-checked numerically against the OvR identity
    sum_k sklearn.metrics.brier_score_loss(y==k, p[:,k]) (A4) — abort with FATAL if the identity
    deviates beyond 1e-12.
    Three-way discipline (Phase 2 fixture precedent): where a closed form, a library value, and a
    hand value all exist, all three must agree or the generator aborts. Every fixture file carries
    the generator name, scipy/sklearn versions read from the env at runtime, and the formula text.
  </action>
  <verify>
    <automated>cd scripts/setfit_fixtures && uv run python gen_claims_fixtures.py > /tmp/gen.log 2>&1; rc=$?; tail -5 /tmp/gen.log; test $rc -eq 0 && shasum -a 256 -c claims_stats/manifest.sha256 --status</automated>
  </verify>
  <acceptance_criteria>
    - claims_stats/ contains t_critical.json, paired_t_cases.json, ece_top_label_cases.json, brier_multiclass_cases.json, manifest.sha256
    - t_critical.json records the env-computed ppf(0.975, 9) value at full precision
    - `git status --porcelain` (via `rtk proxy git status --porcelain`) shows NO change to any pre-existing fixture file or the pre-existing manifest
    - brier fixtures record the A4 identity check passing at <= 1e-12
  </acceptance_criteria>
  <done>Fixture set generated, manifested, self-verified; A1/A3/A4 resolved with recorded evidence</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multiclass top-label ECE + multiclass Brier in aprender-core::calibration</name>
  <files>crates/aprender-core/src/calibration.rs, crates/aprender-core/src/calibration_tests.rs, contracts/calibration-v1.yaml</files>
  <read_first>
    - crates/aprender-core/src/calibration.rs (binary expected_calibration_error at :138-164 — contract macro binding + bin indexing style; brier_score at :378-390; the sibling-test include convention at :392-398)
    - contracts/calibration-v1.yaml (the equations block the binary pair binds to — the additive edit target)
    - crates/aprender-core/src/glm/glm_tests.rs (lines 274-298: the FALSIFY pattern with recorded RED values)
    - scripts/setfit_fixtures/claims_stats/ece_top_label_cases.json and brier_multiclass_cases.json (Task 1 output)
  </read_first>
  <behavior>
    - expected_calibration_error_top_label(probabilities: &[Vec<f32>] or flat + k, labels: &[usize], n_bins) matches every fixture case within 1e-6
    - brier_score_multiclass matches every fixture case within 1e-6 and equals the binary brier_score on a K=2 case reshaped one-hot (consistency check)
    - Degenerate inputs (empty rows, label >= K, row not summing to ~1, n_bins == 0) produce the module's contracted precondition failures, not garbage values
    - A deliberately wrong implementation (bin by predicted-class probability instead of max) reproduces the recorded RED value in the test comment
  </behavior>
  <action>
    Implement `expected_calibration_error_top_label` and `brier_score_multiclass` in
    calibration.rs BESIDE the binary pair, following the binary pair's exact style: same bin
    indexing (`(conf * n_bins as f32) as usize).min(n_bins - 1)`), same precondition macro
    convention (`contract_pre_*!`), `#[must_use]`, ordered labels as `usize` class indices with an
    explicit `n_classes` parameter (the ordered-labels binding EVAL-01 demands — a label out of
    range is a precondition failure). Formulas per RESEARCH Pattern 2: top-label ECE
    (conf=max_k, pred=argmax_k, equal-width 10 bins in the claims usage but n_bins is a parameter)
    and original multiclass Brier (1/N sum_i sum_k (p_ik - y_ik)^2). Bind both with
    `#[provable_contracts_macros::contract("calibration-v1", equation = ...)]` and add the two
    equations to contracts/calibration-v1.yaml as an ADDITIVE edit (formula + domain/codomain +
    invariants, house shape), recording the pv diff command + suggested bump in the contract
    header (materialize old via git show first). Fixture-parity tests in calibration_tests.rs read
    claims_stats fixtures via include_str! (relative path from the source file, the thresholds.rs
    convention) and record the RED value of the named wrong implementation in a comment (glm
    precedent). Do NOT touch ClassifyEvalReport (its ece/brier stay as the unfixtured
    formula-reference only; the claims path never consumes it — D-06).
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib calibration > /tmp/cal.log 2>&1; rc=$?; tail -5 /tmp/cal.log; test $rc -eq 0 && target/release/pv validate contracts/calibration-v1.yaml</automated>
  </verify>
  <acceptance_criteria>
    - calibration.rs contains `pub fn expected_calibration_error_top_label` and `pub fn brier_score_multiclass`, each with a `#[provable_contracts_macros::contract("calibration-v1"` attribute
    - contracts/calibration-v1.yaml gains exactly two equations; pv validate 0 errors; header records the pv diff bump
    - Fixture-parity tests reference claims_stats JSON via include_str! and contain a recorded RED value comment
    - `cargo test -p aprender-core --lib calibration` exits 0; ClassifyEvalReport untouched (`git diff --name-only` does not list classify_eval_report.rs)
  </acceptance_criteria>
  <done>Both metrics shipped, contract-bound, fixture-parity green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: f64 paired statistics + frozen t critical constant</name>
  <files>crates/aprender-core/src/stats/hypothesis.rs, crates/aprender-core/src/stats/tests_claims_stats.rs</files>
  <read_first>
    - crates/aprender-core/src/stats/hypothesis.rs (ttest_1samp at :81-115 — the closed-form core; ttest_rel at :191-208; t_distribution_pvalue at :360-365 with the incomplete-beta relation; how tests_hypothesis_contract.rs is included at :470-472)
    - crates/aprender-core/src/stats/tests_hypothesis_contract.rs (the scipy-oracle test style; note its "no YAML contract for hypothesis testing yet" remark)
    - scripts/setfit_fixtures/claims_stats/t_critical.json and paired_t_cases.json (Task 1 output)
  </read_first>
  <behavior>
    - ttest_rel_f64 reproduces scipy's statistic and two-sided p for every paired_t_cases fixture within 1e-10 (statistic) / 1e-9 (p)
    - paired_ci95_df9 (or the general paired_ci(diffs, t_crit)) reproduces the fixture CI bounds within 1e-10
    - T_CRIT_975_DF9 equals the fixture t_critical value exactly at the recorded precision (a drifted constant turns the test red)
    - Length mismatch and n < 2 produce the module's typed errors (DimensionMismatch / the existing error taxonomy)
  </behavior>
  <action>
    Extend the OWNER module (`aprender-core::stats::hypothesis`, per the alternatives table — no new
    stats module elsewhere): add f64 mirrors `ttest_1samp_f64` + `ttest_rel_f64` reusing the same
    closed forms ((n-1) sample std, se = s/sqrt(n), df = n-1, exact p via the incomplete-beta
    relation in f64), a `pub const T_CRIT_975_DF9: f64` frozen to the fixture value with a doc
    comment citing `scipy.stats.t.ppf(0.975, 9)` and the generator, and a paired-CI helper
    returning mean difference, s_d, and the 95% bounds `d_bar +/- t_crit * s_d / sqrt(n)` taking
    t_crit as a parameter (the claims path passes T_CRIT_975_DF9; df is fixed at 9 by the 10-seed
    design — no inverse CDF is implemented, Ph1 D-14 pattern). Also add the tiny aggregation
    helpers the report will reuse if not already present in f64 (mean, (n-1) sample std, min/max
    over a slice) — site them here only if hypothesis.rs is their natural owner, otherwise leave
    aggregation to the bench library and note it. New tests live in a new sibling
    `tests_claims_stats.rs` included the same way tests_hypothesis_contract.rs is (do not edit that
    file's existing cases). Fixture-parity per case, plus the frozen-constant equality test.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-core --lib stats:: > /tmp/stats.log 2>&1; rc=$?; tail -5 /tmp/stats.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - hypothesis.rs contains `T_CRIT_975_DF9` and `ttest_rel_f64` and a paired-CI helper taking t_crit as a parameter
    - tests_claims_stats.rs asserts the constant against the fixture value and every paired case against scipy outputs
    - No RNG symbol (`rand`, `sample`, `bootstrap`) appears in the new code (grep of the new fns' bodies)
    - `cargo test -p aprender-core --lib stats::` exits 0
  </acceptance_criteria>
  <done>Closed-form f64 paired statistics shipped with frozen, fixture-verified t constant</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python fixture env -> Rust tests | fixture bytes cross from the uv env into the test suite; integrity via per-directory SHA-256 manifest |
| claims arithmetic -> published numbers | a wrong constant or formula corrupts every headline claim downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | claims_stats fixtures | mitigate | own manifest.sha256 + shasum self-verification at generation; include_str! pins bytes at compile time |
| T-05-04-02 | Tampering (RNG smuggling) | paired stats path | mitigate | closed-form only; frozen t constant; grep assertion that no RNG symbol enters the new code; bootstrap exclusion re-enforced by the claims contract in 05-05 |
| T-05-04-03 | Repudiation | constant provenance | mitigate | T_CRIT_975_DF9 doc cites the exact scipy call + generator; equality test red on drift |
| T-05-04-04 | Tampering | existing Phase 1 fixture corpus | mitigate | new directory + new manifest; porcelain check proves pre-existing files untouched |
| T-05-04-SC | Tampering | package installs | accept (n/a) | no new pins; uv.lock already resolves scipy/sklearn |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib calibration` and `--lib stats::` exit 0
- `pv validate contracts/calibration-v1.yaml` 0 errors
- Pre-existing fixture corpus byte-untouched
</verification>

<success_criteria>
- D-07 metrics + D-05/D-06 statistics fixture-verified, contract-bound where owned, zero RNG in the claims math
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-04-SUMMARY.md` when done
</output>
