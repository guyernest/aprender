---
# branch: gsd/phase-2-contract-gate (02-01 policy; no PR — human's call)
phase: 05-benchmark-and-claims-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-train/src/train/setfit/thresholds.rs
  - crates/aprender-train/src/train/setfit/tune.rs
autonomous: true
requirements: [EVAL-02]

must_haves:
  truths:
    - "Thresholds selects its per-class ε table BY REGIME (D-03's prerequisite), while the fixture regime's table keeps byte-identical values and every existing test stays green"
    - "validate_evidence resolves membership and the class table in ONE lookup, so an uncalibrated id can never be judged against any table"
    - "The contract-parse test still asserts len == 1 and full-list equality (the literal changes to 2 only in plan 05-03, at the checkpoint)"
  artifacts:
    - path: "crates/aprender-train/src/train/setfit/thresholds.rs"
      provides: "Per-regime threshold tables + table_for(regime_id) lookup keyed by RegimeCoordinates::covers"
      contains: "table_for"
    - path: "crates/aprender-train/src/train/setfit/tune.rs"
      provides: "validate_evidence threshold lookup routed through the regime-keyed table"
  key_links:
    - from: "crates/aprender-train/src/train/setfit/tune.rs"
      to: "Thresholds::table_for"
      via: "validate_evidence order-of-checks item 1 (fail closed outside the calibrated regime)"
      pattern: "table_for"
    - from: "crates/aprender-train/src/train/setfit/thresholds.rs"
      to: "contracts/setfit-train-lifecycle-v1.yaml"
      via: "include_str! contract pin (never a runtime read)"
      pattern: "include_str!"
---

<objective>
Restructure `Thresholds` from one global class table to per-regime tables, so D-03's fresh
production ε values can land as a SECOND regime entry in plan 05-03 without touching the fixture
regime's meaning. This is the code half of F-R4 ("per-regime thresholds do not exist yet"); it
changes NO threshold value, NO contract byte, and NO test literal about the regime count.

Branch: `gsd/phase-2-contract-gate` (02-01 policy; no PR — human's call).

Purpose: without this restructuring, adding a production regime would apply fixture-scale ε to the
full 30k-vocab encoder — the exact non-transfer D-10(c) warns about.
Output: regime-keyed `Thresholds` with behavior-preserving fixture semantics, proven by the
untouched test suite plus one induced-mutation control.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-benchmark-and-claims-gate/05-CONTEXT.md
@.planning/phases/05-benchmark-and-claims-gate/05-RESEARCH.md  (F-R1, F-R4; Pitfall 1)
@.planning/phases/05-benchmark-and-claims-gate/05-PATTERNS.md  (section 1: thresholds.rs restructuring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regime-keyed Thresholds with byte-identical fixture semantics</name>
  <files>crates/aprender-train/src/train/setfit/thresholds.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/thresholds.rs (FULL file: CALIBRATED_REGIMES at :61, RegimeCoordinates::parse/covers at :107-159, Thresholds::frozen() at :273-300 with the fixture values — embedding 1.1e-5, layer_norm_weight 2.9e-6, layer_norm_bias 1.1e-5, projection_weight 1.8e-5, projection_bias 8.3e-6, attention_key_bias ungated, embedding_delta_floor 2.7e-5 — is_calibrated at :330, the #[cfg(test)] serde structs at :349-382, thresholds_match_the_contract at :394-491, regime_membership_is_component_wise at :617-656)
    - contracts/setfit-train-lifecycle-v1.yaml (lines ~98-160 frozen_thresholds block; ~276-334 calibration_regime block — READ ONLY, no edit in this plan)
  </read_first>
  <action>
    Restructure `Thresholds` so each calibrated regime owns its class table and floor. Concrete
    target shape: introduce `RegimeThresholds { regime: &'static str, classes: BTreeMap<&'static str, ClassThreshold>, embedding_delta_floor: f64 }`
    (names may be adapted to the file's conventions, but a per-regime struct carrying regime id +
    classes + floor must exist), and make `Thresholds` hold a slice/Vec of them plus keep
    `calibrated_regimes()` returning the same `&[&str]` view (derived from the per-regime entries so
    the constant and the tables cannot drift apart — one source). `Thresholds::frozen()` builds
    exactly ONE `RegimeThresholds` today: the fixture regime
    `minilm-slice-h64-l2-a2-i256-v97@1110a243|seeds=1,42,7|cells=s16e2b8,s8e1b4` with the
    byte-identical existing values listed in read_first. Add
    `pub(crate) fn table_for(&self, regime_id: &str) -> Option<&RegimeThresholds>` implemented via
    the existing `RegimeCoordinates::parse` + `covers` component-wise membership (NEVER string
    equality), and reimplement `is_calibrated` as `table_for(...).is_some()` so membership and
    table selection cannot disagree. Keep `CALIBRATED_REGIMES` as the single string-list source the
    contract-parse test compares against (unchanged content, still exactly one entry). Keep the
    `include_str!` contract pin untouched. Do NOT change any test literal in
    `thresholds_match_the_contract` in this task beyond what the type restructuring mechanically
    requires — the len == 1 assertion and the full-list equality assertion survive verbatim.
    Extend the `#[cfg(test)]` serde structs (`ContractFile`/`ContractEquations`/`EvidenceGateEquation`/`CalibrationRegimeEquation`)
    with an OPTIONAL per-regime thresholds field (e.g. `#[serde(default)] per_regime_thresholds`)
    that deserializes to empty against today's contract — the parsing capability lands now so plan
    05-03's edit only adds data + assertions.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit thresholds > /tmp/thr.log 2>&1; rc=$?; tail -5 /tmp/thr.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - thresholds.rs contains `fn table_for` and a per-regime struct carrying regime id + classes + embedding_delta_floor
    - `is_calibrated` delegates to the same lookup as `table_for` (grep shows is_calibrated implemented via table_for or a shared private fn)
    - `thresholds_match_the_contract` still contains the literal `contracted_regimes.len(), 1` assertion and the full-list equality assertion (unchanged semantics)
    - The fixture values are unchanged: `grep -c '1.1e-5\|2.9e-6\|1.8e-5\|8.3e-6\|2.7e-5' crates/aprender-train/src/train/setfit/thresholds.rs` returns a non-zero count and no value literal was edited
    - contracts/setfit-train-lifecycle-v1.yaml is byte-untouched (`git diff --name-only` does not list it)
  </acceptance_criteria>
  <done>Regime-keyed structure in place; all thresholds tests green; contract untouched</done>
</task>

<task type="auto">
  <name>Task 2: Route validate_evidence through the regime table + induced-mutation control</name>
  <files>crates/aprender-train/src/train/setfit/tune.rs, crates/aprender-train/src/train/setfit/thresholds.rs</files>
  <read_first>
    - crates/aprender-train/src/train/setfit/tune.rs (validate_evidence at ~:1139 — order-of-checks item 1: the UncalibratedRegime refusal with observed + calibrated fields; then every downstream threshold read)
    - crates/aprender-train/src/train/setfit/thresholds.rs (the Task 1 result)
  </read_first>
  <action>
    In `validate_evidence`, replace the two-step "is_calibrated then read the global table" with ONE
    `table_for(&evidence.calibration_regime_id)` lookup: `None` produces the exact same
    `SetFitTrainError::UncalibratedRegime { observed, calibrated }` error (same fields, same
    calibrated-list rendering — downstream tests and the 04-15 ladder assert on this refusal shape);
    `Some(regime_table)` supplies every per-class ε, gating flag, and embedding_delta_floor read in
    the rest of the function. No threshold value may be read from anywhere but the resolved regime
    table after this change. Then run the two-sided control (Verification Discipline rule 4): induce
    ONE mutation — make `table_for` return the fixture table for ANY id (drop the covers check) —
    and record which named test goes red (the existing `UncalibratedRegime` negative in the tune or
    evidence suites must catch it; if NOTHING goes red, add a unit test
    `uncalibrated_id_resolves_no_table` asserting `table_for` returns None for the invented
    `minilm-full-h384-l6-a12-i1536-v30522@production` string already used by
    regime_membership_is_component_wise). Revert the mutation, re-run, record green. Then run the
    wider evidence/tune suites.
  </action>
  <verify>
    <automated>CARGO_INCREMENTAL=0 cargo test -p aprender-train --lib --features setfit setfit:: > /tmp/setfit-lib.log 2>&1; rc=$?; tail -5 /tmp/setfit-lib.log; test $rc -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - tune.rs validate_evidence contains exactly one threshold-table resolution site (grep for `table_for` in tune.rs returns >= 1 and the old global-table read path is gone)
    - The UncalibratedRegime error construction is unchanged in shape (same variant, observed + calibrated fields)
    - The SUMMARY records the induced mutation, the named red test, and the reverted green re-run (mutation-control evidence, not assertion)
    - Full `--features setfit` lib suite for aprender-train exits 0
  </acceptance_criteria>
  <done>One-lookup gate; mutation control performed and reverted; suites green</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols CREATED by this plan (newly-created — exclude from drift verification):

- Struct `RegimeThresholds { regime, classes, embedding_delta_floor }` (crates/aprender-train/src/train/setfit/thresholds.rs)
- Fn `Thresholds::table_for(&self, regime_id: &str) -> Option<&RegimeThresholds>`
- Optional `#[cfg(test)]` serde field `per_regime_thresholds` on the contract-parse structs
- Unit test `uncalibrated_id_resolves_no_table` (only if the induced mutation turns nothing else red)
</artifacts_this_phase_produces>

<review_notes>
Cross-AI review (05-REVIEWS.md) responses:

- <!-- REVIEW REJECTED: Gemini Finding 5 / plan-matrix row 05-02 asks that `RegimeCoordinates::parse`/`covers` NORMALIZE architecture prefixes so `minilm-slice-` and `minilm-full-` are treated as one family when dimensions match. REJECTED: `covers` is the gate that makes an uncalibrated architecture resolve to NO table, and prefix-family normalization would make two DIFFERENT architectures share one measured ε set — precisely the non-transfer Ph3 D-10(c) forbids and the thing this plan exists to prevent. The regime id is a rendered-verbatim identity string, not a semantic taxonomy. The legitimate half of the finding (the `minilm-slice-` prefix is a misnomer for the full production encoder, and a future change to `architecture_fingerprint()` would fail every Phase 5 gate closed) is addressed as CONTRACT PROSE in 05-03 Task 1, which records that any change to `architecture_fingerprint()` is a contract-breaking change requiring a new, separately measured regime entry via a `pv diff`-flagged edit — never a silent alias. -->
</review_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regime id (from run evidence) -> threshold selection | an attacker-shaped or drifted regime id must resolve to NO table, never to a wrong table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering (gate weakening) | table_for lookup | mitigate | is_calibrated delegates to table_for (one source of truth); induced-mutation control proves a widened lookup turns a named test red |
| T-05-02-02 | Elevation | validate_evidence | mitigate | single resolution site; no threshold readable without a resolved regime; UncalibratedRegime shape preserved so existing negatives keep biting |
| T-05-02-03 | Repudiation | CALIBRATED_REGIMES vs tables drift | mitigate | calibrated_regimes() derived from the per-regime entries — cannot drift from the tables |
| T-05-02-SC | Tampering | package installs | accept (n/a) | no installs in this plan |
</threat_model>

<verification>
- `cargo test -p aprender-train --lib --features setfit setfit::` exits 0
- `git diff --name-only` limited to thresholds.rs + tune.rs
- contract file byte-untouched
</verification>

<success_criteria>
- Per-regime tables shipped with fixture behavior byte-identical; len==1 and equality assertions intact
- validate_evidence single-lookup; mutation control documented
</success_criteria>

<output>
Create `.planning/phases/05-benchmark-and-claims-gate/05-02-SUMMARY.md` when done
</output>
