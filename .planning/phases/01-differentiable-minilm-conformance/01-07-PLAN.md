---
phase: 01-differentiable-minilm-conformance
plan: 07
type: execute
wave: 5
depends_on: [01-06]
files_modified:
  - crates/aprender-core/src/setfit/loss.rs
  - crates/aprender-core/src/setfit/mod.rs
autonomous: true
requirements: [ENC-04, ENC-06]

must_haves:
  truths:
    - "A developer can compute a finite, tensor-valued, graph-connected cosine-similarity MSE pair loss whose backward reaches encoder parameters"
    - "SetFitMiniLm is the SOLE public entry point binding tokenizer + encoder: MiniLmTokenizer::from_bytes, MiniLmImport::{open, open_slice_fixture}, and BertSentenceEncoder::from_import are all pub(crate), so a mismatched tokenizer/encoder pair is NOT CONSTRUCTIBLE from outside the crate (D-08 as written), with tokenizer-identity equality at the forward boundary retained as defense in depth"
    - "The D-08 seal has been demonstrated RED: a recorded out-of-crate compile probe proves rustc rejects `MiniLmImport::open` from tests/ with E0603, so the seal rests on observed compiler behavior rather than on a grep that has never fired"
    - "Freeze groups address exactly the ENC-04 components (embeddings, per-layer attention/ffn/norm) as validated structure, defaulting to all-trainable, with REPLACEMENT semantics and an explicit clear_freeze"
  artifacts:
    - path: "crates/aprender-core/src/setfit/loss.rs"
      provides: "pair_cosine_mse: tensor-valued SetFit objective (ENC-06)"
      contains: "pub fn pair_cosine_mse"
    - path: "crates/aprender-core/src/setfit/mod.rs"
      provides: "SetFitMiniLm bound model type (D-08) + FreezeGroup enum (D-22) + trainable/frozen partition + conformance READ accessors"
      contains: "pub struct SetFitMiniLm"
  key_links:
    - from: "crates/aprender-core/src/setfit/loss.rs"
      to: "crates/aprender-core/src/autograd/ops/similarity.rs"
      via: "composition of cosine_similarity_rows + mse_loss (never nn/loss.rs f32 utilities)"
      pattern: "cosine_similarity_rows"
    - from: "crates/aprender-core/src/setfit/mod.rs"
      to: "crates/aprender-core/src/setfit/encoder.rs"
      via: "SetFitMiniLm owns BertSentenceEncoder + MiniLmTokenizer; freeze maps groups -> named params; encoder() hands 01-08 the borrow it needs to reach forward_tokens_per_layer"
      pattern: "FreezeGroup"
---

<objective>
Deliver the tensor-valued pair objective (ENC-06 implementation) and the bound model type
`SetFitMiniLm` (D-08) with the per-module, per-layer freeze-group configuration (D-20/D-21/D-22)
that the controlled-step gate (01-08) exercises.

Purpose: completes the public Phase 1 API surface; establishes the trainable/frozen partition
mechanics ENC-04's proof needs.
Output: setfit/loss.rs + SetFitMiniLm + FreezeGroup with tests, plus recorded evidence that the
D-08 seal actually stops an out-of-crate caller.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **D-08 restatement (SUPERSEDED — see the checker-driven revision below).** The `--reviews` pass
  concluded the lower-level constructors had to stay public and restated D-08 as a use-time check.
  The checker showed the premise was false and the user decided to seal. The truth statement is now
  back to the structural claim.
- **`apply_freeze` semantics are specified**: REPLACEMENT (each call fully defines the policy),
  idempotent, order-insensitive, duplicate-tolerant, and reversible via `clear_freeze()`.
- **A dedicated `pair_cosine_mse` contract equation** (declared by 01-01 Task 1) is annotated,
  rather than mislabelling this wrapper as raw `mse_loss` — its inputs are two [B,H] embedding
  matrices plus binary labels, not a prediction vector and a target vector.
- **Label validation rejects non-finite values BEFORE the binary comparison** — `NaN != 0.0 && NaN != 1.0`
  happens to reject NaN today, but only incidentally; make it explicit and tested.
- **ENC-04 wording aligned** with the corrected gate semantics (aggregate per component + data-driven
  exemptions).

Checker-driven revision (2026-08-08, post-`--reviews` re-verification):
- **[B2 — USER DECISION 2026-08-08: SEAL] D-08 is restored to its structural form.** The prior pass
  rejected sealing on the grounds that "it would break the fixture gates"; the checker verified that
  premise is false. Every consumer of the lower-level constructors is in-crate: 01-05/01-06/01-07
  tests are all in-module (`#[cfg(all(test, feature = "setfit"))]`), and 01-08's out-of-crate
  integration harness already drives through `SetFitMiniLm::from_slice_fixture`. `pub(crate)`
  therefore suffices everywhere. 01-05 seals `MiniLmTokenizer::from_bytes` and
  `MiniLmImport::{open, open_slice_fixture}`; 01-06 seals `BertSentenceEncoder::from_import`. The
  `tokenizer_sha256` boundary check is KEPT as defense in depth against in-crate misuse.
- **Consequence for this plan:** `SetFitMiniLm` is now genuinely the sole public constructor, so it
  must expose everything 01-08's out-of-crate harness needs. Two narrow
  `#[cfg(feature = "conformance-fixtures")]` READ accessors are added (Task 2) — read accessors, not
  constructors, so the seal is not reopened.

Checker-driven revisions, iteration 2 (2026-08-08):
- **[W2 — the seal has never been demonstrated red].** The evidence for D-08 was a grep plus the
  assertion that the calls "would not compile". This plan already demands a red demonstration for
  01-08's tolerance-agreement test on the grounds that *an agreement test that has never turned red
  is not evidence*; the same standard applies here. Task 2 now requires a one-off out-of-crate
  compile probe recording the actual E0603, plus a must-match/must-not-match case table for the seal
  regex (CLAUDE.md rule 7 — the `apr`-invocation patterns were wrong five times and every one was
  caught by a case table, none by review), plus a separate re-export scan closing the
  "re-exported elsewhere" evasion the previous scoped grep could not see.
  **Verified constraint (2026-08-08):** the declaration scan must stay scoped to
  `crates/aprender-core/src/setfit/`. Widening it crate-wide produces 14 legitimate pre-existing
  matches (`serialization/apr/mod.rs:53,62`, `bundle/mmap.rs:111,198,333`, `bundle/format.rs:105`,
  `bundle/paging.rs:168`, `bundle/manifest.rs:252`, `inspect/safetensors.rs:179`,
  `format/onnx/reader.rs:12`, `format/gguf/reader_parsing.rs:25`, `audio/capture/audio.rs:213`,
  `index/persistent_hnsw.rs:103`, `serialization/safetensors_include_01.rs:8`) — a scan that cries
  wolf 14 times is a scan nobody reads. The COMPILE PROBE, not the grep, is the primary evidence;
  the greps are cheap tripwires against a future re-widening.
- **[B5 consequence] Per-layer intermediates now exist upstream.** 01-06 owns
  `#[cfg(feature = "conformance-fixtures")] pub fn forward_tokens_per_layer` on
  `BertSentenceEncoder`. This plan's `encoder()` accessor is how 01-08 reaches it; no additional
  accessor is needed here, and 01-08 must NOT add one.
- **[W1 consequence] `SentenceBatch` is read-only out-of-crate** (01-05: `pub(crate)` fields +
  public read accessors). `tokenize()` therefore hands out a batch that a caller can read but
  neither forge nor mutate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md

<interfaces>
```rust
// setfit/loss.rs (feature = "setfit")
/// MSE(cos_sim(za_i, zb_i), labels_i) -> graph-connected [1] tensor. labels in {0.0, 1.0}.
pub fn pair_cosine_mse(za: &Tensor /*[B,H]*/, zb: &Tensor /*[B,H]*/, labels: &[f32])
    -> Result<Tensor /*[1]*/, SetFitError>;

// setfit/mod.rs (feature = "setfit")
pub struct SetFitMiniLm { /* owns MiniLmTokenizer + BertSentenceEncoder — private fields */ }
impl SetFitMiniLm {
    pub fn from_pretrained_dir(dir: &Path, root_seed: u64) -> Result<Self, SetFitError>;
    #[cfg(feature = "conformance-fixtures")]
    pub fn from_slice_fixture(fixture_dir: &Path, root_seed: u64) -> Result<Self, SetFitError>;
    pub fn encode_texts(&self, texts: &[&str]) -> Result<Tensor, SetFitError>; // tokenize once -> encode
    pub fn set_training(&mut self, training: bool);

    // --- Conformance-only READ accessors (D-08 seal stays intact: these hand out borrows of an
    // already-correctly-paired model; they construct nothing). 01-08's out-of-crate harness needs
    // per-layer/forward introspection and the tokenized batch, and the constructors are pub(crate).
    #[cfg(feature = "conformance-fixtures")]
    pub fn encoder(&self) -> &BertSentenceEncoder;
    /// Tokenize with THIS model's tokenizer, yielding a batch already stamped with the matching
    /// tokenizer_sha256. Cannot produce a mismatched pair by construction, and the returned
    /// SentenceBatch is read-only out-of-crate (01-05 W1), so the stamp cannot be re-pointed.
    #[cfg(feature = "conformance-fixtures")]
    pub fn tokenize(&self, texts: &[&str]) -> Result<SentenceBatch, SetFitError>;

    /// REPLACEMENT semantics: the argument fully defines the freeze policy. Any group not listed
    /// becomes trainable again. Idempotent, order-insensitive, duplicate-tolerant.
    /// `apply_freeze(&[])` is equivalent to `clear_freeze()`.
    pub fn apply_freeze(&mut self, groups: &[FreezeGroup]) -> Result<(), SetFitError>;
    /// Restore the D-20 default: every parameter trainable, requires_grad(true) everywhere.
    pub fn clear_freeze(&mut self);
    /// The currently applied policy, normalized (deduplicated, sorted).
    pub fn freeze_policy(&self) -> Vec<FreezeGroup>;

    /// Partition respecting freeze config. Default (no apply_freeze): ALL trainable (D-20).
    pub fn trainable_parameters_mut(&mut self) -> Vec<(String, &mut Tensor)>;
    pub fn frozen_parameters(&self) -> Vec<(String, &Tensor)>;
}

#[derive(Debug, Clone, PartialEq, Eq, PartialOrd, Ord)]
pub enum FreezeGroup {            // validated structure, NOT a glob DSL (D-22)
    Embeddings,
    LayerAttention(usize),        // encoder.layer.N.attention.* (self + output.dense)
    LayerFfn(usize),              // encoder.layer.N.intermediate.* + encoder.layer.N.output.dense.*
    LayerNorm(usize),             // both LayerNorms of layer N
}
```
Group -> name mapping is exact prefix sets over the 01-06 dotted names; out-of-range layer index
-> typed error, and the policy is left UNCHANGED when a call returns Err (no partial application).

**What `encoder()` unlocks for 01-08 (B5).** `&BertSentenceEncoder` reaches 01-06's
`#[cfg(feature = "conformance-fixtures")] pub fn forward_tokens_per_layer(&self, batch)
-> Result<(Tensor, Vec<Tensor>), SetFitError>` — the per-layer intermediates the D-15 localization
gate compares against `forward_per_layer.json`. That method is 01-06's to write; this plan only
provides the borrow. 01-08 adds NEITHER a new accessor here nor a new method on the encoder.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pair_cosine_mse tensor-valued objective</name>
  <files>crates/aprender-core/src/setfit/loss.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/ops/similarity.rs (cosine_similarity_rows + mse_loss from 01-03 — the only building blocks)
    - crates/aprender-core/src/nn/loss.rs (ANTI-analog: confirm nothing is imported from here — f32 returns recreate PF-001)
    - contracts/setfit-encoder-conformance-v1.yaml (the `pair_cosine_mse` equation declared by 01-01 Task 1 — annotate against THAT, not against mse_loss)
  </read_first>
  <behavior>
    - pair_cosine_mse(za, zb, labels) equals mse_loss(cosine_similarity_rows(za, zb, eps), labels) numerically on a hand-computed 3-pair case
    - Result is a [1] tensor with requires_grad when inputs require grad; backward produces finite non-zero grads on BOTH za and zb
    - labels.len() != batch -> Err; a non-finite label (NaN or Inf) -> Err(SetFitError::BatchInvalid) checked EXPLICITLY before the binary comparison; a finite label outside {0.0, 1.0} (e.g. 0.5) -> Err (SetFit pair labels are binary)
    - Shape mismatch between za and zb -> Err before compute
    - Identical embeddings with label 1.0 -> loss ~ 0; orthogonal with label 1.0 -> loss ~ 1
  </behavior>
  <action>
    Implement as a thin composition of the two 01-03 ops (cosine eps: the same explicit constant used
    by the encoder normalize path, 1e-12, documented as such). Validate labels before compute in this
    order — length, finiteness, binary membership — each with its own typed SetFitError; the explicit
    finiteness check must precede the membership check so a NaN produces a message naming
    non-finiteness rather than an incidental "not in {0,1}".

    Annotate with `#[provable_contracts_macros::contract("setfit-encoder-conformance-v1", equation = "pair_cosine_mse")]`
    — the dedicated equation, not `mse_loss`. Annotating this wrapper as raw MSE would misdescribe
    its inputs (two embedding matrices plus binary labels, not pred/target vectors) and weaken the
    contract's falsifiability.

    Do NOT name it after or route through nn/loss.rs / nn/self_supervised.rs (their names imply
    fine-tuning that is not occurring — CONTEXT known trap). Export from setfit/mod.rs. Test fn
    names start `pair_loss_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features setfit pair_loss_</automated>
  </verify>
  <acceptance_criteria>
    - loss.rs contains `pub fn pair_cosine_mse` returning Result<Tensor, SetFitError>; no `use crate::nn::loss` or self_supervised import (source assertion)
    - The contract annotation names equation "pair_cosine_mse" (source assertion)
    - Graph-connectivity test asserts non-zero grads on both inputs after backward
    - Separate tests assert Err for a NaN label and for a finite label 0.5, with distinguishable error content
    - `cargo test -p aprender-core --lib --features setfit pair_loss_` exits 0
  </acceptance_criteria>
  <done>ENC-06 objective implemented as a graph-connected tensor with validated inputs and its own contract equation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetFitMiniLm bound type + FreezeGroup partition with specified semantics (D-08, D-20/D-21/D-22), with the seal demonstrated RED</name>
  <files>crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/mod.rs (current exports)
    - crates/aprender-core/src/setfit/encoder.rs (named_parameters dotted names — the freeze mapping target; the tokenizer-identity boundary check added in 01-06; the conformance-gated `forward_tokens_per_layer` that `encoder()` must expose to 01-08)
    - crates/aprender-core/src/setfit/tokenizer.rs (SentenceBatch read accessors from 01-05 — the batch `tokenize()` returns is read-only out-of-crate)
    - contracts/lora-adapter-trains-base-frozen-v1.yaml (frozen-vs-trainable proof pattern the partition must enable)
  </read_first>
  <behavior>
    - SetFitMiniLm::from_slice_fixture builds tokenizer + encoder together, and the resulting encoder's tokenizer_sha256 equals the tokenizer's — a test asserts that pairing
    - Mismatch is UNCONSTRUCTIBLE from outside the crate: `MiniLmTokenizer::from_bytes`, `MiniLmImport::{open, open_slice_fixture}`, and `BertSentenceEncoder::from_import` are all pub(crate), so the only out-of-crate way to obtain a paired tokenizer+encoder is through SetFitMiniLm (D-08 as written) — and this is proven by a recorded compile probe, not only by grep
    - Defense in depth for IN-crate misuse: a SentenceBatch built by a DIFFERENT in-crate MiniLmTokenizer, passed into this model's encode path, still returns a typed error
    - The conformance accessors hand out borrows only: `encoder()` returns `&BertSentenceEncoder` and `tokenize()` returns a batch stamped with this model's own tokenizer hash — neither can assemble a mismatched pair, and the returned batch is read-only out-of-crate
    - `encoder()` is sufficient for 01-08: a test calls `model.encoder().forward_tokens_per_layer(&model.tokenize(&["hello"])?)` and gets (embeddings_out, layer_outputs) back, proving the wave-6 harness's access path compiles from a caller's position
    - Default partition: trainable_parameters_mut() contains every named parameter, frozen_parameters() empty (D-20 all-trainable), and freeze_policy() is empty
    - apply_freeze([Embeddings, LayerAttention(1)]) moves exactly the mapped prefix sets to frozen: embeddings.* and encoder.layer.1.attention.* (self.query/key/value + output.dense, NOT output.LayerNorm which belongs to LayerNorm(1))
    - REPLACEMENT semantics: after apply_freeze([Embeddings]) then apply_freeze([LayerFfn(0)]), ONLY LayerFfn(0) is frozen — embeddings are trainable again with requires_grad restored to true
    - Idempotence: apply_freeze(P) twice yields the same partition and the same freeze_policy()
    - Order- and duplicate-insensitivity: apply_freeze([A,B]) == apply_freeze([B,A]) == apply_freeze([A,B,A])
    - clear_freeze() restores all-trainable with requires_grad(true) on every parameter; apply_freeze(&[]) is equivalent
    - Frozen tensors get requires_grad(false); trainable/frozen name sets are disjoint and their union equals all names
    - LayerAttention(7) on a 2-layer slice -> Err (out of range, typed) AND the previously applied policy is UNCHANGED (no partial application)
    - encode_texts tokenizes once and encodes through the shared path (no second tokenization)
  </behavior>
  <action>
    Implement SetFitMiniLm per interfaces block with private fields. Because 01-05/01-06 seal the
    lower-level constructors to `pub(crate)` (D-08, user decision 2026-08-08), `from_pretrained_dir`
    and `from_slice_fixture` are the ONLY public ways to build the pair — both load tokenizer and
    encoder from the same source, so the pairing is correct by construction.

    Add the two `#[cfg(feature = "conformance-fixtures")]` READ accessors from the interfaces block
    (`encoder()`, `tokenize()`) so 01-08's out-of-crate harness has what it needs without reopening
    the seal. They are gated on conformance-fixtures, not on `setfit`, so a normal `--features setfit`
    consumer never sees them. Adding a public CONSTRUCTOR to work around the seal is forbidden. Note
    that 01-06 already provides `forward_tokens_per_layer` on the encoder, so `encoder()` is
    sufficient for 01-08's D-15 localization gate — do not add a third accessor here, and 01-08 must
    not add one either.

    **Demonstrate the seal RED (W2). Evidence, not assertion.** Do all three, and record all three in
    the SUMMARY:

    (a) **Out-of-crate compile probe — the PRIMARY evidence.** Create a throwaway
        `crates/aprender-core/tests/zz_seal_probe.rs` whose body calls each sealed constructor from
        an out-of-crate position, e.g.
        `let _ = aprender::setfit::MiniLmImport::open(std::path::Path::new("/nonexistent"));`
        plus one line each for `MiniLmTokenizer::from_bytes` and
        `BertSentenceEncoder::from_import`. Run
        `cargo test -p aprender-core --features conformance-fixtures --test zz_seal_probe > /tmp/seal_probe.log 2>&1; rc=$?`
        (capture the status DIRECTLY, never through a pipe — CLAUDE.md rule 1). Assert `rc != 0` and
        that the log contains `E0603`. Paste the E0603 lines verbatim into the SUMMARY, then DELETE
        the probe file and confirm `git status` is clean of it. This is the same probe-file technique
        that produced the CB-510 evidence in 01-04, and it proves reachability regardless of how many
        re-export paths exist — which a grep cannot.

    (b) **Declaration scan, comment-safe and SCOPED.** 
        `grep -rnE --include='*.rs' '^[^/]*\bpub fn (from_bytes|open|open_slice_fixture|from_import)\b' crates/aprender-core/src/setfit/`
        must return no match. The `^[^/]*` prefix is load-bearing: it excludes `//` and `///` lines,
        so the doc comments in 01-05/01-06 that literally discuss `pub fn open` cannot make the gate
        fire, and a commented-out violation cannot make it pass. Keep the scan scoped to
        `src/setfit/` — verified 2026-08-08, widening it crate-wide yields 14 legitimate pre-existing
        matches (apr/mmap/bundle/onnx/gguf/hnsw readers), and a gate that cries wolf 14 times is a
        gate nobody reads.
        **Re-run the case table rather than re-reading the pattern (CLAUDE.md rule 7).** Verify by
        construction, in a scratch file, that the regex behaves:
        | Input line | Expected |
        |---|---|
        | `    pub fn open(dir: &Path) -> Result<Self, SetFitError> {` | MATCH |
        | `pub fn from_bytes(bytes: &[u8]) -> Result<Self, SetFitError> {` | MATCH |
        | `    #[cfg(feature = "conformance-fixtures")] pub fn open_slice_fixture(` | MATCH |
        | `    pub fn from_import(import: &MiniLmImport, seed: u64) -> ... {` | MATCH |
        | `    pub(crate) fn open(dir: &Path) -> ... {` | no match |
        | `    /// SEALED (D-08): pub(crate). Was pub fn open before the seal.` | no match |
        | `    // pub fn from_bytes(...)` | no match |
        | `    pub fn opened_at(&self) -> u64 {` | no match (word boundary) |
        | `    pub fn tokenizer_sha256(&self) -> &str {` | no match |
        Then MUTATE for real: temporarily change one `pub(crate) fn open` in
        `setfit/import.rs` to `pub fn open`, confirm the scan FIRES, and revert. A guard that has
        never turned red in the scope it guards is not evidence (CLAUDE.md rules 4 and 7).

    (c) **Re-export scan — closes the "re-exported elsewhere" evasion.**
        `grep -rnE --include='*.rs' '^[^/]*pub use .*(from_bytes|open_slice_fixture|from_import)\b' crates/aprender-core/src/`
        must return no match. A sealed method cannot be re-exported (methods are not items), but a
        free-function wrapper or a `pub use` alias could reopen the path without tripping (b); 01-05
        Task 1 already forbids constructor re-exports, and this is the check that proves it.

    Freeze mapping: define the exact prefix sets in a single function documented against the ENC-04
    component list — Embeddings -> "embeddings."; LayerAttention(n) -> "encoder.layer.{n}.attention.self." +
    "encoder.layer.{n}.attention.output.dense."; LayerFfn(n) -> "encoder.layer.{n}.intermediate." +
    "encoder.layer.{n}.output.dense."; LayerNorm(n) -> "encoder.layer.{n}.attention.output.LayerNorm."
    + "encoder.layer.{n}.output.LayerNorm.".

    Implement apply_freeze as: (1) validate EVERY group first (layer indices in range) and return
    Err leaving the stored policy untouched if any is invalid — no partial application; (2) normalize
    the list (dedupe + sort, hence the Ord derive on FreezeGroup); (3) reset every parameter to
    requires_grad(true); (4) apply the normalized policy, setting requires_grad(false) on matched
    names; (5) store the normalized policy. That ordering delivers replacement, idempotence, and
    order/duplicate insensitivity from one code path rather than three special cases. `clear_freeze()`
    is step (3) plus clearing the stored policy.

    Freezing = requires_grad(false) + exclusion from trainable_parameters_mut (the AdamW param set
    in 01-08 is built from that method — freeze BY exclusion, per RESEARCH). Assert in a test that a
    freeze mapping which matches ZERO names is impossible for a valid group — a group whose prefix
    set matches nothing means the encoder's naming drifted, and that must fail loudly rather than
    silently freeze nothing. Coarse policies (top-N layers) are expressible as group lists; no
    string/glob API is exposed (D-22).

    Tests: every behavior bullet, including the exact name-set partition assertions (compare sorted
    name vectors), the replacement/idempotence/order-insensitivity trio, the no-partial-application
    assertion, the empty-match guard, the foreign-batch rejection, and the
    `encoder().forward_tokens_per_layer(tokenize(..))` access-path test. Test fn names start
    `setfit_model_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features conformance-fixtures setfit_model_</automated>
  </verify>
  <acceptance_criteria>
    - mod.rs contains `pub struct SetFitMiniLm`, `pub fn clear_freeze`, `pub fn freeze_policy`, and `pub enum FreezeGroup` with exactly the four variants
    - Partition test asserts disjointness + union-completeness of trainable/frozen name sets
    - LayerAttention freeze test asserts attention.output.LayerNorm remains TRAINABLE (group boundary exactness)
    - Replacement test proves a second apply_freeze un-freezes the first call's groups and restores their requires_grad to true
    - Idempotence and order/duplicate-insensitivity tests present
    - Out-of-range group returns typed error AND leaves the prior policy intact (asserted)
    - A group whose prefix set matches zero named parameters fails the test suite (naming-drift guard)
    - Foreign-tokenizer batch rejection asserted through the SetFitMiniLm encode path (in-crate defense in depth)
    - A test exercises `model.encoder().forward_tokens_per_layer(&model.tokenize(..)?)` and asserts the returned layer_outputs count equals the config's layer count — proving 01-08's access path (B5)
    - **Out-of-crate POSITIVE access probe (mirrors the negative seal probe):** the in-crate test above would compile identically if `forward_tokens_per_layer` were `pub(crate)`, so it cannot by itself prove out-of-crate reachability (CLAUDE.md rule 2 — prove the mechanism engaged, do not label by intent). Create a throwaway `crates/aprender-core/tests/zz_access_probe.rs` containing ONLY the positive line `model.encoder().forward_tokens_per_layer(&model.tokenize(&["hi"])?)`, assert it COMPILES AND PASSES with `rc=$?` captured directly (not through a pipe), record the result in the SUMMARY, then delete the probe and confirm `git status` is clean. Reuses the negative probe's machinery — no new tooling
    - **D-08 seal demonstrated RED:** the out-of-crate probe was run, exited non-zero with `E0603`, the verbatim error lines are in the SUMMARY, and the probe file was deleted (git status clean). Status captured with `rc=$?` directly, not through a pipe
    - **Seal regex case table re-run:** the must-match/must-not-match table above was executed against the pattern, and the real mutation (`pub(crate) fn open` -> `pub fn open` in import.rs) made the scan FIRE before being reverted. Both recorded in the SUMMARY
    - Declaration scan `grep -rnE --include='*.rs' '^[^/]*\bpub fn (from_bytes|open|open_slice_fixture|from_import)\b' crates/aprender-core/src/setfit/` returns no match; the scan stays scoped to src/setfit/ with the 14-false-positive finding recorded as the reason
    - Re-export scan `grep -rnE --include='*.rs' '^[^/]*pub use .*(from_bytes|open_slice_fixture|from_import)\b' crates/aprender-core/src/` returns no match
    - `encoder()` and `tokenize()` exist, are `#[cfg(feature = "conformance-fixtures")]`, and return borrows/batches only — no conformance-gated public constructor was added
    - `cargo test -p aprender-core --lib --features conformance-fixtures setfit_model_` exits 0
  </acceptance_criteria>
  <done>Bound entry point in place with the seal proven by the compiler rather than asserted; freeze groups exact, validated, replacement-semantic, reversible, default all-trainable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API surface | SetFitMiniLm is the SOLE public constructor; every lower-level constructor is pub(crate), so an out-of-crate mismatched pair is unconstructible. Identity checking at use is retained for in-crate misuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-13 | Spoofing | loss identity | mitigate | pair_cosine_mse never routes through f32 loss utilities whose names imply encoder tuning that is not happening (PF-001 class); graph-connectivity asserted by test; annotated against its own contract equation rather than mislabelled as raw MSE |
| T-1-14 | Tampering | freeze misaddressing | mitigate | structured FreezeGroup enum (no string DSL); exact prefix-set tests incl. the LayerNorm boundary; empty-match guard fails loudly if encoder naming drifts so a policy can never silently freeze nothing |
| T-1-23 | Tampering | partial freeze application on error | mitigate | validate-all-then-apply ordering; an invalid group leaves the stored policy and every requires_grad flag untouched (asserted) |
| T-1-21 | Spoofing | tokenizer/encoder mismatch | mitigate | STRUCTURAL: all lower-level constructors pub(crate) (01-05/01-06), SetFitMiniLm builds both halves from one source — D-08 as written, DEMONSTRATED by a recorded out-of-crate E0603 compile probe rather than asserted. DATA LAYER: SentenceBatch is read-only out-of-crate (01-05 W1) so the stamped hash cannot be forged or re-pointed. DEFENSE IN DEPTH: tokenizer_sha256 equality enforced at every forward call (01-06) and asserted here through the bound type's own encode path. Conformance accessors expose borrows only, never construction |
| T-1-29 | Repudiation | seal guard that has never fired | mitigate | must-match/must-not-match case table for the seal regex plus a real mutation that turns it red before revert; a separate re-export scan closes the alias evasion the scoped declaration scan cannot see; the compile probe is the primary evidence and does not depend on any regex being right |
| T-1-11 | DoS | label/group validation | mitigate | typed errors on non-finite labels, non-binary labels, and out-of-range layers |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib --features conformance-fixtures pair_loss_`, then `setfit_model_` — separate invocations (cargo accepts only ONE positional test filter; verified 2026-08-08)
- D-08 seal: out-of-crate probe run and recorded (non-zero exit + E0603 quoted, probe deleted); scoped declaration scan clean; re-export scan clean; regex case table re-run and the real mutation observed turning it red
- 01-08 access path compiles from a caller's position: `encoder().forward_tokens_per_layer(tokenize(..))` exercised by a test here, one wave before 01-08 depends on it
- `cargo clippy -p aprender-core --features setfit -- -D warnings` clean
- `cargo check -p aprender-core --no-default-features` green
</verification>

<success_criteria>
- ENC-06 objective: finite, tensor-valued, graph-connected, input-validated, with its own contract equation
- D-08 delivered as written AND demonstrated: SetFitMiniLm is the sole public entry point, a mismatched
  tokenizer/encoder pair is not constructible from outside the crate, and rustc has been observed
  saying so (E0603, recorded); the forward-call identity check remains as defense in depth for
  in-crate misuse
- 01-08's out-of-crate access path (`encoder()` -> `forward_tokens_per_layer`, `tokenize()`) is proven
  to compile here, in wave 5, rather than discovered in wave 6
- D-20/D-21/D-22: all-trainable default, opt-in structured freeze groups matching ENC-04 components exactly, with replacement semantics, idempotence, and clear_freeze
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-07-SUMMARY.md` when done.
</output>
