---
phase: 01-differentiable-minilm-conformance
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-05]
files_modified:
  - crates/aprender-core/src/setfit/encoder.rs
  - crates/aprender-core/src/setfit/mod.rs
  - crates/aprender-core/src/setfit/error.rs
  - crates/aprender-core/src/nn/transformer/attention_gqa.rs
autonomous: true
requirements: [ENC-03, ENC-05]

must_haves:
  truths:
    - "A developer can run single or mixed-length padded SentenceBatches through one shared Transformer -> masked mean pooling -> L2 normalization path and get a graph-connected [B,384] (or [B,slice_hidden]) tensor"
    - "named_parameters() returns HF dotted names verbatim (encoder.layer.0.attention.self.query.weight style), pooler excluded, order stable"
    - "set_training flips dropout at all four HF-verified sites recursively; registered parameters are byte-identical across mode flips; eval-mode double-encode is bitwise deterministic"
  artifacts:
    - path: "crates/aprender-core/src/setfit/encoder.rs"
      provides: "BertSentenceEncoder: graph-connected batched forward, Module impl, seeded dropout policy"
      contains: "pub struct BertSentenceEncoder"
  key_links:
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/autograd/ops/"
      via: "embedding_gather / additive_attention_mask / masked_mean_pool / l2_normalize_rows"
      pattern: "embedding_gather"
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/nn/transformer/attention_gqa.rs"
      via: "MultiHeadAttention::forward_self with additive mask"
      pattern: "forward_self"
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/setfit/import.rs"
      via: "construction from MiniLmImport (full pin or slice fixture)"
      pattern: "MiniLmImport"
---

<objective>
Build `BertSentenceEncoder` (D-01): the fully graph-connected batched forward path
(embeddings -> 6x BERT layer -> masked mean pool -> L2 normalize) as a new type in the setfit
module, implementing the extended Module trait with HF-dotted names (D-18), HF-verified dropout
placement with a deterministic seeded-RNG policy, and recursive train/eval switching (ENC-05).
`models/bert/` stays untouched.

Purpose: ENC-03's shared path and ENC-05's mode contract materialize here, on top of the proven
ops (01-03), named Module (01-02), and import/tokenizer boundary (01-05).
Output: encoder type with construction from MiniLmImport, mode/dropout/determinism tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md
@.planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md

<interfaces>
```rust
// setfit/encoder.rs (feature = "setfit")
pub struct BertSentenceEncoder { /* embeddings, layers: Vec<...>, config, root_seed, training */ }
impl BertSentenceEncoder {
    pub fn from_import(import: &MiniLmImport, root_seed: u64) -> Result<Self, SetFitError>;
    /// Graph-connected token states [B, S, H]
    pub fn forward_tokens(&self, batch: &SentenceBatch) -> Result<Tensor, SetFitError>;
    /// forward_tokens -> masked_mean_pool -> l2_normalize_rows(eps 1e-12): [B, H], graph-connected
    pub fn encode(&self, batch: &SentenceBatch) -> Result<Tensor, SetFitError>;
}
impl Module for BertSentenceEncoder { /* named_parameters HF-dotted, set_training recursive */ }
```
HF dotted names (verbatim, pooler excluded): embeddings.word_embeddings.weight,
embeddings.position_embeddings.weight, embeddings.token_type_embeddings.weight,
embeddings.LayerNorm.{weight,bias}, and per layer N:
encoder.layer.N.attention.self.{query,key,value}.{weight,bias},
encoder.layer.N.attention.output.dense.{weight,bias},
encoder.layer.N.attention.output.LayerNorm.{weight,bias},
encoder.layer.N.intermediate.dense.{weight,bias},
encoder.layer.N.output.dense.{weight,bias},
encoder.layer.N.output.LayerNorm.{weight,bias}.

Dropout sites (HF transformers v4.57.1 verified, p = 0.1): (1) embeddings post-LayerNorm,
(2) attention probs post-softmax pre-@V, (3) attention output dense -> dropout -> residual+LN,
(4) FFN output dense -> dropout -> residual+LN.
Seed policy: per-site seed = stable_hash(root_seed, dotted_site_name) fed to Dropout::with_seed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BertSentenceEncoder structure, embeddings, layer stack, Module impl</name>
  <files>crates/aprender-core/src/setfit/encoder.rs, crates/aprender-core/src/setfit/mod.rs, crates/aprender-core/src/setfit/error.rs, crates/aprender-core/src/nn/transformer/attention_gqa.rs</files>
  <read_first>
    - .planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md (spike findings: batch>1 grad status, A5 SDPA dropout seeding verdict — determines whether attention_gqa.rs needs the seeded-dropout extension)
    - crates/aprender-core/src/models/bert/layer.rs (:78 — composition order reference ONLY; do not call into models/bert/)
    - crates/aprender-core/src/nn/transformer/attention_gqa.rs (forward_self/forward_qkv — construction params for heads/dropout)
    - crates/aprender-core/src/nn/dropout/mod.rs (with_seed :77-88; PMAT-922 constant-mask mul pattern :110-135)
    - crates/aprender-core/src/setfit/import.rs (MiniLmImport surface from 01-05)
  </read_first>
  <behavior>
    - from_import(slice fixture) builds an encoder whose named_parameters() names exactly match the gradients.json key set from the fixtures (same names, same count, no pooler.*)
    - forward_tokens on a mixed-length 2-sentence SentenceBatch returns [2, S, H] with requires_grad true when weights require grad
    - A SentenceBatch with an OOV id (>= slice vocab), mismatched mask length, an all-padding row, or seq > 256 returns a typed SetFitError BEFORE any layer compute
    - backward() from a scalar loss over forward_tokens output produces finite non-zero grads on every named parameter (reuses the spike assertion helper style)
  </behavior>
  <action>
    Implement the encoder per interfaces block. Embeddings: three embedding_gather calls (word ids,
    position ids 0..S per row, token type ids) combined with autograd-aware add, then LayerNorm then
    site-1 Dropout. Layers: per layer, MultiHeadAttention::forward_self with the
    additive_attention_mask op output, then attention-output dense -> dropout -> residual add -> LN,
    then intermediate dense + GELU -> output dense -> dropout -> residual add -> LN — composition
    built ONLY from nn/ building blocks and the new ops; every intermediate flows through
    autograd-aware operations (add/mul/module forwards). Any Tensor::new/from_vec on a computed
    value without an adjacent grad_fn is the PMAT-913/914/922 bug class — forbidden.
    Validate the SentenceBatch once at the boundary (lengths, OOV vs vocab, all-padding rows,
    seq <= 256) with SetFitError variants; ops afterward still return their own Results.
    Module impl: named_parameters with the exact HF dotted names from the interfaces block
    (D-18 — must equal torch named_parameters() output verbatim so gradients.json keys align with
    zero translation); positional parameters() in the same order.
    If the 01-03 spike found SDPA's internal dropout unseedable, extend MultiHeadAttention
    construction in attention_gqa.rs with an optional seeded attention-probs dropout (additive
    change, default None, no behavior change for existing callers) rather than re-implementing
    attention; otherwise leave attention_gqa.rs untouched and drop it from files_modified.
    Annotate forward_tokens (or encode) with the contract macro (equation setfit_encoder_forward,
    contract setfit-encoder-conformance-v1, D-27).
    Tests in-module (#[cfg(all(test, feature = "setfit"))], slice-based tests additionally gated on
    conformance-fixtures): name-set equality vs gradients.json keys, boundary rejection matrix,
    grad-flow smoke. Test fn names start `encoder_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features setfit,conformance-fixtures encoder_</automated>
  </verify>
  <acceptance_criteria>
    - encoder.rs contains `pub struct BertSentenceEncoder` implementing Module with named_parameters returning HF dotted names (test asserts exact set equality with gradients.json keys)
    - No import of crate::models::bert::embeddings (source assertion — the asserting/unchecked path is not called)
    - Boundary tests assert typed errors for OOV, length mismatch, all-padding row, oversize
    - Grad-flow test asserts finite + non-zero grad per named parameter after backward
    - `cargo test -p aprender-core --lib --features setfit,conformance-fixtures encoder_` exits 0
  </acceptance_criteria>
  <done>Graph-connected encoder with verbatim HF names and fail-closed boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: encode() pipeline, dropout placement + seeded RNG policy, mode contract (ENC-05)</name>
  <files>crates/aprender-core/src/setfit/encoder.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/encoder.rs (Task 1 state)
    - crates/aprender-core/src/nn/tests_named_module.rs (snapshot_named byte-identity helper from 01-02 — reuse, don't duplicate)
    - crates/aprender-core/src/autograd/mod.rs (no_grad at :76)
  </read_first>
  <behavior>
    - encode() == l2_normalize_rows(masked_mean_pool(forward_tokens(batch), mask), 1e-12); output rows have unit L2 norm
    - Train mode: exactly 1 + 3*num_layers dropout sites active (slice: 7); each site's mask is deterministic given root_seed (two encoders with same seed produce identical train-mode outputs; different root_seed differs)
    - Eval mode: no_grad(|| encode(batch)) twice yields bitwise-identical outputs (to_bits equality)
    - set_training(true/false) flips every dropout site recursively; snapshot_named byte-identity across train->eval->train (registered parameters unchanged — ENC-05)
    - Dropout empirical drop rate over a large tensor within [0.08, 0.12] for p=0.1 (statistics test, Rust-side only per D-16)
  </behavior>
  <action>
    Wire encode() through the 01-03 ops with explicit eps 1e-12. Implement the seed policy from the
    interfaces block: derive per-site seeds via a stable non-positional hash of
    (root_seed, dotted site name) — document that renaming a site changes its stream (acceptable;
    positional drift is not). Implement set_training recursion across embeddings dropout, per-layer
    dropouts, and attention (per the A5 resolution). RNG/seed state must NOT appear in
    named_parameters (Pitfall 7). Tests: the five behavior bullets; mode-flip test reuses the
    01-02 snapshot helper. Test fn names start `encoder_mode_` / `encoder_encode_`.
    Fixture-parity comparisons are NOT in this plan — they live in 01-08 after tolerances (D-14);
    only structural/determinism/mode behavior is asserted here.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features setfit,conformance-fixtures encoder_mode_ && cargo test -p aprender-core --lib --features setfit,conformance-fixtures encoder_encode_</automated>
  </verify>
  <acceptance_criteria>
    - encode() present; unit-norm assertion in tests (norm within f32 ulps of 1.0 for non-degenerate rows)
    - Mode-flip byte-identity test present and green (names + to_bits identical across flips)
    - Eval double-encode bitwise-identity test present and green
    - Seeded determinism test present (same seed => identical train-mode output; different seed => different)
    - No tolerance literals referencing fixture families in this file's tests
  </acceptance_criteria>
  <done>ENC-03 path complete and ENC-05 proven: modes flip behavior, never parameters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SentenceBatch input | tokenizer output is trusted-shape but encoder re-validates once (defense in depth for direct constructors) |
| imported weights | non-finite/hostile tensors rejected at import (01-05); encoder assumes validated import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | DoS | forward path on hostile batch | mitigate | single boundary validation with typed SetFitError; no assert!/panic reachable from public API |
| T-1-12 | Repudiation | silent graph detachment in forward | mitigate | grad-flow test per named parameter; 01-08 adds the in-band detach-negative gate + mutants |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib --features setfit,conformance-fixtures encoder_` green
- `cargo clippy -p aprender-core --features setfit -- -D warnings` clean
- `git diff --stat crates/aprender-core/src/models/bert/` empty (D-01 untouched)
- `cargo check -p aprender-core --no-default-features` still green
</verification>

<success_criteria>
- One shared Transformer -> masked mean pooling -> L2 normalization path for batch 1..N mixed lengths (ENC-03 structure)
- HF-dotted named parameters aligned with fixture gradient keys, pooler excluded (D-18)
- Recursive mode switching with byte-identical parameters and deterministic eval (ENC-05)
- HF-verified dropout placement with reproducible seeded policy (discretion area resolved)
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-06-SUMMARY.md` when done.
</output>
