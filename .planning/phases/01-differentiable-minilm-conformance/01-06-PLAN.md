---
phase: 01-differentiable-minilm-conformance
plan: 06
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-05, 01-09]
files_modified:
  - crates/aprender-core/src/setfit/encoder.rs
  - crates/aprender-core/src/setfit/mod.rs
  - crates/aprender-core/src/setfit/error.rs
  - crates/aprender-core/src/nn/transformer/mod.rs
  - crates/aprender-core/src/nn/transformer/positional_encoding.rs
autonomous: true
requirements: [ENC-03, ENC-05]

must_haves:
  truths:
    - "A developer can run single or mixed-length padded SentenceBatches through one shared Transformer -> masked mean pooling -> L2 normalization path and get a graph-connected [B,384] (or [B,slice_hidden]) tensor"
    - "named_parameters() returns HF dotted names verbatim (encoder.layer.0.attention.self.query.weight style), pooler excluded, in exactly the order recorded in gradients.json.parameter_order"
    - "A SentenceBatch produced by a different tokenizer is rejected with a typed error before any compute (D-08 at the boundary)"
    - "set_training flips dropout at all four HF-verified sites recursively; registered parameters are byte-identical across mode flips; eval-mode double-encode is bitwise deterministic"
    - "Backward from a scalar loss over a mixed-length REAL-SLICE batch yields finite gradients on every named parameter and a non-zero aggregate gradient per contracted component"
  artifacts:
    - path: "crates/aprender-core/src/setfit/encoder.rs"
      provides: "BertSentenceEncoder: graph-connected batched forward, Module impl, seeded dropout policy"
      contains: "pub struct BertSentenceEncoder"
  key_links:
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/autograd/ops/"
      via: "embedding_gather / additive_attention_mask / masked_mean_pool / l2_normalize_rows / gelu_exact"
      pattern: "embedding_gather"
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/nn/transformer/mod.rs"
      via: "MultiHeadAttention::forward_self (mod.rs:225) with additive mask; seeded attention-probs dropout hook added to MHA construction"
      pattern: "forward_self"
    - from: "crates/aprender-core/src/setfit/encoder.rs"
      to: "crates/aprender-core/src/setfit/import.rs"
      via: "construction from MiniLmImport (full pin or slice fixture, incl. VocabRemap)"
      pattern: "MiniLmImport"
---

<objective>
Build `BertSentenceEncoder` (D-01): the fully graph-connected batched forward path
(embeddings -> 6x BERT layer -> masked mean pool -> L2 normalize) as a new type in the setfit
module, implementing the extended Module trait with HF-dotted names (D-18), HF-verified dropout
placement with a deterministic seeded-RNG policy, and recursive train/eval switching (ENC-05).
`models/bert/` stays untouched (beyond 01-05's sanctioned A-01 visibility line).

Purpose: ENC-03's shared path and ENC-05's mode contract materialize here, on top of the proven
ops (01-03), the repaired mask + exact GELU (01-09), named Module (01-02), and the
import/tokenizer boundary (01-05).
Output: encoder type with construction from MiniLmImport, mode/dropout/determinism tests green.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **File scope corrected.** `MultiHeadAttention` is at `nn/transformer/mod.rs:93` with
  `forward_self` at :225 — verified. `attention_gqa.rs` holds `LinearAttention` and
  `GroupedQueryAttention`, which are NOT the BERT path; it is removed from files_modified.
  `positional_encoding.rs` is ADDED, because the attention dropout helper (`apply_dropout`, :401)
  lives there and the seeded hook has to reach it.
- **The seeded-dropout extension is the PRIMARY path, not a contingency.** A5 is resolved by
  inspection, not by the spike: `scaled_dot_product_attention` (mod.rs:66-70) calls `apply_dropout`
  (positional_encoding.rs:401), which calls `nn::functional::dropout(x, p, training)`
  (functional.rs:333) — a signature with NO seed parameter. The internal attention-probs dropout is
  therefore not seedable today. Plan for the extension; do not plan for a discovery.
- **Exact GELU.** The FFN uses `Tensor::gelu_exact` (01-09), matching the pinned config's
  `hidden_act: "gelu"`. Using the tanh `Tensor::gelu` would make the FFN parity fixtures fail
  against a correct implementation.
- **Sequence bound corrected** to `min(sentence_max, config.max_position_embeddings)` — the slice
  has only 64 position rows, so a hardcoded `<= 256` would admit an out-of-range position gather.
- **Parameter order is proven against an ordered array** (`gradients.json.parameter_order`), not
  against JSON object keys, which carry no ordering guarantee.
- **Dropout-site count gets a real introspection mechanism** rather than a behavioral proxy.
- **Tokenizer identity is validated at the forward boundary** — this is how D-08 is actually
  enforced now that the fixture path needs public lower-level constructors.
- **The grad-flow assertion is restated** to finite-everywhere + non-zero aggregate per component,
  with the analytically-zero key bias asserted NEAR ZERO. This test also carries the "real-weight
  mixed-batch graph" proof the locked ordering asks for, since it runs on the real slice weights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md
@.planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md
@.planning/phases/01-differentiable-minilm-conformance/01-09-SUMMARY.md

<interfaces>
```rust
// setfit/encoder.rs (feature = "setfit")
pub struct BertSentenceEncoder { /* embeddings, layers: Vec<...>, config, remap, tokenizer_sha256, root_seed, training */ }
impl BertSentenceEncoder {
    pub fn from_import(import: &MiniLmImport, root_seed: u64) -> Result<Self, SetFitError>;
    /// Graph-connected token states [B, S, H]
    pub fn forward_tokens(&self, batch: &SentenceBatch) -> Result<Tensor, SetFitError>;
    /// forward_tokens -> masked_mean_pool -> l2_normalize_rows(eps 1e-12): [B, H], graph-connected
    pub fn encode(&self, batch: &SentenceBatch) -> Result<Tensor, SetFitError>;
    /// Maximum accepted padded sequence length for THIS encoder:
    /// min(SENTENCE_MAX = 256, config.max_position_embeddings)
    pub fn max_seq(&self) -> usize;
    /// Ordered dotted names of every active dropout site. Used by the ENC-05 site-count test.
    #[cfg(test)]
    pub(crate) fn dropout_sites(&self) -> Vec<String>;
}
impl Module for BertSentenceEncoder { /* named_parameters HF-dotted, set_training recursive */ }
```
HF dotted names (verbatim, pooler excluded): embeddings.word_embeddings.weight,
embeddings.position_embeddings.weight, embeddings.token_type_embeddings.weight,
embeddings.LayerNorm.{weight,bias}, and per layer N:
encoder.layer.N.attention.self.{query,key,value}.{weight,bias},
encoder.layer.N.attention.output.dense.{weight,bias},
encoder.layer.N.attention.output.LayerNorm.{weight,bias},
encoder.layer.N.intermediate.dense.{weight,bias},
encoder.layer.N.output.dense.{weight,bias},
encoder.layer.N.output.LayerNorm.{weight,bias}.

Dropout sites (HF transformers v4.57.1 verified, p = 0.1): (1) embeddings post-LayerNorm,
(2) attention probs post-softmax pre-@V, (3) attention output dense -> dropout -> residual+LN,
(4) FFN output dense -> dropout -> residual+LN.
Seed policy: per-site seed = stable_hash(root_seed, dotted_site_name) fed to Dropout::with_seed.

MHA seeded-dropout extension (additive, default None, no behavior change for existing callers):
```rust
// nn/transformer/mod.rs
impl MultiHeadAttention {
    /// Optional seeded attention-probs dropout. None (default) preserves today's behavior:
    /// nn::functional::dropout with the ambient RNG.
    #[must_use]
    pub fn with_attention_dropout_seed(mut self, seed: u64) -> Self;
}
// nn/transformer/positional_encoding.rs
pub(super) fn apply_dropout_seeded(x: &Tensor, p: f32, seed: Option<u64>) -> Tensor;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BertSentenceEncoder structure, embeddings, layer stack, Module impl, boundary validation</name>
  <files>crates/aprender-core/src/setfit/encoder.rs, crates/aprender-core/src/setfit/mod.rs, crates/aprender-core/src/setfit/error.rs</files>
  <read_first>
    - .planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md (spike findings: batch>1 grad status; A5 confirmation)
    - .planning/phases/01-differentiable-minilm-conformance/01-09-SUMMARY.md (repaired add_mask shapes; gelu_exact accuracy result)
    - crates/aprender-core/src/models/bert/layer.rs (:78 — composition order reference ONLY; do not call into models/bert/ beyond the A-01 read_tensor reuse already made in 01-05)
    - crates/aprender-core/src/nn/transformer/mod.rs (MultiHeadAttention :93; new/with_dropout :123-149; q_proj_mut/k_proj_mut/v_proj_mut/out_proj_mut :157-174 — the weight-install path; forward_self :225; Module impl incl. the named override added by 01-02 at :243)
    - crates/aprender-core/src/nn/dropout/mod.rs (with_seed :77-88; PMAT-922 constant-mask mul pattern :110-135)
    - crates/aprender-core/src/setfit/import.rs (MiniLmImport + VocabRemap surface from 01-05)
    - crates/aprender-core/tests/fixtures/setfit/gradients.json (parameter_order array — the name-order target)
  </read_first>
  <behavior>
    - from_import(slice fixture) builds an encoder whose named_parameters() name SEQUENCE equals gradients.json.parameter_order exactly (Vec<String> equality — same names, same count, same order, no pooler.*)
    - forward_tokens on a mixed-length 2-sentence SentenceBatch returns [2, S, H] with requires_grad true when weights require grad
    - Canonical ids are remapped to slice rows INSIDE the encoder via MiniLmImport::vocab_remap(); the SentenceBatch is never mutated, and a canonical id outside the slice closure yields SetFitError::VocabOutOfSlice
    - A SentenceBatch whose tokenizer_sha256 differs from the encoder's returns a typed error BEFORE any compute (D-08 boundary enforcement)
    - A SentenceBatch with an OOV id, mismatched mask length, an all-padding row, or seq > max_seq() returns a typed SetFitError BEFORE any layer compute; for the slice, max_seq() == 64 (min(256, 64)), so a seq of 100 is rejected even though it is under 256
    - backward() from a scalar loss over forward_tokens output on a MIXED-LENGTH REAL-SLICE batch produces finite gradients on every named parameter and a non-zero aggregate gradient per ENC-04 component; attention key biases are asserted NEAR ZERO (the analytically-zero case, see gradients.json.analytically_zero)
  </behavior>
  <action>
    Implement the encoder per interfaces block.

    Embeddings: three embedding_gather calls (word ids — after applying `VocabRemap::to_slice_row`
    when the import carries a remap; position ids 0..S per row; token type ids) combined with
    autograd-aware add, then LayerNorm then site-1 Dropout.

    Layers: per layer, `MultiHeadAttention::forward_self` (nn/transformer/mod.rs:225) with the
    `additive_attention_mask` op output — masking is applied by the repaired `add_mask` from 01-09,
    so the broadcast is correct and graph-connected. Then attention-output dense -> dropout ->
    residual add -> LN, then intermediate dense + **`Tensor::gelu_exact`** (01-09; NOT
    `Tensor::gelu`, which is the tanh approximation and a different function) -> output dense ->
    dropout -> residual add -> LN. Composition built ONLY from nn/ building blocks and the new ops;
    every intermediate flows through autograd-aware operations (add/mul/module forwards). Any
    `Tensor::new`/`from_vec` on a computed value without an adjacent grad_fn is the PMAT-913/914/922
    bug class — forbidden.

    Boundary validation, once, before any layer compute, each with its own SetFitError variant:
    tokenizer identity (`batch.tokenizer_sha256 == self.tokenizer_sha256`), id/type/mask length
    agreement, canonical ids resolvable through the remap (or within vocab for the full pin),
    no all-padding rows, and `batch.seq <= self.max_seq()` where
    `max_seq() = min(SENTENCE_MAX, config.max_position_embeddings)`. Ops afterward still return
    their own Results.

    Module impl: named_parameters with the exact HF dotted names from the interfaces block (D-18 —
    must equal torch `named_parameters()` output verbatim so gradients.json keys align with zero
    translation); positional parameters() in the same order. Prove the ORDER against
    `gradients.json.parameter_order` (an ordered array), not against object keys.

    Annotate forward_tokens (or encode) with the contract macro (equation setfit_encoder_forward,
    contract setfit-encoder-conformance-v1, D-27).

    Tests in-module (#[cfg(all(test, feature = "setfit"))], slice-based tests additionally gated on
    conformance-fixtures): name-ORDER equality vs parameter_order, boundary rejection matrix
    (including the foreign-tokenizer case and the seq-64-vs-256 case), remap failure, and the
    real-slice mixed-length grad-flow test. Test fn names start `encoder_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features conformance-fixtures encoder_</automated>
  </verify>
  <acceptance_criteria>
    - encoder.rs contains `pub struct BertSentenceEncoder` implementing Module with named_parameters returning HF dotted names; a test asserts `Vec<String>` ORDER equality with gradients.json.parameter_order
    - The FFN calls `gelu_exact` and encoder.rs contains no call to `.gelu()` (source assertion)
    - No import of crate::models::bert::embeddings (source assertion — the asserting/unchecked path is not called)
    - `max_seq()` is computed as a min over the config, and a test rejects a batch with seq between max_position_embeddings and 256 on the slice
    - Boundary tests assert typed errors for foreign tokenizer hash, OOV/out-of-slice id, length mismatch, all-padding row, and oversize seq
    - Grad-flow test runs on the REAL slice weights with a mixed-length batch, asserts finite grads on every named parameter and non-zero aggregate per component, and asserts the key biases are near zero rather than non-zero
    - `cargo test -p aprender-core --lib --features conformance-fixtures encoder_` exits 0
  </acceptance_criteria>
  <done>Graph-connected encoder with verbatim HF names in proven order, fail-closed boundary, and real-weight mixed-batch gradient evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: encode() pipeline, seeded dropout policy incl. the MHA attention-probs hook, mode contract (ENC-05)</name>
  <files>crates/aprender-core/src/setfit/encoder.rs, crates/aprender-core/src/nn/transformer/mod.rs, crates/aprender-core/src/nn/transformer/positional_encoding.rs</files>
  <read_first>
    - crates/aprender-core/src/setfit/encoder.rs (Task 1 state)
    - crates/aprender-core/src/nn/transformer/mod.rs (:37-77 scaled_dot_product_attention — dropout_p/training plumbing at :66-70; MultiHeadAttention fields at :93-109 and with_dropout at :146)
    - crates/aprender-core/src/nn/transformer/positional_encoding.rs (apply_dropout at :401 — delegates to nn::functional::dropout; add_mask at :366 as repaired by 01-09 — do not re-touch it)
    - crates/aprender-core/src/nn/functional.rs (:333 `pub fn dropout(x, p, training)` — no seed parameter; this is why the hook is needed)
    - crates/aprender-core/src/nn/dropout/mod.rs (with_seed :77-88 — the seeded RNG the hook routes to)
    - crates/aprender-core/src/nn/tests_named_module.rs (snapshot_named byte-identity helper from 01-02 — reuse, don't duplicate)
    - crates/aprender-core/src/autograd/mod.rs (no_grad at :76)
  </read_first>
  <behavior>
    - encode() == l2_normalize_rows(masked_mean_pool(forward_tokens(batch), mask), 1e-12); output rows have unit L2 norm
    - Train mode: dropout_sites() returns exactly 1 + 3*num_layers dotted names (slice: 7), each unique; the count test reads that list rather than inferring from behavior
    - Each site's mask is deterministic given root_seed: two encoders with the same seed produce identical train-mode outputs; a different root_seed produces different output
    - The attention-probs site is genuinely seeded: two encoders with the same seed produce identical attention-probs dropout patterns, and the existing unseeded behavior is preserved for callers that do not opt in (a MultiHeadAttention built without with_attention_dropout_seed behaves exactly as before)
    - Eval mode: no_grad(|| encode(batch)) twice yields bitwise-identical outputs (to_bits equality)
    - set_training(true/false) flips every dropout site recursively; snapshot_named byte-identity across train->eval->train (registered parameters unchanged — ENC-05)
    - Dropout empirical drop rate over a large tensor within [0.08, 0.12] for p=0.1 (statistics test, Rust-side only per D-16)
  </behavior>
  <action>
    Wire encode() through the 01-03 ops with explicit eps 1e-12.

    **Seeded attention-probs dropout (primary path, A5 confirmed unseedable).** Add, additively:
    (a) `apply_dropout_seeded(x, p, seed: Option<u64>)` in positional_encoding.rs — `None` delegates
        to today's `apply_dropout` so existing callers are byte-for-byte unchanged; `Some(seed)`
        routes through the seeded `Dropout::with_seed` path.
    (b) An optional `attention_dropout_seed: Option<u64>` field on `MultiHeadAttention` with a
        `with_attention_dropout_seed(seed)` builder (default `None`), threaded through
        `forward_qkv` -> `scaled_dot_product_attention` -> the dropout call site at mod.rs:66-70.
    Do NOT re-implement attention math and do NOT alter `add_mask` (01-09 owns it). Keep the
    existing `scaled_dot_product_attention` signature source-compatible for other callers — add the
    seed as an extra parameter only if every call site is updated in the same change, otherwise
    carry it on the MHA struct and read it inside `forward_qkv`. Record which shape was chosen.

    Implement the encoder seed policy from the interfaces block: derive per-site seeds via a stable
    non-positional hash of (root_seed, dotted site name) — document that renaming a site changes its
    stream (acceptable; positional drift is not). Implement set_training recursion across the
    embeddings dropout, per-layer dropouts, and the attention module. RNG/seed state must NOT appear
    in named_parameters (Pitfall 7).

    Add `#[cfg(test)] pub(crate) fn dropout_sites(&self) -> Vec<String>` returning the ordered dotted
    site names, so the site-count assertion is exact introspection rather than a behavioral proxy
    (a proxy cannot distinguish "site missing" from "site present but p effectively 0").

    Tests: the behavior bullets; mode-flip test reuses the 01-02 `snapshot_named` helper. Add a
    regression test proving an MHA constructed WITHOUT the seed builder behaves identically to
    before the change. Test fn names start `encoder_mode_` / `encoder_encode_` / `mha_seeded_dropout_`.
    Fixture-parity comparisons are NOT in this plan — they live in 01-08 after tolerances (D-14);
    only structural/determinism/mode behavior is asserted here.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features conformance-fixtures encoder_mode_ && cargo test -p aprender-core --lib --features conformance-fixtures encoder_encode_ && cargo test -p aprender-core --lib mha_seeded_dropout_</automated>
  </verify>
  <acceptance_criteria>
    - encode() present; unit-norm assertion in tests (norm within f32 ulps of 1.0 for non-degenerate rows)
    - `dropout_sites()` exists and the site-count test asserts its length == 1 + 3*num_layers and that all names are unique
    - Seeded attention dropout proven: same-seed encoders produce identical train-mode attention dropout; a no-seed MultiHeadAttention is proven unchanged by a dedicated regression test
    - Mode-flip byte-identity test present and green (names + to_bits identical across flips), reusing 01-02's snapshot_named rather than a local copy
    - Eval double-encode bitwise-identity test present and green
    - No tolerance literals referencing fixture families in this file's tests
    - `git diff crates/aprender-core/src/nn/transformer/positional_encoding.rs` shows no change to `add_mask` (01-09's repair untouched)
    - `cargo check -p aprender-core --no-default-features` still green (the MHA change is ungated and must not require setfit)
  </acceptance_criteria>
  <done>ENC-03 path complete and ENC-05 proven: modes flip behavior, never parameters; attention dropout is reproducibly seeded without re-implementing attention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SentenceBatch input | a batch may come from a foreign tokenizer or a direct constructor; the encoder re-validates identity and shape once |
| imported weights | non-finite/hostile tensors rejected at import (01-05); encoder assumes validated import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-11 | DoS | forward path on hostile batch | mitigate | single boundary validation with typed SetFitError; sequence bound is min(256, max_position_embeddings) so an over-long batch cannot drive an out-of-range position gather; no assert!/panic reachable from public API |
| T-1-21 | Spoofing | batch/encoder tokenizer mismatch | mitigate | tokenizer_sha256 equality checked at every forward call — the enforceable form of D-08 |
| T-1-12 | Repudiation | silent graph detachment in forward | mitigate | real-slice mixed-length grad-flow test per named parameter with per-component aggregates; 01-08 adds the in-band detach-negative gate + mutants |
| T-1-19 | Tampering | wrong activation silently selected | mitigate | FFN calls gelu_exact and a source assertion forbids `.gelu()` in encoder.rs; 01-05 rejects a config whose hidden_act selects the tanh variant |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib --features conformance-fixtures encoder_` green (single positional filter per invocation; cargo rejects a second — verified 2026-08-08)
- `cargo test -p aprender-core --lib transformer` green (no regression from the MHA seed hook)
- `cargo clippy -p aprender-core --features setfit -- -D warnings` clean
- `git diff --stat crates/aprender-core/src/models/bert/` shows only 01-05's A-01 line (D-01 respected)
- `cargo check -p aprender-core --no-default-features` still green
</verification>

<success_criteria>
- One shared Transformer -> masked mean pooling -> L2 normalization path for batch 1..N mixed lengths (ENC-03 structure), using the repaired broadcast mask and the exact erf GELU
- HF-dotted named parameters proven equal IN ORDER to the frozen parameter_order array, pooler excluded (D-18)
- Recursive mode switching with byte-identical parameters and deterministic eval (ENC-05)
- HF-verified dropout placement with a reproducible seeded policy at all four site classes, including the previously unseedable attention-probs site (A5 discharged)
- Tokenizer/encoder identity enforced at the forward boundary (D-08)
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
