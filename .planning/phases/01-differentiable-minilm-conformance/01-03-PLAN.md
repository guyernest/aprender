---
phase: 01-differentiable-minilm-conformance
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - crates/aprender-core/src/autograd/ops/mod.rs
  - crates/aprender-core/src/autograd/ops/normalize.rs
  - crates/aprender-core/src/autograd/ops/similarity.rs
  - crates/aprender-core/src/autograd/ops/tests_normalize_backward.rs
  - crates/aprender-core/src/autograd/ops/tests_similarity_backward.rs
  - crates/aprender-core/src/autograd/grad_fn.rs
  - crates/aprender-core/src/autograd/tests_matmul_backward.rs
  - crates/aprender-core/tests/batched_graph_spike.rs
autonomous: true
requirements: [ENC-03, ENC-06]

must_haves:
  truths:
    - "l2_normalize_rows, cosine_similarity_rows, and mse_loss are graph-connected ops whose analytic backwards match central finite differences per element"
    - "A 2-sentence mixed-length batch flows gather -> 2 attention/LayerNorm layers -> masked pool -> normalize -> cosine-MSE -> backward with a finite non-zero gradient on EVERY parameter tensor (the phase's highest-risk unknown, resolved)"
    - "A single sentence encoded alone and inside a padded batch produces the same embedding (padding invariance, eval mode)"
  artifacts:
    - path: "crates/aprender-core/src/autograd/ops/normalize.rs"
      provides: "Row-wise L2 normalization with explicit epsilon"
      contains: "pub fn l2_normalize_rows"
    - path: "crates/aprender-core/src/autograd/ops/similarity.rs"
      provides: "cosine_similarity_rows + mse_loss reduction"
      contains: "pub fn cosine_similarity_rows"
    - path: "crates/aprender-core/tests/batched_graph_spike.rs"
      provides: "End-to-end batched gradient-flow proof through existing MHA/LayerNorm broadcast paths (RESEARCH Open Question 1)"
  key_links:
    - from: "crates/aprender-core/tests/batched_graph_spike.rs"
      to: "crates/aprender-core/src/nn/transformer/attention_gqa.rs"
      via: "MultiHeadAttention::forward_self with additive_attention_mask output"
      pattern: "forward_self"
    - from: "crates/aprender-core/src/autograd/ops/similarity.rs"
      to: "crates/aprender-core/src/autograd/grad_fn.rs"
      via: "CosineSimilarityBackward + MseBackward recorded via with_graph"
      pattern: "CosineSimilarityBackward"
---

<objective>
Land the remaining three differentiable primitives (row L2 normalize, cosine similarity, MSE
reduction) with FD tests and contract annotations, then run the batched-graph spike: prove
end-to-end gradient flow at batch > 1 through the existing MultiHeadAttention/LayerNorm broadcast
paths — the phase's single highest technical risk (RESEARCH Pitfall 2 / Open Question 1).

Purpose: completes D-03/D-04; de-risks the full encoder (01-06) before it is built; resolves
Assumption A5 (SDPA dropout seeding).
Output: six-of-six ops done; spike test committed as a permanent regression; A5 finding recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md

<interfaces>
```rust
// autograd/ops/normalize.rs — new L2NormalizeRowsBackward
pub fn l2_normalize_rows(x: &Tensor /*[B,H]*/, eps: f32) -> Tensor /*[B,H]*/;
// x_row / max(||x_row||_2, eps); infallible given [B,H] input, validate rank/shape -> OpError if violated

// autograd/ops/similarity.rs — new CosineSimilarityBackward, MseBackward
pub fn cosine_similarity_rows(a: &Tensor /*[B,H]*/, b: &Tensor /*[B,H]*/, eps: f32)
    -> Result<Tensor /*[B]*/, OpError>;   // shape mismatch -> typed error
pub fn mse_loss(pred: &Tensor /*[B]*/, target: &[f32]) -> Result<Tensor /*[1]*/, OpError>;
```
`OpError` is the shared enum created in plan 01-01 (extend with variants as needed).
Rowwise backward loops copy the SoftmaxLastDimBackward rows-by-features iteration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: l2_normalize_rows + L2NormalizeRowsBackward + FD test</name>
  <files>crates/aprender-core/src/autograd/ops/normalize.rs, crates/aprender-core/src/autograd/ops/mod.rs, crates/aprender-core/src/autograd/grad_fn.rs, crates/aprender-core/src/autograd/ops/tests_normalize_backward.rs, crates/aprender-core/src/autograd/tests_matmul_backward.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/ops/mod.rs (3-step pattern; how 01-01 wired embedding/masking/pooling — follow the same style)
    - crates/aprender-core/src/autograd/grad_fn.rs (SoftmaxLastDimBackward rowwise loop at :1146-1170 — backward template; test_all_backward_names registry)
    - crates/aprender-core/src/nn/conv/tests_pool_flatten_backward_gradflow.rs (FD harness)
  </read_first>
  <behavior>
    - l2_normalize_rows([[3,4],[0.6,0.8]], eps 1e-12) == [[0.6,0.8],[0.6,0.8]]; each output row has L2 norm 1.0 within f32 ulps
    - Near-zero row uses max(norm, eps) — output finite, no NaN
    - Analytic backward (d/dx of x/||x||: (I - y y^T)/||x|| per row) matches central finite differences per element on a non-degenerate [3,5] case
  </behavior>
  <action>
    Implement per the 3-step op pattern with explicit eps parameter (no hidden default). Add
    `L2NormalizeRowsBackward` to grad_fn.rs (pub(crate), rowwise loop, capture forward outputs or
    norms as needed); register the name in test_all_backward_names. Annotate with the contract
    macro (equation l2_normalize_rows, contract id setfit-encoder-conformance-v1). FD test fn names
    start `l2_normalize_rows_`. Row-major throughout.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib l2_normalize_rows</automated>
  </verify>
  <acceptance_criteria>
    - grad_fn.rs contains L2NormalizeRowsBackward; name registered in test_all_backward_names
    - FD test covers every input element; includes a near-zero-row case asserting finite output and finite grads
    - `cargo test -p aprender-core --lib l2_normalize_rows` exits 0
  </acceptance_criteria>
  <done>Normalize op graph-connected and FD-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cosine_similarity_rows + mse_loss + backwards + FD tests</name>
  <files>crates/aprender-core/src/autograd/ops/similarity.rs, crates/aprender-core/src/autograd/ops/mod.rs, crates/aprender-core/src/autograd/grad_fn.rs, crates/aprender-core/src/autograd/ops/tests_similarity_backward.rs, crates/aprender-core/src/autograd/tests_matmul_backward.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/ops/mod.rs (mean at :339-361 — the reduction-to-[1] template for mse_loss)
    - crates/aprender-core/src/autograd/grad_fn.rs (two-input backward precedent, e.g. MatMul/Add backward returning Vec of two grads)
    - crates/aprender-core/src/nn/loss.rs and crates/aprender-core/src/nn/self_supervised.rs (ANTI-analogs: f32-returning — read only to confirm you are NOT reusing their functions or names)
  </read_first>
  <behavior>
    - cosine_similarity_rows(a, a, eps) == vector of 1.0s; orthogonal rows -> 0.0; anti-parallel -> -1.0
    - Backward returns grads for BOTH inputs; each matches central finite differences per element
    - mse_loss(pred [B], target len B) == mean((pred-target)^2) as a [1] tensor with requires_grad when pred requires it; length mismatch -> Err(OpError::LengthMismatch)
    - Composition mse_loss(cosine_similarity_rows(a,b), labels).backward() yields finite non-zero grads on a and b (graph-connected end to end)
  </behavior>
  <action>
    Implement both ops in similarity.rs per the 3-step pattern. Add CosineSimilarityBackward
    (two-input; capture a, b data and norms) and MseBackward (capture pred, target) to grad_fn.rs;
    register both names. Annotate with contract macros (equations cosine_similarity_rows, mse_loss).
    Do NOT call or re-export anything from nn/loss.rs or nn/self_supervised.rs — their f32 returns
    are the PF-001 trap. FD test fn names start `cosine_similarity_rows_` / `mse_loss_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib cosine_similarity_rows && cargo test -p aprender-core --lib mse_loss && cargo test -p aprender-core --lib test_all_backward_names</automated>
  </verify>
  <acceptance_criteria>
    - similarity.rs contains both pub fns returning Result<Tensor, OpError>; no import from nn/loss.rs or nn/self_supervised.rs (source assertion)
    - grad_fn.rs contains CosineSimilarityBackward and MseBackward; names registered
    - FD tests cover both inputs of cosine and the pred input of mse, per element
    - Composition test proves grads reach BOTH embedding inputs through the composed graph
  </acceptance_criteria>
  <done>All six D-03 primitives landed, FD-verified, contract-annotated.</done>
</task>

<task type="auto">
  <name>Task 3: Batched mixed-length graph spike through existing MHA/LayerNorm</name>
  <files>crates/aprender-core/tests/batched_graph_spike.rs</files>
  <read_first>
    - crates/aprender-core/src/nn/transformer/attention_gqa.rs (forward_self at :373-376; forward_qkv mask/dropout plumbing at :320-375; determine how scaled_dot_product_attention consumes dropout_p and training — this is the A5 inspection)
    - crates/aprender-core/src/autograd/mod.rs (no_grad at :76; get_grad / clear_graph API)
    - .planning/phases/01-differentiable-minilm-conformance/01-RESEARCH.md (Pitfall 2 and Open Questions 1 & 3 — what the spike must answer)
  </read_first>
  <action>
    Write an UNGATED integration test (tests/batched_graph_spike.rs, no setfit feature — uses only
    ungated ops + nn building blocks) that constructs a miniature 2-layer encoder chain with
    seeded-random weights (graph-flow proof, not numerical parity — synthetic weights are
    legitimate here; conformance fixtures remain real-weight per D-09): embedding_gather (vocab 32,
    hidden 16) + position gather + add -> LayerNorm -> per layer {MultiHeadAttention::forward_self
    with additive_attention_mask([B,1,1,S]) built by the 01-01 op, residual add, LayerNorm, small
    FFN of two Linear + GELU, residual add, LayerNorm} x2 -> masked_mean_pool -> l2_normalize_rows
    -> cosine_similarity_rows against a second encoded batch -> mse_loss -> backward().
    Batch: 2 sentences, lengths 5 and 9, padded to 9 (dropout off / p=0).
    Assert (a) EVERY parameter tensor in the chain (embedding weight, all Q/K/V/output Linear
    weights+biases, LayerNorm weights+biases, FFN weights+biases) receives a grad that is finite
    AND has at least one element with |g| > 1e-9 — name each tensor in the assertion message;
    (b) grads are not identical across the two batch rows' contributions where inputs differ;
    (c) padding invariance: encoding sentence A alone (batch 1) equals its row in the padded batch
    within 1e-5 (forward, no dropout).
    Record the A5 finding in the test's doc comment AND the SUMMARY: whether MHA's internal SDPA
    dropout is seedable; if not, note that plan 01-06 must apply attention-probs dropout via an
    extension of MultiHeadAttention construction (preferred) rather than re-implementing attention.
    If broadcast backward is BROKEN at batch>1, fixing it is in-scope: fix in the responsible op or
    grad_fn (this is the additive-mask primitive's proof), and document the fix in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --test batched_graph_spike</automated>
  </verify>
  <acceptance_criteria>
    - batched_graph_spike.rs asserts finite + non-zero grads per named parameter tensor (not a single aggregate any() over all)
    - Padding-invariance assertion present with tolerance 1e-5 or tighter
    - Test passes: `cargo test -p aprender-core --test batched_graph_spike` exits 0
    - Doc comment records the A5 SDPA-dropout-seeding finding
  </acceptance_criteria>
  <done>Batch>1 gradient flow proven end-to-end; A5 resolved; regression locked in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public op entry points | caller-supplied shapes/targets untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | DoS | similarity/normalize ops | mitigate | typed OpError on shape/length mismatch; eps guards prevent divide-by-zero NaN propagation |
| T-1-05 | Repudiation | silent detachment at batch>1 | mitigate | spike asserts per-tensor non-zero grads — a severed broadcast path fails loudly, permanently |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib l2_normalize_rows cosine_similarity_rows mse_loss` green
- `cargo test -p aprender-core --test batched_graph_spike` green
- `cargo clippy -p aprender-core -- -D warnings` clean
- `cargo build -p aprender-core --no-default-features` compiles (ops remain ungated)
</verification>

<success_criteria>
- Six of six D-03 primitives complete with FD tests (D-04) and #[contract] annotations (D-27)
- Batch>1 gradient flow through MHA/LayerNorm broadcast paths proven or fixed (Open Question 1 closed)
- A5 (SDPA dropout seeding) answered with evidence, unblocking plan 01-06's dropout design
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md` when done.
</output>
