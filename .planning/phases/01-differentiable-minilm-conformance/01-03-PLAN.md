---
phase: 01-differentiable-minilm-conformance
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-09]
files_modified:
  - crates/aprender-core/src/autograd/ops/mod.rs
  - crates/aprender-core/src/autograd/ops/normalize.rs
  - crates/aprender-core/src/autograd/ops/similarity.rs
  - crates/aprender-core/src/autograd/ops/tests_normalize_backward.rs
  - crates/aprender-core/src/autograd/ops/tests_similarity_backward.rs
  - crates/aprender-core/src/autograd/grad_fn.rs
  - crates/aprender-core/src/autograd/tests_matmul_backward.rs
  - crates/aprender-core/tests/batched_graph_spike.rs
autonomous: true
requirements: [ENC-03, ENC-06]

must_haves:
  truths:
    - "l2_normalize_rows, cosine_similarity_rows, and mse_loss are graph-connected ops returning Result<Tensor, OpError> whose analytic backwards match central finite differences per element on BOTH sides of the epsilon clamp branch"
    - "A 2-sentence mixed-length batch flows gather -> 2 attention/LayerNorm layers -> masked pool -> normalize -> cosine-MSE -> backward with FINITE gradients on every parameter tensor and a non-zero AGGREGATE gradient per contracted component (the phase's highest-risk unknown, resolved)"
    - "A single sentence encoded alone and inside a padded batch produces the same embedding (padding invariance, eval mode)"
    - "The spike is a permanent regression test for the 01-09 attention-mask repair: if broadcast masking ever detaches again, this test fails"
  artifacts:
    - path: "crates/aprender-core/src/autograd/ops/normalize.rs"
      provides: "Row-wise L2 normalization with explicit epsilon and piecewise-documented derivative"
      contains: "pub fn l2_normalize_rows"
    - path: "crates/aprender-core/src/autograd/ops/similarity.rs"
      provides: "cosine_similarity_rows + mse_loss reduction"
      contains: "pub fn cosine_similarity_rows"
    - path: "crates/aprender-core/tests/batched_graph_spike.rs"
      provides: "End-to-end batched gradient-flow proof through the repaired MHA broadcast-mask path (RESEARCH Open Question 1)"
  key_links:
    - from: "crates/aprender-core/tests/batched_graph_spike.rs"
      to: "crates/aprender-core/src/nn/transformer/mod.rs"
      via: "MultiHeadAttention::forward_self (mod.rs:225) with additive_attention_mask output; masking applied by the repaired add_mask (01-09)"
      pattern: "forward_self"
    - from: "crates/aprender-core/src/autograd/ops/similarity.rs"
      to: "crates/aprender-core/src/autograd/grad_fn.rs"
      via: "CosineSimilarityBackward + MseBackward recorded via with_graph"
      pattern: "CosineSimilarityBackward"
---

<objective>
Land the remaining three differentiable primitives (row L2 normalize, cosine similarity, MSE
reduction) with FD tests and contract annotations, then run the batched-graph spike: prove
end-to-end gradient flow at batch > 1 through the existing MultiHeadAttention/LayerNorm broadcast
paths — the phase's single highest technical risk (RESEARCH Pitfall 2 / Open Question 1).

Purpose: completes D-03/D-04; de-risks the full encoder (01-06) before it is built.
Output: all primitives done; spike test committed as a permanent regression.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **`l2_normalize_rows` returns `Result<Tensor, OpError>`.** The previous signature declared a bare
  `Tensor` while the same plan promised typed rank validation — those cannot both be true.
- **The epsilon clamp derivative is piecewise and is now specified as such.** For
  `y = x / max(||x||, eps)`: ABOVE the clamp the derivative is the projected unit-normalization
  Jacobian `(I - y y^T) / ||x||`; BELOW it the denominator is the CONSTANT `eps`, so the derivative
  is simply `I / eps`. Silently applying the projected form below the clamp is wrong. Cosine
  similarity has the identical clamp structure and the identical requirement. FD tests must cover
  BOTH branches.
- **The gradient assertion is restated.** "Every parameter tensor has a non-zero gradient" is
  mathematically unsatisfiable against a correct BERT implementation (`attention.self.key.bias` is
  exactly zero — see the ENC-04 gate wording in the contract authored by 01-01). The spike now
  asserts finite-everywhere plus non-zero AGGREGATE per component.
- **"Gradients differ across batch rows" is replaced with a well-defined test.** A single combined
  backward does not expose per-row contributions; the spike now runs SEPARATE per-row scalar losses
  and compares the resulting gradients, which is measurable.
- **Wave 2 -> wave 3, and now depends on 01-09.** The attention-mask broadcast defect is a known
  fact, not a possible discovery: 01-09 repairs it in wave 2, and this spike becomes its end-to-end
  regression test rather than its discovery mechanism.
- **A5 (SDPA dropout seeding) is RESOLVED BY INSPECTION, not by the spike.** Verified 2026-08-08:
  `scaled_dot_product_attention` (nn/transformer/mod.rs:66-70) calls `apply_dropout`
  (positional_encoding.rs:401), which calls `nn::functional::dropout(x, p, true)` —
  whose signature (functional.rs:333) is `dropout(x: &Tensor, p: f32, training: bool)` and takes NO
  seed. The internal attention-probs dropout is therefore NOT seedable today. Plan 01-06 implements
  the seeded hook as its PRIMARY path, not a contingency. The spike records confirmation only.
- **File scope corrected**: `MultiHeadAttention` lives in `nn/transformer/mod.rs:93` with
  `forward_self` at :225. `attention_gqa.rs` holds `LinearAttention` and `GroupedQueryAttention` —
  different types, not the BERT path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md
@.planning/phases/01-differentiable-minilm-conformance/01-09-SUMMARY.md

<interfaces>
```rust
// autograd/ops/normalize.rs — new L2NormalizeRowsBackward
pub fn l2_normalize_rows(x: &Tensor /*[B,H]*/, eps: f32) -> Result<Tensor /*[B,H]*/, OpError>;
// y_row = x_row / max(||x_row||_2, eps)
// Validates: rank == 2, no zero dimension, eps finite and > 0, all inputs finite -> else OpError

// autograd/ops/similarity.rs — new CosineSimilarityBackward, MseBackward
pub fn cosine_similarity_rows(a: &Tensor /*[B,H]*/, b: &Tensor /*[B,H]*/, eps: f32)
    -> Result<Tensor /*[B]*/, OpError>;   // shape mismatch -> typed error
pub fn mse_loss(pred: &Tensor /*[B]*/, target: &[f32]) -> Result<Tensor /*[1]*/, OpError>;
```
`OpError` is the shared enum created in plan 01-01 (extend with variants as needed — e.g.
`InvalidEpsilon { eps }`, `InvalidRank { expected, got }`; do not rename existing variants).
Rowwise backward loops copy the SoftmaxLastDimBackward rows-by-features iteration.

**Piecewise derivative of the clamp (both ops):**
```
n = ||x_row||_2 ;  d = max(n, eps)
if n >  eps :  dy/dx = (I - y y^T) / n        # d depends on x — projected form
if n <= eps :  dy/dx = I / eps                # d is a CONSTANT — no projection term
```
The branch boundary is exactly `n == eps`; pick one side deterministically (use `n > eps` for the
projected form) and document it. The same structure applies to the cosine denominator
`max(||a||, eps) * max(||b||, eps)`: each factor is independently clamped, giving four branch
combinations, and the derivative drops the corresponding `a`/`b` projection term whenever that
factor is clamped.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: l2_normalize_rows + L2NormalizeRowsBackward + piecewise FD test</name>
  <files>crates/aprender-core/src/autograd/ops/normalize.rs, crates/aprender-core/src/autograd/ops/mod.rs, crates/aprender-core/src/autograd/grad_fn.rs, crates/aprender-core/src/autograd/ops/tests_normalize_backward.rs, crates/aprender-core/src/autograd/tests_matmul_backward.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/ops/mod.rs (3-step pattern; how 01-01 wired embedding/masking/pooling — follow the same style)
    - crates/aprender-core/src/autograd/grad_fn.rs (SoftmaxLastDimBackward rowwise loop at :1146-1170 — backward template; test_all_backward_names registry)
    - crates/aprender-core/src/nn/conv/tests_pool_flatten_backward_gradflow.rs (FD harness)
  </read_first>
  <behavior>
    - l2_normalize_rows([[3,4],[0.6,0.8]], eps 1e-12) == Ok([[0.6,0.8],[0.6,0.8]]); each output row has L2 norm 1.0 within f32 ulps
    - Rank != 2, a zero dimension, eps <= 0, eps non-finite, or a non-finite input element -> Err(OpError::...) naming the condition
    - ABOVE the clamp (||x|| >> eps): analytic backward matches central finite differences per element on a non-degenerate [3,5] case
    - BELOW the clamp (||x|| < eps, e.g. a row of 1e-20 with eps 1e-6): output is finite (no NaN), and the analytic backward equals I/eps per element within FD tolerance — NOT the projected form
    - AT/near the boundary (||x|| within a few ulps of eps): output and grads finite, no NaN, no discontinuous blowup asserted beyond the documented branch choice
  </behavior>
  <action>
    Implement per the 3-step op pattern with an explicit eps parameter (no hidden default) and
    validation FIRST: rank 2, non-zero dims, `eps.is_finite() && eps > 0.0`, all input elements
    finite — each a distinct typed OpError. Add `L2NormalizeRowsBackward` to grad_fn.rs (pub(crate),
    rowwise loop; capture per-row `n` and the clamp decision, or capture enough to recompute it
    deterministically — the backward MUST take the same branch the forward took, so store the branch
    or the raw norm, never re-derive it from the clamped output alone). Register the name in
    test_all_backward_names.

    Implement the derivative piecewise per the interfaces block and put the two-branch formula in a
    doc comment on the backward struct with the reason: below the clamp the denominator is a
    constant, so the projection term vanishes; applying `(I - y y^T)/n` there is simply the wrong
    function and would pass a naive FD test only if the test never visits that branch.

    Annotate with the contract macro (equation l2_normalize_rows, contract id
    setfit-encoder-conformance-v1). FD test fn names start `l2_normalize_rows_`. Row-major throughout.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib l2_normalize_rows</automated>
  </verify>
  <acceptance_criteria>
    - normalize.rs `pub fn l2_normalize_rows` returns `Result<Tensor, OpError>` (source assertion — not a bare Tensor)
    - grad_fn.rs contains L2NormalizeRowsBackward with the piecewise formula documented; name registered in test_all_backward_names
    - FD tests exist for BOTH branches — a dedicated below-eps test asserts the I/eps form, and its name makes the branch explicit (e.g. `l2_normalize_rows_backward_below_epsilon_clamp`)
    - Validation tests cover rank, zero dim, eps <= 0, non-finite eps, non-finite input
    - `cargo test -p aprender-core --lib l2_normalize_rows` exits 0
  </acceptance_criteria>
  <done>Normalize op graph-connected, fallible, and FD-verified on both sides of the clamp.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cosine_similarity_rows + mse_loss + backwards + FD tests</name>
  <files>crates/aprender-core/src/autograd/ops/similarity.rs, crates/aprender-core/src/autograd/ops/mod.rs, crates/aprender-core/src/autograd/grad_fn.rs, crates/aprender-core/src/autograd/ops/tests_similarity_backward.rs, crates/aprender-core/src/autograd/tests_matmul_backward.rs</files>
  <read_first>
    - crates/aprender-core/src/autograd/ops/mod.rs (mean at :339-361 — the reduction-to-[1] template for mse_loss)
    - crates/aprender-core/src/autograd/grad_fn.rs (two-input backward precedent, e.g. MatMul/Add backward returning Vec of two grads)
    - crates/aprender-core/src/nn/loss.rs and crates/aprender-core/src/nn/self_supervised.rs (ANTI-analogs: f32-returning — read only to confirm you are NOT reusing their functions or names)
  </read_first>
  <behavior>
    - cosine_similarity_rows(a, a, eps) == vector of 1.0s; orthogonal rows -> 0.0; anti-parallel -> -1.0
    - Backward returns grads for BOTH inputs; each matches central finite differences per element ABOVE the clamp
    - BELOW the clamp on one input (||a|| < eps, ||b|| normal): the clamped factor contributes no projection term; FD test asserts the analytic grad matches, for both the a-clamped and b-clamped cases
    - Non-finite input, rank/shape mismatch, zero dimension, eps <= 0 -> typed OpError before any compute
    - mse_loss(pred [B], target len B) == mean((pred-target)^2) as a [1] tensor with requires_grad when pred requires it; length mismatch -> Err(OpError::LengthMismatch); a non-finite target value -> Err
    - Composition mse_loss(cosine_similarity_rows(a,b), labels).backward() yields finite non-zero grads on a and b (graph-connected end to end)
  </behavior>
  <action>
    Implement both ops in similarity.rs per the 3-step pattern, with validation before compute. Add
    CosineSimilarityBackward (two-input; capture a, b data, both raw norms, and both clamp decisions
    — same rule as Task 1: the backward must take the branch the forward took) and MseBackward
    (capture pred, target) to grad_fn.rs; register both names. Annotate with contract macros
    (equations cosine_similarity_rows, mse_loss). Do NOT call or re-export anything from nn/loss.rs
    or nn/self_supervised.rs — their f32 returns are the PF-001 trap. FD test fn names start
    `cosine_similarity_rows_` / `mse_loss_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib cosine_similarity_rows && cargo test -p aprender-core --lib mse_loss && cargo test -p aprender-core --lib test_all_backward_names</automated>
  </verify>
  <acceptance_criteria>
    - similarity.rs contains both pub fns returning Result<Tensor, OpError>; no import from nn/loss.rs or nn/self_supervised.rs (source assertion)
    - grad_fn.rs contains CosineSimilarityBackward and MseBackward; names registered
    - FD tests cover both inputs of cosine and the pred input of mse, per element, ABOVE the clamp; plus dedicated below-clamp tests for the a-clamped and b-clamped cases
    - Composition test proves grads reach BOTH embedding inputs through the composed graph
  </acceptance_criteria>
  <done>All D-03 primitives landed, FD-verified on both clamp branches, contract-annotated.</done>
</task>

<task type="auto">
  <name>Task 3: Batched mixed-length graph spike through the repaired MHA broadcast-mask path</name>
  <files>crates/aprender-core/tests/batched_graph_spike.rs</files>
  <read_first>
    - .planning/phases/01-differentiable-minilm-conformance/01-09-SUMMARY.md (the add_mask repair: what changed, which shapes are now covered — this spike is its end-to-end regression test)
    - crates/aprender-core/src/nn/transformer/mod.rs (MultiHeadAttention at :93; forward_self at :225; forward_qkv at :189; scaled_dot_product_attention at :37 — note dropout_p/training plumbing at :66-70)
    - crates/aprender-core/src/nn/functional.rs (:333 `pub fn dropout(x, p, training)` — NO seed parameter; this is the A5 confirmation, already established, just cite it)
    - crates/aprender-core/src/autograd/mod.rs (no_grad at :76; get_grad / clear_graph API)
    - .planning/phases/01-differentiable-minilm-conformance/01-RESEARCH.md (Pitfall 2 and Open Question 1 — what the spike must answer)
  </read_first>
  <action>
    Write an UNGATED integration test (tests/batched_graph_spike.rs, no setfit feature — uses only
    ungated ops + nn building blocks) that constructs a miniature 2-layer encoder chain with
    seeded-random weights (graph-flow proof, not numerical parity — synthetic weights are
    legitimate HERE; real-weight conformance is D-09's job and lands in 01-06/01-08):

      embedding_gather (vocab 32, hidden 16) + position gather + autograd-aware add
      -> LayerNorm
      -> per layer { MultiHeadAttention::forward_self (nn/transformer/mod.rs:225) with the
                     additive_attention_mask([B,1,1,S]) built by the 01-01 op, residual add,
                     LayerNorm, small FFN of two Linear + **gelu_exact** (01-09 — the BERT-correct
                     activation, not the tanh gelu), residual add, LayerNorm } x2
      -> masked_mean_pool -> l2_normalize_rows
      -> cosine_similarity_rows against a second encoded batch -> mse_loss -> backward()

    Batch: 2 sentences, lengths 5 and 9, padded to 9 (dropout off / p = 0).
    Use hidden 16 with 2 heads of 8 so the mask broadcast is exercised at H > 1, and make the two
    batch rows' masks DIFFERENT (lengths 5 vs 9) so a wrong-axis broadcast cannot pass.

    Assertions:
    (a) **Finite everywhere:** every parameter tensor in the chain (embedding weight, all Q/K/V/output
        Linear weights+biases, LayerNorm weights+biases, FFN weights+biases) receives a gradient
        whose every element is finite — name each tensor in the assertion message.
    (b) **Non-zero aggregate per component:** group the parameters into the ENC-04 components
        (embeddings; per-layer attention; per-layer FFN; per-layer norm) and assert the aggregate
        gradient L2 norm of each component is > 1e-9. Do NOT assert a non-zero gradient on every
        individual tensor: `attention.self.key.bias` (here, `k_proj.bias`) has an exactly-zero
        gradient in exact arithmetic — the key bias adds the same constant to every key, and softmax
        is invariant under a constant shift of all logits. A per-tensor gate would fail on correct
        code. Assert instead that the k_proj bias gradient is NEAR ZERO (|g| <= 1e-6 elementwise),
        making the exemption two-sided evidence rather than a hole, and document the proof in the
        assertion message.
    (c) **Per-row contributions differ (well-defined form):** run TWO ADDITIONAL backward passes,
        each from a scalar loss over a SINGLE batch row, and assert the resulting embedding-weight
        gradients differ between the two runs. A single combined backward does not expose per-row
        contributions separately, so comparing "rows" within one backward is not measurable — this
        replaces that earlier, underspecified assertion.
    (d) **Padding invariance:** encoding sentence A alone (batch 1) equals its row in the padded
        batch within 1e-5 (forward, no dropout, eval).
    (e) **Mask repair regression:** assert the masked score path is graph-connected end to end —
        concretely, that gradients reach the Q and K projection weights of BOTH layers. If 01-09's
        repair were reverted, the broadcast fallback would sever this and (e) would fail loudly.

    Record in the test's doc comment AND the SUMMARY: confirmation of the A5 finding (SDPA's
    internal attention-probs dropout is NOT seedable — `nn::functional::dropout` takes no seed,
    functional.rs:333), citing the signature, so plan 01-06 proceeds directly to the seeded-hook
    extension of MultiHeadAttention construction without re-litigating it.

    If any assertion fails, fixing the responsible op or grad_fn is IN SCOPE for this task —
    document the fix in the SUMMARY. Do not weaken an assertion to pass.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --test batched_graph_spike</automated>
  </verify>
  <acceptance_criteria>
    - batched_graph_spike.rs asserts finite gradients per NAMED parameter tensor (not a single aggregate any() over all) and non-zero AGGREGATE per ENC-04 component
    - The k_proj bias is asserted NEAR ZERO with the softmax-shift-invariance proof in the message — not asserted non-zero, and not skipped
    - Separate per-row backward passes are performed and their gradients compared (assertion (c))
    - Padding-invariance assertion present with tolerance 1e-5 or tighter
    - The chain uses `gelu_exact`, not `gelu` (source assertion)
    - H > 1 and unequal per-row masks are used, so a wrong-axis broadcast fails the test
    - Test passes: `cargo test -p aprender-core --test batched_graph_spike` exits 0
    - Doc comment cites the A5 confirmation with the `nn::functional::dropout` signature
  </acceptance_criteria>
  <done>Batch>1 gradient flow proven end-to-end against the repaired mask path; the spike is now a permanent regression guard for 01-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public op entry points | caller-supplied shapes/targets/epsilons untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | DoS | similarity/normalize ops | mitigate | typed OpError on shape/length/rank/epsilon/non-finite input; eps guards prevent divide-by-zero NaN propagation, and the piecewise backward keeps the clamped branch finite |
| T-1-05 | Repudiation | silent detachment at batch>1 | mitigate | spike asserts finite-per-tensor + non-zero-aggregate-per-component grads and Q/K reachability — a severed broadcast path fails loudly, permanently |
| T-1-16 | Repudiation | unsatisfiable gate wording | mitigate | per-tensor non-zero replaced by aggregate-per-component plus a two-sided near-zero assertion on the analytically-zero key bias |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib l2_normalize_rows`, then `--lib cosine_similarity_rows`, then `--lib mse_loss` — separate invocations (cargo accepts only ONE positional test filter; verified 2026-08-08)
- `cargo test -p aprender-core --test batched_graph_spike` green
- `cargo clippy -p aprender-core -- -D warnings` clean
- `cargo build -p aprender-core --no-default-features` compiles (ops remain ungated)
</verification>

<success_criteria>
- All D-03 primitives complete with FD tests covering both clamp branches (D-04) and #[contract] annotations (D-27)
- Batch>1 gradient flow through the repaired MHA broadcast-mask path proven (Open Question 1 closed) and locked as a regression test
- A5 confirmed from the `nn::functional::dropout` signature and cited, unblocking plan 01-06's dropout design without further investigation
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
