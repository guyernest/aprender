---
phase: 01-differentiable-minilm-conformance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-core/src/nn/module.rs
  - crates/aprender-core/src/nn/linear.rs
  - crates/aprender-core/src/nn/normalization/mod.rs
  - crates/aprender-core/src/nn/dropout/mod.rs
  - crates/aprender-core/src/nn/container.rs
  - crates/aprender-core/src/nn/transformer/mod.rs
  - crates/aprender-core/src/nn/tests_named_module.rs
autonomous: true
requirements: [ENC-04, ENC-05]

must_haves:
  truths:
    - "A developer can enumerate (name, tensor) pairs from ANY Module implementor — including ones that never override the method — with names identical in count AND order to the positional parameters() list"
    - "set_training(bool) recursively flips every Dropout in a composite module without adding, removing, or mutating any registered parameter"
    - "Flipping train -> eval -> train leaves every registered parameter byte-identical (f32::to_bits comparison)"
    - "Every module on the BERT encoder path — Linear, LayerNorm, Dropout, Sequential, MultiHeadAttention — returns SEMANTIC names, not positional fallbacks"
  artifacts:
    - path: "crates/aprender-core/src/nn/module.rs"
      provides: "Module trait extension: named traversal (positional-fallback default) + recursive mode propagation (D-17)"
      contains: "fn named_parameters"
    - path: "crates/aprender-core/src/nn/tests_named_module.rs"
      provides: "Named/positional agreement, recursion, duplicate-name, and mode-flip byte-identity tests + shared snapshot helper"
  key_links:
    - from: "crates/aprender-core/src/nn/linear.rs"
      to: "crates/aprender-core/src/nn/module.rs"
      via: "leaf override returning [(\"weight\", ..), (\"bias\", ..)] in positional order"
      pattern: "named_parameters"
    - from: "crates/aprender-core/src/nn/transformer/mod.rs"
      to: "crates/aprender-core/src/nn/module.rs"
      via: "MultiHeadAttention semantic override prefixing q_proj/k_proj/v_proj/out_proj child names"
      pattern: "named_parameters"
    - from: "crates/aprender-core/src/nn/container.rs"
      to: "crates/aprender-core/src/nn/module.rs"
      via: "composite prefixing of child names + set_training recursion"
      pattern: "set_training"
---

<objective>
Extend the existing `Module` trait with named recursive parameter traversal and train/eval
propagation (D-17), and implement it across the leaf/composite implementors on the BERT encoder
path. This is the single parameter abstraction the encoder (plan 01-06), freeze groups (01-07),
and gradient gates (01-08) build on. No new NamedModule/ParameterStore type.

Purpose: ENC-04 needs stable named parameter groups; ENC-05 needs recursive mode switching that
provably does not change registered parameters.
Output: extended trait + implementations + the mode-flip byte-identity test harness.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **The default `named_parameters()` is no longer empty.** An empty default silently breaks the
  named==positional invariant for every parameter-bearing implementor in the repository that does
  not override it — and freeze grouping and optimizer partitioning in 01-07/01-08 TRUST that
  invariant. The default now enumerates `parameters()` with stable numeric names, so the invariant
  holds universally by construction; semantic names are an override, not a prerequisite.
- **`MultiHeadAttention` gains a semantic override** (`nn/transformer/mod.rs:93`, verified — NOT
  `attention_gqa.rs`, which holds `LinearAttention`/`GroupedQueryAttention`). It is on the BERT
  encoder path and already implements `Module` with a non-empty `parameters()` (mod.rs:250).
- The `snapshot_named` byte-identity helper moves to a shared `#[cfg(test)] pub(crate)` location so
  plan 01-06's encoder tests reuse it instead of duplicating it.
- Duplicate-name and ordered-sequence tests added (count+value agreement alone does not prove order
  or uniqueness).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md

<interfaces>
```rust
// Extension to nn/module.rs — default methods with POSITIONAL FALLBACK, leaf/composite overrides (D-17)
pub trait Module: Send + Sync {
    // ... existing: forward, parameters, parameters_mut, train, eval, training ... UNCHANGED

    /// Named parameters. Leaf names are local ("weight", "bias");
    /// composites prefix with a dotted path.
    ///
    /// INVARIANT (holds for every implementor, including non-overriding ones):
    /// `named_parameters()` has the same length as `parameters()`, and element `i` of each
    /// refers to the same tensor. The DEFAULT satisfies this by emitting stable numeric
    /// names ("0", "1", ...) over `parameters()`. Overriding to supply semantic names is
    /// therefore always safe and never changes arity or order.
    fn named_parameters(&self) -> Vec<(String, &Tensor)> {
        self.parameters()
            .into_iter()
            .enumerate()
            .map(|(i, t)| (i.to_string(), t))
            .collect()
    }
    fn named_parameters_mut(&mut self) -> Vec<(String, &mut Tensor)> {
        self.parameters_mut()
            .into_iter()
            .enumerate()
            .map(|(i, t)| (i.to_string(), t))
            .collect()
    }

    /// Recursive mode propagation (existing train()/eval() are flat, leaf-local).
    fn set_training(&mut self, training: bool) {
        if training { self.train() } else { self.eval() }
    }
}
```
HF-dotted full names (e.g. `encoder.layer.0.attention.self.query.weight`, D-18) are composed by
the encoder composite in plan 01-06 — this plan guarantees leaf names, prefix mechanics, and the
universal arity/order invariant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Module trait with positional-fallback named traversal and set_training defaults</name>
  <files>crates/aprender-core/src/nn/module.rs</files>
  <read_first>
    - crates/aprender-core/src/nn/module.rs (existing trait surface at :31-96 — extend, never replace; keep all existing default bodies)
    - .planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md (section "nn/module.rs — named traversal")
  </read_first>
  <action>
    Add the three default methods from the interfaces block to the `Module` trait in module.rs.
    The named defaults MUST delegate to `parameters()` / `parameters_mut()` with enumerated numeric
    names — NOT `Vec::new()`. An empty default would let a parameter-bearing implementor report zero
    names while reporting N positional parameters, silently violating the invariant that 01-07's
    freeze partition and 01-08's optimizer grouping depend on.

    Do NOT change any existing method signature, do NOT introduce a separate trait or
    ParameterStore (D-17). Document on the trait: (1) named length and order ALWAYS equal positional
    length and order — for the default by construction, for overrides by contract; (2) names never
    include RNG or mode state — only learnable tensors; (3) composites prefix children with a dot;
    (4) names must be unique within one implementor's output.

    Add unit tests in module.rs covering BOTH shapes of default behavior on local test structs:
    a unit struct with no parameters (empty named list), and a struct that implements ONLY
    `parameters()`/`parameters_mut()` and never overrides the named methods — assert its
    `named_parameters()` length equals its `parameters()` length and the names are "0".."N-1".
    That second test is the regression guard for the defect this revision fixes.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib nn::module</automated>
  </verify>
  <acceptance_criteria>
    - module.rs contains `fn named_parameters(&self) -> Vec<(String, &Tensor)>`, `fn named_parameters_mut`, and `fn set_training(&mut self, training: bool)` as trait default methods
    - Neither named default body is `Vec::new()`; both delegate to the positional accessors (source assertion)
    - A test proves a non-overriding parameter-bearing implementor gets N names for N parameters
    - No new pub trait besides Module is introduced in nn/ (source assertion: module.rs defines exactly the existing Module trait)
    - `cargo check --workspace` exits 0 (defaults mean zero breakage across the workspace crates)
  </acceptance_criteria>
  <done>Trait extended with invariant-preserving defaults; workspace compiles untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Semantic overrides on Linear, LayerNorm, Dropout, Sequential, MultiHeadAttention; shared mode-flip byte-identity harness</name>
  <files>crates/aprender-core/src/nn/linear.rs, crates/aprender-core/src/nn/normalization/mod.rs, crates/aprender-core/src/nn/dropout/mod.rs, crates/aprender-core/src/nn/container.rs, crates/aprender-core/src/nn/transformer/mod.rs, crates/aprender-core/src/nn/tests_named_module.rs</files>
  <read_first>
    - crates/aprender-core/src/nn/linear.rs (parameters()/parameters_mut ordering at :285-303 — named MUST mirror this order; test wiring style at :315-321)
    - crates/aprender-core/src/nn/normalization/mod.rs (LayerNorm parameter surface: weight/bias)
    - crates/aprender-core/src/nn/dropout/mod.rs (training flag at :136-145; with_seed at :77-88 — RNG state must NOT appear in named_parameters)
    - crates/aprender-core/src/nn/container.rs (Sequential child iteration — prefix mechanics live here)
    - crates/aprender-core/src/nn/transformer/mod.rs (MultiHeadAttention struct at :93; its Module impl parameters()/parameters_mut() at :250-264 — the exact q/k/v/out_proj order the named override must mirror; train/eval at :266)
  </read_first>
  <behavior>
    - Linear::named_parameters() == [("weight", ..)] or [("weight", ..), ("bias", ..)] matching the Some/None bias branch of parameters() exactly
    - LayerNorm::named_parameters() == [("weight", ..), ("bias", ..)] in positional order
    - Dropout::named_parameters() == [] (RNG/seed/mode state excluded)
    - MultiHeadAttention::named_parameters() == ["q_proj.weight", "q_proj.bias", "k_proj.weight", "k_proj.bias", "v_proj.weight", "v_proj.bias", "out_proj.weight", "out_proj.bias"] — same order and arity as its existing parameters() at mod.rs:250
    - Sequential of [Linear, Dropout, Linear] yields ["0.weight", "0.bias", "2.weight", "2.bias"]-style prefixed names, and set_training(false) flips every Dropout child's training() to false
    - For EVERY touched implementor: names are unique (no duplicate key) and `named_parameters()[i].1` is the same tensor as `parameters()[i]`
    - For each implementor: names_and_bits(m) snapshot -> m.set_training(true); m.set_training(false); m.set_training(true) -> snapshot compare: identical name SEQUENCE (not just set), identical f32::to_bits for every element (ENC-05 proof harness, Pitfall 7)
  </behavior>
  <action>
    Override the named methods on each implementor, mirroring positional order (leaf names: weight,
    bias). In container.rs, compose child names with index-dot prefix and override set_training to
    recurse into children (existing flat train()/eval() stay as-is for compatibility).

    In `nn/transformer/mod.rs`, add `named_parameters` / `named_parameters_mut` / `set_training`
    overrides to `impl Module for MultiHeadAttention`, prefixing each child Linear's names with
    `q_proj.` / `k_proj.` / `v_proj.` / `out_proj.` in exactly the order its existing `parameters()`
    uses. This is required because MHA is on the BERT encoder path that 01-06 composes and 01-07
    freezes; a positional-fallback name there would make freeze groups address the wrong tensors.
    Touch ONLY the Module impl block — do not alter MHA's forward math, construction, or dropout
    behavior (the seeded-dropout extension is plan 01-06's, in a later wave).

    Create tests_named_module.rs (wired per existing test-file convention: `#[path = "..."] mod ...`
    inside the nn test module, matching the file's neighbors) containing: per-implementor
    named/positional agreement tests, name-uniqueness tests, the ordered-sequence test, the
    Sequential recursion test, and a reusable byte-identity helper

      `#[cfg(test)] pub(crate) fn snapshot_named(m: &dyn Module) -> Vec<(String, Vec<u32>)>`

    (to_bits) used by the mode-flip test. Place the helper where plan 01-06's encoder tests can call
    it directly — `#[cfg(test)] pub(crate)` in a module reachable as `crate::nn::...` — NOT as a
    private fn inside a `mod tests` block, which 01-06 could not reach. Verify reachability by
    adding a one-line smoke test in a DIFFERENT module that calls it. Test fn names start
    `named_module_`. If a CB-510 include!() file is added, run `bash scripts/check_include_files.sh`
    and confirm `git check-ignore -v <file>` exits 1.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib named_module</automated>
  </verify>
  <acceptance_criteria>
    - linear.rs, normalization/mod.rs, container.rs, and transformer/mod.rs each contain `fn named_parameters` overrides; dropout/mod.rs either omits the override (positional default over an empty parameters()) or returns empty explicitly
    - MultiHeadAttention's named list is asserted equal to the exact 8-name q/k/v/out sequence, and its length equals `parameters().len()`
    - tests assert names().len() == parameters().len() AND pairwise value agreement AND name uniqueness for every implementor touched
    - Ordered-sequence assertion compares Vec<String> equality, not HashSet equality
    - Mode-flip test asserts identical (name, to_bits) snapshots across train->eval->train for a composite containing Dropout
    - `snapshot_named` is callable from a module other than its defining one (proved by a cross-module smoke test) — 01-06 depends on this
    - `cargo test -p aprender-core --lib named_module` exits 0
    - `cargo clippy -p aprender-core -- -D warnings` clean
    - `git diff --stat crates/aprender-core/src/nn/transformer/mod.rs` shows changes confined to the `impl Module for MultiHeadAttention` block
  </acceptance_criteria>
  <done>All BERT-path implementors traverse by semantic name; recursion, uniqueness, order, and byte-identity proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | trait plumbing over in-tree types; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Repudiation | named_parameters ordering | mitigate | positional-fallback default makes named==positional arity/order universal; tests pin the order so later freeze/optimizer grouping cannot silently address the wrong tensor |
| T-1-04 | Tampering | mode switch mutating params | mitigate | byte-identity (to_bits) mode-flip test — the ENC-05 gate |
| T-1-17 | Spoofing | duplicate or fallback names in a freeze-addressed module | mitigate | name-uniqueness assertions + MHA semantic override, so `FreezeGroup::LayerAttention(n)` prefix matching in 01-07 cannot silently match zero tensors |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib named_module`, then `cargo test -p aprender-core --lib nn::module` — separate invocations (cargo accepts only one positional test filter; verified 2026-08-08)
- `cargo check --workspace` green (no downstream breakage from trait defaults)
- `cargo clippy -p aprender-core -- -D warnings` clean
</verification>

<success_criteria>
- One parameter abstraction: extended Module trait, no parallel store (D-17)
- Named length and order equal positional length and order for EVERY implementor, including non-overriding ones
- Every BERT-path module (Linear, LayerNorm, Dropout, Sequential, MultiHeadAttention) returns semantic names
- set_training recursion proven; mode flips leave parameters byte-identical (ENC-05 foundation)
- `snapshot_named` available for reuse by plan 01-06
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
