---
phase: 01-differentiable-minilm-conformance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/aprender-core/src/nn/module.rs
  - crates/aprender-core/src/nn/linear.rs
  - crates/aprender-core/src/nn/normalization/mod.rs
  - crates/aprender-core/src/nn/dropout/mod.rs
  - crates/aprender-core/src/nn/container.rs
  - crates/aprender-core/src/nn/tests_named_module.rs
autonomous: true
requirements: [ENC-04, ENC-05]

must_haves:
  truths:
    - "A developer can enumerate (name, tensor) pairs from any Module implementor, with names identical in order to the positional parameters() list"
    - "set_training(bool) recursively flips every Dropout in a composite module without adding, removing, or mutating any registered parameter"
    - "Flipping train -> eval -> train leaves every registered parameter byte-identical (f32::to_bits comparison)"
  artifacts:
    - path: "crates/aprender-core/src/nn/module.rs"
      provides: "Module trait extension: named traversal + recursive mode propagation (D-17)"
      contains: "fn named_parameters"
    - path: "crates/aprender-core/src/nn/tests_named_module.rs"
      provides: "Named/positional agreement, recursion, and mode-flip byte-identity tests"
  key_links:
    - from: "crates/aprender-core/src/nn/linear.rs"
      to: "crates/aprender-core/src/nn/module.rs"
      via: "leaf override returning [(\"weight\", ..), (\"bias\", ..)] in positional order"
      pattern: "named_parameters"
    - from: "crates/aprender-core/src/nn/container.rs"
      to: "crates/aprender-core/src/nn/module.rs"
      via: "composite prefixing of child names + set_training recursion"
      pattern: "set_training"
---

<objective>
Extend the existing `Module` trait with named recursive parameter traversal and train/eval
propagation (D-17), and implement it across the existing leaf/composite implementors. This is the
single parameter abstraction the encoder (plan 01-06), freeze groups (01-07), and gradient gates
(01-08) build on. No new NamedModule/ParameterStore type.

Purpose: ENC-04 needs stable named parameter groups; ENC-05 needs recursive mode switching that
provably does not change registered parameters.
Output: extended trait + implementations + the mode-flip byte-identity test harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md

<interfaces>
```rust
// Extension to nn/module.rs — default methods, empty/no-op defaults, leaf overrides (D-17)
pub trait Module: Send + Sync {
    // ... existing: forward, parameters, parameters_mut, train, eval, training ... UNCHANGED

    /// Named parameters. Leaf names are local ("weight", "bias");
    /// composites prefix with a dotted path. Order MUST match parameters().
    fn named_parameters(&self) -> Vec<(String, &Tensor)> { Vec::new() }
    fn named_parameters_mut(&mut self) -> Vec<(String, &mut Tensor)> { Vec::new() }

    /// Recursive mode propagation (existing train()/eval() are flat, leaf-local).
    fn set_training(&mut self, training: bool) {
        if training { self.train() } else { self.eval() }
    }
}
```
HF-dotted full names (e.g. `encoder.layer.0.attention.self.query.weight`, D-18) are composed by
the encoder composite in plan 01-06 — this plan only guarantees leaf names + prefix mechanics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Module trait with named traversal and set_training defaults</name>
  <files>crates/aprender-core/src/nn/module.rs</files>
  <read_first>
    - crates/aprender-core/src/nn/module.rs (existing trait surface at :31-96 — extend, never replace; keep all existing default bodies)
    - .planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md (section "nn/module.rs — named traversal")
  </read_first>
  <action>
    Add the three default methods from the interfaces block to the `Module` trait in module.rs:
    `named_parameters()` (default empty), `named_parameters_mut()` (default empty), and
    `set_training(bool)` (default delegates to existing train()/eval()). Do NOT change any existing
    method signature, do NOT introduce a separate trait or ParameterStore (D-17). Document on the
    trait: (1) named order must equal positional order, (2) names never include RNG or mode state —
    only learnable tensors, (3) composites prefix children with a dot. Add doc-tests or unit tests
    in module.rs for the default behavior on a unit struct implementor.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib module && cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - module.rs contains `fn named_parameters(&self) -> Vec<(String, &Tensor)>` and `fn named_parameters_mut` and `fn set_training(&mut self, training: bool)` as trait default methods
    - No new pub trait besides Module is introduced in nn/ (source assertion: module.rs defines exactly the existing Module trait)
    - `cargo check --workspace` exits 0 (defaults mean zero breakage across 78 crates)
  </acceptance_criteria>
  <done>Trait extended with backward-compatible defaults; workspace compiles untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement named traversal + recursion on Linear, LayerNorm, Dropout, Sequential; mode-flip byte-identity harness</name>
  <files>crates/aprender-core/src/nn/linear.rs, crates/aprender-core/src/nn/normalization/mod.rs, crates/aprender-core/src/nn/dropout/mod.rs, crates/aprender-core/src/nn/container.rs, crates/aprender-core/src/nn/tests_named_module.rs</files>
  <read_first>
    - crates/aprender-core/src/nn/linear.rs (parameters()/parameters_mut ordering at :285-303 — named MUST mirror this order; test wiring style at :315-321)
    - crates/aprender-core/src/nn/normalization/mod.rs (LayerNorm parameter surface: weight/bias)
    - crates/aprender-core/src/nn/dropout/mod.rs (training flag at :136-145; with_seed at :77-88 — RNG state must NOT appear in named_parameters)
    - crates/aprender-core/src/nn/container.rs (Sequential child iteration — prefix mechanics live here)
  </read_first>
  <behavior>
    - Linear::named_parameters() == [("weight", ..)] or [("weight", ..), ("bias", ..)] matching the Some/None bias branch of parameters() exactly
    - LayerNorm::named_parameters() == [("weight", ..), ("bias", ..)] in positional order
    - Dropout::named_parameters() == [] (RNG/seed/mode state excluded)
    - Sequential of [Linear, Dropout, Linear] yields ["0.weight", "0.bias", "2.weight", "2.bias"]-style prefixed names, and set_training(false) flips every Dropout child's training() to false
    - For each implementor: names_and_bits(m) snapshot -> m.set_training(true); m.set_training(false); m.set_training(true) -> snapshot compare: identical name set, identical f32::to_bits for every element (ENC-05 proof harness, Pitfall 7)
  </behavior>
  <action>
    Override the named methods on each implementor, mirroring positional order (leaf names: weight,
    bias). In container.rs, compose child names with index-dot prefix and override set_training to
    recurse into children (existing flat train()/eval() stay as-is for compatibility). Create
    tests_named_module.rs (wired per existing test-file convention: #[cfg(test)] #[path] mod or
    include!, matching the file's neighbors) containing: per-implementor named/positional agreement
    tests, the Sequential recursion test, and a reusable
    `fn snapshot_named(m: &dyn Module) -> Vec<(String, Vec<u32>)>` byte-identity helper (to_bits)
    used by the mode-flip test — plan 01-06 will reuse this helper for the full encoder. Test fn
    names start `named_module_`. If a CB-510 include!() file is added, run
    `bash scripts/check_include_files.sh` and confirm `git check-ignore -v <file>` exits 1.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib named_module</automated>
  </verify>
  <acceptance_criteria>
    - linear.rs, normalization/mod.rs, container.rs each contain `fn named_parameters` overrides; dropout/mod.rs either omits the override (empty default) or returns empty explicitly
    - tests assert names().len() == parameters().len() AND pairwise pointer/value agreement for every implementor touched
    - Mode-flip test asserts identical (name, to_bits) snapshots across train->eval->train for a composite containing Dropout
    - `cargo test -p aprender-core --lib named_module` exits 0
    - `cargo clippy -p aprender-core -- -D warnings` clean
  </acceptance_criteria>
  <done>All existing implementors traverse by name; recursion and byte-identity proven by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | trait plumbing over in-tree types; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Repudiation | named_parameters ordering | mitigate | tests pin named==positional order so later freeze/optimizer grouping cannot silently address the wrong tensor |
| T-1-04 | Tampering | mode switch mutating params | mitigate | byte-identity (to_bits) mode-flip test — the ENC-05 gate |
| T-1-SC | Tampering | third-party installs | accept | no packages installed by this plan |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib named_module module` green
- `cargo check --workspace` green (no downstream breakage from trait defaults)
- `cargo clippy -p aprender-core -- -D warnings` clean
</verification>

<success_criteria>
- One parameter abstraction: extended Module trait, no parallel store (D-17)
- Named order == positional order on every touched implementor
- set_training recursion proven; mode flips leave parameters byte-identical (ENC-05 foundation)
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-02-SUMMARY.md` when done.
</output>
