---
phase: 01-differentiable-minilm-conformance
plan: 05
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - Cargo.toml
  - crates/aprender-core/Cargo.toml
  - crates/aprender-core/src/lib.rs
  - crates/aprender-core/src/models/bert/load.rs
  - crates/aprender-core/src/setfit/mod.rs
  - crates/aprender-core/src/setfit/error.rs
  - crates/aprender-core/src/setfit/tokenizer.rs
  - crates/aprender-core/src/setfit/import.rs
autonomous: true
requirements: [ENC-01, ENC-02]

must_haves:
  truths:
    - "A developer can import the pinned all-MiniLM-L6-v2 revision and any mutated architecture, tokenizer, activation, dropout, pooling, normalization, or configuration field fails with a typed error naming the field"
    - "A developer can batch-tokenize texts once and receive ordered CANONICAL token ids, type ids, attention masks, truncation facts, tokenizer identity, and input provenance that exactly match the frozen tokenizer fixtures"
    - "MiniLmTokenizer and MiniLmImport cannot be constructed from outside the crate: their constructors are pub(crate), so the only way an out-of-crate caller obtains a tokenizer/encoder pair is through SetFitMiniLm, which builds them together (D-08 structural — a mismatched pair is not constructible)"
    - "A SentenceBatch is READ-ONLY from outside the crate: its fields are pub(crate) with public read accessors, so out-of-crate code can neither forge a batch carrying a borrowed tokenizer_sha256 nor mutate the ids of a batch obtained from the tokenizer"
    - "A SentenceBatch additionally carries the sha256 of the tokenizer that produced it, so an in-crate mismatch is still rejected at the forward boundary (defense in depth behind the structural seal)"
    - "A build without the setfit feature does not compile or link the tokenizers crate"
  artifacts:
    - path: "crates/aprender-core/src/setfit/tokenizer.rs"
      provides: "MiniLmTokenizer (pub(crate) constructor) + typed SentenceBatch with tokenizer identity and a read-only out-of-crate surface (D-07/D-08)"
      contains: "pub(crate) fn from_bytes"
    - path: "crates/aprender-core/src/setfit/import.rs"
      provides: "Pinned-revision import contract (pub(crate) constructors) reusing bert/config.rs + bert/load.rs helpers (sanctioned one-line visibility change only, A-01)"
      contains: "pub(crate) fn open"
    - path: "crates/aprender-core/src/setfit/error.rs"
      provides: "SetFitError typed enum spanning import/tokenize/batch failures"
      contains: "pub enum SetFitError"
    - path: "crates/aprender-core/Cargo.toml"
      provides: "setfit + conformance-fixtures features; optional tokenizers AND sha2 deps (D-05)"
      contains: "dep:sha2"
  key_links:
    - from: "crates/aprender-core/src/setfit/import.rs"
      to: "crates/aprender-core/src/models/bert/load.rs"
      via: "direct call to read_tensor (made pub(crate) by the sanctioned A-01 one-line change) + detect_bert_prefix + BertLoadError"
      pattern: "read_tensor"
    - from: "crates/aprender-core/src/setfit/tokenizer.rs"
      to: "crates/aprender-core/tests/fixtures/setfit/tokenizer_cases.json"
      via: "parity tests over frozen fixtures"
    - from: "Cargo.toml"
      to: "crates/aprender-core/Cargo.toml"
      via: "workspace tokenizers 0.23.1 pin, default-features false, fancy-regex only"
      pattern: "fancy-regex"
---

<objective>
Introduce the `setfit` feature and module: the workspace-pinned `tokenizers` 0.23.1 dependency,
the typed `SentenceBatch` tokenizer boundary (D-07), and the pinned-revision import contract with
typed rejection of every unsupported variant (ENC-01), including the test-only slice constructor
that keeps the fixture gates and the pin gates from contradicting each other (RESEARCH Pitfall 3).

Purpose: ENC-01 and ENC-02 land here with their parity/rejection proofs; D-05/D-06 feature
discipline starts here while it is cheap.
Output: feature-gated setfit module with tokenizer + import, fixture-parity green, feature matrix
clean.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **A-01 (sanctioned D-01 amendment).** `read_tensor` is private (`models/bert/load.rs:114`,
  verified: `fn read_tensor(` with no `pub`). The prior plan required calling it while D-01 forbade
  touching `models/bert/` — a direct contradiction that made the plan unimplementable. The user
  sanctioned a **one-line visibility change** to `pub(crate) fn read_tensor`. No behavior change, so
  D-01's intent (no in-place *refactor*) holds. Do NOT build a facade and do NOT duplicate the read
  logic. `verify-phase` must treat this as a recorded amendment, not a scope violation.
- **`deny_unknown_fields` removed.** The real HF `config.json` carries many fields this struct does
  not model; a strict deny would reject the pinned model itself. Validate every BEHAVIOR-AFFECTING
  field explicitly; tolerate unmodelled metadata.
- **Mutation matrix widened** to include activation, both dropout probabilities, position embedding
  type, type-vocab size, pad id, and the sentence-transformer max sequence length.
- **`conformance-fixtures = ["setfit"]`**, not `[]` — the slice constructor it gates lives inside a
  `setfit`-gated module, so enabling it alone was meaningless.
- **Tokenizer identity is carried in `SentenceBatch`** — the batch stamps its tokenizer's sha256 and
  the encoder rejects a foreign batch (enforced in 01-06). This is retained as defense in depth,
  NOT as a substitute for D-08 (see the sealing decision below).
- **Canonical ids stay canonical.** `SentenceBatch.input_ids` are always the tokenizer's real
  vocabulary ids; the `orig_to_slice` remap is carried by the slice import and applied inside the
  encoder, never by rewriting the batch.
- **"Tokenize once" is made concrete or made honest** — see Task 2.

Checker-driven revision (2026-08-08, post-`--reviews` re-verification):
- **[B2 — USER DECISION 2026-08-08: SEAL] D-08 is delivered structurally, not by narrowing.** The
  prior pass kept `MiniLmTokenizer::from_bytes` and `MiniLmImport::open` public and downgraded D-08
  to a runtime check. D-08 says the bound type is the *sole public entry point* "so a mismatched
  tokenizer/encoder pair is **not constructible**" (user-confirmed, `01-DISCUSSION-LOG.md:51`); that
  narrowing was never sanctioned. The user has now decided: **seal them**. `from_bytes`, `open`, and
  `open_slice_fixture` become `pub(crate)` (and `BertSentenceEncoder::from_import` in 01-06). The
  `tokenizer_sha256` boundary check STAYS as defense in depth.
  Feasibility was verified by the checker, not assumed: every consumer is in-crate — 01-05/01-06/01-07
  tests are in-module (`#[cfg(all(test, feature = "setfit"))]`), and 01-08's out-of-crate integration
  harness already drives through `SetFitMiniLm::from_slice_fixture` / `from_pretrained_dir`.
  `pub(crate)` therefore suffices everywhere.
- **[B4 — feature closure] `setfit` must enable `sha2`.** `sha2` is declared optional at
  `crates/aprender-core/Cargo.toml:120` and is enabled ONLY by `format-encryption` (:191) and
  `hf-hub-integration` (:195). This plan needs sha2 in PRODUCTION code (Task 2 hashes tokenizer bytes
  and input text; Task 3 pins the tokenizer hash), so the previously specified
  `setfit = ["dep:tokenizers"]` would not compile under `cargo check --features setfit`.
- **[W5] `SetFitError` gains an `Op(OpError)` variant.** 01-06's `forward_tokens`/`encode` and
  01-07's `pair_cosine_mse` return `Result<_, SetFitError>` while composing ops that return
  `Result<_, OpError>`; without a wrapping variant those signatures cannot be written.

Checker-driven revision, iteration 2 (2026-08-08):
- **[W1 — the seal was bypassable at the DATA layer] `SentenceBatch`'s fields become `pub(crate)`
  with public READ accessors.** Every field was `pub`, including `tokenizer_sha256`. Sealing the
  constructors makes a mismatched *pair* unconstructible, but out-of-crate code could still
  hand-build a `SentenceBatch` literal stamped with a correct hash, or take a legitimate batch from
  `SetFitMiniLm::tokenize()` and mutate `input_ids` while leaving the hash intact — then feed either
  to `encoder().forward_tokens()`. D-08's letter survived that; the threat-register claim "the
  encoder rejects a mismatch at every forward call" did not, because the check compares a hash the
  attacker controls. Private-by-default fields close both holes with no cost to in-crate callers
  (01-06 is in the same crate) and no cost to 01-08 (it needs to READ `batch`/`seq` at most).
  `#[non_exhaustive]` is NOT added — it is redundant once the fields are `pub(crate)`, and it would
  needlessly restrict in-crate construction.
- **[B6 consequence] `tokenizer_cases.json` is now the CORPUS OF RECORD** (01-04): it carries a case
  for every text any other fixture uses, so it has MORE cases than the six required classes. Task 2's
  parity test iterates ALL cases, so this strictly widens coverage; the `case count > 4` assertion
  still holds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md

<interfaces>
```rust
// setfit/tokenizer.rs (feature = "setfit")
pub struct SentenceBatch {
    // ALL fields pub(crate) (D-08 data-layer seal, W1). In-crate code (the encoder, 01-06) reads
    // them directly; out-of-crate code reads through the accessors below and can neither construct
    // a SentenceBatch literal nor mutate one it received.
    pub(crate) input_ids: Vec<u32>,        // row-major [B*S] — CANONICAL HF vocabulary ids, always
    pub(crate) token_type_ids: Vec<u32>,   // row-major [B*S]
    pub(crate) attention_mask: Vec<u8>,    // row-major [B*S], 1 keep / 0 pad
    pub(crate) batch: usize,
    pub(crate) seq: usize,                 // padded length (longest in batch, <= max_len)
    pub(crate) truncation: Vec<TruncationFact>,   // per input: { truncated: bool, original_len: usize }
    pub(crate) provenance: Vec<InputProvenance>,  // per input: { index: usize, text_sha256: String }
    pub(crate) tokenizer_sha256: String,   // identity of the tokenizer that produced this batch (D-08)
}
impl SentenceBatch {
    // Read-only public surface. Borrows only — no setters, no `_mut`, no owned-field escape hatch.
    pub fn input_ids(&self) -> &[u32];
    pub fn token_type_ids(&self) -> &[u32];
    pub fn attention_mask(&self) -> &[u8];
    pub fn batch(&self) -> usize;
    pub fn seq(&self) -> usize;
    pub fn truncation(&self) -> &[TruncationFact];
    pub fn provenance(&self) -> &[InputProvenance];
    pub fn tokenizer_sha256(&self) -> &str;
}

pub struct MiniLmTokenizer { /* wraps tokenizers::Tokenizer */ }
impl MiniLmTokenizer {
    /// SEALED (D-08): pub(crate). Out-of-crate callers reach a tokenizer only via SetFitMiniLm,
    /// which constructs tokenizer and encoder together — so a mismatched pair is NOT constructible.
    pub(crate) fn from_bytes(bytes: &[u8]) -> Result<Self, SetFitError>;  // records sha256 of bytes
    pub fn tokenizer_sha256(&self) -> &str;
    pub fn encode_batch(&self, texts: &[&str]) -> Result<SentenceBatch, SetFitError>; // max_length 256
}

// setfit/import.rs (feature = "setfit")
pub struct MiniLmImport { /* validated config + AprV2Reader handle + revision + tokenizer hash + optional slice remap */ }
impl MiniLmImport {
    /// Full pin: config equality vs BertConfig::minilm_l6(), tokenizer sha, revision record.
    /// SEALED (D-08): pub(crate). The public full-pin entry point is
    /// `SetFitMiniLm::from_pretrained_dir` (01-07), which calls this and pairs the result with the
    /// tokenizer loaded from the SAME directory.
    pub(crate) fn open(dir: &Path) -> Result<Self, SetFitError>;
    /// Slice fixtures only: bypasses ONLY architecture-pin equality; every structural,
    /// shape-consistency, and finiteness check stays (RESEARCH Pitfall 3).
    /// SEALED (D-08): pub(crate). Public fixture entry point is
    /// `SetFitMiniLm::from_slice_fixture` (01-07).
    #[cfg(feature = "conformance-fixtures")]
    pub(crate) fn open_slice_fixture(apr: &Path, config: &SliceConfig, remap: &VocabRemap)
        -> Result<Self, SetFitError>;
    pub fn tokenizer_sha256(&self) -> &str;
    /// None for the full-pin path; Some for a slice import. The encoder applies it to canonical
    /// ids at gather time — the SentenceBatch is never rewritten.
    pub fn vocab_remap(&self) -> Option<&VocabRemap>;
}

/// Deserialized from vocab_remap.json (01-04). Canonical id -> slice row.
pub struct VocabRemap { /* orig_to_slice: HashMap<u32, u32>, slice_to_orig: Vec<u32> */ }
impl VocabRemap {
    /// Typed error (not a zero-fill, not a panic) when a canonical id is outside the slice closure.
    pub fn to_slice_row(&self, canonical: u32) -> Result<u32, SetFitError>;
}
```
Plan 01-06 consumes SentenceBatch, MiniLmImport, and VocabRemap — field/method names above are the
contract. 01-06 is in-crate, so it may read the `pub(crate)` fields directly or use the accessors.

**Visibility rule for this whole module (D-08, user decision 2026-08-08):** every constructor that
produces a tokenizer or an import is `pub(crate)`, and `SentenceBatch`'s fields are `pub(crate)`
with read-only public accessors. Read accessors, `encode_batch`, and the types themselves stay
`pub` — sealing constructors and mutable data access is what makes a mismatched pair
unconstructible and an obtained batch unforgeable; sealing accessors would only make the crate
harder to use. If a later plan finds it needs an out-of-crate constructor, the correct move is to
add a narrow `#[cfg(feature = "conformance-fixtures")]` READ method on `SetFitMiniLm` or
`BertSentenceEncoder` (both of which own already-correctly-paired state), NOT to re-widen these.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature plumbing — workspace tokenizers pin, setfit + conformance-fixtures features, module skeleton, sanctioned read_tensor visibility change</name>
  <files>Cargo.toml, crates/aprender-core/Cargo.toml, crates/aprender-core/src/lib.rs, crates/aprender-core/src/models/bert/load.rs, crates/aprender-core/src/setfit/mod.rs, crates/aprender-core/src/setfit/error.rs</files>
  <read_first>
    - Cargo.toml (root [workspace.dependencies] style)
    - crates/aprender-core/Cargo.toml (feature list ~lines 185-215: audio / model-tests precedents; NOTE `sha2` is already declared optional at :120 and is enabled ONLY by `format-encryption` :191 and `hf-hub-integration` :195 — `setfit` must enable it too, see B4)
    - crates/aprender-core/src/models/bert/load.rs (:114 `fn read_tensor(` — the single line A-01 changes; BertLoadError shape at :94-109 — the error style to follow)
    - .claude/skills/pre-release/SKILL.md (Gates 2/5: no external path deps; package must build standalone — the optional dep must not break cargo package)
  </read_first>
  <action>
    Root Cargo.toml [workspace.dependencies]: add
    `tokenizers = { version = "0.23.1", default-features = false, features = ["fancy-regex"] }`
    (exactly — defaults pull onig C / esaxx C++; Pitfall 5).

    crates/aprender-core/Cargo.toml: `tokenizers = { workspace = true, optional = true }`,
    features **`setfit = ["dep:tokenizers", "dep:sha2"]`** and
    **`conformance-fixtures = ["setfit"]`**.
      - `dep:sha2` is MANDATORY, not optional polish: `sha2` is declared optional at :120 and is
        currently enabled only by `format-encryption` (:191) and `hf-hub-integration` (:195). Task 2
        hashes the tokenizer bytes and each input text in PRODUCTION code, and Task 3 checks the
        tokenizer hash pin, so `cargo check -p aprender-core --features setfit` would fail to compile
        without it. Do not reach for it via another feature — declare the dependency where it is used.
      - `conformance-fixtures = ["setfit"]` (not `[]`) because the slice constructor and conformance
        suite it gates live inside `#[cfg(feature = "setfit")] mod setfit`, so an empty marker
        feature could be enabled alone and gate nothing.

    lib.rs: `#[cfg(feature = "setfit")] pub mod setfit;`.

    **Sanctioned A-01 change** — in `crates/aprender-core/src/models/bert/load.rs`, change
    `fn read_tensor(` at line 114 to `pub(crate) fn read_tensor(`. That is the ENTIRE change to
    `models/bert/`: no signature change, no body change, no behavior change, no other line touched.
    Add a one-line comment above it recording why:
    `// pub(crate) for setfit/import.rs reuse (Phase 1 amendment A-01 to D-01: visibility only, no refactor).`
    Do NOT create a facade module and do NOT copy the read logic into setfit — both were considered
    and rejected; a duplicate would drift from the checked-read semantics this reuses.

    setfit/mod.rs: module docs + re-exports. setfit/error.rs: `SetFitError` enum (plain enum +
    Display + Error, no thiserror) with variants at minimum: ImportConfigMismatch { field, expected,
    got }, ImportTensor(BertLoadError-wrapping variant), TokenizerLoad, TokenizerHashMismatch
    { expected, got }, UnsupportedPooling, UnsupportedArchitecture, UnsupportedActivation { got },
    BatchInvalid { reason }, OversizeInput { len, max }, VocabOutOfSlice { canonical_id },
    NonFiniteTensor { tensor, position }, and **`Op(OpError)`**.

    `Op(OpError)` is required, not optional (W5): 01-06's `forward_tokens`/`encode` and 01-07's
    `pair_cosine_mse` are specified to return `Result<_, SetFitError>` while composing the 01-01/01-03
    ops, which return `Result<_, OpError>`. Without a wrapping variant those signatures cannot be
    written and a later plan would have to widen this file (01-07 does not even list error.rs in its
    files_modified, so it could not). Add `impl From<crate::autograd::ops::OpError> for SetFitError`
    so op calls compose with `?`, and have `Display` forward to the inner `OpError` so the op's own
    diagnostic (row index, position, vocab id) is not flattened away. `OpError` is ungated (D-03),
    so this variant is NOT behind `#[cfg(feature = "setfit")]` beyond the module's own gate.

    **Re-export discipline (W2 pre-condition).** `setfit/mod.rs` re-exports TYPES only
    (`SentenceBatch`, `MiniLmTokenizer`, `MiniLmImport`, `SetFitError`, `VocabRemap`, ...). Never
    re-export a sealed constructor as a free function (e.g. `pub use tokenizer::from_bytes;`) — that
    would reopen the D-08 seal through a path the source-assertion greps in 01-07 scan for.

    Feature matrix check: `cargo check -p aprender-core --no-default-features`, `cargo check -p
    aprender-core --features setfit`, `cargo check -p aprender-core --all-features` all pass, and a
    no-setfit build has NO tokenizers node:
    `! cargo tree -p aprender-core --no-default-features -e normal | grep -q tokenizers` (D-06).
  </action>
  <verify>
    <automated>cargo check -p aprender-core --no-default-features && cargo check -p aprender-core --features setfit && sh -c '! cargo tree -p aprender-core --no-default-features -e normal | grep -q tokenizers'</automated>
  </verify>
  <acceptance_criteria>
    - Root Cargo.toml contains the tokenizers 0.23.1 pin with default-features = false and features = ["fancy-regex"]
    - aprender-core Cargo.toml contains `setfit = ["dep:tokenizers", "dep:sha2"]` and `conformance-fixtures = ["setfit"]` (the sha2 entry is what makes `cargo check --features setfit` compile once Tasks 2-3 land)
    - setfit/error.rs contains an `Op(` variant and `impl From<OpError> for SetFitError` (source assertion) — 01-06 and 01-07 depend on it and cannot add it themselves
    - setfit/mod.rs re-exports types only; no `pub use` of a sealed constructor (source assertion)
    - `git diff --numstat crates/aprender-core/src/models/bert/load.rs` shows at most 2 added / 1 removed line (the visibility qualifier plus the amendment comment) and no other hunk in crates/aprender-core/src/models/bert/
    - The changed line reads `pub(crate) fn read_tensor(` and the A-01 amendment comment is present
    - `cargo tree -p aprender-core --no-default-features -e normal` output contains no tokenizers line
    - All three matrix legs type-check
  </acceptance_criteria>
  <done>Feature discipline established; tokenizers cannot leak into minimal consumers; read_tensor reusable via the sanctioned one-line amendment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MiniLmTokenizer + read-only SentenceBatch with tokenizer identity and fixture parity (ENC-02)</name>
  <files>crates/aprender-core/src/setfit/tokenizer.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/tests/fixtures/setfit/tokenizer_cases.json (frozen parity target and schema — from 01-04; ids are CANONICAL. This file is the phase's CORPUS OF RECORD: it holds a case for every text any other fixture uses, so expect MORE than the six required case classes)
    - crates/aprender-bench-tokenizer/src/lib.rs (only in-repo HF tokenizers consumer: dependency form, path-resolution style at :40-61)
    - crates/aprender-core/src/setfit/error.rs (error variants from Task 1)
  </read_first>
  <behavior>
    - For every case in tokenizer_cases.json: encode_batch(texts) yields input_ids, token_type_ids, attention_mask EXACTLY equal (integer equality, no tolerance) to the fixture arrays, and truncated/original_len facts match
    - input_ids are CANONICAL vocabulary ids — the same values the reference tokenizer produced; no slice remapping happens here
    - batch.tokenizer_sha256() == tokenizer.tokenizer_sha256() == the fixture's recorded tokenizer_sha256
    - Padding is to longest-in-batch; batch=1 input yields seq() == its own length (capped at max_len)
    - A >256-token input sets truncated=true with original_len > 256; ids length == 256
    - provenance()[i].text_sha256 equals the sha256 hex of the input text bytes; order preserved
    - Empty text list returns Err(SetFitError::BatchInvalid), not a panic
    - The public surface is read-only: every SentenceBatch accessor returns a borrow or a Copy scalar, and no field is `pub`
  </behavior>
  <action>
    Implement MiniLmTokenizer with a **`pub(crate) fn from_bytes`** constructor (D-08 seal — the
    public way to obtain a tokenizer is `SetFitMiniLm`, 01-07) wrapping
    tokenizers::Tokenizer::from_bytes, with truncation max_length 256 and longest-in-batch padding
    configured via the tokenizers API (not hand-rolled padding). Record sha256 of the tokenizer bytes
    at construction (`sha2`, enabled by the `setfit` feature per Task 1) and stamp it into every
    SentenceBatch it produces — the stamp is defense in depth behind the seal, not a replacement for
    it. `provenance[i].text_sha256` uses the same `sha2` dependency. Build SentenceBatch per the
    interfaces block — row-major flattened, u8 mask.

    **SentenceBatch is read-only from outside the crate (W1).** Declare every field `pub(crate)` and
    add the public read accessors from the interfaces block. Rationale to record in the type's doc
    comment: the encoder's `tokenizer_sha256` equality check is only defense in depth if the value it
    compares is not attacker-supplied. With `pub` fields, out-of-crate code could hand-build a batch
    stamped with a legitimate hash, or mutate `input_ids` on a batch received from
    `SetFitMiniLm::tokenize()` while leaving the hash intact — both defeat the check while the D-08
    constructor seal remains formally intact. Provide NO setter, no `_mut` accessor, and no method
    returning owned interior state that could be mutated and fed back. Do NOT add
    `#[non_exhaustive]`: it is redundant once the fields are `pub(crate)` and it would restrict
    in-crate construction for no benefit.

    **`original_token_counts` without a second tokenization — resolve explicitly.** Configure
    truncation so the overflow is retained in the SAME pass and derive
    `original_len = ids.len() + sum(overflow_encoding.len())` from `Encoding::get_overflowing()`.
    Verify against the fixture's `original_token_counts` for the >256-token case. If the pinned
    tokenizers 0.23.1 API does not retain overflow when padding is enabled, do NOT silently add a
    hidden second pass: perform one explicit untruncated pass, rename the guarantee in the module
    docs from "tokenize once" to "one user-facing call; two internal passes when truncation facts
    are requested", and record the finding in the SUMMARY so ENC-02's claim stays accurate. Whichever
    path is taken, a test must assert the reported original counts match the fixture exactly.

    Tokenizer parity tests live in-module (#[cfg(all(test, feature = "setfit"))]): load
    crates/aprender-core/tests/fixtures/setfit/tokenizer.json + tokenizer_cases.json via a
    repo-relative path with env-var override (APRENDER_SETFIT_FIXTURES) following the
    bench-tokenizer resolution style. Iterate EVERY case in the file (it is the corpus of record, so
    the count grows as other fixtures are added — do not hardcode a case list). Integer equality
    only — tokenizer parity has no epsilon. Test fn names start `tokenizer_parity_` /
    `sentence_batch_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features setfit tokenizer_parity && cargo test -p aprender-core --lib --features setfit sentence_batch</automated>
  </verify>
  <acceptance_criteria>
    - tokenizer.rs contains `pub struct SentenceBatch` with the exact fields in the interfaces block, including `tokenizer_sha256`
    - **Every SentenceBatch field is `pub(crate)`**, and the public read accessors exist. Source assertion (comment-safe form): `grep -nE '^[^/]*\bpub (input_ids|token_type_ids|attention_mask|batch|seq|truncation|provenance|tokenizer_sha256)\b' crates/aprender-core/src/setfit/tokenizer.rs` returns no match
    - No setter, no `_mut` accessor, and no owned-interior-state getter on SentenceBatch (source assertion)
    - `from_bytes` is declared `pub(crate) fn from_bytes` — a bare `pub fn from_bytes` fails this criterion (D-08 seal, source assertion)
    - Parity test iterates ALL cases in tokenizer_cases.json (asserts case count > 4, and does not hardcode a case-name list) with exact integer comparison against CANONICAL ids
    - A test asserts batch.tokenizer_sha256() equals both the tokenizer's own hash and the fixture's recorded hash
    - The original-token-count path is tested against the truncation fixture case, and the chosen pass strategy is documented in the module docs and SUMMARY
    - No epsilon/tolerance literal appears in tokenizer tests
    - `cargo test -p aprender-core --lib --features setfit tokenizer_parity` exits 0
  </acceptance_criteria>
  <done>ENC-02 parity proven against frozen fixtures; SentenceBatch carries token facts, provenance, and tokenizer identity for 01-06, and is unforgeable and immutable from outside the crate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pinned-revision import with widened typed rejection matrix + slice constructor (ENC-01)</name>
  <files>crates/aprender-core/src/setfit/import.rs, crates/aprender-core/src/setfit/mod.rs</files>
  <read_first>
    - crates/aprender-core/src/models/bert/load.rs (read_tensor :114-145 — now pub(crate) per Task 1; detect_bert_prefix :147-162; load_embeddings_from_reader :175-199 — reuse via calls, no further modification)
    - crates/aprender-core/src/models/bert/config.rs (BertConfig::minilm_l6() at :57-69 — the exact pin: 384/6/12/1536/30522/512/2/1e-12/pad 0)
    - crates/aprender-core/tests/fixtures/setfit/slice_config.json and vocab_remap.json (slice dims and remap the bypass constructor accepts)
    - .planning/phases/01-differentiable-minilm-conformance/01-RESEARCH.md (Pitfall 3 — what the slice bypass may and may not skip)
  </read_first>
  <behavior>
    - open() on a dir whose config.json matches the pin and whose tokenizer bytes hash matches succeeds and records revision + hashes
    - Mutating ANY of the following yields a typed error NAMING the field — one distinct test per field (the mutation matrix, >= 15 tests): hidden_size, num_hidden_layers, num_attention_heads, intermediate_size, vocab_size, max_position_embeddings, layer_norm_eps, type_vocab_size, pad_token_id, hidden_act (e.g. "gelu_new" -> UnsupportedActivation), hidden_dropout_prob, attention_probs_dropout_prob, position_embedding_type (e.g. "relative_key" -> rejected), architectures (non-BertModel), pooling mode (non-mean), normalize flag, sentence-transformer max_seq_length
    - A config.json carrying EXTRA unmodelled fields (as the real pinned config does) is ACCEPTED — unknown metadata must not be a rejection reason
    - Tokenizer bytes with one flipped byte -> Err(TokenizerHashMismatch)
    - open_slice_fixture() loads slice_model.apr with slice dims AND the vocab remap, still rejecting a missing tensor, a shape-inconsistent tensor, and non-finite values
    - open_slice_fixture() rejects a remap whose slice rows exceed the slice vocab size, or whose slice_to_orig length disagrees with the slice vocab
    - open() with slice dims fails (the bypass is NOT reachable from the pin path)
    - vocab_remap() is None after open(), Some after open_slice_fixture()
  </behavior>
  <action>
    Implement import.rs per interfaces block. Full-pin path: parse config.json with serde into a
    struct that models every BEHAVIOR-AFFECTING field listed in <behavior> — and **do NOT use
    `deny_unknown_fields`**. The real HF config carries fields this struct does not model
    (torch_dtype, transformers_version, model_type, initializer_range, gradient_checkpointing, and
    more); denying unknown fields would reject the pinned model itself and turn ENC-01 into a gate
    that fails on the correct artifact. The security property comes from validating every field that
    can change behavior, not from refusing to parse metadata. Document that reasoning inline.

    Compare each modelled field against `BertConfig::minilm_l6()` constants (and, for `hidden_act`,
    against the literal `"gelu"` — the exact erf form implemented by `Tensor::gelu_exact` in 01-09;
    `"gelu_new"` / `"gelu_pytorch_tanh"` must be REJECTED, not silently accepted, because they select
    the tanh approximation). Validate the sentence-bert module policy (mean pooling + normalize —
    read modules.json / 1_Pooling/config.json from the pinned layout) and the sentence-transformer
    `max_seq_length`. Check tokenizer sha256 against the recorded pin. Record the revision string
    `1110a243fdf4706b3f48f1d95db1a4f5529b4d41` as data (never fetch by branch).

    Weight loading calls `crate::models::bert::load::read_tensor` directly (now `pub(crate)` per
    Task 1's sanctioned amendment) with every tensor read checked (presence, dtype, numel) plus a
    finiteness scan; construction errors are SetFitError values wrapping BertLoadError, never panics.

    Both constructors are **`pub(crate)`** (D-08 seal, user decision 2026-08-08): `pub(crate) fn open`
    and `pub(crate) fn open_slice_fixture`. Nothing outside the crate constructs a `MiniLmImport`;
    01-07's `SetFitMiniLm::from_pretrained_dir` / `from_slice_fixture` are the public entry points and
    they build the tokenizer alongside the import from the same source, so a mismatched pair cannot be
    assembled. The `MiniLmImport` type itself and its read accessors stay `pub`.

    Slice constructor behind #[cfg(feature = "conformance-fixtures")] takes SliceConfig
    (deserialized from slice_config.json) and VocabRemap (from vocab_remap.json) and bypasses ONLY
    the equality-with-minilm_l6 checks. It VALIDATES the remap: every slice row < slice vocab size,
    slice_to_orig length == slice vocab size, and orig_to_slice/slice_to_orig are mutual inverses.
    Do NOT parameterize the pin path by caller dims — that is PF-011's exact failure mode.

    Tests: the mutation matrix (one field per test, asserting the named field in the error), the
    extra-unknown-fields acceptance test, slice load smoke (reads slice_model.apr through
    open_slice_fixture, gated on conformance-fixtures), remap validation tests, and the
    public-path-rejects-slice test. Test fn names start `import_pin_` / `import_slice_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --lib --features conformance-fixtures import_pin && cargo test -p aprender-core --lib --features conformance-fixtures import_slice</automated>
  </verify>
  <acceptance_criteria>
    - import.rs contains `pub(crate) fn open` and `#[cfg(feature = "conformance-fixtures")]` on `pub(crate) fn open_slice_fixture` (source assertion) — a bare `pub fn` on either fails this criterion (D-08 seal)
    - `grep -nE '^[^/]*\bpub fn open\b' crates/aprender-core/src/setfit/import.rs` returns no match (the seal is structural, not a comment; the `^[^/]*` prefix keeps doc comments from being counted as violations or as passes)
    - No `deny_unknown_fields` anywhere in import.rs (source assertion), and a test proves a config with extra unmodelled fields is accepted
    - At least 15 distinct mutation tests, each asserting the mutated field name appears in the typed error; the set covers hidden_act, both dropout probabilities, position_embedding_type, type_vocab_size, pad_token_id, and sentence-transformer max_seq_length
    - hidden_act "gelu_new" is REJECTED by a dedicated test (it selects the tanh approximation, which is not the pinned semantics)
    - Slice smoke test loads crates/aprender-core/tests/fixtures/setfit/slice_model.apr successfully and exposes Some(vocab_remap)
    - Remap validation tests cover out-of-range slice row and inverse-consistency failure
    - `cargo test -p aprender-core --lib --features conformance-fixtures import_` exits 0 (all these tests are in-module, so `pub(crate)` is sufficient for every one of them — if any test needs wider visibility, that is a signal the test belongs in-module, not that the seal should be widened)
    - `git diff crates/aprender-core/src/models/bert/` shows ONLY the A-01 visibility line + comment from Task 1
  </acceptance_criteria>
  <done>ENC-01 typed rejection proven across the full behavior-affecting field set; slice fixtures load with a validated remap without weakening the public pin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model dir / APR file | untrusted artifact: config.json, tokenizer bytes, tensors |
| text input | arbitrary user strings enter encode_batch |
| SentenceBatch provenance | a batch may originate from a different tokenizer than the encoder's |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-09 | Tampering | import path | mitigate | field-equality pin vs minilm_l6 across ALL behavior-affecting fields (incl. activation, dropouts, position embedding type, pad id, max_seq_length), tokenizer sha256, per-tensor presence/dtype/numel/finiteness checks BEFORE use (PF-011 prevention); unknown metadata tolerated so the gate cannot fail on the correct artifact |
| T-1-10 | Spoofing | mutable-ref model swap | mitigate | immutable revision sha recorded as data; no branch-name fetch anywhere in the module |
| T-1-11 | DoS | hostile text/config input | mitigate | typed SetFitError on oversize/malformed/empty inputs; no assert!/unwrap on reachable paths |
| T-1-21 | Spoofing | batch produced by a foreign tokenizer | mitigate | STRUCTURAL (D-08): `MiniLmTokenizer::from_bytes`, `MiniLmImport::open`, and `open_slice_fixture` are `pub(crate)`, so no out-of-crate caller can assemble a mismatched tokenizer/encoder pair — the only public path is `SetFitMiniLm`, which builds both halves from one source. DATA LAYER (W1): `SentenceBatch`'s fields are `pub(crate)` with read-only accessors, so out-of-crate code can neither forge a batch stamped with a borrowed hash nor mutate the ids of a batch it received. DEFENSE IN DEPTH: the encoder re-checks `tokenizer_sha256` at every forward call (01-06), covering in-crate misuse — and that check is meaningful precisely because the compared value is not out-of-crate-writable |
| T-1-22 | Tampering | slice remap out of range | mitigate | VocabRemap validated for range and mutual-inverse consistency at construction; to_slice_row returns a typed error, never a zero-fill (the aprender-train trap) |
| T-1-SC | Tampering | cargo install of tokenizers 0.23.1 | mitigate | crate verified on crates.io (RESEARCH audit: Approved, cross-ecosystem check done); default-features off removes onig/esaxx native build scripts |
</threat_model>

<verification>
- `cargo test -p aprender-core --lib --features conformance-fixtures tokenizer_parity`, then `import_pin`, then `import_slice`, then `sentence_batch` — separate invocations (cargo accepts only ONE positional test filter; verified 2026-08-08)
- Feature matrix: no-default-features / setfit / all-features all check; no tokenizers node without setfit. `cargo check -p aprender-core --features setfit` specifically proves the `dep:sha2` closure (B4) — it is the leg that would have failed
- Seal assertions: `pub fn from_bytes` / `pub fn open` / `pub fn open_slice_fixture` appear NOWHERE in setfit/ (D-08 structural), and no SentenceBatch field is `pub` (W1 data-layer seal)
- `cargo clippy -p aprender-core --features setfit -- -D warnings` clean
- `git diff crates/aprender-core/src/models/bert/` contains only the A-01 visibility change (D-01 amendment, sanctioned 2026-08-08)
</verification>

<success_criteria>
- ENC-01: pinned import with a >= 15-field typed rejection matrix, proven by tests, that accepts the real config's unmodelled metadata
- ENC-02: tokenizer parity exact against frozen fixtures; SentenceBatch carries canonical ids, token facts, provenance, and tokenizer identity
- D-05/D-06: setfit feature isolates tokenizers; matrix legs green; minimal consumers unaffected; the
  feature is dependency-CLOSED (`dep:sha2` included) so `--features setfit` compiles on its own
- D-08 delivered in its ORIGINAL structural form: tokenizer and import constructors are `pub(crate)`,
  so a mismatched pair is not constructible from outside the crate; `SentenceBatch` is read-only
  out-of-crate so the stamped identity cannot be forged or mutated; the sha256 boundary check is
  retained as defense in depth rather than as the guarantee
- `SetFitError::Op(OpError)` exists so 01-06/01-07 can compose the ungated ops without widening this file
- A-01 discharged as a one-line, behavior-neutral visibility change, recorded for verify-phase
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-05-SUMMARY.md` when done.
</output>
