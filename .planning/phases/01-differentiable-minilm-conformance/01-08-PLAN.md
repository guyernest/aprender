---
phase: 01-differentiable-minilm-conformance
plan: 08
type: execute
wave: 6
depends_on: [01-04, 01-07]
files_modified:
  - crates/aprender-core/tests/setfit_conformance.rs
  - crates/aprender-core/tests/setfit_conformance/forward_parity.rs
  - crates/aprender-core/tests/setfit_conformance/gradient_gate.rs
  - crates/aprender-core/tests/setfit_conformance/detach_negative.rs
  - crates/aprender-core/tests/setfit_conformance/full_weight_parity.rs
  - crates/aprender-core/Cargo.toml
  - Makefile
autonomous: true
requirements: [ENC-03, ENC-04, ENC-05, ENC-06]

must_haves:
  truths:
    - "Slice forward, pooling, normalization, loss, gradient, and optimizer-step values match frozen Python fixtures within tolerances READ FROM THE CONTRACT YAML (zero epsilon literals in test code)"
    - "Every contracted trainable component receives a finite non-zero gradient and changes after one AdamW step; a pinned frozen group stays byte-identical; the loss decreases"
    - "A deliberately detached encoder variant makes the gradient gate FAIL, in every default cargo test run (the gate is not theater)"
    - "make tier2 runs the fixture-parity, gradient, and detach-negative gates; pv validate covers the phase contract in tier3"
  artifacts:
    - path: "crates/aprender-core/tests/setfit_conformance.rs"
      provides: "Conformance harness: fixture loading + contract-tolerance reader"
    - path: "crates/aprender-core/tests/setfit_conformance/detach_negative.rs"
      provides: "In-band D-24 negative gate"
    - path: "Makefile"
      provides: "tier2/tier3 wiring (D-26) + feature-matrix target (D-06)"
      contains: "setfit_conformance"
  key_links:
    - from: "crates/aprender-core/tests/setfit_conformance.rs"
      to: "contracts/setfit-encoder-conformance-v1.yaml"
      via: "tolerances parsed from the contract at test time (D-14 — no literals)"
      pattern: "setfit-encoder-conformance-v1"
    - from: "crates/aprender-core/tests/setfit_conformance/gradient_gate.rs"
      to: "crates/aprender-core/src/nn/optim/mod.rs"
      via: "AdamW::step_with_params over trainable partition from SetFitMiniLm"
      pattern: "step_with_params"
---

<objective>
Close the phase: fixture-parity suites (slice CI gates + full-weight ignored suite), the ENC-04
controlled-step gradient gate with frozen byte-identity, the in-band detach-negative gate (D-24),
scoped mutation testing (D-25), and tier2/tier3 wiring (D-26) with the D-06 feature-matrix target.

Purpose: converts everything built in 01-01..01-07 into the falsifiable Phase 1 gate ROADMAP
success criteria 1-5 require.
Output: green conformance suite wired into make tiers; mutation evidence; phase verifiable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md
@.planning/phases/01-differentiable-minilm-conformance/01-04-SUMMARY.md

<interfaces>
Test-side tolerance access (D-14 — tolerances live ONLY in the contract):
```rust
// harness helper in tests/setfit_conformance.rs
// Parse contracts/setfit-encoder-conformance-v1.yaml (serde_yaml dev-dependency or the in-tree
// aprender-contracts reader — whichever parses the KernelContract shape with less code) and
// expose: fn tolerance(obligation: &str) -> f32. Test files call tolerance("forward_per_layer")
// etc. NO f32 epsilon literals for fixture comparisons anywhere under tests/setfit_conformance/.
```
Fixture schema: see 01-04 PLAN interfaces block (field names are frozen there).
AdamW: `AdamW::new(params, lr)` + `step_with_params(&mut [&mut Tensor])` (nn/optim/mod.rs:350,391).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conformance harness + slice parity suites + full-weight ignored suite</name>
  <files>crates/aprender-core/tests/setfit_conformance.rs, crates/aprender-core/tests/setfit_conformance/forward_parity.rs, crates/aprender-core/tests/setfit_conformance/full_weight_parity.rs, crates/aprender-core/Cargo.toml</files>
  <read_first>
    - contracts/setfit-encoder-conformance-v1.yaml (tolerance table committed by 01-04 — the values tests read)
    - crates/aprender-core/tests/fixtures/setfit/forward_per_layer.json and pooling_normalize.json (schemas from 01-04)
    - crates/aprender-core/tests/falsification_spec_v10_tests.rs (:1-17 — feature-gate style for heavy suites)
    - crates/aprender-core/src/setfit/mod.rs (SetFitMiniLm::from_slice_fixture surface)
  </read_first>
  <action>
    Create the integration harness `tests/setfit_conformance.rs` with `#![cfg(all(feature =
    "setfit", feature = "conformance-fixtures"))]`, submodules per files list. Harness provides:
    fixture dir resolution (repo-relative + APRENDER_SETFIT_FIXTURES override), serde fixture
    structs matching the 01-04 schema, the contract-tolerance reader from the interfaces block
    (add serde_yaml as dev-dependency ONLY if the in-tree aprender-contracts reader cannot expose
    obligation tolerances with less code), and a manifest self-check test that recomputes SHA-256
    over fixture files and compares to manifest.sha256 (sha2 dev-dep — catches silent local edits).
    forward_parity.rs (slice model, eval mode): per-layer token outputs vs forward_per_layer.json
    (assert layer index in failure message — D-15 localization), pooled + normalized vs
    pooling_normalize.json, pair loss forward vs loss_pair.json, batch-1-vs-batch-N padding
    invariance vs batch_invariance.json — each within tolerance(<obligation>).
    full_weight_parity.rs: `#[ignore]`-marked tests additionally gated `#[cfg(feature =
    "model-tests")]`, loading the full pinned checkpoint from APRENDER_MINILM_DIR (01-04 fetch
    script) through the PUBLIC full-pin path and comparing 3 sentence embeddings vs
    full_model_reference.json (D-10 — never in the default gate).
    Test fn names start `conformance_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance conformance_</automated>
  </verify>
  <acceptance_criteria>
    - No f32 tolerance literal in any file under tests/setfit_conformance/ (fixture comparisons all call the tolerance reader)
    - Per-layer parity failure messages name the layer index
    - Manifest self-check test present and green
    - full_weight_parity tests are #[ignore] + model-tests gated; default run skips them
    - `cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance conformance_` exits 0
  </acceptance_criteria>
  <done>Slice parity green under contract tolerances; full-weight suite gated and documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ENC-04 controlled-step gradient gate + D-24 detach-negative gate</name>
  <files>crates/aprender-core/tests/setfit_conformance/gradient_gate.rs, crates/aprender-core/tests/setfit_conformance/detach_negative.rs</files>
  <read_first>
    - crates/aprender-core/tests/fixtures/setfit/gradients.json and optimizer_step.json (parity targets)
    - crates/aprender-core/src/nn/optim/mod.rs (AdamW step_with_params :350, struct :391)
    - contracts/lora-adapter-trains-base-frozen-v1.yaml (:38-79 — the three-guard falsification framing to replicate)
    - crates/aprender-core/src/setfit/encoder.rs (forward internals — the detached variant must sever exactly one link)
  </read_first>
  <behavior>
    - Gradient parity: backward through pair_cosine_mse on the fixture pair batch produces named grads matching gradients.json within tolerance("gradients"), for EVERY key (no skips)
    - Controlled step (dropout disabled, fixture hyperparams lr 2e-5): every trainable named tensor has finite grad with at least one |g| > 1e-9 AND at least one changed element post-step; post-step values match optimizer_step.json within tolerance("optimizer_step")
    - Frozen pin (D-21): with FreezeGroup::LayerAttention(1) applied, every frozen tensor is bitwise-identical post-step (f32::to_bits) and received no optimizer update
    - Loss decreases: loss after the step < loss before (loss-reducing direction, success criterion 4)
    - Detach-negative (D-24): a test-local detached forward (reproduces the PMAT-913 class: pooled output rebuilt via Tensor::new from .data() before the loss) makes the SAME gate helper report failure — the test asserts the gate rejects it (zero grads reach encoder params)
  </behavior>
  <action>
    gradient_gate.rs: build SetFitMiniLm::from_slice_fixture, eval dropout off (p inert via eval
    or seed policy — match fixture generation conditions, D-16), run the fixture pair batch through
    pair_cosine_mse, backward, assert grad parity per named key; then AdamW::new with lr from
    optimizer_step.json over trainable_parameters_mut, one step_with_params, assert the four gate
    bullets. Extract the pass/fail core into a reusable helper
    `fn assert_encoder_updates(...) -> Result<(), String>` (named-grad finite/non-zero, delta
    present, frozen byte-identity) so detach_negative.rs can invoke the IDENTICAL logic.
    detach_negative.rs: construct the detached variant WITHOUT modifying production code (a
    test-side wrapper around forward_tokens output: read .data(), rebuild via Tensor::new, feed to
    the same loss) and assert assert_encoder_updates returns Err listing all-zero/no-grad
    parameters. This runs in every default `--features setfit,conformance-fixtures` test pass —
    in-band, not ignored. Test fn names start `gradient_gate_` / `detach_negative_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance gradient_gate_ && cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance detach_negative_</automated>
  </verify>
  <acceptance_criteria>
    - Gradient parity iterates every gradients.json key (asserts key count equals named_parameters count)
    - Frozen byte-identity asserted via to_bits comparison, not approximate equality
    - Loss-decrease assertion present (strict <)
    - detach_negative asserts gate FAILURE on the severed variant (not merely that grads are zero) using the same helper as the positive gate
    - Both suites green in the automated command
  </acceptance_criteria>
  <done>Success criterion 4 + 5 machinery proven positive AND negative, in-band.</done>
</task>

<task type="auto">
  <name>Task 3: Tier wiring (D-26), feature-matrix target (D-06), scoped cargo-mutants (D-25)</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (tier2 recipe :185-197; PV_BIN :801; contract validate loop :849 — determine whether the loop globs contracts/ or lists them)
    - .planning/phases/01-differentiable-minilm-conformance/01-RESEARCH.md (Validation Architecture — sampling commands; D-26 placement rules)
  </read_first>
  <action>
    Makefile changes ONLY (do NOT touch .github/workflows/*.yml — CI workflow edits require prior
    user approval per CLAUDE.md; list any needed CI matrix legs in the SUMMARY as a follow-up):
    (1) tier2: add `cargo test -p aprender-core --lib --features setfit` and
    `cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance`
    (fixture-parity + gradient + detach-negative per D-26). Measure runtime; if the conformance
    suite pushes tier2 past its <5s budget, keep detach_negative + gradient_gate in tier2 and move
    forward_parity to tier3, documenting the split in the Makefile comment.
    (2) tier3: ensure `$(PV_BIN) validate contracts/setfit-encoder-conformance-v1.yaml` runs — if
    the :849 loop already globs all contracts, verify by running it; if list-based, append.
    (3) New target `setfit-feature-matrix` wired into tier3: no-default-features check, setfit
    check, all-features check, plus `! cargo tree -p aprender-core --no-default-features -e normal | grep -q tokenizers`.
    (4) Run scoped mutation testing once and triage:
    `cargo mutants --no-times --timeout 300 --in-place -f 'crates/aprender-core/src/autograd/ops/*.rs' -f 'crates/aprender-core/src/setfit/encoder.rs' -- --features setfit,conformance-fixtures`
    — every MISSED mutant in a graph-recording block (set_grad_fn / with_graph / record) gets a
    new test in the appropriate suite before this task completes; other survivors are documented
    in the SUMMARY with rationale. Record the caught/missed counts in the SUMMARY (verification
    discipline: cite the mutants.out numbers, not intent).
    (5) Run `bash scripts/check_include_files.sh` and `bash scripts/check_package_includes.sh`
    (CB-510 — fixtures and new include!/test files must be tracked and packaged correctly).
  </action>
  <verify>
    <automated>make tier2 && make setfit-feature-matrix</automated>
  </verify>
  <acceptance_criteria>
    - Makefile tier2 recipe contains a setfit_conformance invocation (or the documented tier2/tier3 split with detach_negative + gradient_gate in tier2)
    - `make setfit-feature-matrix` exists, exits 0, and includes the negative tokenizers-leak check
    - tier3 path runs pv validate over setfit-encoder-conformance-v1.yaml (prove by running the tier3 contract step or the loop with the new contract present)
    - mutants run executed; zero MISSED mutants in graph-recording blocks of the scoped files; counts recorded in SUMMARY
    - Both CB-510 scripts exit 0
    - No file under .github/workflows/ modified (git diff assertion)
  </acceptance_criteria>
  <done>Phase gate lives in the tiers; detachment hunted by machine; feature matrix enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files | committed JSON/APR consumed by tests — manifest self-check guards local tampering |
| gate integrity | the gate itself is the security control for PF-001 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Repudiation | tolerance drift / re-baselining | mitigate | tests read tolerances from the versioned contract only; manifest self-check test recomputes SHA-256 |
| T-1-12 | Repudiation | gradient gate theater | mitigate | in-band detach-negative (D-24) + scoped cargo-mutants (D-25) with zero missed graph-recording mutants |
| T-1-15 | DoS | tier2 budget blowout | mitigate | documented tier2/tier3 split rule keeps pre-commit fast while gates stay in-tier (D-26) |
| T-1-SC | Tampering | third-party installs | accept | no new packages (serde_yaml only as dev-dependency if chosen — workspace-internal decision, crates.io mature) |
</threat_model>

<verification>
- `cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance` fully green
- `make tier2` green within budget policy; `make setfit-feature-matrix` green
- pv validate green on the phase contract via tier3 path
- mutants.out: 0 missed in graph-recording blocks of scoped files; counts cited in SUMMARY
- `cargo test -p aprender-core --features setfit,model-tests --test setfit_conformance -- --ignored` documented as the D-10 command (run locally if APRENDER_MINILM_DIR populated; not required for CI green)
</verification>

<success_criteria>
- ROADMAP success criteria 1-5 all have executable, in-tier proofs:
  parity within contract tolerances (2), mode/param invariance (3, via 01-06 tests in tier2),
  gradient/step/frozen/loss-direction gate (4), loss connectivity + detach-failure (5),
  typed import rejection (1, via 01-05 tests in tier2)
- D-24/D-25/D-26 discharged with cited evidence
- Phase ready for /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-08-SUMMARY.md` when done.
</output>
