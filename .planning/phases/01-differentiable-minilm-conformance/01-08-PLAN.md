---
phase: 01-differentiable-minilm-conformance
plan: 08
type: execute
wave: 6
depends_on: [01-04, 01-07]
files_modified:
  - crates/aprender-core/tests/setfit_conformance.rs
  - crates/aprender-core/tests/setfit_conformance/tolerances_generated.rs
  - crates/aprender-core/tests/setfit_conformance/forward_parity.rs
  - crates/aprender-core/tests/setfit_conformance/gradient_gate.rs
  - crates/aprender-core/tests/setfit_conformance/frozen_gate.rs
  - crates/aprender-core/tests/setfit_conformance/detach_negative.rs
  - crates/aprender-core/tests/setfit_conformance/full_weight_parity.rs
  - crates/aprender-core/Cargo.toml
  - crates/aprender-core/src/generated_contracts.rs
  - Makefile
autonomous: true
requirements: [ENC-03, ENC-04, ENC-05, ENC-06]

must_haves:
  truths:
    - "Slice forward, activation, pooling, normalization, loss, gradient, and optimizer-step values match frozen Python fixtures within tolerances DERIVED FROM THE CONTRACT YAML (no hand-written epsilon in any comparison), and a test proves the generated constants still equal the contract whenever the contract is reachable"
    - "EVERY batch this harness feeds to the model comes from SetFitMiniLm::tokenize(fixture.texts) — no SentenceBatch is ever hand-constructed — so the slice vocab remap is exercised inside the encoder exactly as production does it"
    - "Per-layer parity compares against BertSentenceEncoder::forward_tokens_per_layer (01-06), reached through SetFitMiniLm::encoder(), and every failure message names the layer index"
    - "On an ALL-TRAINABLE model: every named gradient is finite, every contracted component has a non-zero aggregate gradient, exempt tensors are near zero, the post-AdamW state matches optimizer_step.json with the FULL fixture hyperparams, and the loss decreases to the recorded loss_after"
    - "On a SEPARATE frozen-policy model: trainable/frozen partitions are disjoint and complete, frozen tensors receive no optimizer update, and frozen tensors are bitwise identical post-step"
    - "A deliberately detached encoder variant whose leaf explicitly requires grad makes the gradient gate FAIL in every feature-enabled conformance run (--features setfit,conformance-fixtures) and every make tier2 run — sanctioned D-24 narrowing accepted by user 2026-08-07; CI matrix legs are an approval-gated follow-up"
    - "make tier2 runs the gradient, frozen, and detach-negative gates, with fixture-parity in tier2 if the <5s budget holds, else in tier3 (fallback accepted by user 2026-08-07); the tier3 RECIPE ITSELF invokes pv over the phase contract, proven by a make tier3 run — not merely appended to a CONTRACTS list feeding a target no tier depends on"
    - "One successful full-weight (D-10) suite execution is recorded before the phase is declared complete"
  artifacts:
    - path: "crates/aprender-core/tests/setfit_conformance.rs"
      provides: "Conformance harness: fixture loading + generated tolerance constants + contract-agreement check + the single tokenize-from-texts batch builder"
    - path: "crates/aprender-core/tests/setfit_conformance/frozen_gate.rs"
      provides: "Frozen-policy proof on its own clean model (separate from all-trainable optimizer parity)"
    - path: "crates/aprender-core/tests/setfit_conformance/detach_negative.rs"
      provides: "In-band D-24 negative gate"
    - path: "Makefile"
      provides: "tier2/tier3 wiring (D-26) + feature-matrix target (D-06); the tier3 recipe gains a pv invocation it previously lacked entirely"
      contains: "setfit_conformance"
  key_links:
    - from: "crates/aprender-core/tests/setfit_conformance/tolerances_generated.rs"
      to: "contracts/setfit-encoder-conformance-v1.yaml"
      via: "constants generated from the contract, with a runtime agreement test whenever the contract is reachable (D-14 — no hand-written epsilons)"
      pattern: "setfit-encoder-conformance-v1"
    - from: "crates/aprender-core/tests/setfit_conformance/forward_parity.rs"
      to: "crates/aprender-core/src/setfit/encoder.rs"
      via: "SetFitMiniLm::encoder() -> forward_tokens_per_layer (01-06, conformance-gated pub) — the per-layer intermediates forward_per_layer.json records"
      pattern: "forward_tokens_per_layer"
    - from: "crates/aprender-core/tests/setfit_conformance/forward_parity.rs"
      to: "crates/aprender-core/tests/fixtures/setfit/forward_per_layer.json"
      via: "fixture `texts` -> SetFitMiniLm::tokenize -> encoder; the fixture's case_id joins back to tokenizer_cases.json"
      pattern: "tokenize"
    - from: "Makefile"
      to: "contracts/setfit-encoder-conformance-v1.yaml"
      via: "pv invocation inside the tier3 recipe itself (D-26); CONTRACTS-list membership alone is insufficient because contract-validate is reachable only from contract-check"
      pattern: "contract-validate"
    - from: "crates/aprender-core/tests/setfit_conformance/gradient_gate.rs"
      to: "crates/aprender-core/src/nn/optim/rm_sprop.rs"
      via: "AdamW::step_with_params over trainable partition from SetFitMiniLm, hyperparams configured from optimizer_step.json"
      pattern: "step_with_params"
---

<objective>
Close the phase: fixture-parity suites (slice CI gates + full-weight ignored suite), the ENC-04
controlled-step gradient gate on an all-trainable model, the frozen-policy proof on its own clean
model, the in-band detach-negative gate (D-24), scoped mutation testing (D-25), and tier2/tier3
wiring (D-26) with the D-06 feature-matrix target.

Purpose: converts everything built in 01-01..01-07 and 01-09 into the falsifiable Phase 1 gate
ROADMAP success criteria 1-5 require.
Output: green conformance suite wired into make tiers; mutation evidence; phase verifiable.

Sanctioned scope notes (user acceptances, 2026-08-07 plan-phase checkpoint — PRESERVED, do not
regress):
- D-24 narrowing ACCEPTED: the detach-negative gate is enforced in every feature-enabled test run
  (`--features setfit,conformance-fixtures`) and via `make tier2` wiring — NOT in default-feature
  `cargo test` runs. CI matrix legs adding those features are proposed as a follow-up PR requiring
  user approval (CI workflow edits need prior approval per CLAUDE.md); list them in the SUMMARY.
  verify-phase: this is a sanctioned narrowing, not scope reduction.
- D-26 fallback ACCEPTED: if the conformance suite exceeds tier2's <5s budget (near-certain, est.
  10-30s), the heavy fixture-parity suite moves to tier3 (pre-push) while the fast gates
  (gradient_gate + frozen_gate + detach_negative) stay in tier2. Pre-approved — triggering it is not
  a deviation.

Review-driven revisions in this pass (2026-08-08, `/gsd:plan-phase 1 --reviews`):
- **The all-trainable optimizer parity and the frozen proof are SPLIT onto two clean models.** They
  were conflated: `optimizer_step.json` records a step in which EVERY parameter was trainable, so a
  Rust model with `FreezeGroup::LayerAttention(1)` applied cannot match it. One test state cannot
  prove both outcomes. Model A (all-trainable) proves gradient + step parity against the fixture;
  Model B (frozen policy) proves disjointness, no-update, and bitwise identity — a purely internal
  property needing no Python reference.
- **The ENC-04 gate is restated** to finite-everywhere + non-zero aggregate per contracted component
  + two-sided data-driven exemptions from `gradients.json.analytically_zero`. The prior per-tensor
  non-zero requirement fails on correct code.
- **Tolerances reach tests via generated crate-owned constants**, not by parsing a workspace-root
  YAML at test runtime — the contract lives outside the crate directory, so runtime parsing breaks
  `cargo package` verification and standalone crate testing. D-14 is preserved by an agreement test.
- **The detached leaf explicitly sets `requires_grad`**, so the negative test distinguishes graph
  detachment from a wholly non-differentiable loss.
- **A full-weight (D-10) run is required and recorded** before phase completion, even though it
  stays out of default CI.
- **Mutation budget is bounded and recorded.**
- **Every `cargo test` command passes at most one positional filter** (verified 2026-08-08: a second
  positional argument exits with `error: unexpected argument`).

Checker-driven revisions (2026-08-08, post-`--reviews` re-verification):
- **[B3 — D-26] The tier3 `pv validate` wiring would not have run.** The checker read the Makefile:
  `tier3` (:201-221) runs `cargo test --all`, clippy, and four shell scripts — there is NO `pv`
  invocation anywhere in it, and `tier4: tier3` adds only pmat. The `CONTRACTS` list (:803) feeds
  `contract-validate` (:845), which is reachable ONLY from `contract-check` (:876) — a target no tier
  depends on. Task 3's old step (2) ("if list-based, append") would have landed the phase gate in a
  target outside the tiers, which is exactly what D-26's rationale rejects: *"a target outside the
  tiers is a target that stops being run."* Step (2) now requires editing the `tier3` recipe itself,
  and the escape hatch is deleted from the acceptance criterion.
- **[W9 — CLAUDE.md muda] Tolerance generation must not hand-roll a YAML parser.** The old text said
  "parse the YAML with serde_yaml". CLAUDE.md: *"Writing a bash/yq/python script that re-implements
  what `pv` already does is muda and will be rejected."* See Task 1 for the required check-first
  procedure and the dogfooded fallback. The DESIGN (generated constants + a red-demonstrated
  agreement test) is unchanged and still preserves D-14 — only the generation mechanism moved.
- **[I10] The "contracts/ is outside the crate dir — structurally impossible" rationale was wrong**
  and is corrected in the interfaces block above. The conclusion (generated constants) stands, for
  the two-sources-of-truth reason rather than the impossibility reason.
- **[B2 consequence] The lower-level constructors are now `pub(crate)`** (D-08 seal, user decision
  2026-08-08). This harness is an out-of-crate integration test, so it constructs models ONLY via
  `SetFitMiniLm::from_slice_fixture` / `from_pretrained_dir` and reaches the encoder through the
  conformance-gated `encoder()` / `tokenize()` accessors added by 01-07.

Checker-driven revisions, iteration 2 (2026-08-08):
- **[B5 — the per-layer intermediates now EXIST; call them unconditionally].** The old Task 1 text
  said: "*If* per-layer intermediates are not reachable through the encoder's existing public
  methods, add a `#[cfg(feature = "conformance-fixtures")]` READ method to
  `crates/aprender-core/src/setfit/encoder.rs`." That was wrong twice over: it targeted a file NOT in
  this plan's `files_modified` (the same objection already sustained for `SetFitError::Op` in 01-07),
  and it was written as a discovery when 01-06's frozen interface already PROVED the intermediates
  were unreachable. Capturing them is not a trivial accessor either — it required restructuring
  01-06's forward loop, i.e. a wave-6 plan editing a wave-4 plan's core implementation. 01-06 now
  owns `#[cfg(feature = "conformance-fixtures")] pub fn forward_tokens_per_layer(&self, batch)
  -> Result<(Tensor, Vec<Tensor>), SetFitError>`, built on the same shared `forward_layers` as
  `forward_tokens`. This plan CALLS it, unconditionally, via `model.encoder()`. It adds NO method to
  `encoder.rs` and NO accessor to `mod.rs`.
- **[B6 — build every batch through the tokenizer, from the fixture's `texts`].** After the D-08
  seal the only public batch producer is `SetFitMiniLm::tokenize(&self, texts: &[&str])`, which takes
  TEXTS — but the fixtures carried ids only, so the sole compilable route was hand-constructing a
  `SentenceBatch`, which would have silently bypassed the tokenizer boundary this plan's own
  acceptance criterion demands **while passing every listed check**. 01-04 now emits `texts` +
  `case_id` on `forward_per_layer`, `pooling_normalize`, `loss_pair`, and `batch_invariance`, and a
  `source` pointer on `gradients` / `optimizer_step`. Every batch in this harness is now built by one
  shared helper that calls `model.tokenize(case.texts)`, and a source assertion forbids
  `SentenceBatch` literals. (01-05 also made `SentenceBatch`'s fields `pub(crate)`, so a literal no
  longer compiles out-of-crate at all — the assertion catches an attempt to reopen that, and gives a
  readable failure instead of a type error.)
- **[W4 — do not make tier3 depend on ~45 contracts this phase does not control].** Task 3 step (2)
  keyed the form-(a)-vs-(b) choice on runtime budget alone. But `@$(MAKE) contract-validate` iterates
  the whole `CONTRACTS` list; if ANY pre-existing contract is red today, tier3 goes red for reasons
  this phase did not cause and this phase gets blamed. Run `make contract-validate` STANDALONE first
  and record its status; that status, not just wall clock, now selects the wiring form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-differentiable-minilm-conformance/01-CONTEXT.md
@.planning/phases/01-differentiable-minilm-conformance/01-PATTERNS.md
@.planning/phases/01-differentiable-minilm-conformance/01-04-SUMMARY.md

<interfaces>
Test-side tolerance access (D-14 — tolerances originate ONLY in the contract):
```rust
// crates/aprender-core/tests/setfit_conformance/tolerances_generated.rs  (GENERATED, committed)
// Header records: source contract path, contract metadata.version, and the contract file's sha256
// at generation time. Regenerated by the documented command in the harness doc comment.
pub const FORWARD_PER_LAYER: f32 = /* from contract */;
pub const ACTIVATION: f32 = /* ... */;
pub const POOLING_NORMALIZE: f32 = /* ... */;
pub const LOSS_PAIR: f32 = /* ... */;
pub const GRADIENTS: f32 = /* ... */;
pub const OPTIMIZER_STEP: f32 = /* ... */;
pub const BATCH_INVARIANCE: f32 = /* ... */;
pub const FULL_MODEL_REFERENCE: f32 = /* ... */;
pub const ZERO_GRAD_FLOOR: f32 = /* ... */;
pub const CONTRACT_SHA256: &str = "...";
```
Rationale for generating rather than parsing at runtime (CORRECTED 2026-08-08 — the earlier
"structurally impossible" wording was inaccurate): a crate-local `crates/aprender-core/contracts/`
DOES exist (it holds `model-families/`, consumed by `crates/aprender-core/build.rs`), and the crate's
`exclude` list enumerates individual `contracts/*.yaml` entries — so a crate-local contract copy is
structurally possible. It is nonetheless the WRONG design: copying the tolerance table into the crate
would create two sources of truth for the tolerances, which is precisely what D-14 exists to prevent
(loosening a tolerance must require a contract edit that `pv diff` flags with a semver bump).
The workspace-root `contracts/setfit-encoder-conformance-v1.yaml` is not part of the packaged crate,
so runtime parsing from a packaged crate would fail — hence generated constants. D-14's property is
preserved because (a) the generated file is derived, never hand-edited, and (b) the agreement test
below fails the build if the two ever diverge in a workspace checkout.

Agreement test (in tests/setfit_conformance.rs): locate
`contracts/setfit-encoder-conformance-v1.yaml` by walking up from CARGO_MANIFEST_DIR. If found
(every dev and CI run), parse it and assert every generated constant equals the contract value AND
that the file's sha256 equals `CONTRACT_SHA256`. If NOT found (packaged-crate context only), assert
`CONTRACT_SHA256` is a well-formed 64-hex string and pass — documented as the only skip in the suite.

**The ONE batch builder (B6).** Every model input in this harness goes through a single helper:
```rust
// tests/setfit_conformance.rs
fn batch_from_case(model: &SetFitMiniLm, texts: &[String])
    -> Result<SentenceBatch, SetFitError>
{
    let refs: Vec<&str> = texts.iter().map(String::as_str).collect();
    model.tokenize(&refs)          // the ONLY public batch producer after the D-08 seal
}
```
No submodule constructs a `SentenceBatch` any other way. Each fixture case supplies `texts` and a
`case_id`; the harness asserts the `case_id` resolves in `tokenizer_cases.json` and that the
resolved case's `input_ids` equal the fixture's `input_ids_canonical` AND equal
`batch.input_ids()` — that triple equality is what proves the Rust tokenizer, the fixture, and the
corpus of record all agree, and it is why the ids are still recorded even though the texts drive
the run. `input_ids_slice` is NEVER fed to the model; it exists only so a remap bug can be
diagnosed, because the remap must happen inside the encoder.

Fixture schema: see 01-04 PLAN interfaces block (field names are frozen there; `texts` + `case_id`
on forward_per_layer / pooling_normalize / loss_pair / batch_invariance, `source` on gradients /
optimizer_step).

Per-layer access path (B5): `model.encoder()` (01-07, conformance-gated) ->
`BertSentenceEncoder::forward_tokens_per_layer(&batch)` (01-06, conformance-gated `pub`) ->
`(embeddings_out, layer_outputs)`. `layer_outputs.last()` equals `forward_tokens`'s output and the
fixture's `final_tokens`.

AdamW: impl lives in nn/optim/rm_sprop.rs (struct declared nn/optim/mod.rs:391).
`AdamW::new(params, lr)` defaults betas (0.9, 0.999), eps 1e-8, weight_decay 0.01
(rm_sprop.rs:10-24); builder `.betas(b1,b2).eps(e).weight_decay(wd)`;
`step_with_params(&mut [&mut Tensor])` (rm_sprop.rs:87). Decay is DECOUPLED:
`param -= lr * wd * param` applied before the Adam update (rm_sprop.rs:80) — same ordering as
torch AdamW's `p.mul_(1 - lr*wd)`. Do NOT rely on these defaults in the gate: configure every
hyperparam explicitly from optimizer_step.json (defaults can drift; the fixture is the arbiter).
NOTE: the plain `Adam` struct in nn/optim/mod.rs uses COUPLED L2 decay and defaults wd 0.0 —
wrong optimizer for this gate; use `AdamW`.

**ENC-04 gate semantics (canonical, used by gradient_gate.rs and detach_negative.rs):**
- every named trainable tensor: gradient exists, all elements finite;
- every contracted component (embeddings; per-layer attention; per-layer FFN; per-layer norm):
  aggregate gradient L2 norm > 0;
- per-tensor non-zero required only for names ABSENT from `gradients.json.analytically_zero`;
- names PRESENT in that list must satisfy `max|g| <= ZERO_GRAD_FLOOR` — the exemption is two-sided,
  so an unexpectedly large key-bias gradient is a failure, not a pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conformance harness + generated tolerance constants + slice parity suites + full-weight ignored suite</name>
  <files>crates/aprender-core/tests/setfit_conformance.rs, crates/aprender-core/tests/setfit_conformance/tolerances_generated.rs, crates/aprender-core/tests/setfit_conformance/forward_parity.rs, crates/aprender-core/tests/setfit_conformance/full_weight_parity.rs, crates/aprender-core/Cargo.toml</files>
  <read_first>
    - .planning/phases/01-differentiable-minilm-conformance/01-04-SUMMARY.md (FIRST: whether 01-04 Task 3's codegen drift check found generated_contracts.rs would change. If it did, regenerating `crates/aprender-core/src/generated_contracts.rs` via `pv codegen` is the FIRST action of this task and lands as its OWN commit before any test code.)
    - contracts/setfit-encoder-conformance-v1.yaml (tolerance table committed by 01-04 — the values the generated constants must equal)
    - crates/aprender-core/tests/fixtures/setfit/forward_per_layer.json, pooling_normalize.json, activation_reference.json (schemas from 01-04 — note `texts` + `case_id`; inspect keys with a python one-liner, do not read whole fixtures into context)
    - crates/aprender-core/tests/falsification_spec_v10_tests.rs (:1-17 — feature-gate style for heavy suites)
    - crates/aprender-core/src/setfit/mod.rs (SetFitMiniLm::from_slice_fixture, encoder(), tokenize() surfaces from 01-07)
    - crates/aprender-core/src/setfit/encoder.rs (forward_tokens_per_layer signature from 01-06 — the per-layer source)
  </read_first>
  <action>
    If 01-04's SUMMARY reported codegen drift, run
    `pv codegen contracts/ -o crates/aprender-core/src/generated_contracts.rs`, verify
    `cargo check -p aprender-core`, and commit that alone FIRST.

    Create the integration harness `tests/setfit_conformance.rs` with
    `#![cfg(all(feature = "setfit", feature = "conformance-fixtures"))]`, submodules per the files
    list. Harness provides:
      - fixture dir resolution (repo-relative + APRENDER_SETFIT_FIXTURES override);
      - serde fixture structs matching the 01-04 schema, INCLUDING `texts` and `case_id`;
      - `batch_from_case(model, texts)` — the single batch builder from the interfaces block, and the
        only place `model.tokenize(..)` is called;
      - `tolerances_generated.rs` — GENERATE it from the contract. **Do not hand-roll a YAML parser**
        (CLAUDE.md: re-implementing what `pv` already does is muda and will be rejected). Procedure,
        in order, recording the finding either way in the SUMMARY:
          (i)  Check `pv` for an existing tolerance-emitting path: `pv codegen --help`,
               `pv equations --help`, and inspect what `pv codegen contracts/ -o <tmp>` emits. If a
               path exists, USE IT.
          (ii) If none exists, use the in-tree contract crate rather than a bespoke parser: add
               `aprender-contracts` as a DEV-dependency and call
               `aprender_contracts::schema::parser::parse_contract(path)` (parser.rs:15), reading
               `tolerance: Option<f64>` off each proof obligation (types.rs:307). This is the same
               deserialization `pv validate` itself uses, so the generator cannot drift from `pv`'s
               interpretation of the contract.
          (iii) A raw `serde_yaml` re-parse of the contract is NOT acceptable — it would be a second,
               divergent reader of a schema the workspace already owns.
        Planning-time finding (verified 2026-08-08, re-verify at execution rather than trusting this):
        neither `pv codegen` nor `pv equations` handles tolerances today — `grep -n tolerance` over
        both command modules returns nothing, and `pv equations --format` accepts only
        text/latex/ptx/asm, none of which emit Rust constants. Path (ii) is therefore the expected
        outcome. If you conclude the right fix is to teach `pv` to emit tolerances, that is a real
        engineering task with its own ticket — note it in the SUMMARY, do not silently attempt it here.
        The generated file's header must record the source path, the contract's metadata.version, the
        regeneration command, and a "DO NOT HAND-EDIT" notice;
      - the contract-agreement test from the interfaces block (walks up from CARGO_MANIFEST_DIR to
        find the contract; full equality + sha256 check when found; documented single skip when not);
      - a manifest self-check test that recomputes SHA-256 over fixture files and compares to
        manifest.sha256 (sha2 dev-dep — catches silent local edits);
      - a `case_id` join check: for every fixture case, resolve `case_id` in `tokenizer_cases.json`
        and assert the resolved `input_ids` equal the fixture's `input_ids_canonical` AND equal
        `batch.input_ids()` from the tokenized batch (the triple equality from the interfaces block).

    forward_parity.rs (slice model, eval mode). **Every batch comes from
    `batch_from_case(&model, &case.texts)` — never a hand-built `SentenceBatch`, never a
    pre-remapped id array.** Suites:
      - activation parity (`Tensor::gelu_exact` vs activation_reference.json, within ACTIVATION — an
        independent localization point so a GELU regression does not present as a whole-layer
        failure; this one takes no batch, it is a pure numeric grid);
      - per-layer token outputs: call
        `let (emb, layers) = model.encoder().forward_tokens_per_layer(&batch)?;` and compare `emb`
        against `embeddings_out`, `layers[i]` against `layer_outputs[i]`, and `layers.last()` against
        `final_tokens`, all within FORWARD_PER_LAYER. **Every failure message names which one**
        ("embeddings_out", or "layer {i}", or "final_tokens") — D-15 localization. This is a direct,
        unconditional call: 01-06 owns that method and this plan neither adds nor widens anything in
        `src/`;
      - pooled + normalized vs pooling_normalize.json;
      - pair loss forward vs loss_pair.json, with the a-side and b-side batches each built from
        `a_texts` / `b_texts`;
      - batch-1-vs-batch-N padding invariance vs batch_invariance.json: tokenize `single.texts` and
        `padded_batch.texts` separately and compare the `target_row` of the padded result against the
        single result.
    The slice remap is exercised INSIDE the encoder because the batch carries canonical ids — that is
    the whole point of driving from texts.

    full_weight_parity.rs: `#[ignore]`-marked tests additionally gated `#[cfg(feature = "model-tests")]`,
    loading the full pinned checkpoint from APRENDER_MINILM_DIR — specifically the `full_model.apr`
    plus config/tokenizer/modules that 01-04's `fetch_full_weights.py` now produces — through
    `SetFitMiniLm::from_pretrained_dir`, which is the public full-pin path now that
    `MiniLmImport::open` is `pub(crate)` (D-08 seal). Build its batch from
    `full_model_reference.json`'s `texts` through the same `batch_from_case` helper. Compare 3
    sentence embeddings vs full_model_reference.json and assert the run's `full_manifest.json`
    revision and APR sha256 (so the SUMMARY can cite WHICH bytes were tested, per CLAUDE.md
    verification discipline rule 2). D-10 — never in the default gate.

    **Construction rule for this whole harness (D-08 seal, user decision 2026-08-08).** This is an
    out-of-crate integration test. `MiniLmTokenizer::from_bytes`, `MiniLmImport::{open,
    open_slice_fixture}`, and `BertSentenceEncoder::from_import` are all `pub(crate)` and are NOT
    callable from here. Build every model via `SetFitMiniLm::from_slice_fixture` /
    `from_pretrained_dir`, every batch via `SetFitMiniLm::tokenize` (through `batch_from_case`), and
    reach per-layer intermediates via `encoder().forward_tokens_per_layer(..)`. **Do NOT add a method
    to `crates/aprender-core/src/setfit/encoder.rs` or `mod.rs` — neither file is in this plan's
    files_modified, and everything this harness needs already exists** (01-06 B5, 01-07). If
    something genuinely appears to be missing, STOP and surface it rather than reaching into a
    wave-4/wave-5 plan's files.

    Test fn names start `conformance_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features conformance-fixtures --test setfit_conformance conformance_</automated>
  </verify>
  <acceptance_criteria>
    - No hand-written f32 tolerance literal in any file under tests/setfit_conformance/ except tolerances_generated.rs (which is derived and carries a DO-NOT-HAND-EDIT header)
    - The contract-agreement test exists, and a deliberate local edit of one generated constant makes it FAIL (demonstrate this once during development and record it in the SUMMARY — an agreement test that has never turned red is not evidence)
    - Activation parity test present and separate from per-layer parity
    - Per-layer parity calls `model.encoder().forward_tokens_per_layer(..)` and compares embeddings_out, each layer_outputs[i], and final_tokens; failure messages name which of the three (and the layer index) diverged
    - **No file under tests/setfit_conformance/ constructs a `SentenceBatch` other than via `batch_from_case` / `model.tokenize`** — source assertion: `grep -rn 'SentenceBatch *{' crates/aprender-core/tests/setfit_conformance/` returns no match, and `grep -rn '\.tokenize(' crates/aprender-core/tests/setfit_conformance/` shows the call only inside `batch_from_case`
    - The `case_id` join check is present and green: every fixture case_id resolves in tokenizer_cases.json, and resolved `input_ids` == fixture `input_ids_canonical` == `batch.input_ids()`
    - `input_ids_slice` is never fed to the model (source assertion — it appears only in diagnostic messages, if at all); the remap happens inside the encoder
    - Manifest self-check test present and green
    - full_weight_parity tests are #[ignore] + model-tests gated; default run skips them; they assert the revision and APR sha256 from full_manifest.json, and construct via `SetFitMiniLm::from_pretrained_dir`
    - The tolerance generator uses `pv` or the in-tree `aprender-contracts` parser; `grep -rn "serde_yaml" crates/aprender-core/` shows no new bespoke contract parsing, and the `pv codegen`/`pv equations` check result is recorded in the SUMMARY
    - No file under tests/setfit_conformance/ calls `MiniLmImport::`, `MiniLmTokenizer::from_bytes`, or `BertSentenceEncoder::from_import` (source assertion — the D-08 seal means these would not compile; the assertion catches an attempt to reopen it)
    - `git diff --stat crates/aprender-core/src/` shows AT MOST one changed file, `generated_contracts.rs`, and only if 01-04's SUMMARY reported codegen drift — in which case it landed as this task's FIRST commit, alone, before any test code. Nothing else under `src/` changes: the per-layer method is 01-06's and is already landed, and no accessor is added to `setfit/mod.rs` (01-07's). If you find yourself needing to edit any other file under `src/`, STOP and surface it — that is the B5 defect class recurring
    - `cargo test -p aprender-core --features conformance-fixtures --test setfit_conformance conformance_` exits 0
  </acceptance_criteria>
  <done>Slice parity green under contract-derived tolerances that survive cargo package, driven end-to-end from fixture texts through the real tokenizer; full-weight suite gated, documented, and self-identifying.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ENC-04 gates — all-trainable gradient/step parity, separate frozen-policy proof, D-24 detach-negative</name>
  <files>crates/aprender-core/tests/setfit_conformance/gradient_gate.rs, crates/aprender-core/tests/setfit_conformance/frozen_gate.rs, crates/aprender-core/tests/setfit_conformance/detach_negative.rs</files>
  <read_first>
    - crates/aprender-core/tests/fixtures/setfit/gradients.json (source pointer, grads, parameter_order, zero_grad_floor, analytically_zero) and optimizer_step.json (source pointer; adamw block lr 2e-5 / betas [0.9,0.999] / eps 1e-8 / weight_decay 0.01; loss_before; loss_after; note declaring ALL-TRAINABLE)
    - crates/aprender-core/tests/fixtures/setfit/loss_pair.json (a_texts / b_texts — the batch these gradients were recorded on, per the `source` pointer)
    - crates/aprender-core/src/nn/optim/rm_sprop.rs (AdamW impl: new/builders :10-43, decoupled decay :80, step_with_params :87 — NOT the coupled-decay Adam in mod.rs)
    - contracts/lora-adapter-trains-base-frozen-v1.yaml (:38-79 — the three-guard falsification framing to replicate for the frozen proof)
    - crates/aprender-core/src/setfit/encoder.rs (forward internals — the detached variant must sever exactly one link)
    - crates/aprender-core/src/setfit/mod.rs (apply_freeze / clear_freeze / trainable_parameters_mut / frozen_parameters from 01-07)
  </read_first>
  <behavior>
    ALL THREE FILES: the pair batch is rebuilt by tokenizing `loss_pair.json`'s `a_texts` / `b_texts`
    through `batch_from_case` (Task 1's helper). `gradients.json.source` and
    `optimizer_step.json.source` name that fixture and those case ids; a test asserts the pointer
    matches what it loaded, so a future fixture regeneration that moves the pair batch fails loudly
    instead of silently comparing against the wrong reference.

    MODEL A — all-trainable (gradient_gate.rs). Freshly constructed, clear_freeze() asserted, so it
    matches the configuration optimizer_step.json actually recorded:
    - Gradient parity: backward through pair_cosine_mse on the fixture pair batch produces named grads matching gradients.json within GRADIENTS, for EVERY key (no skips); the Rust name ORDER equals gradients.json.parameter_order
    - ENC-04 gate (canonical semantics from the interfaces block): all grads finite; non-zero aggregate per contracted component; per-tensor non-zero for names absent from analytically_zero; `max|g| <= ZERO_GRAD_FLOOR` for names present in it
    - Controlled step (dropout disabled): AdamW configured explicitly with ALL hyperparams read from optimizer_step.json's adamw block — `AdamW::new(params, lr).betas(b1, b2).eps(e).weight_decay(wd)` with each value sourced from the parsed fixture, never builder defaults; post-step values match optimizer_step.json within OPTIMIZER_STEP
    - Hyperparameter agreement: the test asserts the fixture's recorded hyperparams equal the values it configured — fail loud on drift between fixture and gate
    - Decoupled decay verified: with weight_decay 0.01 active, post-step parity vs the torch-AdamW-generated fixture IS the decoupled-decay check; if parity fails specifically on decay semantics, fixing AdamW or documenting the divergence is IN-SCOPE, never absorbed by a widened tolerance
    - Post-step delta: per contracted component, aggregate delta > 0; per-tensor delta for names absent from analytically_zero; for exempt names only the post-step parity is asserted (a zero-gradient parameter still moves under decoupled decay, but that is an implementation detail the fixture already pins — do not assert it independently)
    - Loss decreases: loss_before and loss_after match the fixture within LOSS_PAIR, and loss_after < loss_before (strict)

    MODEL B — frozen policy (frozen_gate.rs). A SEPARATE, freshly constructed model. This proof is
    entirely Rust-internal and is NOT compared to any Python post-step fixture, because
    optimizer_step.json describes an all-trainable step and cannot validate a partially frozen one:
    - apply_freeze([FreezeGroup::LayerAttention(1)]) (D-21's deliberately pinned group)
    - trainable and frozen name sets are disjoint and their union equals the full named set
    - every frozen tensor has requires_grad == false and is absent from trainable_parameters_mut()
    - snapshot frozen tensors -> backward -> AdamW step over the trainable partition ONLY -> every frozen tensor is BITWISE identical (f32::to_bits) to its snapshot
    - at least one TRAINABLE tensor did change, so the test cannot pass vacuously by doing nothing
    - the trainable partition still satisfies the ENC-04 gate (finite, non-zero aggregate per unfrozen component)

    DETACH-NEGATIVE (detach_negative.rs):
    - A test-local detached forward reproduces the PMAT-913 class: read `.data()` from the pooled
      output, rebuild via `Tensor::from_vec`, and EXPLICITLY call `requires_grad_(true)` on the
      rebuilt leaf before feeding it to the loss. Without that call the loss would be
      non-differentiable outright, and the test would pass for the wrong reason — it must
      distinguish "graph severed upstream" from "nothing differentiable anywhere"
    - The SAME gate helper used by Model A reports failure on this variant; the test asserts the Err
      and asserts its message names the parameters that received no gradient
  </behavior>
  <action>
    Extract the pass/fail core into ONE reusable helper shared by all three files:
    `fn assert_encoder_updates(named_grads, components, exemptions, floor, deltas) -> Result<(), String>`
    implementing the canonical ENC-04 semantics. Both the positive gate and the negative gate MUST
    call the identical function — that identity is what makes D-24 evidence rather than theater.

    All three files obtain their batch through Task 1's `batch_from_case` helper, tokenizing
    `loss_pair.json`'s `a_texts` / `b_texts`. Do not hand-build a `SentenceBatch` and do not feed
    `a_ids_slice` / `b_ids_slice` to the model — the encoder does the remap, and that is part of what
    these gates prove.

    gradient_gate.rs (MODEL A): build `SetFitMiniLm::from_slice_fixture`, assert `freeze_policy()`
    is empty (matching the fixture's all-trainable note), assert the `source` pointer matches the
    loss_pair case ids it tokenized, eval / dropout inert (match fixture generation conditions,
    D-16), run the pair batch through pair_cosine_mse, backward, assert grad parity per named key and
    the canonical gate; then construct AdamW from the parsed adamw block, assert hyperparameter
    agreement, one `step_with_params`, assert post-step parity and the loss-decrease bullets. If
    post-step values diverge only on decayed parameters, treat it as a gate finding: fix rm_sprop.rs
    or document the divergence in the SUMMARY and contract; never widen a tolerance to pass.

    frozen_gate.rs (MODEL B): a second, clean `SetFitMiniLm::from_slice_fixture`. Do NOT reuse
    Model A's instance — a model that already took a step is not a clean frozen-proof subject.
    Apply the freeze, run the same pair batch and loss, step over the trainable partition only, and
    assert the bullets. State in the file's doc comment WHY there is no Python comparison here.

    detach_negative.rs: construct the detached variant WITHOUT modifying production code (a test-side
    wrapper around forward_tokens output: read `.data()`, rebuild via `Tensor::from_vec`, call
    `requires_grad_(true)`, feed to the same loss) and assert `assert_encoder_updates` returns Err
    listing the parameters with no/zero gradient. D-24 scope (user-accepted narrowing, 2026-08-07):
    this gate runs in every feature-enabled `--features setfit,conformance-fixtures` test pass and
    via make tier2 (Task 3) — in-band and not ignored within that feature set; default-feature
    `cargo test` does not compile it, and the CI matrix legs enabling it in CI are an approval-gated
    follow-up PR (record in SUMMARY). Test fn names start `gradient_gate_` / `frozen_gate_` /
    `detach_negative_`.
  </action>
  <verify>
    <automated>cargo test -p aprender-core --features conformance-fixtures --test setfit_conformance gradient_gate_ && cargo test -p aprender-core --features conformance-fixtures --test setfit_conformance frozen_gate_ && cargo test -p aprender-core --features conformance-fixtures --test setfit_conformance detach_negative_</automated>
  </verify>
  <acceptance_criteria>
    - All three files build their batch via Task 1's `batch_from_case` from loss_pair.json's `a_texts`/`b_texts`; no `SentenceBatch` literal and no use of `a_ids_slice`/`b_ids_slice` as model input (source assertion)
    - gradient_gate.rs asserts `gradients.json.source` / `optimizer_step.json.source` name the loss_pair case ids it actually tokenized
    - Model A and Model B are separate `from_slice_fixture` instances in separate files; gradient_gate.rs asserts `freeze_policy()` is empty and frozen_gate.rs applies a freeze — neither file does both
    - frozen_gate.rs contains no comparison against optimizer_step.json post_step values, and its doc comment states why
    - frozen_gate.rs asserts at least one trainable tensor changed (no vacuous pass)
    - Gradient parity iterates every gradients.json key (asserts key count equals named_parameters count) and asserts name ORDER against parameter_order
    - The ENC-04 gate helper implements finite + non-zero-aggregate + two-sided exemptions; a grep of the suite shows no assertion requiring a non-zero gradient on EVERY tensor
    - AdamW configured via explicit builder calls (.betas/.eps/.weight_decay) with values parsed from optimizer_step.json — grep of gradient_gate.rs shows no bare `AdamW::new(...)` without the builder chain
    - Hyperparam agreement assertion present, with weight_decay 0.01 in effect for the step
    - Post-step parity covers weight-decayed parameters; any decay-semantics divergence is fixed or documented, never absorbed by tolerance
    - Frozen byte-identity asserted via to_bits comparison, not approximate equality
    - Loss-decrease assertion present (strict <) and loss_before/loss_after parity against the fixture
    - detach_negative.rs calls `requires_grad_(true)` on the rebuilt leaf (source assertion) and asserts gate FAILURE using the SAME helper as the positive gate
    - All three suites green in the automated command
  </acceptance_criteria>
  <done>Success criteria 4 + 5 machinery proven positive AND negative, on configurations that can each actually hold, driven through the tokenizer boundary, in-band under the sanctioned D-24 scope.</done>
</task>

<task type="auto">
  <name>Task 3: Tier wiring (D-26), feature-matrix target (D-06), bounded cargo-mutants (D-25), recorded D-10 run</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (tier2 recipe :185-197; **tier3 recipe :201-221 — read it and confirm for yourself that it contains NO `pv` invocation**; PV_BIN :801; `CONTRACTS := ...` list-based, :803-843; `contract-validate` :845-851 which iterates `$(CONTRACTS)`; `contract-check` :876, the ONLY target that depends on contract-validate and which no tier depends on)
    - .planning/phases/01-differentiable-minilm-conformance/01-RESEARCH.md (Validation Architecture — sampling commands; D-26 placement rules)
    - .planning/phases/01-differentiable-minilm-conformance/01-VALIDATION.md (the feedback-latency budget and its THREE documented exceptions — task 1-08-03 is one of them; `make tier2`/`make tier3` are expected to exceed 60s by design and that is recorded, not a violation)
  </read_first>
  <action>
    Makefile changes ONLY (do NOT touch .github/workflows/*.yml — CI workflow edits require prior
    user approval per CLAUDE.md; list the needed CI matrix legs — `--features
    setfit,conformance-fixtures` test legs — in the SUMMARY as the user-approval-gated follow-up
    PR accepted 2026-08-07 for D-24 enforcement in CI):

    (1) tier2: add `cargo test -p aprender-core --lib --features setfit` and
    `cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance`
    (fixture-parity + gradient + frozen + detach-negative per D-26). Measure runtime; if the
    conformance suite pushes tier2 past its <5s budget, keep detach_negative + gradient_gate +
    frozen_gate in tier2 and move forward_parity to tier3, documenting the split in a Makefile
    comment citing the 2026-08-07 acceptance. This tier2->tier3 fallback was PRE-ACCEPTED and is
    expected to trigger (suite est. 10-30s) — invoking it is the sanctioned path, not a deviation;
    record the measured runtime and the placement chosen in the SUMMARY. Each recipe line passes at
    most ONE positional test filter (a second exits with `error: unexpected argument`, verified
    2026-08-08) — use separate lines or a shared test-name prefix.

    (2) **tier3 must actually invoke pv (D-26).** The checker verified the current state: the `tier3`
    recipe (:201-221) contains no `pv` invocation at all, and `contract-validate` (:845) — which
    iterates the list-based `CONTRACTS` variable (:803) — is reachable only from `contract-check`
    (:876), which no tier depends on. Appending the new contract to `CONTRACTS` and stopping would
    therefore leave the phase gate in a target outside the tiers, which D-26 explicitly rejects
    ("a target outside the tiers is a target that stops being run"). Do BOTH:
      (a) append `contracts/setfit-encoder-conformance-v1.yaml` to the `CONTRACTS` list, and
      (b) add a line to the **`tier3` recipe itself** that runs it.

      **Choose the form with evidence, not preference (W4).** BEFORE editing tier3, run
      `make contract-validate > /tmp/cv.log 2>&1; rc=$?` (status captured directly, never through a
      pipe — CLAUDE.md rule 1) and record BOTH `rc` and the wall clock in the SUMMARY. Then:
        - If `rc == 0` AND the measured time keeps tier3 inside its 1-5 minute budget, use the
          preferred broad form `@$(MAKE) contract-validate`, which keeps the phase contract alongside
          the other contract gates — what D-26 asks for.
        - If `rc != 0` — i.e. one or more of the ~45 pre-existing contracts is ALREADY red — do NOT
          wire the broad form. Wiring it would make `make tier3` fail for reasons this phase did not
          cause, and this phase would be blamed for a pre-existing defect. Use the narrow form
          `@$(PV_BIN) validate contracts/setfit-encoder-conformance-v1.yaml` instead, and record in
          the SUMMARY which contracts were already red (from /tmp/cv.log) as a surfaced pre-existing
          finding for a separate ticket.
        - If `rc == 0` but the broad form blows the tier3 budget (`PV_BIN` is `cargo run --release`,
          so the first invocation pays a contracts-CLI build and the loop then validates ~45
          contracts — the Wave 0 `pv` pre-build removes the build cost but not the loop), fall back to
          the narrow form and record the measured times.
      Record which form was chosen, why, and the measured tier3 runtime in the SUMMARY. Either form
      is acceptable; leaving the validation reachable only from `contract-check` is NOT.

    (3) New target `setfit-feature-matrix` wired into tier3: no-default-features check, setfit
    check, all-features check, plus `! cargo tree -p aprender-core --no-default-features -e normal | grep -q tokenizers`.

    (4) Scoped mutation testing with an EXPLICIT budget (D-25). Run:
    `cargo mutants --no-times --timeout 300 --in-place -f 'crates/aprender-core/src/autograd/ops/*.rs' -f 'crates/aprender-core/src/setfit/encoder.rs' -f 'crates/aprender-core/src/nn/transformer/positional_encoding.rs' -- --features setfit,conformance-fixtures`
    (positional_encoding.rs is included because 01-09's repaired `add_mask` is now a graph-recording
    site — exactly the class D-25 exists to hunt). Declare the budget BEFORE running: a wall-clock
    cap and a mutant cap, both recorded in the SUMMARY. If the run exceeds the budget, narrow the
    file globs (drop the broadest one first) rather than lowering `--timeout`, and record what was
    excluded and why. Every MISSED mutant inside a graph-recording block (`set_grad_fn` /
    `with_graph` / `record` / the `scores.add(&expanded)` line) gets a new test in the appropriate
    suite before this task completes; other survivors are documented with rationale. Note that
    `setfit/encoder.rs` now contains the shared `forward_layers` both public forward entry points
    call — a mutant surviving there would be evidence the per-layer gate is not actually exercising
    the production path. Record the caught/missed counts from mutants.out — cite the numbers, not
    intent (CLAUDE.md verification discipline).

    (5) Run `bash scripts/check_include_files.sh` and `bash scripts/check_package_includes.sh`
    (CB-510 — fixtures and new include!/`#[path]` test files must be tracked and packaged correctly).

    (6) **Record one successful D-10 full-weight run before the phase is declared complete.** Run
    `uv run scripts/setfit_fixtures/fetch_full_weights.py` then
    `cargo test -p aprender-core --features setfit,conformance-fixtures,model-tests --test setfit_conformance -- --ignored`.
    Paste into the SUMMARY: the revision, the `full_manifest.json` APR sha256, the test names that
    ran, and the pass/fail result. The slice does not prove full-model behavior, so the phase must
    not be declared complete on slice evidence alone — but this run stays OUT of the default gate
    (D-10 / SAFE-02). If the machine cannot fetch the checkpoint, say so explicitly in the SUMMARY
    as an unmet phase-completion item rather than omitting it.
  </action>
  <verify>
    <automated>make tier2 && make setfit-feature-matrix && sed -n '/^tier3:/,/Tier 3: PASSED/p' Makefile | grep -q -e 'contract-validate' -e 'PV_BIN'</automated>
  </verify>
  <acceptance_criteria>
    - Makefile tier2 recipe contains a setfit_conformance invocation (or the pre-accepted tier2/tier3 split with detach_negative + gradient_gate + frozen_gate in tier2, documented in a Makefile comment citing the 2026-08-07 acceptance)
    - No Makefile recipe line passes two positional cargo test filters
    - Measured conformance-suite runtime and chosen tier placement recorded in SUMMARY
    - `make setfit-feature-matrix` exists, exits 0, and includes the negative tokenizers-leak check
    - **`make contract-validate` was run STANDALONE first**, its exit status captured directly (`rc=$?`, not through a pipe) and recorded in the SUMMARY along with its wall clock; the tier3 wiring form was chosen from that evidence per the rule in the action, and any already-red pre-existing contracts are named in the SUMMARY as a surfaced finding rather than absorbed into this phase
    - The `tier3` recipe in the Makefile itself contains a pv invocation (`$(MAKE) contract-validate` or a direct `$(PV_BIN) validate` line) — `sed -n '/^tier3:/,/Tier 3: PASSED/p' Makefile | grep -q -e 'contract-validate' -e 'PV_BIN'` exits 0. Appending to `CONTRACTS` alone does NOT satisfy this criterion, because `contract-validate` is reachable only from `contract-check`, which no tier depends on
    - `make tier3` was RUN, and its output shows `contracts/setfit-encoder-conformance-v1.yaml` being validated; the measured tier3 runtime and the chosen wiring form are recorded in the SUMMARY. Per CLAUDE.md verification rule 1, capture the recipe's status directly (`make tier3 > /tmp/t3.log 2>&1; rc=$?`), never through a pipe
    - Mutation budget declared in the SUMMARY BEFORE the run; mutants run executed within it; zero MISSED mutants in graph-recording blocks of the scoped files (including positional_encoding.rs `add_mask` and setfit/encoder.rs `forward_layers`); caught/missed counts cited from mutants.out
    - Both CB-510 scripts exit 0
    - No file under .github/workflows/ modified (git diff assertion); required CI matrix legs listed in SUMMARY as the approval-gated follow-up
    - One D-10 full-weight run recorded in the SUMMARY with revision + APR sha256 + result, or explicitly listed as unmet with the reason
  </acceptance_criteria>
  <done>Phase gate lives in the tiers per the sanctioned D-26 placement, wired in a form chosen from measured evidence rather than assumption; detachment hunted by machine within a declared budget; feature matrix enforced; full-weight evidence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture files | committed JSON/APR consumed by tests — manifest self-check guards local tampering |
| gate integrity | the gate itself is the security control for PF-001 |
| generated tolerance constants | a derived artifact that could drift from its contract source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07 | Repudiation | tolerance drift / re-baselining | mitigate | tolerances originate only in the versioned contract; the generated constants file is derived and marked DO-NOT-HAND-EDIT; the agreement test (contract equality + sha256) fails the build on any divergence in a workspace checkout, and has been demonstrated to turn red; manifest self-check test recomputes fixture SHA-256 |
| T-1-12 | Repudiation | gradient gate theater | mitigate | in-band detach-negative (D-24, sanctioned feature-enabled scope) sharing the IDENTICAL helper as the positive gate, with an explicitly `requires_grad_(true)` leaf so it cannot pass for the wrong reason; bounded cargo-mutants (D-25) over ops + encoder (incl. the shared `forward_layers`) + the repaired mask with zero missed graph-recording mutants; fixture-hyperparam agreement assertion |
| T-1-16 | Repudiation | unsatisfiable or vacuous gate | mitigate | ENC-04 restated as finite + non-zero aggregate + two-sided exemptions so it neither fails on correct code nor skips silently; the frozen proof asserts at least one trainable tensor changed so it cannot pass by doing nothing |
| T-1-24 | Repudiation | conflated all-trainable / frozen evidence | mitigate | two separate clean models in two separate files; frozen_gate.rs is forbidden from comparing against the all-trainable post-step fixture; the `source` pointer assertion proves each gate ran on the batch its fixture was recorded from |
| T-1-27 | Repudiation | gate passes while bypassing the tokenizer boundary | mitigate | ONE `batch_from_case` helper calling `SetFitMiniLm::tokenize(case.texts)`; source assertions forbid `SentenceBatch` literals and forbid feeding `input_ids_slice`; a triple-equality join check ties fixture ids, the corpus of record, and the Rust tokenizer's output together, so the slice remap is provably exercised inside the encoder |
| T-1-30 | Repudiation | per-layer gate compares against a path production never runs | mitigate | per-layer values come from 01-06's `forward_tokens_per_layer`, which shares one `forward_layers` implementation with `forward_tokens` and is bitwise-asserted equal to it there; this plan adds no forward path of its own and modifies no file under src/ |
| T-1-15 | DoS | tier2 budget blowout | mitigate | pre-accepted tier2/tier3 split rule (user, 2026-08-07) keeps pre-commit fast while gates stay in-tier (D-26); mutation run bounded by a declared budget |
| T-1-31 | Repudiation | phase blamed for pre-existing red contracts | mitigate | `make contract-validate` is run standalone BEFORE wiring, with its status recorded; a pre-existing failure selects the narrow single-contract tier3 form and is surfaced as its own finding instead of turning this phase's tier3 red |
| T-1-SC | Tampering | third-party installs | accept | no new production packages; `sha2` as a dev-dependency and the IN-TREE `aprender-contracts` crate as the contract parser (no new third-party YAML parser — a bespoke serde_yaml re-parse was rejected as muda per CLAUDE.md and would have been a second, drift-prone reader of a schema the workspace owns) |
| T-1-26 | Repudiation | phase gate outside the tiers | mitigate | pv invocation added to the tier3 RECIPE, not just to the CONTRACTS list; `make tier3` is run and its output cited, closing the D-26 failure mode where a gate exists but is never reached |
</threat_model>

<verification>
- `cargo test -p aprender-core --features setfit,conformance-fixtures --test setfit_conformance` fully green (no positional filter = whole suite; when filtering, one filter per invocation — verified 2026-08-08)
- Every batch traced to `SetFitMiniLm::tokenize`: `grep -rn 'SentenceBatch *{' crates/aprender-core/tests/setfit_conformance/` empty; `.tokenize(` appears only in `batch_from_case`
- Per-layer parity sourced from `encoder().forward_tokens_per_layer(..)`; `git diff --stat crates/aprender-core/src/` shows no change from this plan
- `make tier2` green within budget policy (pre-accepted fallback documented if triggered); `make setfit-feature-matrix` green
- `make contract-validate` run standalone first, status + wall clock recorded, wiring form chosen from that evidence
- `make tier3` was run and its output shows `contracts/setfit-encoder-conformance-v1.yaml` validated by pv; the tier3 recipe itself contains the invocation (grep-asserted), and the status was captured directly, not through a pipe (CLAUDE.md rule 1)
- The tolerance-agreement test was demonstrated to FAIL under a deliberate one-constant edit, and the demonstration is recorded
- gradient_gate.rs configures AdamW explicitly from optimizer_step.json (builder chain present; agreement assertion green)
- mutants.out: 0 missed in graph-recording blocks of scoped files; counts cited in SUMMARY; budget declared beforehand
- D-10: `cargo test -p aprender-core --features setfit,conformance-fixtures,model-tests --test setfit_conformance -- --ignored` executed once, with revision + APR sha256 + result recorded (or explicitly listed as unmet)
</verification>

<success_criteria>
- ROADMAP success criteria 1-5 all have executable, in-tier proofs:
  parity within contract-derived tolerances (2), mode/param invariance (3, via 01-06 tests in tier2),
  gradient/step gate on an all-trainable model plus a separate frozen/byte-identity proof and the
  loss-direction check, with full fixture hyperparams incl. decoupled weight_decay 0.01 (4),
  loss connectivity + detach-failure (5), typed import rejection (1, via 01-05 tests in tier2)
- Per-layer localization (success criterion 2) is proven against real production intermediates
  obtained through the sealed public surface — no harness-local forward path, no edit to src/
- Every model input in the harness passes through the real tokenizer, so the slice remap is
  exercised where production exercises it
- D-24 (as narrowed per user acceptance 2026-08-07: feature-enabled runs + make tier2; CI legs
  approval-gated follow-up), D-25 (bounded, with the repaired mask in scope), and D-26 (with the
  accepted tier3 fallback, AND with the pv invocation living in the tier3 recipe so the contract gate
  is actually reached) discharged with cited evidence
- Tolerance constants generated by `pv` or the in-tree `aprender-contracts` parser — no bespoke YAML
  reader was introduced (CLAUDE.md dogfooding rule)
- One full-weight (D-10) execution recorded, so the phase is not declared complete on slice evidence alone
- Phase ready for /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/01-differentiable-minilm-conformance/01-08-SUMMARY.md` when done.
</output>
