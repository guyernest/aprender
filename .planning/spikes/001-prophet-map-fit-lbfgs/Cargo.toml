[package]
name = "spike-001-prophet-map-fit-lbfgs"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone: not a member of the aprender workspace, but built against it by path.
[workspace]

[dependencies]
aprender = { path = "../../../crates/aprender-core", package = "aprender-core", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
